---
phase: 01-beta
plan: "04"
type: execute
wave: 3
depends_on: ["01-02","01-03"]
files_modified: ["packages/application/src/policy.ts","packages/application/src/status.ts","apps/api/src/main.ts","apps/api/src/security.ts","tests/integration/local-auth.test.ts","apps/api/src/pairing.ts","packages/persistence/src/sessions.ts","tests/integration/pairing.test.ts","tests/unit/output-policy.test.ts"]
autonomous: true
requirements: ["SEC-01","ARCH-02","JOB-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-11：共享应用经真实HTTP提供synthetic任务，不依赖LLM或学校。"
    - "D-15：每次输出执行scope/operation/destination策略；未配对和伪造请求不披露状态。"
  artifacts:
    - path: "packages/application/src/policy.ts"
      provides: "接线受认证API与应用策略"
    - path: "apps/api/src/pairing.ts"
      provides: "实现一次性显式配对与会话撤销"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "packages/application/src/jobs.ts"
      via: "受认证HTTP路由经应用入队"
    - from: "apps/api/src/pairing.ts"
      to: "packages/persistence/src/sessions.ts"
      via: "一次性pending→只读session事务"
---

<objective>
共享应用、loopback API与显式页面配对。
Purpose: 履行D-11、D-15和SEC-01、ARCH-02、JOB-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-02-SUMMARY.md
@.planning/phases/01-beta/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-1: 接线受认证API与应用策略</name>
  <read_first>packages/application/src/policy.ts, packages/application/src/status.ts, apps/api/src/main.ts, apps/api/src/security.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/application/src/policy.ts, packages/application/src/status.ts, apps/api/src/main.ts, apps/api/src/security.ts, tests/integration/local-auth.test.ts</files>
  <behavior>先写以下可失败行为测试：实际HTTP最小enqueue/query可用；无权限请求无版本/路径泄漏；业务规则不复制进HTTP handler。</behavior>
  <action>Fastify只listen127.0.0.1所选端口，trustProxy=false，精确Host127.0.0.1:port及Origin http://127.0.0.1:port；无CORS，bodyLimit16384，错误只code/stage/nextAction。Bearer摘要匹配credential与scope/destination绑定；CLI/MCP无Origin仍必须认证，browser mutation必须精确Origin+CSRFheader。routes GET /api/status, POST /api/jobs, GET /api/jobs/:id, POST /api/jobs/:id/cancel, POST /api/control/shutdown走application/ports，Zodparse输入输出，status:read/jobs:write/control:shutdown权限独立；status会话不能mutate。API/Worker分别观测、checkedAt、无记录/错误不伪healthy。输出policy只允许当前安装synthetic scope及已登记local-ui/local-cli/model-mcp目的地；model返回去敏且不含Profilepath、真实root、token。auth每IP30/min、queue最多1000pending，满返回明确429。测试非loopback配置、Host/Origin错、跨scope、invalidJSON、任意URL、认证缺失、超限与敏感canary不外泄。 增加POST /api/control/maintenance enter|exclusive|exit仅installer权限，调用JobStore持久条件事务；普通jobs写与cancel均在事务检查maintenance gate和generation。安装自检使用独立短期OSkeyring credential_id绑定operationId/generation/jobs:selfcheck权限，不允许普通客户端自称selfcheck或修改operation字段。测试普通客户端在candidate启动维护窗口不能写/取消/冒充自检，HTTP检查不替代SQLite条件。 application/status必须读取02 StatusProjectionStore，聚合Worker独立心跳、自身identity、06测得CLI/MCP probes、09写入install journal的去敏projection；返回各checkedAt/未知或stale，而非UI注入状态。POST /api/control/status-projection仅installer scope允许当前operation写自检测量，普通客户端不能伪造pass。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/local-auth.test.ts</automated></verify>
  <acceptance_criteria>实际HTTP最小enqueue/query可用；无权限请求无版本/路径泄漏；业务规则不复制进HTTP handler。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>实际HTTP最小enqueue/query可用；无权限请求无版本/路径泄漏；业务规则不复制进HTTP handler。</done>
</task>

<task type="auto" tdd="true">
  <name>04-2: 实现一次性显式配对与会话撤销</name>
  <read_first>apps/api/src/pairing.ts, packages/persistence/src/sessions.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>apps/api/src/pairing.ts, packages/persistence/src/sessions.ts, tests/integration/pairing.test.ts, tests/unit/output-policy.test.ts</files>
  <behavior>先写以下可失败行为测试：人类核对是唯一approve入口；重放/跨源/撤销测试通过，正常paired会话仅读状态。</behavior>
  <action>公开GET /status只有通用壳；GET /api/pairing/nonce仅精确同源可读，POST /api/pairing/pending要求nonce+Origin，设置HttpOnly SameSite=Strict无Domain待配对cookie，返回不具认证效力的关联码；5分钟TTL/每安装最多5pending/每分钟5次。已认证CLI POST /api/pairing/:code/approve需用户主动核对码，不自动首个请求。页面同源POST /api/pairing/exchange单次消费交换成全新只读会话，8小时过期、服务重启撤销；cookie Path=/，不谎称loopbackHTTP有TLS/Secure属性跨端口隔离。撤销/过期/重放/码穷举/CSRF/固定会话攻击须失败，关联码单独不能登录。CSP default-src 'self'; frame-ancestors 'none'; object-src 'none'，no-store/nosniff；不使用URLtoken/localStorage。所有响应经策略和redaction。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/pairing.test.ts && node scripts/dev/runtime.mjs npm run test:unit -- --run tests/unit/output-policy.test.ts</automated></verify>
  <acceptance_criteria>人类核对是唯一approve入口；重放/跨源/撤销测试通过，正常paired会话仅读状态。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>人类核对是唯一approve入口；重放/跨源/撤销测试通过，正常paired会话仅读状态。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-06 | S/I/E | critical | 浏览器/CLI到HTTP | mitigate | loopback Host/Origin/身份scope目的地、严格响应 | tests/integration/local-auth.test.ts |
| T-01-07 | S/T/D | high | 配对及会话 | mitigate | 关联码非secret、CLI显式批准、TTL/限流/CSRF单次exchange | tests/integration/pairing.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-11：共享应用经真实HTTP提供synthetic任务，不依赖LLM或学校。
D-15：每次输出执行scope/operation/destination策略；未配对和伪造请求不披露状态。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-04-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
