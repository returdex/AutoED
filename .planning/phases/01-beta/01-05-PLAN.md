---
phase: 01-beta
plan: "05"
type: execute
wave: 4
depends_on: ["01-04"]
files_modified: ["apps/worker/src/main.ts","packages/test-support/src/synthetic-provider.ts","tests/integration/worker.test.ts","packages/platform/src/processes.ts","apps/cli/src/lifecycle.ts","tests/integration/process-lifecycle.test.ts","tests/native/process-ownership.test.ts"]
autonomous: true
requirements: ["ARCH-01","JOB-01","DIST-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-01：按需启动API/Worker，关闭CLI/Codex不依赖父进程且不配置登录自启动。"
    - "D-02：启动后真实Worker执行synthetic自检，API和Worker失败单独可诊断。"
    - "D-07：不以PID或过期租约推断所有权，未知进程停止操作而非误杀。"
  artifacts:
    - path: "apps/worker/src/main.ts"
      provides: "Worker入口、心跳和确定性synthetic功能"
    - path: "packages/platform/src/processes.ts"
      provides: "受管进程监督与独立生命周期"
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "packages/application/src/job-runner.ts"
      via: "独立Worker真实claim/run/commit"
    - from: "apps/cli/src/lifecycle.ts"
      to: "packages/platform/src/processes.ts"
      via: "受管Node detached start/owned stop"
---

<objective>
独立进程与Worker执行闭环。
Purpose: 履行D-01、D-02、D-07、D-11和ARCH-01、JOB-01、DIST-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>05-1: Worker入口、心跳和确定性synthetic功能</name>
  <read_first>apps/worker/src/main.ts, packages/test-support/src/synthetic-provider.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-01, D-02, D-07, D-11；01-PLAN-INDEX接口表。</read_first>
  <files>apps/worker/src/main.ts, packages/test-support/src/synthetic-provider.ts, tests/integration/worker.test.ts</files>
  <behavior>先写以下可失败行为测试：真实Worker从DB领取并提交，A/B有可观测行为差异而非字符串不同；停止与失联可区分。</behavior>
  <action>Worker独立受管Node进程构造application.JobRunner+SQLite；写自己的编译身份/进程创建身份/5s心跳，旧心跳标stale不healthy。只运行固定synthetic echo及digest，字符串数据不作为指令或URL。A构建仅echo原值；B构建额外digest返回Node crypto SHA256输入UTF8，独立计算测试已知向量，A对digest报UNSUPPORTED_CAPABILITY。记录capabilities来自编译执行分支；无网络、学校provider、LLMkey。SIGTERM/受认证shutdown停止领取、完成或取消受控步骤后退出；超时保留可恢复任务/fence。 Worker遵循02持久maintenance谓词：quiescing不领取普通新任务，exclusive只执行当前operation绑定自检，外部普通job即使直接写入也不能claim/commit；测试窗口中迟到旧代际完成被拒。 Worker真实心跳经02 StatusProjectionStore.writeComponent，role/build由本进程编译信息产生，API status读取同表。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/worker.test.ts</automated></verify>
  <acceptance_criteria>真实Worker从DB领取并提交，A/B有可观测行为差异而非字符串不同；停止与失联可区分。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>真实Worker从DB领取并提交，A/B有可观测行为差异而非字符串不同；停止与失联可区分。</done>
</task>

<task type="auto" tdd="true">
  <name>05-2: 受管进程监督与独立生命周期</name>
  <read_first>packages/platform/src/processes.ts, apps/cli/src/lifecycle.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-01, D-02, D-07, D-11；01-PLAN-INDEX接口表。</read_first>
  <files>packages/platform/src/processes.ts, apps/cli/src/lifecycle.ts, tests/integration/process-lifecycle.test.ts, tests/native/process-ownership.test.ts</files>
  <behavior>先写以下可失败行为测试：I子进程退出证明独立，N需逐OS；不安装系统自启动；安全stop不误伤无关process。</behavior>
  <action>ProcessSupervisor使用绝对managedNode+entrypoint、detached/unref/noIPC、stdio受保护去敏日志或ignore、shell=false/windowsHide；禁止launchctl登录项、TaskScheduler/RunKey。记录installId/role/buildId/PID/启动nonce/OS创建时间+executablecanonicalpath；inspect以认证挑战及OS身份交叉核对，关闭优先HTTPcontrol且Worker受控退出，再允许经全部所有权检查kill；PID复用/未知路径/未知nonce拒绝。实现lifecycle start/stop/diagnose供CLI，启动重复请求复用同安装同身份进程；端口占用停下重新预览。测试父进程退出API/Worker仍存活、分别杀API/Worker健康差异、未知进程与旧PID不得触及；真正Codex退出N留14。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/process-lifecycle.test.ts && node scripts/dev/runtime.mjs npm run test:native -- --run tests/native/process-ownership.test.ts</automated></verify>
  <acceptance_criteria>I子进程退出证明独立，N需逐OS；不安装系统自启动；安全stop不误伤无关process。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>I子进程退出证明独立，N需逐OS；不安装系统自启动；安全stop不误伤无关process。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-08 | T/E/D | critical | 监督进程到OS | mitigate | 多因子所有权检查、认证shutdown、detached无autostart | tests/native/process-ownership.test.ts |
| T-01-09 | T/R | high | Worker执行到结果 | mitigate | 编译能力/真实运算/fence，A/B行为测试 | tests/integration/worker.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-01：按需启动API/Worker，关闭CLI/Codex不依赖父进程且不配置登录自启动。
D-02：启动后真实Worker执行synthetic自检，API和Worker失败单独可诊断。
D-07：不以PID或过期租约推断所有权，未知进程停止操作而非误杀。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-05-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
