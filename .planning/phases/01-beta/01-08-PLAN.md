---
phase: 01-beta
plan: "08"
type: execute
wave: 6
depends_on: ["01-03","01-06","01-07"]
files_modified: ["packages/installer/src/verify-manifest.ts","scripts/build/synthetic-sign.mjs","tests/integration/manifest-verification.test.ts","scripts/install/bootstrap.sh","scripts/install/bootstrap.ps1","packages/installer/src/download.ts","tests/integration/bootstrap.test.ts","packages/installer/src/preview.ts","packages/installer/src/install.ts","packages/installer/src/launchers.ts","tests/integration/install-preview.test.ts","docs/INSTALL.md"]
autonomous: true
requirements: ["DIST-02","PLAT-01","SEC-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-04/D-05：安装前预览目标/根/依赖/影响，一次确认绑定准确范围；改变范围或OS授权暂停。"
    - "D-09/D-10：无Node干净系统仍有本机bootstrap；OS/CPU不匹配在变更前拒绝。"
    - "D-11/D-12：只安装隔离的AutoED-Rebuild，不改全局Node/PATH、不卸载旧产品或删档案。"
  artifacts:
    - path: "packages/installer/src/verify-manifest.ts"
      provides: "建立安装验签实现及合成签名测试支持"
    - path: "scripts/install/bootstrap.sh"
      provides: "无Node bootstrap、受管下载与信任检查"
    - path: "packages/installer/src/preview.ts"
      provides: "预览、确认绑定与稳定启动入口"
  key_links:
    - from: "scripts/install/bootstrap.ps1"
      to: "packages/installer/src/install.ts"
      via: "验Node/签名/hash后执行受管installer"
    - from: "packages/installer/src/launchers.ts"
      to: "active.json"
      via: "核验安装身份后稳定入口选择immutable build"
---

<objective>
无需手装Node的受管安装与范围确认。
Purpose: 履行D-04、D-05、D-09、D-10、D-11、D-12和DIST-02、PLAT-01、SEC-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-03-SUMMARY.md
@.planning/phases/01-beta/01-06-SUMMARY.md
@.planning/phases/01-beta/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>08-1: 建立安装验签实现及合成签名测试支持</name>
  <read_first>packages/installer/src/verify-manifest.ts, scripts/build/synthetic-sign.mjs（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-04, D-05, D-09, D-10, D-11, D-12；01-PLAN-INDEX接口表。</read_first>
  <files>packages/installer/src/verify-manifest.ts, scripts/build/synthetic-sign.mjs, tests/integration/manifest-verification.test.ts</files>
  <behavior>先写以下可失败行为测试：真实验签/hash/target坏例拒绝，10前已可签验合成A/B；真实key与发布仍不发生。</behavior>
  <action>实现供bootstrap/installer统一使用的manifest严格schema、Ed25519精确bytes验签、target/version/hash/size/compatibility校验，publickey来自编译时信任根/批准root，不接受下载响应自荐key。synthetic-sign使用node:crypto生成临时仅测试key，签测试A/B精确manifest字节，临时根权限保护且结束销毁；只在测试入口输出fixturepublickey不输出privatekey，生产bootstrap不可接受任意--trust-key或skip-verify。08 bootstrap测试和10局部A/B产物用同一真实verifier+显式注入fixturekey，不依赖11发布工具、12实际密钥或远程，因此无依赖循环。11复用该schema/verifier实现release签名，不能复制弱化规则。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/manifest-verification.test.ts</automated></verify>
  <acceptance_criteria>真实验签/hash/target坏例拒绝，10前已可签验合成A/B；真实key与发布仍不发生。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>真实验签/hash/target坏例拒绝，10前已可签验合成A/B；真实key与发布仍不发生。</done>
</task>

<task type="auto" tdd="true">
  <name>08-2: 无Node bootstrap、受管下载与信任检查</name>
  <read_first>scripts/install/bootstrap.sh, scripts/install/bootstrap.ps1, packages/installer/src/download.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-04, D-05, D-09, D-10, D-11, D-12；01-PLAN-INDEX接口表。</read_first>
  <files>scripts/install/bootstrap.sh, scripts/install/bootstrap.ps1, packages/installer/src/download.ts, tests/integration/bootstrap.test.ts</files>
  <behavior>先写以下可失败行为测试：测试no-Node环境命令路径、篡改manifest/Node/archive/traversal拒绝及不碰legacy；真实Windows运行留14。</behavior>
  <action>mac系统shell/curl/shasum/tar与Windows内置PowerShell下载/Get-FileHash/Expand-Archive启动；先检测OS/arch与localroot，不依赖npm/Node/admin，不执行curl|sh或永久ExecutionPolicy变更。完整提示词先下载bootstrap到文件核对用户批准的SHA256再运行；bootstrap按固定URL+官方独立签名校验的Nodehash下载Node24.20.0到私有staging，验后解包执行受管Node验证Ed25519发行manifest及全部产物hash，再运行已验installer。所有URL仅批准官方Node/CDN/returdexrelease域，redirect每跳复核，无私网跳转，最大下载体积按manifest和可用磁盘预检。解包拒绝绝对路径、..、symlink/junction越界、重复覆盖，文件数/总大小≤manifest；key更换/降版/签名或hash错硬停。安装器元文件bootstrap/publickey信任由11/12建立，不能从同一未可信URL自行接受新key。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/bootstrap.test.ts</automated></verify>
  <acceptance_criteria>测试no-Node环境命令路径、篡改manifest/Node/archive/traversal拒绝及不碰legacy；真实Windows运行留14。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>测试no-Node环境命令路径、篡改manifest/Node/archive/traversal拒绝及不碰legacy；真实Windows运行留14。</done>
</task>

<task type="auto" tdd="true">
  <name>08-3: 预览、确认绑定与稳定启动入口</name>
  <read_first>packages/installer/src/preview.ts, packages/installer/src/install.ts, packages/installer/src/launchers.ts, docs/INSTALL.md（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-04, D-05, D-09, D-10, D-11, D-12；01-PLAN-INDEX接口表。</read_first>
  <files>packages/installer/src/preview.ts, packages/installer/src/install.ts, packages/installer/src/launchers.ts, tests/integration/install-preview.test.ts, docs/INSTALL.md</files>
  <behavior>先写以下可失败行为测试：重复相同确认幂等；scope改变要求新确认；入口独立可定位，不默认改宿主配置/系统自启动。</behavior>
  <action>preview结构含当前/目标身份、OS支持、root别名、Node/browser版本及字节、受管program/process/entrypoints影响、停机/保留数据、autostart=false、recoverability；hash规范化scope并要求确认token绑定installId+targethash+paths+权限。拒绝确认零受管变更（允许用户批准bootstrap的隔离temp下载但不安装）。新增下载/路径/权限变化回preview，不默认同意。安装分区为program/<build>/runtime/<version>/browser/<revision>/data/installer-staging，稳定bin/autoed-rebuild(.cmd)及独立MCP名autoed-rebuild-m1，不覆写已有同名未知入口。launcher读取原子active记录并验installId/hash后用绝对Node执行目标，不上全局PATH；输出精确绝对命令/单独MCP注册预览，经用户scope确认才写指定宿主配置。INSTALL记录mac/Win完整可复制安装升级提示词、无需手装依赖、无学校访问、只读旧目录、安全信任起点/OS警告不关闭防护；最终hash/publickey由发布生成而非编造。接01-07presentation显示进度不伪百分比。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/install-preview.test.ts</automated></verify>
  <acceptance_criteria>重复相同确认幂等；scope改变要求新确认；入口独立可定位，不默认改宿主配置/系统自启动。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>重复相同确认幂等；scope改变要求新确认；入口独立可定位，不默认改宿主配置/系统自启动。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-12 | T/E | critical | 公网artifact到执行/解包 | mitigate | 固定信任根+签名hash+路径/大小/跳转约束 | tests/integration/bootstrap.test.ts |
| T-01-13 | E/R | high | 安装批准到本机变更 | mitigate | scopehash确认、权限变化暂停、独立入口 | tests/integration/install-preview.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-04/D-05：安装前预览目标/根/依赖/影响，一次确认绑定准确范围；改变范围或OS授权暂停。
D-09/D-10：无Node干净系统仍有本机bootstrap；OS/CPU不匹配在变更前拒绝。
D-11/D-12：只安装隔离的AutoED-Rebuild，不改全局Node/PATH、不卸载旧产品或删档案。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-08-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
