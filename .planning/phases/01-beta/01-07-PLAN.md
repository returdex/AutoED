---
phase: 01-beta
plan: "07"
type: execute
wave: 4
depends_on: ["01-04"]
files_modified: ["apps/api/src/static.ts","apps/api/src/main.ts","scripts/build/build.mjs","tests/integration/static-assets.test.ts","apps/status/index.html","apps/status/src/main.ts","apps/status/styles.css","tests/ui/status.spec.ts","packages/contracts/src/presentation.ts","tests/unit/presentation.test.ts","tests/ui/accessibility.spec.ts"]
autonomous: true
requirements: ["DIST-01","SEC-01","ARCH-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-02/D-03：页面分别显示API/Worker、版本差异与安装清理结果，不虚报完成。"
    - "D-15：页面权限失效清空保护数据，网络失联仅显示带时间旧快照。"
  artifacts:
    - path: "apps/api/src/static.ts"
      provides: "注册API静态入口及浏览器构建接线"
    - path: "apps/status/index.html"
      provides: "只读状态页与完整状态映射"
    - path: "packages/contracts/src/presentation.ts"
      provides: "键盘、窄屏与统一反馈投影"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/static.ts"
      via: "注册受控静态页及资源"
    - from: "apps/status/src/main.ts"
      to: "/api/status"
      via: "same-origin认证读取/401清除"
    - from: "scripts/build/build.mjs"
      to: "apps/status/index.html"
      via: "构建复制并由真实API托管"
---

<objective>
批准的最小状态页与终端反馈文案。
Purpose: 履行D-02、D-03、D-15和DIST-01、SEC-01、ARCH-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>07-1: 注册API静态入口及浏览器构建接线</name>
  <read_first>apps/api/src/static.ts, apps/api/src/main.ts, scripts/build/build.mjs（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-02, D-03, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>apps/api/src/static.ts, apps/api/src/main.ts, scripts/build/build.mjs, tests/integration/static-assets.test.ts</files>
  <behavior>先写以下可失败行为测试：API main真实注册static，build显式产出浏览器assets，未配对不泄漏；最终UI整体在下一task运行。</behavior>
  <action>由本task拥有修改04 API main，注册白名单/status及静态资源，不暴露文件任意路径；扩展01 build将apps/status/index.html、styles.css及浏览器专用TS编译输出到dist/apps/status，浏览器bundle不得引用Node/秘密库。静态壳通用不带版本/安装信息，auth敏感数据只走/api/status；CSP/no-store/nosniff保持04。测试真实build后启动API，GET/status资源可达、路径逃逸/未知文件拒绝、未配对只通用壳；页面代码由下一task创建，task定向验证通过需该task先创建最小真实index+styles输入合同，或先以测试临时synthetic输入验证builder，不能伪造最终UI已完成。此task不创建生产UI文件，测试使用临时输入路径并在下一task真实输出回归。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/static-assets.test.ts</automated></verify>
  <acceptance_criteria>API main真实注册static，build显式产出浏览器assets，未配对不泄漏；最终UI整体在下一task运行。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>API main真实注册static，build显式产出浏览器assets，未配对不泄漏；最终UI整体在下一task运行。</done>
</task>

<task type="auto" tdd="true">
  <name>07-2: 只读状态页与完整状态映射</name>
  <read_first>apps/status/index.html, apps/status/src/main.ts, apps/status/styles.css（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-02, D-03, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>apps/status/index.html, apps/status/src/main.ts, apps/status/styles.css, tests/ui/status.spec.ts</files>
  <behavior>先写以下可失败行为测试：页面原生HTML/CSS/TS无CDN/分析/完整管理UI；错误与权限行为符合契约。</behavior>
  <action>按UI-SPEC原样采用中文copy、960px、24/16px边距、4/8/16/24/32/48/64px间距、14/16/20/28字号、400/600字重、中性色及蓝focus。白名单静态资源由API在构建输出部署；main通过authenticated /api/status拉取，textContent写不可信值。页面仅刷新/配对关联码显示，不提供启动升级/任意执行；配对需实际CLI核对且以公开壳显示。展示7区块：scope/APIWorker/五身份/job/install/详情，checkedAt与未验证，stale非健康；401/403立即清空旧snapshot，断网保留stale，Worker停止独立文案。初始服务不存在不伪造离线托管；CLI负责。UI tests真实本地API+synthetic测试注入加载/空/failed/mismatch/cleanup_pending/rollback/no-old/human_needed/401/network，每种断言准确文案及刷新不产生job。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:ui -- tests/ui/status.spec.ts</automated></verify>
  <acceptance_criteria>页面原生HTML/CSS/TS无CDN/分析/完整管理UI；错误与权限行为符合契约。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>页面原生HTML/CSS/TS无CDN/分析/完整管理UI；错误与权限行为符合契约。</done>
</task>

<task type="auto" tdd="true">
  <name>07-3: 键盘、窄屏与统一反馈投影</name>
  <read_first>packages/contracts/src/presentation.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-02, D-03, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/contracts/src/presentation.ts, tests/unit/presentation.test.ts, tests/ui/accessibility.spec.ts</files>
  <behavior>先写以下可失败行为测试：所有完成/失败文案有逻辑测试；自动布局键盘测试有真实DOM，不仅检查CSS文本。</behavior>
  <action>集中纯函数把install阶段/结果映射UI-SPEC终端与网页copy，供08/09使用；complete仅target-start+feature-verified+cleanupdone，rollback显示升级失败，首次失败无旧版，unknown不pass。保留结构化code/stage/impact/nextAction且不dumpstack/path。UI测试键盘focus、details语义、刷新不抢焦点/重复播报、aria-live polite、320px及200%缩放长中文/版本可读，禁止颜色唯一信号；不保存敏感截图。人工实际浏览器N在14，自动UI不替代人类反馈。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:unit -- --run tests/unit/presentation.test.ts && node scripts/dev/runtime.mjs npm run test:ui -- tests/ui/accessibility.spec.ts</automated></verify>
  <acceptance_criteria>所有完成/失败文案有逻辑测试；自动布局键盘测试有真实DOM，不仅检查CSS文本。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>所有完成/失败文案有逻辑测试；自动布局键盘测试有真实DOM，不仅检查CSS文本。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-11 | I/T | high | 状态UI输出与授权失效 | mitigate | textContent/CSP/401清空，stale单独呈现 | tests/ui/status.spec.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-02/D-03：页面分别显示API/Worker、版本差异与安装清理结果，不虚报完成。
D-15：页面权限失效清空保护数据，网络失联仅显示带时间旧快照。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-07-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
