---
phase: 01-beta
plan: "12"
type: execute
wave: 10
depends_on: ["01-11"]
files_modified: ["release/approval.json","release/trust-root.json","release/prerequisites.json"]
autonomous: false
requirements: ["DIST-03","SEC-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-05/D-16：新增密钥保管和首次信任由用户明确确认，OS授权/OAuth绝不模拟。"
    - "D-14：发布用returdex独立身份；不因本地commit正确就假定remote正确，也不全局切账号。"
  artifacts:
    - path: "release/approval.json"
      provides: "确认本机发布密钥及首次信任作用域"
    - path: "release/trust-root.json"
      provides: "本机授权、公开指纹复核与隔离returdex认证"
  key_links:
    - from: "scripts/release/trust.mjs"
      to: "release/approval.json"
      via: "显式scope批准后OSkeyring初始化"
    - from: "scripts/release/preflight.mjs"
      to: "release/prerequisites.json"
      via: "独立实际认证身份核验"
---

<objective>
发布密钥与远程身份的人工作用域门禁。
Purpose: 履行D-05、D-14、D-16和DIST-03、SEC-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-11-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>12-1: 确认本机发布密钥及首次信任作用域</name>
  <read_first>scripts/release/trust.mjs, release/trust-policy.json, docs/RELEASE.md（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-05, D-14, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>release/approval.json</files>
  <action>硬停止，向用户说明提案：创建单一本机Ed25519发布私钥，仅OSkeyring service AutoED-Rebuild-Release内保存；公开公钥/指纹用于完整安装提示词固定信任，不上传私钥，不加入云CI，无付费证书；这不是Apple/Windows代码签名。公开returdex/AutoED仅在13确认无冲突后创建。用户可拒绝，拒绝停发布不减要求、不偷偷改checksum-only。收到真实明确许可后，将scope/日期/许可文字摘要记录approval.json（无秘密）；对该文件自动校验只核scope，不将文件存在当人工批准。</action>
  <how-to-verify>阅读拟议密钥保管、首次信任和公开仓库范围；用户明确同意后才执行下一任务。此为新增权限前置批准，不是产品UAT，因此不受先发布beta顺序阻碍。</how-to-verify>
  <resume-signal>用户明确批准本机密钥保管与首次信任；不同意或需改动则停止并修订计划。</resume-signal>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/trust.mjs check-approval --receipt release/approval.json</automated></verify>
  <acceptance_criteria>存在可追溯真实用户批准；自动验证scope与预览一致，无实际key/remote变更。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>存在可追溯真实用户批准；自动验证scope与预览一致，无实际key/remote变更。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>12-2: 本机授权、公开指纹复核与隔离returdex认证</name>
  <read_first>release/approval.json, scripts/release/trust.mjs, scripts/release/preflight.mjs（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-05, D-14, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>release/trust-root.json, release/prerequisites.json</files>
  <action>在批准后由执行器运行trust init，工具生成私钥并直接存keyring，用户只处理系统提示；Agent不读取私钥。显示公钥fingerprint供用户确认首次信任写入trust-root.json。用现有可明确选returdex的连接器/认证先自动只读核login；若无法选择且活动为ywan1303，暂停请用户通过隔离GH_CONFIG_DIR官方gh auth login完成returdexOAuth（mac ~/Library/Application Support/AutoED-Rebuild-Release/github；Windows %LOCALAPPDATA%/AutoED-Rebuild-Release/github，原生权限保护）；该新本机目录需预览确认，绝不全局switch/读取token。无人工提示时工具自动完成，不编造人类动作。prerequisites只含公钥fingerprint/OSkey可用布尔/认证login/批准receipt，无token。当前任务不创建repo/tag，不上传。</action>
  <how-to-verify>用户处理OS权限或官方OAuth（如触发），核对公开fingerprint和returdex用户名；勿在聊天粘贴任何密码/token/私钥。返回同意或错误后继续。</how-to-verify>
  <resume-signal>用户确认公开指纹；必要OS/OAuth完成且只读预检returdex通过，否则保持阻塞。</resume-signal>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/trust.mjs selfcheck --public release/trust-root.json && node scripts/dev/runtime.mjs node scripts/release/preflight.mjs --identity-only</automated></verify>
  <acceptance_criteria>本机私钥可签验且不外泄；实际remote login=returdex；未更改全局身份，失败不进入13。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>本机私钥可签验且不外泄；实际remote login=returdex；未更改全局身份，失败不进入13。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-20 | S/I/E | critical | 人类授权到密钥/OAuth | mitigate | 硬停止、明确scope、OSsecret本机保管、独立认证目录 | tests/integration/release-trust.test.ts; tests/integration/release-gates.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-05/D-16：新增密钥保管和首次信任由用户明确确认，OS授权/OAuth绝不模拟。
D-14：发布用returdex独立身份；不因本地commit正确就假定remote正确，也不全局切账号。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-12-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
