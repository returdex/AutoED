---
phase: 01-beta
plan: "03"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["packages/platform/src/paths.ts","packages/platform/src/permissions.ts","packages/platform/src/platform.ts","tests/native/platform-probes.test.ts","packages/platform/src/credentials.ts","packages/platform/src/installation.ts","tests/native/secret-store.test.ts","tests/unit/credential-redaction.test.ts"]
autonomous: true
requirements: ["SEC-01","PLAT-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-09/D-10：安装前检测真实OS/CPU，Windows11机器型号未知不假称已验证。"
    - "D-11/D-15：新安装标识、根和秘密均与legacy隔离，原生凭据保护失败不降级明文。"
  artifacts:
    - path: "packages/platform/src/paths.ts"
      provides: "受管根与原生权限适配器"
    - path: "packages/platform/src/credentials.ts"
      provides: "OS凭据库与安装身份初始化"
  key_links:
    - from: "packages/platform/src/installation.ts"
      to: "packages/platform/src/permissions.ts"
      via: "建立隔离根时原生权限核验"
    - from: "packages/platform/src/credentials.ts"
      to: "@napi-rs/keyring"
      via: "当前用户OSsecret store adapter"
---

<objective>
平台隔离根、原生密钥库与安装身份。
Purpose: 履行D-09、D-10、D-11、D-15和SEC-01、PLAT-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-1: 受管根与原生权限适配器</name>
  <read_first>packages/platform/src/paths.ts, packages/platform/src/permissions.ts, packages/platform/src/platform.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-09, D-10, D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/platform/src/paths.ts, packages/platform/src/permissions.ts, packages/platform/src/platform.ts, tests/native/platform-probes.test.ts</files>
  <behavior>先写以下可失败行为测试：匹配平台能创建隔离temp根并验证权限；不匹配/路径逃逸/网络盘失败；本机pass不填Windowspass。</behavior>
  <action>目标矩阵建议macOS14+arm64、Windows11 x64，实际验收分别记录具体OS/CPU（本机26.5.2arm64；Windows待实测），其他组合UNSUPPORTED_PLATFORM，不自动用Rosetta/WSL。root macOS ~/Library/Application Support/AutoED-Rebuild，Windows %LOCALAPPDATA%/AutoED-Rebuild；区分program/runtime/browser/data/secrets/installer-staging和未来Profile禁止导出区域。realpath与Windows junction/reparse校验每层，拒绝legacy同根/嵌套、同步或网络卷、受管路径外逃；无法确认local卷暂停。mac0700/0600；Windows按当前SID设DACL移除继承并核验，保留必须SYSTEM/Administrators权限但拒绝其他普通用户读取；不能用chmod替代。只读preflight输出脱敏root别名、系统/架构/权限结果，不碰未知目录。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:native -- --run tests/native/platform-probes.test.ts</automated></verify>
  <acceptance_criteria>匹配平台能创建隔离temp根并验证权限；不匹配/路径逃逸/网络盘失败；本机pass不填Windowspass。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>匹配平台能创建隔离temp根并验证权限；不匹配/路径逃逸/网络盘失败；本机pass不填Windowspass。</done>
</task>

<task type="auto" tdd="true">
  <name>03-2: OS凭据库与安装身份初始化</name>
  <read_first>packages/platform/src/credentials.ts, packages/platform/src/installation.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-09, D-10, D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/platform/src/credentials.ts, packages/platform/src/installation.ts, tests/native/secret-store.test.ts, tests/unit/credential-redaction.test.ts</files>
  <behavior>先写以下可失败行为测试：原生库拒绝不写明文；独立namespace隔离，测试仅合成秘密；人工OS授权如遇即停。</behavior>
  <action>使用@napi-rs/keyring1.3.0适配macKeychain/WindowsCredentialManager；每install_id UUID命名空间、每CLI/MCP/API服务/installer的256bit随机token，product适配器内存使用，校验记录保存摘要+scope+destination。不把secret传argv/env/log/model/文件；locked/denied/unavailable直接SECRET_STORE_UNAVAILABLE，不fallback。installation元数据仅非秘密install_id、已选端口43187（冲突重新预览，不连未知服务）、root ownership与批准范围。初建服务credential与client credential由installer产品代码写密钥库，不让Agent读取；撤销删除或轮换生效。native用临时syntheticcanary测试set/get/delete、拒绝/锁定路径；输出只布尔、错误码，不打印canary。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:native -- --run tests/native/secret-store.test.ts && node scripts/dev/runtime.mjs npm run test:unit -- --run tests/unit/credential-redaction.test.ts</automated></verify>
  <acceptance_criteria>原生库拒绝不写明文；独立namespace隔离，测试仅合成秘密；人工OS授权如遇即停。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>原生库拒绝不写明文；独立namespace隔离，测试仅合成秘密；人工OS授权如遇即停。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-04 | I/E | critical | OS秘密与文件路径 | mitigate | 原生keyring/DACL、路径所有权与无明文fallback | tests/native/secret-store.test.ts |
| T-01-05 | T/E | high | 新旧安装边界 | mitigate | canonical roots、junction拒绝、独立install_id/端口 | tests/native/platform-probes.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-09/D-10：安装前检测真实OS/CPU，Windows11机器型号未知不假称已验证。
D-11/D-15：新安装标识、根和秘密均与legacy隔离，原生凭据保护失败不降级明文。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-03-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
