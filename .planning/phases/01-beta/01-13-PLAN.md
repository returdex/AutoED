---
phase: 01-beta
plan: "13"
type: execute
wave: 11
depends_on: ["01-12"]
files_modified: ["release/beta-artifacts.json","release/install-prompts.md","release/publication.json","release/availability.json"]
autonomous: true
requirements: ["DIST-03","DIST-02","DIST-01","PLAT-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-13：用户人工更新前已有两个完整可获取beta与准确安装提示词。"
    - "D-14：只发布returdex/AutoED的全新不可覆盖beta，许可和身份真实验证。"
    - "D-15/D-16：Windows产物尚未native验收如实标注；发布后硬停等待14，不把发布当通过。"
  artifacts:
    - path: "release/beta-artifacts.json"
      provides: "构建签名产物与版本绑定安装说明"
    - path: "release/publication.json"
      provides: "正确身份创建/发布并匿名重新获取"
  key_links:
    - from: "scripts/release/publish.mjs"
      to: "returdex/AutoED"
      via: "仅核准owner/repo的不可覆盖发布"
    - from: "scripts/release/verify-availability.mjs"
      to: "release/availability.json"
      via: "匿名GET全部资产验签hash"
---

<objective>
发布两个可获取的synthetic beta。
Purpose: 履行D-13、D-14、D-15、D-16和DIST-03、DIST-02、DIST-01、PLAT-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>13-1: 构建签名产物与版本绑定安装说明</name>
  <read_first>release/beta-artifacts.json, release/install-prompts.md（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-13, D-14, D-15, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>release/beta-artifacts.json, release/install-prompts.md</files>
  <action>调用10 assemble及01 build生成A/B实际独立目录，先确定两个未用0.1.0-beta.N，默认1/2但任何占用先查来源冲突；禁止覆盖，改N要重新构建/签名/检查且更新预览。A只有echo，B有digest，各target包含预编译JS+本target完整依赖/Node/Chromium/安装脚本/标准许可；不上传任何私密数据；合成自包含diagnostics/native-report.mjs及生产依赖必须随包，不要求开发测试套件。跑全部适用S/I/UI和本机N自动检查，执行12publicroot签名，再从装配目录重验所有hash及实际mac加载，Windows仅PE/NAPI/闭包静态。生成两个版本各自mac/Win完整提示词：精确公开URL、bootstrap SHA256、官方Nodehash、公钥fingerprint、安装scope预览、调用方式、MCP独立注册、更新A→B步骤，无占位hash。记录未测Windows/OS组合not_run，包装不代表原生运行。</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/build/assemble.mjs --release-pair && node scripts/dev/runtime.mjs node scripts/release/preflight.mjs --artifacts release/beta-artifacts.json</automated></verify>
  <acceptance_criteria>两个版本×两个target四套完整产物及精确提示词可验签，行为差异测试通过，无私密文件。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>两个版本×两个target四套完整产物及精确提示词可验签，行为差异测试通过，无私密文件。</done>
</task>

<task type="auto">
  <name>13-2: 正确身份创建/发布并匿名重新获取</name>
  <read_first>release/publication.json, release/availability.json（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-13, D-14, D-15, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>release/publication.json, release/availability.json</files>
  <action>执行完整preflight，API实际returdex且git身份一致，若同名repo已存在且不是此次操作可证明ownedcreationreceipt则停止人工决定。确认无冲突后仅创建公开returdex/AutoED和本仓库remote，保留repoID receipt；不动legacyremote、不同账号/globalsettings。推送批准源码及不可覆盖beta tags，发布所有平台archive/signature/manifest/bootstrap/完整prompt/license；禁止stableclaim。匿名新请求重新GET两beta所有产物并验签/hash/size/安装依赖闭包，记录URL/version/build/platform/时间/结果；可用性失败不能给用户UAT。公开资源敏感扫描失败先阻断不上传，发现后不谎称成功。完成后只交14用户更新清单并硬停，无学校登录。 推送前preflight必须扫描确切push tree与将公开reachable历史所有blob；发现秘密/运行数据停下且不得自动改历史。</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/publish.mjs --pair release/beta-artifacts.json && node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs --pair release/beta-artifacts.json --out release/availability.json</automated></verify>
  <acceptance_criteria>匿名获取及验签两beta全产物通过，远程owner/tag/build可追溯；未得到人工反馈所以阶段仍未完成。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>匿名获取及验签两beta全产物通过，远程owner/tag/build可追溯；未得到人工反馈所以阶段仍未完成。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-21 | S/T/I/R | critical | 本地构建到公开发布 | mitigate | returdex/同名检查、签名、allowlist、公开GET验证、不可覆盖 | tests/integration/release-gates.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-13：用户人工更新前已有两个完整可获取beta与准确安装提示词。
D-14：只发布returdex/AutoED的全新不可覆盖beta，许可和身份真实验证。
D-15/D-16：Windows产物尚未native验收如实标注；发布后硬停等待14，不把发布当通过。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-13-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
