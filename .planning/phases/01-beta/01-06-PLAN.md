---
phase: 01-beta
plan: "06"
type: execute
wave: 5
depends_on: ["01-05"]
files_modified: ["packages/client/src/http.ts","apps/cli/src/main.ts","tests/integration/cli.test.ts","apps/mcp/src/main.ts","packages/application/src/identity.ts","scripts/install/selfcheck.mjs","tests/integration/client-wiring.test.ts","tests/integration/build-identity.test.ts"]
autonomous: true
requirements: ["ARCH-02","DIST-01","SEC-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-03：安装自检调用实际CLI/MCP→HTTP→Worker，新功能执行成功才可匹配。"
    - "D-11/D-15：MCP为薄客户端无DB/Profile访问，出口具目的地检查，后端关闭明确不可用。"
  artifacts:
    - path: "packages/client/src/http.ts"
      provides: "CLI与共享HTTP客户端"
    - path: "apps/mcp/src/main.ts"
      provides: "stdio MCP与跨组件真实自检"
  key_links:
    - from: "apps/mcp/src/main.ts"
      to: "packages/client/src/http.ts"
      via: "stdio工具调用真实authenticatedHTTP"
    - from: "scripts/install/selfcheck.mjs"
      to: "apps/mcp/src/main.ts"
      via: "官方SDK spawn已安装入口→Job→Worker"
---

<objective>
真实CLI/stdio MCP入口与独立身份探针。
Purpose: 履行D-03、D-11、D-15和ARCH-02、DIST-01、SEC-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>06-1: CLI与共享HTTP客户端</name>
  <read_first>packages/client/src/http.ts, apps/cli/src/main.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-03, D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/client/src/http.ts, apps/cli/src/main.ts, tests/integration/cli.test.ts</files>
  <behavior>先写以下可失败行为测试：真实CLIspawn→HTTP→job响应；拒绝wronginstall/proxyredirect；token无argv/env/stdout。</behavior>
  <action>client从明确installation元数据定位loopback、通过platformcredentialadapter内存获token，禁止跨hostredirect/环境代理；错误映射BACKEND_UNAVAILABLE/IDENTITY_MISMATCH/AUTH_REQUIRED不自动启动MCP后端。CLI命令start/stop/status/selftest --kind echo|digest --value text/jobs get|cancel/pair approve CODE/open-status；pair approve提示准确码用户确认，拒绝不变更。对model目的地默认去敏json，面向本人安装preview另用localterminal策略。open-status只开无tokenURL。CLI实际identity独立编译；调用前校验APIinstall/protocol/schema，status显示全部实际观测而不是manifest重复。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/cli.test.ts</automated></verify>
  <acceptance_criteria>真实CLIspawn→HTTP→job响应；拒绝wronginstall/proxyredirect；token无argv/env/stdout。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>真实CLIspawn→HTTP→job响应；拒绝wronginstall/proxyredirect；token无argv/env/stdout。</done>
</task>

<task type="auto" tdd="true">
  <name>06-2: stdio MCP与跨组件真实自检</name>
  <read_first>apps/mcp/src/main.ts, packages/application/src/identity.ts, scripts/install/selfcheck.mjs（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-03, D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>apps/mcp/src/main.ts, packages/application/src/identity.ts, scripts/install/selfcheck.mjs, tests/integration/client-wiring.test.ts, tests/integration/build-identity.test.ts</files>
  <behavior>先写以下可失败行为测试：从实际spawn入口完成协议→HTTP→DB→Worker闭环；健康200与版本字符串不能代替能力证据。</behavior>
  <action>官方MCP server2.0.0注册autoed_status、autoed_selftest、autoed_job_get三个严格工具，不接受URL/JS/selector/路径；protocol握手用官方SDK，不自写framing。MCP仅clienthttp+credentialadapter，不直开DB/读取Profile，stdout只JSONRPC。selfcheck用官方MCPclient2.0.0 spawn传入实际安装绝对MCPlauncher，分别收manifest/API/Worker/CLI/MCP各自build/protocol/schema/capability；经过真实selftest enqueue→Worker运算→查询，B必须digest已知输入正确、A必须明确unsupported且echo可用。失联/mismatch/未检查任一维度都不得complete。测试旧MCP入口、旧Worker、篡改manifest、同字符串不同build及API停止，sourceimportboundary继续检查MCP禁driver/fs。 安装维护窗口selfcheck使用03/04创建的operation-scoped短期keyring credential；仅把非秘密credential_id传MCP，客户端内存取token，HTTP校验operation/generation/能力，结束即撤销。真实MCP→HTTP链不能用直接回调绕过维护锁。 自检完成通过04 installer-only status-projection接口写五身份实测/featureResult/jobId，失败同样记录unknown/mismatch而不填目标值。client-wiring集成测在真实自检后GET/api/status可读同一job/probes，并确认旧入口差异可见。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/client-wiring.test.ts tests/integration/build-identity.test.ts</automated></verify>
  <acceptance_criteria>从实际spawn入口完成协议→HTTP→DB→Worker闭环；健康200与版本字符串不能代替能力证据。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>从实际spawn入口完成协议→HTTP→DB→Worker闭环；健康200与版本字符串不能代替能力证据。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-10 | S/T/I | high | stdio/CLI及版本身份 | mitigate | SDK严格工具、目的地策略、实际五组件探针/功能运算 | tests/integration/build-identity.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-03：安装自检调用实际CLI/MCP→HTTP→Worker，新功能执行成功才可匹配。
D-11/D-15：MCP为薄客户端无DB/Profile访问，出口具目的地检查，后端关闭明确不可用。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-06-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
