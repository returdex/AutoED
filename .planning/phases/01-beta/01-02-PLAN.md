---
phase: 01-beta
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["packages/persistence/src/database.ts","packages/persistence/src/jobs.ts","packages/application/src/jobs.ts","tests/integration/job-storage.test.ts","packages/persistence/src/runtime-status.ts","packages/application/src/job-runner.ts","packages/persistence/src/claims.ts","tests/integration/job-recovery.test.ts","tests/unit/job-state.test.ts"]
autonomous: true
requirements: ["JOB-01","ARCH-02"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-11：任务持久化由真实SQLite支持，不依赖Codex、LLM或内存队列。"
    - "D-15：失租/取消/重试不覆盖最后成功结果，失败与空结果可区分。"
  artifacts:
    - path: "packages/persistence/src/database.ts"
      provides: "建立schema与幂等入队查询"
    - path: "packages/application/src/job-runner.ts"
      provides: "实现领取、fence、取消与故障恢复"
  key_links:
    - from: "packages/application/src/jobs.ts"
      to: "packages/persistence/src/jobs.ts"
      via: "JobStore端口真实SQLite事务"
    - from: "packages/application/src/job-runner.ts"
      to: "packages/persistence/src/claims.ts"
      via: "claim/heartbeat/fenced commit+maintenance条件"
---

<objective>
真实SQLite与持久任务状态机。
Purpose: 履行D-11、D-15和JOB-01、ARCH-02，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-1: 建立schema与幂等入队查询</name>
  <read_first>packages/persistence/src/database.ts, packages/persistence/src/jobs.ts, packages/application/src/jobs.ts, packages/persistence/src/runtime-status.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/persistence/src/database.ts, packages/persistence/src/jobs.ts, packages/application/src/jobs.ts, tests/integration/job-storage.test.ts, packages/persistence/src/runtime-status.ts</files>
  <behavior>先写以下可失败行为测试：真实临时DB重开仍能查job；同key竞争无重复；错误不损毁成功结果；实际引擎不符拒绝。</behavior>
  <action>实现ports：SQLite WAL、foreign_keys=ON、synchronous=FULL、busy_timeout=2000；建立schema_migrations、jobs、synthetic_results、runtime_observations、maintenance_generation。jobs字段按RESEARCH：unique(scope,idempotency_key)、payload_hash、attempt/max_attempts、next_run_at、owner/until/fence、cancel_requested、checkpoint/result/error_code。enqueue同key同payload返回原job_id；不同payload409 IDEMPOTENCY_CONFLICT。独立保存last_success_result与最近失败，错误不置空历史成功。启动查询sqlite_version/sqlite_source_id，要求批准清单3.53.4对应来源，漂移禁止启动；不用系统sqlite替代。migration单向前进并记录schema兼容区间/写入代际供09使用，首次只有schema1。 maintenance_generation持久行额外存state/operation_id/owner/lease_until；所有正常enqueue在同事务要求state=open并绑定generation，exclusive返回MAINTENANCE_ACTIVE。仅已认证installer发起的operation-scoped selfcheck可写synthetic专用scope，其结果不增加用户写入代际。提供enterMaintenance/markExclusive/exitMaintenance条件事务，过期锁不自动清除或恢复写入，须installer核实事实。 实现StatusProjectionStore为独立runtime_status表：upsert各组件实际观测/最近install阶段与cleanup/selfcheck结果；列只允许契约脱敏projection，不保存secret/root/Profile。read返回各记录checkedAt；缺失not_observed、过期stale，失败保留last_success及最近error，generation/op绑定防过期投影覆盖。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/job-storage.test.ts</automated></verify>
  <acceptance_criteria>真实临时DB重开仍能查job；同key竞争无重复；错误不损毁成功结果；实际引擎不符拒绝。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>真实临时DB重开仍能查job；同key竞争无重复；错误不损毁成功结果；实际引擎不符拒绝。</done>
</task>

<task type="auto" tdd="true">
  <name>02-2: 实现领取、fence、取消与故障恢复</name>
  <read_first>packages/application/src/job-runner.ts, packages/persistence/src/claims.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-11, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>packages/application/src/job-runner.ts, packages/persistence/src/claims.ts, tests/integration/job-recovery.test.ts, tests/unit/job-state.test.ts</files>
  <behavior>先写以下可失败行为测试：至少一次尝试、最多一次有效业务提交有证据；不声称恰好执行一次；所有错误可查询且重启可恢复。</behavior>
  <action>BEGIN IMMEDIATE短事务claim queued/到期retry_wait，递增fence；lease30s、heartbeat5s，时钟接口可注入。commit/heartbeat/checkpoint均WHERE owner+fence+running+lease_until>now；commit另检查cancel_requested=0，并同事务写唯一业务结果。取消running仅置请求，Worker确认中断才cancelled；与提交竞争按事务赢家可解释。最多3尝试，1/2/4秒有界退避（最后一次失败不再调度），不可重试/权限错误立即failed。多子进程真实DB测试双worker争抢、kill后恢复、迟到旧fence、睡眠/时钟跳变、busy/磁盘满注入、取消/完成竞争；SQL故障不能擦掉好数据。租约到期只重新调度，不杀任意PID。 claim禁止quiescing/exclusive状态领取普通任务；已有running普通任务在quiescing允许以当前generation完成，exclusive前必须排空，否则停；exclusive或generation改变后的普通commit被拒。selfcheck job必须operation_id与当前exclusive gate一致，其claim/commit仍受fence及取消约束。增加candidate维护期间enqueue/claim/迟到commit被拒且用户写入代际不变的真实DB负测。 对expired-running显式BEGIN IMMEDIATE回收：WHERE state=running AND lease_until<=now，先递增fence作废旧owner，attempt>=max_attempts置failed(RETRY_EXHAUSTED)，否则置retry_wait并设置next_run_at；领取时attempt+1。cancel_requested优先转cancelled且不得重跑。测试kill后旧running确实回收、连续3次崩溃最终失败、旧owner迟到提交拒绝，不靠只领取queued的测试伪恢复。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/job-recovery.test.ts && node scripts/dev/runtime.mjs npm run test:unit -- --run tests/unit/job-state.test.ts</automated></verify>
  <acceptance_criteria>至少一次尝试、最多一次有效业务提交有证据；不声称恰好执行一次；所有错误可查询且重启可恢复。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>至少一次尝试、最多一次有效业务提交有证据；不声称恰好执行一次；所有错误可查询且重启可恢复。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-03 | T/R/D | high | 并发任务与SQLite | mitigate | 条件事务fence/幂等/取消、有限busy重试和last-success保留 | tests/integration/job-recovery.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-11：任务持久化由真实SQLite支持，不依赖Codex、LLM或内存队列。
D-15：失租/取消/重试不覆盖最后成功结果，失败与空结果可区分。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-02-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
