---
phase: 01-beta
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["package.json","package-lock.json","tsconfig.json","scripts/dev/runtime.mjs","scripts/build/build.mjs","vitest.config.ts","playwright.config.ts","packages/test-support/src/harness.ts","tests/unit/bootstrap.test.ts","packages/domain/src/model.ts","packages/contracts/src/index.ts","packages/application/src/ports.ts","tests/unit/contracts.test.ts","tests/unit/import-boundaries.test.ts"]
autonomous: true
requirements: ["PLAT-01","ARCH-02","DIST-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-10/D-11：依赖、OS/CPU与实际引擎验证有单一清单；尚未验证平台不标支持通过。"
    - "D-15：synthetic契约包含scope、来源权利、目的地与独立状态维度，不接受任意浏览器指令。"
    - "D-16：本计划须用户批准；生成文件不授权执行，全部产品证据not_run。"
  artifacts:
    - path: "package.json"
      provides: "锁定受管Node与唯一构建配置"
    - path: "vitest.config.ts"
      provides: "建立真实测试设施与安全临时根"
    - path: "packages/domain/src/model.ts"
      provides: "定义应用端口、状态与编译身份契约"
  key_links:
    - from: "scripts/dev/runtime.mjs"
      to: "package-lock.json"
      via: "固定受管Node执行npm ci与build"
    - from: "packages/contracts/src/index.ts"
      to: "packages/application/src/ports.ts"
      via: "strict schema绑定应用端口"
---

<objective>
受管开发运行时、测试骨架与共享契约。
Purpose: 履行D-10、D-11、D-15、D-16和PLAT-01、ARCH-02、DIST-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md

</context>

<tasks>

<task type="auto">
  <name>01-1: 锁定受管Node与唯一构建配置</name>
  <read_first>package.json, package-lock.json, tsconfig.json, scripts/dev/runtime.mjs, scripts/build/build.mjs（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-10, D-11, D-15, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>package.json, package-lock.json, tsconfig.json, scripts/dev/runtime.mjs, scripts/build/build.mjs</files>
  <action>先核对批准的目标根、官方Node24.20.0签名校验清单和研究候选版本；不得全局替换宿主Node26。runtime.mjs用宿主仅做下载/验哈希/解包，随后所有npm/build/test子进程用本仓库忽略的.runtime/dev-toolchain/受管Node24；写入前预览该工具目录及下载。失败硬停不降版本。package精确固定研究表的10组依赖（含MCP server/client2.0.0、Playwright及test同版），辅助types依赖核对后精确固定，npm lock不得范围漂移。一个root包承载apps/packages目录、NodeNext+strict，无虚构workspace包。scripts设typecheck=tsc --noEmit；test:unit/test:integration/test:native=对应Vitest project；test:ui=playwright test；build=node scripts/build/build.mjs；check:bootstrap=node scripts/dev/runtime.mjs --check。build编译所有入口到dist保持目录结构，并在每个实际入口独立注入同一commit/tree/dependencyHash/version/buildId与A或B capability；不是运行时五次读取manifest。开发构建不得发布标签。首次bootstrap仅建立实际配置不创建空pass测试。 .runtime/已被仓库.gitignore忽略；npm ci仅清理node_modules，绝不能删除受管runtime；build清理仅dist/build。测试连续npm ci期间受管Node路径仍存在且可执行。</action>
  <verify><automated>node scripts/dev/runtime.mjs --check</automated></verify>
  <acceptance_criteria>命令核对package/lock/Node24、script清单、无系统配置改动；缺库明确失败。build身份来自编译输入且测试将读取各进程。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>命令核对package/lock/Node24、script清单、无系统配置改动；缺库明确失败。build身份来自编译输入且测试将读取各进程。</done>
</task>

<task type="auto" tdd="true">
  <name>01-2: 建立真实测试设施与安全临时根</name>
  <read_first>vitest.config.ts, playwright.config.ts, packages/test-support/src/harness.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-10, D-11, D-15, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>vitest.config.ts, playwright.config.ts, packages/test-support/src/harness.ts, tests/unit/bootstrap.test.ts</files>
  <behavior>先写以下可失败行为测试：框架可执行至少一个行为测试；不存在空测试/全skip通过，native须拒绝WSL伪装Windows。</behavior>
  <action>创建Vitest unit/integration/native三个project及Playwright localhost-only配置；拒绝学校host，默认关闭trace/video/screenshot，UI仅synthetic。harness创建仅本测试拥有的temp根/进程与synthetic canary，回收必须匹配创建记录，不碰legacy或真实Profile。记录S/I/N及OS/arch/build，每项not_run起始，skip不能汇总pass。bootstrap测试验证脚本存在、生产无LLM包、锁定精确版本、测试networkallowlist；所有测试用受管runtime，单场景目标30秒未实测，原生安装可更长并记耗时。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:unit -- --run tests/unit/bootstrap.test.ts</automated></verify>
  <acceptance_criteria>框架可执行至少一个行为测试；不存在空测试/全skip通过，native须拒绝WSL伪装Windows。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>框架可执行至少一个行为测试；不存在空测试/全skip通过，native须拒绝WSL伪装Windows。</done>
</task>

<task type="auto" tdd="true">
  <name>01-3: 定义应用端口、状态与编译身份契约</name>
  <read_first>packages/domain/src/model.ts, packages/contracts/src/index.ts, packages/application/src/ports.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-10, D-11, D-15, D-16；01-PLAN-INDEX接口表。</read_first>
  <files>packages/domain/src/model.ts, packages/contracts/src/index.ts, packages/application/src/ports.ts, tests/unit/contracts.test.ts, tests/unit/import-boundaries.test.ts</files>
  <behavior>先写以下可失败行为测试：非法scope/任意browser操作拒绝；端口供下游直接实现，domain/application方向有测试。</behavior>
  <action>先写拒绝测试再定义：JobState queued/running/retry_wait/succeeded/failed/cancelled，cancelRequested独立；scope={installationId,source:'synthetic',courseId:'selftest'}；Observation独立auth/capability/health/freshness/completeness及partial/empty/error/not_observed/deleted。Zod严格对象禁额外URL/JS/selector/Profile字段。JobRequest={kind:'echo'|'digest',value:string<=4096,idempotencyKey:uuid,scope};BuildIdentity={version,buildId,commit,dependencyHash,protocol:1,schemaMin:1,schemaMax:1,capabilities:string[]}；Status给API/Worker独立观测与checkedAt，不臆造成功。ports定义JobStore.enqueue/claim/heartbeat/commit/requestCancel/query，OutputPolicy.authorize(scope,operation,destination)，SecretStore.get/set/delete，ProcessSupervisor.start/stop/inspect，IdentityProbe.collect。domain零外部driver/transport依赖；静态import测试检查transitive边界，MCP不得导入persistence、Profile、平台通用fs/process模块或直接访问业务数据；仅允许窄packages/client凭据与安装发现适配器间接使用platform.credentials/installation。检查跨层传递依赖时明确该allowlist，不错误禁止已批准认证接线。 同时定义MaintenanceGate {operationId,generation,state:'open'|'quiescing'|'exclusive',owner,leaseUntil}；JobStore所有enqueue/claim/commit接收并校验expectedGeneration，受限selfcheck权限绑定operationId和generation，不可由普通body自选。 定义StatusProjectionStore.read/writeComponent/writeInstall/writeSelfcheck：组件观测含role/build/checkedAt/health及证据来源，InstallProjection含operationId/stage/result/targetBuild/actualBuild/cleanup/checkedAt，SelfcheckProjection含jobId/probes/featureResult/checkedAt。只保存脱敏字段，未观测未知，不将目标manifest当实际观测；写方法有expectedGeneration/operationId控制。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:unit -- --run tests/unit/contracts.test.ts tests/unit/import-boundaries.test.ts</automated></verify>
  <acceptance_criteria>非法scope/任意browser操作拒绝；端口供下游直接实现，domain/application方向有测试。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>非法scope/任意browser操作拒绝；端口供下游直接实现，domain/application方向有测试。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-01 | T/E | high | 供应链/构建配置 | mitigate | 精确lock、官方Node验签hash、禁全局执行 | tests/unit/bootstrap.test.ts |
| T-01-02 | I/E | high | 跨层输入输出 | mitigate | 严格schema/边界测试、合成scope及目的地契约 | tests/unit/contracts.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-10/D-11：依赖、OS/CPU与实际引擎验证有单一清单；尚未验证平台不标支持通过。
D-15：synthetic契约包含scope、来源权利、目的地与独立状态维度，不接受任意浏览器指令。
D-16：本计划须用户批准；生成文件不授权执行，全部产品证据not_run。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-01-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
