---
phase: 01-beta
plan: "09"
type: execute
wave: 7
depends_on: ["01-08"]
files_modified: ["packages/installer/src/journal.ts","packages/installer/src/upgrade.ts","packages/installer/src/snapshot.ts","tests/integration/upgrade-journal.test.ts","packages/installer/src/recovery.ts","packages/installer/src/cleanup.ts","tests/integration/upgrade-recovery.test.ts","tests/integration/managed-cleanup.test.ts"]
autonomous: true
requirements: ["DIST-02","DIST-01","JOB-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-02/D-03：安装升级自动启动并真实自检，cleanup_pending或身份不符均不是成功。"
    - "D-06：有安全证据才恢复旧版，明确升级失败且不循环重试。"
    - "D-07/D-08：新写入/不兼容schema/未知所有权阻止恢复；首次失败不虚报回滚，Profile不备份。"
    - "D-12：仅清理本安装受管旧程序/入口/进程，允许明确不活动回滚副本但不删档案。"
  artifacts:
    - path: "packages/installer/src/journal.ts"
      provides: "持久操作journal与维护激活事务"
    - path: "packages/installer/src/recovery.ts"
      provides: "安全恢复判据与所有权清理"
  key_links:
    - from: "packages/installer/src/upgrade.ts"
      to: "packages/installer/src/journal.ts"
      via: "每迁移/激活边界持久意图"
    - from: "packages/installer/src/upgrade.ts"
      to: "scripts/install/selfcheck.mjs"
      via: "exclusive maintenance内真实MCP自检"
    - from: "packages/installer/src/recovery.ts"
      to: "packages/installer/src/snapshot.ts"
      via: "安全代际判据后验证一致快照恢复"
---

<objective>
持久升级日志、数据安全恢复与完整清理。
Purpose: 履行D-02、D-03、D-06、D-07、D-08、D-12和DIST-02、DIST-01、JOB-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>09-1: 持久操作journal与维护激活事务</name>
  <read_first>packages/installer/src/journal.ts, packages/installer/src/upgrade.ts, packages/installer/src/snapshot.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-02, D-03, D-06, D-07, D-08, D-12；01-PLAN-INDEX接口表。</read_first>
  <files>packages/installer/src/journal.ts, packages/installer/src/upgrade.ts, packages/installer/src/snapshot.ts, tests/integration/upgrade-journal.test.ts</files>
  <behavior>先写以下可失败行为测试：每边界中断后能解释状态；真实DB快照一致；所有状态持久可查询；不备份Profile。</behavior>
  <action>实现单install更新锁+fsync日志：preview→confirmed→download_verified→quiesced→snapshot_ready→migrated→activated→started→feature_verified→cleaned→complete；每步intent/done写入、targethash/scopehash/installid/代际、恢复时核对现实不信字符串。quiesce拒新业务写、停止已核实旧API/Worker，SQLite backupAPI取一致DB+引用对象manifest本地保护快照，明确排除所有Profile/秘密目录，不copy活跃主DB。迁移前记录schema/generation，维护窗口新candidate只允许selfcheck，切换同卷active临时指针+替换；Windows文件占用拒绝非覆盖运行程序。调用05start和06实际selfcheck，首次也执行；最终步骤未全完成不解除维护。faultinject每journal边界停进程/重启/磁盘满/锁冲突，确保可恢复且不两个active。 通过04 installer-only maintenance route和02持久gate进入quiescing→exclusive，不能仅installer本地布尔阻挡写入；candidate服务继续执行同一DB谓词。维护租约过期保持关闭，恢复须核查journal/进程/schema再决定。新目标验证完成及清理完成后才exitMaintenance并递增generation；普通凭据不能开锁。 每个journal持久边界同步StatusProjectionStore.writeInstall脱敏摘要（stage/result/target/actual/cleanup/checkedAt），API运行时由installer认证路由写；quiesced时由受控maintenance adapter写同一SQLite状态表。恢复重放确保projection来源journal真实完成点；真实GET/api/status在重启后显示cleanup_pending/rollback而非纯UIfixture，纳入upgrade-journal测试。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/upgrade-journal.test.ts</automated></verify>
  <acceptance_criteria>每边界中断后能解释状态；真实DB快照一致；所有状态持久可查询；不备份Profile。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>每边界中断后能解释状态；真实DB快照一致；所有状态持久可查询；不备份Profile。</done>
</task>

<task type="auto" tdd="true">
  <name>09-2: 安全恢复判据与所有权清理</name>
  <read_first>packages/installer/src/recovery.ts, packages/installer/src/cleanup.ts（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-02, D-03, D-06, D-07, D-08, D-12；01-PLAN-INDEX接口表。</read_first>
  <files>packages/installer/src/recovery.ts, packages/installer/src/cleanup.ts, tests/integration/upgrade-recovery.test.ts, tests/integration/managed-cleanup.test.ts</files>
  <behavior>先写以下可失败行为测试：safe rollback/unsafe停机/no-old/cleanup_pending四分支独立；旧活跃入口不存在才完成；资料与无关进程未改。</behavior>
  <action>自动恢复仅oldartifact存在且验签、快照完整、无新业务写generation、旧schema兼容、所有进程拥有身份成立；在维护锁内恢复DB/对象manifest+oldactive、重启实际oldselfcheck，报告UPGRADE_FAILED_ROLLED_BACK不complete，不再次自动尝试。未知写入/代际、无法确保schema/快照/进程所有权→HUMAN_NEEDED并保留脱敏journal，不反向破坏迁移/覆盖新写入/恢复浏览器Profile。首次失败报告INSTALL_FAILED_NO_PREVIOUS并仅回收有记录的暂存程序，不动数据/未知文件。cleanup枚举本install受管理清单与入口/startuprefs/processes，删除已确认过时程序/引用并重探PID身份，隔离rollback副本明确inactive；若lock/access拒绝→CLEANUP_PENDING不complete。迁移失败/后写入/坏快照/旧MCP/旧PID复用/未知启动项/删除失败测试；只删除manifest证明拥有对象，legacy始终零写，档案canary保持。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/upgrade-recovery.test.ts tests/integration/managed-cleanup.test.ts</automated></verify>
  <acceptance_criteria>safe rollback/unsafe停机/no-old/cleanup_pending四分支独立；旧活跃入口不存在才完成；资料与无关进程未改。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>safe rollback/unsafe停机/no-old/cleanup_pending四分支独立；旧活跃入口不存在才完成；资料与无关进程未改。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-14 | T/R/D | critical | 升级跨DB/文件/进程事务 | mitigate | fsyncjournal/maintenancegeneration/一致快照与逐边界故障注入 | tests/integration/upgrade-journal.test.ts |
| T-01-15 | T/E/I | critical | 恢复和清理到用户数据 | mitigate | 恢复安全判据、Profile排除、所有权清理、unknown硬停 | tests/integration/upgrade-recovery.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-02/D-03：安装升级自动启动并真实自检，cleanup_pending或身份不符均不是成功。
D-06：有安全证据才恢复旧版，明确升级失败且不循环重试。
D-07/D-08：新写入/不兼容schema/未知所有权阻止恢复；首次失败不虚报回滚，Profile不备份。
D-12：仅清理本安装受管旧程序/入口/进程，允许明确不活动回滚副本但不删档案。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-09-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
