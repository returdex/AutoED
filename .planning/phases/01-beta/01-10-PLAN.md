---
phase: 01-beta
plan: "10"
type: execute
wave: 8
depends_on: ["01-09","01-07"]
files_modified: ["scripts/build/assemble.mjs","scripts/build/native-artifacts.mjs","scripts/build/platform-matrix.json","tests/integration/artifact-assembly.test.ts","tests/native/install-upgrade.test.ts","tests/integration/two-build-upgrade.test.ts","scripts/test/native-report.mjs","docs/PHASE1-UAT.md"]
autonomous: true
requirements: ["PLAT-01","DIST-01","DIST-02","ARCH-01","SEC-01","JOB-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "D-03：两个beta由不同执行能力证实更新，旧入口混用被拒绝。"
    - "D-09/D-10/D-15：Windows交叉装配仅静态验证，不冒充native运行；真正设备检测及UAT留not_run。"
    - "D-13：适用自动检查先行，生成用户可安装Windows产物不要求先从源码构建。"
  artifacts:
    - path: "scripts/build/assemble.mjs"
      provides: "构建可审计mac/Windows交付目录"
    - path: "tests/native/install-upgrade.test.ts"
      provides: "自动双构建升级与原生故障矩阵"
  key_links:
    - from: "scripts/build/assemble.mjs"
      to: "scripts/build/native-artifacts.mjs"
      via: "按target精确上游包装配及完整性校验"
    - from: "scripts/test/native-report.mjs"
      to: "packages/installer/src/recovery.ts"
      via: "包内合成诊断真实升级故障"
---

<objective>
双beta行为、原生自动检查与平台产物装配。
Purpose: 履行D-03、D-09、D-10、D-13、D-15和PLAT-01、DIST-01、DIST-02、ARCH-01、SEC-01、JOB-01，交付可观察行为及安全验证。
Output: 本计划files_modified中的实现、测试或审批证据。当前仅规划、全部产品证据not_run；须用户批准相关PLAN后执行，不自动进入下一阶段。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-PATTERNS.md
@.planning/phases/01-beta/01-UI-SPEC.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-PLAN-INDEX.md
@.planning/phases/01-beta/01-09-SUMMARY.md
@.planning/phases/01-beta/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>10-1: 构建可审计mac/Windows交付目录</name>
  <read_first>scripts/build/assemble.mjs, scripts/build/native-artifacts.mjs, scripts/build/platform-matrix.json（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-03, D-09, D-10, D-13, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>scripts/build/assemble.mjs, scripts/build/native-artifacts.mjs, scripts/build/platform-matrix.json, tests/integration/artifact-assembly.test.ts</files>
  <behavior>先写以下可失败行为测试：两target产物依赖闭包可审计；错误架构/完整性拒绝；WindowsN保持not_run。</behavior>
  <action>按最新RESEARCH Windows Artifact Feasibility证据装配darwin-arm64和win32-x64两个target：Node24.20.0官方包与签名校验hash；better-sqlite3 13.0.3 npm tar含prebuilds/win32-x64.node(NAPI10)；@napi-rs/keyring-win32-x64-msvc1.3.0及对应darwin包；锁定Playwright Chromium1234/151.0.7922.34 targetbrowser和runtime纯JS依赖。逐包npm integrity(SHA512)、license、sourceURL、targetarch、dependencyclosure写产物清单，禁复制宿主node_modules到Windows；禁止执行Windows二进制于mac并假造加载。校验PE/MachO架构、NAPI兼容元数据、全部require路径存在、archivehash，不能找到targetprebuilt即停止，无云CI/临时编译链/数据库替换。Chromium官方HEAD成功不等于完整性，实际下载保存大小/hash并写受签manifest；先验所有文件再运行。矩阵声明最低依赖mac14+/Win11，与实际测过26.5.2arm64、Win待测分列；检查英文/中文空格路径不假称文件静态=运行。 产物必须含scripts/install bootstrap/selfcheck、受管installer运行文件及diagnostics/native-report.mjs；这些全为合成诊断或产品代码，hash纳入签名。 本task仅实现assembler/native校验并用tests临时fixture目录验证；diagnostics真实源由下一task先创建，本task不得运行依赖完整diagnostics的最终release装配。只有10-2创建runner后才执行真实完整A/B装配和安装测试，避免task级未来依赖。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/artifact-assembly.test.ts</automated></verify>
  <acceptance_criteria>两target产物依赖闭包可审计；错误架构/完整性拒绝；WindowsN保持not_run。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>两target产物依赖闭包可审计；错误架构/完整性拒绝；WindowsN保持not_run。</done>
</task>

<task type="auto" tdd="true">
  <name>10-2: 自动双构建升级与原生故障矩阵</name>
  <read_first>scripts/test/native-report.mjs, docs/PHASE1-UAT.md（新文件不存在时不读取旧库替代）；01-CONTEXT.md D-03, D-09, D-10, D-13, D-15；01-PLAN-INDEX接口表。</read_first>
  <files>tests/native/install-upgrade.test.ts, tests/integration/two-build-upgrade.test.ts, scripts/test/native-report.mjs, docs/PHASE1-UAT.md</files>
  <behavior>先写以下可失败行为测试：本机适用自动测试有实际证据；Windows静态产物和待人工N不混写；两个能力真不同。</behavior>
  <action>构建A=0.1.0-beta.1/echo、B=0.1.0-beta.2/echo+digest（若标签占用，发布前选择下一两个未用N并全部重新构建，禁止覆写）；真实归档安装A后执行echo并对digest拒绝，升级B经已安装MCP入口返回已知SHA256，检查五身份及旧进程/入口清理。自动I包括篡改/错入口/cleanup失败/断电journal/迁移失败/无旧版/未知写入/重复安装/非拥有进程；N在当前原生平台已批准独立temp用户根执行Node/browser/SQLite/keyring/ACL实际探针与A→B，不能用宿主Node26。Windows无执行环境不跳过伪pass，报告not_run等待发布后用户；写native-report只含合成build/OS/arch/codes/结果。UAT精确步骤区分无Node干净用户、Codex退出、手动更新、UI键盘320px200%、OS授权拒绝、回滚/清理；文件顶部阻止beta availability未pass时请求用户执行。 native-report.mjs是可交付的自包含synthetic诊断入口，依赖随包生产模块而非Vitest/npm源码；由assemble复制到diagnostics/native-report.mjs并提供受管Node绝对命令。支持--scenario jobs|permissions|install-recovery及--root显式批准临时根；运行真实双Worker/fence/取消、权限探针和已发布A/B恢复用例，写脱敏N报告且退出非零表示失败。发布包允许该合成诊断代码，不含真实样本。Windows用户不安装开发依赖即可跑所有所需N自动探针。 10只生成局部测试A/B，使用08 synthetic-sign的临时fixturekey并通过同一verify-manifest实际签验；不依赖11发布工具/12真实key，测试信任根不可作为可发布生产信任根。13重新用真实批准key构建签名公开beta。 本task顺序固定：先创建自包含native-report与测试，再调用10-1 assembler完成含runner的真实完整A/B目录，最后运行安装/恢复检查；缺runner或hash记录必须装配失败。</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/two-build-upgrade.test.ts && node scripts/dev/runtime.mjs npm run test:native -- --run tests/native/install-upgrade.test.ts</automated></verify>
  <acceptance_criteria>本机适用自动测试有实际证据；Windows静态产物和待人工N不混写；两个能力真不同。 自动检查不替代人工反馈；未执行不填pass。</acceptance_criteria>
  <done>本机适用自动测试有实际证据；Windows静态产物和待人工N不混写；两个能力真不同。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
本计划代码/公开产物→受管运行时，客户端/数据→应用，以及应用→本机OS/SQLite；仅本机单用户，不承诺抵御同用户恶意进程或管理员。
## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Test reference |
|---|---|---|---|---|---|---|
| T-01-16 | T/S | critical | 跨平台原生依赖及交付目录 | mitigate | 独立target装配/npm integrity/机器头/依赖闭包/真实加载分离 | tests/integration/artifact-assembly.test.ts |
| T-01-17 | R/T | high | 测试证据与版本切换 | mitigate | A/B真实功能、逐OS ledger、未跑不pass | tests/integration/two-build-upgrade.test.ts |
ASVS5.0语义：V1/V2输入、V3/V4前端API、V5文件、V6/V7/V8认证会话授权、V11密码学、V13–V16配置/保护/架构/日志；这里只列适用控制，不声明完整合规。
</threat_model>

<verification>
每task命令用01-01受管运行时及框架；新测试在所属task先创建再执行。每wave适用typecheck/S/I/UI回归；native缺平台留not_run，禁止WSL替代。所有安全反例实际断言拒绝/数据保留；不得grep存在替代行为。人工gate的自动命令仅检查前置条件。
</verification>

<success_criteria>
D-03：两个beta由不同执行能力证实更新，旧入口混用被拒绝。
D-09/D-10/D-15：Windows交叉装配仅静态验证，不冒充native运行；真正设备检测及UAT留not_run。
D-13：适用自动检查先行，生成用户可安装Windows产物不要求先从源码构建。
执行证据含版本/平台/类型/结果；文档通过不代表实现通过。D-16相关PLAN未获批准不得执行；新增权限、人工操作、失败门禁立即暂停。
</success_criteria>

<output>
完成后创建 .planning/phases/01-beta/01-10-SUMMARY.md，记录真实结果/缺口，不写legacy或学校数据。
</output>
