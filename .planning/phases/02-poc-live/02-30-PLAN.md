---
phase: 02-poc-live
plan: "30"
type: execute
wave: 31
depends_on:
  - "02-29"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-04
  - UAT-01
must_haves:
  truths:
    - "02-30 是未来门禁：只有用户已显式恢复原生 Windows 11 UAT，且02-25 actual update、02-26 A1、02-27 A2、02-28 B1、02-29 B2均有completed SUMMARY与current strict gate，才可呈现B3；当前不得执行。"
    - "B3开始前必须由Windows paired server为Moodle/EdStem mint一套新的durable one-time pending bundle，绑定current build/config/binding/runtime generation与pre-exit actual host session；A/B1/B2/macOS action不能复用。"
    - "用户本人通过当前签名Windows beta发布步骤完全退出真实Codex宿主，再正常重新进入实际安装的Codex；只关闭窗口/thread/MCP、child selfcheck、CLI/test/new spawn或同一host session都不能算完整退出/重进。"
    - "Codex宿主缺席期间，既有backend与Worker必须继续独立存活；只允许签名beta prompt/support matrix中的status-only minimal read，且不start/restart/spawn/selfcheck/job/source request。"
    - "重新进入后的authenticated actual client handshake必须证明同一受管installation、current version/build/artifact与新的host session；childSelfcheck固定`not_used`，不能用子进程、PID变化或新selfcheck结果代替。"
    - "B3只核对脱敏minimal status、Moodle/EdStem current state、confirmed binding/approved scope及content boundary；不启动浏览器/Profile、不访问学校、不发source probe、不读取课程正文/帖子/文件/成绩。"
    - "runtime EvidenceLedger必须恰有Moodle与EdStem两个current non-corrected `windows/L/B3_codex_exit` results；paired server是唯一writer，repo gate与SUMMARY只读/编排，无B3 planning receipt JSON。"
    - "任何host exit/reentry未证明、backend/Worker停止、minimal read失败、selfcheck/spawn冒充、build/source/binding mismatch、sensitive output或任一non-pass都立即阻止02-31 C；macOS cells保持完全不变，Windows C/D/reauth与Phase3继续blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      provides: "用户未来显式恢复原生Windows UAT的脱敏交接；不存在时B3不可执行"
    - path: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      provides: "native non-WSL Windows 11 actual beta/update/build/runtime/paired UI的strict update machine gate"
      contains: "WINDOWS_UPDATE_COMPLETE"
    - path: ".planning/phases/02-poc-live/02-29-SUMMARY.md"
      provides: "真实用户完成Windows B2 Worker restart后的current strict前置；只作人类反馈交接，不能替代B2 ledger cells"
    - path: "release/phase2-install-prompt.md"
      provides: "与current signed Windows beta version/build/hash绑定的actual Codex quit/re-entry与status-only minimal-read步骤；不得由agent临时拼命令"
      contains: "Windows"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger与durable paired pending-action store；B3 source-of-truth和server-only one-time writer"
      contains: "EvidenceLedger"
    - path: "apps/status/src/main.ts"
      provides: "Windows paired protected B3 action、lifecycle/current source状态、脱敏结果与用户确认"
      contains: "CheckpointLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows B2/B3 exact-cell canonical read-only verifier；不append L、不启动服务/child selfcheck/browser/source"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-30-SUMMARY.md"
      provides: "仅两个Windows B3 cells与真实feedback strict pass后生成的02-31前置；无parallel planning receipt"
  key_links:
    - from: ".planning/phases/02-poc-live/02-29-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "completed SUMMARY + `--verify --checkpoint B2 --platform windows --read-only`双重前置；SUMMARY/单source/macOS B2不可代填"
      pattern: "WINDOWS_B2_WORKER_RESTART_COMPLETE|B2_worker_restart"
    - from: "apps/status/src/main.ts"
      to: "packages/persistence/src/auth.ts"
      via: "用户退出前从paired UI发起B3，server持久化绑定pre-exit actual host session/current build/config/binding/generation的two-source bundle"
      pattern: "B3_codex_exit|pending-action|human_action"
    - from: "signed Windows beta status-only client"
      to: "backend/Worker continuity observer"
      via: "actual host exit后且reentry前只读既有服务minimal status；start/restart/spawn/selfcheck/source request为零"
      pattern: "status|read-only|childSelfcheck"
    - from: "actual installed Codex reentry"
      to: "paired server B3 workflow"
      via: "authenticated same installation/version/build/artifact + different host session；child/CLI/MCP/test principal拒绝"
      pattern: "actual_client|host_session|build"
    - from: "paired server B3 workflow"
      to: "packages/persistence/src/auth.ts"
      via: "actual exit→service continuity→minimal read→actual reentry→user confirmation顺序成立后，source-specific child与actual result同事务consume/append"
      pattern: "B3_codex_exit|windows|EvidenceLedger"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-31 eligibility"
      via: "只读conjunction验证two-source B3 Windows/L current non-corrected cells、host/client/services/minimal-read/privacy字段与真实feedback；macOS不变"
      pattern: "WINDOWS_B3_CODEX_EXIT_COMPLETE|--checkpoint B3"
---

<objective>
在用户未来已明确恢复原生Windows UAT且Windows B2 strict pass后，执行B3不可替代人工生命周期门：用户完全退出真实Windows Codex宿主，在宿主缺席时证明AutoED backend/Worker独立存活并完成签名入口的最小只读status，然后从实际安装Codex正常重进并核对actual client beta identity与双来源状态。

Purpose: 落实D-03、D-09、D-12、D-14、D-17–D-18与AUTH-02/AUTH-04/UAT-01，证明Codex退出不会终止backend/Worker、真实客户端可接续current安装，同时拒绝child selfcheck/new spawn/窗口关闭/旧session冒充host exit/reentry。
Output: 一个future-only blocking `checkpoint:human-action`、Moodle/EdStem两个独立`windows/L/B3_codex_exit` results、canonical read-only B3 gate和strict pass后的02-30 SUMMARY。当前不执行，不创建B3 planning receipt，不访问学校，不释放02-31。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-20-SUMMARY.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@.planning/phases/02-poc-live/02-27-SUMMARY.md
@.planning/phases/02-poc-live/02-28-SUMMARY.md
@.planning/phases/02-poc-live/02-29-PLAN.md
@.planning/phases/02-poc-live/02-29-SUMMARY.md
@release/phase2-install-prompt.md
@packages/domain/src/model.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/processes.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时必须读取02-29 SUMMARY与current signed Windows beta的真实接口；只允许机械import命名适配，不得复用macOS correlation、发明status/quit命令或添加repo writer：

```typescript
type EvidenceCellKey = {
  platform: 'macos' | 'windows';
  source: 'moodle' | 'edstem';
  scenario:
    | 'A1_login' | 'A2_binding' | 'A2_course_visibility'
    | 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3'
    | 'B2_worker_restart' | 'B3_codex_exit'
    | 'C_os_restart' | 'D_24h_recheck' | 'reauth';
  evidence: 'S' | 'I' | 'N' | 'L';
};
```

Windows B2 canonical prerequisite：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B2 --runtime current --platform windows --read-only
```

strict pass code=`WINDOWS_B2_WORKER_RESTART_COMPLETE`，要求completed 02-29 SUMMARY与Moodle/EdStem两个current non-corrected `windows/L/B2_worker_restart` cells；SUMMARY不能代替ledger。

Windows B3 canonical post-check：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform windows --read-only
```

strict pass code=`WINDOWS_B3_CODEX_EXIT_COMPLETE`。唯一合法conjunction是Moodle/EdStem两个current non-corrected `windows/L/B3_codex_exit` cells，actual host exit/reentry identity proven、backend+Worker survived、actual installed client/build matched、minimal read pass、`childSelfcheck=not_used`、`sensitiveOutput=none`，以及current binding/scope/content boundary一致；macOS cells前后不变、Windows C/D/reauth仍not_run/human_needed、Phase3 blocked。

真正L writer只存在于published paired Windows server workflow。用户退出前从protected UI开始B3，server为两source mint新的durable pending bundle，语义绑定`platform=windows,checkpoint=B3,scenario=B3_codex_exit,current version/build/config/scope/binding/runtime generation,pre-exit actual host session,expiry`；内部exact字段名复用signed beta contract，不在本计划另造export type。bundle必须存在backend-owned durable state，不依赖Codex/MCP/browser memory，并且只能在observed actual host session change后单次consume。repo gate/SUMMARY/chat/public API/CLI/MCP无L writer权。

用户步骤唯一来源是current Windows beta signed manifest绑定的`release/phase2-install-prompt.md`/support matrix及02-29 SUMMARY引用；若缺exact complete-quit、host-absent status-only minimal read、actual installed-client reentry步骤，必须human_needed→修复/全测/新beta/Windows实际更新/重做依赖，不得现场编path、PowerShell、service name或selfcheck替代。
</interfaces>

<hard_gates>
- future-only：02-24 explicit native Windows resume、02-25 actual update、02-26 A1、02-27 A2、02-28 B1、02-29 B2任一未strict pass时不执行、不mint B3 bundle、不提示退出Codex。
- 用户必须本人按signed release步骤完整退出并正常重进actual installed Windows Codex；当前agent在退出期间不能继续、观察或代填结果。
- window close、thread结束、MCP reload、CLI/selfcheck/test/new child Codex、相同host session、PID变化、kill/taskkill或重启Worker都不能满足B3。
- host absent期间只允许运行signed release的status-only minimal read；不得start/restart/spawn/selfcheck/job/source probe/browser/Profile/学校请求。
- B3 feedback/SUMMARY/gate只含allowlisted enums；无identity/course/origin/path/PID/host digest/Profile/Cookie/action ID/raw status/DOM/network/screenshot。
- 任一failed/human_needed立即阻止02-31 C；不自动补测、不覆盖失败、不执行C。macOS cells与Phase3保持不变。
</hard_gates>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-30-1：用户完全退出Windows Codex、验证独立服务并从真实客户端重进</name>
  <files>none（用户动作只影响仓库外current Windows beta/runtime、host lifecycle observation、paired pending-action与append-only EvidenceLedger；strict pass后仅创建脱敏02-30 SUMMARY）</files>
  <read_first>AGENTS.md的Product boundary、Execution after plan approval、Data integrity and verification；02-CONTEXT D-03/D-09/D-12/D-14/D-17–D-18；02-24-SUMMARY explicit resume；02-25 update receipt/SUMMARY；02-26/27/28 SUMMARY的Windows A/B1 gates；02-29-SUMMARY的exact B2/current build/Worker generation与B3 published steps pointer；current signed Windows beta `release/phase2-install-prompt.md`/support matrix；02-UI-SPEC CheckpointLedger/Privacy；最终EvidenceLedger/pending-action/read-only B2/B3 contracts。禁止读取legacy Profile、Cookie/凭据、raw DB/WAL、私人页面/网络或课程内容。</read_first>
  <what-built>未来用户已显式恢复原生Windows 11 UAT、实际更新current signed beta并完成A1/A2/B1/B2；B3/C/D/reauth仍not_run/human_needed。current beta具备backend-owned durable B3 correlation、actual host lifecycle observer、signed status-only minimal-read入口与actual installed-client handshake。</what-built>
  <action>先验证future chain：02-24 resume、02-25 update receipt/SUMMARY及02-26/27/28 completed SUMMARY均属于current native Windows 11 build；运行exact B2 verifier并要求`WINDOWS_B2_WORKER_RESTART_COMPLETE`、completed02-29 SUMMARY与两source `B2_worker_restart` Windows/L current non-corrected cells。重新核对current generation、backend/Worker、binding/scope与privacy状态；B3 two-source cells/pending bundle及C cells必须为空/not_run，macOS cells取read-only baseline。任一缺失、旧build/generation、pre-filled B3/C、macOS代填或敏感输出立即human_needed，不呈现B3动作。

preflight通过后，只在Windows paired protected UI让用户点击一次`开始 B3：完全退出 Codex`。paired server必须重新验证B2/current build/config/binding/generation，并为Moodle/EdStem创建新的durable source children；bundle保存在backend-owned state且绑定pre-exit authenticated actual host session。UI只有在bundle持久化成功、无旧correlation且signed status/reentry步骤可用时才显示可以退出。agent不得点击、退出/重开Codex、运行status或自动批准。

硬暂停并逐字呈现signed current Windows beta prompt/support matrix与02-29 SUMMARY指向的exact步骤；不得自行写PowerShell/path/service/快捷键。用户本人按发布步骤完全退出actual Windows Codex。退出期间本thread/agent必然不可用，不得声称观察结果。backend observer必须先确认pre-exit authenticated host session已断开且actual host exit成立；关闭窗口/thread/MCP、child process退出、PID变化或kill/taskkill均不算。

在actual host absent之后且reentry之前，用户只运行signed beta提供的status-only minimal read。该principal只能读取既有backend/Worker与脱敏current status，不能start/restart/spawn/repair服务、运行child selfcheck、创建job、访问school/source或打开browser/Profile。backend与Worker必须是退出前同一受管服务并持续survived；minimal read pass不能由cached CLI fixture或selfcheck输出代替。

用户按signed步骤正常重开actual installed Windows Codex后返回本thread。server要求authenticated actual-client handshake证明同一installation/current version/build/artifact、不同host session；CLI/MCP/test/installer/selfcheck/new child Codex不满足。用户只在paired protected UI核对client build matched、backend/Worker survived、minimal read pass、Moodle/EdStem current state consistent、binding confirmed、approved scope matched、content boundary preserved、`childSelfcheck=not_used`、`sensitiveOutput=none`；本轮source/browser/course request必须为0。

用户点击一次确认B3结果后，paired server以本轮actual lifecycle sequence+user confirmation+source children形成payload外human_action authority，并分别append Moodle/EdStem `windows/L/B3_codex_exit` immutable result；failed/human_needed也真实保留并消费correlation。chat feedback不是authority，repo script不append/record。用户只返回`<resume-signal>` fixed block；收到真实feedback后运行exact B3 read-only verifier并重读macOS baseline。只有`WINDOWS_B3_CODEX_EXIT_COMPLETE`与全部strict字段成立、macOS cells unchanged才完成；任何non-pass阻止02-31且不自动重试。
  </action>
  <how_to_verify>
    1. 在Windows paired protected UI确认current beta/build、B2 pass、B3 pending，C/D/reauth not_run/human_needed、Phase3 blocked；不要复制identity/course或截图。
    2. 点击一次`开始 B3：完全退出 Codex`，等待UI显示durable checkpoint已准备。不要重复点击、停止backend/Worker、关闭Profile或发source probe。
    3. 严格按signed Windows beta发布步骤完整退出actual Codex；只关闭窗口/thread、reload MCP、运行child Codex/selfcheck均不算。不要用Task Manager/taskkill强制制造退出；正常退出失败则human_needed。
    4. Codex退出后，只运行发布步骤中的status-only minimal-read入口。不要运行selfcheck、start/restart、测试、npm、new Codex、MCP probe或自写命令。确认其只报告backend/Worker survived、current build及脱敏minimal status；若它启动/修复服务、失败或泄漏敏感资料，记failed并停止。
    5. 按signed正常入口重开actual installed Windows Codex并回到本任务；不要从terminal spawn child。等待paired UI显示new actual host session、same installation/current client build matched。
    6. 本地核对backend/Worker survived、minimal read pass、Moodle/EdStem current state consistent、binding/scope matched、content boundary preserved、child selfcheck not used、sensitive output none；不得打开学校或课程。
    7. 点击一次确认B3结果并只复制`<resume-signal>`脱敏block。任一not_observed/stopped/mismatch/selfcheck_used/sensitive_output_detected必须failed/human_needed并停止C。
  </how_to_verify>
  <verify>
    <automated>checkpoint前运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B2 --runtime current --platform windows --read-only`并要求`WINDOWS_B2_WORKER_RESTART_COMPLETE`；用户真实重进、返回strict feedback且paired server已形成B3 results后，运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform windows --read-only`并要求`WINDOWS_B3_CODEX_EXIT_COMPLETE`。两命令必须只读、exit0，repo L-write/start/restart/spawn/selfcheck/browser/source调用为0。</automated>
  </verify>
  <acceptance_criteria>
    1. explicit Windows resume、current native update、A1/A2/B1及completed02-29 SUMMARY+B2 verifier全部strict pass；B3/C起始cells/correlation为空，macOS baseline已读取。
    2. 用户从actual installed Windows Codex按signed步骤完整退出；server证明pre-exit actual host session结束。window/thread/MCP/child/PID-only/kill/taskkill不能满足。
    3. actual exit后、reentry前，signed status-only principal对既有backend/Worker完成minimal read；两服务survived，start/restart/spawn/repair/job/selfcheck/source/browser调用均为0。
    4. reentry是actual installed Codex authenticated client，同installation/version/build/artifact且host session已变化；CLI/MCP/test/installer/child selfcheck/new spawn拒绝，`childSelfcheck=not_used`。
    5. Moodle/EdStem current state consistent、binding confirmed、approved scope matched、content boundary preserved；本轮学校/course/Profile/browser request为0，旧last-success不冒充新source read。
    6. paired server向两source各append一个current non-corrected `windows/L/B3_codex_exit` result；authority绑定本轮durable bundle/actual lifecycle/build/generation/source，跨platform/scenario/build/replay失败。
    7. strict pass唯一为`WINDOWS_B3_CODEX_EXIT_COMPLETE`+actual exit/reentry+backend/Worker survived+actual client/build matched+minimal read pass+childSelfcheck not_used+sensitiveOutput none+source/binding/content一致；真实feedback匹配。
    8. repo gate/SUMMARY/chat/public/CLI/MCP没有L writer，不创建B3 planning receipt JSON；failed/human_needed immutable且立即阻止02-31。
    9. feedback/verifier/SUMMARY无identity/course/origin/path/PID/host digest/Profile/Cookie/action ID/raw status/DOM/network/screenshot；macOS cells不变，Windows C/D/reauth与Phase3仍blocked。
  </acceptance_criteria>
  <resume-signal>重新进入actual installed Windows Codex后，只粘贴paired UI生成的完整`02-30_WINDOWS_B3_RESULT`脱敏块：`result=pass|failed|human_needed`、`result_code=WINDOWS_B3_CODEX_EXIT_COMPLETE|&lt;published safe code&gt;`、`platform=windows`、`codex_exit=observed|not_observed`、`backend=survived|stopped|not_observed`、`worker=survived|stopped|not_observed`、`minimal_read=pass|failed|not_observed`、`reentry=actual_client|child_or_selfcheck|not_observed`、`client_build=matched|mismatch|not_observed`、`moodle_state=consistent|mismatch|not_observed`、`edstem_state=consistent|mismatch|not_observed`、`binding=confirmed|mismatch|not_observed`、`content_boundary=preserved|violated|not_observed`、`child_selfcheck=not_used|used|not_observed`、`sensitive_output=none|detected|not_observed`、`screenshot=not_used`、`observed_at=&lt;ISO-8601 with timezone&gt;`、`macos=unchanged`、`phase3=blocked`。不要发送Terminal命令/输出、姓名、邮箱、组织、课程、origin/path、Profile/PID/Cookie、action ID、host信息、截图或日志。</resume-signal>
  <done>未来用户真实完成Windows actual Codex退出→host-absent服务/minimal-read验证→actual installed client新session重进，two-source B3 Windows/L results与fixed feedback全部strict pass且read-only verifier返回`WINDOWS_B3_CODEX_EXIT_COMPLETE`时才允许02-31 C；否则保持blocked。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| Windows B2 SUMMARY/ledger → B3 eligibility | SUMMARY、旧build、单source或macOS B2可能冒充current Windows B2 | completed02-29 SUMMARY+exact B2 Windows/L two-cell current conjunction；缺一不呈现B3 |
| actual Codex host → lifecycle observer | window/thread/MCP/PID/child变化可能冒充full host exit | pre-exit authenticated actual host session+OS/process observation；kill/child/selfcheck不接受 |
| host-absent user command → backend/Worker | status入口可能暗中start/restart/spawn/repair或运行selfcheck | only signed status-only principal；existing service minimal read；所有mutation/selfcheck/source调用为0 |
| actual reentry → B3 workflow | child Codex/CLI/MCP/test/installer或同session可能冒充actual installed client | authenticated same install/build/artifact + new host session；principal/role exact；childSelfcheck not_used |
| paired server bundle → EvidenceLedger | bundle可能丢失、重放、跨source/platform/scenario/build使用或由repo伪造 | backend-durable source children、ordered lifecycle+actual result、one-time consume/append、repo read-only |
| protected status → feedback/SUMMARY | raw status、host/PID/path/Profile/identity/course可能泄漏 | fixed allowlist、protected-local identity、sensitiveOutput none、无截图/raw output |
| Windows B3 → C/macOS/Phase3 | 单一Windows B3或macOS result可能跨格推进 | exact Windows B3 cells只释放02-31；macOS unchanged，C/D/reauth/Phase3保持缺口 |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking test |
|---|---|---:|---|---|---|---|
| T2-30-01 | Spoofing / Tampering | Critical | B2→B3 prerequisite | mitigate | V2认证、V4授权、V13业务规则 | current two-source `windows/L/B2_worker_restart` + completedSUMMARY + build/generation一致；任一缺失/跨平台代填阻断。 |
| T2-30-02 | Spoofing / Repudiation | Critical | actual host exit/reentry | mitigate | V2身份、V4持续授权、V16审计 | authenticated pre-exit actual host session、actual exit、same installed client/build/new session按序证明；window/PID/child/selfcheck/kill不算。不可证明即blocker。 |
| T2-30-03 | Spoofing / Elevation of Privilege | Critical | status-only minimal read/child selfcheck | mitigate | V2principal、V4least privilege、V13API | signed status-only principal只读既有服务；start/restart/spawn/repair/selfcheck/job/source=0；childSelfcheck必须not_used。任何child替代为Critical blocker。 |
| T2-30-04 | Tampering / Denial of Service | High | backend/Worker continuity | mitigate | V4持续授权、V13状态、V16provenance | host absent窗口同一owned backend/Worker持续survived；status不能自动修复；stopped/not_observed直接failed。 |
| T2-30-05 | Tampering / Repudiation / Elevation of Privilege | Critical | B3 L append | mitigate | V4writer authority、V13事务、V16audit | server-only durable source bundle+ordered actual lifecycle/result+user confirmation同事务consume/append；repo/public/chat无write。重放/跨格即blocker。 |
| T2-02 | Information Disclosure | Critical | status/UI/feedback/output | mitigate | V7日志错误、V8数据保护、V14配置 | full identity仅protected UI；固定redaction；无raw status/path/PID/host/Profile/Cookie/action/course/screenshot；`sensitiveOutput=none`。泄漏即阻断C。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | B3 cells→C/macOS/Phase3 | mitigate | V1证据分层、V4authority、V13gate、V16provenance | exactWindows/source/scenario/L current cells；macOS unchanged；B3仅释放C。S/I/N/macOS/B2/单source不可代填，跨格即blocker。 |

所有High/Critical项必须`mitigate`，不得accept、transfer、降级warning或依赖后续C修复。本表仅表达ASVS 5.0 L1适用控制意图，不声明完整合规。任何fact不可观察、child/selfcheck/spawn冒充、服务不连续、build/source/binding mismatch、writer provenance失败或敏感输出都保持failed/human_needed并阻止02-31。
</threat_model>

<verification>
1. future chain与B2 canonical verifier strict pass；B3/C cells/bundle起始为空，macOS baseline/current build/generation一致。
2. paired server在exit前建立two-source durable bundle；Codex退出不销毁bundle，A/B1/B2/macOS correlation不可重放。
3. 用户按signed Windows步骤完全退出actual Codex；window/thread/MCP/PID/child/kill不能满足actual host exit。
4. host absent期间signed status-only minimal read成功，backend/Worker survived，start/restart/spawn/repair/job/selfcheck/source/browser调用全0。
5. actual installed client以same installation/build/artifact/new host session重进；child/CLI/MCP/test/installer/selfcheck不能代替。
6. server append exactly two current `windows/L/B3_codex_exit` results；strictfeedback与lifecycle/current states一致，failed/human_needed不可隐藏。
7. B3 read-only verifier只在`WINDOWS_B3_CODEX_EXIT_COMPLETE`完整conjunction时exit0；repo无writer/DB/process/browser/source权限且无planning receipt。
8. privacy/platform检查无敏感/raw输出，macOS unchanged，Windows C/D/reauth、Phase1/2/sharedProfile/Phase3缺口保持；02-31不自动执行。
</verification>

<success_criteria>
- AUTH-02：真实Windows Codex full exit、backend/Worker survival、host-absent minimal read与actual installed client new-session reentry均有顺序化live证据，不是child selfcheck推断。
- AUTH-04：backend/Worker保持独立、current generation/build/installation一致；status-only入口不启动/修复服务，未知/停止/old session立即失败。
- UAT-01：Moodle/EdStem分别形成独立`windows/L/B3_codex_exit` result与真实用户脱敏feedback；B1/B2/macOS/S/I/N不可代填。
- paired-server-only writer与repo read-only gate成立，无B3 planning receipt；隐私输出固定且`sensitiveOutput=none`、`childSelfcheck=not_used`。
- B3 strict pass只释放02-31 C；任一failure阻止C，macOS cells不变，Windows C/D/reauth与Phase3继续blocked。
</success_criteria>

<output>
只有future用户真实strict feedback、Moodle/EdStem两个current non-corrected B3 Windows/L results均pass且canonical B3 verifier返回`WINDOWS_B3_CODEX_EXIT_COMPLETE`后，才创建`.planning/phases/02-poc-live/02-30-SUMMARY.md`。SUMMARY只记录public version/build、platform=windows、checkpoint=B3、B2 prerequisite verified、codexExit/backend/worker/minimalRead/reentry/clientBuild/two-source-state/binding/contentBoundary/childSelfcheck/sensitiveOutput脱敏enum、observedAt、02-31 eligibility、macOS unchanged与C/D/reauth/Phase3 gaps；不得创建planning receipt JSON，不得记录identity/course/origin/path/PID/host/Profile/Cookie/action/Terminal/raw status/DOM/network/screenshot。failed/human_needed时保留runtime immutable result、不建成功SUMMARY、不执行02-31。
</output>
