---
phase: 02-poc-live
plan: 26
type: execute
wave: 22
depends_on: [02-25]
files_modified:
  - scripts/release/phase2-live-gate.mjs
  - tests/integration/phase2-live-gate.test.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, SEC-02, UAT-01]
must_haves:
  truths:
    - "本计划当前不可执行；只有 02-24 的显式 Windows native resume 与 02-25 的真实 Windows 11 beta 更新/readiness gate 均 strict pass 后，才允许用户另行开始 02-26。"
    - "用户只在原生 Windows 11、当前已验证 beta 所打开的 Moodle 官方 headed 窗口中亲自输入密码并完成 MFA；产品、执行器、聊天、WSL、macOS 和 fixture 均不读取或代填秘密。"
    - "Moodle 登录通过后，产品只执行一次规定的 EdStem 有界只读 auth probe；如 EdStem 需要交互/MFA，必须停下并由用户在官方窗口亲自完成，绝不自动弹窗、输入或继续。"
    - "Windows A1 对 Moodle 与 EdStem 分别证明 approved origin、positive marker 与 protected-local full identity observed；完整身份只显示在 paired protected local UI，不进入 EvidenceLedger safe cell、SUMMARY、CLI、MCP、日志或诊断。"
    - "只有 current native Windows paired server 的一次性 human-action correlation 加 actual probe result 能 append 两个 `windows/A1_login/L` events；repo gate、反馈、fixture、S/I/N、macOS/WSL 不能 mint、补写或改写 L。"
    - "两个来源均 strict pass 后才允许 canonical checkpoint verifier 返回 `WINDOWS_A1_COMPLETE` 并创建脱敏 SUMMARY；不创建 planning receipt。任一失败、human_needed、mismatch、来源串格、证据泄漏或更新/readiness drift 都阻塞 Windows A2 与 Phase 3。"
  artifacts:
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows A1 exact-cell 只读 verifier；无 EvidenceLedger/DB/browser/source 写权限"
      exports: ["verifyPhase2LiveCheckpoint"]
    - path: "tests/integration/phase2-live-gate.test.ts"
      provides: "native Windows/update/resume、两来源 A1/L、隐私、只读和不可替代性负例矩阵"
  key_links:
    - from: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      to: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      via: "exact future resume 只释放 02-25；02-25 再证明真实 native Windows update/readiness"
      pattern: "RESUME_PHASE2_WINDOWS_NATIVE_UAT|WINDOWS_UPDATE_COMPLETE"
    - from: "current paired Windows UI action"
      to: "EvidenceLedger"
      via: "server-owned one-time human_action authority + actual source result"
      pattern: "platform.*windows.*scenario.*A1_login.*evidence.*L"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: ".planning/phases/02-poc-live/02-26-SUMMARY.md"
      via: "`--verify --checkpoint A1` 只读核对 current native runtime + two exact Windows/A1_login/L cells；真实用户反馈仅允许生成脱敏完成 SUMMARY"
      pattern: "WINDOWS_A1_COMPLETE|A1_login"
    - from: ".planning/phases/02-poc-live/02-26-SUMMARY.md"
      to: "02-27 Windows A2 eligibility"
      via: "canonical checkpoint verifier fresh pass + completed 02-26 SUMMARY + fresh protected paired projection"
      pattern: "A2_binding|A2_course_visibility|candidate"
---

<objective>
为未来恢复的原生 Windows 11 建立不可自动跨越的 A1 登录/MFA live gate：在明确 Windows resume 与真实 beta 更新/readiness 已分别通过后，由用户本人在官方 Moodle 窗口完成登录/MFA，再由产品执行规定的 EdStem 有界只读认证探测，并逐来源验证 origin、positive marker 与仅在 protected local UI 可见的完整身份。

Purpose: 取得独立、不可由 macOS/WSL/fixture/S-I-N 代替的 Windows 11 A1 live 证据，同时证明密码、MFA、Cookie/Profile、原始页面/网络与完整身份不会进入模型、仓库或一般输出；任一失败诚实阻塞 Windows A2 与 Phase 3。

Output: 两个 current `windows/A1_login/L` EvidenceLedger events、repo-side strict read-only checkpoint verifier 与仅 final pass 才创建的脱敏 SUMMARY。当前阶段不执行本计划、不启动 Windows runtime/browser、不访问学校、不生成 receipt。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-08-PLAN.md
@.planning/phases/02-poc-live/02-16-PLAN.md
@.planning/phases/02-poc-live/02-24-PLAN.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-25-PLAN.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md

<interfaces>
<!-- 执行前必须以已完成 02-25 的 exact contract 为准；不得用相似 macOS 文件或手工声明代替。 -->

Windows native resume authority：
```text
02-24-SUMMARY.md 只能在用户未来主动返回下列 exact block、并 fresh 复验 Windows 88/88 cells 均 not_run/human_needed 后存在：
RESUME_PHASE2_WINDOWS_NATIVE_UAT
native_windows_11_available=yes
resume_windows_phase2=yes
allow_next_plan=02-25-only
macos_not_substitute=acknowledged
phase3_remains_blocked=acknowledged
```

02-25 Windows update authority：
```text
.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
Phase2WindowsUpdateGateReceipt
checkpoint = '02-25-windows-update'
platform = 'windows'
nativePlatform = { os:'windows-11', kind:'native', wsl:false, proof:'managed-runtime-selfcheck' }
result = 'pass'
resultCode = 'WINDOWS_UPDATE_COMPLETE'
```

02-26 开始前必须运行并逐字通过：
```bash
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json \
  --artifacts release/phase2-beta-artifacts.json \
  --availability release/phase2-availability.json \
  --runtime current --platform windows --read-only
```
它绑定 repoId=1350421724、exact version/tag/artifact/manifest/build、签名、fresh anonymous availability、native Windows 11 non-WSL managed runtime、cleanup complete、actual entrypoints matched、API/Worker healthy、paired UI ready；并明确两来源 `sourceConfiguration='not_confirmed'`、`schoolAccess='not_started'`、`profile='not_created'`、Windows source evidence `not_run/human_needed`、Phase3 blocked。02-25 SUMMARY 只证明真实用户 update feedback 存在，不能替代 receipt/current runtime verifier；02-26 必须在实际登录前由用户通过 paired protected UI 分别确认固定 approved source/scope，再由产品创建本安装专用 Profile 并取得唯一 ownership，不能把 02-25 的 `not_confirmed/not_created` 假装成已完成。

来自 `packages/contracts/src/index.ts`：
```typescript
export type NativePlatform = 'macos' | 'windows';
export type SourceId = 'moodle' | 'edstem';
export type EvidenceClass = 'S' | 'I' | 'N' | 'L';
export type UatScenario =
  | 'A1_login'
  | 'A2_binding'
  | 'A2_course_visibility'
  | 'B1_reopen_1'
  | 'B1_reopen_2'
  | 'B1_reopen_3'
  | 'B2_worker_restart'
  | 'B3_codex_exit'
  | 'C_os_restart'
  | 'D_24h_recheck'
  | 'reauth';
```

来自 `packages/application/src/ports.ts`：
```typescript
export type EvidenceWriterAuthority =
  | { kind:'automated'; evidence:'S'|'I'|'N'; platform:NativePlatform; producerId:string }
  | { kind:'human_action'; evidence:'L'; platform:NativePlatform; actionReceiptId:string };

export interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: ExactEvidenceKey): Promise<readonly EvidenceReceipt[]>;
}
```

02-26 runtime EvidenceLedger source-of-truth：
```typescript
export interface Phase2WindowsA1LedgerCell {
  schema: 1;
  platform: 'windows';
  nativePlatform: { os:'windows-11'; kind:'native'; wsl:false; proof:'managed-runtime-selfcheck' };
  evidence: 'L';
  scenario: 'A1_login';
  source: 'moodle' | 'edstem';
  version: string;
  buildId: string;
  artifactSha256: string;
  result: 'pass';
  resultCode: 'AUTHENTICATED';
  sourceConfiguration: 'confirmed';
  observedOrigin: 'matched_approved';
  positiveMarker: 'observed';
  identity: 'protected_local_observed';
  interaction: 'completed_by_user' | 'not_required';
  bindingConsistency: 'candidate';
  courseVisibility: 'not_run';
  contentAccess: 'not_started';
  captures: { screenshot:false; rawDom:false; storage:false; rawNetwork:false; secrets:false };
  substitutes: { wsl:false; macos:false; fixture:false };
  observedAt: string;
}

export interface Phase2WindowsA1PassProjection {
  schema: 1;
  status: 'pass';
  checkpoint: 'A1';
  platform: 'windows';
  nativePlatform: { os:'windows-11'; kind:'native'; wsl:false };
  version: string;
  buildId: string;
  scenario: 'A1_login';
  resultCode: 'WINDOWS_A1_COMPLETE';
  sources: readonly ['moodle','edstem'];
  sourceConfiguration: 'confirmed';
  profile: 'dedicated_current';
  auth: 'authenticated';
  observedOrigin: 'matched_approved';
  positiveMarker: 'observed';
  identity: 'protected_local_observed';
  bindingConsistency: 'candidate';
  courseVisibility: 'not_run';
  contentAccess: 'not_started';
  macosEvidence: 'unchanged';
  windowsNext: 'A2_pending_human_action';
  phase1: 'partial';
  phase2: 'incomplete';
  phase3: 'blocked';
}
```

Canonical read-only verifier（唯一 machine completion authority）：
```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint A1 --runtime current --platform windows --read-only
```

`--verify` 只能经 existing signed local-client discovery 调用 redacted status 与两个 exact read endpoints：`GET /api/auth/receipts?platform=windows&source={moodle|edstem}&scenario=A1_login&evidence=L`。它要求 exact 两个 current、non-corrected、same version/build/generation 的 pass cells，并复验 02-25 native update proof、source configuration confirmed、dedicated current Profile、macOS evidence unchanged；stdout 仅为 `Phase2WindowsA1PassProjection`，strict safe code=`WINDOWS_A1_COMPLETE`。它不得 import persistence、打开 DB/WAL/Profile、调用 `EvidenceLedger.append`、接收/输出 actionReceiptId、打开浏览器或请求学校。02-26 不创建 planning receipt；runtime EvidenceLedger exact cells 是机器事实，completed SUMMARY 只证明本轮真实用户反馈存在，不能替代 verifier。

02-27 Windows A2 handoff：只有上述 canonical verifier fresh exit 0 且 `02-26-SUMMARY.md` completed 后才 eligible。fresh paired protected projection 必须显示两来源 `auth='authenticated'`、current safe result `AUTHENTICATED`、完整 identity 只在 protected UI、binding `consistency='candidate'`、`nextAction={kind:'confirm_binding',candidateBindingId}`、course visibility `not_run`。`candidateBindingId` 只从 current projection 到 paired UI；不得进入 ledger/SUMMARY/chat。此 declarative `nextAction` 不是 L authority：02-27 开始前 `A2_binding`/`A2_course_visibility` 的 Windows/L cells 必须为空、不得预 mint actionReceiptId/correlation；用户在 02-27 的独立 paired action才创建新的 exact Windows/build/generation/config/scope/scenario 一次性 correlation，绝不复用 A1 或 macOS correlation。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定 Windows A1 strict checkpoint gate 与不可替代性负例</name>
  <read_first>AGENTS.md 的 native Windows/live evidence/秘密/Profile/原始捕获禁令；02-CONTEXT D-03–D-07、D-09、D-12–D-15、D-17–D-18；02-24 exact resume stop；02-25 exact Windows update/readiness contract；02-16 macOS A1 repo-side gate；现有 `scripts/release/phase2-live-gate.mjs` 与 `tests/integration/phase2-live-gate.test.ts` 全文。</read_first>
  <files>scripts/release/phase2-live-gate.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - RED: 只有 02-24 completed resume、02-25 current native Windows update verifier pass、current protected status 与两个 exact current non-corrected `windows/A1_login/L/pass/AUTHENTICATED` events 全部相符时，checkpoint verifier 才返回 `WINDOWS_A1_COMPLETE`。
    - RED: WSL/Linux/macOS platform、fixture、static Windows artifact、S/I/N、旧/重复/纠正/跨 source/scenario/build/generation 事件、人工自报 identity/origin、缺 update/resume proof 均失败且 out/ledger/runtime 不变。
    - RED: full identity、账户/组织、origin path/query、课程、Profile/Cookie、密码/MFA/input、DOM/storage/network、action ID、endpoint/port/PID/path 不得出现在 argv、stdout/stderr 或测试快照。
    - RED: verify 只读复核 current signed native Windows runtime、update digest、redacted status 与两个 exact events；任一 drift 非零退出且零写。
  </behavior>
  <action>先按 `<behavior>` 添加失败测试，再扩展现有 `phase2-live-gate.mjs` 的 strict `A1` checkpoint verifier，而不是建立平行 writer、planning recorder或receipt schema。argv/result 必须 exact-key/strict enum/ISO timestamp/hash 校验并拒绝额外字段。preflight 先调用02-25 canonical read-only verifier，核对02-24 resume SUMMARY、02-25 update receipt/SUMMARY、current native Windows 11 signed runtime、paired UI/API/Worker ready、两来源仍not_confirmed、school access not_started、Profile not_created与A1 cells not_run；这是开始状态，不得伪装成A1通过。actual A1后 verifier 才要求两来源source configuration confirmed、dedicated Profile current/owned、两个exact L cells和status同一current build/generation。任何缺失返回 `human_needed`且不产生pass。

依 D-03/D-17/T2-08，将OS proof绑定server-attested native platform，不接受`process.platform`自报、浏览器UA、远程客户端hint、WSL或静态Windows构建文件；macOS receipt/version相同也不能代替。依D-04–D-07/D-14/T2-01/T2-02/T2-07，verifier只通过read-only redacted API对照两个exact Windows/L event digests与current status；禁止persistence/DB/append/browser/source import，禁止URL/port/Profile/action ID参数。stdout strict匹配`Phase2WindowsA1PassProjection`，pass code只能是`WINDOWS_A1_COMPLETE`，stderr仅stable safe code。macOS cells/digests必须unchanged；本计划不创建planning receipt，也不允许工具把用户反馈转成机器证据。

产品服务器仍是 L 唯一 writer：只有 current native Windows paired UI 的 server-owned one-time correlation 与 actual result 才能构造 payload 外 `{kind:'human_action',evidence:'L',platform:'windows',actionReceiptId}`；必须绑定 source=`moodle|edstem`、scenario=`A1_login`、approved config/scope、current build/generation 与真实 probe。Moodle human action 后的 EdStem follow-up 可继承同一 server-held Windows A1 workflow correlation，但 background/fixture/chat/feedback/repo gate 不得自升 L。若发布 beta 不具备此已测试链路，停为 `human_needed`，走修复→全套测试→新 immutable beta→02-25 重新实际更新，不在本计划添加写入绕过。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs pnpm exec vitest run tests/integration/phase2-live-gate.test.ts --testNamePattern='Windows A1|native Windows|read-only|redaction' &amp;&amp; node scripts/dev/runtime.mjs pnpm exec eslint scripts/release/phase2-live-gate.mjs tests/integration/phase2-live-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `phase2-live-gate.mjs` 复用并导出 `verifyPhase2LiveCheckpoint`，支持exact `--verify --checkpoint A1 --runtime current --platform windows --read-only`；不存在Windows A1 planning recorder、第二个EvidenceLedger writer、DB reader或browser/source client。
    2. tests 明确覆盖 02-24 resume 与 02-25 native Windows update/readiness 皆 pass 的正例；任一缺失/drift/失败均在 browser/source/action 前停止，out/ledger/runtime hash/counter不变。
    3. Windows A1 exact key 为 `platform='windows',scenario='A1_login',evidence='L'` 且 Moodle/EdStem 各一 current non-corrected pass；S/I/N、macOS、WSL、fixture、静态 artifact、跨来源/场景/build/generation 不能通过。
    4. verifier不import persistence、不打开DB/WAL/Profile、不append、不接受actionReceiptId/URL/port/path、不启动browser/process、不请求学校；spy counters全为0。
    5. strict stdout只允许`Phase2WindowsA1PassProjection`字段；stdout/stderr/fixtures不含full identity、email/account/org、origin/path/course、Profile/Cookie/secret/input、DOM/storage/network、PID/port/action ID/screenshot。
    6. failed/human_needed/intermediate/extra arg/未知 code/401/403/stale/future timestamp/duplicate/corrected/drift时verifier非零且runtime/ledger/files hash不变；工具不伪造resume/update/live pass或写planning receipt。
    7. A1 pass 仍固定 binding=candidate、courseVisibility=not_run、contentAccess=not_started、phase1=partial、phase2=incomplete、phase3=blocked；不提前 mint Windows A2 action/correlation 或写 A2 cells。
  </acceptance_criteria>
  <done>Windows A1 machine gate已通过TDD固定为native Windows + actual paired L + two-source current result的严格只读checkpoint verifier；所有替代、泄漏、串格与写权限路径均有失败测试。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 用户在原生 Windows 11 完成官方 Moodle/EdStem A1 登录与身份核对</name>
  <read_first>Task 1 的 canonical gate；02-24 completed SUMMARY 与 exact resume；02-25 Windows update receipt/SUMMARY/canonical verifier；paired protected UI 的 official login、local full identity、safe status contracts；AGENTS.md 关于用户本人密码/MFA、失败阻塞和禁止原始捕获的规定。</read_first>
  <files>.planning/phases/02-poc-live/02-26-SUMMARY.md（仅 strict final pass + canonical checkpoint verifier exit 0 后由 execute-plan 创建；不创建任何 02-26 planning receipt）</files>
  <what-built>已实现并测试的 current beta 只允许 paired protected UI 发起官方 Moodle headed login；用户完成后只运行 bounded read-only auth probe，再按固定顺序运行 EdStem auth probe。完整身份仅在本机 protected UI，所有反馈、ledger safe cells与SUMMARY均为脱敏状态。</what-built>
  <action>本 checkpoint 当前不得开始。执行器首先要求已完成的02-24 SUMMARY、02-25 SUMMARY与exact 02-25 Windows update receipt，并逐字运行02-25 canonical native Windows/current runtime verifier；缺任一项、用户未另行开始02-26、runtime非原生Windows 11、update/paired不ready、source configuration不是`not_confirmed`、Profile不是`not_created`或schoolAccess不是`not_started`，都保持`human_needed`，不启动浏览器/学校请求、不生成pending action/receipt。计划获批、02-24 resume或02-25 update均不自动批准本登录。

前置strict pass后，先让用户在paired protected UI逐来源查看并确认产品固定提供的approved source/scope；请求体只能含source与approvedConfigId/approvedScopeId引用，不接受URL/path/query/selector。确认后才由current Windows installation创建受保护的专用Profile并取得唯一ownership；不得复制macOS/WSL/Codex/everyday Chrome Profile或Cookie。随后指示用户点击Moodle“打开官方登录”。确认窗口approved official origin且使用该dedicated Profile；用户本人输入密码并完成MFA，执行器/产品不读取、键入、记录或请求任何秘密。用户回到paired UI点击“我已完成登录”；server在动作前后校验current ownership/generation/cancellation/origin，执行一次bounded read-only Moodle auth probe，只观察approved origin、positive marker与protected full identity projection，绝不读课程内容。用户在protected UI本人核对显示的是预期本地完整身份，但反馈只记`protected_local_observed`。

只有 Moodle committed `AUTHENTICATED` 后才允许一次 EdStem bounded read-only auth probe。若 EdStem 返回 `INTERACTION_REQUIRED`，立即保持同一 checkpoint blocked，仅让用户点击 paired UI 明示的 EdStem 官方窗口并亲自登录/MFA；绝不由后台自动弹窗/输入/重试，也不在 Moodle 失败后探测 EdStem。每来源 actual result 与 server-owned current Windows human correlation 分别 append exact `windows/A1_login/L` event；unknown origin、marker缺失、identity mismatch、ownership/fence/cancel、download/write、parser/network/permission 错误任一项立即停止，不尝试课程或其他来源写操作。

用户只复制 paired UI 的 exact 脱敏块：
```text
02-26_WINDOWS_A1_RESULT
checkpoint=windows_A1
platform=windows
native_os=windows_11
version=<public semver prerelease>
build_id=<public build id>
result=pass|failed|human_needed
result_code=A1_AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|INTERACTION_REQUIRED|ORIGIN_MISMATCH|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR
moodle=pass|failed|human_needed
moodle_code=AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|INTERACTION_REQUIRED|ORIGIN_MISMATCH|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR
moodle_origin=matched_approved|not_observed
moodle_marker=observed|not_observed
moodle_identity=protected_local_observed|not_observed|mismatch
moodle_interaction=completed_by_user
edstem=pass|failed|human_needed|not_run
edstem_code=AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|INTERACTION_REQUIRED|ORIGIN_MISMATCH|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR|NOT_RUN
edstem_origin=matched_approved|not_observed
edstem_marker=observed|not_observed
edstem_identity=protected_local_observed|not_observed|mismatch
edstem_interaction=not_required|completed_by_user|pending_user
binding_consistency=candidate|mismatch|not_observed
course_visibility=not_run
content_access=not_started
screenshot=not_used
raw_capture=not_used
secrets_shared=no
wsl_substitute=no
macos_substitute=no
fixture_substitute=no
observed_at=<ISO-8601 with timezone>
phase1=partial
phase2=incomplete
phase3=blocked
```
`human_needed/INTERACTION_REQUIRED`的EdStem block是中间状态，不创建SUMMARY；执行器给出官方窗口本人操作步骤并再次暂停。只有final block两来源均pass/AUTHENTICATED、origin matched、marker/identity observed、binding candidate、course/content未开始、无替代/捕获/秘密且固定phase gaps同时满足时，才运行canonical `--verify --checkpoint A1 --runtime current --platform windows --read-only`。verifier必须从runtime exact cells独立得到strict`WINDOWS_A1_COMPLETE`，不能信任聊天block构造结果。feedback或verifier任一失败都保持阻塞且不创建SUMMARY；不得手工编辑ledger、创建planning receipt、补写L或重跑学校动作覆盖失败。</action>
  <how-to-verify>
    1. 当前不要执行；只有未来 02-24/02-25 均真实完成且用户另行开始 02-26，才在那台原生 Windows 11 当前 beta 的 paired protected UI 操作。
    2. 先确认 UI 显示 current version/build、native Windows、API/Worker/paired ready、两来源 configured、school access not started；不要发送设备名、路径、端口、账户或截图。
    3. 点击 Moodle 官方登录；只在官方页面亲自输入密码/MFA。返回 UI 点击完成后，在 protected identity 面板本人核对完整身份正确；不要把身份复制到聊天。
    4. 查看 Moodle origin matched、positive marker observed、auth authenticated 后再等待 EdStem probe；Moodle 失败时确认 EdStem 未运行。
    5. 若 EdStem 需要交互，只点击 paired UI 的 EdStem 官方登录并亲自完成；否则后台不得弹窗。再次在 protected UI 核对 EdStem 完整身份正确。
    6. 确认未访问课程正文/帖子/文件/成绩，未截图、未导出 DOM/storage/network、未共享秘密，WSL/macOS/fixture均未参与。
    7. 只粘贴 UI 生成的完整 exact `02-26_WINDOWS_A1_RESULT` block；不添加姓名、邮箱、学校、组织、课程、origin URL、Profile/Cookie、action ID、日志或截图。
  </how-to-verify>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A1 --runtime current --platform windows --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. 02-24 exact resume、02-25 actual native Windows update/readiness 与用户另行开始02-26三项独立成立；任一缺失时browser/source/action/L/SUMMARY增量为0且无02-26 planning receipt。
    2. 登录发生在current beta管理的原生Windows 11 official Moodle headed window；密码/MFA只由用户本人输入，产品/执行器/聊天/日志/测试从未读取或返回。
    3. Moodle先完成并 pass 后才运行EdStem bounded read-only auth probe；EdStem需要交互时先停、由用户本人完成，后台不自动弹窗/输入/继续。
    4. 两来源分别为 current `windows/A1_login/L/pass/AUTHENTICATED`，origin matched、positive marker observed、identity protected_local_observed；完整身份只在paired protected local UI。
    5. Windows L authority仅由paired server的一次性human correlation+actual result产生；WSL/macOS/fixture/S/I/N/feedback/repo gate未写或代填，actionReceiptId未离开server。
    6. 用户final block逐字段strict匹配；failed/human_needed/mismatch/not_run/extra/sensitive/旧block均不创建SUMMARY，不推进02-27。
    7. canonical checkpoint verifier只从current redacted runtime与两个exact cells得出`WINDOWS_A1_COMPLETE`并exit0；不创建planning receipt。stdout/SUMMARY无full identity/origin URL/course/Profile/Cookie/secret/action ID/raw data/screenshot/device/path。
    8. pass后 binding仍candidate、courseVisibility not_run、contentAccess not_started、phase1 partial、phase2 incomplete、phase3 blocked；02-27前Windows `A2_binding`/`A2_course_visibility` L cells为空且无预生成A2 correlation。
  </acceptance_criteria>
  <resume-signal>当前不要执行或回复。未来全部前置通过且你另行决定开始02-26后，只粘贴paired protected UI生成的完整 `02-26_WINDOWS_A1_RESULT` 脱敏块。若EdStem为 `human_needed/INTERACTION_REQUIRED`，本checkpoint继续阻塞，待你本人在EdStem官方窗口完成后再返回新的完整块。不要发送密码、MFA、姓名、邮箱、学校/组织、origin URL、课程、Profile/Cookie、action ID、截图、页面、网络或日志。</resume-signal>
  <done>真实native Windows A1两来源final pass已由exact L cells与只读checkpoint gate复验，严格脱敏SUMMARY可生成且无planning receipt；否则本计划保持blocked且Windows A2/Phase3均未推进。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 与阻断政策

本 gate 以 OWASP ASVS L1 的 V1 架构边界、V2 身份验证、V4 访问控制、V5 输入验证、V8 数据保护、V9 通信、V13 API/业务流程与 V16 日志为最低基线。下列 High/Critical 项全部 disposition=`mitigate`；任何缓解缺失、测试不通过、真实反馈失败或证据泄漏都是本计划 blocker，不能降级接受、转成技术债或继续 02-27。

## Trust Boundaries

| Boundary | 风险 |
|---|---|
| future resume/02-25 update → 02-26 eligibility | “设备可用”或静态Windows包可能被误当实际native/current readiness与登录授权 |
| paired Windows UI → official school windows | untrusted页面、钓鱼origin、密码/MFA与popup可能越界进入产品/模型 |
| BrowserProvider → source adapters | 原始DOM/network/storage与写/下载能力可能泄漏或产生学校写入 |
| paired human workflow → EvidenceLedger L | background/fixture/chat可伪造human authority，或跨平台/场景复用action |
| protected identity UI → safe outputs | 姓名、邮箱、组织、账号可能进入ledger/SUMMARY/log/CLI/MCP/chat |
| redacted runtime/feedback → repo completion gate | repo工具可能直读DB、补写L、信任自报或泄漏本机信息 |
| A1 completion → A2 scheduler | 自动推进可能复用A1 correlation或提前确认binding/course |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | ASVS | Mitigation Plan |
|---|---|---:|---|---|---|---|
| T2-26-NATIVE | Spoofing/Elevation of Privilege | Critical | 02-24/02-25 → Windows A1 | mitigate | V1,V4,V13 | exact resume SUMMARY + 02-25 signed native runtime/update receipt + current read-only verifier；WSL/macOS/UA/hint/static artifact拒绝。缺任一项零执行。 |
| T2-26-SECRET | Information Disclosure | Critical | official login/MFA window | mitigate | V2,V8,V16 | 用户本人仅在official headed page输入；禁止读取input/keystroke/Cookie/storage、截图/trace/HAR/video/console/request body；safe outputs扫描。泄漏即blocker。 |
| T2-26-ORIGIN | Spoofing/Tampering | Critical | redirects/positive marker | mitigate | V1,V5,V9,V13 | approved config逐跳origin校验、positive marker、before/after fence；unknown origin immediate stop，不向外输出path/query/raw页面。 |
| T2-26-WRITE | Tampering/Elevation of Privilege | Critical | browser/source request | mitigate | V4,V13 | auth-only fixed action；仅GET/HEAD/read，禁止submit/upload/post/reply/download与任意URL/JS/selector；write/download触发typed stop。 |
| T2-26-L | Spoofing/Repudiation/Elevation of Privilege | Critical | paired server → EvidenceLedger | mitigate | V4,V5,V13 | payload外server-owned one-time `human_action/windows` authority绑定actual source/config/build/generation/scenario；repo/chat/background/fixture无append权限，exact key防串格。 |
| T2-26-IDENTITY | Information Disclosure | High | protected projection/presenter | mitigate | V4,V8,V16 | full identity仅paired protected local UI；外部仅`protected_local_observed|mismatch`与irreversible digests；schema/scan拒绝email/name/org/origin/course/Profile/actionID/raw data。 |
| T2-26-GATE | Spoofing/Tampering/Information Disclosure | High | phase2-live-gate/feedback/SUMMARY | mitigate | V4,V5,V8,V16 | strict argv/schema、current native runtime/update proof/two exact cells、read-only endpoints、allowlisted stdout；无DB/append/browser/source权限，drift/extra/401/403全失败，不创建planning receipt。 |
| T2-26-DOS | Denial of Service | High | Moodle→EdStem progression | mitigate | V13 | Moodle非pass即Ed不运行；bounded single probe、取消/fence检查；interaction required停等本人，不自动重试/弹窗。 |
| T2-26-AUTO | Elevation of Privilege/Repudiation | Critical | checkpoint/orchestrator → A2 | mitigate | V1,V4,V13 | blocking human-action，无default/timeout/auto approval；final exact block+verifier+SUMMARY才释放02-27，且A2需新correlation；失败不可覆盖。 |
| T2-26-MERGE | Tampering | High | platform evidence matrix | mitigate | V4,V13 | Windows/A1/L独立append-only cells与completed SUMMARY；macOS/WSL/S/I/N不可填Windows；phase1 partial、phase2 incomplete、phase3 blocked保持显式。 |
</threat_model>

<verification>
1. 确认当前没有执行本计划：02-24/02-25未future-complete时，Windows runtime/browser/school/action/L/SUMMARY增量均为0，且不存在任何02-26 planning receipt。
2. 运行 Task 1 targeted integration tests；正例要求 exact resume + actual native Windows current update + 两项 current Windows/A1_login/L pass，所有替代/串格/泄漏/写权限负例失败。
3. 未来前置满足后先运行02-25 canonical read-only verifier，确认native Windows 11 current beta、paired API/Worker/UI ready、source configuration not_confirmed、Profile not_created、schoolAccess not_started、A1 not_run；随后仅由用户在paired protected UI确认fixed approved source/scope并创建dedicated Profile。
4. 用户实际执行时对照 bounded counters：只有点击Moodle official login才出现窗口，只有本人完成后Moodle probe，只有Moodle pass后EdStem probe；任何失败后后续counter不增加。
5. 对照EvidenceLedger exact endpoints：Moodle/EdStem各一个current non-corrected `windows/A1_login/L/pass/AUTHENTICATED`；无macOS/WSL/fixture/S-I-N代填，无A2事件。
6. strict final feedback与canonical checkpoint verifier独立一致；verifier从current exact cells得到`WINDOWS_A1_COMPLETE`，repo gate零DB/append/browser/source写且不创建planning receipt，所有输出通过敏感扫描。
7. 确认binding仍candidate、course/content未开始、Windows A2 cells/correlation为空、Phase1 partial、Phase2 incomplete、Phase3 blocked；任一失败不生成成功SUMMARY且不执行02-27。
</verification>

<success_criteria>
- AUTH-01：未来用户在原生Windows 11官方Moodle窗口本人登录/MFA；Moodle通过后仅按规定运行EdStem有界只读auth probe，必要交互再次由用户本人完成。
- AUTH-02：两来源分别验证approved origin、positive marker与protected-local identity observed，形成独立Windows/A1_login/L证据；WSL/macOS/fixture/S-I-N不能替代。
- SEC-02：密码/MFA/Cookie/Profile/full identity/raw DOM/storage/network/input/screenshot从未进入repo gate、ledger safe cell、SUMMARY、CLI/MCP/log/chat；source write/download与unknown origin立即阻断。
- UAT-01：paired-server-only L exact cells、strict human feedback、completed SUMMARY与canonical read-only checkpoint verifier共同构成可复核A1 gate；不创建planning receipt，failed/human_needed不被合并、覆盖或自动批准。
- 02-27只有在Windows A1 verifier exit0且02-26 SUMMARY completed后eligible；A2使用新paired human correlation，不能复用A1/macOS action，课程仍not_run。
- Windows后续B/C/D/reauth仍not_run/human_needed，共享Profile未最终采用；Phase1保持partial、Phase2 incomplete、Phase3 blocked。
</success_criteria>

<output>
当前不得创建 `.planning/phases/02-poc-live/02-26-SUMMARY.md`，且本计划在任何时候都不创建02-26 planning receipt。未来只有02-24/02-25真实完成、用户另行开始本checkpoint、两个exact Windows/A1_login/L events均final pass且canonical checkpoint verifier返回`WINDOWS_A1_COMPLETE`后，才创建 `.planning/phases/02-poc-live/02-26-SUMMARY.md`。SUMMARY只含公开version/build、native Windows A1 pass safe code、两来源AUTHENTICATED/marker/identity-observed状态、source configuration confirmed、dedicated Profile current（不含路径）、binding candidate、course/content not_run、event digests、A2新correlation要求、Windows后续not_run、Phase1/2/3 gaps；不得含设备名、账户/身份、学校/组织、origin URL、课程、Profile路径/Cookie、secret/action ID、path/PID/port、raw data、截图或日志。
</output>
