---
phase: 02-poc-live
plan: "25"
type: execute
wave: 25
depends_on: ["02-24"]
files_modified:
  - .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
autonomous: false
requirements: [AUTH-02, AUTH-04, UAT-01]
must_haves:
  truths:
    - "This future-only plan starts only from the exact 02-24 native-Windows resume and a fresh 88-possible/22-required-L audit with all required L gaps unrun."
    - "The user actually updates Codex on native Windows 11 to the same immutable signed beta; WSL/Linux/macOS, CI, static artifacts and self-reported platform cannot substitute."
    - "The signed pre/post update verifier proves exact version/build/artifact, native non-WSL platform, managed cleanup, API/Worker/paired UI readiness and zero source/Profile/evidence activity."
    - "The update receipt is distribution evidence only: it writes no S/I/N/L, leaves all 22 required Windows L cells not_run/human_needed and releases only 02-40."
  artifacts:
    - path: .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
      provides: "Actual native Windows update result bound to immutable beta"
  key_links:
    - from: .planning/phases/02-poc-live/02-24-SUMMARY.md
      to: .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
      via: "exact future resume and 02-25-only eligibility"
      pattern: "native_windows_11_available|resume_02-25_only"
    - from: scripts/release/verify-phase2-update-gate.mjs
      to: .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
      via: "signed read-only pre/post verifier"
      pattern: "WINDOWS_UPDATE_READY|WINDOWS_UPDATE_COMPLETE"
---

<objective>
Perform the future user-controlled native Windows 11 update to the already published immutable beta.

Purpose: Establish the actual installed Windows build before current-build native and live evidence, without treating distribution as evidence.
Output: Strict redacted Windows update receipt. No school login/source access/Profile creation/evidence write occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@release/phase2-beta-artifacts.json
@release/phase2-publication.json
@release/phase2-availability.json
@release/phase2-install-prompt.md
@scripts/release/verify-phase2-update-gate.mjs
@scripts/release/phase2-live-gate.mjs

<interfaces>
Pre-update:
`node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --artifacts release/phase2-beta-artifacts.json --tests release/phase2-test-report.json --publication release/phase2-publication.json --availability release/phase2-availability.json --prompt release/phase2-install-prompt.md --platform windows --read-only`

Post-update:
`node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only`

Audit invariant before/after:
`possibleCellCount=88, requiredCellCount=22, passCellCount=0, notRunCellCount=22, humanNeededCellCount=22, phase3=blocked`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume the signed Windows update preflight</name>
  <files>.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json</files>
  <action>Require a completed 02-24 SUMMARY containing the exact future native-Windows response, fresh strict counts and 02-25-only eligibility. Consume, never edit, the verifier finalized in 02-37 and hash-bound into 02-39. Refetch current highest complete immutable beta anonymously and verify repository/returdex identity, exact tag/version/build, assets/manifests/hashes/signature/fingerprint, Windows support/dependency closure, license and byte-exact prompt. Reject mutable latest, fallback to an older beta, partial newer beta, identity/hash drift, WSL/static-platform substitution or any missing signed capability. Run the signed audit and require 88 possible/22 required L/all 22 required not_run. All checks are read-only; no receipt, runtime, source, Profile, browser or evidence mutation occurs.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --artifacts release/phase2-beta-artifacts.json --tests release/phase2-test-report.json --publication release/phase2-publication.json --availability release/phase2-availability.json --prompt release/phase2-install-prompt.md --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only</automated></verify>
  <done>The exact signed Windows beta and prompt are ready for a real user-controlled update, with all required Windows L evidence still absent.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User performs the actual native Windows Codex update</name>
  <files>.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json</files>
  <what-built>The exact immutable Windows asset, signature, hashes, build and full install prompt are verified and ready; no installation or login has been claimed.</what-built>
  <how-to-verify>
    1. In Codex on an actual native Windows 11 device, follow the byte-exact `release/phase2-install-prompt.md`; do not use WSL/Linux, another computer's result or a static archive inspection.
    2. Approve only unavoidable Windows OS prompts yourself. Do not log into Moodle/EdStem, create the Profile, or access school content.
    3. Confirm the installer reports the exact version/build/hash, obsolete managed programs/entrypoints/processes cleared, API/Worker healthy and paired UI ready.
    4. Return the strict allowlisted update-result block displayed by the installer. Do not paste paths, PID, account, device, Profile, credentials, logs, screenshots or network details.
  </how-to-verify>
  <resume-signal>Return the exact signed installer result block; `approved`, plan approval, silence or a substituted platform remains blocked.</resume-signal>
  <action>Strictly parse the real feedback and compare it to Task 1. Only the conjunction `nativePlatform=windows-11`, `platformKind=native`, `wsl=false`, exact version/build/hash, cleanup complete, API/Worker healthy, paired UI ready, source not_confirmed, school not_started, Profile not_created and Phase3 blocked may create the receipt. Run the signed post-update verifier and repeat the 88-possible/22-required audit. Any failed/human_needed/cleanup_pending/mismatch/unavailable result creates no pass receipt and does not release 02-40. A product defect requires a fully tested higher immutable beta, fresh publication/availability and another actual update.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only</automated></verify>
  <done>The actual native Windows installation matches the immutable beta and remains source/Profile/evidence clean; only 02-40 becomes eligible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| future resume → update eligibility | Old or broader authorization must not start Windows work. |
| public release → Windows device | Remote bytes and support claims are untrusted until signature/hash verification. |
| user feedback/runtime → receipt | Self-report cannot replace native platform/current managed-runtime proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-25-01 | S/E | native platform | mitigate | Actual Windows 11 attestation, non-WSL check and signed managed-runtime inventory. |
| T2-25-02 | T/R | release/update identity | mitigate | Immutable highest-complete beta, exact hashes/signature/build and pre/post verifier. |
| T2-25-03 | T/E | update → evidence | mitigate | Update receipt has no evidence authority; strict 22-required-L zero-pass audit before/after. |
| T2-25-04 | I | feedback/receipt | mitigate | Strict allowlist excludes device, path, account, Profile, credentials and logs. |
</threat_model>

<verification>
Preflight and postflight must consume the same signed verifier and identity. The required Windows L matrix remains 0/22 pass; 02-40, not 02-26, is the sole next dependency.
</verification>

<success_criteria>
- Exact immutable beta is actually installed on native Windows 11.
- Cleanup and paired local services are verified from the current managed runtime.
- Update writes no evidence and leaves all 22 required Windows L cells not_run/human_needed.
- Only Plan 02-40 becomes eligible.
</success_criteria>

<output>
On strict pass, create `.planning/phases/02-poc-live/02-25-SUMMARY.md` with public version/build/artifact hashes, native Windows/non-WSL, cleanup/services/UI status, possible=88/requiredL=22/pass=0, source/Profile/school not started, Phase3 blocked and 02-40 eligible. Otherwise create no success SUMMARY.
</output>
