---
phase: 02-poc-live
plan: 25
type: execute
wave: 21
depends_on:
  - "02-24"
files_modified:
  - scripts/release/verify-phase2-update-gate.mjs
  - tests/integration/phase2-release-gates.test.ts
  - .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
autonomous: false
requirements:
  - AUTH-02
  - UAT-01

must_haves:
  truths:
    - "02-25 在 02-24 完成、用户曾主动发送 exact Windows resume block 且 02-24 SUMMARY 只授予 `02-25-only` eligibility 之前完全不可执行；当前 PLAN 获批、macOS 已通过、设备被检测到或 generic auto-mode 都不是恢复授权。"
    - "执行时重新解析公开不可覆盖 release 集合，选择当时最高、完整且受支持的 `0.1.0-beta.N` exact tag；不使用 mutable `latest`、旧缓存、Mac 资产或未签名/不完整的新版本回退。"
    - "用户看到的 Windows 更新步骤逐字来自该 exact beta 的签名 prompt，并在原生 Windows 11 上由用户本人执行；WSL/Linux/macOS、静态 Windows 构件、下载成功或发布成功都不能代替实际更新。"
    - "只有签名/hash/manifest/build/support matrix、匿名完整字节可获取性、native Windows 11/非 WSL、cleanup、实际 build/entrypoints、API、Worker、paired UI 全部 strict pass，才创建唯一 Windows update receipt。"
    - "缺设备、未授权、网络/发布/签名/hash/build/support/安装/cleanup/runtime/UI 任一失败或未观察均保持 `not_run / human_needed`；没有默认值、超时通过、自动批准或失败后降级到旧 beta。"
    - "02-25 update receipt 只证明 exact Windows beta 已获取并在原生 Windows 11 正确运行；它不写 Moodle/EdStem N/L、不建立 Profile、不确认来源/身份/课程、不使 Phase 1/2 complete，且只允许 02-26 成为 eligible，不自动执行 02-26。"
  artifacts:
    - path: "scripts/release/verify-phase2-update-gate.mjs"
      provides: "严格只读的 Windows pre/post-update verifier；固定 returdex/AutoED trust root，拒绝任意 URL/path/browser 参数和跨平台代填"
      exports:
        - verifyPhase2UpdateGate
        - validatePhase2WindowsUpdateGateReceipt
    - path: "tests/integration/phase2-release-gates.test.ts"
      provides: "then-latest resolution、Windows platform proof、receipt schema、immutable availability 与只读/零证据污染负例"
    - path: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      provides: "用户未来 exact resume 已被 02-24 观察、fresh 88-cell audit 仍全缺口且只授予 02-25 eligibility 的上游硬门"
    - path: "release/phase2-beta-artifacts.json"
      provides: "当时最新 exact beta 的签名 artifact/build/support identity；若不是公开最高完整 beta，必须经正常不可覆盖 release 流程更新"
    - path: "release/phase2-publication.json"
      provides: "returdex/AutoED repository ID、不可覆盖 tag/release/asset IDs 与 public immutable identity"
    - path: "release/phase2-availability.json"
      provides: "已提交的匿名完整字节 availability proof；checkpoint 前必须另做 fresh proof"
    - path: "release/phase2-install-prompt.md"
      provides: "签名 manifest 绑定的 exact Windows 获取/更新/迁移/自检/cleanup 指令，不允许执行器重新拼写或猜测"
    - path: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      provides: "仅 strict pass 后生成的脱敏 Phase2WindowsUpdateGateReceipt；02-26 的唯一 Windows update 前置"
  key_links:
    - from: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      to: "02-25 execution"
      via: "strict `RESUME_PHASE2_WINDOWS_NATIVE_UAT` observed + `allow_next_plan=02-25-only` eligibility；缺失时零执行"
      pattern: "02-25-only|windows.*not_run|phase3.*blocked"
    - from: "release/phase2-publication.json"
      to: "release/phase2-beta-artifacts.json"
      via: "固定 repository ID 1350421724、exact highest beta.N tag/commit/release/asset IDs 与 signed manifest/hash/build/support 交叉绑定"
      pattern: "1350421724|immutable|buildId"
    - from: "release/phase2-install-prompt.md"
      to: "native Windows update"
      via: "prompt bytes/hash 在签名 handoff 内且只含 exact tag/asset/hash/trust root；用户在实际 Windows Codex 中逐字执行"
      pattern: "windows|sha256|cleanup"
    - from: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      to: "scripts/release/verify-phase2-update-gate.mjs"
      via: "post-update strict schema + fixed local discovery 读取 current managed runtime；不接受调用者 URL/port/path/PID/Profile/browser handle"
      pattern: "02-25-windows-update|WINDOWS_UPDATE_COMPLETE"
    - from: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      to: "02-26 eligibility"
      via: "receipt 与 post-update verifier 同时 pass 才允许下一计划；不自动启动登录或创建 live writer authority"
      pattern: "sourceConfiguration|schoolAccess|phase3"
---

<objective>
在用户未来明确恢复 Windows validation 后，重新选择并验证当时最新的不可覆盖 Phase 2 beta，向用户提供签名 handoff 中的 Windows 精确更新步骤，等待用户在原生 Windows 11 完成真实更新，再以严格只读门禁建立 02-26 可消费的脱敏 update receipt。

Purpose: 落实 AUTH-02、UAT-01 与 D-03/D-12/D-14/D-17–D-18，防止旧 beta、mutable latest、macOS安装、WSL、静态产物或自动 selfcheck 被冒充为 Windows 原生更新；让缺设备、未授权和任一失败继续保持真实 `not_run / human_needed`。

Output: Windows pre/post-update read-only verifier、完整负例、用户本人执行的 native Windows update checkpoint，以及仅 strict pass 后存在的 `.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json`。本计划现在不得执行；PLAN 获批不等于 02-24 resume。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@release/phase2-test-report.json
@release/phase2-beta-artifacts.json
@release/phase2-publication.json
@release/phase2-availability.json
@release/phase2-install-prompt.md
@scripts/release/verify-availability.mjs
@scripts/release/verify-phase2-update-gate.mjs
@scripts/release/phase2-live-gate.mjs
@tests/integration/phase2-release-gates.test.ts

<interfaces>
02-24 的唯一恢复授权必须已由 completed SUMMARY 脱敏记录，原始用户信号为：

```text
RESUME_PHASE2_WINDOWS_NATIVE_UAT
native_windows_11_available=yes
resume_windows_phase2=yes
allow_next_plan=02-25-only
macos_not_substitute=acknowledged
phase3_remains_blocked=acknowledged
```

02-25 不重新解释或放宽该信号。缺 `.planning/phases/02-poc-live/02-24-SUMMARY.md`、SUMMARY 未记录 fresh audit、native Windows 11 readiness、exact signal 或 `02-25-only`，一律输出安全码 `WINDOWS_RESUME_NOT_AUTHORIZED` 并零执行；PLAN批准、自由文本“继续”、macOS pass、设备自动发现或旧 resume 均无效。

Windows required-cell baseline 的 canonical 只读命令保持：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --audit-matrix --platform windows --runtime current --read-only
```

在 02-25 完成前 strict output 仍必须是 `{schema:1,status:'pass',audit:'platform_matrix',platform:'windows',requiredCellCount:88,notRunCellCount:88,humanNeededCellCount:88,passCellCount:0,macosWritesApplied:0,phase3:'blocked'}`；beta availability/update 不可改变任一 source scenario cell。

扩展既有 `scripts/release/verify-phase2-update-gate.mjs`，不另造 parallel release gate。它保留 02-13/02-14 macOS CLI/receipt 语义，并新增 `validatePhase2WindowsUpdateGateReceipt` 与 Windows pre/post mode。Windows pre-update canonical CLI 为：

```text
gate_tmp=$(mktemp -d)
node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs \
  --release release/phase2-beta-artifacts.json \
  --publication release/phase2-publication.json \
  --out "$gate_tmp/availability.json"
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --artifacts release/phase2-beta-artifacts.json \
  --tests release/phase2-test-report.json \
  --publication release/phase2-publication.json \
  --availability release/phase2-availability.json \
  --fresh-availability "$gate_tmp/availability.json" \
  --prompt release/phase2-install-prompt.md \
  --platform windows --read-only
```

pre-update exit 0 的单行 safe result 必须等价于：

```typescript
type Phase2WindowsPreUpdateGateResult = {
  schema: 1;
  status: 'pass';
  mode: 'pre-update';
  platform: 'windows';
  code: 'WINDOWS_UPDATE_READY';
  repositoryId: 1350421724;
  version: string;
  tag: string;
  buildId: string;
  artifactSha256: string;
  manifestSha256: string;
  signature: 'verified';
  availability: 'fresh_anonymous_full_bytes';
  support: 'windows_native_supported';
  windowsCells: '88/88 not_run/human_needed';
  phase3: 'blocked';
};
```

`version` 必须由固定 `returdex/AutoED`（repository ID `1350421724`）公开不可覆盖 release 集合按 SemVer 解析为当时最高的完整 `0.1.0-beta.N`，然后绑定 exact tag/asset，不调用 mutable `latest` URL。若公开更高 beta 存在但 canonical tests/artifacts/publication/availability/prompt、签名或 support 不完整，必须 `LATEST_BETA_HANDOFF_INCOMPLETE` 并阻断，不能静默回退到较旧 beta。若本计划对 verifier 的 Windows support 产生任何字节变化，而该字节/hash 尚未纳入 selected beta 的 signed source/release handoff，则必须回到完整 typecheck+unit+integration+UI+native+scan、以更高 beta.N 不可覆盖发布并 fresh reverify；不得在此 gate 内覆盖旧 tag/asset或把未签名本地脚本当发布证明。

用户反馈只接受以下 exact allowlisted block；不得附日志、截图、路径或自由文本：

```text
02-25_WINDOWS_UPDATE_RESULT
checkpoint=02-25-windows-update
platform=windows
version=<preflight exact version>
artifact_sha256=<preflight exact Windows asset SHA-256>
result=pass|failed|human_needed
result_code=WINDOWS_UPDATE_COMPLETE|WINDOWS_DEVICE_UNAVAILABLE|WINDOWS_AUTHORIZATION_REQUIRED|WINDOWS_UPDATE_FAILED|WINDOWS_CLEANUP_PENDING|WINDOWS_VERSION_MISMATCH|WINDOWS_SIGNATURE_MISMATCH|WINDOWS_BUILD_MISMATCH|WINDOWS_ENTRYPOINT_MISMATCH|WINDOWS_SERVICE_UNHEALTHY|WINDOWS_PAIRED_UI_UNAVAILABLE|WINDOWS_NATIVE_PLATFORM_NOT_CONFIRMED|WSL_NOT_ALLOWED|RESULT_NOT_OBSERVED
native_windows_11=confirmed|not_observed
wsl=excluded|detected|not_observed
cleanup=complete|cleanup_pending|not_observed
actual_build=matched|mismatch|not_observed
entrypoints=matched|mismatch|not_observed
api=healthy|unhealthy|not_observed
worker=healthy|unhealthy|not_observed
paired_ui=ready|unavailable|not_observed
source_configuration=moodle:not_confirmed|confirmed|not_observed,edstem:not_confirmed|confirmed|not_observed
school_access=not_started
profile=not_created
windows_source_evidence=not_run/human_needed
macos_not_substitute=acknowledged
phase3=blocked
observed_at=<ISO-8601 with timezone>
```

只有 exact pass conjunction 成立才原子创建以下唯一 machine handoff；failed/human_needed 时文件必须不存在，或已有 pass 文件 hash 保持不变且本轮不被当作新结果：

```typescript
interface Phase2WindowsUpdateGateReceipt {
  schema: 1;
  checkpoint: '02-25-windows-update';
  platform: 'windows';
  nativePlatform: {
    os: 'windows-11';
    kind: 'native';
    wsl: false;
    proof: 'managed-runtime-selfcheck';
  };
  repositoryId: 1350421724;
  version: string;
  tag: string;
  artifactSha256: string;
  manifestSha256: string;
  buildId: string;
  signature: 'verified';
  availability: 'fresh_anonymous_full_bytes';
  support: 'windows_native_supported';
  result: 'pass';
  resultCode: 'WINDOWS_UPDATE_COMPLETE';
  cleanup: 'complete';
  actualBuild: 'matched';
  entrypoints: 'matched';
  api: 'healthy';
  worker: 'healthy';
  pairedUi: 'ready';
  sourceConfiguration: { moodle: 'not_confirmed'; edstem: 'not_confirmed' };
  schoolAccess: 'not_started';
  profile: 'not_created';
  windowsSourceEvidence: 'not_run/human_needed';
  macosNotSubstitute: 'acknowledged';
  phase3: 'blocked';
  observedAt: string;
  feedbackDigest: string;
}
```

Windows post-update canonical read-only CLI 为：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json \
  --artifacts release/phase2-beta-artifacts.json \
  --availability release/phase2-availability.json \
  --runtime current --platform windows --read-only
```

post-update exit 0 的单行 safe result 必须是 `{schema:1,status:'pass',mode:'post-update',platform:'windows',code:'WINDOWS_UPDATE_COMPLETE',nativePlatform:'windows-11',platformKind:'native',wsl:false,version,buildId,cleanup:'complete',api:'healthy',worker:'healthy',pairedUi:'ready',sourceConfiguration:'not_confirmed',schoolAccess:'not_started',profile:'not_created',windowsSourceEvidence:'not_run/human_needed',phase3:'blocked'}`。verifier 通过既有受管安装 inventory、固定 loopback discovery、signed status/selfcheck 与 paired static readiness 取证，不接受调用者提供 URL、port、installation root、PID、Profile、selector、browser handle、operation 或 source 参数，也不启动/更新/重启服务、打开浏览器/学校或写 EvidenceLedger。
</interfaces>

<hard_gates>
- 当前不得执行本计划。只有 completed 02-24 SUMMARY 证明用户未来主动发送 exact resume block、fresh 88-cell audit仍全缺口且 eligibility=`02-25-only`，执行器才可开始 Task 1；计划批准、macOS-first例外或自动设备检测不授权。
- D-03/D-17–D-18：原生 Windows 11 证据必须来自实际 Windows managed runtime；WSL/Linux/macOS、UA/env hint、静态 win32 artifact、CI或fixture不可替代。02-25前后 Windows 88个source evidence cells仍not_run/human_needed，Phase3 blocked。
- UAT-01：必须在人工动作前重新验证 then-latest exact immutable beta 的全部测试、签名/hash/build/support、publication与fresh anonymous完整字节availability；任何更新后的beta handoff不完整时不得回退旧beta。
- 用户本人在原生 Windows Codex 中执行 signed prompt 并处理OS授权。执行器不得远程控制设备、替用户批准、请求/读取密码/MFA、采集输入/截图/log/raw network、创建Profile、确认来源或访问学校。
- release/update authority 不等于 Moodle/EdStem N/L writer。不得调用 EvidenceLedger append、phase2 live record、login/probe或source action；02-25 pass 只释放02-26 eligibility，仍不得自动执行02-26。
- 任一 High/Critical threat、test、scan、availability、native platform、update或post gate失败即阻断；保留failed/human_needed/not_run，不得覆盖已发布tag/asset、修正失败receipt或把cleanup_pending称为成功。
</hard_gates>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-25-1：在 future resume 后扩展并运行严格 Windows update preflight</name>
  <files>scripts/release/verify-phase2-update-gate.mjs, tests/integration/phase2-release-gates.test.ts</files>
  <read_first>AGENTS.md 全文，尤其 Execution after plan approval、Data integrity and verification、Installation/upgrades/releases 与 Current stop condition；02-CONTEXT D-03/D-12/D-14/D-17–D-18；02-RESEARCH 的 T2-02/T2-08、Validation Architecture 与 beta sequence；02-VALIDATION 的 Beta gate/Hard Gates/Windows matrix；02-PATTERNS 的 release/evidence/human gate；02-13/02-14/02-24 PLAN 与 completed SUMMARY；02-24 exact resume/88-cell contract；现有 release receipts、install prompt、availability/update/live gate 实现与其测试。禁止读取 legacy、Profile/Cookie、runtime DB/WAL、学校页面、身份/课程或私人路径。</read_first>
  <behavior>
    - "无 completed 02-24 SUMMARY、resume非exact/非本轮、eligibility非02-25-only、88 cells非全缺口 -> WINDOWS_RESUME_NOT_AUTHORIZED，所有release/network/runtime/write计数为0。"
    - "公开最高完整0.1.0-beta.N + exact canonical handoff + fresh anonymous full bytes + Windows support -> WINDOWS_UPDATE_READY；更高beta不完整时阻断而不回退。"
    - "mutable latest、错误owner/repository ID、tag/version/asset/build/hash/signature/prompt/support drift、partial/authenticated/stale availability -> safe nonzero code，receipt与runtime不变。"
    - "platform=macos/linux/wsl、browser/UA/env伪装、静态Windows artifact -> 不得满足native Windows 11或post gate。"
    - "post receipt/runtime 任一schema/platform/build/cleanup/service/UI/config/access/profile/evidence/gate不匹配 -> nonzero；不spawn/restart/fetch school/write ledger。"
  </behavior>
  <action>先验证执行时 `.planning/phases/02-poc-live/02-24-SUMMARY.md` 已真实存在且只记录用户 exact future resume、native Windows 11 available、`02-25-only` eligibility、fresh 88/88 `not_run/human_needed`、Phase1 partial/Phase2 incomplete/sharedProfile unverified/Phase3 blocked；缺失、非exact、SUMMARY早于resume、任何Windows pass或更宽授权时立即安全失败，release查询/download/update/device/job/action调用均为0。当前没有该SUMMARY时本任务必须保持未执行，不能为了测试创建伪SUMMARY到planning目录。

按 TDD 先在 `phase2-release-gates.test.ts` 写 RED，再扩展既有 `verify-phase2-update-gate.mjs`；保留macOS pre/post API与 `phase2-gate.mjs` 职责，不创建第二套gate。增加 `<interfaces>` 的 Windows CLI/schema/strict output。argv strict allowlist在任何 discovery/fetch 前拒绝缺 `--read-only`、额外参数、任意URL/port/path/PID/Profile/selector/browser handle/operation/source；error只输出stable code。post mode必须从受管安装固定discovery读取已签名 current status/selfcheck/paired static readiness和native Windows platform attestation，证明Windows 11、native、非WSL；不得相信receipt/用户/platform字符串单独断言。

negative matrix至少逐项覆盖：02-24 SUMMARY missing/extra/stale/wrong resume/eligibility；public release集合空、顺序/semver异常、mutable latest、错误owner或repository ID、同tag conflict、较高beta handoff missing/partial而错误回退；tests/artifacts/publication/committed+fresh availability/prompt缺失/extra/schema drift；asset id/name/bytes/hash、manifest/hash、Ed25519 fingerprint/signature、commit/tree/buildId、Windows target/architecture/dependency closure/support matrix、PolyForm license、prompt bytes或embedded exact值不一致；fresh receipt非anonymous/非full-byte/超过10分钟/未来/redirect越界；Windows bootstrap未签名或verifier bytes未进入selected signed source handoff。post matrix覆盖receipt missing/extra/unknown enum、feedbackDigest非SHA-256、observedAt无timezone/未来、version/tag/hash/build mismatch、platform非Windows 11/native、WSL、cleanup_pending、actual build/entrypoint/API/Worker/UI任一非pass、source已confirmed、school access已开始、Profile已创建、Windows evidence被填、Phase3抬高、current runtime absent/stale，以及恶意exception注入path/PID/Profile/identity/course/token/raw response。每个失败都断言输入/receipt/Git/remote/runtime/entrypoints/process/pairing/L rows/Windows 88-cell baseline前后不变。

通过测试后，按固定public repo集合解析最高SemVer beta，并要求 canonical handoff恰指向该exact version。运行完整preflight CLI与Windows matrix audit：先重新验证tests/artifacts/publication/committed availability/prompt；再以`mktemp` fresh anonymous获取每个exact asset全字节，逐项复验redirect allowlist、size/SHA-256、两manifest Ed25519 signature/fingerprint、archive closure、build/support/license/prompt；最后pre gate输出WINDOWS_UPDATE_READY且88格仍全缺口。若本任务代码变化尚未包含于selected beta signed handoff，或当时出现更高但不完整beta，停止为human_needed并回到完整test/scan/新更高beta.N不可覆盖publish/availability流程，之后从头重跑；绝不就地修改release receipt、覆盖remote或降级旧beta。只有全部通过才向Task 2交付签名prompt原文字节及safe public version/tag/Windows asset SHA-256/manifest SHA-256/buildId；不写update receipt或source evidence。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "Windows update gate|latest immutable beta|native platform|read-only" &amp;&amp; gate_tmp=$(mktemp -d) &amp;&amp; trap 'rm -rf "$gate_tmp"' EXIT &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs --release release/phase2-beta-artifacts.json --publication release/phase2-publication.json --out "$gate_tmp/availability.json" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --artifacts release/phase2-beta-artifacts.json --tests release/phase2-test-report.json --publication release/phase2-publication.json --availability release/phase2-availability.json --fresh-availability "$gate_tmp/availability.json" --prompt release/phase2-install-prompt.md --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. 02-24 SUMMARY exact证明用户future resume、native Windows 11 available、02-25-only eligibility与fresh 88-cell全缺口；缺失/非exact时preflight在release/network/runtime activity前以WINDOWS_RESUME_NOT_AUTHORIZED退出。
    2. `rg -n "validatePhase2WindowsUpdateGateReceipt|WINDOWS_UPDATE_READY|02-25-windows-update" scripts/release/verify-phase2-update-gate.mjs tests/integration/phase2-release-gates.test.ts` 找到唯一canonical Windows schema/gate测试；未新增第二个gate脚本，既有macOS测试仍pass。
    3. then-latest解析只接受固定returdex/AutoED repository ID 1350421724公开immutable `0.1.0-beta.N` exact tag；不使用latest别名。公开更高beta缺任一handoff时返回LATEST_BETA_HANDOFF_INCOMPLETE，不能选择旧beta。
    4. tests/artifacts/publication/availability/prompt在version/tag/commit/tree/build/asset/signature/fingerprint/hash/Windows support/license上完全一致；selected verifier bytes/hash包含于signed handoff，否则保持human_needed直到新beta完整发布。
    5. fresh availability≤10分钟、非未来、anonymous、所有资产完整字节；URL从固定receipt派生并限制GitHub HTTPS release/CDN allowlist，无caller URL/credential/cookie，任一redirect/size/hash/signature/closure错误nonzero。
    6. preflight exact CLI exit 0并只输出Phase2WindowsPreUpdateGateResult allowlisted字段；stdout/stderr无path/PID/port/Profile/identity/course/token/raw HTTP。输入、Git/remote/runtime/ledger/88 cells前后不变。
    7. post-mode所有negative fixture nonzero；Windows native proof只来自managed current runtime且要求Windows 11/native/非WSL，静态artifact、UA/env、macOS/Linux/WSL不能pass；write/spawn/restart/browser/school request/ledger append计数为0。
    8. Windows audit在preflight前后均精确required/notRun/humanNeeded=88、pass=0、macosWritesApplied=0、phase3=blocked；Task 1不创建02-25 receipt、不开始安装/login/Profile/source动作、不自动执行02-26。
  </acceptance_criteria>
  <done>future resume authority、then-latest exact immutable Windows beta、signed prompt与fresh anonymous availability均被strict只读验证；Windows matrix仍全缺口，执行器只可进入人工更新checkpoint。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-25-2：等待用户在原生 Windows 11 获取并更新 exact beta</name>
  <files>.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json（仅strict pass后创建）</files>
  <read_first>Task 1 的本轮WINDOWS_UPDATE_READY safe result、exact signed `release/phase2-install-prompt.md` Windows章节与fresh availability；02-24 SUMMARY 的02-25-only授权；AGENTS.md 的human update、managed cleanup、credentials/Profile、Windows native evidence与failed gate规则；02-14 macOS update receipt模式只作schema隔离参考，绝不能复用其platform/evidence。</read_first>
  <what-built>当时最新完整不可覆盖beta的Windows exact tag/asset/hash/manifest/signature/build/support与匿名可获取性已fresh复验；Windows 88个source evidence cells仍not_run/human_needed，尚未安装、登录或访问学校。</what-built>
  <action>只向用户显示 Task 1 验证过的公开version/tag、Windows asset SHA-256、manifest SHA-256、buildId/support和签名prompt原文，不重新拼接URL/hash/步骤。明确这次授权只覆盖02-25 Windows beta获取/原生更新，不授权来源确认、Profile创建、学校登录/MFA、probe、课程访问或02-26+。用户必须在真正运行AutoED的原生Windows 11 Codex中本人执行prompt；WSL/Linux/macOS/远程CI/仅下载不能使用。

用户步骤必须精确为：
1. 核对屏幕上的exact version/tag与Windows asset SHA-256等于prompt；任一不一致立即回复`human_needed/WINDOWS_VERSION_MISMATCH`或`WINDOWS_SIGNATURE_MISMATCH`，不要下载/运行。
2. 在原生Windows 11的实际Codex中新开更新任务，逐字粘贴已验证prompt；不替换tag/URL/hash/trust key，不使用latest，不把prompt交给macOS/WSL或另一台未授权设备。
3. 用户本人决定prompt明确列出的Windows OS/安装授权；不向agent粘贴系统密码、token、主机名、用户名、路径、IP、日志、截图。若要求学校登录、创建Profile、访问legacy、删除课程资料或额外未知授权，立即停止并报告WINDOWS_AUTHORIZATION_REQUIRED。
4. 等待受管更新完成预览、数据保护/迁移或恢复、启动、自检，以及旧受管program/entrypoint/process清理。cleanup_pending、rollback、失败、卡住、只下载或只解压均不是pass。
5. 用prompt的固定只读检查核对实际运行version/build、受管entrypoints、API/Worker与paired UI；不得输入caller URL/port/path。保持Moodle/EdStem source_configuration都not_confirmed、school_access=not_started、profile=not_created，不点击login/probe。
6. 从脱敏结果填写完整`02-25_WINDOWS_UPDATE_RESULT`；未观察字段必须写not_observed，不能猜matched/healthy/ready。不要附自由文本、截图、完整日志、路径、PID/Profile、账户/身份/学校/课程或凭据。

收到反馈后strict parse exact keys/enums/ISO timezone，并与本轮preflight exact version/artifact SHA-256交叉核对。`WINDOWS_DEVICE_UNAVAILABLE`、`WINDOWS_AUTHORIZATION_REQUIRED`、任何failed/human_needed/not_observed/mismatch/unhealthy/unavailable/cleanup_pending、WSL、source confirmed、school access/profile已开始、Windows evidence变化或Phase3非blocked都保持checkpoint未完成和`not_run/human_needed`；不创建/改写receipt、不尝试远程修复、不自动执行02-26。若产品缺陷需修复，走完整测试/scan/更高beta.N不可覆盖发布/fresh availability，再重新从Task1执行。

仅当所有pass字段同时成立，使用strict schema从allowlisted反馈与signed handoff原子创建`02-25-WINDOWS-UPDATE-RECEIPT.json`；nativePlatform proof不能仅从用户字段生成，必须由post gate读取实际managed runtime确认为Windows 11/native/非WSL。`feedbackDigest`只哈希规范化allowlisted block，不复制原消息。立即运行canonical post-update CLI；只有其exit 0且safe output精确WINDOWS_UPDATE_COMPLETE才完成checkpoint。update receipt不调用EvidenceLedger，不是AUTH-02 source N/L receipt；Windows 88格仍not_run/human_needed，Phase1 partial、Phase2 incomplete、sharedProfile unverified、Phase3 blocked。完成最多使02-26 eligible，必须停下等待02-26自身批准/人工登录门，不得自动执行。</action>
  <how_to_verify>
    1. 用户在原生Windows 11实际Codex中逐字执行显示的signed prompt，而不是回复“批准/成功”、只看release页、只下载资产或在macOS/WSL执行。
    2. 用户核对cleanup、actual build、entrypoints、API、Worker、paired UI；不创建Profile、不确认来源、不打开学校、不登录/MFA。
    3. 用户只回复完整`02-25_WINDOWS_UPDATE_RESULT`脱敏块；exact version与artifact SHA-256匹配Task1，未知项写not_observed。
    4. 缺设备/未授权/失败时如实回复对应result_code并保持等待；不要提供设备名、账户、路径、IP、截图、日志、Profile/Cookie或凭据。
  </how_to_verify>
  <verify>
    <automated>暂停前原样重跑Task 1的fresh availability、Windows pre-update gate与88-cell audit并全部exit 0；恢复后strict parser校验`02-25_WINDOWS_UPDATE_RESULT`。仅pass conjunction成立时运行 `node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only`；用户实际Windows更新无自动替代，必须等待真实反馈。</automated>
  </verify>
  <acceptance_criteria>
    1. checkpoint前再次fresh复验then-latest exact beta，prompt bytes/signature/version/tag/Windows asset SHA-256/manifest/build/support全部相等；任一新beta、过期availability或drift都回Task1，不显示旧prompt。
    2. 用户明确报告在原生Windows 11实际Codex执行exact prompt；macOS/WSL/Linux/CI/static artifact/download/publication/selfcheck单独均不能满足。
    3. feedback字段完整且exact：result=pass、result_code=WINDOWS_UPDATE_COMPLETE、native_windows_11=confirmed、wsl=excluded、cleanup=complete、actual_build/entrypoints=matched、API/Worker=healthy、paired_ui=ready。
    4. source_configuration恰为两来源not_confirmed、school_access=not_started、profile=not_created、windows_source_evidence=not_run/human_needed、macos_not_substitute=acknowledged、phase3=blocked；未打开学校/登录/probe或写N/L。
    5. strict pass后receipt精确满足Phase2WindowsUpdateGateReceipt，version/tag/artifact/manifest/build/signature/support与signed handoff相同；nativePlatform由managed runtime证明Windows11/native/wsl=false，receipt无自由文本、path/PID/Profile/identity/course/log/screenshot/secret。
    6. canonical post gate exit 0且safe stdout精确WINDOWS_UPDATE_COMPLETE/current build/cleanup/services/UI/config/access/profile/evidence/gate；verifier运行前后runtime/entrypoint/pairing/ledger/Windows88 cells不变，未spawn/restart/open browser/fetch school。
    7. post gate后Windows matrix仍required/notRun/humanNeeded=88、pass=0，Phase1 partial、Phase2 incomplete、sharedProfile unverified、Phase3 blocked；update receipt只使02-26 eligible，不使AUTH-02或Phase2通过。
    8. 任一缺设备/未授权/failed/human_needed/not_observed/mismatch/unhealthy/unavailable/cleanup_pending/WSL/敏感反馈保持checkpoint blocking；receipt与success SUMMARY不存在，不自动批准、降级旧beta或执行02-26。
  </acceptance_criteria>
  <resume-signal>只回复完整`02-25_WINDOWS_UPDATE_RESULT`块。只有所有exact pass字段、actual native Windows runtime proof与canonical post gate同时通过才会恢复；`approved`、自由文本成功、macOS/WSL结果、release链接、截图或日志均不会解除门禁。</resume-signal>
  <done>用户已在原生Windows 11实际Codex获取并更新then-latest exact不可覆盖beta，managed current runtime证明build/cleanup/entrypoints/services/paired UI正确且学校/Profile/source尚未开始；strict receipt与post gate通过后仅释放02-26 eligibility。否则保持not_run/human_needed。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Required control |
|---|---|---|
| 02-24 resume/SUMMARY → 02-25 scheduler | 计划批准、自由文本或设备发现被冒充未来Windows授权 | exact resume已由completed 02-24 SUMMARY记录；02-25-only；无default/timeout/auto路径 |
| public release set → selected beta | mutable latest、旧缓存、同tag冲突或不完整更高beta造成回退/降级 | 固定repository ID；SemVer最高exact immutable tag；更高不完整即阻断；签名handoff全绑定 |
| publication/availability → executable bytes | page/partial/authenticated download或redirect替换被当作可获取 | fresh匿名完整字节；redirect allowlist；size/SHA-256/Ed25519/closure逐项复验 |
| signed prompt → human Windows action | 步骤被重写、URL/hash/trust root被替换或扩大授权 | prompt bytes纳入signed manifest；逐字显示；只授权Windows update，不授权学校/Profile/source |
| human feedback → update receipt | 猜测、自由文本、macOS/WSL结果或敏感内容被转换为pass | strict allowlisted schema/enums/exact hashes；native proof来自current managed runtime；敏感字段拒绝 |
| Windows managed runtime → native platform/build claim | UA/env/WSL/static artifact/另一个安装冒充原生Windows 11 current build | 固定managed inventory/loopback discovery；signed status；native kind + WSL=false；exact build/entrypoints/services |
| update receipt → EvidenceLedger/phase gate | distribution/update被写成source N/L或Phase2/3 pass | no live writer；88 cells不变；Profile/source/school未开始；只释放02-26 eligibility |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking verification |
|---|---|---:|---|---|---|---|
| T2-25-AUTHZ | Spoofing / Elevation of Privilege / Repudiation | Critical | 02-24 resume → scheduler | mitigate | V1架构、V4访问控制、V13业务流程 | exact completed SUMMARY + 02-25-only；缺失时任何I/O前拒绝；无auto/default/timeout。任何未授权执行即Critical blocker。 |
| T2-25-SUPPLY | Tampering / Spoofing | Critical | release selection/asset/trust root | mitigate | V2供应链边界、V5验证、V14配置/完整性 | 固定returdex/repository ID；highest exact beta.N；immutable tag；Ed25519+SHA-256+manifest+closure+support；更高不完整不回退。任一drift阻断。 |
| T2-25-AVAIL | Spoofing / Information Disclosure | High | public availability fetch | mitigate | V5输入/重定向验证、V13数据保护、V16安全输出 | 无Authorization/Cookie fresh full-byte fetch；GitHub HTTPS allowlist；无caller URL；临时根清理；safe output。partial/cache/redirect异常阻断。 |
| T2-25-PLATFORM | Spoofing / Elevation of Privilege | Critical | Windows native assertion | mitigate | V2平台身份、V4授权、V14安全配置 | managed current runtime证明Windows11/native/Wsl=false；UA/env/static/macOS/Linux/WSL无效。非原生pass即Critical blocker。 |
| T2-25-FEEDBACK | Tampering / Repudiation / Information Disclosure | High | user feedback → receipt | mitigate | V5 strict schema、V13数据最小化、V16审计 | exact enum/conjunction/hash/time；feedbackDigest不复制raw input；拒绝path/PID/Profile/identity/course/log/screenshot/secret；not_observed不能pass。 |
| T2-25-RUNTIME | Tampering / Denial of Service | High | verifier → installed runtime | mitigate | V4最小权限、V13完整性、V14安全配置 | literal runtime=current、fixed discovery、只读；不spawn/restart/update/open browser；build/cleanup/services/UI必须current pass。写入或副作用即阻断。 |
| T2-08（继承） | Tampering / Repudiation / Elevation of Privilege | Critical | update receipt → evidence/phase gate | mitigate | V1信任级别、V4 writer authorization、V16 provenance | update无EvidenceLedger authority；88格前后全缺口；Phase3 blocked；只释放02-26。任一Mac/fixture/update→Windows source N/L或phase pass即Critical blocker。 |
| T2-02（继承） | Information Disclosure / Elevation of Privilege | Critical | prompt/output/Profile/credentials | mitigate | V6/V7会话数据、V13/V14保护、V16日志 | 不请求/读credentials/MFA/input；不创建/导出Profile；无HAR/trace/screenshot/raw logs/network；敏感输出负例/scan。任一泄露阻断。 |

所有 High/Critical 项必须 `mitigate`，不得 accept/transfer、降级warning或以未来计划修复后继续。ASVS 5.0 L1仅表示适用控制意图，不声明完整合规。自动preflight只能证明distribution readiness；实际Windows更新必须等待用户，且更新pass仍不是source N/L或Phase完成。
</threat_model>

<verification>
1. 先证明02-24 completed SUMMARY含exact future resume与02-25-only授权；否则02-25所有release/runtime/network/activity=0。
2. TDD运行Windows update gate正负矩阵，并回归既有macOS gate；所有schema/argv/safe-output/platform/read-only副作用检查通过。
3. 从固定公开release集合解析最高完整beta.N并绑定canonical handoff；不存在mutable latest/旧beta回退，代码若未入signed handoff则先新不可覆盖发布。
4. fresh匿名完整获取全部exact资产，验证redirect/size/hash/Ed25519/manifest/closure/build/support/license/prompt；pre gate返回WINDOWS_UPDATE_READY。
5. checkpoint前后运行Windows matrix audit；88/88持续not_run/human_needed、pass=0、Phase3 blocked，update不写source evidence。
6. 等待用户在原生Windows 11实际Codex执行signed prompt并返回strict safe block；缺设备/未授权/失败/未观察均保持blocking。
7. strict pass时创建唯一Windows update receipt并运行canonical post gate；managed current runtime证明Windows11/native/non-WSL、exact build、cleanup、entrypoints、services、paired UI。
8. 最后扫描receipt/SUMMARY/gate output无路径/PID/Profile/身份/课程/凭据/log/screenshot/raw network；只允许02-26 eligible，停止而不执行。
</verification>

<success_criteria>
- AUTH-02：用户确实在原生Windows 11运行then-latest exact beta；这只建立Windows update/readiness前置，全部source×scenario N/L仍待02-26–02-33独立验证。
- UAT-01：人工更新前已验证不可覆盖发布、签名/hash/build/support、fresh anonymous availability与exact prompt；缺设备、未授权、失败或未观察均是not_run/human_needed。
- D-03/D-17：macOS、WSL/Linux、fixture、CI、静态Windows artifact与publication不能代替native Windows update；Windows 88 source cells不被update receipt代填。
- D-12/D-14：用户本人执行OS/update动作；反馈、receipt与SUMMARY只含allowlisted脱敏字段，未请求/读取凭据/MFA/input/截图/log/raw network/Profile资料。
- D-18/T2-08：Phase1 partial、Phase2 incomplete、sharedProfile unverified、Phase3 blocked；02-25 strict pass最多使02-26 eligible，不能自动执行后续计划。
- 所有High/Critical threat tests、pre/post gates与敏感输出检查通过；任一失败即保持checkpoint未完成且成功receipt/SUMMARY不存在。
</success_criteria>

<output>
仅在 strict pass conjunction、`.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json` 与 canonical post gate 全部通过后创建 `.planning/phases/02-poc-live/02-25-SUMMARY.md`。SUMMARY 只记录公开version/tag/artifact SHA-256/manifest SHA-256/build、native Windows11/non-WSL、cleanup/entrypoints/API/Worker/paired UI、source_configuration=not_confirmed、school_access=not_started、profile=not_created、Windows source evidence仍not_run/human_needed、Phase1/2 incomplete、sharedProfile unverified、Phase3 blocked，以及02-26 eligible但未执行。缺设备/未授权/failed/human_needed时不创建成功receipt或SUMMARY；不得记录主机名、用户/路径/IP/PID、Profile/Cookie、身份/学校/课程、凭据、日志、截图或raw network。
</output>
