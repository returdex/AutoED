---
phase: 02-poc-live
plan: "11"
type: execute
wave: 8
depends_on:
  - "02-06"
  - "02-08"
  - "02-09"
files_modified:
  - packages/test-support/src/auth-e2e.ts
  - tests/ui/auth-e2e.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "真实 UI Chromium 经 paired loopback API、durable auth job/Worker、sealed source adapter 与独立 synthetic source Chromium 完成全链路；测试仅访问受控 loopback/内存 fulfill 页面，真实 Moodle、EdStem、学校 DNS/代理请求始终为零。"
    - "同 origin redirect 与来源专属正面 marker 缺一不可认证；页面/HTTP 200 只能显示导航或健康观察，不能让 Moodle/EdStem 状态或总体门禁变成 authenticated/pass。"
    - "用户可见进度严格为 Moodle probe → Moodle 正面认证 → EdStem probe → 两来源结果；EdStem 不会在 Moodle 成功前被探测，额外交互/MFA/停止会立即停住且不出现伪完成。"
    - "严格稳定主体+组织+批准范围只形成待确认候选；确认后才允许课程可见性动作；同名/同邮箱但稳定主体不同进入 identity_mismatch，课程访问和继续动作全部禁用且不覆盖旧绑定。"
    - "busy、停止、焦点、键盘和窄屏均由真实浏览器行为验证：重复点击/Enter 只发一个请求，取消在 Worker 确认前只显示已请求停止，结果后焦点落在总体结果标题，320/599/759/1280px 无横向溢出。"
    - "401/403 在同一 UI turn 清空 protected 内存、DOM、pending action/candidate 和 clipboard source；完整身份只存在于当前 paired protected visible text，永不进入 attributes/ARIA/live-region/storage/URL/console/receipt。"
    - "A、B1-1/B1-2/B1-3、B2、B3、C、D、reauth 的 platform/source/evidence cells 不可合并；合成时钟不能绕过 D 的 24 小时门，Windows 始终 not_run/human_needed，Phase 3 始终 blocked。"
    - "本计划所有产物与结果只标 S/UI（必要的跨层断言可标 I），绝不标 N/L、真实登录、真实重启、真实 24 小时或 live pass；macOS-first 例外不改变 Windows 与 Phase 3 硬门。"
  artifacts:
    - path: "packages/test-support/src/auth-e2e.ts"
      provides: "全本地双浏览器/loopback API/Worker/SQLite/source pages 组合 harness、可控时钟、网络与敏感输出审计"
      exports:
        - createSyntheticAuthE2E
      contains: "realSchoolRequests"
    - path: "tests/ui/auth-e2e.spec.ts"
      provides: "来源探针到 paired UI 的全链路顺序、绑定、安全清理、证据门禁、键盘与响应布局 E2E"
      contains: "describe('synthetic auth browser to paired UI E2E'"
  key_links:
    - from: "tests/ui/auth-e2e.spec.ts"
      to: "packages/test-support/src/auth-e2e.ts"
      via: "每个场景创建独立受管临时根、DB、API/Worker/source browser/UI browser 并在 finally 验证关停和零外网"
      pattern: "createSyntheticAuthE2E"
    - from: "packages/test-support/src/auth-e2e.ts"
      to: "packages/platform/src/source-adapters.ts"
      via: "02-06 local positive/malicious pages 通过 sealed 四动作与 official-origin逐跳校验进入 SourceProbeResult；不绕过 adapter 注入结果"
      pattern: "SealedSourceAdapters|createMaliciousSourceFixture"
    - from: "packages/test-support/src/auth-e2e.ts"
      to: "packages/application/src/auth-jobs.ts"
      via: "UI fixed route 产生 durable AuthJob，测试驱动真实 AuthJobRunner.runOnce，状态提交再由 API presenter 读取"
      pattern: "AuthJobService|AuthJobRunner"
    - from: "tests/ui/auth-e2e.spec.ts"
      to: "apps/status/src/main.ts"
      via: "实际构建 assets + HTTP pairing，不直接调用 render function 或注入 DOM state"
      pattern: "buildStatusAssets|/status"
    - from: "tests/ui/auth-e2e.spec.ts"
      to: "packages/persistence/src/auth.ts"
      via: "逐 cell 读取 append-only synthetic receipt/gate projection，证明场景/平台/evidence 不可串格"
      pattern: "not_run|human_needed|phase3"
---

<objective>
用全本地 synthetic browser/UI E2E 证明 Phase 2 的关键用户路径与安全不变量在真实边界接线后仍成立：official-origin/positive-marker 探针按 Moodle→EdStem 顺序推进，身份绑定/冲突、取消/失权、protected UI 隐私和不可串格证据门禁从来源页面一直贯穿到 paired 页面。

Purpose: 落实 D-04–D-18 与 AUTH-01/AUTH-02/AUTH-03/SEC-02/UAT-01，在 beta assembly 与任何官方登录之前，以行为证据阻断 T2-01 任意来源操作、T2-02 敏感资料泄漏、T2-07 protected UI 越权和 T2-08 synthetic/native/live/平台串格。
Output: 一个只在测试使用的全栈 auth E2E harness 与一个 Playwright E2E spec。本计划不修改生产行为、不接触真实学校/Profile/账户、不发布、不登录、不执行 native/live 人工场景。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-DISCUSSION-LOG.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-PLAN.md
@.planning/phases/02-poc-live/02-02-PLAN.md
@.planning/phases/02-poc-live/02-03-PLAN.md
@.planning/phases/02-poc-live/02-04-PLAN.md
@.planning/phases/02-poc-live/02-05-PLAN.md
@.planning/phases/02-poc-live/02-06-PLAN.md
@.planning/phases/02-poc-live/02-07-PLAN.md
@.planning/phases/02-poc-live/02-08-PLAN.md
@.planning/phases/02-poc-live/02-09-PLAN.md
@playwright.config.ts
@packages/test-support/src/harness.ts
@packages/test-support/src/auth-fixture.ts
@tests/integration/source-adapters.test.ts
@tests/integration/auth-worker.test.ts
@tests/integration/auth-api.test.ts
@tests/ui/status.spec.ts
@tests/ui/auth-status.spec.ts
@tests/ui/accessibility.spec.ts

<interfaces>
执行时先读取 02-06/02-08/02-09 SUMMARY 与实际导出；若名称有机械变化，只适配 import，不得重建平行状态、直接写 projection 或绕过真实边界：

```typescript
export class SealedSourceAdapters implements SourceProbePort {
  probe(request: SourceProbeRequest, signal: AbortSignal): Promise<SourceProbeResult>;
}

export interface AuthJobService {
  requestProbe(command: AuthProbeCommand, context: WriteContext): Promise<{ jobId: string }>;
  recordExplicitLogout(source: SourceId, context: WriteContext): Promise<AuthFlowState>;
  query(jobId: string, source: SourceId): Promise<AuthJob | null>;
  cancel(jobId: string, source: SourceId, context: WriteContext): Promise<AuthJob>;
}
export class AuthJobRunner {
  runOnce(owner: string, context: WriteContext): Promise<AuthJob | null>;
}

export function registerAuthRoutes(/* fixed paired routes only */): void;
export interface ProtectedAuthStatusProjection { /* paired local UI only */ }
export interface RedactedEvidenceReceipt { /* no full identity/course/Profile/raw page */ }
```

02-08 固定 API 与 02-09 UI contract 是唯一控制面：

```text
GET  /api/auth/status
GET  /api/auth/receipts?{exact EvidenceCellKey}
POST /api/auth/configuration/confirm
POST /api/auth/login/open
POST /api/auth/probe
POST /api/auth/logout-intent
POST /api/auth/binding/confirm
```

现有 `syntheticTest` 只保护 UI browser context；本计划的 harness 还必须对 source browser/provider 增加独立 route deny-all、closed local proxy 与 request audit。只有由 harness 显式 fulfill 的 synthetic `.invalid` official origins 和 API/UI loopback origin 可访问；不得把真实 Moodle/EdStem hostname 写入可发请求的配置、DNS、hosts 或 proxy bypass。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-11-1：组合全本地双浏览器 E2E 并证明顺序、正面标志、绑定与停止语义</name>
  <read_first>AGENTS.md 全文，尤其 Credentials and login、Source and content policy、Data integrity and verification；Phase 2 全部 CONTEXT/RESEARCH/VALIDATION/PATTERNS/UI-SPEC/PLAN-OUTLINE；02-01 至 02-09 PLAN 与已存在 SUMMARY；02-06 的 `auth-fixture.ts`/`source-adapters.test.ts`；02-07 的 `auth-jobs.ts`/`auth-worker.test.ts`；02-08 的 auth API integration；02-09 的 UI spec；playwright.config.ts、test-support/harness.ts、tests/ui/status.spec.ts。不得读取 legacy Profile、真实浏览历史或私人样本。</read_first>
  <files>packages/test-support/src/auth-e2e.ts, tests/ui/auth-e2e.spec.ts</files>
  <behavior>
    - RED：harness 真实启动临时 SQLite/API、AuthJobService/Runner、sealed adapters、独立 source Chromium context、built status assets 与 UI Chromium；UI 只经 HTTP pairing/fixed routes 驱动，测试不得直接调用 presenter/render 或写 DOM/projection 假装进度。
    - RED：Moodle direct/same-origin 1–3 hop redirect + positive marker 才能 authenticated；任一 hop 越界、HTTP 200 无 marker、marker ambiguity/parser drift 均 fail closed，EdStem probe/job/request count 为 0。
    - RED：成功轨迹可按 UI 观察 `正在检查 Moodle` → Moodle authenticated/EdStem not_observed → `正在检查 EdStem 登录状态；不会读取课程内容。` → EdStem authenticated；事件/请求/持久化时间顺序严格且每步不跳跃。
    - RED：需要 popup/interaction/MFA 时立即 human_needed，只有用户明确 UI 动作才能继续；后台不弹出 UI official login，fixture 不输入 password/MFA，不生成或记录 input/keystroke。
    - RED：stable subject+organization+scope 完整一致只产生 binding candidate；paired UI 确认 opaque candidate 后才允许一个 approved course visibility probe。相同 display name/email 但 stable subject 或 organization 不同得到 identity_mismatch、course requests=0、旧 confirmed binding 未覆盖。
    - RED：probe busy 时 click/Enter 重复激活只入队/执行一个 job；停止先显示 `已请求停止`，真实 Worker cancel/abort/commit 后才显示 `已停止`；停止、AbortSignal、lease/generation/fence 变化后 source request 与 store commit 计数不再增加。
    - RED：source/UI 两个 browser context 与 Node HTTP audit 均只见 loopback/明确 synthetic `.invalid` fulfill；真实学校 host、DNS、代理、service worker、WebSocket 和未映射 request 为 0/abort，trace/video/screenshot/HAR/console/request-body capture 为 0。
  </behavior>
  <action>先创建 `tests/ui/auth-e2e.spec.ts` 的 RED cases，再创建 `packages/test-support/src/auth-e2e.ts` 使其 GREEN。此 task 以 D-04–D-07 固定 protected identity/严格绑定/mismatch，以 D-08–D-11 固定 Profile 复用仍待验证、后台不弹窗、显式登出/自然过期分离、单来源隔离和三次恢复上限，并以 D-13 把课程动作限制为一个 approved visibility probe；测试不得把这些决策缩成单一 logged-in/ready。`createSyntheticAuthE2E()` 必须组合真实 02-02 SQLite auth stores/ledger、02-07 SQLiteAuthJobStore/AuthJobService/AuthJobRunner、02-06 SealedSourceAdapters、02-08 actual API/pairing、02-09 built status assets；除可替换 Clock、OS/native ownership proof 和 02-06 synthetic source page transport 外，不得以 fake presenter、直接 DB projection、route-level canned success 或 DOM injection 替代生产边界。每个测试使用 `createHarness()` 独立 0700 临时根/数据库，固定 synthetic installation/build/config/scope/source identities，finally 关闭 UI/source contexts、API、Worker/runner、DB 并清理目录；不得读取或写入真实 runtime/Profile。

复用 02-06 `createMaliciousSourceFixture` 的 direct/same-origin redirect/cross-origin/no-marker/positive/interaction/parser/course variants，通过 source browser context 的 `route.fulfill` 服务两个独立 `.invalid` origins。增加进程内 request audit：记录 context/source/action/method/origin/classification 但不保存 body/header/page text；未映射请求先计数后 abort。source context 另设 closed local proxy、serviceWorkers=block、acceptDownloads=false、trace/video/screenshot/HAR/console recording off。不得使用真实学校 hostname 做“应该被阻断”的主动请求；以预定义 `realSchoolRequests` 计数器和禁止 host-pattern guard 证明任何代码路径尝试构造真实目标即在网络层前失败。

测试通过 UI 完成现有 pairing，调用 configuration/login/probe/binding/stop fixed routes，并用显式 runner pump 驱动 durable job；每个 pump 前后等待 UI 可观察中间 projection，而不是一次性等待最终结果。顺序断言同时检查 UI 文本、AuthJob source/action、adapter调用日志、SourceObservation current/last-success 与 request audit 时间。Moodle 只有 observed origin 全链一致、positive marker 唯一且 bounded identity evidence 可用时才产生 EdStem follow-up；200/no marker、跨 origin、parser/interaction/MFA 都停止。测试不得以页面 URL 或网络成功手工写 authenticated。

binding cases 至少覆盖：严格 evidence→candidate→paired confirm→一个 approved course visibility；证据不足→manual confirmation；same name/email + different stable subject；different organization；confirmed identity drift。后 3 类必须显示 mismatch/hard stop、courseAccess blocked、course request count=0、旧 binding/last-success 保留。candidate confirm 只点击 02-09 UI 并提交 server opaque id，不在 fixture 自造 confirmed binding。停止测试在 navigate/wait/read/commit 各 barrier 注入可控阻塞与 Abort/fence loss；直到 Runner 返回 cancelled 才允许 UI 显示停止完成，旧 Worker 后续请求/提交为零。所有测试结果 metadata 固定 `evidence:'S'`（跨层 integration 可另记 I），不得调用 N/L authority 或生成 pass receipt。
  </action>
  <verify>
    <automated>npm run test:ui -- --grep "auth E2E local containment|official redirect and marker|Moodle then EdStem|binding and mismatch|busy stop and fence" &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    1. `createSyntheticAuthE2E` 实际组合 API/SQLite/AuthJobRunner/SealedSourceAdapters/built UI/source+UI Chromium；测试中不存在直接调用 `render*`、`present*`、`SourceObservationStore.write` 或 `AccountBindingStore.write` 来制造成功状态的路径。
    2. Moodle/EdStem direct 与同 origin 1/2/3 hop+positive marker 通过；200/no marker、任一 redirect hop/final origin 越界、ambiguous/oversize marker 逐项保持 unauthenticated/not_observed/parser/origin safe state，后续 EdStem/locator/course request=0。
    3. 进度测试在一个用例中捕获至少四个严格有序 UI/ledger/job observations：Moodle running、Moodle authenticated、EdStem running、EdStem authenticated；EdStem adapter request timestamp 必须晚于 Moodle positive result commit。
    4. interaction/MFA fixture 显示 human_needed 且后台新 page/连续 popup/probe=0；fixture/API/DOM/log 没有 password、MFA、input value 或 keystroke 字段/值。
    5. binding matrix 对 strict candidate、manual confirmation、same-name/different-subject、organization mismatch、confirmed drift 均通过；只有 paired confirm 后 course visibility count=1，所有 mismatch case 为 0 且旧 confirmed binding/last-success 深度相等。
    6. 双击/Enter busy case 的 API enqueue、AuthJob row、adapter probe 均恰为 1；stop 在四个 barrier 的 UI/Runner/DB 结果一致，取消确认前不显示完成，Abort/fence 后 request/commit count 不再增长。
    7. 每个 case teardown 时 `realSchoolRequests=0`、unmapped fulfilled=0、external sockets=0、downloads=0、popup interactions=0、non-GET/HEAD successes=0，且没有 HAR/trace/video/screenshot/console/body artifact；检测到任一项即测试失败而不是清理后忽略。
    8. 定向 UI E2E 与 typecheck 通过，无 skip/todo/retry；结果标签只有 S/I，EvidenceLedger 中 L/N/Windows cells 仍无新增。
  </acceptance_criteria>
  <done>真实边界接线证明 source 页面只有 approved origin+positive marker 才能按 Moodle→EdStem 推进；绑定/冲突与停止/fence 贯穿 Worker、持久化、API、paired UI，且双浏览器全程无真实学校或敏感捕获。</done>
</task>

<task type="auto" tdd="true">
  <name>02-11-2：用真实浏览器锁定失权清理、敏感面、证据门禁、键盘与窄屏</name>
  <read_first>Task 1 完成后的 auth-e2e.ts/auth-e2e.spec.ts；Phase 2 全部 CONTEXT/RESEARCH/VALIDATION/PATTERNS/UI-SPEC/PLAN-OUTLINE 与 02-06/02-08/02-09 PLAN/SUMMARY；02-UI-SPEC 的 Information Architecture、CheckpointLedger、Interaction State Machine、Responsive Layout、Accessibility、Privacy and Failure Display；02-08 protected/redacted/error schemas；02-09 clearProtected/stale/CTA/focus implementation；playwright.config.ts、tests/ui/auth-status.spec.ts、tests/ui/accessibility.spec.ts。</read_first>
  <files>packages/test-support/src/auth-e2e.ts, tests/ui/auth-e2e.spec.ts</files>
  <behavior>
    - RED：full name/email/course/origin/path/Cookie/Profile/action-id sentinels 的位置审计精确区分允许的 paired protected visible identity text 与禁止的 DOM attributes/ARIA/live-region/storage/URL/console/clipboard/receipt/error；未配对和 purge 后全部位置零命中。
    - RED：auth status、receipt、login/probe/logout/binding 任一路径返回 401 或 403 时，同一 UI turn 后 protected DOM、snapshot、pending actionReceiptId、candidate id、clipboard source 与 mutation control 全清；返回配对 shell且不能用旧数据重渲染。
    - RED：network/5xx（非 401/403）只保留带原 readAt 的 stale paired snapshot，所有 mutation disabled；恢复成功读取后才恢复 current。stale 不能把 healthy/authenticated/pass 当当前状态。
    - RED：A login/binding/course、B1-1/B1-2/B1-3、B2 Worker restart、B3 Codex exit、C OS restart、D 24h、reauth 的任一 synthetic receipt 只改变 exact platform/source/scenario/evidence cell；删除/失败一个 cell 只增加对应 gap，不能由另一个 cell 补齐。
    - RED：测试 Clock 即使跳到 23:59:59 仍显示 earliest recheck、`等待跨日复查`、无 skip/continue；跳到 ≥24h 也只证明 UI eligibility 逻辑，不写 L/N/pass 或宣称真实跨日。reauth 永远单独，不计入 D。
    - RED：所有 macOS S/I synthetic cells 都通过仍显示 Windows `not_run / human_needed`、固定跨平台文案和 Phase 3 blocked；Linux/WSL/browser OS 字符串或 query 不能改变平台 cell。
    - RED：键盘 Tab/Shift+Tab/Enter/Space/Escape 可完成当前允许动作并保留可见 2px/4px focus；busy 重复键无重复请求，完成聚焦 OverallGate。状态全部有可见中文+机器值，不只颜色/icon/ARIA。
    - RED：1280、759、599、320px 与 200% zoom 保持固定信息顺序、两来源等权/窄屏 Moodle→EdStem 单列、48px controls、可换行长值、无水平 overflow；不产出 screenshot。
  </behavior>
  <action>继续先写 RED 再扩展 Task 1 harness 的故障/时间/ledger controls。此 task 按 D-12 将 A/B/C/D 各子项独立，按 D-14 只显示脱敏结果单，按 D-15 禁止截图证据流，按 D-16 分离 24h 与 reauth，并按 D-17–D-18 保持 Windows not_run/human_needed、Phase 1/2 未整体完成和 Phase 3 blocked；D-04–D-07 的 protected identity/mismatch 以及 D-08–D-11 的停止/恢复语义在失权与窄屏场景中仍不得放宽。controls 只允许：撤销/过期当前 paired session、为下一条固定 auth route 注入 401/403/5xx/network abort、推进 injected monotonic test Clock、追加严格 synthetic S（或 integration I）receipt 到 exact `EvidenceCellKey`、读取无敏感计数器；不得提供 `setLivePass`、`setNativePass`、`setWindowsPassFromMac`、直接 DOM/projection 注入或绕过 EvidenceLedger authority 的 helper。Clock advance 必须在 test/report 中标记 simulated，绝不能改变 evidence class/provenance。

以唯一 identity/course/Profile/Cookie/page-path/action-id sentinels 驱动一条成功 paired 流程。写 DOM surface inventory helper：分别收集 visible text nodes、所有 attribute name/value（含 aria/title/id/data/href/value）、`[aria-live]` history、localStorage/sessionStorage、document URL/history、console/pageerror、clipboard mock payload、API response/error/receipt JSON。允许 full identity/course 只出现在 PrivacyNotice 后 `#protected` 的 visible text node；official origin 只允许 canonical origin visible text，不允许 path/query；Profile/Cookie/page/action ID 在任何面均零出现。不得使用截图/DOM HTML dump作为检查或产物。逐 route 注入 401/403，并在一个 macrotask+render turn 内断言 `#protected.replaceChildren()` 后 inventory 全零、旧 closure/action/candidate 无法重放；network/5xx 则断言 timestamped stale + mutation count 0。

用 EvidenceLedger 的真实 append/list 权限模型生成完整但仅 synthetic 的 cell matrix：S 只用 synthetic authority，I 只用 integration authority；不构造 `human_action`、N 或 L。逐个插入/删除期望（新建独立 fixture而非删除 append-only row）A-login/A-binding/A-course、B1×3、B2、B3、C、D、reauth，对 Moodle/EdStem、macOS/Windows/evidence 交叉检查 UI/gaps。`all macOS synthetic pass` 只是恶意测试向量，OverallGate 必须仍显示 Windows not_run/human_needed 和 Phase 3 blocked；测试报告不得使用 “macOS live passed”。为 D 用基准 `successfulAt` 与 injected Clock 分别在 `+23:59:59.999`、`+24:00:00` 检查 CTA/文本；后者只允许显示可以由用户发起检查，不能自动产生 receipt 或 pass。reauth cell 与 D 分开，不能替代。

用 Playwright keyboard 和 computed style 测试真实 UI：从页面顶部 Tab 到唯一 primary CTA，Enter/Space 各激活一次，busy 时重复键 request count 不变，Escape 只关闭原生 dialog/返回焦点或请求停止而不伪完成，结果后 `document.activeElement` 为 OverallGate result heading。对每个 status badge 同时断言 visible中文解释+机器值和非空边框/文字，不把 aria-label 当唯一状态。依次设置 1280/759/599/320 viewport 与 200% CSS zoom，断言 component heading/DOM 顺序不变、两卡等宽或正确单列、control min-height≥48px、focus outline=2px/offset=4px、长 origin/email/fingerprint/code `overflow-wrap:anywhere` 且 `scrollWidth<=innerWidth`。Playwright config 的 screenshot/trace/video 必须保持 off，spec 不调用 screenshot API。
  </action>
  <verify>
    <automated>npm run test:ui -- --grep "auth E2E protected purge|sensitive surfaces|receipt cell isolation|24 hour gate|Windows blocked|keyboard and narrow layout" &amp;&amp; npm run typecheck &amp;&amp; npm run test:ui -- --grep "paired auth|real browser pairs|actual DOM keyboard"</automated>
  </verify>
  <acceptance_criteria>
    1. surface inventory 证明 full name/email/course 仅存在于 paired `#protected` 可见 text node，canonical origin 无 path/query；DOM attributes、ARIA/live history、storage、URL/history、console/pageerror、clipboard、receipt/error/API JSON 无完整身份/course，Profile/Cookie/path/action-id 在所有面零命中。
    2. 对 status/receipt/login/probe/logout/binding 分别注入 401 与 403；每次一个 UI turn 后 protected DOM/内存/action/candidate/clipboard/mutation 均清空且旧值不可重放。network/5xx 仅得到带原 readAt 的 stale UI，mutation request=0，成功 refresh 前不移除 stale。
    3. A-login/A-binding/A-course、B1-1/B1-2/B1-3、B2、B3、C、D、reauth 每项的 exact-cell isolation 测试通过；任一缺失/failed/human_needed 只改变该 cell/gap，不被另一来源、场景、evidence 或平台 pass 覆盖。
    4. test Clock 在 24h 前 1ms 时显示 earliest time + `等待跨日复查` 且无跳过/继续/自动 probe；达到 24h 只使用户发起检查 CTA eligible，ledger append=0、L/N=0、D status 不自动 pass；reauth 保持独立。
    5. 全部 macOS S/I synthetic cells pass、伪 Linux/WSL/Windows browser hints、query/local storage 尝试均不能更新 Windows；UI 逐字显示 `macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。`，Windows cells 全为 `not_run / human_needed`。
    6. 键盘用例证明可达所有当前允许 control、busy Enter/Space 只请求一次、Escape 不伪完成、完成焦点落 OverallGate result；每个状态有可见中文+机器值，aria-live 不含 identity/course sentinel。
    7. 1280px 为两列等权，759/599/320px 为 Moodle→EdStem 单列；599/320px padding/controls 满足 16px/全宽，全部 control min-height≥48px、focus 2px/4px，200% zoom 无水平 overflow且长值未截断。
    8. `rg -n "page\\.screenshot|toHaveScreenshot|screenshot:|trace:.*on|video:.*on" tests/ui/auth-e2e.spec.ts packages/test-support/src/auth-e2e.ts` 零命中；定向与既有 UI 回归、typecheck 全通过，无 skip/todo/retry、无真实学校/native/live evidence。
  </acceptance_criteria>
  <done>真实 paired 浏览器在失权、网络、证据缺口、跨日等待、键盘与窄屏条件下仍遵守隐私和硬门：敏感内容只在获准可见文本，401/403 无残留，场景/证据/平台不可合并，24 小时与 Windows 门无法被 synthetic 跳过。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | 不可信输入/风险 | 强制控制 |
|---|---|---|
| synthetic source page → source Chromium/sealed adapter | redirect、marker、popup、download、表单/写请求、隐藏内容可能扩权或伪认证 | 独立 deny-all route/proxy；逐 hop approved origin；positive marker；静态有界 visible locator；所有非 GET/HEAD/交互/越界 fail closed |
| UI Chromium → paired loopback API | 未配对、CSRF/replay、重复激活、动态 route/body 可能越权 | actual HTTP pairing、same-origin+CSRF、fixed routes、server-issued one-time action、busy guard、request audit |
| Worker/job/store → UI projection | 失租/取消旧 Worker、跨来源状态或 last-success 可能错误提交/混淆 | request/commit 前 fence；durable job/transaction；per-source observations；中间状态/计数断言；不直接写 projection |
| protected projection → browser surfaces | 完整身份/course/Profile/Cookie/path/action ID 可能进入 DOM attribute、ARIA、storage、console、clipboard/receipt | visible-text-only allowlist、surface inventory、401/403 purge、redacted receipt、无 HTML/screenshot dump |
| synthetic clock/receipt → UAT gate | 加速 24h、macOS/S/I 或 browser platform hint 可能伪造 D、Windows、N/L/Phase3 | injected Clock 明确 simulated；typed exact-cell ledger；无 human/N/L authority；Windows固定缺口；Phase3 hard block |
| test harness → external network/filesystem | UI/source browsers 或 Node HTTP 可能误连学校或真实 Profile | 两上下文独立 deny-all、closed proxy、loopback assertion、pre-network host guard、临时根、零外网/无 artifact teardown audit |

## STRIDE / ASVS L1 威胁登记

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 控制意图 | 具体缓解与硬阻断 |
|---|---|---:|---|---|---|---|
| T2-01 | Spoofing / Tampering / Elevation of Privilege | Critical | source fixture、redirect/marker、sealed adapter、UI command | mitigate | V1 架构边界、V4 功能级授权、V5 输入/输出验证、V13 API/业务逻辑 | 真实四动作链、逐 hop origin、positive marker、fixed UI/API、无任意 URL/JS/selector/write；双 context 网络审计。任一越界/无 marker authenticated、写/下载成功或真实学校 request 即 Critical blocker，阻止 02-13/beta/live。 |
| T2-02 | Information Disclosure | Critical | Profile/browser/source/DOM/storage/receipt/error/test artifacts | mitigate | V1 数据分类、V8 敏感数据保护、V14 安全配置、V16 日志/错误 | 临时 synthetic Profile；无 Cookie/raw capture/HTML/HAR/trace/video/screenshot/console/body；surface inventory；purge；无敏感 test artifact。任一敏感 sentinel 出现在非 protected visible text 或磁盘/输出即 Critical blocker。 |
| T2-07 | Spoofing / Information Disclosure / Elevation of Privilege | High | pairing、protected UI、action intent、401/403 | mitigate | V2 认证、V3 会话、V4 CSRF/授权、V5 安全 DOM、V16 错误 | actual pairing+same-origin+CSRF、server one-time intent、busy/replay tests、401/403 全 purge、ARIA/storage/clipboard隔离。任一未配对读取、mutation replay 或 purge 后残留即 High blocker。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | High | EvidenceLedger、Clock、OverallGate、platform detection | mitigate | V1 证据等级分离、V4 writer authority、V13 状态/交易规则、V16 可审计结果 | S/I-only authority、exact OS/source/scenario/evidence cells、A/B/C/D/reauth 独立、24h no-auto-pass、Windows not_run、Phase3 blocked。任一 synthetic→N/L、macOS→Windows、cell merge 或 simulated 24h→pass 即 High blocker。 |

ASVS 5.0 L1 仅作为适用控制意图，不声明完整合规。全部 Critical/High 项必须 `mitigate` 且由真实浏览器 negative cases 证明；不得 accept/transfer、以 screenshot 人工目测替代、把失败标 flaky/skip，或用 fixture pass 代替 native/live。
</threat_model>

<verification>
1. 两个 task 都先提交/运行 `auth-e2e.spec.ts` RED 行为，再扩展 harness；不得从生产层抄出逻辑在 test helper 中复算后只测自己。
2. 全链路定向：`npm run test:ui -- --grep "auth E2E"`；全部使用 `syntheticTest` 且 source context 另有独立网络 containment。
3. UI/类型回归：`npm run typecheck && npm run test:ui`；既有 status/auth-status/accessibility tests 不得降级或 skip。
4. 安全回归：`npm run test:unit -- --run tests/unit/auth-contracts.test.ts tests/unit/auth-state.test.ts tests/unit/output-policy.test.ts && node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/source-adapters.test.ts tests/integration/auth-worker.test.ts tests/integration/auth-api.test.ts`。
5. 每个 E2E teardown 强制检查 source/UI/Node request audits、downloads/popups/write success、browser artifacts、敏感 surface 与临时根；先失败再清理，不吞掉 audit failure。
6. `git status --short` 不得出现测试运行生成的 screenshot/trace/video/HAR/Profile/DB/log；这些文件即使被 ignore 也不得保留在 repo/runtime 之外的未授权位置。
7. 证据声明审计：测试名、reporter output、fixture types 与 SUMMARY 只可称 synthetic S/UI 或 integration I；不得包含 native/live/pass-account/macOS-live/Windows-pass 声明。
8. hard gates 审计：官方登录/MFA、真实绑定、更新、Worker/Codex/OS restart、跨日、reauth 与 Windows 均未执行；Windows `not_run/human_needed`，Phase 1/2 未整体完成，Phase 3 blocked。
</verification>

<success_criteria>
- AUTH-01：全链路证明 official-origin redirect 与来源专属 positive marker 都成立后才认证，Moodle→EdStem 顺序可见；fixture 不输入凭据、不联系学校、不把 URL/200 当成功。
- AUTH-02：UI/ledger 对 A、B1×3、B2、B3、C、D、reauth 保持独立；24h synthetic clock 不自动 pass，Windows 仍 not_run/human_needed，因此只证明 gate 逻辑而非真实生命周期。
- AUTH-03：五维状态、last-success、binding candidate/manual/mismatch 在来源→Worker→store→API→UI 不串来源；mismatch 硬停课程访问且保留旧 binding。
- SEC-02/T2-01：两 browser context 与 Node 审计证明任意 redirect/交互/写/下载/未映射请求 fail closed、真实学校请求为零；UI/API 无任意操作入口。
- T2-02/T2-07：完整身份只在当前 paired protected visible text；attributes/ARIA/storage/URL/console/clipboard/receipt/error 与 401/403 后 DOM/内存无敏感残留，且无 screenshot/HAR/trace/video/console/body artifact。
- UAT-01/T2-08：fixture 仅形成 S/I；不写 N/L、不合并 checkpoint、不用 macOS或浏览器平台提示填 Windows，不解除 Phase 3，后续 beta/live 硬人工门保持原样。
- busy/stop/fence、keyboard/focus/live-region、唯一可见状态文本和 1280/759/599/320px+200% zoom 均有真实浏览器断言，不依赖 screenshot。
- 所有 High/Critical blocker、定向 E2E、既有 UI、unit/integration 安全回归与 typecheck 全部通过；任一失败阻止 beta assembly/live。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-11-SUMMARY.md`，只记录脱敏 S/UI/I 命令、场景数、零外网/零敏感面/零串格计数和仍待 beta/native/live/Windows 的缺口；不得附截图、DOM/HTML、学校 host/path、完整身份/course、Profile/Cookie、action ID、DB/log、真实登录或 native/live pass 声明。
</output>
