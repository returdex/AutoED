---
phase: 02-poc-live
plan: "37"
type: execute
wave: 9
depends_on: ["02-35", "02-36"]
files_modified:
  - scripts/release/phase2-live-gate.mjs
  - scripts/release/phase2-native-evidence.mjs
  - scripts/release/verify-phase2-update-gate.mjs
  - tests/integration/phase2-live-gate.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "Before beta assembly, the canonical gate implements the complete finite record grammar consumed by A1, A2, macOS B3 and macOS reauth, plus every read-only A1/A2/B1/B2/B3/C/D/reauth/audit/final branch for both platforms."
    - "Every CLI flag, strict feedback schema, exact L-event digest conjunction, allowed pass-only output and stable result/error code consumed by plans 14-34 is present, tested and included in the signed artifact; later plans only consume it and never edit the gate or its tests."
    - "Strict read-only macOS and native-Windows pre/post-update modes are complete before assembly and are never edited after signing."
    - "The final branch reads the exact evidence-requiredness registry rather than demanding all 176 possible cells pass."
    - "The native evidence command runs only on the declared current native platform/build, produces the exact named S/I/N obligations through server-held automated authority, and cannot write L or another platform."
    - "Record mode writes only a sanitized planning handoff after current build/artifact/runtime and exact server-observed L digests pass; it never writes EvidenceLedger/DB/Profile/browser/source/L authority. Other gate modes are read-only, and the native producer alone has a narrow automated S/I/N API."
  artifacts:
    - path: scripts/release/phase2-live-gate.mjs
      provides: "Complete canonical sanitized record plus checkpoint/audit/final verifier"
      exports: [recordPhase2LiveCheckpoint, verifyPhase2LiveCheckpoint, auditPhase2EvidenceMatrix, verifyPhase2FinalGate]
    - path: scripts/release/phase2-native-evidence.mjs
      provides: "Signed current-build native S/I/N producer and verifier"
      exports: [runPhase2NativeEvidence]
    - path: scripts/release/verify-phase2-update-gate.mjs
      provides: "Signed read-only macOS and native-Windows update handoff"
      exports: [verifyPhase2UpdateGate]
    - path: tests/integration/phase2-live-gate.test.ts
      provides: "All branches, flags, codes, requiredness and write-boundary negatives"
  key_links:
    - from: scripts/release/phase2-live-gate.mjs
      to: packages/domain/src/live-evidence.ts
      via: "final/audit branches consume exact requiredness registry"
      pattern: "requiredEvidenceKeys"
    - from: scripts/release/phase2-live-gate.mjs
      to: "GET /api/auth/status + exact GET /api/auth/receipts cells"
      via: "record mode derives a pass-only sanitized planning handoff from current redacted runtime plus server-observed event digests, without ledger authority"
      pattern: "recordPhase2LiveCheckpoint|ledgerEventDigest|read-only-inputs"
    - from: scripts/release/phase2-native-evidence.mjs
      to: packages/application/src/live-checkpoints.ts
      via: "current runtime attestation mints only automated platform-bound authority"
      pattern: "signed_automated|native_preflight"
---

<objective>
Finish every canonical live verifier branch and the native S/I/N producer before the release is assembled.

Purpose: Eliminate post-release script mutation and ensure later checkpoints consume immutable signed behavior.
Output: Complete read-only live/update gates, native evidence command, and exhaustive negative tests. No publish, update, login or live action occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-35-SUMMARY.md
@.planning/phases/02-poc-live/02-36-SUMMARY.md
@.planning/phases/02-poc-live/02-14-PLAN.md
@.planning/phases/02-poc-live/02-16-PLAN.md
@.planning/phases/02-poc-live/02-17-PLAN.md
@.planning/phases/02-poc-live/02-20-PLAN.md
@.planning/phases/02-poc-live/02-23-PLAN.md
@.planning/phases/02-poc-live/02-34-PLAN.md

<interfaces>
The signed script has one closed record grammar. These are the only accepted `--record` argv forms; argument order may be normalized internally, but the key set and values are exact and extra/mixed flags fail before runtime discovery:

| Branch | Exact required arguments | Allowed filesystem output |
|---|---|---|
| A1 | `--record --checkpoint A1 --feedback <0700-mktemp>/A1-result.json --out .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only-inputs` | only the exact A1 path |
| A2 | `--record --checkpoint A2 --feedback <0700-mktemp>/A2-result.json --out .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --a1 .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only-inputs` | only the exact A2 path |
| B3 macOS | `--record --checkpoint B3 --feedback <0700-mktemp>/B3-result.json --runtime current --platform macos --read-only-inputs` | none; return one pass-only redacted handoff object on stdout |
| reauth macOS | `--record --checkpoint reauth --feedback <0700-mktemp>/reauth-result.json --runtime current --platform macos --read-only-inputs` | none; return one pass-only redacted handoff object on stdout |

`--record` is not a live-evidence writer. It may read only the executor-owned strict feedback file plus current installed redacted status and exact receipt endpoints through fixed local client discovery. It cannot import persistence, open DB/WAL, call `EvidenceLedger.append`, mint/accept/return `actionReceiptId`, mutate runtime, access protected identity/config, open Profile/browser, or send a source request. Failed or `human_needed` runtime events remain append-only in the server ledger and must yield nonzero safe failure with no pass handoff.

The feedback parser is a strict discriminated union of the exact schemas and pass conjunctions already fixed in downstream plans:

- A1: `02-16_A1_RESULT`; exact keys through `observed_at`; pass only for both sources `pass/AUTHENTICATED`, binding `candidate`, course `not_run`, content `not_started`, Windows `not_run/human_needed`, Phase 3 `blocked`.
- A2: `02-17_A2_RESULT`; pass only for `A2_COMPLETE`, confirmed binding/mode/identities/scope, both course results visible, preserved content boundary, no screenshot and fixed Windows/Phase 3 gaps.
- B3 macOS: `02-20_B3_RESULT`; pass only for `B3_COMPLETE`, actual Codex exit, surviving backend/Worker, absent status read, actual-client reentry, matched build, both source states consistent, confirmed binding, preserved content boundary, no screenshot and fixed gaps.
- reauth macOS: `02-23_REAUTH_RESULT`; pass only for `REAUTH_COMPLETE` and the natural-expiry or consented explicit-Moodle path conjunction, including exact two-source outcomes, no automatic reopen, consistent identity/scope, preserved isolation/last-success/content boundary, no screenshot and fixed gaps.

For each branch, record mode must verify current immutable version/build/artifact/manifest/runtime generation and the latest non-corrected server-observed event digests: two `A1_login` cells, four `A2_binding|A2_course_visibility` cells, two `B3_codex_exit` cells, or two `reauth` cells respectively, always `{platform:'macos', evidence:'L'}` and exact source/scenario. Missing, duplicate, corrected, conflicting, stale/future, wrong source/scenario/platform/evidence/result/code/build/generation/config/scope/binding/parent, S/I/N/fixture substitution, feedback/runtime disagreement, or sensitive sentinel fails without output.

A1/A2 pass files use the existing `writeInstallerRecord` durability pattern from `packages/installer/src/launchers.ts`: validate the exact allowlisted target, require an existing protected parent, create a same-directory 0600 temporary with `wx`, write canonical JSON, fsync and re-parse it, publish with atomic no-replace semantics, fsync the directory where supported, and remove only the owned temporary on failure. Existing target, symlink/non-regular parent/target, competing writer, partial write, rename/link race or post-write hash mismatch returns a stable safe code and never overwrites/deletes the prior receipt. B3/reauth reject `--out` and perform zero filesystem writes. Stdout is one bounded allowlisted JSON object; stderr is one stable safe code. Neither may include input/output path, endpoint/port, PID, Profile, origin/course, identity/fingerprint, action/correlation, raw response, page/network data or exception text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement every immutable checkpoint, audit and final gate branch</name>
  <files>scripts/release/phase2-live-gate.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - Strict commands support the four exact `--record` forms in `<interfaces>`, `--verify --checkpoint A1|A2|B1|B2|B3|C|D|reauth|final`, B1 `--round 1|2|3`, `--audit-matrix --platform`, `--runtime current`, and required read-only flags.
    - Every record feedback object is exact-key/enum/timestamp parsed and is accepted only when current build/artifact/runtime status and all branch-specific latest non-corrected L-event digests satisfy the unique pass conjunction.
    - A1/A2 publish only to their exact allowlisted 0600 planning paths with atomic no-replace behavior; B3/reauth reject `--out` and write no file. Any failure/human_needed leaves output absent or byte-identical and never changes the runtime ledger.
    - macOS and Windows variants emit the exact stable codes referenced by plans 16-34, with identical schema and platform-specific current-runtime proof.
    - D derives earliest time from current non-corrected A evidence and server time; reauth is independent.
    - Audit reports possible and required counts separately; final checks only registry obligations and never infers missing classes.
  </behavior>
  <action>Follow the Plan 35-41 mapping in `02-PATTERNS.md`: this gate is explicitly no-analog/research-contract-first, while its strict argv/safe-error style follows `scripts/release/preflight.mjs` and its file publication follows `packages/installer/src/launchers.ts#writeInstallerRecord`. Write the complete script now, using the installed local client’s fixed discovery and redacted status/exact-receipt APIs only. Implement the four `<interfaces>` record forms before identity selection/signing, with exact A1/A2/B3/reauth feedback schemas and unique pass conjunctions from Plans 16/17/20/23. For each, bind current signed version/build/artifact/manifest/runtime generation, status, predecessor and the exact two/four latest non-corrected server-observed macOS/L event digests; never infer an event from feedback or a SUMMARY. A failed/human_needed event remains in the runtime append-only ledger and must not produce a pass handoff.

For A1/A2, enforce the sole output paths from the grammar and implement protected 0600 same-directory temporary, canonical serialization, fsync/re-parse, atomic no-replace publication and directory sync; pre-existing or racing output remains byte-identical. B3/reauth reject `--out` and produce only the bounded pass handoff on stdout with zero filesystem writes. All branches delete only their owned temporary feedback/output temp in `finally`; they never delete/replace a prior receipt. Implement all read-only branches named in plans 14-34, including receipt verification where a file handoff exists, no-file branches backed by runtime ledger+SUMMARY prerequisites, both platform audits, and the final exact-registry conjunction. Preserve stable sorted gaps and safe single-line output. Reject extra/mixed/duplicate flags, alternate path spelling, symlink/non-regular feedback/output, feedback outside the executor 0700 temp boundary, URL/port/DB/Profile/browser/action ID, write/repair/force/ignore-platform and non-current runtime before any client call.

Add table-driven positive fixtures for all four exact record commands and every read-only command consumed downstream. Add negative tests for every omitted/extra/duplicate/mixed flag; each unknown key/enum and invalid cross-field feedback conjunction; missing/duplicate/corrected/conflicting event; wrong result/code/build/artifact/runtime generation/config/scope/binding/parent/source/scenario/platform/evidence; S/I/N/fixture substitution; stale/future time; feedback/status disagreement; existing output, symlink/path escape, permission/partial-write and competing-writer races; B3/reauth `--out`; and injected sensitive sentinels. Snapshot ledger/runtime and filesystem before each negative and assert: ledger/runtime unchanged, A1/A2 target absent or byte-identical, no temp remains, B3/reauth create no file, and persistence/DB/append/login/probe/browser/source/process counters are zero. Plans 16/17/20/23 must consume these signed branches without later script/test edits.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>Every later canonical command is available and tested before signing; no checkpoint plan needs to edit the gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable macOS and Windows update-gate modes</name>
  <files>scripts/release/verify-phase2-update-gate.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - Pre-update modes bind exact artifact/test/publication/availability/prompt identity without changing runtime.
    - Post-update modes require current managed build, native platform, paired API/Worker/UI readiness and cleanup success; Windows rejects WSL/Linux/static artifact substitution.
    - Extra fields, identity drift, stale/future receipt, incomplete cleanup, unavailable asset or evidence/gate mutation fails closed with stable safe codes.
  </behavior>
  <action>Follow the exact `verify-phase2-update-gate.mjs` mapping in `02-PATTERNS.md`: reuse `preflight.mjs` closed argv/exact identity/safe-error mechanics; the Phase 2 pre/post conjunction is research-contract-first. Implement both platform pre/post modes before assembly. Keep the verifier incapable of installation, update, process restart, Profile/browser/source access or EvidenceLedger writes. Define the strict schemas, flags and stable codes consumed by 02-14 and 02-25, and test wrong platform, WSL substitution, mismatched build/hash, stale/future receipt, failed cleanup, unavailable asset and mutation attempts. Publication fields may be absent only in the prerelease fixture path used by tests; the signed runtime path requires the exact receipts produced later by 02-13.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts -t "macOS update gate|Windows update gate|read-only|platform spoof|identity drift"</automated></verify>
  <done>Both update checkpoints consume immutable signed behavior already tested before publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add current-build native S/I/N production and verification</name>
  <files>scripts/release/phase2-native-evidence.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - Command fails on non-native/WSL/platform mismatch and on runtime/artifact/build/manifest drift.
    - It runs the signed bounded synthetic, integration and native checks for the current installed build and appends only named build obligations assigned to signed_automated S/I/N.
    - Re-run is idempotent; partial failure records no pass bundle and retains prior failure/last success.
    - Attempts to write L, another platform, an unrequired obligation or a caller-selected scenario are rejected before append.
  </behavior>
  <action>Follow the exact `phase2-native-evidence.mjs` mapping in `02-PATTERNS.md`: this producer is research-contract-first; only bounded native-report durability/platform mechanics and preflight closed argv/safe output are reusable analogs. Create a strict installed command with no arbitrary path/URL/operation parameters. Discover the current managed runtime through the fixed installation inventory, verify the signed build/artifact/manifest, assert actual native OS, and run the bounded packaged checks named by `PHASE2_BUILD_OBLIGATIONS`. The paired server derives automated authority from its signed runtime and actual platform, not from CLI JSON. Append named S/I/N obligations transactionally as a build-bound bundle or retain a safe failure; never mint human_action authority or fill possible source×scenario S/I/N cells. This command is consumed on macOS during build-bound native verification and on Windows by 02-40 after actual update.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts -t "native evidence|requiredness|platform authority|failure retention"</automated></verify>
  <done>The signed beta contains a narrow current-build producer for platform-required S/I/N evidence, with no route to L or cross-platform promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI argv → verifier | Arguments are untrusted and must not select endpoints, writers or scope exclusions. |
| runtime projection → gate | Data must match current signed build and exact registry keys. |
| native command → automated authority | Actual OS/build attestation, not payload, determines S/I/N authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-37-01 | T/E | gate argv/branches | mitigate | Closed four-form record grammar plus exhaustive read-only branch tests; no runtime write/repair modes. |
| T2-37-02 | S/T | current runtime binding | mitigate | Signed build/artifact/manifest/platform equality before any result. |
| T2-37-03 | E | native evidence producer | mitigate | Server-derived automated authority limited to registry S/I/N and actual OS. |
| T2-37-04 | I | tool output | mitigate | Allowlisted JSON/safe errors and injected-secret/path sentinel tests. |
| T2-37-05 | S/T/E | update verifier | mitigate | Native-platform proof, strict schemas, stable codes and zero mutation authority. |
| T2-37-06 | T/R/I | sanitized record output | mitigate | Exact pass conjunction, current server event digests, allowlisted path, 0600 atomic no-replace or zero-file stdout handoff, safe output and race/sentinel negatives. |
</threat_model>

<verification>
Run the full integration file and typecheck. Enumerate every command used by plans 14-34 and prove each parses in a positive fixture and rejects malformed/extra arguments. For all four record forms, snapshot runtime ledger and output surfaces before every negative and prove no runtime write; A1/A2 are atomic/no-overwrite at the exact allowed paths, while B3/reauth write no file.
</verification>

<success_criteria>
- No canonical live or update gate branch is implemented after release.
- The complete A1/A2/B3/reauth record grammar and strict schemas exist before signing; every downstream command is a byte-for-byte member of that grammar.
- Record mode can only create a sanitized pass handoff from current exact server L-event digests; failure/human_needed creates no pass output and never writes runtime evidence.
- Required/possible evidence semantics are explicit and consistent.
- Native Windows can later produce current-build S/I/N before its L gates.
- The tools cannot write L, browse sources, mutate Profile, or bypass platform scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-37-SUMMARY.md` listing branch/code coverage and S/I tests only.
</output>
