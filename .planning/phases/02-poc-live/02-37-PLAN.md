---
phase: 02-poc-live
plan: "37"
type: execute
wave: 9
depends_on: ["02-35", "02-36"]
files_modified:
  - scripts/release/phase2-live-gate.mjs
  - scripts/release/phase2-native-evidence.mjs
  - scripts/release/verify-phase2-update-gate.mjs
  - tests/integration/phase2-live-gate.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "Before beta assembly, the canonical read-only gate implements A1, A2, B1 rounds 1-3, B2, B3, C, D, reauth, platform audit and final branches for both macOS and Windows."
    - "Every CLI flag and stable result/error code consumed by plans 14-34 is present, strict, tested and included in the signed artifact; later plans only consume it."
    - "Strict read-only macOS and native-Windows pre/post-update modes are complete before assembly and are never edited after signing."
    - "The final branch reads the exact evidence-requiredness registry rather than demanding all 176 possible cells pass."
    - "The native evidence command runs only on the declared current native platform/build, produces the exact named S/I/N obligations through server-held automated authority, and cannot write L or another platform."
    - "Both tools are read-only with respect to source/Profile/browser/evidence except the native producer's narrow automated S/I/N API; neither accepts DB path, URL, port, selector, action authority or arbitrary operation."
  artifacts:
    - path: scripts/release/phase2-live-gate.mjs
      provides: "Complete canonical checkpoint/audit/final verifier"
      exports: [verifyPhase2LiveCheckpoint, auditPhase2EvidenceMatrix, verifyPhase2FinalGate]
    - path: scripts/release/phase2-native-evidence.mjs
      provides: "Signed current-build native S/I/N producer and verifier"
      exports: [runPhase2NativeEvidence]
    - path: scripts/release/verify-phase2-update-gate.mjs
      provides: "Signed read-only macOS and native-Windows update handoff"
      exports: [verifyPhase2UpdateGate]
    - path: tests/integration/phase2-live-gate.test.ts
      provides: "All branches, flags, codes, requiredness and write-boundary negatives"
  key_links:
    - from: scripts/release/phase2-live-gate.mjs
      to: packages/domain/src/live-evidence.ts
      via: "final/audit branches consume exact requiredness registry"
      pattern: "requiredEvidenceKeys"
    - from: scripts/release/phase2-native-evidence.mjs
      to: packages/application/src/live-checkpoints.ts
      via: "current runtime attestation mints only automated platform-bound authority"
      pattern: "signed_automated|native_preflight"
---

<objective>
Finish every canonical live verifier branch and the native S/I/N producer before the release is assembled.

Purpose: Eliminate post-release script mutation and ensure later checkpoints consume immutable signed behavior.
Output: Complete read-only live/update gates, native evidence command, and exhaustive negative tests. No publish, update, login or live action occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-35-SUMMARY.md
@.planning/phases/02-poc-live/02-36-SUMMARY.md
@.planning/phases/02-poc-live/02-14-PLAN.md
@.planning/phases/02-poc-live/02-16-PLAN.md
@.planning/phases/02-poc-live/02-34-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement every immutable checkpoint, audit and final gate branch</name>
  <files>scripts/release/phase2-live-gate.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - Strict commands support `--verify --checkpoint A1|A2|B1|B2|B3|C|D|reauth|final`, B1 `--round 1|2|3`, `--audit-matrix --platform`, `--runtime current`, and required read-only flags.
    - macOS and Windows variants emit the exact stable codes referenced by plans 16-34, with identical schema and platform-specific current-runtime proof.
    - D derives earliest time from current non-corrected A evidence and server time; reauth is independent.
    - Audit reports possible and required counts separately; final checks only registry obligations and never infers missing classes.
  </behavior>
  <action>Write the complete script now, using the installed local client’s fixed discovery and redacted status/receipt APIs only. Implement all branches named in plans 14-34, including receipt verification where a planning handoff exists, no-receipt branches where plans intentionally rely on runtime ledger+SUMMARY, both platform audits, and the final exact-registry conjunction. Preserve stable sorted gaps and safe single-line output. Reject extra/mixed flags, URL/port/DB/Profile/browser/action ID, write/repair/force/ignore-platform and non-current runtime. Add exhaustive branch/code/flag tests, wrong build/platform/source/scenario/class/correction tests, and spies proving no evidence append, browser/source access, process change, Profile access or planning write.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>Every later canonical command is available and tested before signing; no checkpoint plan needs to edit the gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable macOS and Windows update-gate modes</name>
  <files>scripts/release/verify-phase2-update-gate.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - Pre-update modes bind exact artifact/test/publication/availability/prompt identity without changing runtime.
    - Post-update modes require current managed build, native platform, paired API/Worker/UI readiness and cleanup success; Windows rejects WSL/Linux/static artifact substitution.
    - Extra fields, identity drift, stale/future receipt, incomplete cleanup, unavailable asset or evidence/gate mutation fails closed with stable safe codes.
  </behavior>
  <action>Implement both platform pre/post modes before assembly. Keep the verifier incapable of installation, update, process restart, Profile/browser/source access or EvidenceLedger writes. Define the strict schemas, flags and stable codes consumed by 02-14 and 02-25, and test wrong platform, WSL substitution, mismatched build/hash, stale/future receipt, failed cleanup, unavailable asset and mutation attempts. Publication fields may be absent only in the prerelease fixture path used by tests; the signed runtime path requires the exact receipts produced later by 02-13.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts -t "macOS update gate|Windows update gate|read-only|platform spoof|identity drift"</automated></verify>
  <done>Both update checkpoints consume immutable signed behavior already tested before publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add current-build native S/I/N production and verification</name>
  <files>scripts/release/phase2-native-evidence.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - Command fails on non-native/WSL/platform mismatch and on runtime/artifact/build/manifest drift.
    - It runs the signed bounded synthetic, integration and native checks for the current installed build and appends only named build obligations assigned to signed_automated S/I/N.
    - Re-run is idempotent; partial failure records no pass bundle and retains prior failure/last success.
    - Attempts to write L, another platform, an unrequired obligation or a caller-selected scenario are rejected before append.
  </behavior>
  <action>Create a strict installed command with no arbitrary path/URL/operation parameters. Discover the current managed runtime through the fixed installation inventory, verify the signed build/artifact/manifest, assert actual native OS, and run the bounded packaged checks named by `PHASE2_BUILD_OBLIGATIONS`. The paired server derives automated authority from its signed runtime and actual platform, not from CLI JSON. Append named S/I/N obligations transactionally as a build-bound bundle or retain a safe failure; never mint human_action authority or fill possible source×scenario S/I/N cells. This command is consumed on macOS during build-bound native verification and on Windows by 02-40 after actual update.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts -t "native evidence|requiredness|platform authority|failure retention"</automated></verify>
  <done>The signed beta contains a narrow current-build producer for platform-required S/I/N evidence, with no route to L or cross-platform promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI argv → verifier | Arguments are untrusted and must not select endpoints, writers or scope exclusions. |
| runtime projection → gate | Data must match current signed build and exact registry keys. |
| native command → automated authority | Actual OS/build attestation, not payload, determines S/I/N authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-37-01 | T/E | gate argv/branches | mitigate | Closed argv grammar, exhaustive branch tests and no write/repair modes. |
| T2-37-02 | S/T | current runtime binding | mitigate | Signed build/artifact/manifest/platform equality before any result. |
| T2-37-03 | E | native evidence producer | mitigate | Server-derived automated authority limited to registry S/I/N and actual OS. |
| T2-37-04 | I | tool output | mitigate | Allowlisted JSON/safe errors and injected-secret/path sentinel tests. |
| T2-37-05 | S/T/E | update verifier | mitigate | Native-platform proof, strict schemas, stable codes and zero mutation authority. |
</threat_model>

<verification>
Run the full integration file and typecheck. Enumerate every command used by plans 14-34 and prove each parses in a positive fixture and rejects malformed/extra arguments.
</verification>

<success_criteria>
- No canonical live or update gate branch is implemented after release.
- Required/possible evidence semantics are explicit and consistent.
- Native Windows can later produce current-build S/I/N before its L gates.
- The tools cannot write L, browse sources, mutate Profile, or bypass platform scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-37-SUMMARY.md` listing branch/code coverage and S/I tests only.
</output>
