---
phase: 02-poc-live
plan: "13"
type: execute
wave: 13
depends_on: ["02-39"]
files_modified:
  - release/phase2-publication.json
  - release/phase2-availability.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "The completed beta.31 publication is grandfathered; every later candidate is rejected unless its 02-38 selection binds the passing exact-source rehearsal attestation required by `.planning/RELEASE-STABILIZATION.md`."
    - "D-01: Moodle and EdStem implementation and automated tests complete together in Phase 2 before one unified authentication beta is published for human verification; no Moodle-only live beta may prefill dual-source results. Exactly the 02-38/02-39 selected and signed beta identity is published once; an existing tag, asset or receipt is never overwritten."
    - "Anonymous refetch independently verifies both target assets, manifests, signatures, trust fingerprint, hashes, complete live-checkpoint capability set and install prompt."
    - "The signed update verifier supports strict read-only macOS and native-Windows pre/post-update modes before any human update checkpoint begins."
    - "Publication and availability do not claim installation, login, live evidence, Windows native evidence or Phase 2 completion."
  artifacts:
    - path: scripts/release/verify-phase2-update-gate.mjs
      provides: "Read-only signed beta/update handoff for macOS and Windows"
    - path: release/phase2-publication.json
      provides: "Immutable publication receipt"
    - path: release/phase2-availability.json
      provides: "Anonymous independent availability receipt"
  key_links:
    - from: release/phase2-beta-artifacts.json
      to: release/phase2-publication.json
      via: "exact version, tag, commit, tree, build, asset and signature equality"
      pattern: "version|tag|commit|tree|buildId|sha256"
    - from: release/phase2-publication.json
      to: release/phase2-availability.json
      via: "anonymous refetch and independent cryptographic verification"
      pattern: "anonymous|signature|fingerprint|sha256"
    - from: scripts/release/verify-phase2-update-gate.mjs
      to: release/phase2-availability.json
      via: "strict pre-update identity and availability validation"
      pattern: "platform|pre-update|availability|read-only"
---

<objective>
Publish the already tested and signed Phase 2 beta immutably, prove anonymous availability, and provide the complete read-only cross-platform update gate.

Purpose: Finish distribution only after every production live workflow and every canonical verifier branch is inside the signed artifact.
Output: Immutable publication/availability receipts and strict macOS/Windows update verification. No update, login, source access or live evidence occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/RELEASE-STABILIZATION.md
@.planning/phases/02-poc-live/02-38-SUMMARY.md
@.planning/phases/02-poc-live/02-39-SUMMARY.md
@release/phase2-build-selection.json
@release/phase2-test-report.json
@release/phase2-beta-artifacts.json
@release/phase2-install-prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the signed cross-platform update handoff</name>
  <files>None (consume signed/source-complete update verifier read-only)</files>
  <behavior>
    - macOS and Windows pre-update modes require exact artifact/test/publication/availability/prompt identity and make no runtime mutation.
    - post-update modes require the exact current managed build, native platform, paired API/Worker/UI readiness and cleanup success; Windows rejects WSL/Linux/static artifact substitution.
    - Extra fields, mismatches, stale/future build, unavailable asset, failed cleanup, source access, Profile creation, evidence mutation or phase promotion fail closed with canonical safe codes.
  </behavior>
  <action>Consume the verifier finalized in 02-37 and assert its hash is present in both 02-39 signed manifests. Exercise the signed macOS and Windows pre/post fixtures against the selected identity and capability hashes; do not edit the verifier or add a post-sign branch. Re-run the existing negative matrix for platform spoofing, WSL substitution, identity/hash drift, stale/future receipts, incomplete cleanup, unavailable assets and attempted evidence/gate mutation.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "macOS update gate|Windows update gate|read-only|platform spoof|identity drift"</automated></verify>
  <done>Both platform update paths are proven present, hash-bound and non-mutating in the signed beta.</done>
</task>

<task type="auto">
  <name>Task 2: Publish once and prove anonymous artifact availability</name>
  <files>release/phase2-publication.json, release/phase2-availability.json</files>
  <action>Preserve beta.31 as the grandfathered completed publication. For every later candidate, first reject publication unless the 02-38 selection binds a passing, exact-source rehearsal-attestation digest under `.planning/RELEASE-STABILIZATION.md`; publication plans may not diagnose or edit release source/tooling inline. Re-run immutable version/repository/returdex identity preflight and reject any tag/asset/receipt conflict. Publish exactly the two already signed archives and approved metadata under the selected beta tag without rebuild, resign, overwrite or force. From an unauthenticated clean temporary directory, refetch release metadata, both assets and manifests; independently verify hashes, signatures, fingerprint, exact capability hashes, license and prompt. Write strict publication and availability receipts atomically only after all checks pass. Classify any race or mismatch using the canonical policy: a public-object/artifact/source drift invalidates the candidate and returns to R0/R1 before a later beta is selected; only a strictly proven pre-publication transient may retain identity, and it still requires the policy's focused diagnosis and two complete clean gates.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "immutable publication|anonymous availability|no overwrite" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/publish.mjs --release release/phase2-beta-artifacts.json --receipt release/phase2-publication.json &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs --release release/phase2-beta-artifacts.json --publication release/phase2-publication.json --out release/phase2-availability.json</automated></verify>
  <done>The selected beta is published once and anonymously obtainable with exact identity and capability verification; no human update or live claim is made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| signed local artifacts → public release | Remote state can race or substitute bytes. |
| anonymous network → availability receipt | Downloaded metadata/assets are untrusted until independently verified. |
| receipts/runtime → update verifier | User/runtime claims cannot replace exact signed identity or native-platform proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-13-01 | S/T | publication identity | mitigate | Exact returdex repository, tag, commit/tree/build and no-overwrite checks. |
| T2-13-02 | T/R | anonymous artifacts | mitigate | Fresh unauthenticated refetch, hashes, Ed25519 signature/fingerprint and receipt binding. |
| T2-13-03 | S/T/E | update verifier | mitigate | Native-platform proof, strict schemas, safe codes and zero mutation authority. |
| T2-13-04 | I | receipts/output | mitigate | Allowlists and sensitive scans; no paths, Profile, account, source or credential data. |
</threat_model>

<verification>
Run targeted update/publication negative tests, publish once, then independently refetch and verify. Confirm release receipts retain live/native gaps and Phase 3 remains blocked.
</verification>

<success_criteria>
- No release plan changes more than ten files.
- The public assets are byte-identical to 02-39 and anonymously verifiable.
- Both platform update checkpoints consume a verifier finalized before publication.
- No post-release plan modifies release or live-gate implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-13-SUMMARY.md` with public version/tag/build, asset hashes, availability result and explicit native/live gaps.
</output>
