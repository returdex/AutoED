---
phase: 02-poc-live
plan: "13"
type: execute
wave: 9
depends_on:
  - "02-10"
  - "02-11"
  - "02-12"
files_modified:
  - scripts/release/phase2-gate.mjs
  - scripts/release/verify-phase2-update-gate.mjs
  - scripts/build/assemble.mjs
  - scripts/release/preflight.mjs
  - scripts/release/publish.mjs
  - scripts/release/verify-availability.mjs
  - tests/integration/phase2-release-gates.test.ts
  - release/phase2-test-report.json
  - release/phase2-beta-artifacts.json
  - release/phase2-install-prompt.md
  - release/phase2-publication.json
  - release/phase2-availability.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SEC-02
  - UAT-01

must_haves:
  truths:
    - "同一已选定 source commit/tree/build identity 上的 typecheck、完整 unit、integration、UI、macOS native 与敏感输出扫描全部通过后，统一 Moodle+EdStem beta 才可被签名或发布；任一失败立即阻断。"
    - "发布版本严格为尚未占用的下一个 `0.1.0-beta.N`；本地 tag、远程 tag、GitHub release、asset 或既有 receipt 任一冲突都不能覆盖、复用或就地修补，必须重新选号并从构建/测试/签名开始。"
    - "新 beta 继续使用 Phase 1 已由用户确认的 Ed25519 公钥指纹与 OS Keychain 私钥；下载内容、命令行、环境变量、仓库或 fixture 不能替换 trust root、提供私钥或跳过签名。"
    - "Git author/committer、隔离 GitHub login、repository owner/name/id/visibility、remote URL 与 PolyForm Noncommercial 1.0.0 均逐项真实核对；本地身份正确不替代远程身份，已有正确仓库也不能被重新创建或按名字猜测接管。"
    - "darwin-arm64 与 win32-x64 产物的 version/build/protocol/capability/manifest/signature/hash/size/dependency closure/support matrix 完整一致；Windows native/live 仍明确 not_run/human_needed，macOS 或静态闭包不能代填。"
    - "公开上传前，tracked/reachable history、工作树、test output、装配目录、archives、manifest、prompt 和 receipts 的敏感扫描均为零命中；Profile、Cookie、凭据、DB/WAL、日志、原始页面/网络、完整身份/课程资料、私钥/token 不进入 Git 或资产。"
    - "发布后必须以无认证客户端重新获取每个不可变资产，并重新验证 exact URL、完整 bytes、size、SHA-256、Ed25519 manifest、archive member closure、version/build/support matrix；HTTP 200 或 release 页面存在不等于可用。"
    - "publication/availability pass 只证明 beta 可获取且完整，不写 L/pass receipt、不证明学校登录/Profile/live/Windows 通过，也不解除 Phase 1/2 未完成或 Phase 3 blocked。"
    - "02-14/02-15 只能通过独立只读 update gate 消费发布交接：更新前校验 committed+fresh anonymous availability 与 exact prompt，更新后校验 strict 人工 receipt 和当前 loopback build/API/Worker/paired UI；验证器不启动、更新或重启产品，且不输出路径、PID、Profile 或私人身份。"
  artifacts:
    - path: "scripts/release/phase2-gate.mjs"
      provides: "绑定 commit/tree/build 的完整测试、去敏扫描与只含状态/哈希的 Phase2TestReport"
      exports:
        - "runPhase2QualityGate"
        - "validatePhase2TestReport"
    - path: "scripts/release/verify-phase2-update-gate.mjs"
      provides: "02-14/02-15 共用的严格只读 release handoff、可选 macOS update receipt 与 current loopback readiness 验证器"
      exports:
        - "verifyPhase2UpdateGate"
        - "validatePhase2MacosUpdateGateReceipt"
    - path: "scripts/build/assemble.mjs"
      provides: "保留 Phase 1 pair assembler，并新增单版本统一 auth beta 的双 target 装配与签名"
      exports:
        - "assemblePhase2Release"
    - path: "scripts/release/preflight.mjs"
      provides: "已有 repository receipt、增量版本、license、history/package/sensitive-output 与 build/test 绑定预检"
      exports:
        - "phase2ArtifactPreflight"
    - path: "scripts/release/publish.mjs"
      provides: "既有 returdex/AutoED repository 上单一不可覆盖 prerelease 的发布入口"
      exports:
        - "publishPhase2Release"
    - path: "scripts/release/verify-availability.mjs"
      provides: "单一增量 beta 的匿名完整字节、签名、hash 与 archive closure 复验"
      exports:
        - "verifyPhase2Availability"
    - path: "release/phase2-beta-artifacts.json"
      provides: "统一 Moodle+EdStem beta 的唯一 local built/signed/verified receipt"
      contains: "built_signed_verified_local"
    - path: "release/phase2-install-prompt.md"
      provides: "02-14 唯一可消费的 exact version/build/fingerprint/bootstrap hash/update prompt"
      contains: "publication/availability pass 后方可使用"
    - path: "release/phase2-availability.json"
      provides: "匿名重新获取全部资产后的 availability pass 与 machine-verifiable asset map"
      contains: "\"anonymous\": true"
  key_links:
    - from: "scripts/release/phase2-gate.mjs"
      to: "release/phase2-test-report.json"
      via: "每项完整 suite 与四类敏感扫描只记录 command id/result/evidence/hash，不保存 raw stdout/stderr"
      pattern: "typecheck|unit|integration|ui|native|sensitiveOutput"
    - from: "release/phase2-test-report.json"
      to: "release/phase2-beta-artifacts.json"
      via: "artifact receipt 固定 testReport SHA-256、source commit/tree 与相同 buildId"
      pattern: "testReportSha256|commit|tree|buildId"
    - from: "scripts/build/assemble.mjs"
      to: "scripts/release/trust.mjs"
      via: "两 target manifest 仍由既有 OS Keychain Ed25519 root 签名，无替代 key 参数"
      pattern: "signReleaseFile|trust-root.json"
    - from: "scripts/release/preflight.mjs"
      to: "release/publication.json"
      via: "增量发布必须绑定 Phase 1 已创建的 repository ID 与 immutable releases，不再走首次建仓路径"
      pattern: "1350421724|returdex/AutoED|repositoryId"
    - from: "scripts/release/publish.mjs"
      to: "release/phase2-publication.json"
      via: "新 tag/release/assets 全部成功后才原子记录单版本 immutable publication receipt"
      pattern: "publishPhase2Release|immutable"
    - from: "scripts/release/verify-availability.mjs"
      to: "release/phase2-availability.json"
      via: "无认证重新下载 artifact receipt 中每个 URL，复验 bytes/hash/signature/closure 后写 pass"
      pattern: "verifyPhase2Availability|anonymous"
    - from: "release/phase2-availability.json"
      to: "release/phase2-install-prompt.md"
      via: "02-14 只有在 version/build/fingerprint/asset-set 完全相等且 availability=pass 时才可展示 prompt"
      pattern: "version|buildId|fingerprint|assets"
    - from: "scripts/release/verify-phase2-update-gate.mjs"
      to: "release/phase2-beta-artifacts.json"
      via: "pre-update strict read-only mode 交叉校验 tests/publication/committed+fresh availability/prompt；post-update mode 交叉校验 strict receipt 与当前 loopback build/readiness"
      pattern: "verifyPhase2UpdateGate|Phase2MacosUpdateGateReceipt|--read-only"
---

<objective>
在既有 `returdex/AutoED`、PolyForm Noncommercial 1.0.0 与用户确认的不可覆盖 Ed25519 trust root 下，对 Phase 2 全部自动层执行最终质量/去敏门，构建并签名一个统一 Moodle+EdStem 认证 beta，发布为新的 immutable prerelease，再以匿名客户端完整重新取得并复验所有资产。

Purpose: 落实 D-01、UAT-01、T2-02/T2-08，使后续人工更新和 live 登录只面向一个经完整 suite、身份、许可、签名、hash、support matrix 和公开可用性证明的真实 beta；任何测试、身份、签名、远程冲突、敏感输出或资产错误都 fail closed。
Output: 可复用的 Phase 2 release gate、单版本装配/预检/发布/availability 扩展，以及 build-bound test、artifact、prompt、publication、anonymous availability receipts。发布不等于 live pass。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/01-beta/01-APPROVAL.md
@.planning/phases/01-beta/01-CONTEXT.md
@.planning/phases/01-beta/01-RESEARCH.md
@.planning/phases/01-beta/01-VALIDATION.md
@.planning/phases/01-beta/01-08-PLAN.md
@.planning/phases/01-beta/01-08-SUMMARY.md
@.planning/phases/01-beta/01-10-PLAN.md
@.planning/phases/01-beta/01-10-SUMMARY.md
@.planning/phases/01-beta/01-11-PLAN.md
@.planning/phases/01-beta/01-11-SUMMARY.md
@.planning/phases/01-beta/01-12-PLAN.md
@.planning/phases/01-beta/01-12-SUMMARY.md
@.planning/phases/01-beta/01-13-PLAN.md
@.planning/phases/01-beta/01-13-SUMMARY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-DISCUSSION-LOG.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-10-SUMMARY.md
@.planning/phases/02-poc-live/02-11-SUMMARY.md
@.planning/phases/02-poc-live/02-12-SUMMARY.md
@.planning/phases/02-poc-live/02-14-PLAN.md
@.planning/phases/02-poc-live/02-15-PLAN.md
@release/approval.json
@release/trust-root.json
@release/prerequisites.json
@release/publication.json
@release/availability.json
@release/beta-artifacts.json
@release/install-prompts.md
@LICENSE
@LICENSING.md
@scripts/build/assemble.mjs
@scripts/release/trust.mjs
@scripts/release/manifest.mjs
@scripts/release/preflight.mjs
@scripts/release/publish.mjs
@scripts/release/verify-availability.mjs

<interfaces>
Phase 1 已建立且不得替换的真实信任与仓库身份：

```text
repository        = returdex/AutoED
repositoryId      = 1350421724
remote URL        = https://github.com/returdex/AutoED.git
isolated login    = returdex
global login      = ywan1303 (只观察，禁止改变)
git author/name   = returdex
git email         = 73513006+returdex@users.noreply.github.com
algorithm         = Ed25519
public fingerprint= fe7168c33489a34aaac2cefba36bc62bca76f9406a4b7293927a6b7e22201557
Keychain service  = AutoED-Rebuild-Release
Keychain account  = release-ed25519-private-v1
LICENSE SHA-256   = c0ea4a896d2c8c394b29f9427589996db826cd501c512279ff0ed3ef48fabbe5
LICENSING SHA-256 = 6c2ebeac33c85ff1dca2ed6c23b6c4221885313b4b310381c7229f61f6b1cfd0
```

不得因既有 `release/prerequisites.json` 仍描述“首次创建前无 remote/repo”而重写或伪造它。Phase 2 增量预检必须读取 Phase 1 `release/publication.json`，验证当前 repository ID/owner/name/visibility、已发布 tag/asset 与 remote 精确相符；这是后续发布 receipt，不是新的首次信任审批。

执行时选择版本的唯一算法：匿名列举 `returdex/AutoED` releases/tags，隔离 `returdex` API 列举同一数据，读取本地 tags、`release/publication.json` 和所有 phase2 receipt；对所有匹配 `0.1.0-beta.N` 的 N 取最大值并选择 `N+1`。四处观察必须一致；任何无法列举、冲突、重复、非递增、已有同名 tag/release/asset 或 repository ID 不同都停止。若选择后发生竞态冲突，删除仅本地未发布的 staging（不得删远程/tag/旧资产），选择新的 N，并从 quality gate、build、native、sign、preflight 全部重跑。

本计划新增的稳定 handoff schema：

```typescript
export interface Phase2TestReport {
  schema: 1;
  plan: '02-13';
  status: 'pass';
  version: `0.1.0-beta.${number}`;
  commit: string;
  tree: string;
  buildId: string;
  commands: readonly {
    id: 'typecheck'|'unit'|'integration'|'ui'|'native';
    evidence: 'S'|'I'|'N';
    platform: 'cross-platform'|'macos';
    result: 'pass';
    commandSha256: string;
  }[];
  sensitiveOutput: {
    result: 'pass';
    trackedAndHistory: 'pass';
    workingTree: 'pass';
    capturedOutput: 'pass';
    artifactTreeAndArchives: 'pass';
    forbiddenMatches: 0;
  };
  gaps: { windowsNative: 'not_run'; live: 'not_run'; phase3Eligibility: 'blocked' };
}

export interface Phase2BetaArtifacts {
  schema: 1;
  plan: '02-13';
  status: 'built_signed_verified_local';
  repository: 'returdex/AutoED';
  repositoryId: 1350421724;
  version: `0.1.0-beta.${number}`;
  commit: string;
  tree: string;
  buildId: string;
  fingerprint: string;
  testReportSha256: string;
  capabilities: readonly [
    'moodle.auth_probe', 'moodle.course_visibility_probe',
    'edstem.auth_probe', 'edstem.course_visibility_probe',
    'auth.binding', 'auth.recovery', 'paired.auth_status', 'evidence.receipts'
  ];
  targets: readonly {
    target: 'darwin-arm64'|'win32-x64';
    buildId: string;
    manifest: string;
    signature: string;
    bootstrap: string;
    support: {
      artifactClosure: 'pass'; synthetic: 'pass'; integration: 'pass'; ui: 'pass';
      native: 'pass'|'not_run'; live: 'not_run';
    };
    assets: readonly { name: string; url: string; localPath: string; bytes: number; sha256: string }[];
  }[];
  shared: readonly { name: string; url: string; localPath: string; bytes: number; sha256: string }[];
}

export interface Phase2PublicationReceipt {
  schema: 1;
  plan: '02-13';
  status: 'published';
  repository: 'returdex/AutoED';
  repositoryId: 1350421724;
  version: string;
  tagCommit: string;
  buildId: string;
  immutable: true;
  releaseId: number;
  assets: readonly { name: string; bytes: number; sha256: string }[];
  publishedAt: string;
}

export interface Phase2AvailabilityReceipt {
  schema: 1;
  plan: '02-13';
  status: 'pass';
  repository: 'returdex/AutoED';
  repositoryId: 1350421724;
  version: string;
  buildId: string;
  fingerprint: string;
  anonymous: true;
  manifestHashes: readonly string[];
  assets: readonly {
    name: string; url: string; bytes: number; sha256: string;
    signatureVerified: true; closureVerified: true;
  }[];
  support: { macosNative: 'pass'; windowsNative: 'not_run'; live: 'not_run'; phase3Eligibility: 'blocked' };
  checkedAt: string;
}
```

`release/phase2-install-prompt.md` 是 02-14 的唯一提示来源：必须含 exact version/tag、repository ID、source commit/tree、build ID、上述固定 fingerprint、macOS/Windows bootstrap exact URL+SHA-256、安装根、升级预览/cleanup 条件、MCP reload、status/selfcheck、统一 Moodle+EdStem auth beta 能力、Windows `not_run/human_needed`、`availability.status=pass` 前禁止使用、发布不等于 live pass。不得含 placeholder、私钥/token、Profile path、学校 origin、账户/课程或登录数据。

02-14/02-15 及后续人工计划只允许调用独立只读 update gate，不得自行拼版本/hash。`scripts/release/phase2-gate.mjs` 继续只负责版本/identity/full-suite/sensitive-output release quality gate；不得把 update receipt、loopback runtime 或 UI readiness 职责塞回它。

本计划同时导出 strict receipt 与验证结果契约；receipt 可选，仅在 post-update mode 存在，unknown/extra key、自由文本或非 exact enum 都拒绝：

```typescript
export interface Phase2MacosUpdateGateReceipt {
  schema: 1;
  checkpoint: '02-14-macos-update';
  platform: 'macos';
  version: string;
  tag: string;
  artifactSha256: string;
  manifestSha256: string;
  buildId: string;
  result: 'pass';
  resultCode: 'UPDATE_COMPLETE';
  cleanup: 'complete';
  actualBuild: 'matched';
  entrypoints: 'matched';
  api: 'healthy';
  worker: 'healthy';
  pairedUi: 'ready';
  sourceConfiguration: { moodle: 'not_confirmed'; edstem: 'not_confirmed' };
  schoolAccess: 'not_started';
  windows: 'not_run/human_needed';
  phase3: 'blocked';
  observedAt: string;
  feedbackDigest: string;
}

export type Phase2UpdateGateRequest =
  | {
      mode: 'pre-update'; readOnly: true; platform: 'macos';
      artifactsPath: string; testsPath: string; publicationPath: string;
      availabilityPath: string; freshAvailabilityPath: string; promptPath: string;
    }
  | {
      mode: 'post-update'; readOnly: true; runtime: 'current';
      receiptPath: string; artifactsPath: string; availabilityPath: string;
    };

export interface Phase2UpdateGateResult {
  schema: 1;
  status: 'pass';
  mode: 'pre-update'|'post-update';
  version: string;
  buildId: string;
  artifactSha256: string;
  manifestSha256: string;
  availability: 'verified';
  runtime: 'not_checked'|'ready';
  api: 'not_checked'|'healthy';
  worker: 'not_checked'|'healthy';
  pairedUi: 'not_checked'|'ready';
  sourceConfiguration: 'not_checked'|{ moodle:'not_confirmed'; edstem:'not_confirmed' };
  schoolAccess: 'not_checked'|'not_started';
  windows: 'not_run/human_needed';
  live: 'not_run';
  phase3: 'blocked';
}
```

更新前 canonical CLI（`fresh-availability` 必须是本次调用前新生成、anonymous/full-byte/signature/hash/closure pass、时间不在未来且 age ≤ 10 分钟的临时 receipt；验证器读取但不创建或修改它）：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --artifacts release/phase2-beta-artifacts.json \
  --tests release/phase2-test-report.json \
  --publication release/phase2-publication.json \
  --availability release/phase2-availability.json \
  --fresh-availability <mktemp>/availability.json \
  --prompt release/phase2-install-prompt.md \
  --platform macos \
  --read-only
```

更新后 canonical CLI（02-15 同样复用）：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json \
  --artifacts release/phase2-beta-artifacts.json \
  --availability release/phase2-availability.json \
  --runtime current \
  --read-only
```

pre-update mode 逐字段核对 artifacts/tests/publication/committed availability/fresh availability/prompt 的 schema、version/tag/commit/tree/buildId/fingerprint/testReportSha256/repositoryId/immutable asset set、asset ID/name/URL/bytes/hash、manifest/signature/closure/support gaps及 prompt bytes/hash；fresh receipt 的 `checkedAt` 除新鲜性外不要求等于 committed receipt。post-update mode strict 校验 `Phase2MacosUpdateGateReceipt` 与 artifacts/availability，并只读发现当前受管安装的固定 loopback endpoint（不得接受 URL/port/browser handle 参数），要求 signed runtime status/selfcheck 的 version/buildId/artifact+manifest hash、API/Worker health 和既有配对 UI static readiness 全等；不得启动/重启 backend、打开浏览器、改变配对、确认来源或访问学校。两模式都要求 `--read-only`，参数混模、缺参、extra arg 或非 canonical path mode 均 fail closed。

验证器 stdout 只能是单行 `Phase2UpdateGateResult` JSON；stderr 只能是 stable safe error code，不得出现绝对/相对路径、PID/port、Profile/Cookie/token、GitHub login/email、学校/身份/course、raw receipt/HTTP/runtime body。运行前后输入文件 hash、Git status、remote refs、runtime process/entrypoint/pairing 状态必须不变；失败不写 receipt、不自动更新/修复、不生成 L evidence。
</interfaces>

<hard_gates>
- 只允许复用已批准的本机 Keychain release key 与隔离 `returdex` OAuth；不得请求/读取/打印/导出私钥或 token，不得改变全局 Git/GitHub 身份，不得创建第二 trust root 或接受下载自荐 key。
- 已有 `returdex/AutoED` 是 Phase 1 receipt 证明的正确公开仓库；本计划不得重新创建、删除、转移、改可见性、强推、改写历史、删除/替换任何旧 tag/release/asset，或触碰 `returdex/AutoED-legacy`。
- 只构建/发布程序、受管 Node/Chromium、安装器、manifest/signature/bootstrap、标准 license/source/prompt；不得访问 Moodle/EdStem/学校、打开官方登录、创建/读取真实 Profile、请求凭据/MFA、读取课程或写来源。
- full suite 或敏感扫描任一失败、macOS native 未真实执行、Windows 被误标 pass、identity/license/trust/build 不一致、remote/tag/asset conflict、签名/hash/closure/anonymous fetch 任一失败，都必须在发布或 live 前硬停。
- 发布和匿名 availability 只形成 distribution/automated evidence，不生成 UAT L receipt；02-14 更新、02-15 范围确认、02-16 登录/MFA 和后续 live 全部仍是硬人工门禁。
- Windows 原生/live 继续 `not_run / human_needed`；macOS pass、static Windows artifact、S/I/N 或公开 beta 不能使 Phase 1/2 complete，也不能解除 Phase 3 blocked。
</hard_gates>
</context>

<tasks>

<task type="auto">
  <name>02-13-1：选定不可覆盖版本并对 exact build 运行完整 suite 与敏感输出门</name>
  <read_first>AGENTS.md 的 Execution after plan approval、Credentials and login、Data integrity and verification、Installation/upgrades/releases；Phase 1 01-10/11/12/13 PLAN+SUMMARY；Phase 2 全部 CONTEXT/RESEARCH/VALIDATION/PATTERNS/UI-SPEC/PLAN-OUTLINE 与 02-10/11/12 SUMMARY；package.json、vitest.config.ts、playwright.config.ts；release/approval.json、trust-root.json、prerequisites.json、publication.json、availability.json；scripts/release/preflight.mjs、tests/integration/release-gates.test.ts；当前 git status/log/remote/tag（只读）。</read_first>
  <files>scripts/release/phase2-gate.mjs, scripts/release/preflight.mjs, tests/integration/phase2-release-gates.test.ts, release/phase2-test-report.json</files>
  <action>
先在 `tests/integration/phase2-release-gates.test.ts` 写失败测试，再实现 `scripts/release/phase2-gate.mjs` 与 `preflight.mjs` 的增量 release preflight。测试覆盖：四来源版本观察一致/不一致、已占用 N、并发占用、错误 repo ID/owner/visibility/remote、isolated login 错、global identity 被改、git author/committer/email 错、trust fingerprint drift、license byte/hash drift、任一 suite 非零、native skip/todo、captured output/working tree/artifact/history 敏感命中、Windows 被标 pass，以及 Phase3/live 被误标 pass；全部必须在 sign/publish spy 前失败。

版本选择严格执行 `<interfaces>` 算法；不得只看本地 tag 或在已有冲突上覆盖。以只读方式重新验证当前公开 repository 精确为 `returdex/AutoED`、ID `1350421724`、public、remote `https://github.com/returdex/AutoED.git`，Phase 1 publication receipt 中全部旧 release/tag/asset 仍存在且 hash/size 未改变；隔离 login=`returdex`、global observed login 仍 `ywan1303`、repo-local author/committer=`returdex`/`73513006+returdex@users.noreply.github.com`。已有仓库是预期状态，不再使用 `identityOnly()` 的“repo/remote 必须不存在”分支；新增 `phase2ArtifactPreflight` 必须绑定 existing repository receipt，保留 Phase 1 首次 preflight API 和 tests 不变。

`runPhase2QualityGate({version, commit, tree})` 先以 `AUTOED_RELEASE_VERSION=<selected>` 执行 production build 并读取 build identity，然后在同一 commit/tree、无中途源码变化下按顺序运行以下 exact commands：

```text
npm run typecheck
npm run test:unit -- --run
node scripts/dev/runtime.mjs npm run test:integration -- --run
npm run test:ui
node scripts/dev/runtime.mjs npm run test:native -- --run
```

每项必须真实执行、退出 0、无 `.skip/.todo/.only`/unsupported-as-pass；native 必须报告实际 `darwin-arm64`、evidence=N，并包含 02-12 profile ownership/lifecycle suite。Windows native 不运行且只能记 `not_run`。runner 可把 stdout/stderr 流式传给当前终端和内存 scanner，但不得把 raw output 写入 report/log；report 只记录 command id、SHA-256、evidence/platform/result、commit/tree/buildId。测试中或运行期间检测到 source/tree/build 变化立即 `BUILD_IDENTITY_DRIFT` 并删除未签 staging。

敏感扫描必须四路全部 pass：一是复用 `scanReachableHistory` 扫将推送 tree 的全部 reachable blobs；二是检查 tracked/untracked/staged/unstaged 工作树，拒绝 Profile、Cookie、数据库/WAL/SHM、backup、log/export、`.env`、private key/token、runtime identity/sample 等路径或资料；三是对 suite stdout/stderr 的内存流做 secret/绝对 Profile 路径/真实姓名邮箱课程/学校 URL/raw DOM/HTML/request-response/header-body/credential/input/screenshot/trace/HAR/video/console capture sentinel 扫描，只输出 safe code 和计数；四是在 Task 2 装配后复用同一 scanner 读取 public tree 与每个 archive member，`artifactTreeAndArchives` 在 Task 2 通过前 report 不得成为 `status:pass`。代码/测试中的安全关键词不因字面出现而误报；扫描必须针对路径、high-entropy secret、真实/注入 sentinel 和输出 payload，并用 immutable reviewed-fixture hash+path 机制处理既有 synthetic negative fixtures。任何新 exception 需停止人工审查，不得自动加入 allowlist。

生成 `release/phase2-test-report.json` 严格满足 schema；不得保存 stdout/stderr、绝对路径、PID、nonce、身份、学校/course、Profile/Cookie 或任何 S/I/N/L receipt payload。Task 1 结束时允许 report 临时 `status:'candidate_artifacts_pending'`；Task 2 完成 artifact scan、build binding 与 packaged native selfcheck 后才原子改为 `pass`，在此之前不得 sign/publish。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "version identity full suite sensitive gate" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-gate.mjs --version auto --out release/phase2-test-report.json</automated>
  </verify>
  <acceptance_criteria>
    1. 四来源 version observation 对同一最大 N 一致并选择 N+1；任一来源不可用/不一致、同名 tag/release/asset/receipt 已存在或 race 占用时 quality/sign/publish spy=0，且不修改任何远程或旧 release。
    2. identity preflight 实际输出 login=`returdex`、repository=`returdex/AutoED`、repositoryId=`1350421724`、visibility=public、remote exact、globalIdentityUnchanged=true、git author/committer/email exact；任一差异测试非零。
    3. report 恰有 typecheck/unit/integration/ui/native 五项且全部 result=pass；native=macos/N，Windows 仅 gaps.windowsNative=not_run；命令或源码任一失败/skip/identity drift 不能生成 pass report。
    4. `release/phase2-test-report.json` 的 version/commit/tree/buildId 与 `build/identity.json`、Git 当前 tree 一致；JSON 不含 rawOutput/stdout/stderr/path/PID/identity/course/Profile/Cookie/credential 字段。
    5. reachable history、working tree、captured output 三路 scan 先通过；artifact scan 尚未完成时 report 不得为最终 pass、sign/publish 调用为 0。
    6. `LICENSE` 与 `LICENSING.md` 分别字节一致且 SHA-256 精确为 `c0ea4a896d2c8c394b29f9427589996db826cd501c512279ff0ed3ef48fabbe5`、`6c2ebeac33c85ff1dca2ed6c23b6c4221885313b4b310381c7229f61f6b1cfd0`；许可名称不得被替换成 Apache/MIT 或缩小标准权限。
    7. 定向 gate tests 与真实完整命令退出 0；无学校请求、官方登录、真实 Profile、credential/MFA、课程读取、source write/download 或 L receipt。
  </acceptance_criteria>
  <done>已为一个未占用 beta 版本建立 exact commit/tree/build 的完整自动测试证据；身份、许可、信任和前三路敏感扫描通过，但在 artifact scan/签名完成前仍不可发布。</done>
</task>

<task type="auto">
  <name>02-13-2：装配统一双来源 beta、绑定测试报告并以既有 trust root 签名</name>
  <read_first>Task 1 的 phase2-test-report.json、selected version、commit/tree/buildId 与 gate 实现；01-08/01-10/01-11/01-12/01-13 SUMMARY；02-10/11/12 SUMMARY；scripts/build/assemble.mjs、platform-matrix.json、build.mjs、native-artifacts.mjs；scripts/release/manifest.mjs、trust.mjs、preflight.mjs；packages/installer/src/verify-manifest.ts、archive-core.ts；release/trust-root.json、publication.json、LICENSE、LICENSING.md；tests/integration/artifact-assembly.test.ts、manifest-verification.test.ts、release-trust.test.ts、phase2-release-gates.test.ts。</read_first>
  <files>scripts/build/assemble.mjs, scripts/release/preflight.mjs, tests/integration/phase2-release-gates.test.ts, release/phase2-test-report.json, release/phase2-beta-artifacts.json, release/phase2-install-prompt.md</files>
  <action>
先扩展 `phase2-release-gates.test.ts` 的单版本装配、build/report binding、support matrix、manifest/signature/hash/closure、license、sensitive archive 与 prompt negative tests，再在 `scripts/build/assemble.mjs` 新增 `assemblePhase2Release({version,testReportPath,outputPath})` 及 CLI `--phase2-release --version <exact> --test-report release/phase2-test-report.json --out release/phase2-beta-artifacts.json`。保留 `assembleReleasePair` 和 Phase 1 A/B tests；不得复制 manifest/verifier/signing 实现或把既有 pair 改写为当前 beta。

单版本 build 的唯一 variant 是 `PHASE2_AUTH`，必须包含 Phase 1 echo/digest 兼容能力与 `<interfaces>` 中八项 auth capabilities，以及 API/Worker/status UI/installer/bootstrap/native-report/受管 Node 24.20.0/Playwright Chromium 1234 (151.0.7922.34)/SQLite/keyring 的完整 target closure。禁止复制宿主 node_modules、runtime DB/Profile/log/export 或开发 secrets。分别装配 `darwin-arm64`、`win32-x64`，每个 manifest 绑定 product/schema/protocol、version、commit/tree/buildId、testReportSha256、固定 capability tuple、target OS/arch/min version、dependency versions/source/integrity/license、所有 artifact name/role/format/files/unpacked size/bytes/SHA-256 和 support matrix。

support 必须诚实：两 target artifactClosure/S/I/UI=pass；darwin native=pass 且在装配后的 program 上重新执行 packaged selfcheck/native diagnostics，并确认报告 buildId 等于 manifest；win32 native=not_run、两 target live=not_run。Windows PE/NAPI/closure 静态检查不变成 native pass。02-12 的 source-native report 只有 commit/tree 相同才可作为前置，并必须由本 task packaged diagnostic 重新绑定 final build；任何 mismatch 停止。

manifest 精确 bytes 继续调用 `scripts/release/trust.mjs` 的 `signReleaseFile`，私钥仅从已批准 Keychain service/account 使用；API 不新增 trust-key/private-key/skip-sign/unsigned 参数，不读 key bytes，不将 fingerprint 变化自动接受。每 target 本地重验 Ed25519 signature、public fingerprint、manifest、每个 artifact full bytes/hash/size 和 archive member closure。共享资产必须含 byte-identical `LICENSE`、`LICENSING.md`、当前批准 source archive 和 `release/phase2-install-prompt.md`；source archive 只来自 exact Git tree/allowlist，不含 `.git`、runtime、release key、token 或私人资料。

在签名前完成 Task 1 第四路 artifact scan：扫描 staging/public tree、所有 regular files 与展开的 tar/zip members、manifest/signature/bootstrap/source/prompt/receipts，拒绝 forbidden path、symlink/junction escape、duplicate/archive traversal、secret/high-entropy token/private key、绝对本机/Profile path、DB/WAL/log/raw DOM/network/identity/course/school/credential/input/screenshot/trace/HAR/video 内容。扫描 pass 后原子将 `phase2-test-report.json` 更新为最终 `status:'pass'` 并写其 SHA-256；然后生成符合 schema 的 `phase2-beta-artifacts.json`，所有 localPath 必须在 `.runtime/releases/<version>/` 且 URL 精确为 GitHub immutable release URL。

生成 `release/phase2-install-prompt.md`，精确填入 version/tag/repository ID/commit/tree/buildId/fingerprint、macOS/Windows bootstrap URL+SHA-256、固定安装根、更新预览/backup/cleanup=complete、MCP reload/status/selfcheck、统一 Moodle+EdStem auth beta 能力和禁止粘贴密码/MFA/身份/课程。明确 `availability.status=pass 前禁止使用`、Windows native/live not_run/human_needed、publication 不等于 live；不得残留 `UNESTABLISHED`、`TBD`、`待填写` 或让用户手输 URL/trust key/Profile path。prompt bytes/hash 纳入 shared assets 与 signed manifest relation。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts tests/integration/artifact-assembly.test.ts tests/integration/manifest-verification.test.ts tests/integration/release-trust.test.ts -t "phase2 single release|assembly|manifest|trust" &amp;&amp; node scripts/dev/runtime.mjs node scripts/build/assemble.mjs --phase2-release --version-from release/phase2-test-report.json --test-report release/phase2-test-report.json --out release/phase2-beta-artifacts.json &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/preflight.mjs --phase2-artifacts release/phase2-beta-artifacts.json --test-report release/phase2-test-report.json</automated>
  </verify>
  <acceptance_criteria>
    1. `phase2-beta-artifacts.json` 只有一个 version、两个 target、固定八项 auth capabilities；version/commit/tree/buildId/testReportSha256/fingerprint 在 report、artifact receipt、两 manifest、两 packaged diagnostics 中完全一致。
    2. darwin support 为 artifactClosure/S/I/UI/native pass、live not_run；win32 为 artifactClosure/S/I/UI pass、native/live not_run。任何代码路径不能把 Windows 静态 closure 或 macOS result 改成 Windows native/live pass。
    3. 两 target manifest/signature 由 fingerprint `fe7168c33489a34aaac2cefba36bc62bca76f9406a4b7293927a6b7e22201557` 验证；key replacement、unsigned、bad bytes/hash/size/closure/build/testReport 或 downgrade/replay negative tests 全部拒绝。
    4. artifact tree/archive/source/prompt scan forbiddenMatches=0；public assets 无 Profile/Cookie/storageState/credential/DB/WAL/log/raw identity/course/page/network/private key/token 或本机绝对路径，Git status 无 runtime/Profile/DB/log 泄漏。
    5. 共享 `LICENSE`/`LICENSING.md` 与批准 bytes/hash 相同；source archive 对 exact commit/tree；prompt 无 `UNESTABLISHED`、`TBD`、`PLACEHOLDER`、任意 URL/trust-key/Profile 输入。
    6. prompt 包含 exact version、repositoryId=1350421724、commit/tree/buildId/fingerprint、两 bootstrap URL/SHA-256、更新/cleanup/MCP/selfcheck 步骤、availability 前禁止使用、Windows/live 缺口和 publication≠live pass。
    7. 本地 preflight 退出 0 才允许 Task 3；任一失败不得签署新 manifest、创建本地 tag 或调用远程 publish。
  </acceptance_criteria>
  <done>统一 Moodle+EdStem beta 已在本地以 exact test report/build、双 target closure、固定许可和既有 Ed25519 root 完整签名/复验；仍未因本地成功产生公开可用性或 live 证据。</done>
</task>

<task type="auto">
  <name>02-13-3：向既有仓库发布单一 immutable prerelease，并匿名重取全部资产</name>
  <read_first>Task 2 的 test/artifact/prompt receipts 与 preflight 输出；Phase 1 01-11/12/13 PLAN+SUMMARY；02-14-PLAN.md 与 02-15-PLAN.md 的 canonical CLI、`Phase2MacosUpdateGateReceipt` 和 hard gates；release/publication.json、availability.json、prerequisites.json、trust-root.json；scripts/release/preflight.mjs、publish.mjs、verify-availability.mjs；tests/integration/release-gates.test.ts、phase2-release-gates.test.ts；当前本地/远程 tag、release、asset、repo ID、remote、isolated/global login（先只读解析）。</read_first>
  <files>scripts/release/phase2-gate.mjs, scripts/release/verify-phase2-update-gate.mjs, scripts/release/publish.mjs, scripts/release/verify-availability.mjs, tests/integration/phase2-release-gates.test.ts, release/phase2-publication.json, release/phase2-availability.json</files>
  <action>
先为既有 repo 增量发布、冲突/部分失败、旧资产不可变、匿名 redirect/full-byte/signature/closure negative cases 写失败测试，再扩展发布工具。`publishPhase2Release(artifactPath, receiptPath)` 与 CLI `--release release/phase2-beta-artifacts.json --receipt release/phase2-publication.json` 必须调用 Task 2 `phase2ArtifactPreflight`，再重新查询四来源版本状态、git tree/身份、isolated/global login、repo owner/name/id/visibility、remote 与旧 publication asset map；检查到 staging 后版本被占用、旧 tag/release/asset bytes 变化、repo ID 不是 1350421724、任何身份/许可/trust/build/report drift 都在 push/upload 前停止。

此路径只允许在既有 public repository ID `1350421724` 上新增一个 annotated tag 和一个 prerelease；绝不走 create repository、adopt by name、force push、tag move/delete、release edit/delete、asset overwrite/clobber。tag 必须指向 artifact receipt 的 exact commit/tree。若本地同名 tag 已存在但远程未发布，也视为冲突并按 `<interfaces>` 从 quality gate 重跑；不把 tag 指向相同 commit 当作可复用。发布 source tree、tag、release 与 receipt 必须保持可追溯；网络/API/权限中途失败记 `failed`/`publishing_incomplete` 并阻断 availability/live，不删除远程已成功对象、不谎称 published，等待安全重试/人工判断。

上传的 asset set 必须与 `phase2-beta-artifacts.json` exact-name/size/SHA-256 一致且无额外项；release notes 明确 unified Moodle+EdStem authentication POC beta、PolyForm Noncommercial 1.0.0、macOS automated/native 状态、Windows native/live not_run、human UAT not_run、publication not live pass。全部 API 返回后读取 remote tag commit、release ID、asset IDs/name/size/digest，并生成严格 `Phase2PublicationReceipt`；不得记录 token、local path、IP、完整 GitHub credential response 或 private metadata。

扩展 `verify-availability.mjs` 新增 `verifyPhase2Availability({artifactPath,publicationPath,outPath})` 与 CLI：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs \
  --release release/phase2-beta-artifacts.json \
  --publication release/phase2-publication.json \
  --out release/phase2-availability.json
```

它必须使用无 Authorization/Cookie 的 fresh fetch，资产 URL 只能从 exact `https://github.com/returdex/AutoED/releases/download/<version>/<encoded-name>` 开始，redirect 每跳仅允许 GitHub release asset hosts、HTTPS、无 userinfo/fragment/private IP，且最终完整 bytes 必须匹配 content-length/receipt size/SHA-256。下载全部 assets 到新的受限临时根，在内存/临时根重新验证两 manifest 的 Ed25519 signature/fingerprint、version/commit/tree/buildId/testReportSha256/support matrix、每个 artifact hash 与 archive members，再验证 prompt/license/source/publication asset set；HTTP 200、部分资产、只验 manifest、只验 hash、authenticated cache 或 release page 均不能 pass。finally 清理仅此 verifier 自建临时根。

只有全部验证成功才原子写 `Phase2AvailabilityReceipt status=pass, anonymous=true`。随后新增独立 `scripts/release/verify-phase2-update-gate.mjs`，导出 `verifyPhase2UpdateGate` 与 `validatePhase2MacosUpdateGateReceipt` 并实现 `<interfaces>` 两个互斥 canonical CLI；保留 `phase2-gate.mjs` 的版本/identity/full-suite/sensitive-output release quality gate 职责与 API，不从其中搬走或复制 release 检查逻辑。

先为 update verifier 写 exact negative matrix：pre-update 下分别篡改/missing/extra artifacts、tests、publication、committed availability、fresh availability、prompt 的每个绑定字段；fresh receipt 缺失、非 anonymous、非 full-byte/signature/hash/closure pass、未来时间、超过 10 分钟、asset extra/missing；错误 repository ID、mutable publication、Windows/live/Phase3 被抬高；prompt bytes/hash 或 embedded URL/version/hash drift；缺 `--read-only`、混入 receipt/runtime、extra arg、非 macos platform。post-update 下覆盖 receipt missing/extra key、非 exact enum、feedbackDigest 非 SHA-256、observedAt 无 timezone/在未来、version/tag/artifact/manifest/build mismatch、cleanup_pending、build/entrypoint/API/Worker/UI 任一非 pass、来源已 confirmed/unknown、school access 已开始、Windows/live/Phase3 抬高；current runtime absent/stale/wrong version/build/hash/signature、API/Worker unhealthy、paired UI unavailable、接受外部 URL/port/browser handle、以及 verifier 尝试 spawn/restart/write/rename/fetch/login/浏览器操作。所有 negative case 必须非零并断言文件 hash、Git status、remote refs、进程/entrypoint/pairing snapshot 与 L row count 前后相等。

pre-update mode 只 strict read 六个输入，按照 schema 交叉核对 committed 与本次 fresh anonymous availability；fresh availability 必须由上一个 `verify-availability.mjs` 命令写入 verifier 外部 `mktemp`，`checkedAt` 是有 timezone 的有效 ISO-8601、不得在未来且 age ≤ 10 分钟。除 `checkedAt` 外，两个 availability 的 repository/version/build/fingerprint/manifest hashes/exact asset proof/support 必须相等，并与 tests/artifacts/publication/prompt 完整绑定。post-update mode 仅接受 strict `Phase2MacosUpdateGateReceipt`、artifacts、availability 与 literal `--runtime current`；从既有受管安装 inventory/固定 loopback discovery 读取当前 signed status/selfcheck 和 paired UI static readiness，不接受调用者 URL/port/path/selector/browser handle，要求 exact target version/build/artifactSha256/manifestSha256、API/Worker healthy、pairedUi ready、两来源 not_confirmed、schoolAccess not_started。它不得启动/重启/更新 backend，不改变 entrypoint/pairing，不打开浏览器或学校。

两模式都使用 strict argv allowlist 和 `--read-only` hard requirement，只输出 `<interfaces>` 的单行 allowlisted result JSON；error 只输出 stable code。输出 scrubber 必须拒绝路径、PID/port、Profile/private identity、login/email/course、secret/raw body；即使恶意 fixture 把这些值置入 exception/message，也只能得到 safe code。将该脚本、两种 exact CLI、schemas、negative tests 和 public release handoff hash 纳入 `phase2-beta-artifacts.json`/signed manifest/source archive；02-14/02-15 只消费此文件，不再调用 `phase2-gate.mjs --verify-update-handoff`。不得自动执行更新、启动产品、打开登录或写 EvidenceLedger L；任何 handoff/fresh/runtime mismatch 保持 human_needed/live blocked。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts tests/integration/release-gates.test.ts -t "incremental immutable publish|anonymous availability|read-only update gate|legacy release regression" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/publish.mjs --release release/phase2-beta-artifacts.json --receipt release/phase2-publication.json &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs --release release/phase2-beta-artifacts.json --publication release/phase2-publication.json --out release/phase2-availability.json &amp;&amp; update_gate_tmp=$(mktemp -d) &amp;&amp; trap 'rm -rf "$update_gate_tmp"' EXIT &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs --release release/phase2-beta-artifacts.json --publication release/phase2-publication.json --out "$update_gate_tmp/availability.json" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --artifacts release/phase2-beta-artifacts.json --tests release/phase2-test-report.json --publication release/phase2-publication.json --availability release/phase2-availability.json --fresh-availability "$update_gate_tmp/availability.json" --prompt release/phase2-install-prompt.md --platform macos --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. publication receipt 的 repository/ID/version/tagCommit/buildId/releaseId/asset map 与 artifact receipt、actual GitHub API 完全一致，`immutable=true`；旧 Phase 1 tags/releases/assets 在发布前后 name/id/size/hash 不变。
    2. 错 login/global identity drift/repo ID/visibility/remote/tag/release/asset conflict、force/overwrite、部分 upload、API/权限失败均非零退出且 availability 不为 pass；工具没有 create/delete/edit/force/clobber 路径。
    3. availability 使用无 Authorization/Cookie fetch，匿名重取 asset count 与 artifact/publication exact set 相等；每项 URL/bytes/content-length/SHA-256/signature/closure 均通过，任一篡改/缺失/extra/redirect/build/support mismatch 失败。
    4. `phase2-availability.json` 严格含 `plan:'02-13'`、status=pass、repositoryId=1350421724、version/buildId/fingerprint、anonymous=true、两 manifest hashes、全部 asset proofs、macosNative=pass、windowsNative/live=not_run、phase3Eligibility=blocked。
    5. pre-update canonical CLI 对 artifacts/tests/publication/committed availability/fresh availability/prompt 返回 `mode='pre-update',status='pass'`；fresh receipt 是 ≤10 分钟、非未来的独立 anonymous full-byte证明，除 checkedAt 外与 committed receipt/exact asset set 相等。运行前后所有输入 hash、Git、remote、runtime/pairing 与 L row count 不变。
    6. post-update fixture canonical CLI `--receipt <strict fixture> --artifacts ... --availability ... --runtime current --read-only` 只在 receipt exact 且 loopback version/build/artifact+manifest hash、API/Worker、paired UI、source not_confirmed/school not_started 全部相符时 pass；receipt/runtime 任一 negative case 均 nonzero，write/spawn/restart/browser/network-to-school spies=0。
    7. 两模式 stdout 仅含 `Phase2UpdateGateResult` allowlisted fields，stderr 仅 stable safe code；注入 path/PID/port/Profile/login/email/identity/course/token/raw body 的 fixture 不能使这些值出现在输出。脚本 hash 已进入 signed artifact/source handoff，02-14/02-15 不需猜版本/hash或访问私钥。
    8. publication/availability receipts 与 captured output 无 token/private key/local path/Profile/Cookie/学校/身份/课程/raw HTTP body；发布 assets 通过最终 T2-02 scan。
    9. 命令退出 0 只证明 distribution handoff 可进入人工更新门；EvidenceLedger L row 增量=0、Windows 仍 not_run/human_needed、Phase 1/2 incomplete、Phase 3 blocked，未执行安装、官方登录/MFA或任何来源访问。
  </acceptance_criteria>
  <done>一个新的统一双来源 beta 已在既有 returdex/AutoED 以不可覆盖 tag/release 发布，全部资产可由匿名客户端完整重取并复验；exact update prompt 可交给 02-14，但所有 human/live/Windows 门禁仍未执行。</done>
</task>

</tasks>

<threat_model>
本计划沿用 OWASP ASVS 5.0 L1 的构建完整性、身份/访问控制、密码学、敏感数据保护、安全配置、文件处理、日志与供应链验证原则；不声称完整合规或平台 code signing/notarization。下列 Critical/High 项全部 disposition=`mitigate`，任一失败即阻断发布或 live。

## Trust Boundaries

| Boundary | 不可信输入/风险 | 强制控制 |
|---|---|---|
| source/test workspace → release candidate | 测试未全跑、源码变化、敏感 runtime/Profile/身份资料混入 | exact commit/tree/build；完整五套命令；四路敏感扫描；artifact allowlist/closure |
| version/identity receipts → GitHub API | 账号错误、同名 repo/ID 冲突、旧资产篡改、版本覆盖 | isolated returdex + repo-local identity；固定 repository ID/remote；四来源版本共识；无 force/edit/delete/clobber |
| local build → signed manifest | build/test/report drift、私钥泄漏、下载自荐 key、unsigned bypass | 既有 Keychain key；固定 fingerprint；精确 manifest bytes；testReport hash；无 key/skip 参数 |
| archives/bootstrap/prompt → installer/user | path traversal、错误 target、hash/size/closure/license/support 欺骗 | 双 target inventory、safe archive、signature/hash/size、byte-identical license、exact support matrix/prompt |
| authenticated publish → public anonymous retrieval | 部分上传、cache/redirect/HTTP 200 假可用、资产替换 | exact asset set；fresh no-auth full-byte GET；redirect allowlist；重新验签/hash/closure/build/prompt |
| release/update receipts → current loopback runtime | receipt 篡改、旧 build、假 health/UI、调用方注入 URL/port、验证器泄漏本机状态 | strict schema/argv；signed build/hash 绑定；固定 loopback discovery；只读前后快照；allowlisted 脱敏输出 |
| automated publication → EvidenceLedger/phase gate | publish/availability 被冒充 live/Windows/user acceptance | receipts 固定 distribution 状态；无 L writer；Windows/live not_run；phase3Eligibility=blocked |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Severity | STRIDE | Component | ASVS L1 intent | Mitigation / blocking assertion |
|---|---:|---|---|---|---|
| T2-02 | Critical | I | test output、history、artifact、receipt、logs | V8 data protection、V14 secure config、V16 logging | tracked/history/working/output/artifact/archive 四路扫描；无 Profile/Cookie/credential/DB/raw identity/network/private key/token。任一命中即阻断 sign/publish/live。 |
| T2-08 | High | T/R/E | test/support/publication/availability receipts、Phase3 gate | V1 trust classification、V4 authority、V13 business rules、V16 provenance | S/I/N/L 与 OS 明确；Windows/live not_run；publication/availability 无 L authority且 Phase3 blocked。任一自动伪 live/跨格即 blocker。 |
| T-01-18（继承） | Critical | S/T/I | Keychain Ed25519 trust、manifest、bootstrap | V6 cryptography、V11 crypto inventory、V15 secure communication | 固定已批准 fingerprint、exact manifest bytes、OS key 不导出、无下载 key/skip-sign；key/signature/hash drift 即 blocker。 |
| T-01-19（继承） | Critical | S/I/R/E | Git/GitHub identity、repository、public assets | V4 access control、V13 API、V16 audit | returdex 隔离 login+repo-local identity、repository ID 1350421724、remote/visibility、旧资产 immutable、匿名复验；任一冲突即 blocker。 |
| T-01-21（继承） | Critical | T/R | local build→public release | V5 file validation、V13 transaction integrity、V14 config | commit/tree/build/test report/target manifest 全绑定；single immutable tag/release；部分失败不称 published。 |
| T2-13-SSRF | High | S/T/D | anonymous availability fetch/redirect | V5 URL validation、V12 file/network safety、V13 API | exact GitHub URL、HTTPS redirect host allowlist、无 private IP/userinfo、size/time limits、fresh temp root；异常 redirect/partial bytes 即 blocker。 |
| T2-13-UPDATE | High | S/T/I/E | update handoff receipt、current loopback status/UI、verifier output | V4 authority、V5 validation、V8 data protection、V13 business logic、V16 logging | 独立 strict read-only verifier；互斥 canonical argv；receipt/build/hash/health/UI exact binding；禁止 caller URL/port/browser handle 与任何写/启动/重启；仅 safe code/allowlisted JSON。任一 drift/leak 即 blocker。 |
</threat_model>

<verification>
1. 版本/身份/质量门：运行 Task 1 exact full suite；确认五项命令全部实际执行、四来源版本一致、identity/license/trust exact，前三路敏感扫描 pass。
2. 装配/签名：运行 Task 2 CLI 与 preflight；两 target manifest、signature、hash、size、closure、build/test report/support matrix、license/prompt 全部 exact，第四路 artifact scan pass。
3. 发布前再次运行 `npm run typecheck && npm run test:unit -- --run && node scripts/dev/runtime.mjs npm run test:integration -- --run && npm run test:ui && node scripts/dev/runtime.mjs npm run test:native -- --run`；源码/tree/build 变化则丢弃 staging 并从 Task 1 重来。
4. identity-only external check：隔离 login=returdex、global=ywan1303 unchanged、local git identity exact、repository=returdex/AutoED ID 1350421724 public、origin exact；不输出 token。
5. immutable publish：新 tag/release/asset set 精确，旧 release map 前后相同；任何冲突/部分失败无 availability pass。
6. anonymous availability：无 auth 完整下载所有资产并重验 signature/hash/size/archive closure/version/build/testReport/support/prompt；availability JSON 与 prompt exact match。
7. read-only update handoff：用 `mktemp` fresh availability 运行 pre-update canonical CLI；再用 strict receipt/current runtime fixture 运行 post-update canonical CLI 和完整 negative matrix，确认输入/Git/remote/process/pairing/L ledger before/after 不变且输出无路径/PID/Profile/private identity。
8. 最终去敏与 gate：tracked/history/working/output/artifact/receipts/verifier output 再扫零命中；L row 增量=0、Windows/live not_run/human_needed、Phase 1/2 incomplete、Phase 3 blocked。
</verification>

<success_criteria>
- D-01：Moodle 与 EdStem 的固定 probes、binding、recovery、paired status 与 receipt 能力在同一个新 beta/build 中装配、签名和发布，不拆成 Moodle-only live 资产。
- AUTH-01–AUTH-04/SEC-02：完整 typecheck/unit/integration/UI/native 与 T2-01–T2-08 上游矩阵全 green；native/profile/worker/adapter/API/UI 实际进入相同 commit/tree/build 的发布证据。
- T2-02：代码历史、工作树、test output、public tree/archives 和 release receipts 均无敏感/运行资料或私钥/token，既有 Profile/学校数据从未被访问。
- release identity：只有 repo-local/isolated `returdex` 可向 public repository ID 1350421724 发布；global identity 未改，legacy repo/remote 未触碰，PolyForm Noncommercial 1.0.0 bytes 未改。
- release integrity：新 `0.1.0-beta.N` 未占用、不可覆盖，两个 target 的 version/build/manifest/signature/hash/closure/support matrix 与 test report 完整绑定，旧资产不变。
- availability：无认证重新取得全部资产并重验成功，exact update prompt 含真实 version/build/fingerprint/bootstrap hash 且仅在 availability pass 后可交给 02-14。
- update handoff：`verify-phase2-update-gate.mjs` 作为签名 release handoff 的只读组成，pre-update 严格绑定 immutable tests/artifacts/publication/committed+fresh availability/prompt，post-update 严格绑定可选 `Phase2MacosUpdateGateReceipt` 与 current loopback build/API/Worker/paired UI；不输出路径/PID/Profile/private identity，不改变 runtime 或 evidence。
- UAT-01/T2-08：发布成功不写 L 或 live pass；Windows native/live 仍 `not_run/human_needed`，Phase 1/2 不整体完成，Phase 3 继续 blocked。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-13-SUMMARY.md`，只记录脱敏 version/build/commit、repository ID、fingerprint、asset/test/scan counts、manifest/availability hashes、support gaps 和 02-14 prompt path；不得记录私钥/token、Profile/PID/path、Cookie/credential、学校/身份/课程、raw test/HTTP output 或 live pass。SUMMARY 必须明确 publication/availability ≠ update/live acceptance，Windows/L 与 Phase 3 仍被阻断。
</output>
