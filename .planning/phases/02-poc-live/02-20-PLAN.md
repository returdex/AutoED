---
phase: 02-poc-live
plan: "20"
type: execute
wave: 16
depends_on:
  - "02-19"
files_modified:
  - scripts/release/phase2-live-gate.mjs
  - tests/integration/phase2-live-gate.test.ts
autonomous: false
requirements:
  - AUTH-02
  - AUTH-04
  - UAT-01
must_haves:
  truths:
    - "B3 只有在 02-19 SUMMARY 已由真实用户完成且 current EvidenceLedger 中 Moodle/EdStem 的 `B2_worker_restart×macos×L` exact cells 均为 current-build pass 时才能开始；不存在 B2 planning receipt 或 SUMMARY 代填。"
    - "用户通过真实 macOS Codex 应用的 Quit 动作完全退出宿主；关闭窗口、reload MCP、运行 selfcheck、spawn 新 Codex 子进程、测试 harness 或只断开当前对话都不能算宿主退出。"
    - "Codex 完全退出期间，既有受管 backend 与 Worker 保持独立运行；用户只运行已发布安装的只读 status 命令完成最小状态读取，该命令不启动/重启服务、不创建 selfcheck/job、不访问学校。"
    - "用户重新进入的是实际安装的 Codex 客户端；受认证 host handshake 必须证明同一 installation、目标 beta version/build/artifact 与一个新的真实 host instance，不能用新 spawn 的 selfcheck/CLI/MCP probe 冒充 re-entry。"
    - "重新进入后，Moodle/EdStem 的认证状态、confirmed binding、approved scope 与 last-success 分开且一致；B3 不读取课程正文、帖子、文件、成绩，不发新的学校 probe，也不把旧 last-success 伪装为本轮来源访问。"
    - "paired server workflow 是唯一 `human_action/L` writer：它将 actual host exit、退出期间 status read、backend/Worker continuity、actual client re-entry 与本轮用户 action 绑定到 Moodle/EdStem 的独立 `B3_codex_exit` cells；repo-side gate 只读核对。"
    - "B3 拥有独立脱敏 pass/failed/human_needed 结果；任何退出未观察、服务停止、status 不可读、selfcheck 冒充、build/identity/source mismatch 或隐私越界都立即阻止 02-21 C，不能由 B1/B2/S/I/N/旧 feedback 补写。"
    - "macOS-first 只改变顺序；Windows 始终 `not_run/human_needed`，B3 不证明 C/D/reauth 或共享 Profile 最终成立，Phase 1/2 不整体完成，Phase 3 仍 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-19-SUMMARY.md"
      provides: "真实用户完成 B2 Worker restart 后的脱敏 hard-gate 记录；仅作为 prerequisite pointer，不能替代 ledger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "唯一 repo-side live gate；本计划增加 B3 strict feedback parser 与 exact-cell read-only verify，绝无 L writer/DB/browser 权限"
      contains: "verifyPhase2LiveCheckpoint"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger 与 source/scenario/platform/evidence 精确寻址；B3 source of truth"
      contains: "EvidenceLedger"
    - path: "apps/status/src/main.ts"
      provides: "paired protected B3 action、host lifecycle/dual-source 脱敏状态与 exact feedback；完整身份不离开 protected UI"
      contains: "Codex"
    - path: "release/phase2-install-prompt.md"
      provides: "签名 beta 绑定的实际 managed status/client reload 入口；用户不接受 agent 临时拼出的 path/命令"
      contains: "status"
    - path: ".planning/phases/02-poc-live/02-20-SUMMARY.md"
      provides: "仅在两个 B3 exact L cells 与真实 feedback strict pass 后创建的 02-21 prerequisite；无 parallel receipt JSON"
  key_links:
    - from: ".planning/phases/02-poc-live/02-19-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "B3 preflight 同时要求 completed SUMMARY 与 B2 exact current EvidenceLedger conjunction；SUMMARY 单独不能通过"
      pattern: "B2|B2_worker_restart|pass"
    - from: "release/phase2-install-prompt.md"
      to: "Codex-absent status read"
      via: "用户只运行已签名/已安装的只读 status 命令；禁止 selfcheck、start/restart、source probe 或调用者自选 path"
      pattern: "status|buildId|read-only"
    - from: "actual Codex Quit/re-entry"
      to: "paired server B3 workflow"
      via: "server-held durable correlation 绑定 pre-exit authenticated host instance、host-exit observation、independent status read 与 new actual-host handshake"
      pattern: "B3_codex_exit|host_exit|actual_client"
    - from: "paired server B3 workflow"
      to: "packages/persistence/src/auth.ts"
      via: "actual result + one-time source-specific human_action authority 在同一事务 append Moodle/EdStem B3 L events；public API/gate tool 无 append"
      pattern: "human_action|B3_codex_exit|actionReceiptId"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-21 eligibility"
      via: "B3 read-only verifier 只在 two-source exact L pass、lifecycle/client/build/source/privacy conjunction 完整时返回 pass；02-20 SUMMARY 仅记录该结果"
      pattern: "--checkpoint B3|B3_COMPLETE|C_os_restart"
---

<objective>
执行 macOS 检查点 B3 的不可替代人工生命周期验证：用户完全退出真实 Codex 应用，在宿主缺席期间证明 AutoED backend/Worker 仍独立运行并完成一次只读最小 status，随后正常重进真实 Codex，复核 actual client/beta identity 与双来源状态；任何不确定或失败都阻止 C。

Purpose: 落实 D-03、D-12、D-14 与 AUTH-02/AUTH-04/UAT-01，证明“Codex 退出不终止本地服务且同一受管安装可被真实客户端重新接续”，同时排除新 spawn selfcheck、普通 MCP reload 或旧状态快照冒充宿主完全退出/重进。
Output: repo-side B3 strict parser/read-only verifier、Moodle/EdStem 两个独立 `B3_codex_exit×macos×L` events、真实用户的独立脱敏 B3 feedback，以及 strict pass 后的 02-20 SUMMARY。本计划不创建 B3 planning receipt JSON、不执行 OS reboot/C/D/reauth、不访问课程内容或 Windows。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-17-SUMMARY.md
@.planning/phases/02-poc-live/02-18-SUMMARY.md
@.planning/phases/02-poc-live/02-19-PLAN.md
@.planning/phases/02-poc-live/02-19-SUMMARY.md
@release/phase2-install-prompt.md
@docs/PHASE1-UAT.md
@packages/domain/src/model.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/processes.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs
@tests/integration/phase2-live-gate.test.ts

<interfaces>
执行时以 02-19 SUMMARY 的真实导出做机械适配，但不得改变以下已由 02-17 锁定的 scenario 或 writer boundary：

```typescript
export type UatScenario =
  | 'A1_login' | 'A2_binding' | 'A2_course_visibility'
  | 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3'
  | 'B2_worker_restart' | 'B3_codex_exit'
  | 'C_os_restart' | 'D_24h_recheck' | 'reauth';

export interface EvidenceCellKey {
  platform: 'macos' | 'windows';
  source: 'moodle' | 'edstem';
  scenario: UatScenario;
  evidence: 'S' | 'I' | 'N' | 'L';
}

export type EvidenceWriterAuthority =
  | { kind:'automated'; evidence:'S'|'I'|'N'; platform:'macos'|'windows'; producerId:string }
  | { kind:'human_action'; evidence:'L'; platform:'macos'|'windows'; actionReceiptId:string };
```

B2 没有 planning receipt。B3 的 canonical prerequisite 只能是 completed `02-19-SUMMARY.md` 加下列 exact read-only command；后者从 current EvidenceLedger/status 验证两 source `B2_worker_restart×macos×L`，SUMMARY 不能代填：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B2 --runtime current --platform macos --read-only
```

02-20 扩展同一 repo-side gate 的 exact B3 interface；`record` 只 strict parse 0700 mktemp/0600 feedback 并通过 authenticated redacted API 核对 current status/exact cells，不 import persistence、不直接开 DB/WAL、不调用 `EvidenceLedger.append`、不 start/restart/spawn/selfcheck/browser/source request：

```typescript
export interface Phase2B3Feedback {
  schema: 1;
  checkpoint: 'B3';
  platform: 'macos';
  result: 'pass' | 'failed' | 'human_needed';
  resultCode: B3SafeCode;
  codexExit: 'observed' | 'not_observed';
  backend: 'survived' | 'stopped' | 'not_observed';
  worker: 'survived' | 'stopped' | 'not_observed';
  absentStatusRead: 'pass' | 'failed' | 'not_observed';
  reentry: 'actual_client' | 'selfcheck_or_spawn' | 'not_observed';
  clientBuild: 'matched' | 'mismatch' | 'not_observed';
  sourceState: { moodle:'consistent'|'mismatch'|'not_observed'; edstem:'consistent'|'mismatch'|'not_observed' };
  bindingConsistency: 'confirmed' | 'mismatch' | 'not_observed';
  contentBoundary: 'preserved' | 'violated' | 'not_observed';
  screenshot: 'not_used';
  windows: 'not_run/human_needed';
  phase3: 'blocked';
  observedAt: string;
}

export type B3SafeCode =
  | 'B3_COMPLETE'
  | 'B2_PRECONDITION_FAILED'
  | 'CODEX_EXIT_NOT_OBSERVED'
  | 'BACKEND_STOPPED'
  | 'WORKER_STOPPED'
  | 'STATUS_UNAVAILABLE'
  | 'HOST_REENTRY_NOT_OBSERVED'
  | 'SELF_CHECK_NOT_ACCEPTED'
  | 'BUILD_IDENTITY_MISMATCH'
  | 'SOURCE_STATE_MISMATCH'
  | 'IDENTITY_MISMATCH'
  | 'PROFILE_OWNERSHIP_UNCONFIRMED'
  | 'CONTENT_BOUNDARY_VIOLATION'
  | 'RESULT_NOT_OBSERVED';

export function parsePhase2B3Feedback(value: unknown): Phase2B3Feedback;
export function recordPhase2LiveCheckpoint(input: {
  checkpoint:'B3'; feedbackPath:string; runtime:'current'; platform:'macos'; readOnlyInputs:true;
}): Promise<RedactedB3GateResult>;
export function verifyPhase2LiveCheckpoint(input: {
  checkpoint:'B3'; runtime:'current'; platform:'macos'; readOnly:true;
}): Promise<RedactedB3GateResult>;
```

Canonical commands：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --record --checkpoint B3 --feedback <0700-mktemp>/B3-result.json --runtime current --platform macos --read-only-inputs

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform macos --read-only
```

`B3_COMPLETE` 的唯一合法 conjunction 是 exact current version/build/artifact、两 source `B3_codex_exit×macos×L` latest non-corrected events 都 pass、`codexExit=observed`、backend/worker survived、退出期间 authenticated status-only read pass、reentry=actual_client、client build matched、Moodle/EdStem source state consistent、binding confirmed、content boundary preserved、screenshot not_used、Windows/Phase3 固定。gate stdout 只允许 `{schema,status,checkpoint,platform,version,buildId,resultCode,codexExit,backend,worker,absentStatusRead,reentry,clientBuild,sourceState,bindingConsistency,contentBoundary,observedAt,windows,phase3,gaps}`；stderr 仅 stable safe code。

真正的 B3 L writer 只在已发布 beta 的 paired server workflow 内。用户在退出前从 protected UI 发起 B3，server 为 Moodle/EdStem 分别建立 durable correlation，精确绑定 `checkpoint=B3,round=0,platform=macos,source,scenario=B3_codex_exit,version,buildId,runtimeGeneration,installationIdDigest,preExitHostInstanceDigest,approvedConfigId,approvedScopeId`。correlation 必须存于 backend-owned durable state而非 Codex/MCP/browser memory，并要求按序观察：authenticated actual Codex host disconnect+OS exit proof → 同 backend/Worker continuity → host absent 时来自已发布 status-only client principal 的 read → 新 actual Codex host authenticated handshake（同 installation/build、不同 host-instance digest）→ user paired confirmation。actual result 与 one-time consume/append 同事务；actionReceiptId/PID/path/host digest 永不输出。selfcheck principal、installer operation、MCP reload、CLI process本身、测试 harness、任意 spawn 或仅新 PID 都不能满足 actual-host handshake。若当前 beta 缺少此链，立即 human_needed并回到修复→完整测试→新 immutable beta→实际更新→重做 A/B；不得由 gate script/public route/DB 补写。
</interfaces>

<hard_gates>
- 用户必须真实退出并稍后重新进入 Codex，当前 agent 在退出期间不能继续工作或声称看到结果；只有用户恢复本线程并给出 strict feedback 后才可 finalize。
- 退出期间只允许 `release/phase2-install-prompt.md` 中已签名 beta 的 exact managed `status` 只读入口；不得使用 `selfcheck`、source probe、start/restart、测试命令、源码 npm 命令、调用者自选 path 或新 spawn Codex。
- `docs/PHASE1-UAT.md` Case 2 只能提供“真实退出/重新进入”的既有人工语义，不能把其中 bounded synthetic self-test 当 B3 source-auth evidence；B3 必须使用 Phase 2 exact live cells。
- 任一 failed/human_needed 保留 immutable B3 event并停止；不执行 C。Windows not_run/human_needed、Phase3 blocked。
</hard_gates>

<tasks>

<task type="auto" tdd="true">
  <name>02-20-1：扩展只读 B3 gate，并核验 B2/current runtime 前置</name>
  <read_first>AGENTS.md 的 Product boundary、Data integrity and verification、Execution after plan approval；02-CONTEXT.md D-03/D-12/D-14/D-17–D-18；02-19-PLAN.md 与 02-19-SUMMARY.md 的 exact B2 contract；02-17/02-18/02-19 SUMMARY 的 final scenario/gate semantics；scripts/release/phase2-live-gate.mjs；tests/integration/phase2-live-gate.test.ts；packages/persistence/src/auth.ts；release/phase2-install-prompt.md；docs/PHASE1-UAT.md Case 2。</read_first>
  <files>scripts/release/phase2-live-gate.mjs, tests/integration/phase2-live-gate.test.ts</files>
  <behavior>
    - RED：B3 pass 只在 current B2 exact pass + two-source B3 exact L pass + actual exit/service continuity/absent status/actual reentry/build/source/binding/privacy conjunction 全部成立时通过。
    - RED：window close、MCP reload、selfcheck、CLI process、新 spawn Codex、same host instance、status 命令自动 start/restart、旧/cross-source/cross-scenario/S/I/N/Windows event 均不能满足 B3。
    - RED：failed/human_needed/conflicting/corrected event、missing user feedback、build/generation/scope drift、sensitive output 全部 fail closed；gate 的 append/DB/process/browser/source request counters 为 0。
  </behavior>
  <action>在既有 live-gate integration suite 写 B3 RED matrix，使用本地 synthetic redacted status/event harness；不得创建 human_action success authority、真实 L pass、Codex/学校/Profile fixture。实现 `parsePhase2B3Feedback` 与 B3 record/verify read-only branch，复用 02-16/17 gate 的 strict argv、local client credential/discovery、atomic/no-output-on-failure 与 recursive output allowlist。`--record` 只把真实用户 strict feedback 与 server 已存在的 current B3 exact event digests/status 对齐，不 append；`--verify` 不需要 feedback，重新核对 B2 prerequisite、B3 exact cells、current build/source state和 gaps。拒绝 caller URL/port/path/PID/action id/host digest、extra flag 和没有 `--read-only(-inputs)` 的命令。A1/A2/B1/B2 既有 branches 必须 regression pass，scenario 不改名。

运行 02-19 的 exact B2 verifier并要求 exit 0；同时要求 `02-19-SUMMARY.md` 明确 B2 strict pass/02-20 unblocked。直接从 current redacted status/EvidenceLedger 证明 Moodle/EdStem `B2_worker_restart×macos×L` 都是 current-build pass，old Worker generation request/commit=0，new Worker current，binding/scope一致；SUMMARY 不能替代任一 cell。再证明 B3 two source cells、pending B3 correlation 与 C_os_restart cells/correlation 都为 0/not_run，school request/content read 增量为 0。任一 missing/failed/human_needed/correction/build drift/stale Worker/预填 B3/C/隐私泄漏返回 `B2_PRECONDITION_FAILED|RESULT_NOT_OBSERVED` 并停止，不呈现人工 task。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts &amp;&amp; test -s .planning/phases/02-poc-live/02-19-SUMMARY.md &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B2 --runtime current --platform macos --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. B3 parser/record/verify positive与完整negative matrix通过；A1/A2/B1/B2 regression无变化，exact scenario为`B3_codex_exit`。
    2. gate script只读 current redacted status/exact events；persistence/DB/append/process/spawn/selfcheck/browser/source request调用均为0，输出无path/PID/Profile/action/private资料。
    3. 02-19 SUMMARY存在且B2 verifier exit0；Moodle/EdStem exact `B2_worker_restart×macos×L` current-build pass，旧Worker请求/提交为0。
    4. B3与C exact cells/pending correlations均为0/not_run；preflight未发学校请求、未打开Profile、未读取内容、未生成human authority。
    5. Windows保持not_run/human_needed，共享Profile仍待B3/C/D/reauth，Phase1/2未整体完成，Phase3 blocked。
    6. 任一非strict前置阻止Task2，不允许SUMMARY/旧event/S/I/N/selfcheck代填。
  </acceptance_criteria>
  <done>只读 B3 gate 安全矩阵通过且 B2/current runtime strict pass、B3/C尚未开始时，才可进入真实 Codex quit checkpoint；否则保持 human_needed。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-20-2：用户完全退出 Codex、验证独立服务并从真实客户端重进</name>
  <read_first>Task 1 strict pass；02-19-SUMMARY.md 的 current beta/build与paired UI打开方式；release/phase2-install-prompt.md 中签名绑定的 exact managed status/client reload入口；docs/PHASE1-UAT.md Case2 的真实Codex退出语义（不得运行其synthetic self-test）；02-UI-SPEC 的B3 row/PrivacyNotice；apps/status/src/main.ts 的B3 action/feedback；scripts/release/phase2-live-gate.mjs B3 parser/verifier。</read_first>
  <files>无产品/receipt文件；strict pass后仅创建02-20-SUMMARY.md</files>
  <what-built>current beta 已完成 A、B1三轮与B2实际Worker restart；B3/C/D/reauth和Windows仍未运行。B3 server workflow可在Codex退出前建立durable correlation，并只接受actual host exit、独立status read和真实client re-entry。</what-built>
  <action>必须硬暂停并原样呈现 `<how-to-verify>` 与 `<resume-signal>`。agent不得代替用户退出/重开Codex、运行status、观察退出期间状态、生成feedback或自动批准。用户发起B3时，server必须先重新核对current build/B2 exact pass/binding/scope，并建立两个source-specific durable correlations；correlation不依赖Codex/MCP/browser内存。若UI没有B3 action、status-only released command或durable lifecycle观察，返回human_needed并回到修复/测试/新beta/实际更新，不用临时shell/selfcheck旁路。

用户按步骤完全退出后，本执行线程必然中断；不得在Codex缺席期间发送“仍在运行”评论或假装看见status。退出期间用户只运行发布prompt中的exact managed status只读命令；该命令必须连接既有backend，不start/restart/spawn服务，不创建job/selfcheck，不probe学校。server以认证principal与时间顺序记录status read发生在actual host exit之后、actual re-entry之前，并观察同一backend/Worker continuity。用户正常重开真实Codex后，server要求新的authenticated actual-host handshake；同installation/version/build/artifact且host instance不同才能继续。selfcheck/CLI/MCP/test process均不能满足。

重进后用户只在paired protected UI核对actual client/beta identity、backend/Worker continuity、Moodle/EdStem redacted current state、confirmed binding/approved scope与content boundary；完整身份只在protected UI本人查看，不复制。点击一次B3结果确认后，server用本轮source-specific correlation+actual lifecycle result+user action形成payload外human_action authority，分别append Moodle/EdStem `B3_codex_exit×macos×L` event；失败/human_needed也append immutable result并消费本轮correlation。用户返回strict feedback后，executor把allowlisted block写入0700 mktemp/0600 JSON，运行canonical B3 `--record ... --read-only-inputs`核对feedback/status/event digests，finally删除临时文件，再运行B3 read-only verifier。repo gate不append。任何失败/缺项/重放/敏感输出阻止02-21；不覆盖失败或另启selfcheck补测。</action>
  <how-to-verify>
    1. 在paired protected UI确认页面显示current public beta version/build、checkpoint B3、B2 pass、C/D/reauth与Windows not_run/human_needed、Phase3 blocked。不要复制identity/course或截图。
    2. 点击一次`开始 B3：完全退出 Codex`。等待UI显示“durable checkpoint已准备；可以退出”，不要刷新、重复点击、运行source probe或关闭backend/Worker/Profile。
    3. 使用macOS Codex菜单的`Quit Codex`或`⌘Q`完全退出真实Codex应用；只关闭窗口、关闭当前thread、reload MCP不算。不要用kill/killall/Activity Monitor强退；若正常Quit失败，返回human_needed而不是强制制造pass。
    4. Codex退出后，在Terminal只运行`release/phase2-install-prompt.md`为本beta给出的exact managed **status** 命令。不要运行selfcheck、start、restart、npm、测试、`codex exec`、新agent/MCP probe或任意自写命令。确认输出只显示backend=running、worker=running、current build和最小脱敏status；若命令启动了服务、不可达或输出私人/path/PID资料，记failed并停止。
    5. 通过正常macOS应用入口重新打开已安装的Codex；不要从Terminal spawn另一个Codex/selfcheck。回到本任务并打开paired UI，等待其显示“actual client re-entry observed”，且version/build/artifact与退出前一致、host instance为新实例。
    6. 在paired protected UI核对backend/Worker均survived，Moodle/EdStem current state consistent、binding confirmed、approved scope matched；不得发学校probe或打开课程。完整身份如显示仅本人核对，不发送给agent。
    7. 确认B3行显示content body/posts/files/grades=`not_read`、screenshot=`not_used`，再点击一次`确认 B3 结果`并复制严格脱敏block。任何not_observed/mismatch/stopped/selfcheck_or_spawn/content violation都必须failed/human_needed并停止C。
    8. 只返回下列exact block，不添加解释、路径、命令输出、身份、课程、origin、Profile/Cookie、PID、截图或日志：

```text
02-20_B3_RESULT
schema=1
checkpoint=B3
platform=macos
result=pass|failed|human_needed
result_code=B3_COMPLETE|CODEX_EXIT_NOT_OBSERVED|BACKEND_STOPPED|WORKER_STOPPED|STATUS_UNAVAILABLE|HOST_REENTRY_NOT_OBSERVED|SELF_CHECK_NOT_ACCEPTED|BUILD_IDENTITY_MISMATCH|SOURCE_STATE_MISMATCH|IDENTITY_MISMATCH|PROFILE_OWNERSHIP_UNCONFIRMED|CONTENT_BOUNDARY_VIOLATION|RESULT_NOT_OBSERVED
codex_exit=observed|not_observed
backend=survived|stopped|not_observed
worker=survived|stopped|not_observed
absent_status_read=pass|failed|not_observed
reentry=actual_client|selfcheck_or_spawn|not_observed
client_build=matched|mismatch|not_observed
moodle_state=consistent|mismatch|not_observed
edstem_state=consistent|mismatch|not_observed
binding=confirmed|mismatch|not_observed
content_boundary=preserved|violated|not_observed
screenshot=not_used
windows=not_run/human_needed
phase3=blocked
observed_at=<ISO-8601 with timezone>
```
  </how-to-verify>
  <verify>
    <automated>checkpoint前重复`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B2 --runtime current --platform macos --read-only`；收到真实strict feedback且server已写exact B3 events后，运行canonical B3 `--record`，随后运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform macos --read-only`。任何non-pass只能核对immutable failure并保持02-21 blocked。</automated>
  </verify>
  <acceptance_criteria>
    1. 用户从真实Codex应用正常Quit；server以authenticated host/OS identity观察actual exit。window close、thread结束、MCP reload、kill或same host instance不能满足。
    2. actual exit后、re-entry前，发布的status-only client成功读取既有backend/Worker；服务均survived，start/restart/spawn/job/selfcheck/source request增量全为0。
    3. re-entry来自正常启动的真实installed Codex，authenticated client证明同installation/version/build/artifact且new host instance；CLI/MCP/selfcheck/test/new spawn均被拒。
    4. Moodle/EdStem current state consistent、binding confirmed、approved scope matched；本轮school/course/content request为0，last-success不被改写成新probe。
    5. paired server内部向Moodle/EdStem各append一个`B3_codex_exit×macos×L` result；authority绑定本轮durable correlation/actual lifecycle/source/build/generation，action ID不外泄，重复/跨scenario/platform/build失败。
    6. feedback strict pass唯一conjunction为B3_COMPLETE+actual exit+services survived+absent status pass+actual client+build matched+two states consistent+binding confirmed+content preserved+screenshot not_used+fixed gaps。
    7. failed/human_needed event保留且立即阻断C；repo recorder/verifier append/DB/process/browser/source权限为0，不创建B3 planning receipt JSON。
    8. chat/SUMMARY/stdout/stderr无identity/course/origin/path/Profile/PID/Cookie/action id/host digest/raw status/DOM/network/screenshot；Windows与Phase3不变。
  </acceptance_criteria>
  <done>只有真实用户完成完整Codex退出→宿主缺席status读取→真实client重进，two-source B3 L cells与strict feedback全部pass且read-only verifier exit0时才完成；任一其他结果阻止02-21 C。</done>
  <resume-signal>重新进入真实Codex后，只粘贴paired UI生成的完整`02-20_B3_RESULT`脱敏block。不要只回复approved/通过，不要附Terminal命令/输出、姓名、邮箱、组织、课程、origin/path、Profile/PID/Cookie、action ID、截图或日志。failed/human_needed会保留并停止C，不会自动补测。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| B2 SUMMARY/ledger → B3 preflight | SUMMARY、旧build或单source pass可能冒充current B2 hard gate |
| actual Codex host → backend lifecycle observer | window close、MCP disconnect、PID变化或spawn process可能冒充完整宿主退出 |
| Codex-absent Terminal → backend status | 用户可能运行selfcheck/start/restart/自选path，间接启动服务或制造新进程证据 |
| actual client re-entry → B3 workflow | selfcheck/CLI/MCP/test process或旧host instance可能冒充正常重进的Codex client |
| paired server workflow → EvidenceLedger | correlation可能重放、跨source/scenario/platform/build使用或由feedback/gate伪造L authority |
| protected UI/feedback → chat/SUMMARY | full identity/course、path/PID/Profile/action ID或raw status可能泄漏 |
| macOS B3 → C/Windows/Phase3 | 单平台B3 pass可能被夸大为C/D/reauth/Windows或最终共享Profile通过 |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Category | Severity | Component | Disposition | ASVS L1 intent | Mitigation plan / blocker |
|---|---|---:|---|---|---|---|
| T2-20-01 | Spoofing / Tampering | Critical | B2→B3 prerequisite | mitigate | V2 Authentication、V4 Access Control、V13 Business Logic | completed 02-19 SUMMARY + current two-source exact B2 macOS/L conjunction + build/generation/fence；任一缺项阻断。 |
| T2-20-02 | Spoofing / Repudiation | Critical | Codex full-exit observation | mitigate | V2、V4、V16 Audit | pre-exit authenticated host instance + OS exit proof；window/MCP/thread/PID-only/kill不算；server durable correlation按序记录。无法证明即`CODEX_EXIT_NOT_OBSERVED`。 |
| T2-20-03 | Spoofing / Elevation | Critical | absent status / actual re-entry | mitigate | V2、V4、V13 | signed released status-only principal且no start/spawn；re-entry必须actual Codex host handshake、same install/build/new instance。selfcheck/CLI/MCP/test拒绝。 |
| T2-20-04 | Tampering / Denial of Service | High | backend/Worker continuity | mitigate | V4持续授权、V13状态机、V16 provenance | same owned services/heartbeats在host absent窗口持续；status read不能启动/修复；stopped/unobserved直接失败，last-success保留。 |
| T2-20-05 | Tampering / Repudiation / Elevation | Critical | B3 L append | mitigate | V4 writer authority、V13 transaction、V16 audit | server-only durable source correlations + actual lifecycle/result + one-time consume/append；exact source/scenario/platform/build；repo gate/public API无write。 |
| T2-20-06 | Information Disclosure | High | status/UI/feedback/output | mitigate | V7 Error/Logging、V8 Data Protection | protected UI only for full identity；allowlisted feedback/stdout；无path/PID/Profile/Cookie/action/host digest/raw status/screenshot。泄漏即blocker。 |
| T2-20-07 | Tampering / Repudiation | Critical | B3→C/Windows/Phase3 projection | mitigate | V4、V13、V16 | B3 only exact cells；C/D/reauth与Windows保持not_run，Phase3 blocked，共享Profile仍hypothesis。跨格传播即blocker。 |

ASVS 5.0 L1 仅表示适用控制意图，不声称完整合规。所有 High/Critical 项必须 `mitigate`；任何事实不可观察、writer provenance不成立、selfcheck/spawn冒充、服务不连续、build/source mismatch或输出泄漏都保持failed/human_needed，不得accept/transfer、自动批准或推进C。
</threat_model>

<verification>
1. integration suite证明B3 strict parser/read-only gate及selfcheck/spawn/window-close/cross-cell/privacy负例；gate无append/DB/process/browser/source权限。
2. B2 canonical verifier exit0且02-19 SUMMARY completed；B3/C cells和pending correlations起始为空，school/content request增量0。
3. 用户发起durable B3后真实Codex完全退出；退出窗口内released status-only read成功，backend/Worker未被start/restart且持续running。
4. 用户正常重进真实Codex；authenticated same install/build/new host instance成立，selfcheck/CLI/MCP/test process不能代替。
5. server append two-source exact B3 macOS/L events，严格feedback与events/status一致；failed/human_needed不删除，pass需完整conjunction。
6. B3 canonical read-only verifier exit0才创建成功SUMMARY并允许02-21；否则C blocked。
7. privacy/platform scan确认无private/raw资料、无截图，Windows not_run/human_needed、Phase1/2 incomplete、Phase3 blocked。
</verification>

<success_criteria>
- AUTH-02/D-03/D-12：真实Codex退出、backend/Worker独立存活、真实client重进均有时间有序证据；不是selfcheck/spawn/旧状态推断。
- AUTH-04：服务/Worker/Profile ownership与generation/fence可证明；status read不自动启动或篡改，未知/停止立即失败。
- UAT-01/D-14：Moodle/EdStem分别拥有`B3_codex_exit×macos×L`真实event与独立脱敏feedback；B3不能由B1/B2/S/I/N或另一平台代填。
- 隐私：完整身份只在protected UI；chat/log/gate/SUMMARY无identity/course/path/PID/Profile/Cookie/action/raw资料/screenshot。
- 失败阻止C；Windows/C/D/reauth与Phase3缺口保持，macOS-first不扩大验证结论。
</success_criteria>

<output>
只有用户真实strict pass、Moodle/EdStem两个B3 exact L events均pass且canonical B3 verifier exit0后，才创建`.planning/phases/02-poc-live/02-20-SUMMARY.md`。SUMMARY只记录checkpoint=B3、public version/build、platform=macos、B2 prerequisite verified、codex_exit/backend/worker/absent_status/reentry/client_build/two-source-state/binding/content-boundary脱敏enum、resultCode=B3_COMPLETE、observedAt、`C_os_restart` two-source cells=`not_run`、pending C correlation=`none`、C eligibility与Windows/Phase3 gaps；不得记录identity/course/origin/path/Profile/PID/Cookie/action/host digest/Terminal命令或输出/raw status/DOM/network/screenshot。failed/human_needed时不创建成功SUMMARY、不运行C，保留append-only B3 event。

02-21 不得从本 SUMMARY、聊天或 `docs/PHASE1-UAT.md` 发明完整 macOS reboot 指令；唯一可用的人类步骤来源是 immutable beta 签名 manifest 内、与 `release/phase2-install-prompt.md` 同 version/build/hash 绑定的已发布 C/reboot instructions 及其 paired protected UI 呈现。若该 signed release instruction 缺少“正常完整 OS restart、不得 selfcheck/soft restart/agent代做、重启后 actual boot identity/managed entrypoint/status 恢复”的 exact steps，02-21 必须 human_needed 并走修复→完整测试→新 beta→更新→重做依赖 gate，不能现场补文案。C 开始前必须重验 B3 verifier、上述 C cells/correlation 仍空；C 只能 mint 新 durable `checkpoint=C,scenario=C_os_restart` correlations，绑定 pre-reboot boot identity并要求 post-reboot boot identity不同，绝不复用/升级已消费 B3 correlation。
</output>
