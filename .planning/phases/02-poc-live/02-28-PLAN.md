---
phase: 02-poc-live
plan: 28
type: execute
wave: 29
depends_on:
  - "02-27"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-04
  - UAT-01

must_haves:
  truths:
    - "02-28 只在 02-24 completed SUMMARY 证明用户明确恢复原生 Windows、02-25 actual Windows update/readiness strict pass、02-27 Windows A2 strict pass 且 current build/session/config/scope/Profile 未漂移后才可执行；计划存在、macOS通过或自动设备检测都不是授权。"
    - "用户在真实原生 Windows 11 上，通过 current paired protected UI 将同一个 AutoED 专属 Profile 正常关闭并重开三次；执行器不得代替点击、kill进程、删锁、重启Worker/Codex/OS或自动批准。"
    - "三轮严格使用 `B1_reopen_1`、`B1_reopen_2`、`B1_reopen_3`，并各自为 Moodle 与 EdStem 形成独立 current Windows/L append-only result；一个overall pass、另一轮/source/platform/evidence或S/I/N不能代填。"
    - "每轮只有在本安装拥有的browser由OS确认actual exit后才允许重开；running/unknown/PID reuse/owner mismatch/lease expiry均不能授权接管、kill、删锁或继续。"
    - "每轮重开都从同一专属 Profile 按 Moodle→EdStem 顺序复核认证、Windows A2 confirmed identity/binding/approved scope、共享Profile观察和同一个批准课程的最小可见性；不读课程正文、帖子、文件、成绩或反馈。"
    - "任一轮任一来源 failed/human_needed、identity/scope/Profile mismatch、actual exit未确认、敏感输出、缺exact cell或用户未返回本轮脱敏反馈，立即停止且不呈现下一轮；失败不能被后续自动重试或latest pass覆盖。"
    - "三个round-specific canonical read-only verifiers与completed 02-28 SUMMARY共同构成02-29 B2前置；本计划不创建planning receipt，不写macOS cells，不完成Windows全链/Phase1/2，不最终采用shared Profile且Phase3保持blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      provides: "用户未来明确恢复native Windows UAT且只释放Windows链逐计划执行的上游授权"
    - path: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      provides: "当前原生Windows11/non-WSL beta update/build/services/paired UI strict machine gate"
      contains: "WINDOWS_UPDATE_COMPLETE"
    - path: ".planning/phases/02-poc-live/02-27-SUMMARY.md"
      provides: "Windows A2真实用户确认与四个current A2 Windows/L cells的脱敏人工前置"
    - path: "packages/persistence/src/auth.ts"
      provides: "runtime append-only EvidenceLedger，保存Windows×两来源×三轮×L六个不可互相替代的结果"
      contains: "EvidenceLedger"
    - path: "packages/platform/src/profile-coordinator.ts"
      provides: "Windows专属Profile single-owner、OS actual-exit confirmation、owner identity/generation/fence与only-owned recovery"
      contains: "ProfileOwnershipCoordinator"
    - path: "packages/platform/src/browser-provider.ts"
      provides: "受管persistent Profile的正常close/reopen及request/commit前current ownership guard"
      contains: "LocalPlaywrightBrowserProvider"
    - path: "apps/status/src/main.ts"
      provides: "paired protected UI中的B1-1/B1-2/B1-3独立行、唯一当前动作、protected核对与脱敏结果复制"
      contains: "CheckpointLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "三个Windows B1 round的canonical read-only verifier；无pending-action/L writer/DB/Profile/browser/source权限"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-28-SUMMARY.md"
      provides: "仅三轮六个runtime exact cells和三次真实用户反馈全pass后生成的B2 eligibility交接"
  key_links:
    - from: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      to: ".planning/phases/02-poc-live/02-27-SUMMARY.md"
      via: "explicit native Windows resume→actual update→A1/A2 strict pass连续前置；缺任一即B1 activity=0"
      pattern: "windows|resume|A2|blocked"
    - from: "apps/status/src/main.ts"
      to: "packages/platform/src/profile-coordinator.ts"
      via: "每轮仅由用户在paired UI触发normal close；只有OS actual-exit+exact owner proof后才release/reopen"
      pattern: "PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|PROFILE_EXITED"
    - from: "packages/platform/src/browser-provider.ts"
      to: "packages/application/src/auth-jobs.ts"
      via: "新current owner/generation/fence下按Moodle→EdStem执行fixed auth/course-visibility minimum probes；旧owner请求/提交为0"
      pattern: "assertCurrent|moodle.auth_probe|edstem.auth_probe"
    - from: "paired Windows B1 workflow"
      to: "packages/persistence/src/auth.ts"
      via: "每轮/来源新的server-only one-time human correlation与actual close/reopen/result同事务append exact Windows/L cell"
      pattern: "B1_reopen_1|B1_reopen_2|B1_reopen_3"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-29 eligibility"
      via: "round 1/2/3三个只读verifier分别证明六个current non-corrected Windows/L pass；completed SUMMARY只证明三次真实feedback存在"
      pattern: "WINDOWS_B1_REOPEN_1_COMPLETE|WINDOWS_B1_REOPEN_2_COMPLETE|WINDOWS_B1_REOPEN_3_COMPLETE"
---

<objective>
执行未来原生 Windows 11 live 检查点 B1：用户将同一个 AutoED 专属 Profile 正常关闭并重开三次，每轮分别复核 Moodle、EdStem、共享 Profile观察、Windows A2 identity/binding/approved scope及同一个批准课程的最小可见性，并生成六个不可互相替代的Windows/L runtime结果。

Purpose: 落实 D-03、D-09、D-12、D-14、D-17–D-18 与 AUTH-02/AUTH-04/UAT-01，用三次真实、独立、严格顺序的Windows Profile生命周期证据验证共享Profile恢复；防止一次成功、合并结果、macOS、WSL、fixture或S/I/N被冒充为B1三轮pass。

Output: 三个blocking human-action checkpoints、Moodle/EdStem×`B1_reopen_1|B1_reopen_2|B1_reopen_3`六个runtime Windows/L events、三个canonical round verifier与strict pass后的02-28 SUMMARY。本计划不创建planning receipt、不执行B2/Worker restart、B3/C/D/reauth，也不改变macOS evidence。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@.planning/phases/02-poc-live/02-27-SUMMARY.md
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/browser-provider.ts
@packages/platform/src/source-adapters.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/verify-phase2-update-gate.mjs
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时以02-24/02-25/02-27 completed SUMMARY和published beta最终导出为准；不得建立第二套Profile、ledger、receipt writer或browser接口。稳定domain contract为：

```typescript
type SourceId = 'moodle' | 'edstem';
type NativePlatform = 'macos' | 'windows';
type EvidenceClass = 'S' | 'I' | 'N' | 'L';
type UatScenario =
  | 'A1_login'
  | 'A2_binding'
  | 'A2_course_visibility'
  | 'B1_reopen_1'
  | 'B1_reopen_2'
  | 'B1_reopen_3'
  | 'B2_worker_restart'
  | 'B3_codex_exit'
  | 'C_os_restart'
  | 'D_24h_recheck'
  | 'reauth';

interface EvidenceCellKey {
  platform: NativePlatform;
  source: SourceId;
  scenario: UatScenario;
  evidence: EvidenceClass;
}
type EvidenceWriterAuthority =
  | { kind:'automated'; evidence:'S'|'I'|'N'; platform:NativePlatform; producerId:string }
  | { kind:'human_action'; evidence:'L'; platform:NativePlatform; actionReceiptId:string };
interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: EvidenceCellKey): Promise<EvidenceReceipt[]>;
}
```

02-28 required cells精确为：

```text
windows × moodle × B1_reopen_1 × L
windows × edstem × B1_reopen_1 × L
windows × moodle × B1_reopen_2 × L
windows × edstem × B1_reopen_2 × L
windows × moodle × B1_reopen_3 × L
windows × edstem × B1_reopen_3 × L
```

每一格只能由published paired server内部生成的本轮one-time bundle写入。bundle绑定platform=windows、checkpoint=B1、round、exact scenario、source、current installation/version/build、runtime generation/fence、paired session、approvedConfigId/approvedScopeId、Windows A2 binding digest、Profile owner/reservation digest和expiry；action ID不离开server。actual user action、owned browser close、OS actual-exit、same Profile reopen、actual source result与payload外`human_action/L/windows` authority必须同事务consume/append。failed/human_needed同样是immutable L result并立即阻断；修复后的新尝试必须由新用户动作产生linked correction，不能delete/overwrite/以latest-pass隐藏旧失败，也不能在本计划中跳过失败继续下一轮。

每轮只允许四个sealed read-only actions：`moodle.auth_probe`、`edstem.auth_probe`、`moodle.course_visibility_probe`、`edstem.course_visibility_probe`；operation只能引用persisted approved config/scope IDs。最小读取只包含positive auth marker、稳定identity/org evidence digest及同一个approved course的visible marker；不得输出/读取course body、posts、files、grades、feedback、DOM、URL path/query、request/response或下载。

future preflight必须依次运行以下现有canonical read-only gates：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json \
  --artifacts release/phase2-beta-artifacts.json \
  --availability release/phase2-availability.json \
  --runtime current --platform windows --read-only

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint A2 --runtime current --platform windows --read-only
```

第一条必须证明02-24 explicit resume、native Windows11/non-WSL、actual current beta/build/cleanup/API/Worker/paired UI；第二条必须返回`WINDOWS_A2_COMPLETE`并证明Moodle/EdStem的`A2_binding|A2_course_visibility`四个current non-corrected Windows/L cells、confirmed identity/scope/course visibility与真实02-27 feedback。completed 02-25和02-27 SUMMARY缺一不可；SUMMARY不替代runtime authority。

三轮canonical read-only verifier分别固定为：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B1 --round 1 --runtime current --platform windows --read-only

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B1 --round 2 --runtime current --platform windows --read-only

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B1 --round 3 --runtime current --platform windows --read-only
```

round-specific strict pass codes依次为`WINDOWS_B1_REOPEN_1_COMPLETE`、`WINDOWS_B1_REOPEN_2_COMPLETE`、`WINDOWS_B1_REOPEN_3_COMPLETE`。每条stdout只允许：

```typescript
interface Phase2WindowsB1RoundPassProjection {
  schema: 1;
  status: 'pass';
  checkpoint: 'B1';
  platform: 'windows';
  round: 1 | 2 | 3;
  scenario: 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3';
  code: 'WINDOWS_B1_REOPEN_1_COMPLETE' | 'WINDOWS_B1_REOPEN_2_COMPLETE' | 'WINDOWS_B1_REOPEN_3_COMPLETE';
  version: string;
  buildId: string;
  sources: ['moodle','edstem'];
  profile: 'same_dedicated';
  actualExit: 'confirmed';
  ownership: 'current';
  identityScope: 'consistent';
  minimalRead: 'pass';
  contentBoundary: 'preserved';
  macos: 'unchanged';
  phase3: 'blocked';
}
```

gate只能通过既有signed local-client discovery读取redacted current status与两个exact round cells；不得import persistence、直读DB/WAL/Profile、取得pending action、mint/consume authority、append/correct ledger、打开/关闭browser、访问学校、接受URL/port/path/PID/Profile/selector/browser handle/action ID或写模式。每轮只接受下列strict feedback，round/scenario/pass code按1/2/3固定替换：

```text
02-28_WINDOWS_B1_<round>_RESULT
checkpoint=B1
round=<1|2|3>
scenario=B1_reopen_<round>
platform=windows
version=<current public version>
build_id=<current public build id>
result=pass|failed|human_needed
result_code=WINDOWS_B1_REOPEN_<round>_COMPLETE|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|PROFILE_EXIT_NOT_CONFIRMED|PROFILE_REOPEN_FAILED|MOODLE_RECOVERY_FAILED|EDSTEM_RECOVERY_FAILED|IDENTITY_MISMATCH|APPROVED_SCOPE_MISMATCH|INTERACTION_REQUIRED|MINIMAL_READ_FAILED|CONTENT_BOUNDARY_VIOLATED|RESULT_NOT_OBSERVED
actual_exit=confirmed|failed|not_observed
profile=same_dedicated|mismatch|not_observed
moodle=pass|failed|human_needed
moodle_code=<published safe code>
edstem=pass|failed|human_needed
edstem_code=<published safe code>
identity_scope=consistent|mismatch|not_observed
minimal_read=pass|failed|human_needed
content_boundary=preserved|violated|not_observed
screenshot=not_used
observed_at=<ISO-8601 with timezone>
macos=unchanged
phase3=blocked
```

feedback只是checkpoint编排的必要人工事实，不是L authority。若published beta不支持exact Windows B1 scenarios、round discriminator、paired-server-only L bundles、actual-exit/ownership guards或上述read-only verifiers，必须human_needed并回到修复→完整测试/scan→更高immutable beta→actual Windows update→重新完成依赖live gates；不得live patch、另造public receipt/write route或使用SUMMARY/chat代写。
</interfaces>

<hard_gates>
- 当前仍是future-only：02-24 explicit resume、02-25 update receipt+SUMMARY+current gate、02-27 SUMMARY+A2 gate任一缺失/non-pass时，不呈现B1动作、不关闭/重开browser、不访问学校、不mint action、不写L。
- 三轮严格串行：round1两来源+用户feedback+verifier pass才呈现round2；round1+round2 pass才呈现round3。任一failed/human_needed/not_run/缺反馈立即停止，不能继续“收集其余轮次”。
- 每轮只操作本安装创建并拥有的Windows dedicated Profile/browser。不得关闭日常Chrome、kill PID/process name、删锁、改lease/Cookie/storage、复制Profile或启动fresh/split Profile；unknown/running/PID reuse/control mismatch时零stop/kill/release/reopen。
- lease expiry、按钮点击、process disappearance或browser platform hint都不证明exit；必须同一owner identity的OS actual-exit confirmed。只有release完成后才能以新generation/fence重开。
- 每轮Moodle与EdStem必须独立验证same Profile/current Windows A2 identity/binding/scope和one-course minimal visibility。interaction/MFA、identity/scope/Profile mismatch或任一source non-pass立即停止；不自动登录或修复。
- 六格必须是Windows/L/current build/exact source/exact scenario的真实human_action results；macOS、WSL/Linux、S/I/N、A2/B2、另一round/source、overall/latest row均不可代填。macOS evidence/event digests与writer activity前后不变。
- 不创建B1 planning receipt JSON，不新增第二ledger/public writer/generic browser operation。runtime EvidenceLedger是source of truth，SUMMARY只证明三次真实反馈存在。
- B1不重启Worker/Codex/OS，不执行24h/reauth，不读取course content、不写/download source，不请求截图，不输出credential/Profile/process/full identity/course/origin/raw page/network/action ID。
- 三轮pass只释放02-29 B2 eligibility；Windows B2/B3/C/D/reauth仍not_run/human_needed，Phase1 partial、Phase2 incomplete、shared Profile未最终adopted、Phase3 blocked，且不自动执行02-29。
</hard_gates>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-28-1：用户完成Windows专属Profile第一次正常关闭重开</name>
  <files>none（用户动作只影响仓库外current Windows managed browser/Profile runtime、paired server pending action与既有append-only EvidenceLedger；不修改源码/学校资料/规划receipt）</files>
  <read_first>AGENTS.md的Credentials and login、Data integrity and verification、Windows native evidence、source read-only与human gate规则；02-CONTEXT D-03/D-09/D-12–D-15/D-17–D-18；02-24 explicit resume SUMMARY；02-25 update receipt/SUMMARY；02-27 A2 SUMMARY与canonical gate；最终Profile ownership/browser guard/EvidenceLedger/paired protected UI contracts。禁止读取legacy、Profile/Cookie、raw DB/WAL、设备/进程私人字段、完整身份/course/origin或页面网络。</read_first>
  <what-built>用户未来已明确恢复native Windows UAT、更新current signed beta并完成Windows A1/A2；B1三轮六格仍not_run/human_needed，当前只允许开始`B1_reopen_1`。</what-built>
  <action>执行器先验证02-24 completed SUMMARY的exact resume；运行`<interfaces>`的02-25 update gate与Windows A2 gate并要求completed 02-25/02-27 SUMMARY、current version/build/generation/config/scope/paired session/Profile ownership一致。读取B1 round1/2/3 redacted exact-cell projection，六格都必须为空/not_run且macOS baseline digests/writer count记录在内存。发现预填/旧build/correction、非原生Windows、A2 drift、Profile in-use/unknown、in-flight request、maintenance/update或后续scenario event立即human_needed；不得清理或修正。

preflight pass后硬暂停并原样显示`<how_to_verify>`与`<resume_signal>`。用户只能从current paired protected UI启动B1-1。paired server为本轮两个source mint新的server-only bundle；产品只对owned dedicated browser执行normal close，等待OS actual-exit与ownership release后才以same Profile/new current owner generation/fence重开，再按Moodle→EdStem执行fixed auth与one-course visibility minimum probes。旧owner在release后request/commit=0；不restart Worker/Codex/Windows、不触发登录/MFA。

paired server用actual user action+close/exit/reopen/source result同事务appendMoodle/EdStem两个`windows/L/B1_reopen_1` events；failed/human_needed也immutable保留并立即停止。用户在protected UI核对same Profile、A2 identity/binding/scope与minimal read并返回本轮strict block；未收到真实feedback即使runtime pass也不完成。恢复后strict parse exact keys/enums/current version/build/time，运行round1 canonical verifier。只有两个source current non-corrected pass、feedback pass、code=`WINDOWS_B1_REOPEN_1_COMPLETE`、round2/3仍空且macOS unchanged才进入Task2；任何non-pass停止，不呈现第二轮或自动重开。</action>
  <how_to_verify>
    1. 在原生Windows 11 paired local UI确认current version/build与A2相同，A2 pass，当前唯一动作是`B1-1 第一次关闭重开`；B1-2/B1-3与B2仍未运行。
    2. 点击产品的正常关闭动作一次；不要force quit、kill PID、删除lock、修改lease/Profile/Cookie、退出日常Chrome或restart Worker/Codex/Windows。
    3. 等待UI明确显示OS actual exit与ownership released。若PROFILE_IN_USE、PROFILE_OWNERSHIP_UNCONFIRMED、unknown或仍关闭中，停止并返回human_needed，不重复启动。
    4. 在paired UI触发B1-1重开/检查一次；产品应复用同一dedicated Profile并依次检查Moodle、EdStem。出现登录/MFA、第二Profile/browser、下载/写入或越界页面立即停止。
    5. 仅在protected UI本地核对两来源authenticated、same Profile、完整identity/binding/approved scope与Windows A2一致、同一个approved course minimal visibility=pass；不要打开或发送正文/帖子/文件/grades/identity/course/origin。
    6. 使用`复制脱敏结果单`并只返回`02-28_WINDOWS_B1_1_RESULT`。任一字段非pass/confirmed/same/consistent/preserved/not_used时如实failed/human_needed并停止。
  </how_to_verify>
  <verify>
    <automated>checkpoint前运行02-25 update gate、`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A2 --runtime current --platform windows --read-only`并确认六个B1 cells均not_run；用户反馈且paired server形成actual results后，只运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 1 --runtime current --platform windows --read-only`并要求safe code=`WINDOWS_B1_REOPEN_1_COMPLETE`。</automated>
  </verify>
  <acceptance_criteria>
    1. explicit Windows resume、actual current update与Windows A2四格+真实feedback均strict pass；B1动作前六格not_run且macOS baseline/current build无漂移。
    2. 用户本人只用paired fixed B1-1动作normal close/reopen；OS actual-exit+exact owner proof先于release/reopen，无kill/delete-lock/lease-as-exit/daily-browser/Worker/Codex/OS影响。
    3. 新owner/current generation复用same dedicated Profile；旧owner在release后source request/commit=0，无fresh/split Profile、Cookie读取/复制或自动login/MFA。
    4. Moodle/EdStem分别复核auth、A2 identity/binding/approved scope和same-course minimal visibility；course content/write/download/另一个scope操作为0。
    5. runtime ledger恰有两个current Windows/L/`B1_reopen_1` events；round2/3仍not_run，macOS/S/I/N/另一source/scenario不可代填且macOS hashes/writer count不变。
    6. strict user block与round1 verifier都pass，stdout/feedback无Profile/PID/owner/full identity/course/origin/action ID/raw page/network/screenshot；任一non-pass立即阻断Task2/02-29。
  </acceptance_criteria>
  <resume_signal>只回复完整`02-28_WINDOWS_B1_1_RESULT`块，精确使用`<interfaces>`字段/枚举；不要发送姓名、邮箱、组织、课程、origin、Profile/PID/owner/Cookie、action ID、路径、截图、日志或raw错误。自由文本`approved`不会解除门禁。</resume_signal>
  <done>用户完成第一次真实Windows Profile close/reopen；两个`B1_reopen_1` Windows/L exact cells、strict feedback与round1 verifier全pass，round2/3和macOS均未改变，才允许第二轮。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-28-2：用户完成Windows专属Profile第二次独立关闭重开</name>
  <files>none（只影响仓库外current Windows runtime与既有EvidenceLedger；不创建planning receipt或修改第一轮结果）</files>
  <read_first>Task1真实feedback与`WINDOWS_B1_REOPEN_1_COMPLETE` verifier输出；02-27 A2 current gate；最终Profile actual-exit/owner/generation/fence与paired server-only bundle contract；本plan的round2 exact scenario/result schema。不得读取或复制第一轮action authority、Profile/process/private source数据。</read_first>
  <what-built>B1-1的Moodle/EdStem两个current Windows/L cells与真实feedback已独立pass；B1-2/B1-3仍not_run，第二轮必须使用新user action、new bundle与new close/actual-exit/reopen lifecycle。</what-built>
  <action>重新运行round1 verifier、A2 gate与current update/build/readiness checks，要求第一轮仍是current non-corrected pass、无failed correction、macOS unchanged；确认round2/3 cells为空、无in-flight request、Profile current owned且UI唯一动作是B1-2。任何drift/non-pass/预填立即停止。

硬暂停等待用户按`<how_to_verify>`执行第二次完整normal close→OS actual-exit→ownership release→same Profile reopen；不能复用第一轮窗口、browser process、bundle、feedback或result。paired server为Moodle/EdStem分别mint绑定scenario=`B1_reopen_2`的新one-time child，用第二轮actual results append两个独立Windows/L events。任一source/ownership/Profile/identity/scope/minimal read non-pass立即保存真实结果并停止，不显示B1-3。

用户返回`02-28_WINDOWS_B1_2_RESULT`后strict parse并运行round2 verifier；只有code=`WINDOWS_B1_REOPEN_2_COMPLETE`、round1仍pass、round2两个source pass、round3仍空、macOS unchanged且本轮真实feedback存在，才进入Task3。不得把round1 pass复制到round2、把两轮合并为count=2或自动循环第三次。</action>
  <how_to_verify>
    1. 确认UI显示B1-1 pass且锁定不可覆盖，当前唯一动作是`B1-2 第二次关闭重开`；若直接显示B1-3/B2或缺第一轮，停止。
    2. 再次点击产品normal close一次，等待新的OS actual-exit/ownership released；不要以第一轮exit、lease expiry、按钮点击或process disappearance代替。
    3. 触发B1-2重开/检查一次；必须是same dedicated Profile、新current owner lifecycle，Moodle→EdStem独立检查。不要restart Worker/Codex/OS或登录/MFA。
    4. 在protected UI再次独立核对两来源auth、same Profile、A2 identity/binding/scope与one-course minimal visibility；不要假设第一轮结果持续有效。
    5. 只复制并返回`02-28_WINDOWS_B1_2_RESULT`；任一失败/unknown/not_observed如实报告并停止，不执行第三轮。
  </how_to_verify>
  <verify>
    <automated>用户动作前后分别运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 1 --runtime current --platform windows --read-only`；本轮反馈后再运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 2 --runtime current --platform windows --read-only`并要求safe code=`WINDOWS_B1_REOPEN_2_COMPLETE`。</automated>
  </verify>
  <acceptance_criteria>
    1. round1 verifier在第二轮前后保持WINDOWS_B1_REOPEN_1_COMPLETE且event digest不变；round2动作前两格not_run、round3仍not_run、current build/A2/macOS不变。
    2. 用户执行第二次独立normal close/reopen，具有新的actual-exit/ownership lifecycle和server bundle；第一轮action/feedback/process/result replay全部拒绝。
    3. 旧owner在第二轮release后request/commit=0；new current owner复用same Profile且无fresh/split/copy/login/MFA。
    4. Moodle/EdStem分别实际复核auth/identity/binding/scope/minimal read并形成`B1_reopen_2` Windows/L events；不能用round1/count/overall/S/I/N/macOS代替。
    5. strict feedback与round2 verifier全pass、code exact、sensitive scan零命中；failed/human_needed/not_observed立即阻断Task3/02-29，不能自动重试覆盖。
    6. pass后只有round1/2各两格通过，round3仍not_run；B2/B3/C/D/reauth、Phase1/2/3状态未推进。
  </acceptance_criteria>
  <resume_signal>只回复完整`02-28_WINDOWS_B1_2_RESULT`块，精确使用`<interfaces>`字段/枚举；不要引用第一轮结果代填，不要发送任何私人/进程/Profile/action/log/screenshot数据。</resume_signal>
  <done>第二次独立Windows Profile close/reopen的两个exact cells、真实feedback与round2 verifier全pass，第一轮保持不变且第三轮未运行，才允许第三轮。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-28-3：用户完成Windows专属Profile第三次独立关闭重开并锁定B1交接</name>
  <files>none（真实动作只影响仓库外runtime EvidenceLedger；不创建B1 planning receipt、不改前两轮/macOS结果）</files>
  <read_first>Task1/Task2真实feedback与两个round verifier；02-27 A2/current update gate；最终exact cells、server authority、actual-exit/ownership与privacy contracts；02-29只读B2前置要求。禁止读取raw ledger DB/Profile/process/identity/course或action IDs。</read_first>
  <what-built>B1-1和B1-2分别以四个current Windows/L exact cells与两次真实用户feedback通过；B1-3两格仍not_run，B2尚不可见。</what-built>
  <action>重新运行round1/round2、A2与update gates，要求前两轮current non-corrected pass且digest不变，round3两格为空，B2及后续未运行，macOS baseline不变。硬暂停等待第三次独立normal close→actual-exit→release→same Profile reopen；为本轮mint scenario=`B1_reopen_3`的新server-only children，绝不重放前两轮authority或自动循环。

paired server按相同固定顺序取得两来源actual result并append两个`B1_reopen_3` Windows/L events。用户在protected UI第三次独立核对Profile、identity/binding/scope/minimal read/content boundary，返回`02-28_WINDOWS_B1_3_RESULT`。strict parse后依次运行round1、round2、round3三个canonical verifiers；只有第三条返回`WINDOWS_B1_REOPEN_3_COMPLETE`且全部六格current non-corrected pass、三次feedback各自存在、macOS unchanged，才完成B1并创建脱敏SUMMARY。任何non-pass立即停止，不创建成功SUMMARY、不呈现或执行02-29。

02-29 handoff固定为：runtime EvidenceLedger六个exact Windows/L cells是source of truth；`.planning/phases/02-poc-live/02-28-SUMMARY.md`只证明三个真实user-feedback checkpoints完成。02-29必须重新运行02-25 update gate、02-27 A2 gate和本计划三个round verifier，全部exit0后才允许显示B2；B2使用新的checkpoint/scenario=`B2_worker_restart`/current build/generation/paired session server correlation，不能复用任何B1 action或让B1自动触发Worker restart。</action>
  <how_to_verify>
    1. 确认UI显示B1-1/B1-2两个独立pass，当前唯一动作是`B1-3 第三次关闭重开`，B2仍locked。
    2. 第三次执行normal close并等待新的OS actual-exit/ownership released；不要复用前两轮exit或做force/process/lock操作。
    3. 第三次触发same Profile reopen/检查，核对Moodle、EdStem、identity/binding/scope及one-course minimal visibility；不进行Worker/Codex/OS restart、login/reauth或content读取。
    4. 核对UI有三轮×两来源六个独立Windows/L rows，各自scenario/time/safe code；没有overall row代替、没有macOS/S/I/N/另一round代填。
    5. 只返回`02-28_WINDOWS_B1_3_RESULT`；任一non-pass/not_observed如实报告并停止。即使全部pass，也不要点击B2或要求自动继续。
  </how_to_verify>
  <verify>
    <automated>用户反馈后依次运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 1 --runtime current --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 2 --runtime current --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 3 --runtime current --platform windows --read-only`；三条safe code分别必须为WINDOWS_B1_REOPEN_1_COMPLETE、WINDOWS_B1_REOPEN_2_COMPLETE、WINDOWS_B1_REOPEN_3_COMPLETE。</automated>
  </verify>
  <acceptance_criteria>
    1. round1/2在第三轮前后仍current non-corrected pass且digests不变；round3动作前not_run，B2及后续为空，macOS evidence/writer count不变。
    2. 用户执行第三次独立normal close/reopen，新的actual-exit/owner lifecycle和server bundle均绑定round3；前两轮replay/merge/auto-loop被拒绝。
    3. Moodle/EdStem两个`B1_reopen_3` Windows/L events分别来自actual result+本轮human action；same Profile、identityScope consistent、minimalRead pass、contentBoundary preserved。
    4. runtime ledger最终恰有六个current Windows/L required-cell pass；任何failed/human_needed correction、missing/duplicate/cross-round/source/platform/evidence/build污染均使最终gate nonzero。
    5. 三次strict feedback各自存在，三个round verifier依次返回exact pass code；gate/feedback/SUMMARY候选无credential/Profile/process/full identity/course/origin/action ID/raw page/network/screenshot。
    6. 不存在B1 planning receipt JSON、parallel ledger/public writer或macOS event增量；SUMMARY不能替代六格runtime authority。
    7. pass只标记B1完成并使02-29 eligible；B2 correlation尚未mint、Worker restart/request=0，02-29不自动执行，B3/C/D/reauth与Phase3仍blocked。
  </acceptance_criteria>
  <resume_signal>只回复完整`02-28_WINDOWS_B1_3_RESULT`块，精确使用`<interfaces>`字段/枚举；不要发送前两轮或私人数据，不要附截图/日志。所有pass也只完成B1，不授权自动执行02-29。</resume_signal>
  <done>三次独立native Windows Profile close/reopen均由用户完成；六个exact Windows/L cells、三次脱敏feedback与三个read-only verifier全部strict pass，macOS不变且B2未运行，才允许02-29 eligible。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| future plan/summaries → Windows B1 action | 计划批准、macOS pass或部分Windows前置可能被冒充resume/readiness | 02-24 explicit resume + 02-25 current update + 02-27 A2三重canonical preflight；缺一activity=0 |
| paired UI → Profile lifecycle | 未配对/重放/任意browser action可能关闭日常浏览器或绕过轮次 | current paired principal/CSRF、fixed round action、server one-time bundle、only-owned Profile；无public/generic browser operation |
| process/lease state → actual exit | PID reuse、process disappearance或lease expiry可能被误判为owned browser退出 | installation+nonce+PID OS-start identity+exact executable/control proof；OS actual-exit；unknown human_needed；不kill/delete-lock |
| round N close/reopen → round N+1 | UI/automation可能循环三次、复制一次pass或失败后继续 | 三个blocking checkpoints、exact scenario/round、每轮新feedback+bundle+verifier；non-pass立即停止 |
| reopened browser → untrusted sources | 页面/redirect/interaction可能扩大访问、读内容或写来源 | sealed actions、persisted config/scope、official-origin逐跳、bounded visible marker、无content/write/download/API fallback |
| paired actual result → EvidenceLedger | client/chat/repo/旧round/macOS/S-I-N可能伪造Windows L | server-only payload外human authority、source/scenario/build绑定、同事务append、append-only correction、gate read-only |
| six cells → B2 eligibility | overall/latest/partial cells或SUMMARY可能被当B1完成 | 三个round-specific exact-cell verifiers+三个真实feedback+completed SUMMARY；runtime ledger为source of truth |
| protected state → feedback/SUMMARY | Profile/process/identity/course/raw页面网络可能泄漏 | protected-local full values；fixed redacted blocks；safe codes/digests；no screenshots/logs；401/403 purge |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking verification |
|---|---|---:|---|---|---|---|
| T2-28-AUTHZ | Spoofing / Elevation of Privilege / Repudiation | Critical | resume/update/A2 → B1 | mitigate | V1架构、V4访问控制、V13流程 | exact completed SUMMARIES + current update/A2 gates；无default/auto/timeout；缺一不展示动作。未授权执行即Critical blocker。 |
| T2-05 | Spoofing / Tampering / Elevation of Privilege | Critical | browser/Profile lifecycle | mitigate | V2进程身份、V4授权、V14配置 | only-owned Profile、OS-start identity、actual-exit、current generation/fence；lease/PID/process-name/lock不可授权；unknown零破坏。误杀/接管即Critical blocker。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | six Windows/L cells/B2 gate | mitigate | V1证据分级、V4writer authority、V13业务状态、V16provenance | exact platform/source/scenario/evidence/build；三轮新server bundle；append-only；三个read-only verifier。merge/cross-cell/macOS/S-I-N/auto pass即Critical blocker。 |
| T2-28-SEQUENCE | Tampering / Repudiation | High | B1 round progression | mitigate | V11业务逻辑、V13状态机、V16审计 | 三个blocking checkpoints；round N strict pass+feedback后才显示N+1；failure停止；无auto loop/replay/latest覆盖。跳轮即High blocker。 |
| T2-03 | Spoofing / Tampering | Critical | reopened identity/binding/scope | mitigate | V2身份、V4对象授权、V13状态 | 每轮current Windows A2 identity/binding/scope digest比较；same display name不足；mismatch在content request前硬停且不覆盖。身份串换即Critical blocker。 |
| T2-01 | Tampering / Elevation of Privilege | Critical | source page → fixed probes | mitigate | V4授权、V5输入、V12资源、V13API | persisted config/scope、sealed four actions、official-origin逐跳、bounded marker；无URL/JS/selector/handle/write/download/API fallback。越界即Critical blocker。 |
| T2-02 | Information Disclosure | Critical | Profile/source/protected UI → output | mitigate | V7错误日志、V8数据保护、V14配置 | 无credential/MFA/input/Cookie/Profile path/PID/owner/full identity/course/raw DOM/network/action ID/screenshot；fixed feedback+safe gate。任一泄漏即失败。 |
| T2-28-AUTO | Elevation of Privilege / Repudiation | High | B1 pass → B2 | mitigate | V4用户授权、V13流程 | B1仅02-29 eligible；B2需new server correlation和独立human gate；本计划不restart Worker或auto-execute。自动推进即High blocker。 |

所有High/Critical项必须`mitigate`，不得accept/transfer、降级warning或把失败留给后续。ASVS 5.0 L1仅表示适用控制意图，不声明完整合规。自动read-only gate只复验证据；三轮动作与反馈必须来自真实用户/真实native Windows runtime。
</threat_model>

<verification>
1. future execution先证明02-24 explicit Windows resume、02-25 actual current native update与02-27 Windows A2 strict pass；当前未恢复/任一缺失时02-28 activity=0。
2. B1开始前精确枚举六个Windows/L required cells全not_run，记录macOS digests/writer count baseline；不存在预填、合并或跨格。
3. 每轮分别等待用户normal close→OS actual-exit→same Profile reopen；only-owned检查、old owner request/commit=0且不影响Worker/Codex/OS/日常browser。
4. 每轮Moodle/EdStem分别执行fixed auth+one-course minimal probes并比较A2 identity/binding/scope；interaction/mismatch/content/write/download立即停止。
5. 每轮新的server-only bundle同事务append两个exact Windows/L events；feedback不是authority，repo gate只读，失败immutable且不进入下一轮。
6. round1/2/3 verifiers逐个要求exact scenario/current build/two-source/non-corrected pass及对应safe code；任一cross-round/source/platform/evidence/latest substitution非零。
7. 敏感输出扫描覆盖feedback、gate stdout/stderr、status copy与SUMMARY候选；无Profile/PID/owner/identity/course/origin/action ID/raw page/network/screenshot/log/credential。
8. 最终六格pass、三次真实feedback和三个verifier共同允许SUMMARY/B2 eligibility；macOS unchanged、B2未运行、Phase1/2 incomplete、sharedProfile unverified、Phase3 blocked。
</verification>

<success_criteria>
- AUTH-02：用户在native Windows 11对同一dedicated Profile完成三次独立normal close/reopen；Moodle/EdStem每轮分别恢复且与Windows A2 identity/binding/scope一致。
- AUTH-04：每轮only-owned browser经OS actual-exit后才reopen，新current owner/generation/fence生效；lease expiry/PID reuse/unknown owner不能授权清理或继续，旧owner请求/提交为0。
- UAT-01：三个scenario各有Moodle/EdStem真实Windows/L append-only result与独立用户反馈；任一failed/human_needed/not_run立即阻断，不能合并为自动pass。
- D-03/D-17–D-18/T2-08：macOS/WSL/Linux/S/I/N/另一round/source/overall/latest均不能代填Windows B1；macOS证据不变，Windows后续仍缺失，Phase3 blocked。
- 无planning B1 receipt、parallel ledger/public writer、任意browser/source operation、course content/write/download或敏感输出；runtime exact cells是唯一机器事实。
- 02-29只有在completed 02-28 SUMMARY与三个canonical round verifierfresh exit0后eligible，并必须使用新的B2 human correlation；本计划不自动执行Worker restart。
</success_criteria>

<output>
仅在三个真实user checkpoints分别反馈strict pass、六个current non-corrected Windows/L exact cells全pass且三个canonical round verifiers依次返回`WINDOWS_B1_REOPEN_1_COMPLETE`、`WINDOWS_B1_REOPEN_2_COMPLETE`、`WINDOWS_B1_REOPEN_3_COMPLETE`后创建`.planning/phases/02-poc-live/02-28-SUMMARY.md`。SUMMARY只记录公开version/build、platform=windows、checkpoint=B1、round/scenario/source safe result codes、same dedicated Profile/actual-exit/identityScope/minimalRead/contentBoundary结论、三次observedAt、event digests、macOS unchanged、02-29 eligible但未执行，以及B2/B3/C/D/reauth/Phase1/2/3 gaps；不创建任何B1 planning receipt JSON，不记录设备名、账户/身份、学校/组织、课程/origin、Profile/Cookie、PID/owner/generation/fence/path、action ID、raw page/network、截图或日志。任一failed/human_needed/not_run时不创建成功SUMMARY、不覆盖失败event、不执行02-29。
</output>
