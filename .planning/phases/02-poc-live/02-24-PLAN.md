---
phase: 02-poc-live
plan: "24"
type: execute
wave: 24
depends_on: ["02-23"]
files_modified: []
autonomous: false
requirements: [AUTH-02, AUTH-04, UAT-01]
must_haves:
  truths:
    - "After macOS reauth, Phase 2 stops indefinitely before any Windows plan until the user later provides the exact native-Windows resume authorization."
    - "The Windows audit reports 88 possible cells but exactly 22 required L cells; all 22 required cells remain not_run/human_needed and possible nonrequired S/I/N cells do not block merely by being absent."
    - "macOS, fixture, S/I/N, static Windows artifact, WSL/Linux, planning approval or silence cannot create Windows evidence or release 02-25."
    - "The paired UI states Phase 1 partial, Phase 2 incomplete, shared Profile unverified and Phase 3 blocked with the exact Windows warning."
  artifacts:
    - path: scripts/release/phase2-live-gate.mjs
      provides: "Signed read-only exact-requiredness platform audit"
    - path: apps/status/src/main.ts
      provides: "Protected Windows gap and hard-stop projection"
  key_links:
    - from: scripts/release/phase2-live-gate.mjs
      to: packages/domain/src/live-evidence.ts
      via: "88 possible keys and 22 explicit Windows L obligations"
      pattern: "possibleCellCount|requiredCellCount|requiredEvidenceKeys"
    - from: apps/status/src/main.ts
      to: scripts/release/phase2-live-gate.mjs
      via: "same redacted counts and phase-block projection"
      pattern: "Windows|not_run|human_needed|blocked"
---

<objective>
Audit the honest Windows evidence gap and enforce the approved macOS-first hard stop.

Purpose: Preserve D-03/D-17/D-18 without the impossible rule that every possible evidence-class cell must pass.
Output: Read-only 88-possible/22-required-L audit and a blocking future-resume checkpoint. No Windows/update/login/live action or receipt is created.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-23-SUMMARY.md
@.planning/phases/02-poc-live/02-35-SUMMARY.md
@scripts/release/phase2-live-gate.mjs
@apps/status/src/main.ts

<interfaces>
Canonical command:
`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only`

Required strict projection:
`{schema:1,status:'pass',audit:'platform_matrix',platform:'windows',possibleCellCount:88,requiredCellCount:22,passCellCount:0,notRunCellCount:22,humanNeededCellCount:22,macosWritesApplied:0,phase3:'blocked'}`

Exact later resume:
`02-24_WINDOWS_RESUME=native_windows_11_available`
`02-24_SCOPE=resume_02-25_only`
`02-24_MACOS_SUBSTITUTION=forbidden`
`02-24_AUTO_ADVANCE=forbidden`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit exact Windows requiredness and cross-platform isolation</name>
  <files>None (read-only audit)</files>
  <action>Verify current macOS reauth and build identity, then consume the signed platform-audit branch without modifying it. Require `possibleCellCount=88` from 2 sources × 11 scenarios × S/I/N/L and `requiredCellCount=22` from the explicit Windows L obligations only. Each required L key must appear exactly once as absence-derived not_run/human_needed with no event/digest; `passCellCount=0` and `macosWritesApplied=0`. Possible nonrequired S/I/N cells may remain not_run and must not be counted as missing required evidence. Run evidence-isolation and protected-UI tests proving macOS/fixture/S/I/N/WSL/static artifact cannot fill Windows L; no command may initialize not_run rows, append/correct evidence, inspect DB/Profile, access school, spawn/restart a process or alter Phase state.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform macos --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/evidence-isolation-matrix.test.ts &amp;&amp; npm run test:ui -- --grep "receipt cell isolation|Windows blocked"</automated></verify>
  <done>The signed audit reports 88 possible/22 required Windows cells, all required L gaps remain honest, and Phase 3 remains blocked with zero writes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Stop until a future native Windows 11 resume</name>
  <files>None (blocking future-resume checkpoint)</files>
  <what-built>The macOS-first work is complete only through its approved branch. Windows has 22/22 required L cells not_run/human_needed; Phase 1 is partial, Phase 2 incomplete, shared Profile unverified and Phase 3 blocked.</what-built>
  <how-to-verify>
    1. In the paired UI, confirm the exact warning: `macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。`
    2. Confirm `possible=88`, `required L=22`, `required pass=0`, `required not_run/human_needed=22` and no Windows native/live receipt.
    3. Do not resume until an actual native Windows 11 device is available and you intend to begin only Plan 02-25.
  </how-to-verify>
  <resume-signal>Later return all four exact lines in the interfaces block. Free text, silence, prior plan approval, WSL/Linux/macOS or a request to skip Windows does not resume.</resume-signal>
  <action>Remain blocked indefinitely. Do not poll, schedule, create a SUMMARY/resume marker, run 02-25–02-34/40, or treat generic auto-mode as approval. After the exact future response, rerun Task 1 completely; only a fresh strict pass permits a redacted 02-24 SUMMARY stating 02-25-only eligibility. Resume itself creates no evidence and does not auto-start 02-25.</action>
  <verify><automated>Before and after the future response, rerun Task 1 and require the same strict counts and zero-write/isolation results; while waiting, assert 02-25–02-34/40 receipts/actions/processes remain absent.</automated></verify>
  <done>Only an exact future native-Windows resume plus a fresh audit releases 02-25; otherwise the phase remains hard blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| possible-cell vocabulary → required gate | A Cartesian display set must not silently become the completion requirement. |
| macOS/fixture → Windows | Evidence cannot cross platform or trust class. |
| user response → future execution | Only exact future authorization may release one next plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-24-01 | T/R | requiredness projection | mitigate | Fixed 88 possible/22 required-L counts, exact keys, absence-derived status and no initialization. |
| T2-24-02 | S/T/E | cross-platform evidence | mitigate | Payload-external platform authority and isolation tests; macOS/S/I/N/fixture cannot write Windows L. |
| T2-24-03 | E | resume gate | mitigate | Exact four-line response, fresh rerun and 02-25-only release; no auto-mode. |
| T2-24-04 | I | status/audit output | mitigate | Redacted allowlist and no device/account/Profile/path/event payload. |
</threat_model>

<verification>
The same signed read-only command and UI must agree on possible/required counts and the blocked phase projection. No Windows work runs in the current macOS-first session.
</verification>

<success_criteria>
- Requiredness is explicit: 88 possible, 22 required Windows L cells.
- All required Windows L cells remain not_run/human_needed at the stop.
- Future Windows work cannot begin without exact user resume and fresh isolation audit.
</success_criteria>

<output>
Do not create `02-24-SUMMARY.md` while blocked. After a valid future resume and fresh pass, create it with public build, the 88/22/0/22 counts, fixed warning and 02-25-only eligibility; include no device, account, Profile, path, source-private or action data.
</output>
