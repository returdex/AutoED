---
phase: 02-poc-live
plan: "24"
type: execute
wave: 20
depends_on:
  - "02-23"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-04
  - UAT-01
must_haves:
  truths:
    - "02-24 只有在 02-23 SUMMARY 完成且 canonical reauth verifier 证明 Moodle/EdStem 当前 `reauth` macOS/L exact cells 均为 current non-corrected pass 后才可审计；缺失或 non-pass 时停在 macOS gate，不能借 Windows deferred 状态掩盖失败。"
    - "Windows 11 required-cell registry 精确覆盖 Moodle/EdStem × 11 个 `UatScenario` × S/I/N/L，共 88 个单元；每个单元均保持 `not_run / human_needed`，不是通过 append 假 receipt 初始化。"
    - "任何 macOS receipt、synthetic S、integration I、native N、fixture、browser OS hint、Linux/WSL 或静态 Windows artifact 都不能改变任何 Windows cell，尤其不能生成/满足 Windows/L。"
    - "paired protected UI 必须逐字显示 `macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。`，并显示 Phase 1/2 未整体完成、共享 Profile 仍为待验证而非最终采用。"
    - "02-24 的 read-only 审计、自动隔离测试、macOS live pass 与计划文件/SUMMARY 都不创建 Windows N/L authority、不打开来源、不运行 Windows、不标记 Phase 1/2 complete 或 Phase 3 eligible。"
    - "自动审计通过后执行器必须停在不可自动批准的 blocking human-action checkpoint；当前不创建、启动、调度或预热 02-25–02-34，不发布/更新 Windows beta，不要求 Windows 登录或学校访问。"
    - "只有用户未来主动确认原生 Windows 11 可用并明确恢复 Phase 2 Windows native UAT，才允许 02-24 完成并释放 02-25；该信号只允许进入 02-25 自身门禁，不批准后续登录、live pass 或整条 Windows 链自动执行。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-23-SUMMARY.md"
      provides: "macOS D 后独立 reauth strict pass 与当前 beta/build/runtime 的脱敏前置；缺失时 Windows deferred audit 不得掩盖 macOS失败"
    - path: "packages/domain/src/model.ts"
      provides: "NativePlatform、SourceId、EvidenceClass、11 个 exact UatScenario 与 Phase2Gate required-cell registry"
      contains: "UatScenario"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger 与缺失 required cell → not_run/human_needed projection；跨 platform/evidence/scenario writer authority fail closed"
      contains: "EvidenceLedger"
    - path: "packages/contracts/src/presentation.ts"
      provides: "Windows gaps、Phase 1/2 incomplete、Phase 3 blocked 与 shared Profile unverified 的脱敏 projection"
      contains: "RedactedEvidenceReceipt"
    - path: "apps/status/src/main.ts"
      provides: "paired UI 的 88-cell Windows 缺口呈现、固定警告与不可自动推进状态"
      contains: "macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。"
    - path: "tests/integration/evidence-isolation-matrix.test.ts"
      provides: "macOS→Windows、S/I/N→L、source/scenario/platform 污染对完整 ledger 零影响的真实 SQLite 证明"
    - path: "tests/ui/auth-e2e.spec.ts"
      provides: "Windows 88-cell gaps、固定警告、shared Profile未采用和Phase3 blocked的真实built-UI行为证明"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "只读复验 current macOS reauth 与 Windows unchanged/Phase3 blocked 状态；无 L writer、Windows writer或计划推进权限"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      provides: "仅在未来用户明确恢复 Windows native UAT 后生成的脱敏停止/恢复交接；仍不含任何 Windows pass"
  key_links:
    - from: ".planning/phases/02-poc-live/02-23-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "completed SUMMARY 与 `--verify --checkpoint reauth --runtime current --platform macos --read-only` 双重前置，防止 deferred stop 隐藏未通过 macOS gate"
      pattern: "reauth|macos|read-only"
    - from: "packages/domain/src/model.ts"
      to: "packages/persistence/src/auth.ts"
      via: "`windows × {moodle,edstem} × 11 scenarios × {S,I,N,L}` 的完整笛卡尔 required-cell projection；无 event 时只投影 not_run/human_needed"
      pattern: "windows|EvidenceCellKey|not_run|human_needed"
    - from: "tests/integration/evidence-isolation-matrix.test.ts"
      to: "packages/persistence/src/auth.ts"
      via: "临时数据库污染矩阵比较全部 cell baseline/hash/count，拒绝 macOS/S/I/N 更新 Windows/L；不触碰 current runtime"
      pattern: "EVIDENCE_OS_POLLUTION|EVIDENCE_CLASS_POLLUTION|windows"
    - from: "packages/contracts/src/presentation.ts"
      to: "apps/status/src/main.ts"
      via: "protected status/receipt projection 驱动固定 Windows gaps、逐字警告、Phase3 blocked 与 shared Profile待验证，不靠 browser platform hint"
      pattern: "not_run|human_needed|phase3Eligibility"
    - from: "02-24 blocking checkpoint"
      to: "02-25 eligibility"
      via: "默认无边；只有未来用户主动提供 exact resume signal 才释放依赖，generic auto-approve/auto-advance/计划完成均无效"
      pattern: "RESUME_PHASE2_WINDOWS_NATIVE_UAT"
---

<objective>
在 macOS-first 已批准范围的末端建立明确硬停止：只读审计并持续展示 Windows 11 的全部 88 个 source×scenario×evidence 单元为 `not_run / human_needed`，证明 macOS/S/I/N 无法代填 Windows/L，并在一个不可自动跨越的人工 checkpoint 前停止，不执行未来 Windows plans。

Purpose: 落实 D-03、D-17–D-18 与 AUTH-02/AUTH-04/UAT-01，防御 T2-08 跨平台/跨证据类伪进度；让用户能清楚看到 macOS 已完成的范围、Windows 仍缺失的原生/live证据和 Phase 3 硬阻塞，而不会因当前执行顺序例外改变产品完成标准。
Output: 88-cell Windows gap audit、现有隔离/UI 自动证据、固定用户可见警告和一个无限期 blocking human-action stop。当前不新增 receipt/marker JSON、不修改源码、不运行 Windows/学校来源、不完成 02-24、不释放 02-25，直到用户未来主动恢复。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-10-SUMMARY.md
@.planning/phases/02-poc-live/02-11-SUMMARY.md
@.planning/phases/02-poc-live/02-12-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-15-SUMMARY.md
@.planning/phases/02-poc-live/02-16-SUMMARY.md
@.planning/phases/02-poc-live/02-17-SUMMARY.md
@.planning/phases/02-poc-live/02-18-SUMMARY.md
@.planning/phases/02-poc-live/02-19-SUMMARY.md
@.planning/phases/02-poc-live/02-20-SUMMARY.md
@.planning/phases/02-poc-live/02-21-SUMMARY.md
@.planning/phases/02-poc-live/02-22-SUMMARY.md
@.planning/phases/02-poc-live/02-23-PLAN.md
@.planning/phases/02-poc-live/02-23-SUMMARY.md
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs
@tests/integration/evidence-isolation-matrix.test.ts
@tests/ui/auth-e2e.spec.ts

<interfaces>
执行时以 02-23 SUMMARY 与真实导出为准；不得另造 Windows receipt、resume marker、parallel ledger、第二套 gate 或 platform detector。Windows required-cell 集合按最终 exact enum 计算：

```typescript
type SourceId = 'moodle' | 'edstem';
type NativePlatform = 'macos' | 'windows';
type EvidenceClass = 'S' | 'I' | 'N' | 'L';
type UatScenario =
  | 'A1_login'
  | 'A2_binding'
  | 'A2_course_visibility'
  | 'B1_reopen_1'
  | 'B1_reopen_2'
  | 'B1_reopen_3'
  | 'B2_worker_restart'
  | 'B3_codex_exit'
  | 'C_os_restart'
  | 'D_24h_recheck'
  | 'reauth';

type EvidenceCellKey = {
  platform: NativePlatform;
  source: SourceId;
  scenario: UatScenario;
  evidence: EvidenceClass;
};
```

因此 Windows audit 必须精确枚举 `1 platform × 2 sources × 11 scenarios × 4 evidence classes = 88 cells`，无遗漏、重复或聚合。`not_run/human_needed` 是缺少合格 exact event 时的 gate projection，不是写入一个伪 receipt；当前 runtime 的 Windows event count、event digests 与 writer activity 必须保持不变。

02-23 canonical strict preflight：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform macos --read-only
```

该 verifier 必须以 `REAUTH_COMPLETE` 确认 Moodle/EdStem `reauth` macOS/L current non-corrected pass、per-source isolation/recovery、Windows unchanged、Phase3 blocked；它与 B1/C/D/reauth 的 repo gate 一样只读，不能作为 Windows/L writer 或 02-25 resume authority。

Windows current required-cell audit 只能使用以下 canonical read-only command：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only
```

strict output 必须为 `{schema:1,status:'pass',audit:'platform_matrix',platform:'windows',requiredCellCount:88,notRunCellCount:88,humanNeededCellCount:88,passCellCount:0,macosWritesApplied:0,phase3:'blocked'}`。该命令只读 required-cell projection，不初始化/修正 row；任一 Windows pass、缺 cell、macOS 污染、非88计数或 Phase3 抬高必须 nonzero。

Windows 隔离自动证据使用既有 canonical suites，不新增 live/runtime writer：

```text
node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/evidence-isolation-matrix.test.ts
npm run test:ui -- --grep "receipt cell isolation|Windows blocked"
```
</interfaces>

<hard_gates>
- D-17：macOS-first 只改变执行顺序；Windows 11 N/L 与所有 required cells 继续 `not_run/human_needed`，macOS/S/I/N/fixture/Linux/WSL/静态 artifact 不得代填。
- D-18：02-24 的自动审计通过不等于 Phase 1/2 complete、shared Profile adopted 或 Phase 3 eligible；只有完整原生 Windows 链真实通过，或用户明确重定义平台范围并同步 REQUIREMENTS/ROADMAP，才能改变双平台门。
- 当前无 Windows resume：不得创建/执行/调度 02-25–02-34，不下载/更新 Windows beta，不运行远程 Windows/WSL，不登录/MFA，不访问学校，不 mint Windows pending action，不写 Windows N/L。
- generic auto mode、auto-approve、执行器推断、设备检测、现有 Windows artifact、用户过去提到有电脑或计划文件存在都不是 resume signal。
- 只有用户未来主动提供 `<resume-signal>` 的 exact redacted readiness block 才能完成 checkpoint；该信号仅允许进入 02-25 自身 beta availability/update gate，不能自动执行 02-26–02-34。
</hard_gates>
</context>

<tasks>

<task type="auto">
  <name>02-24-1：只读审计 Windows 88-cell 缺口与跨平台证据隔离</name>
  <files>none（仅读 current redacted gate/projection并在隔离临时数据库/本地 synthetic UI harness 运行既有测试；不得修改 runtime ledger、源码、Profile、学校资料或 planning receipts）</files>
  <read_first>AGENTS.md 的 Data integrity and verification、Execution after plan approval、Current stop condition；02-CONTEXT.md D-03、D-17–D-18；02-RESEARCH.md T2-08/Validation Architecture；02-PATTERNS.md Evidence and hard gates；02-23-SUMMARY.md 的 exact macOS reauth pass/current build 与 canonical read-only verifier；02-01/02-02 SUMMARY 的 UatScenario/EvidenceCellKey/required-cell projection/ledger writer authority；02-09/02-11 SUMMARY 的 fixed warning/OverallGate/Windows rows；02-10 SUMMARY 与 evidence-isolation-matrix 的 pollution cases。禁止读取 legacy/runtime DB/WAL、Profile/Cookie、私人页面或学校来源。</read_first>
  <action>先确认 `.planning/phases/02-poc-live/02-23-SUMMARY.md` 是 completed strict pass，并运行 `<interfaces>` 的 exact reauth verifier；必须得到两来源 current non-corrected `reauth` macOS/L pass、per-source isolation/recovery、Windows unchanged、Phase3 blocked。缺 SUMMARY、nonzero、旧 build/generation、任一 macOS gate 缺失或 verifier 非 read-only 时立即 `human_needed`，不得进入 deferred stop 或用 Windows not_run掩盖 macOS失败。

然后运行 canonical Windows `--audit-matrix`，并以最终 domain required-cell registry 与 current redacted status/receipt projection 核对全部 88 个 Windows keys；不可直接读 DB、推断 SQL 空表、只看总 gap 数或抽样。strict output 必须精确为 required/notRun/humanNeeded=88、pass=0、macosWritesApplied=0、phase3=blocked；每个 `(windows, source, scenario, evidence)` 必须恰出现一次且状态为 `not_run/human_needed`，event/digest 为 absent，不存在 pass/failed/corrected/latest 聚合代填。任何 missing/duplicate/unknown scenario、Windows event、Windows/L authority、platform/browser hint 派生结果均为 T2-08 blocker；本任务不得通过 append “not_run” event 修复，必须停回实现→完整测试→新不可覆盖 beta→实际更新。

记录 current runtime 的 Windows 88-cell redacted status/digest/count baseline 到内存，不落盘；运行 `<interfaces>` 的 evidence isolation integration test 与 UI targeted test。integration 使用独立临时 SQLite，逐项证明 macOS receipt不能写/查询成 Windows、S/I/N authority不能写L、source/scenario/platform不能互填、失败尝试后全矩阵 baseline/hash/count不变。UI test 证明即使所有 macOS synthetic/fixture cells pass、browser声称Windows或Linux/WSL，88 个 Windows cells仍 `not_run/human_needed`，OverallGate逐字显示 `macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。`，Phase 1/2 incomplete、shared Profile待验证。synthetic harness 不得产生 N/L 或接触 current runtime。

测试后重新运行 exact reauth verifier 与 canonical Windows audit；两次 audit strict output、88-cell baseline/digest/count与writer activity必须完全不变，Windows仍全缺口、Phase3 blocked。自动命令只证明门禁/隔离和当前缺口，没有 Windows native/live证据；不得生成 Windows receipt、SUMMARY pass、resume token，或把 macOS共享Profile观察升级为最终adopted。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform macos --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/evidence-isolation-matrix.test.ts &amp;&amp; npm run test:ui -- --grep "receipt cell isolation|Windows blocked" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform macos --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. 02-23 completed SUMMARY 与两次 exact reauth verifier 均匹配 current beta/build/runtime并返回 `REAUTH_COMPLETE`；Moodle/EdStem `reauth` macOS/L current non-corrected pass，Windows unchanged、Phase3 blocked。
    2. required-cell audit 精确得到 88 个唯一 Windows keys：2 sources × 11 exact scenarios × 4 evidence classes，无遗漏、重复、合并、未知值或额外 scenario。
    3. 两次 canonical Windows audit 都 exit 0 且 strict output 精确为 required/notRun/humanNeeded=88、pass=0、macosWritesApplied=0、phase3=blocked；每格无event/digest，没有通过写not_run receipt、latest-row、总gap或默认platform字符串伪初始化。
    4. isolation test逐项拒绝 macOS→Windows、S/I/N→L、fixture→N/L、另一source/scenario/platform互填；每个拒绝后完整临时ledger baseline/hash/count零变化。
    5. tests只使用临时SQLite/synthetic UI，不读取/写current runtime DB/Profile，不访问学校，不生成N/L；current Windows 88-cell baseline、event count/digests与writer activity前后完全相同。
    6. paired UI/真实built assets逐字显示`macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。`；Windows行全部not_run/human_needed，Phase1/2 incomplete、shared Profile待验证，无Phase3 eligible/complete/adopted误导。
    7. 自动审计通过只允许进入Task 2的停止checkpoint；02-25–02-34 execution count=0，Windows beta/update/login/school/browser/process/action/receipt调用均为0。
  </acceptance_criteria>
  <done>current runtime与隔离回归共同证明Windows 88-cell矩阵完整保持not_run/human_needed、macOS/S/I/N不能更新Windows/L、固定警告和Phase3阻断正确；执行器只能进入停止checkpoint，不能推进Windows。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-24-2：在 macOS-first 边界无限期停止，等待用户未来主动恢复原生 Windows UAT</name>
  <files>none（该 checkpoint 不创建 resume 文件/receipt/SUMMARY，不修改 roadmap/state/runtime，不启动未来 plans；用户未恢复时保持未完成）</files>
  <read_first>Task 1 的 strict 88-cell audit；AGENTS.md 的 Windows native evidence/failed live gate/automatic advancement 禁令；02-CONTEXT D-03/D-17–D-18；ROADMAP 02-25–02-34 的未来 Windows hard gates；当前 paired UI 的 exact Windows warning/Phase3 blocked projection。</read_first>
  <what-built>macOS-first 允许的设计、实现、自动测试、beta发布与macOS A/B/C/D/reauth已按各自门禁完成；Windows 11全部88个单元仍是not_run/human_needed，Phase1/2未整体完成，共享Profile未最终采用，Phase3 blocked。</what-built>
  <action>在 Task 1 strict pass 后，向用户只报告脱敏停止状态：`checkpoint=02-24-windows-deferred`、`windows_cells=88/88 not_run_human_needed`、固定警告、`phase1=partial`、`phase2=incomplete`、`shared_profile=unverified`、`phase3=blocked`。不得要求用户现在确认、不得将沉默/计划批准/之前的macOS-first例外视为resume，也不得启动任何后台wait/poll/automation来检测Windows设备。

随后永久暂停当前 execute chain，不标记Task/plan/phase完成，不创建02-24成功SUMMARY，不调用02-25，不预先下载/验证Windows beta，不生成Windows pending action，不连接Windows/WSL，不打开学校来源。generic auto-approve、auto-advance、terminal condition、定时器或后续无关消息均不能跨越此checkpoint。

只有用户未来主动提供 `<resume-signal>` 的完整exact block，明确原生Windows 11设备可用并授权恢复Phase2 Windows native UAT，才重新运行Task 1的reauth/current 88-cell只读审计并确认所有Windows单元仍not_run/human_needed。复验通过后仅允许完成02-24、创建脱敏SUMMARY并把02-25变为eligible；不得把resume写成任何Windows pass，不得自动执行02-25或其后的登录/生命周期门。缺字段、自由文本“继续”、WSL/Linux/macOS替代、设备不可用、想跳到Phase3或要求复用macOS结果均保持checkpoint blocked并请求明确修正。</action>
  <how_to_verify>
    1. 当前只查看 paired UI：确认固定警告逐字存在，Windows 88/88 cells 均 `not_run/human_needed`，Phase 1 partial、Phase 2 incomplete、shared Profile unverified、Phase 3 blocked。
    2. 不需要现在操作 Windows、下载 beta、登录学校、提供设备资料或回复批准；保持本 checkpoint 等待即可。
    3. 未来只有在原生 Windows 11 设备真实可用且你决定恢复时，才发送 `<resume-signal>` 的固定脱敏块；不要发送主机名、用户名、路径、IP、账户、课程、Cookie、Profile、截图或日志。
    4. resume 后应只看到 02-25 的 Windows beta 可获取性/更新门被允许开始；02-26 登录及后续 plans仍必须逐个等待各自真实人工动作，不能自动连跑。
  </how_to_verify>
  <verify>
    <automated>进入 checkpoint 前，Task 1 的完整命令必须 exit 0，并在同一 current runtime 上确认 Windows 88/88=`not_run/human_needed`、Phase1/2 incomplete、sharedProfile=unverified、Phase3=blocked；checkpoint 等待期间 02-25–02-34 process/job/action/receipt/start count 必须持续为0。用户未来返回 exact resume block 后，先原样重跑 Task 1 完整 read-only verifier+integration+UI 命令，仍全部 exit 0 才可释放02-25。</automated>
  </verify>
  <acceptance_criteria>
    1. 停止状态明确显示exact警告、Windows 88/88 not_run/human_needed、Phase1 partial、Phase2 incomplete、shared Profile unverified、Phase3 blocked。
    2. 用户未主动resume时，Task 2/02-24保持未完成；不存在自动批准、超时通过、沉默即同意、计划完成推断或后台设备检测。
    3. 02-25–02-34均未执行/调度/预热；Windows下载、更新、bootstrap、browser/Profile、学校访问、login/MFA、pending action、N/L receipt增量全部为0。
    4. resume只接受完整exact redacted block并要求native Windows 11 available；Linux/WSL/macOS/fixture/static artifact/remote platform hint不得替代。
    5. resume前后都重验88 cells全为not_run/human_needed且cross-platform隔离通过；resume本身不填任何cell、不markpass、不adopt共享Profile、不解除Phase3。
    6. 完成checkpoint最多使02-25 eligible；仍不得自动execute 02-25或批准02-26–02-34，后续每个human gate继续独立阻塞。
  </acceptance_criteria>
  <resume-signal>当前不要回复此信号。未来只有在原生 Windows 11 设备真实可用且你明确决定恢复时，主动发送完整脱敏块：`RESUME_PHASE2_WINDOWS_NATIVE_UAT`、`native_windows_11_available=yes`、`resume_windows_phase2=yes`、`allow_next_plan=02-25-only`、`macos_not_substitute=acknowledged`、`phase3_remains_blocked=acknowledged`。不要包含设备名、账户、学校、课程、路径、IP、Profile/Cookie、截图或日志。</resume-signal>
  <done>在用户未来主动提供exact resume block并通过fresh 88-cell/隔离复验前，02-24保持blocking且未来Windows plans完全未执行；resume后只释放02-25自身门禁，不生成Windows pass或自动推进。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| macOS/automated receipts → Windows required cells | 已通过的macOS或S/I/N结果可能被错误聚合成Windows/N/L | exact platform/source/scenario/evidence key；authority交叉校验；全矩阵baseline零变化 |
| absent ledger event → not_run projection | “初始化”可能被实现为伪receipt或缺格被隐藏 | 由required-cell registry枚举88格；absence只投影not_run/human_needed；不append初始化事件 |
| browser/OS hint/WSL → native platform | UA、环境变量、Linux/WSL或静态artifact可能冒充原生Windows | native platform只由受信OS evidence建立；本计划无Windows authority，全部保持not_run |
| protected status/receipts → UI/OverallGate | UI可能隐藏缺口、改文案或显示Phase3 eligible/shared Profile adopted | exact warning、88独立rows、typed gate projection、built-UI回归；不用颜色/总数代替 |
| plan completion/auto-mode → 02-25 scheduler | 自动执行器可能把审计成功当成用户恢复Windows | blocking human-action；无默认/超时/auto approval；exact future resume signal只释放02-25 eligibility |
| future user resume → Windows evidence | “继续”可能被错误解释为Windows pass或整链批准 | exact redacted readiness block；fresh re-audit；resume不写receipt，只进入02-25独立门 |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking test |
|---|---|---:|---|---|---|---|
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | EvidenceLedger/required cells/Phase2Gate | mitigate | V1信任级别、V4 writer authorization、V13业务状态、V16 provenance | 88个exact Windows cells、payload外authority、append-only、absence-derived not_run；macOS/S/I/N/fixture/另一格不可代填。任何Windows/L污染、缺格隐藏或Phase3抬高均为Critical blocker。 |
| T2-24-PLATFORM | Spoofing / Elevation of Privilege | High | browser hint、Linux/WSL、静态artifact → Windows native claim | mitigate | V2平台身份、V4授权、V14安全配置 | 本计划无Windows native writer；UA/env/query/storage/WSL/static closure不得改变platform。任何非原生结果写Windows N/L即High blocker。 |
| T2-24-DEFAULT | Tampering / Repudiation | High | missing event → initialized status | mitigate | V5 schema validation、V13状态完整性、V16审计 | 笛卡尔registry恰88格；无event只投影not_run/human_needed；禁止初始化receipt、bulk update或latest-row填充。遗漏/重复/伪event即High blocker。 |
| T2-24-UI | Spoofing / Repudiation | High | gate projection → user | mitigate | V4授权状态、V5安全呈现、V16错误/状态可审计 | 固定逐字警告、Windows独立rows、Phase1/2 incomplete、Profile unverified、Phase3 blocked；真实built UI测试。隐藏缺口或误导complete/adopted即High blocker。 |
| T2-24-AUTO | Elevation of Privilege / Repudiation | Critical | checkpoint/orchestrator → future Windows plans | mitigate | V1架构边界、V4用户授权、V13业务流程 | checkpoint无自动/default/timeout路径；用户未来exact signal只释放02-25；计划批准/macOS pass不等于resume。任何02-25预执行或自动跨门即Critical blocker。 |
| T2-04（继承） | Spoofing / Tampering | High | macOS ownership evidence → Windows AUTH-04 claim | mitigate | V2进程身份、V4访问控制、V14生命周期 | macOS process/Profile evidence只能留在macOS exact cells；Windows B/ownership N/L全not_run。复用macOS PID/fence/Profile结论为Windows即High blocker。 |

所有 High/Critical 项必须 `mitigate`，不得 accept、transfer、降级为warning或以“Windows以后再跑”为理由省略矩阵。ASVS 5.0 L1仅表示适用控制意图，不声明完整合规。自动测试pass只证明隔离与缺口诚实；任何污染、缺格、误导UI、auto-advance或future-plan activity都使02-24失败并保持Phase3 blocked。
</threat_model>

<verification>
1. 先验证02-23 completed SUMMARY及canonical reauth current read-only gate，防止Windows deferred状态掩盖macOS reauth失败。
2. 从最终exact enum计算并逐key读取Windows 88 cells；全部为not_run/human_needed、无event/digest/authority，不能只看总数或抽样。
3. 运行evidence-isolation integration：macOS→Windows、S/I/N→L、fixture→N/L、source/scenario/platform互填全拒绝，完整临时ledger baseline不变。
4. 运行真实built UI targeted suite：88个Windows gaps和固定文案精确；Phase1/2 incomplete、shared Profile unverified、Phase3 blocked；browser/WSL hint无效。
5. 自动测试前后重跑current reauth verifier并比较Windows redacted baseline/digests/counts/writer activity；current runtime零写、学校/Profile/Windows访问为零。
6. 审计通过后进入不可自动批准checkpoint；检查02-25–02-34未执行/调度/预热且无任何Windows beta/action/receipt。
7. 用户未future-resume时保持02-24未完成；resume后先fresh重跑全部审计，只允许02-25 eligible，不产生Windows pass或Phase3进度。
</verification>

<success_criteria>
- AUTH-02/D-17：Windows 11 × Moodle/EdStem × 11 scenarios × S/I/N/L 共88格全部诚实保持not_run/human_needed；macOS生命周期/live结果不代替Windows。
- AUTH-04/D-03：Windows Profile ownership/Worker/Codex/OS lifecycle均未验证；共享Profile仍unverified，不复用macOS process/fence结论、不宣称最终adopted。
- UAT-01/D-18：paired UI逐字警告`macOS 结果不能替代 Windows 验证；Phase 3 仍被阻塞。`；Phase1 partial、Phase2 incomplete、Phase3 blocked。
- T2-08：现有integration/UI行为测试及current read-only baseline共同证明macOS/S/I/N/fixture/WSL不能更新Windows/L或推进gate。
- 当前macOS-first scope在blocking checkpoint硬停；未收到用户未来exact resume signal前，02-25–02-34保持unexecuted且02-24不完成。
- future resume仅释放02-25独立门禁，不新增Windows证据、不自动执行后续计划、不改变Phase3阻塞。
</success_criteria>

<output>
当前停止期间不得创建 `.planning/phases/02-poc-live/02-24-SUMMARY.md` 或任何resume/Windows receipt文件，因为成功SUMMARY会让依赖看似可推进。只有用户未来主动提供exact `<resume-signal>` 且fresh 88-cell/隔离审计仍strict pass后，才创建02-24-SUMMARY；其内容仅限current公开version/build、Windows cells=88/88 not_run/human_needed、fixed warning、Phase1 partial、Phase2 incomplete、sharedProfile unverified、Phase3 blocked、resume observedAt及02-25-only eligibility。不得含设备/账户/学校/课程/Profile/PID/path/IP/Cookie/action ID/log/screenshot，也不得写任何Windows pass或自动启动02-25。
</output>
