---
phase: 02-poc-live
plan: "14"
type: execute
wave: 14
depends_on:
  - "02-13"
files_modified:
  - .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json
  - .planning/phases/02-poc-live/02-14-MACOS-NATIVE-RECEIPT.json
autonomous: false
requirements:
  - AUTH-02
  - UAT-01
must_haves:
  truths:
    - "只有 02-13 的统一 Moodle+EdStem beta 在 returdex 既有仓库以新且不可覆盖的 0.1.0-beta.N 发布，并经匿名完整字节回取、manifest/signature/hash/closure 复验全部通过后，用户才会看到更新提示词。"
    - "交给用户的更新提示词逐字来自 `release/phase2-install-prompt.md`，其中 version、tag、artifact URL/bytes/SHA-256、manifest/signature/build identity、目标 macOS 与预期均与四份 02-13 receipt 完全一致；执行者不猜测或手工改写。"
    - "仓库内 Phase task 只作为 controller 等待；用户本人在同一主机/账户的 local projectless Codex updater task 中粘贴提示词并处理必要的 macOS 授权/重启。updater 只执行有界自动更新且不写 repo/planning、不登录来源、不创建 gate receipt；controller 不伪造结果或把 publication 当成设备已更新。"
    - "仅当实际安装版本/hash/build、全部受管 entrypoints、API/Worker、paired UI 均核对一致，cleanup=complete，且来源配置仍未确认、没有打开学校页面时，02-14 才可通过。"
    - "failed、human_needed、cleanup_pending、version/build/entrypoint mismatch、service unhealthy、paired UI unavailable、反馈缺项或无法取得 beta 中任一情况都保持硬阻塞；不得执行 02-15 或后续登录/live 计划。"
    - "用户反馈和 SUMMARY 只保存固定脱敏 code/状态、公开 version/hash/build identity 与时间；不得含姓名、邮箱、课程、学校 path、Profile/Cookie、OS 私有路径、原始日志、截图或凭据。"
    - "此 macOS 更新只满足后续 macOS live 场景的版本前置，不填写任何 AUTH-02 source×scenario L cell；Windows 始终 not_run/human_needed，Phase 1/2 不整体通过，Phase 3 仍 blocked。"
  artifacts:
    - path: "release/phase2-beta-artifacts.json"
      provides: "02-13 已签名统一 beta 的 version/tag/build/manifest 与逐平台资产 hash/bytes；02-14 只读"
      contains: "0.1.0-beta."
    - path: "release/phase2-publication.json"
      provides: "returdex repository id、不可覆盖 tag/release/asset ids 与发布身份 receipt；02-14 只读"
      contains: "1350421724"
    - path: "release/phase2-availability.json"
      provides: "匿名完整回取及 manifest/signature/hash/closure 的 pass receipt；checkpoint 的唯一 availability gate"
      contains: "\"status\": \"pass\""
    - path: "release/phase2-install-prompt.md"
      provides: "用户在 Codex 中执行的精确 macOS 更新提示词；必须与 release receipts 绑定且逐字呈现"
      contains: "0.1.0-beta."
    - path: ".planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json"
      provides: "仅在 strict human pass 后生成的脱敏、机器可读 macOS actual-update gate；02-15 只读验证"
      contains: "UPDATE_COMPLETE"
  key_links:
    - from: "release/phase2-install-prompt.md"
      to: "release/phase2-beta-artifacts.json"
      via: "prompt 中 version/tag/URL/bytes/SHA-256/manifest/build identity 必须逐字段等于签名 artifact receipt"
      pattern: "version|sha256|manifest|build"
    - from: "release/phase2-availability.json"
      to: "release/phase2-publication.json"
      via: "匿名回取的 immutable tag/asset ids/full-byte hashes 与 publication receipt 完全一致且 status=pass"
      pattern: "anonymous|immutable|status"
    - from: "02-14 human feedback"
      to: "02-15 eligibility"
      via: "仅 exact MacosUpdateFeedback 的 pass+cleanup complete+actual build/entrypoints/services/UI match 可解除本检查点；任何其他组合硬阻塞"
      pattern: "UPDATE_COMPLETE|cleanup=complete|paired_ui=ready"
    - from: ".planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json"
      to: "release/phase2-beta-artifacts.json"
      via: "02-15 read-only gate 将 receipt 的 version/tag/artifact/manifest/build 与 immutable beta receipt 重新逐项比较"
      pattern: "artifactSha256|manifestSha256|buildId"
---

<objective>
在 Phase 2 不可覆盖 beta 已匿名可获取并完成完整复验后，停等用户在真实 Codex 中亲自更新 macOS 安装，再以严格脱敏反馈确认“实际运行的正是该 beta”，而不是把发布、下载或自检候选误称为用户已更新。

Purpose: 落实 D-12、D-14 与 AUTH-02/UAT-01 的人工更新前置门；任何 availability、安装、清理、实际入口或服务结果不确定时都阻止来源范围确认和后续 live 登录。
Output: 一个只读自动 release preflight 和一个不可自动批准的 `checkpoint:human-action`。本计划不修改仓库、不发布新资产、不打开学校来源、不登录、不写 source live receipt。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/RELEASE-STABILIZATION.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-13-PLAN.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@release/phase2-beta-artifacts.json
@release/phase2-test-report.json
@release/phase2-publication.json
@release/phase2-availability.json
@release/phase2-install-prompt.md

<interfaces>
02-13 必须稳定交付且 02-14 只读以下五项；若 SUMMARY 的实际字段名有机械差异，以其 schema 为准，但不得减少验证语义：

```typescript
interface Phase2BetaArtifacts {
  version: `0.1.0-beta.${number}`;
  tag: string;
  build: BuildIdentity;
  manifestSha256: string;
  signature: { keyId: string; verified: true };
  targets: readonly {
    platform: 'macos' | 'windows'; arch: string;
    assetUrl: string; assetId: number; bytes: number; sha256: string;
  }[];
}

interface Phase2AvailabilityReceipt {
  status: 'pass' | 'failed';
  anonymous: true;
  immutable: true;
  version: string; tag: string; checkedAt: string;
  manifestVerified: boolean; signatureVerified: boolean;
  closureVerified: boolean; fullByteAssetsVerified: boolean;
}
```

`release/phase2-install-prompt.md` 是唯一 update prompt 来源。执行时必须将其中已经绑定 receipt 的完整内容逐字展示；不得用本 PLAN 的示意文本重新拼接 version/hash，也不得从“latest”重解析为不同 release。

用户恢复 checkpoint 时只接受以下逐行 `MacosUpdateFeedback`；值均不含私人账户/课程/Profile/路径/日志：

```text
02-14_UPDATE_RESULT
checkpoint=02-14-macos-update
platform=macos
version=<必须等于 preflight exact version>
artifact_sha256=<必须等于 preflight 当前 macOS asset sha256>
result=pass|failed|human_needed
result_code=UPDATE_COMPLETE|UPDATE_FAILED|UPDATE_HUMAN_NEEDED|CLEANUP_PENDING|VERSION_MISMATCH|ENTRYPOINT_MISMATCH|SERVICE_UNHEALTHY|PAIRED_UI_UNAVAILABLE|RESULT_NOT_OBSERVED
cleanup=complete|cleanup_pending|not_observed
actual_build=matched|mismatch|not_observed
entrypoints=matched|mismatch|not_observed
api=healthy|unhealthy|not_observed
worker=healthy|unhealthy|not_observed
paired_ui=ready|unavailable|not_observed
source_configuration=moodle:not_confirmed|confirmed|not_observed,edstem:not_confirmed|confirmed|not_observed
school_access=not_started
windows=not_run/human_needed
phase3=blocked
observed_at=<ISO-8601 with timezone>
```

只有 `result=pass`、`result_code=UPDATE_COMPLETE`、`cleanup=complete`、actual_build/entrypoints=`matched`、API/Worker=`healthy`、paired_ui=`ready`、两来源配置=`not_confirmed`、school_access=`not_started`、Windows/Phase3 固定值和 exact version/hash 全部同时成立才允许完成 02-14。`artifact_sha256` 是公开发布资产校验值，不是机器私有资料。

strict pass 后由 executor 创建且后续计划只读以下唯一 machine gate；不得在 failed/human_needed 时创建：

```typescript
interface Phase2MacosUpdateGateReceipt {
  schema: 1;
  checkpoint: '02-14-macos-update';
  platform: 'macos';
  version: string; tag: string;
  artifactSha256: string; manifestSha256: string; buildId: string;
  result: 'pass'; resultCode: 'UPDATE_COMPLETE'; cleanup: 'complete';
  actualBuild: 'matched'; entrypoints: 'matched'; api: 'healthy'; worker: 'healthy';
  pairedUi: 'ready';
  sourceConfiguration: { moodle: 'not_confirmed'; edstem: 'not_confirmed' };
  schoolAccess: 'not_started';
  windows: 'not_run/human_needed'; phase3: 'blocked';
  observedAt: string;
  feedbackDigest: string; // normalized allowlisted feedback SHA-256; raw feedback/log is not copied
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>02-14-1：只读复验不可覆盖 beta 与精确安装提示词前置</name>
  <read_first>AGENTS.md 的 Installation, upgrades and releases、Execution after plan approval、Credentials and login、Data integrity and verification；02-CONTEXT.md D-12、D-14、D-17–D-18；02-RESEARCH.md Validation Architecture/Recommended Plan Structure；02-VALIDATION.md Beta gate/Hard Gates；02-PATTERNS.md Evidence and hard gates；02-13-PLAN/SUMMARY 与五个 release handoff 文件。不得从 legacy、旧 beta、“latest”别名或未复验本地缓存取得更新值。</read_first>
  <files>none（只读 preflight；不得修改 release receipts、tag、asset、仓库或安装）</files>
  <action>按 UAT-01 在呈现任何人工步骤前执行只读 preflight。先 strict parse `phase2-test-report.json`，要求 typecheck/unit/integration/UI/native applicable suite 与 sensitive scan 全部为 pass，且 build/report hashes 指向同一个 `phase2-beta-artifacts.json`；native 只认可 02-13 声明的实际 macOS N 范围，绝不据此填写 Windows/live。再交叉核对 artifacts/publication：owner 必须为 `returdex`、repository id 必须为 `1350421724`、version/tag 必须是 02-13 选定的新 `0.1.0-beta.N`，immutable=true，asset id 非旧 asset 且 tag/asset 不可覆盖，Git author/committer 与 GitHub authenticated owner 的已验证 receipt 存在，PolyForm Noncommercial 1.0.0 与 support matrix 完整。不得调用 update/delete release/tag/asset API。

在 `mktemp -d` 创建本次只读复验输出目录，以 02-13 的 `verify-availability.mjs` 对 publication 中固定 tag/asset URL 做匿名完整字节重新获取；比较 macOS asset id/bytes/SHA-256、manifest SHA-256、签名 key id/verification、build identity 与 closure，所有网络目标必须仍是既有 returdex release/CDN allowlist。临时输出只用于比较，finally 删除；仓库内 `phase2-availability.json` 不得被重写。要求仓库 receipt 与此次复验均为 status=pass、anonymous=true、immutable=true，且 checked version/tag/assets 与 artifacts/publication 完全一致。404、认证依赖、partial/range-only、hash/signature/closure mismatch、remote conflict、rate limit/网络不确定都视为 failed/human_needed 并立即停止，不进入 Task 2。

最后读取 `phase2-install-prompt.md` 并按 02-13 prompt schema 验证：文件 hash 已绑定到签名 manifest；内部 exact version/tag、macOS target、asset URL/id/bytes/SHA-256、manifest/signature/build、更新前预览、数据保留、迁移/恢复、实际 entrypoint/service/UI 自检、cleanup complete 条件和失败停止文案逐项等于上述 receipts。prompt 必须明确只更新新产品受管安装、不写 legacy、不删除课程资料、不复制/备份 Profile、不登录学校、不输入密码/MFA、不覆盖 beta；不得包含 credential/token/Profile path/private user path。任何缺项或差异都是 Critical blocker。只有全部通过才把 prompt 原文、exact version、macOS asset SHA-256、manifest SHA-256、build identity 和预期反馈 schema交给 checkpoint；不创建 L/pass receipt（D-12、D-14）。</action>
  <verify>
    <automated>tmp_dir=$(mktemp -d) &amp;&amp; trap 'rm -rf "$tmp_dir"' EXIT &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-availability.mjs --release release/phase2-beta-artifacts.json --publication release/phase2-publication.json --out "$tmp_dir/availability.json" &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --artifacts release/phase2-beta-artifacts.json --tests release/phase2-test-report.json --publication release/phase2-publication.json --availability release/phase2-availability.json --fresh-availability "$tmp_dir/availability.json" --prompt release/phase2-install-prompt.md --platform macos --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. 02-13 test report 的适用套件/sensitive scan 全 pass 且其 build/artifact/report hashes 与 unified beta receipt 一致；任何 skip、failed、human_needed、不同 build 或敏感扫描命中阻止 checkpoint。
    2. artifacts/publication 对同一 exact version/tag/build/manifest/signature/macOS asset id/bytes/SHA-256 一致；owner=`returdex`、repository id=`1350421724`、immutable=true，且不是已有 tag/asset overwrite。
    3. 新匿名 full-byte refetch 与 committed availability 都为 pass/anonymous/immutable，并逐项复验 hash/signature/closure/build；临时比较后仓库 release files 的 before/after SHA-256 完全不变。
    4. install prompt 的文件 hash 绑定 manifest，且所有 version/tag/URL/id/bytes/hash/signature/build/target 值逐字段等于 receipts；包含实际更新、迁移/恢复、entrypoint/API/Worker/UI/cleanup 验证与失败停止，没有 legacy write、Profile backup、school login 或敏感值。
    5. preflight 只输出公开 release identity 与脱敏 safe code；不输出本机路径、Profile/Cookie、姓名/邮箱/course、credentials、raw logs 或截图，也不写 UAT ledger L/N/Windows cell。
    6. 任一失败保持 02-14 `human_needed/blocked` 并停止；不得显示 Task 2 的更新指令、切换到别的 release、自动重建/发布或继续 02-15。
  </acceptance_criteria>
  <done>同一个不可覆盖 Phase 2 beta 已经由两次匿名 full-byte verification 证明可获取，且唯一 prompt 与 exact version/hash/build/预期完全绑定；尚未声称用户设备已更新。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-14-2：用户在 Codex 中实际更新 macOS 并返回脱敏安装结果</name>
  <read_first>Task 1 的通过结果与 exact version/macOS asset SHA-256/manifest/build；经验证的 `release/phase2-install-prompt.md` 原文；AGENTS.md 的 hard human gates；02-CONTEXT.md D-12、D-14、D-17–D-18；UAT-01。不得读取或展示 Profile、Cookie、学校身份/course、原始安装日志或本机私有路径。</read_first>
  <files>.planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json（仅 strict pass 后由 executor 生成；用户更新由本人执行，非 pass 时此文件不得存在）</files>
  <what-built>02-13 已构建、签名、发布并匿名复验一个不可覆盖的统一 Moodle+EdStem macOS beta；Task 1 已再次核对 exact version/hash/build 和唯一 update prompt。发布成功仍不代表这台设备已经更新。</what-built>
  <action>暂停自动执行并向用户展示以下内容：① exact version/tag；② macOS asset URL、bytes 与 SHA-256；③ manifest SHA-256、签名 key id 与 build identity；④ `release/phase2-install-prompt.md` 的完整原文，必须逐字复制，不得用摘要、省略号、latest 链接或本 PLAN 的 placeholder 替代；⑤ 下列编号步骤与 `MacosUpdateFeedback` 模板。明确告诉用户：当前 AutoED repository task 是 controller，不能执行或证明更新；必须由用户在同一主机/账户新建一个不附属于 AutoED project/worktree 的 **local projectless Codex updater task**，逐字粘贴 prompt 启动有界自动更新，并本人点击 OS 授权、重启 Codex。updater 不得写 repo/`.planning`、创建 gate receipt、打开 Moodle/EdStem、输入密码/MFA或访问课程；controller 不会合成结果。

用户步骤必须精确为：
1. 核对屏幕上 exact version 和 macOS asset SHA-256 与 prompt 内完全一致；若不一致，停止并回复 `result=human_needed` + `result_code=VERSION_MISMATCH`。
2. 在同一台 macOS、同一用户账户的 Codex 中新建 **local projectless task**：不要选择 AutoED project，不要选择其 repository/worktree，也不要创建 cloud task。逐字粘贴所显示的已验证 prompt；不要替换 tag/URL/hash，不要使用 `latest`，不要发给另一个账户/主机执行。若任务因读取 AutoED `AGENTS.md` 而回复“当前 human-action gate，不能更新”，说明建成了 project-attached task；停止并报告 `result=human_needed`、`result_code=UPDATE_TASK_CONTEXT_INVALID`，这不是产品失败，不需要新 beta。
3. 只对该 prompt 明确要求的 macOS 系统授权本人作出决定；不要把密码、MFA、token、Cookie、Profile 路径或安装日志粘贴回聊天。任何意外要求学校登录、访问 legacy、删除课程资料或导出 Profile 时立即停止，报告 `UPDATE_HUMAN_NEEDED`。
4. 等更新任务明确完成迁移、启动、自检与旧受管程序/entrypoint/process 清理。`cleanup_pending`、已恢复旧版、失败、卡住或只下载/发布都不是成功；不要继续来源配置。
5. 按 prompt 的只读检查核对实际运行 version/build、全部受管 entrypoints、API/Worker health；打开本安装的本地管理页并按既有方式配对，确认 paired UI ready。不要点击来源确认/登录/probe；两来源 `source_configuration` 应保持 `not_confirmed`，`school_access=not_started`。
6. 从产品/安装器的脱敏结果中填写完整 `02-14_UPDATE_RESULT` 模板；不得附截图、完整日志、姓名、邮箱、课程名、学校 URL path、Profile/Cookie、本机私有路径或凭据。若任何字段未观察到，必须填 `not_observed`，不能猜 `matched/healthy/ready`。

恢复时逐字段 strict parse 用户反馈，并与 Task 1 exact version/hash 比较。只有全部 pass 条件成立才结束 checkpoint；此时用 strict schema 从 allowlisted feedback 与 02-13 receipts 构造 `.planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json`，固定写入上方 `Phase2MacosUpdateGateReceipt`，`feedbackDigest` 只哈希规范化 allowlisted block，不复制原始聊天、日志或路径；写后立即用 02-13 `verify-phase2-update-gate.mjs --receipt ... --read-only` 重新核对 immutable beta receipt 与当前 loopback runtime/API/static pairing readiness。任何 `failed`、`human_needed`、`cleanup_pending`、mismatch/unhealthy/unavailable/not_observed、配置已确认、school_access 非 not_started、Windows/Phase3 值变化或缺字段都保持 blocking，原样记录脱敏结果码并停止，不创建/保留 gate receipt、不运行 02-15。`UPDATE_TASK_CONTEXT_INVALID` 只说明 updater 错误继承了 controller gate：保持 checkpoint blocking，改用 local projectless task 重试同一 beta.31 prompt，不进入产品修复、不升 beta。若真正需要产品或发布修复，按 `.planning/RELEASE-STABILIZATION.md` 回到 R0/R1 无编号演练并分类失败；完整演练通过后才在 02-38 选择更高 beta.N，再依次执行 02-39→02-13→02-14。绝不覆盖本次 tag/asset，也不将失败改写为通过。</action>
  <how_to_verify>
    1. 用户实际执行屏幕中逐字显示的 `release/phase2-install-prompt.md`，而不是回复“已批准”或仅查看 release 页面。
    2. 用户按步骤核对实际安装、cleanup、actual build、entrypoints、API、Worker、paired UI 与未开始来源访问。
    3. 用户只回复完整 `02-14_UPDATE_RESULT` 脱敏块；exact version 与 artifact_sha256 必须匹配 Task 1，未知项写 not_observed。
    4. executor 不接受自由文本“成功/可以继续”、截图、发布 receipt 或 synthetic selfcheck 作为实际更新证据。
  </how_to_verify>
  <verify>
    <automated>在 checkpoint 暂停前重复 Task 1 的 `verify-phase2-update-gate.mjs --read-only` 并要求 exit 0；恢复后以 strict parser 对 `02-14_UPDATE_RESULT` 执行字段枚举、exact version/hash 与 pass conjunction 校验。用户实际 Codex 更新本身没有可替代的自动命令，因此必须等待真实反馈。</automated>
  </verify>
  <acceptance_criteria>
    1. checkpoint 前展示的 prompt 与 `release/phase2-install-prompt.md` 字节一致，version/tag/macOS URL/bytes/SHA-256/manifest/signature/build 均为 Task 1 exact values，无 placeholder/latest/省略。
    2. 用户明确报告本人在同一主机/账户的 local projectless Codex updater task 中运行该 prompt，并本人处理 OS 授权/重启；AutoED project/worktree task 的 inherited-gate refusal、仅“release 已发布”“下载成功”“approved”或 synthetic selfcheck 不被接受为 update。
    3. feedback 完整匹配 `MacosUpdateFeedback`；version/artifact_sha256 exact match，result=pass、result_code=UPDATE_COMPLETE、cleanup=complete、actual_build/entrypoints=matched、API/Worker=healthy、paired_ui=ready。
    4. source_configuration 恰为 Moodle/EdStem 都 not_confirmed，school_access=not_started；本 checkpoint 未打开学校、未登录、未写任一 source×scenario L receipt。
    5. windows 固定 `not_run/human_needed`、phase3=`blocked`；macOS update pass 只允许 02-15 开始范围确认，不宣称 AUTH-02、Phase 1/2 或共享 Profile 已通过。
    6. strict pass 后 `02-14-UPDATE-RECEIPT.json` 精确满足 `Phase2MacosUpdateGateReceipt`，其 version/tag/hash/build 与 02-13 receipts 相等且 read-only runtime gate exit 0；receipt 无自由文本、路径、identity/course/Profile/Cookie/log/screenshot 字段。
    7. 任何 failed/human_needed/cleanup_pending/mismatch/unhealthy/unavailable/not_observed/缺字段或敏感反馈保持 checkpoint blocking；不自动批准、不创建 gate receipt/pass summary、不执行依赖计划。
  </acceptance_criteria>
  <resume-signal>请只回复完整的 `02-14_UPDATE_RESULT` 块。只有所有 exact pass 条件成立才会恢复；`approved`、自由文本成功说明、发布链接或截图都不会解除门禁。</resume-signal>
  <done>用户已在真实 Codex 中安装并核对 exact Phase 2 beta，cleanup/actual build/entrypoints/services/paired UI 全部匹配；来源配置和学校访问尚未开始，Windows 与 Phase 3 仍阻塞。否则本 task 保持未完成。</done>
</task>

<task type="auto">
  <name>02-14-3：在实际更新后生成 current-build macOS named S/I/N evidence</name>
  <read_first>02-14-UPDATE-RECEIPT.json；02-37/02-39 SUMMARY中的signed native-evidence command/hash；02-35 exact requiredness；D-03/D-12/D-14/D-17–D-18。</read_first>
  <files>.planning/phases/02-poc-live/02-14-MACOS-NATIVE-RECEIPT.json</files>
  <action>仅在Task2真实macOS更新strict pass后，运行signed installed `phase2-native-evidence.mjs --platform macos --runtime current`。要求实际native macOS、exact immutable build/artifact/manifest和`PHASE2_BUILD_OBLIGATIONS`中全部macOS named S/I/N obligations pass；server从signed runtime与实际OS派生automated authority，不能相信CLI payload。随后只读audit确认22个macOS required L cells仍not_run/human_needed，possible nonrequired S/I/N cells不影响required count，Windows未变化且Phase3 blocked。事务性写入allowlisted receipt；partial/failure保留safe failure且不创建pass receipt、不释放02-15。工具不得打开学校/Profile、写L或跨平台证据。</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/phase2-native-evidence.mjs --platform macos --runtime current &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform macos --runtime current --read-only</automated></verify>
  <done>当前实际安装的signed macOS build具备全部named S/I/N证据，22个required L cells仍等待后续真实人工门；仅此时02-15 eligible。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | 不可信输入/风险 | 强制控制 |
|---|---|---|
| GitHub publication/asset → local preflight | tag/asset 可能被覆盖、错账号发布、匿名不可取或部分下载 | 固定 returdex repo id、immutable receipt、asset id/bytes/hash、匿名 full-byte refetch、manifest/signature/closure/build 交叉验证 |
| release receipts → install prompt | prompt 可能引用 latest、不同版本/hash、未签名资产或遗漏失败/清理预期 | prompt hash 绑定 manifest；逐字段等于 artifacts/publication/availability；唯一文件逐字呈现，差异即 blocker |
| displayed prompt → user's actual Codex | agent 可能代执行/改写，用户可能在错误主机/账户或仅查看 release | checkpoint:human-action；exact values；用户本人粘贴/授权；实际安装反馈，publication 不作 update 证据 |
| updater → existing installation/data | 失败迁移、旧 entrypoint/process、cleanup_pending、legacy/Profile/课程资料误操作 | approved prompt 的预览/恢复/数据保留；只更新新受管安装；cleanup complete + actual build/entrypoint/service checks |
| user feedback → next live plan | 自由文本、截图或缺项可能被误当 pass，敏感资料可能进入聊天/ledger | strict MacosUpdateFeedback allowlist、exact version/hash、conjunctive pass gate、禁止截图/log/identity/path、非 pass 保持 blocking |
| macOS update result → evidence/platform gate | 程序更新可能被误写成 source L、Windows pass 或 Phase 2/3 complete | 不 append source L；Windows not_run/human_needed；Phase3 blocked；只解除 02-15 的 macOS版本前置 |

## STRIDE / ASVS L1 威胁登记

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 控制意图 | 具体缓解与硬阻断 |
|---|---|---:|---|---|---|---|
| T2-14-01 | Spoofing / Tampering | Critical | release/tag/assets/manifest/signature | mitigate | V1 架构与供应链边界、V4 授权发布、V8 完整性、V14 安全配置 | 固定 returdex repo id/identity、不可覆盖 tag/asset ids、匿名 full-byte hash、签名/closure/build 复验。任一 mismatch、覆盖、匿名不可取或 wrong owner 即 Critical blocker，禁止显示 update prompt。 |
| T2-14-02 | Tampering / Elevation of Privilege | Critical | exact install prompt/updater | mitigate | V5 输入验证、V8 软件完整性、V13 业务流程、V14 配置 | prompt hash 绑定 manifest且逐字段交叉；无 latest/placeholder；仅受管安装；迁移恢复与 cleanup complete。prompt 差异、legacy/Profile/data 越界或 cleanup_pending 即 Critical blocker。 |
| T2-14-03 | Spoofing / Repudiation | High | human checkpoint/actual installed state | mitigate | V2 人工认证动作、V4 动作授权、V13 状态机、V16 审计 | 用户本人 Codex 更新；strict feedback；publish/download/synthetic ≠ installed；actual build/entrypoints/services/UI 全 conjunctive。agent 自批、自由文本或 not_observed 即 High blocker。 |
| T2-02 | Information Disclosure | High | prompt、反馈、summary/chat | mitigate | V8 数据最小化、V16 安全日志/错误 | 只允许公开 release identity+脱敏 code；禁止 identity/course/Profile/Cookie/path/raw log/screenshot/credential。任一敏感值出现即 blocker并要求重新脱敏反馈。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | High | receipt/Windows/Phase gate | mitigate | V1 证据分级、V4 authority、V13 gate、V16 provenance | update feedback不写source L；Windows固定not_run；Phase3 blocked；失败不可覆盖/改pass。任一 macOS update→Windows/L/Phase pass 即 High blocker。 |

ASVS 5.0 L1 仅作为本计划适用控制意图，不声称完整合规。所有 Critical/High 项必须 `mitigate`；任一 blocker 都保持 checkpoint 未完成，不得 accept/transfer、自动批准或留债后继续。
</threat_model>

<verification>
1. 02-13 SUMMARY 与五项 handoff 必须存在且 strict parse；缺失即停止，不能从旧 Phase 1 beta 或 GitHub latest 猜值。
2. Task 1 在临时目录重新匿名 full-byte fetch/verify，并以 read-only gate script 检查 tests/artifacts/publication/availability/prompt；仓库 receipt hashes 前后不变。
3. Task 2 只能在 Task 1 全 pass 后显示；显示 exact prompt 全文与 exact values，然后硬暂停，等待用户本人实际 Codex 更新。
4. 恢复只 strict parse 完整 `02-14_UPDATE_RESULT`；pass 为所有字段 conjunction，不能按一个 `result=pass`、截图、自由文本或 release receipt 判定。
5. privacy scan：prompt/feedback/SUMMARY 不含姓名、邮箱、课程、学校 path、Profile/Cookie、本机私有路径、credentials、raw logs 或 screenshot；公开 version/hash/build 可保留。
6. strict pass 后以 `verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json --read-only` 核对 receipt↔immutable beta↔当前 loopback runtime/paired shell；非 pass 时 receipt 必须不存在。
7. evidence scan：本计划不调用 EvidenceLedger human_action append，不写 Moodle/EdStem L/N/Windows cell；Windows not_run/human_needed、Phase3 blocked。
8. failed/human_needed/cleanup_pending/mismatch/not_observed 均停在 02-14；`UPDATE_TASK_CONTEXT_INVALID` 只纠正为 local projectless updater 并重试同一 prompt，不升 beta。若需真正产品或发布修复，先按 `.planning/RELEASE-STABILIZATION.md` 完成 R0/R1 无编号演练和失败分类，演练通过后才选择更高 beta.N 并重走 02-38→02-39→02-13→02-14；绝不覆盖发布物或继续 02-15。
</verification>

<success_criteria>
- UAT-01：人工场景前已有 exact、匿名可获取、签名/hash/closure 验证的 immutable beta 和逐字 update prompt；用户实际更新后才可能通过版本门。
- AUTH-02/D-12：macOS 后续 A/B/C/D/reauth 都将在同一实际安装 beta 上开始，但本计划本身不填写这些 source live cells，也不执行登录/重启/跨日。
- D-14：用户反馈为固定脱敏 update block；没有身份、课程、Profile、Cookie、私有路径、原始日志或截图。
- 发布、匿名下载、自检候选与用户设备 actual update 明确分开；actual version/hash/build/entrypoints/API/Worker/paired UI 和 cleanup 必须全部通过。
- 失败、human_needed、cleanup_pending 或任何 mismatch/not_observed 不会被自动批准，不会产生 pass summary，不会进入 02-15。
- paired UI ready 且两来源 config not_confirmed/school access not_started，证明下一步仍是人工范围确认而不是自动学校访问。
- Windows 保持 not_run/human_needed，Phase 1/2 不整体通过，Phase 3 继续 blocked；macOS-first 仅是执行顺序例外。
- threat model 中所有 High/Critical blocker 均未触发；若触发，本计划保持 blocking 而非降级。
</success_criteria>

<output>
仅在 checkpoint 的 strict pass conjunction 成立、`02-14-UPDATE-RECEIPT.json` 写入并通过 read-only gate 后创建 `.planning/phases/02-poc-live/02-14-SUMMARY.md`，记录公开 exact version/tag/asset SHA-256/build、脱敏 update result code、cleanup/build/entrypoint/service/UI 状态、source_configuration=not_confirmed、school_access=not_started、Windows/Phase3 缺口和 observed_at。任何非 pass 保持 checkpoint 未完成，gate receipt 必须不存在且不创建成功 SUMMARY；不得记录 prompt 中的本机路径、身份/course、Profile/Cookie、raw log、截图或凭据。
</output>
