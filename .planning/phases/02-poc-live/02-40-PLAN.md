---
phase: 02-poc-live
plan: "40"
type: execute
wave: 26
depends_on: ["02-25"]
files_modified:
  - .planning/phases/02-poc-live/02-40-WINDOWS-NATIVE-RECEIPT.json
autonomous: false
requirements: [AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "Only after the actual 02-25 native Windows update, the signed installed native-evidence command verifies the exact current build and actual Windows 11 environment."
    - "Every exact named build obligation assigned to Windows S/I/N passes before the first Windows L checkpoint; nonrequired possible cells remain not_run without blocking."
    - "The producer cannot write L, substitute macOS/Linux/WSL/static artifact evidence, select arbitrary scenarios, or promote Phase 2/3."
    - "Failure is retained append-only and blocks 02-26; a pass receipt is written only from the current server's redacted verified result."
  artifacts:
    - path: .planning/phases/02-poc-live/02-40-WINDOWS-NATIVE-RECEIPT.json
      provides: "Current signed-build native Windows S/I/N verification receipt"
  key_links:
    - from: scripts/release/phase2-native-evidence.mjs
      to: packages/domain/src/live-evidence.ts
      via: "signed installed command consumes exact named Windows automated obligations"
      pattern: "requiredEvidenceKeys|signed_automated"
    - from: .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
      to: .planning/phases/02-poc-live/02-40-WINDOWS-NATIVE-RECEIPT.json
      via: "exact version/build/artifact/platform equality"
      pattern: "version|buildId|platform|sha256"
---

<objective>
Produce and verify the final signed-build Windows S/I/N evidence before any Windows live L gate.

Purpose: Keep native automated evidence separate from live human evidence and make the future Windows branch reachable without inference.
Output: A redacted current-build Windows native-evidence receipt or a hard human/technical blocker. No login or school access occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
@release/phase2-beta-artifacts.json
@scripts/release/phase2-native-evidence.mjs
@scripts/release/phase2-live-gate.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run signed current-build Windows S/I/N production and verification</name>
  <files>.planning/phases/02-poc-live/02-40-WINDOWS-NATIVE-RECEIPT.json</files>
  <action>First verify 02-24 resume and 02-25 update receipts, exact immutable beta identity, actual native Windows 11/non-WSL status and paired API/Worker/UI readiness. Run only the signed installed `phase2-native-evidence.mjs --platform windows --runtime current`; it discovers the managed runtime through its fixed inventory and asks the paired server to derive signed_automated authority. Require every Windows S/I/N item in `PHASE2_BUILD_OBLIGATIONS` for this build to pass, with no missing/duplicate/unrequired obligation and no L event. Then run the signed live-gate Windows audit and require `{possibleCellCount:88,requiredCellCount:22,passCellCount:0,notRunCellCount:22,humanNeededCellCount:22}`: all 22 required L cells remain not_run/human_needed, possible nonrequired S/I/N cells do not block, and Phase 3 remains blocked. Atomically create the allowlisted receipt only from those verified redacted results. On any mismatch or partial failure, retain the server failure event and prior success, do not create a pass receipt, and keep 02-26 blocked.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/phase2-native-evidence.mjs --platform windows --runtime current &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only</automated></verify>
  <done>Current native Windows named S/I/N obligations pass for the signed installed build while all 22 required Windows L cells remain unclaimed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Resolve unavoidable Windows OS approval if requested</name>
  <files>.planning/phases/02-poc-live/02-40-WINDOWS-NATIVE-RECEIPT.json</files>
  <what-built>The signed bounded native-evidence command is ready and has either completed or stopped at an actual Windows OS approval prompt; it never requests school credentials or browser login.</what-built>
  <how-to-verify>
    1. If Windows presents an OS security/firewall/executable approval for the exact signed AutoED beta, approve only that named managed executable.
    2. Do not enter Moodle/EdStem credentials, open a source login, disable security controls, use WSL/Linux, or supply paths/ports/selectors.
    3. Return `02-40_WINDOWS_OS_APPROVAL=approved` after the real prompt, or `02-40_WINDOWS_OS_APPROVAL=blocked` with the safe visible error code.
  </how-to-verify>
  <resume-signal>Return the exact allowlisted approval line; silence, free text, planning approval or macOS evidence does not resume.</resume-signal>
  <action>Use this checkpoint only if the signed command actually reports `human_needed` for an unavoidable native Windows OS approval. After approval, rerun Task 1 from the beginning and create the receipt only on strict pass. A blocked/failed response remains a hard gate and does not release 02-26.</action>
  <verify><automated>After an approved response, rerun Task 1's two exact commands and require exit 0; otherwise assert `02-40-WINDOWS-NATIVE-RECEIPT.json` is absent and 02-26 remains blocked.</automated></verify>
  <done>The native evidence either passes after a real required OS approval or remains honestly blocked with no live/evidence promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| installed runtime/OS → native authority | Platform/build claims are untrusted until signed runtime and actual OS attest them. |
| native checks → EvidenceLedger | Automated authority must be limited to exact Windows S/I/N obligations. |
| user OS approval → continuation | Human feedback may acknowledge an OS prompt but cannot create evidence or choose scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-40-01 | S/T | Windows platform/build | mitigate | Exact update receipt, signed manifest and actual native Windows/non-WSL checks. |
| T2-40-02 | T/E | automated evidence authority | mitigate | Server-derived signed_automated authority constrained by requiredness registry to S/I/N. |
| T2-40-03 | R | partial/failure results | mitigate | Transactional pass bundle, append-only failure retention and no pass receipt on partial result. |
| T2-40-04 | I | receipt/output | mitigate | Strict allowlist excludes device, account, Profile, path, PID, source and credential data. |
</threat_model>

<verification>
Verify exact current identity/platform and registry obligations, then audit that L remains absent and Phase 3 blocked. Never substitute fixture or another platform evidence.
</verification>

<success_criteria>
- Native Windows S/I/N evidence is current-build and exact named-obligation-registry bound.
- Required Windows L remains not_run/human_needed before 02-26.
- Failure or missing OS approval blocks progression without deleting prior evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-40-SUMMARY.md` with only public build/platform, required S/I/N counts, safe result codes and remaining L gaps.
</output>
