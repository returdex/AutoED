---
phase: 02-poc-live
plan: "41"
type: execute
wave: 10
depends_on: ["02-37"]
files_modified:
  - scripts/release/phase2-gate.mjs
  - scripts/build/assemble.mjs
  - scripts/release/preflight.mjs
  - scripts/release/publish.mjs
  - scripts/release/verify-availability.mjs
  - tests/integration/phase2-release-gates.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "All quality, assembly, signing, publication, availability and update-handoff tooling changes are complete and tested before one beta identity is selected."
    - "The tooling requires the durable live workflows, complete live/audit/final/update gates and native-evidence command in both signed target archives."
    - "No later release-chain plan edits source or tests; 02-38→02-39→02-13 only creates build-bound receipts/artifacts/publication records."
  artifacts:
    - path: scripts/release/phase2-gate.mjs
      provides: "Immutable identity and full-suite quality gate"
    - path: scripts/build/assemble.mjs
      provides: "Dual-target capability-closed signed assembly"
    - path: scripts/release/publish.mjs
      provides: "No-overwrite immutable publication"
    - path: scripts/release/verify-availability.mjs
      provides: "Anonymous independent asset verification"
    - path: tests/integration/phase2-release-gates.test.ts
      provides: "Complete release-chain positive/negative contract tests"
  key_links:
    - from: scripts/release/phase2-gate.mjs
      to: scripts/build/assemble.mjs
      via: "exact selection/test-report schema consumed without source drift"
      pattern: "version|commit|tree|buildId|testReportSha256"
    - from: scripts/build/assemble.mjs
      to: scripts/release/publish.mjs
      via: "immutable signed artifact receipt only"
      pattern: "sha256|signature|fingerprint|immutable"
---

<objective>
Finalize and test the complete release toolchain before selecting the immutable Phase 2 beta identity.

Purpose: Ensure build identity remains unchanged through quality binding, assembly, signing, publication and availability verification.
Output: Source-complete release tooling and negative tests. No identity selection, signing, publication, update or live work occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-37-SUMMARY.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@scripts/release/phase2-live-gate.mjs
@scripts/release/phase2-native-evidence.mjs
@scripts/release/verify-phase2-update-gate.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize quality, assembly and signed capability closure</name>
  <files>scripts/release/phase2-gate.mjs, scripts/build/assemble.mjs, scripts/release/preflight.mjs, tests/integration/phase2-release-gates.test.ts</files>
  <behavior>
    - Quality gate requires all pinned suites, scans and every live/native/update branch test on one clean identity.
    - Both archive manifests require identical durable-workflow and gate member hashes/capabilities.
    - Missing/wrong member, identity drift, unsigned/alternate key, dependency/license/support mismatch or sensitive member fails before artifact receipt.
  </behavior>
  <action>Follow every Plan 41 file mapping in `02-PATTERNS.md`: `phase2-gate.mjs` reuses preflight’s closed argv/exact identity mechanics; assemble/preflight/publish/availability remain exact modifications of their existing contracts; release tests follow `release-gates.test.ts`. Complete all source changes needed by 02-38/39. Extend quality and assembly contracts for exact requiredness, durable paired-server workflows, all canonical live/audit/final/update branches, native S/I/N command, dual target closure, approved Ed25519 trust root, dependency/license/support checks and sensitive scans. Tests use fixtures only and cover missing/stale/mismatched capabilities, source/tree drift, alternate keys and private-member leaks. Generated build selection/report/artifact/prompt remain absent in this plan.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "quality gate|capability closure|signed assembly|source drift" &amp;&amp; npm run typecheck</automated></verify>
  <done>Quality and assembly tooling is source-complete before identity selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize immutable publication and anonymous availability tooling</name>
  <files>scripts/release/publish.mjs, scripts/release/verify-availability.mjs, scripts/release/preflight.mjs, tests/integration/phase2-release-gates.test.ts</files>
  <behavior>
    - Publish accepts only one selected signed receipt and rejects existing/conflicting tag/asset/receipt without overwrite or fallback.
    - Anonymous verification downloads complete bytes into a clean temporary directory and checks metadata, hashes, signatures, fingerprint and capability closure independently.
    - Any race/drift leaves prior remote objects untouched and requires a new identity/full rerun.
  </behavior>
  <action>Follow the exact publish/availability/preflight mappings and excerpts in `02-PATTERNS.md`, including atomic no-replace local receipts and anonymous full-byte/hash/signature/member verification. Complete immutable publish, fresh anonymous refetch and cross-platform update-handoff preflight implementations. Preserve fixed returdex repository identity, SemVer beta selection, PolyForm license and no-force/no-delete rules. Add negative tests for version races, higher partial beta, redirect/repository substitution, partial/authenticated fetch, hash/signature/capability/prompt mismatch and unsafe output. No actual remote mutation occurs in these tests.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "immutable publication|anonymous availability|update handoff|no overwrite"</automated></verify>
  <done>Publication/availability tooling is complete; subsequent release plans only execute it against the selected identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/tooling → selected identity | Any later source edit would invalidate the selected commit/tree/build. |
| signed artifacts → remote release | Publication must not replace or mix immutable objects. |
| anonymous network → availability | Downloaded bytes/metadata are untrusted until independent verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-41-01 | T/R | release source closure | mitigate | Finish all source/tests first; later plans generate outputs only and enforce clean-tree identity. |
| T2-41-02 | S/T | signing/publication | mitigate | Fixed key/repository identity, exact receipt chain and no overwrite/force/delete paths. |
| T2-41-03 | T/R | anonymous verification | mitigate | Fresh full-byte fetch, redirect allowlist, independent hashes/signatures/capability closure. |
| T2-41-04 | I | artifacts/output | mitigate | Member/output allowlists and sensitive sentinel scans. |
</threat_model>

<verification>
Run targeted release-toolchain tests and typecheck. Confirm no generated release receipt, artifact, tag, asset, update or evidence mutation was produced.
</verification>

<success_criteria>
- All release-chain source/test edits precede 02-38 identity selection.
- Later release plans create outputs only and cannot drift the selected source tree.
- Immutable/no-overwrite and anonymous verification negatives pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-41-SUMMARY.md` listing tooling capability/test coverage and zero external mutations.
</output>
