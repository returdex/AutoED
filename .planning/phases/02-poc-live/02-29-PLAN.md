---
phase: 02-poc-live
plan: 29
type: execute
wave: 25
depends_on: [02-28]
files_modified: []
autonomous: false
requirements: [AUTH-02, AUTH-04, UAT-01]
must_haves:
  truths:
    - "只有 Windows B1 三轮的 Moodle/EdStem current-build native Windows/L exact cells 与 completed 02-28 SUMMARY 全部通过，B2 固定动作才可见；SUMMARY、macOS、S/I/N 或部分轮次不能替代。"
    - "用户本人只在原生 Windows 11 current beta paired protected UI 点击一次固定的 managed Worker restart；执行器不得用 shell、PID、进程名、服务控制器或自写命令代操作。"
    - "restart boundary 建立后，旧 Worker generation 在每次来源请求前与提交前都被 fence；旧 generation 的 post-boundary request 与 commit 计数必须同时为零，迟到结果不得落库。"
    - "新 Worker 只有在同 native Windows installation、current build/entrypoint 与 ownership proof 成立后才成为 current generation，并继续持有同一个专属 Profile，而不是复制/新建 Profile 或接管未知进程。"
    - "新 Worker 按 Moodle→EdStem 固定顺序恢复双来源最小只读状态；完整身份仅在 protected UI 由用户核对，binding/scope/Profile一致且不读取课程正文、帖子、文件或成绩。"
    - "Moodle 与 EdStem 分别 append exact `windows/B2_worker_restart/L` immutable event；L 只能由 paired server 的一次性 human action 与 actual restart/source result 共同写入。"
    - "B2 不创建 planning receipt；canonical read-only verifier 返回 `WINDOWS_B2_WORKER_RESTART_COMPLETE` 且真实用户反馈存在后才生成 SUMMARY。任一失败立即阻断 Windows B3，macOS 证据保持 unchanged，Phase 3 仍 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-28-SUMMARY.md"
      provides: "Windows B1 三轮真实用户反馈与 B2 eligibility；不能替代六个 runtime exact cells"
      contains: "B2"
    - path: "packages/application/src/auth-jobs.ts"
      provides: "managed Worker lease/generation/fence、before-request/before-commit current assertion 与 recovery"
      contains: "AuthJobRunner"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger 与 per-source Windows B2_worker_restart/L cells"
      contains: "EvidenceLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows B1/B2 exact-cell current-runtime只读 gate；无 ledger writer 或 planning receipt"
      contains: "verifyPhase2LiveCheckpoint"
    - path: "apps/status/src/main.ts"
      provides: "paired protected UI 的固定 B2 action、ownership/fence/source recovery 脱敏状态与 strict feedback"
      contains: "b_worker_restart"
    - path: ".planning/phases/02-poc-live/02-29-SUMMARY.md"
      provides: "仅 strict pass 后产生的 B3 人工前置；B3仍须 fresh复验runtime exact cells"
  key_links:
    - from: ".planning/phases/02-poc-live/02-28-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "分别复验 Windows B1 round 1/2/3 exact cells；SUMMARY只证明真实反馈"
      pattern: "--checkpoint B1|--round|--platform windows"
    - from: "apps/status/src/main.ts"
      to: "managed Worker lifecycle"
      via: "paired human B2 fixed action绑定current installation/build/generation；不暴露通用进程控制"
      pattern: "B2|worker|restart"
    - from: "managed Worker lifecycle"
      to: "packages/application/src/auth-jobs.ts"
      via: "持久化restart boundary，fence旧generation，再认证new current generation；request/commit双边界均assertCurrent"
      pattern: "assertCurrent|generation|fence"
    - from: "paired server B2 workflow"
      to: "packages/persistence/src/auth.ts"
      via: "server-only one-time Windows human_action authority + actual restart/source results 分别append exact cells"
      pattern: "human_action|windows|B2_worker_restart|L"
    - from: "Windows B2_worker_restart exact cells"
      to: "02-30 B3 eligibility"
      via: "canonical read-only B2 verifier + completed 02-29 SUMMARY；无planning receipt"
      pattern: "WINDOWS_B2_WORKER_RESTART_COMPLETE|--checkpoint B2"
---

<objective>
为未来原生 Windows 11 执行不可自动跨越的 B2 managed Worker restart gate：用户从 current beta paired protected UI 重启本安装实际受管 Worker，产品证明旧 generation 在 restart boundary 后不能再发出来源请求或提交结果，并由新的 current Worker 使用同一个专属 Profile 恢复 Moodle 与 EdStem 的身份、binding、approved scope 与最小状态。

Purpose: 以真实 native Windows Worker 生命周期验证 AUTH-04 ownership/fence 与 durable recovery，而不是用 PID变化、测试 harness、macOS结果或单来源恢复推断；以两个独立 Windows/L events和真实用户反馈满足AUTH-02/UAT-01。

Output: runtime EvidenceLedger内Moodle/EdStem两个`windows/B2_worker_restart/L` append-only events、canonical read-only verifier结果和仅final pass时创建的02-29 SUMMARY。无B2 planning receipt，不执行B3/C/D/reauth，不改变macOS cells或Phase3阻塞。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-03-PLAN.md
@.planning/phases/02-poc-live/02-07-PLAN.md
@.planning/phases/02-poc-live/02-10-PLAN.md
@.planning/phases/02-poc-live/02-19-PLAN.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@.planning/phases/02-poc-live/02-27-SUMMARY.md
@.planning/phases/02-poc-live/02-28-PLAN.md
@.planning/phases/02-poc-live/02-28-SUMMARY.md

<interfaces>
<!-- 02-29只消费已发布接口；live执行时不得patch current beta、增加route或建立第二套证据。 -->

Windows update与A2 prerequisite（均须fresh只读复验）：
```bash
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json \
  --artifacts release/phase2-beta-artifacts.json \
  --availability release/phase2-availability.json \
  --runtime current --platform windows --read-only
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint A2 --runtime current --platform windows --read-only
```
第一条必须返回`WINDOWS_UPDATE_COMPLETE`并证明same native Windows 11 non-WSL current build；第二条必须返回`WINDOWS_A2_COMPLETE`并证明四个current non-corrected Windows/L `A2_binding|A2_course_visibility` cells、binding/scope/course visibility/content boundary均通过。completed 02-25/02-27 SUMMARY只证明真实反馈，不能替代gate。

Windows B1 prerequisite（02-28 source-of-truth为runtime EvidenceLedger，completed SUMMARY不替代它）：
```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B1 --round 1 --runtime current --platform windows --read-only
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B1 --round 2 --runtime current --platform windows --read-only
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B1 --round 3 --runtime current --platform windows --read-only
```
每轮必须读取Moodle/EdStem各一个current、non-corrected、same native Windows version/build的`B1_reopen_<n>/L` pass cell；round1/2/3 strict code分别为`WINDOWS_B1_REOPEN_1_COMPLETE`、`WINDOWS_B1_REOPEN_2_COMPLETE`、`WINDOWS_B1_REOPEN_3_COMPLETE`，且输出均包含same_dedicated、actualExit confirmed、ownership current、identityScope consistent、minimalRead pass、contentBoundary preserved、macOS unchanged、Phase3 blocked。round不能合并、补跳或由macOS/S/I/N/fixture代填。只有fresh Windows update/A2 gates、三条B1命令均exit0且`.planning/phases/02-poc-live/02-28-SUMMARY.md` completed才eligible。

来自既有application/persistence contracts：
```typescript
export interface AuthJobRunner {
  runOnce(owner: ProfileOwnerIdentity, context: WriteContext): Promise<void>;
}

export interface AuthJobStore {
  assertCurrent(jobId: string, owner: ProfileOwnerIdentity, context: WriteContext): Promise<void>;
  heartbeat(jobId: string, owner: ProfileOwnerIdentity, context: WriteContext): Promise<void>;
  commitTransition(jobId: string, owner: ProfileOwnerIdentity, transition: AuthFlowState, context: WriteContext): Promise<void>;
  recoverExpired(now: string, context: WriteContext): Promise<readonly AuthJob[]>;
}

export type EvidenceWriterAuthority =
  | {kind:'automated'; evidence:'S'|'I'|'N'; platform:'macos'|'windows'; producerId:string}
  | {kind:'human_action'; evidence:'L'; platform:'macos'|'windows'; actionReceiptId:string};
```

每来源Windows B2 runtime cell必须strict满足：
```typescript
export interface WindowsB2WorkerRestartCell {
  schema: 1;
  platform: 'windows';
  evidence: 'L';
  scenario: 'B2_worker_restart';
  source: 'moodle' | 'edstem';
  version: string;
  buildId: string;
  result: 'pass' | 'failed' | 'human_needed';
  resultCode:
    | 'B2_WORKER_RESTARTED'
    | 'B2_PRECONDITION_FAILED'
    | 'B2_WORKER_EXIT_UNCONFIRMED'
    | 'B2_WORKER_START_FAILED'
    | 'B2_OLD_GENERATION_REQUESTED'
    | 'B2_OLD_GENERATION_COMMITTED'
    | 'B2_NEW_GENERATION_UNCONFIRMED'
    | 'B2_WORKER_OWNER_MISMATCH'
    | 'B2_PROFILE_MISMATCH'
    | 'B2_SOURCE_RECOVERY_FAILED'
    | 'IDENTITY_MISMATCH'
    | 'RESULT_NOT_OBSERVED';
  workerRestart: 'completed'|'failed'|'human_needed';
  oldGeneration: 'fenced'|'request_observed'|'commit_observed'|'not_observed';
  oldRequestsAfterBoundary: 0 | 1;
  oldCommitsAfterBoundary: 0 | 1;
  newGeneration: 'current'|'mismatch'|'not_observed';
  workerOwner: 'same_installation'|'mismatch'|'not_observed';
  profile: 'same_dedicated'|'mismatch'|'not_observed';
  bindingConsistency: 'consistent'|'mismatch'|'not_observed';
  identityScope: 'consistent'|'mismatch'|'not_observed';
  minimalRead: 'pass'|'failed'|'not_observed';
  contentBoundary: 'preserved'|'violated'|'not_observed';
  observedAt: string;
}
```

每个source event的唯一L authority由paired server在用户点击B2时创建，绑定`checkpoint=B2,platform=windows,source,scenario=B2_worker_restart,current version/build,old/new generation boundary,installation/Worker owner proof,Profile owner digest,paired session,approved config/scope`。actual restart与source result同一次性consume/append；actionReceiptId、PID、raw generation/fence/owner/Profile path不离开server。failed/human_needed同样append immutable结果并阻断；任何新尝试必须是新的human action与linked correction，不能删除/覆盖失败。

Canonical Windows B2 machine gate：
```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B2 --runtime current --platform windows --read-only
```
唯一pass projection：
```typescript
{
  schema:1; status:'pass'; checkpoint:'B2'; platform:'windows';
  nativePlatform:{os:'windows-11';kind:'native';wsl:false};
  version:string; buildId:string; scenario:'B2_worker_restart';
  resultCode:'WINDOWS_B2_WORKER_RESTART_COMPLETE'; sources:['moodle','edstem'];
  worker:{restart:'completed';oldGeneration:'fenced';oldRequestsAfterBoundary:0;oldCommitsAfterBoundary:0;newGeneration:'current';owner:'same_installation';profile:'same_dedicated'};
  identityScope:'consistent'; bindingConsistency:'consistent'; minimalRead:'pass'; contentBoundary:'preserved';
  macosEvidence:'unchanged'; windowsNext:'B3_pending_human_action'; phase3:'blocked';
}
```
verifier只经signed local-client discovery读取redacted current status和两个exact cells；不直接读DB/WAL、append/correct ledger、控制进程、打开Profile/browser、请求学校或创建planning receipt。SUMMARY只证明真实用户feedback，不是machine authority。

02-30 B3 handoff contract：只有上述B2 verifier fresh exit0且`02-29-SUMMARY.md` completed才eligible；B3 cells与pending correlation起始必须empty/not_run。B3唯一read-only machine gate为：
```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B3 --runtime current --platform windows --read-only
```
其唯一pass code固定`WINDOWS_B3_CODEX_EXIT_COMPLETE`，并要求`codexExit='observed'`, `backend='survived'`, `worker='survived_or_recovered_current'`, `absentStatusRead='pass'`, `reentry='actual_client'`, `clientBuild='matched'`, `sourceState='consistent'`, `bindingConsistency='consistent'`, `minimalRead='pass'`, `contentBoundary='preserved'`, `selfcheck='not_used'`, macOS unchanged、Phase3 blocked。

B3 declarative next action不是authority：02-29完成时只允许`windowsNext='B3_pending_human_action'`，不得预mint/consume B3 actionReceiptId或写B3 cells。02-30由用户在paired UI独立发起后，server才mint绑定Windows/current build/pre-exit actual-host instance/source的durable correlations；禁止复用B2 correlation。退出期间的exact status-only命令与完整退出/重进步骤只能取自current immutable beta signed manifest中与`release/phase2-install-prompt.md`同version/build/hash绑定的已发布Windows B3 instructions及paired protected UI呈现；缺失时human_needed，不能现场发明命令、用selfcheck/spawn/源码脚本代替。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 只读复验 Windows B1 三轮并封闭 B2 restart 边界</name>
  <read_first>AGENTS.md的native Windows、only-owned process、Profile、live evidence与失败阻塞规则；02-CONTEXT D-03、D-09、D-12、D-14、D-17–D-18；02-VALIDATION ownership/evidence rows；02-28 PLAN/SUMMARY与B1 round exact verifier；current beta的Worker/job/ownership/status/live-gate exports。不得读取实际DB、Profile、PID、进程参数、full identity或课程。</read_first>
  <files>none（只读preflight；不得restart Worker、修改runtime/ledger/Profile/config、创建receipt/SUMMARY或访问学校）</files>
  <action>先逐字运行`<interfaces>`的02-25 Windows update gate与02-27 A2 gate，分别要求`WINDOWS_UPDATE_COMPLETE`、`WINDOWS_A2_COMPLETE`且same native Windows current build/session/config/scope/Profile；再strict读取02-28 SUMMARY，要求B1-1/B1-2/B1-3×Moodle/EdStem均pass、B2 eligible、macOS unchanged、Phase3 blocked。最后运行三条B1 canonical read-only verifier，分别要求`WINDOWS_B1_REOPEN_1_COMPLETE|2_COMPLETE|3_COMPLETE`并确认六个runtime exact Windows/L cells仍current、non-corrected、same_dedicated/actualExit/ownership/identity/minimal/content均pass，无source/scenario/platform/build污染。SUMMARY与runtime任一不一致即停止。

对current immutable beta执行只读能力检查：paired UI必须有独立Windows B2 row与唯一fixed action“重启本安装受管Worker”，明确不重启API、browser/Profile、Codex或Windows。UI可显示old fenced/request0/commit0/new current/same installation+Profile/two-source recovery的脱敏枚举，但不得暴露PID、raw generation/fence、owner、executable或Profile path。不存在通用process command/PID输入/public restart route/跳过/bulk receipt write。

读取动作前redacted baseline，只保留current build、native Windows proof、Worker healthy/current、API healthy、Profile same_dedicated/owned、两来源authenticated/binding consistent、B2 cells not_run、macOS unchanged；确认B2/B3/C cells与pending correlations均空、无in-flight source action/maintenance/update。运行现有unit/integration/native fence tests，证明only-owned Worker restart、before-request与before-commit双重`assertCurrent`、late result drop、new generation recovery不重置attempt、Profile不重开/复制、旧worker未知退出fail closed。测试仅使用synthetic authority且绝不写L。若已发布beta缺任何capability/gate，返回human_needed并走修复→全测试→新immutable beta→实际Windows update→重做A/B1；不得在live时patch或绕过。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A2 --runtime current --platform windows --read-only &amp;&amp; for round in 1 2 3; do node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round "$round" --runtime current --platform windows --read-only || exit 1; done &amp;&amp; node scripts/dev/runtime.mjs pnpm exec vitest run tests/unit tests/integration --testNamePattern='Worker|generation|fence|stale|B2|Windows' &amp;&amp; node scripts/dev/runtime.mjs pnpm run test:native -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    1. fresh02-25 update gate=`WINDOWS_UPDATE_COMPLETE`、02-27 A2 gate=`WINDOWS_A2_COMPLETE`、completed02-28 SUMMARY与B1 round1/2/3 exact codes共同证明same current native Windows build及六个Windows/L pass；任一missing/non-pass/correction/drift/污染阻止Task2。
    2. B2 action仅paired protected UI可见且为fixed managed Worker restart；无PID/command/public route/skip/general process control。
    3. baseline显示current native Windows build、API/Worker healthy、same dedicated owned Profile、两来源一致；B2/B3/C events与pending correlations为空，macOS unchanged。
    4. 自动测试证明old generation在request/commit两边界均被fence且late result丢弃；只回收本installation确知owned Worker，未知/仍运行进程fail closed。
    5. preflight不restart/kill/spawn Worker、不打开Profile/browser、不访问学校、不写ledger/L/SUMMARY/receipt；输出不含identity/course/origin/path/PID/actionID/raw data。
    6. published beta缺少server-only B2 correlation、owned lifecycle、exact writer/verifier或Windows UI步骤时保持human_needed，不现场加route/script/DB write。
  </acceptance_criteria>
  <done>Windows B1六个runtime cells与真实SUMMARY已复验，current beta具备安全B2能力，动作前B2/B3/C均为空且无副作用；否则不会展示人工restart步骤。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 用户重启原生 Windows 受管 Worker 并核对旧代际隔离与双来源恢复</name>
  <read_first>Task1 strict pass/baseline；02-28 completed SUMMARY与B1 exact cells；current signed beta paired UI中的B2 released instructions；既有ProfileOwnership/AuthJobRunner/EvidenceLedger/ProtectedAuthStatusProjection contracts；本plan的B2 cell/verifier/pass projection。</read_first>
  <files>none（真实动作仅改变仓库外managed Worker/runtime与既有append-only EvidenceLedger；pass后由execute-plan创建02-29-SUMMARY.md；不创建B2 planning receipt、不修改源码/Profile/学校资料）</files>
  <what-built>current beta已提供paired-only固定B2动作、受管Worker ownership/restart boundary、旧generation request/commit双fence、新Worker recovery与两个source-specific Windows/L结果；repo gate只能只读复验。</what-built>
  <action>Task1完成后硬暂停，逐字呈现`<how-to-verify>`与`<resume-signal>`；agent不得代替用户点击restart、运行shell/service manager/Task Manager、kill/spawn进程或自动批准。用户点击B2动作时，paired server必须先重新核对current build、B1三轮exact pass、Profile ownership与source state，再为Moodle/EdStem建立本轮server-only one-time correlation，并在进程变化前持久化restart boundary。actionID不得离开server。

产品只请求本installation inventory中可证明owned的actual managed Worker优雅退出；API、browser/Profile、Codex、Windows与不相关进程保持不变。未确认退出时立即failed/human_needed，绝不按PID/name强杀或把lease expiry当退出。boundary后old generation/fence在任何新source request前和commit前均失败，迟到结果丢弃；server只记录脱敏`fenced/request0/commit0`，不输出raw值。新Worker必须由approved managed entrypoint启动并以same installation/current build/owner proof注册新generation，才可成为current；不得接管陌生进程或创建/复制Profile。

新Worker成为current后，使用same dedicated Profile按Moodle→EdStem fixed bounded read-only minimal checks恢复；每步前后检查ownership/generation/cancellation。用户在protected UI核对两来源完整身份仍正确、binding/scope一致、Profile same_dedicated，并确认只读了auth/minimal visibility状态而无body/posts/files/grades。任何identity/profile/owner/build mismatch、source失败、旧request/commit、敏感输出立即停止。

paired server只在actual restart/result与本轮human action共同成立时，用payload外`{kind:'human_action',evidence:'L',platform:'windows',actionReceiptId}`为两个source分别append immutable`B2_worker_restart`event；failed/human_needed也保留并消费correlation。用户只返回paired UI生成的exact block：
```text
02-29_WINDOWS_B2_RESULT
checkpoint=B2
platform=windows
native_os=windows-11
version=<public version>
build_id=<public build id>
result=pass|failed|human_needed
result_code=WINDOWS_B2_WORKER_RESTART_COMPLETE|B2_PRECONDITION_FAILED|B2_WORKER_EXIT_UNCONFIRMED|B2_WORKER_START_FAILED|B2_OLD_GENERATION_REQUESTED|B2_OLD_GENERATION_COMMITTED|B2_NEW_GENERATION_UNCONFIRMED|B2_WORKER_OWNER_MISMATCH|B2_PROFILE_MISMATCH|B2_SOURCE_RECOVERY_FAILED|IDENTITY_MISMATCH|RESULT_NOT_OBSERVED
worker_restart=completed|failed|human_needed
old_generation=fenced|request_observed|commit_observed|not_observed
old_requests_after_boundary=0|1
old_commits_after_boundary=0|1
new_generation=current|mismatch|not_observed
worker_owner=same_installation|mismatch|not_observed
profile=same_dedicated|mismatch|not_observed
moodle=pass|failed|human_needed
moodle_code=AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR
edstem=pass|failed|human_needed|not_run
edstem_code=AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR|NOT_RUN
identity_scope=consistent|mismatch|not_observed
binding_consistency=consistent|mismatch|not_observed
minimal_read=pass|failed|not_observed
content_boundary=preserved|violated|not_observed
screenshot=not_used
macos_evidence=unchanged
observed_at=<ISO-8601 with timezone>
windows_next=B3_pending_human_action
phase3=blocked
```
executor只strict parse字段/enum，拒绝extra/free text/unknown code/sensitive值；反馈不是L authority。无论用户声称pass与否，运行canonical B2 read-only verifier。只有final block满足唯一pass conjunction且verifier从两个exact cells独立返回`WINDOWS_B2_WORKER_RESTART_COMPLETE`、本轮真实feedback存在，才创建completed SUMMARY。任何non-pass立即停止，不创建planning receipt、不呈现B3步骤、不自动第二次restart覆盖失败。</action>
  <how-to-verify>
    1. 只在原生Windows 11 current beta paired protected UI确认B1三轮均通过且B2显示ready；不要在WSL/macOS/Terminal运行替代命令。
    2. 确认Moodle/EdStem均authenticated、binding/scope consistent、Profile=same dedicated/owned，B2尚not_run；不要复制完整身份。
    3. 只点击一次“B2：重启受管Worker”，不要使用Task Manager、Services、PowerShell、PID、进程名、selfcheck或安装器；确认文案声明API/browser/Profile/Codex/Windows不重启。
    4. 等待UI依序显示old worker actual exit confirmed、old generation fenced、old requests after boundary=0、old commits after boundary=0、new generation current、owner same installation、Profile same dedicated。任一not_observed/mismatch/非零立即记failed并停止。
    5. 等待Moodle再EdStem最小状态恢复；在protected UI本人核对两来源完整身份正确、binding/scope不变。不要发送姓名、邮箱、组织、course、origin或fingerprint。
    6. 确认未读取body/posts/files/grades，未截图、未输出Profile/PID/path/raw generation/actionID/network/DOM/log，macOS cells unchanged，B3仍pending/not_run。
    7. 只复制完整exact`02-29_WINDOWS_B2_RESULT`脱敏块；不手改code、不添加日志或说明。任何failed/human_needed都是真实阻断结果，不能再restart覆盖。
  </how-to-verify>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B2 --runtime current --platform windows --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. 用户本人只从paired protected UI点击一次fixed B2 action；agent/shell/Task Manager/Services/selfcheck/installer均未控制Worker。
    2. only-owned actual Worker退出被确认；未知/未退出进程未被kill/reclaim，API/browser/Profile/Codex/Windows与无关进程未重启。
    3. restart boundary后old generation request=0且commit=0，late result未提交；new generation current且owner same_installation/current build/managed entrypoint成立。
    4. new Worker复用same dedicated Profile，Moodle→EdStem均恢复；identity/binding/scope consistent、minimalRead pass、contentBoundary preserved，完整身份只在protected UI。
    5. runtime ledger有Moodle/EdStem各一个current non-corrected`windows/B2_worker_restart/L`event；paired-server-only authority成立，macOS/S/I/N/B1/B3/另一source不能代填。
    6. feedback strict pass唯一conjunction为`WINDOWS_B2_WORKER_RESTART_COMPLETE`+completed+fenced+request0+commit0+new current+same owner/Profile+两来源/identity/binding/minimal/content pass；failed/human_needed/extra/sensitive/旧block均阻断。
    7. canonical verifier exit0并只输出allowlisted pass projection；不读DB/Profile、不写ledger/receipt、不控制进程/学校。completed SUMMARY可创建但不是machine authority。
    8. B3/C/D/reauth cells仍not_run、B3 correlation未mint，macOS unchanged、Phase3 blocked；任一失败不展示02-30步骤、不自动重试。
  </acceptance_criteria>
  <resume-signal>当前不要执行。未来Task1全部strict pass后，只粘贴paired protected UI生成的完整`02-29_WINDOWS_B2_RESULT`脱敏块。不要发送PID、generation/fence raw值、进程/设备名、路径、Profile/Cookie、账户/身份、课程/origin、actionID、截图、页面、网络或日志；failed/human_needed必须原样返回，不能要求我代操作或自动重试。</resume-signal>
  <done>只有真实用户Windows managed Worker restart、old-generation双边界零活动、new current Worker同Profile双来源恢复、两个exact Windows/L cells、strict feedback与read-only verifier全部pass时B2才完成并允许B3；其他结果保持blocked。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 与阻断政策

采用OWASP ASVS L1的V1架构边界、V2进程/客户端身份、V4持续授权、V5输入验证、V8数据保护、V13业务流程与V16安全日志意图，不声称完整合规。所有High/Critical威胁均必须`mitigate`；任一事实不可观察、测试失败、writer provenance缺失、敏感泄漏或真实feedback non-pass都是02-29/02-30 blocker，不得accept、transfer或降级为债务。

## Trust Boundaries

| Boundary | 风险 |
|---|---|
| B1 SUMMARY/runtime cells → B2 eligibility | partial/旧build/macOS/S-I-N或SUMMARY可能冒充三轮Windows/L |
| paired UI B2 action → managed process lifecycle | 任意进程/PID/命令可能越权重启不属于本安装的Worker或其他服务 |
| old Worker → request/commit boundaries | 旧lease/generation可能在restart后继续请求来源或提交迟到结果 |
| new Worker → Profile/source state | 新进程可能接管未知Profile、复制Cookie或以单来源/换号状态冒充恢复 |
| paired server → EvidenceLedger L | background/feedback/repo tool可能伪造human authority或跨平台/场景写L |
| protected UI/runtime → SUMMARY/CLI/chat | PID、generation、Profile path、full identity、课程/raw data可能泄漏 |
| B2 completion → B3 | 自动推进可能预mint B3 correlation、发布现场步骤或把B2 feedback当machine authority |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | ASVS | Mitigation Plan |
|---|---|---:|---|---|---|---|
| T2-29-PRE | Spoofing/Tampering | High | B1→B2 gate | mitigate | V1,V4,V13 | 三个round canonical verifier+六个exact current Windows/L cells+completed SUMMARY；platform/scenario/source/build strict，macOS/S-I-N/partial拒绝。 |
| T2-29-PROC | Elevation of Privilege/Tampering | Critical | managed restart | mitigate | V1,V2,V4 | paired fixed action；inventory/installation/owner proof；只退出确知owned Worker；无PID/command/public route；unknown/未退出fail closed。 |
| T2-29-STALE | Tampering/Repudiation | Critical | old Worker request/commit | mitigate | V4,V13 | durable boundary；before-request与before-commit双`assertCurrent`；old fenced且post-boundary request/commit=0；late result drop；failure immutable。 |
| T2-29-NEW | Spoofing/Elevation | Critical | new Worker registration | mitigate | V2,V4,V13 | native Windows managed entrypoint、same installation/current build/owner proof后才current；不以PID/name自报，不接管陌生进程。 |
| T2-29-PROFILE | Information Disclosure/Tampering | Critical | new Worker→Profile/source | mitigate | V4,V8,V13 | same dedicated Profile ownership fence；不复制Cookie/Profile；Moodle→Ed fixed read-only；identity/binding/scope mismatch立即停。 |
| T2-29-L | Spoofing/Repudiation/Elevation | Critical | paired server→EvidenceLedger | mitigate | V4,V5,V13,V16 | one-time server-only Windows human authority绑定actual restart/source/build/generation；two exact append-only cells；repo/chat/fixture无writer权；failed保留。 |
| T2-29-LEAK | Information Disclosure | High | UI/gate/SUMMARY | mitigate | V8,V16 | 仅safe enum/count/digest；禁止PID/raw generation/fence/owner/Profile path/full identity/course/origin/actionID/raw page/network/screenshot；敏感扫描。 |
| T2-29-GATE | Tampering/Elevation | High | repo verifier | mitigate | V4,V5,V13 | read-only signed client/exact cells；无DB/append/process/browser/source权限；strict argv/stdout；无planning receipt。 |
| T2-29-AUTO | Elevation/Repudiation | Critical | checkpoint→B3 | mitigate | V1,V4,V13 | blocking human-action，无default/timeout/auto approve；feedback+verifier+SUMMARY才eligible；B3 correlation须新human action，步骤只来自signed beta。 |
</threat_model>

<verification>
1. strict读取02-28 SUMMARY并运行B1 round1/2/3 canonical Windows verifier；六个exact cells任一non-pass即停止。
2. 只读检查current beta B2 fixed action、only-owned lifecycle、safe UI、server-only correlation与B2 verifier；B2/B3/C cells/correlations起始为空。
3. 运行Worker/generation/fence/recovery tests，证明before-request/commit双边界、late drop、unknown process fail closed、same Profile、测试不写L。
4. 未来用户只点击paired B2动作；核对old exit/fenced/request0/commit0/new current/same owner/Profile与Moodle→EdStem双来源恢复。
5. 读取两个exact `windows/B2_worker_restart/L` cells；source/scenario/platform/build/evidence污染不能代替，failed/human_needed不可删除。
6. strict feedback与canonical B2 verifier同时pass才创建SUMMARY；repo gate零写且所有输出敏感扫描为零。
7. B3仍pending/not_run、无预mint correlation，macOS unchanged、Phase3 blocked；non-pass不展示后续步骤。
</verification>

<success_criteria>
- AUTH-04：真实native Windows owned Worker被restart；旧generation在request/commit双边界均零活动，新generation仅凭same installation/current build/managed owner proof成为current并复用same dedicated Profile。
- AUTH-02：Moodle/EdStem各有独立current `windows/B2_worker_restart/L` event，paired-server-only one-time human provenance成立；macOS/S/I/N/fixture/feedback不能代填。
- UAT-01：真实用户strict反馈、runtime exact cells与canonical read-only verifier共同决定B2；无planning receipt，SUMMARY不替代ledger。
- 任一old request/commit、owner/Profile/identity/binding/source mismatch、human_needed或隐私命中立即阻断02-30；不自动重试、覆盖失败或预mint B3。
- pass仍不满足B3/C/D/reauth或Phase3；macOS evidence unchanged，Phase3 blocked。
</success_criteria>

<output>
只有用户真实strict pass、Moodle/EdStem两个current non-corrected`windows/B2_worker_restart/L`events均pass且canonical B2 verifier返回`WINDOWS_B2_WORKER_RESTART_COMPLETE`后，才创建`.planning/phases/02-poc-live/02-29-SUMMARY.md`。SUMMARY只记录checkpoint=B2、public version/build、platform=windows/native non-WSL、result pass/safe code、oldGeneration=fenced、old requests/commits=0、newGeneration=current、workerOwner=same_installation、profile=same_dedicated、两来源safe result、identity/binding/minimalRead/contentBoundary、observedAt、B3 pending human action、macOS unchanged与Phase3 blocked；不得记录PID/raw generation/fence/owner/executable/Profile path、设备/账户/full identity/course/origin、Cookie/actionID、raw page/network、截图或日志。failed/human_needed时不创建completed SUMMARY、不创建planning receipt、不覆盖immutable failure、不重试掩盖，并保持02-30 blocked。
</output>
