---
phase: 02-poc-live
plan: "33"
type: execute
wave: 34
depends_on:
  - "02-32"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "02-33 是未来原生Windows硬人工门：只有用户已显式恢复native Windows 11 UAT，且02-32 completed SUMMARY与current strict D gate同时成立时才可进入；当前不得执行或自动推进。"
    - "reauth首先等待Moodle或EdStem真实自然过期；产品、agent和脚本不设置、猜测或声称任何默认观察时长，也不通过删除/修改Profile、Cookie、storage或session制造过期。"
    - "若用户自己定义的观察deadline到达而两source均未自然过期，必须在Windows paired protected UI再次阻塞并由用户明确选择exact `wait_until`继续等待或仅授权一次Moodle official logout；无默认选择、超时同意或合成反馈。"
    - "需要重新认证的source单独进入reauth_required并暂停其任务，另一source与last successful state保持可用；用户本人只在official page输入密码/MFA，产品无fill/type/click/submit、读取input或绕过challenge能力。"
    - "target=Moodle时，用户本人完成Moodle恢复后必须执行exact one新的EdStem bounded auth follow-up；target=EdStem只可能来自自然过期，EdStem恢复时Moodle保持unaffected_authenticated且Moodle新login/probe为0。"
    - "Moodle与EdStem必须分别产生一个current non-corrected `windows/L/reauth` result，真实authority只来自published paired Windows server的一次性source children与actual result；repo gate、SUMMARY、chat、public API、S/I/N/macOS无writer权。"
    - "reauth是独立于D及macOS reauth的新Windows evidence；无planning receipt JSON，completed 02-33 SUMMARY仅证明用户真实decision/feedback，不能替代runtime EvidenceLedger。"
    - "任一自然过期未观察却无一次性同意、自动logout/reopen、identity/scope/binding漂移、source isolation失败、敏感输出、MFA自动化或non-pass都保持failed/human_needed；macOS cells不变且Phase3继续blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-32-SUMMARY.md"
      provides: "真实用户完成Windows D cross-day gate的脱敏交接；必须与current D ledger gate同时成立"
    - path: "release/phase2-install-prompt.md"
      provides: "与current signed Windows beta version/build/hash绑定的reauth、official login及用户反馈步骤；缺失时不得临时编写"
      contains: "Windows"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger、per-source reauth状态及paired-server-only one-time pending-action authority"
      contains: "EvidenceLedger"
    - path: "apps/api/src/auth.ts"
      provides: "paired session/CSRF保护的explicit logout intent、user-login completion与per-source isolation入口；public/CLI/MCP无L writer"
      contains: "logout-intent"
    - path: "apps/status/src/main.ts"
      provides: "Windows paired protected reauth状态、自然过期提示、无默认值的user-deadline决定、一次性Moodle logout同意及脱敏结果"
      contains: "CheckpointLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows D与reauth exact-cell canonical read-only verifier；不append ledger、不修改runtime/Profile/Cookie"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-33-SUMMARY.md"
      provides: "只在two-source Windows reauth cells、真实用户decision/feedback与strict verifier全部pass后创建的阶段交接；无parallel planning receipt"
  key_links:
    - from: ".planning/phases/02-poc-live/02-32-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "completed SUMMARY + `--verify --checkpoint D --platform windows --read-only`双重前置；SUMMARY/单source/macOS D不可代填"
      pattern: "WINDOWS_D_24H_RECHECK_COMPLETE|D_24h_recheck"
    - from: "apps/status/src/main.ts"
      to: "apps/api/src/auth.ts"
      via: "paired protected UI在actual expiry后开始source-specific reauth；只有user-defined deadline已到且用户明确authorized_once时才能开始Moodle explicit logout path"
      pattern: "reauth_required|logout-intent|authorized_once"
    - from: "apps/api/src/auth.ts"
      to: "packages/persistence/src/auth.ts"
      via: "server为选定path mint绑定Windows/source/build/generation/config/scope/binding/decision的一次性children，并以actual login/follow-up结果同事务consume/append"
      pattern: "reauth|human_action|EvidenceLedger"
    - from: "Moodle reauth completion"
      to: "EdStem sealed adapter"
      via: "仅target=Moodle路径在恢复commit后发exact one新bounded EdStem auth follow-up；不是旧缓存、循环probe或课程正文读取"
      pattern: "moodle|edstem|reauth"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: ".planning/phases/02-poc-live/02-33-SUMMARY.md"
      via: "只读验证exact two current non-corrected Windows/L reauth cells、path/decision/isolation/recovery/privacy及macOS unchanged；SUMMARY无authority"
      pattern: "WINDOWS_REAUTH_COMPLETE|--checkpoint reauth"
---

<objective>
在Windows D strict pass之后，执行独立且不可自动批准的native Windows reauth live gate：优先等待任一source真实自然过期；若用户自行定义的deadline已到且仍未过期，才由用户在paired protected UI明确授权一次Moodle official logout。随后用户本人完成official re-login/MFA，并验证source隔离、身份/授权scope、Moodle恢复后的单次EdStem follow-up及脱敏最小状态。

Purpose: 落实D-02、D-05、D-08、D-10–D-12、D-14、D-16–D-18与AUTH-02/AUTH-03/SEC-02/UAT-01，证明Windows reauth不会损坏专用Profile/Cookie、不会自动输入凭据或把另一source连带登出，并保持live evidence/platform/writer边界。
Output: 一个future-only blocking `checkpoint:human-action`、Moodle/EdStem两个独立`windows/L/reauth` results、canonical read-only reauth gate和strict pass后的02-33 SUMMARY。当前不执行，不创建reauth planning receipt，不更改macOS evidence，不解除Phase3 blocker。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-23-PLAN.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@.planning/phases/02-poc-live/02-27-SUMMARY.md
@.planning/phases/02-poc-live/02-28-SUMMARY.md
@.planning/phases/02-poc-live/02-29-SUMMARY.md
@.planning/phases/02-poc-live/02-30-SUMMARY.md
@.planning/phases/02-poc-live/02-31-SUMMARY.md
@.planning/phases/02-poc-live/02-32-PLAN.md
@.planning/phases/02-poc-live/02-32-SUMMARY.md
@release/phase2-install-prompt.md
@packages/domain/src/model.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/browser.ts
@packages/platform/src/profile-coordinator.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时必须读取02-32 SUMMARY与current signed Windows beta的真实接口；只允许机械import命名适配，不得从macOS复制correlation、发明deadline或添加repo-side ledger writer：

```typescript
type EvidenceCellKey = {
  platform: 'macos' | 'windows';
  source: 'moodle' | 'edstem';
  scenario:
    | 'A1_login' | 'A2_binding' | 'A2_course_visibility'
    | 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3'
    | 'B2_worker_restart' | 'B3_codex_exit'
    | 'C_os_restart' | 'D_24h_recheck' | 'reauth';
  evidence: 'S' | 'I' | 'N' | 'L';
};
```

Windows D canonical prerequisite：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint D --runtime current --platform windows --read-only
```

strict pass code=`WINDOWS_D_24H_RECHECK_COMPLETE`，要求completed 02-32 SUMMARY与Moodle/EdStem两个current non-corrected `windows/L/D_24h_recheck` cells；SUMMARY只证明真实用户反馈，不能替代ledger。

Windows reauth canonical post-check：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform windows --read-only
```

strict pass code=`WINDOWS_REAUTH_COMPLETE`。唯一合法conjunction是Moodle/EdStem两个current non-corrected `windows/L/reauth` cells、`path=natural_expiry|explicit_logout`、source-specific actual re-login、identity/scope/binding一致、isolation/lastSuccess/contentBoundary preserved、所需peer check成立、`automaticReopen=none`、`screenshot=not_used`、`sensitiveOutput=none`、macOS cells unchanged与Phase3 blocked。

路径contract：

- `natural_expiry`可真实发生于`moodle|edstem`，不接受agent/产品设置的观察时长，且无logout intent。若target=Moodle，Moodle恢复commit后exact one新EdStem bounded auth follow-up；若target=EdStem，EdStem恢复且Moodle=`unaffected_authenticated`、Moodle新login/probe=0。
- `explicit_logout`只允许在用户自己定义的deadline已到且两source均未自然过期后，由用户在paired protected UI作一个新的blocking decision：exact `wait_until`继续等待，或授权`authorized_once`；target固定Moodle。server记录一次性logout intent，绝不删除/修改Profile、Cookie、storage、session文件或自动重新打开login。Moodle恢复commit后exact one新EdStem bounded auth follow-up。
- 任一路径都只允许用户本人在已批准official origin上输入password/MFA并完成挑战；BrowserProvider/adapters/API/MCP无fill/type/click/submit、input/keystroke/credential读取、MFA bypass或arbitrary URL/selector能力。

真正L writer只存在于published paired Windows server workflow。server在actual expiry或explicit authorized_once decision后，为Moodle/EdStem mint新的source children，语义绑定`platform=windows,checkpoint=reauth,scenario=reauth,path,targetSource,decisionDigest,logoutIntent(if explicit),current version/build/generation/config/scope/binding/session,expiry`；内部字段名复用current beta contract，不在本计划另造export type。children只能与actual target login result及peer isolation/follow-up result同事务one-time consume/append；action/correlation ID不出server。repo verifier/SUMMARY/chat/public API/CLI/MCP无L writer权。
</interfaces>

<hard_gates>
- future-only：02-32 completed SUMMARY与current Windows D verifier未同时strict pass时，不观察/制造expiry、不mint reauth bundle、不打开school/login、不呈现logout consent；当前不得执行。
- 不设或暗示任何观察duration，尤其不能把D的24h anchor/earliest当reauth deadline。deadline只能由用户自己定义；到达后仍必须单独阻塞等待用户选择exact `wait_until`或一次性`authorize_moodle_logout`，不选择即保持human_needed。
- 自然过期优先；不得删除/篡改Profile、Cookie、local/session storage、浏览器数据库或专用Profile文件，不清cache、不私密窗口、不换Profile、不kill browser来制造过期。
- 用户本人只在official page登录/MFA；agent/产品不请求、读取、记录、回传、自动输入password/MFA/input/keystroke，不绕过school challenge。
- explicit logout只针对Moodle且只消费一次本轮user consent；不会授权EdStem logout、其他source write、自动reopen/login或业务POST/form/quiz/upload。
- feedback/SUMMARY/gate仅含固定allowlisted enum与timestamp；无姓名/邮箱/组织/course/origin/URL/path/Profile/PID/Cookie/action ID/DOM/selector/request/raw error/screenshot。
- 任一failed/human_needed立即阻止后续Phase3 progression；不自动重试、不覆盖失败、不用D/macOS/S/I/N/单source代填。macOS evidence始终只读且unchanged。
</hard_gates>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-33-1：用户选择真实Windows reauth路径并完成人工恢复与source隔离复核</name>
  <files>none（用户动作只影响仓库外current Windows beta/runtime、official auth session、paired pending-action与append-only EvidenceLedger；strict pass后仅创建脱敏02-33 SUMMARY）</files>
  <read_first>AGENTS.md的Credentials and login、Source and content policy、Data integrity and verification；02-CONTEXT D-02/D-05/D-08/D-10–D-12/D-14/D-16–D-18；02-23-PLAN的macOS reauth边界仅作语义参考、不可复用evidence；02-24/25 SUMMARY与Windows update receipt；02-26至02-31 completed SUMMARY的native Windows A/B/C链；02-32-SUMMARY的exact D/current build/config/binding/next action与published reauth steps pointer；current signed beta `release/phase2-install-prompt.md`/support matrix；最终paired protected UI、explicit logout、EvidenceLedger、read-only D/reauth contracts。禁止读取legacy data、raw DB/WAL、Profile/Cookie/凭据、私人页面、课程正文或network captures。</read_first>
  <what-built>未来用户已显式恢复native Windows 11并在current signed beta上完成A/B/C/D；Windows reauth仍not_run/human_needed。current paired server具备per-source auth state、自然过期检测、user-defined deadline decision、一次性Moodle logout intent、official login handoff、source-specific pending children与append-only L writer。</what-built>
  <action>先验证future chain：02-24 resume、02-25 actual Windows update与02-26至02-31 completed SUMMARY属于current native Windows build；运行exact D verifier并要求`WINDOWS_D_24H_RECHECK_COMPLETE`、completed02-32 SUMMARY与两source `D_24h_recheck` Windows/L current non-corrected cells。读取current paired protected projection，要求两source起始authenticated、binding confirmed、approved scope matched、content boundary/lastSuccess preserved；Windows reauth cells/correlation为空/not_run，macOS EvidenceLedger取只读baseline。任一旧build/generation、pre-filled reauth、单source/D缺失、macOS污染、WSL/非native或敏感输出立即human_needed，不呈现reauth动作。

preflight通过后保持自然过期优先。只允许current beta的既有bounded background auth probes识别真实source expiry；不新设观察interval/deadline，不轮询课程、打开课程正文或把temporary network failure当expiry。用户可在paired protected UI声明自己的观察deadline，但产品只保存server-side decision provenance/digest，不建议或填充时长。deadline之前只能继续等待；不得显示可被默认触发的logout。

若Moodle或EdStem实际自然过期，paired server将仅该source标为`reauth_required`并暂停其任务，保留其last successful state与另一source正常状态；不得自动打开login。用户本人在paired protected UI点击该source唯一CTA后，server mint全新的`path=natural_expiry,targetSource=moodle|edstem` two-source pending children，无logout intent。若实际过期不可证明则不能选择此路径。

若用户自己定义的deadline已到且两source都没有自然过期，paired protected UI必须再次阻塞，清楚提供exact `wait_until`与`仅本次授权Moodle official logout`两个选择，无默认/倒计时/超时同意。用户选择`wait_until`则保持checkpoint未完成且不mint/consume任何logout action，并由用户自行给出下一观察deadline；D的24h不得复用。只有用户明确点击一次授权，server才记录bound `decisionDigest + logoutIntent=authorized_once`、mint `path=explicit_logout,targetSource=moodle` children并调用既有Moodle official logout workflow；该同意不适用于EdStem、未来重试、其他POST或自动login。logout不得删除/编辑Profile、Cookie、storage文件或关闭/重建专用Profile；失败即failed/human_needed。

两条路径都由用户本人在current paired UI点击`打开官方登录`并在已批准official origin完成password/MFA/challenge；agent不得询问或接收秘密，产品不得fill/type/click/submit/read input/keystroke、保存认证body或绕过MFA。每次redirect必须保持已批准official auth origin；popup/download/out-of-scope redirect或非认证业务write立即阻断。用户完成后回到paired UI点击一次`我已完成登录`；server以one-time source child关联actual bounded auth result，不接受chat文字作为authority。

如果target=Moodle，只有Moodle authenticated/identity/scope/binding恢复commit成功后，server才对EdStem发exact one新的sealed bounded auth follow-up；它只能验证EdStem仍authenticated或可恢复的最小标志，不读course body、不循环probe。若target=EdStem（只能natural expiry），完成EdStem恢复后不得对Moodle发新login/probe，必须证明Moodle一直`unaffected_authenticated`。两条路径都验证peer isolation、lastSuccess/contentBoundary preserved、identity/scope/binding一致、automaticReopen none。

用户在paired protected UI核对固定脱敏结果并点击一次确认。paired server将本轮human decision/actual expiry或logout intent、target actual login与peer result同事务consume children，分别append Moodle/EdStem current `windows/L/reauth` immutable result；failed/human_needed也真实append并阻断。repo gate不append、无`--record`，chat feedback/SUMMARY无authority。用户只粘贴`<resume-signal>`固定块；收到真实feedback后运行exact Windows reauth read-only verifier并比较macOS baseline。只有`WINDOWS_REAUTH_COMPLETE`与全部strict字段成立才生成completed02-33 SUMMARY；否则保持Phase3 blocked且不自动重试。
  </action>
  <how_to_verify>
    1. 在Windows paired protected UI确认current native beta/build、D pass、Moodle/EdStem authenticated、binding/scope matched、reauth not_run/human_needed、Phase3 blocked；不要截图或复制identity/course。
    2. 优先等待真实自然过期。不要删除Cookie/Profile、清storage/cache、换Profile、私密窗口、logout、kill browser或重复probe。若尚未过期，只能继续等待。
    3. 若任一source真实自然过期，确认只有该source显示reauth_required且另一source/last success保持；点击该source唯一login CTA。不要把temporary network error当expiry。
    4. 若你自己选择的deadline已到且两source均未过期，在blocking choice中选择exact `wait_until`并自行给出下一deadline，或明确点击`仅本次授权Moodle official logout`。不要把D的24h当deadline；没有明确授权时不要logout，也不要对EdStem logout。
    5. 只在打开的official source page亲自输入password/MFA并完成challenge。不要向agent粘贴密码、验证码、页面、网络输出或截图；若origin越界、popup/download、自动填充/提交或业务write出现，停止并报failed。
    6. 回到paired UI点击一次`我已完成登录`。target=Moodle时等待exact one新EdStem follow-up；target=EdStem时确认Moodle unaffected_authenticated且Moodle新login/probe=0。
    7. 核对两source authenticated/current、identity/scope/binding一致、isolation/lastSuccess/contentBoundary preserved、automatic reopen none、sensitive output none；点击一次确认并只复制`<resume-signal>`脱敏块。
  </how_to_verify>
  <verify>
    <automated>checkpoint前运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint D --runtime current --platform windows --read-only`并要求`WINDOWS_D_24H_RECHECK_COMPLETE`；用户真实完成选择、official re-login/MFA、peer check并提交strict feedback后，运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform windows --read-only`并要求`WINDOWS_REAUTH_COMPLETE`。两命令必须只读、exit0，repo L-write、Profile/Cookie mutation、browser login automation与macOS write均为0。</automated>
  </verify>
  <acceptance_criteria>
    1. completed02-32 SUMMARY与exact D verifier同时证明Moodle/EdStem current non-corrected `windows/L/D_24h_recheck` strict pass；reauth cells/correlation起始为空，macOS baseline已读取。
    2. path只可能为真实`natural_expiry`，或用户自定义deadline已到后的`explicit_logout`；产品/agent未设置duration且未复用D 24h。自然expiry前用户唯一非logout决定是exact `wait_until`；natural path无logout intent，explicit path有新的Moodle-only `authorized_once`用户决定。
    3. 未删除/篡改Profile、Cookie、local/session storage、browser DB或cache，未用private profile/kill/rebuild制造expiry；`automaticReopen=none`。
    4. 只有target source进入reauth_required并暂停，peer与lastSuccess保持；temporary network/parser/health错误不冒充auth expiry。
    5. 用户本人只在approved official origin输入password/MFA；产品/agent未request/read/log/return/fill/type/click/submit credential/input/keystroke，未bypass challenge，未执行business write。
    6. target=Moodle时Moodle恢复commit后exact one新EdStem bounded auth follow-up为authenticated|recovered；target=EdStem只来自natural path，EdStem恢复且Moodle=unaffected_authenticated、Moodle新login/probe=0。
    7. paired server向Moodle/EdStem各append一个current non-corrected `windows/L/reauth` result；authority绑定本轮path/decision/target/build/generation/config/scope/binding/session与actual results，跨platform/scenario/build/replay失败。
    8. strict pass唯一为`WINDOWS_REAUTH_COMPLETE`+two-source pass+path/consent合法+target personal re-login+peer isolation/follow-up+identity/scope/binding/lastSuccess/contentBoundary preserved+automaticReopen none+screenshot not_used+sensitiveOutput none。
    9. repo gate/SUMMARY/chat/public/CLI/MCP没有L writer，不创建02-33 reauth planning receipt JSON；failed/human_needed immutable且阻止Phase3 progression，不自动重试或补写。
    10. feedback/verifier/SUMMARY无identity/course/origin/URL/path/Profile/PID/Cookie/action ID/DOM/selector/request/raw error/screenshot；macOS cells不变，Phase3仍blocked直到后续独立总门明确解除。
  </acceptance_criteria>
  <resume-signal>只粘贴paired UI生成的完整`02-33_WINDOWS_REAUTH_RESULT`脱敏块：`result=pass|failed|human_needed`、`result_code=WINDOWS_REAUTH_COMPLETE|&lt;published safe code&gt;`、`platform=windows`、`decision=natural_expiry|authorize_moodle_logout|not_completed`、`path=natural_expiry|explicit_logout|not_completed`、`target_source=moodle|edstem|not_observed`、`user_deadline=user_defined_reached|not_applicable|not_reached`、`logout_intent=authorized_once|not_applicable|not_observed`、`target_reauth=authenticated|failed|not_observed`、`peer_source=authenticated_followup|recovered_followup|unaffected_authenticated|failed|not_observed`、`peer_new_probe_count=1|0|not_observed`、`identity_scope=consistent|mismatch|not_observed`、`source_isolation=preserved|violated|not_observed`、`last_success=preserved|violated|not_observed`、`content_boundary=preserved|violated|not_observed`、`automatic_reopen=none|detected|not_observed`、`credential_automation=none|detected|not_observed`、`sensitive_output=none|detected|not_observed`、`screenshot=not_used`、`observed_at=&lt;ISO-8601 with timezone&gt;`、`macos=unchanged`、`phase3=blocked`。若选择`wait_until`，checkpoint继续暂停且不要提交完成块；不要发送deadline具体时长、姓名、邮箱、组织、课程、origin/URL/path、Profile/PID/Cookie、password/MFA、action ID、DOM/selector/request、截图或日志。</resume-signal>
  <done>未来用户在native Windows current beta上真实完成合法natural或explicit path、target personal re-login及peer isolation/follow-up，two-source Windows/L reauth results与fixed feedback全部strict pass且read-only verifier返回`WINDOWS_REAUTH_COMPLETE`时，才创建completed02-33 SUMMARY；否则保持failed/human_needed和Phase3 blocked。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| Windows D SUMMARY/ledger → reauth eligibility | SUMMARY、旧build、单source或macOS D可能冒充current Windows D | completed02-32 SUMMARY+exact D Windows/L two-cell current conjunction；缺一不呈现reauth |
| auth/health observation → expiry classification | network/parser/health错误或人为Cookie损坏可能被误报成自然过期 | only existing bounded auth evidence；分离五维状态；禁止Profile/Cookie mutation制造expiry |
| user-defined deadline → explicit logout | UI/agent可能发明duration、默认同意或把deadline到达当logout authority | duration只由用户定义；到达后新的blocking choice；only Moodle authorized_once，无默认/timeout |
| paired UI → official login | page可索取秘密、越界redirect或诱导业务write | approved official origins each redirect；user-only input/MFA；无fill/click/submit/body读取；popup/download/write阻断 |
| target reauth → peer source | 一个source的失败/恢复可能错误暂停、logout或重登另一source | source-scoped state/jobs；Moodle target exact-one EdStem follow-up；EdStem target Moodle zero-new-probe |
| paired server children → EvidenceLedger | correlation可能重放、跨platform/source/path/build使用或由repo伪造 | server-only bound source children、actual result、one-time consume/append；public/repo无writer |
| protected UI → feedback/SUMMARY | identity、course、deadline、Profile/Cookie或页面细节可能泄漏 | fixed enum projection；无具体deadline/identity/path/secret/screenshot/raw output |
| Windows reauth → macOS/Phase3 | 单一source或macOS/S/I/N可能填Windows，或reauth自动解除Phase3 | exact Windows two-source L conjunction；macOS unchanged；Phase3仍blocked |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking test |
|---|---|---:|---|---|---|---|
| T2-33-01 | Spoofing / Tampering | Critical | D→reauth prerequisite | mitigate | V2认证、V4授权、V13业务规则 | current two-source `windows/L/D_24h_recheck`+completedSUMMARY+build/generation一致；任一缺失/跨平台代填阻断。 |
| T2-33-02 | Tampering / Elevation of Privilege | Critical | expiry/deadline/logout decision | mitigate | V4交易授权、V13业务逻辑、V16provenance | natural expiry必须可观察；无invented duration；explicit path需user-defined deadline reached后新的Moodle-only authorized_once。默认/自动logout为blocker。 |
| T2-33-03 | Information Disclosure / Elevation of Privilege | Critical | official login/MFA | mitigate | V2认证、V4least privilege、V8数据保护 | user-only official page input；无credential/input/keystroke capture、fill/type/click/submit或MFA bypass；redirect越界/popup/download/write阻断。 |
| T2-33-04 | Tampering / Denial of Service | High | dedicated Profile/session | mitigate | V4资源授权、V8敏感数据、V13状态 | 不删除/改Cookie/Profile/storage/cache，不kill/rebuild制造expiry；automatic reopen none。任何mutation即failed。 |
| T2-33-05 | Tampering / Denial of Service | High | source isolation/recovery | mitigate | V4对象级授权、V13状态完整性 | target-only pause；保留peer/lastSuccess；Moodle→exact-one EdStem follow-up，EdStem→Moodle zero new probe。跨source副作用阻断。 |
| T2-33-06 | Tampering / Repudiation / Elevation of Privilege | Critical | Windows reauth L append | mitigate | V4writer authority、V13事务、V16audit | server-only path/source-bound children+actual results+user decision同事务consume/append；repo/public/chat无write。重放/跨格即blocker。 |
| T2-03 | Spoofing / Tampering | Critical | post-reauth identity/scope/binding | mitigate | V2身份、V4对象授权、V13业务规则 | protected identity、approved scope与confirmed binding必须一致；漂移/unknown不接受重新绑定或继续。 |
| T2-02 | Information Disclosure | Critical | UI/feedback/output | mitigate | V7错误日志、V8数据保护、V14配置 | fixed allowlist；无deadline detail/identity/course/origin/path/Profile/PID/Cookie/secret/DOM/network/screenshot；sensitiveOutput none。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | evidence/platform/Phase3 gate | mitigate | V1证据分层、V4authority、V13gate、V16provenance | exact Windows/source/scenario/L current cells；macOS unchanged；S/I/N/D/单source不可代填，Phase3不由本plan解除。 |

所有High/Critical项必须`mitigate`，不得accept、transfer、降级warning或留给后续总门修复。本表仅表达ASVS 5.0 L1适用控制意图，不声明完整合规。任何事实不可观察、deadline/consent不明确、Profile/Cookie mutation、credential automation、identity/scope mismatch、source isolation失败、writer provenance失败或敏感输出都保持failed/human_needed并阻止Phase3 progression。
</threat_model>

<verification>
1. completed02-32 SUMMARY与Windows D canonical verifier fresh strict pass；reauth cells/correlation起始为空，current native build/generation/config/binding一致，macOS baseline已只读取得。
2. natural expiry优先且可观察；无产品/agent duration。fallback只在user-defined deadline reached后经新的blocking choice形成Moodle-only authorized_once consent。
3. Profile/Cookie/storage/browser state未被破坏；无自动logout/reopen/login、credential/MFA capture或bypass、越界origin、popup/download/business write。
4. target-only pause与personal official re-login成立；Moodle target exact-one EdStem follow-up，EdStem target Moodle unaffected/zero-new-probe，lastSuccess/contentBoundary preserved。
5. paired server append exactly two current `windows/L/reauth` results；failed/human_needed不可覆盖，repo/public/SUMMARY/chat无writer且无planning receipt。
6. Windows reauth verifier只在`WINDOWS_REAUTH_COMPLETE`完整conjunction时exit0；fixed feedback与ledger一致且无敏感输出。
7. macOS EvidenceLedger bitwise/semantic projection unchanged，Phase3仍blocked；本plan不自动执行任何后续阶段或宣称Phase1/2完成。
</verification>

<success_criteria>
- AUTH-02：Windows D后独立完成two-source reauth live evidence；自然过期优先，explicit fallback有真实user-defined deadline与一次性Moodle logout consent，失败阻断。
- AUTH-03：target source个人身份、approved scope与binding恢复一致；另一source隔离、lastSuccess及五维状态保持，Moodle恢复后只有一个新EdStem follow-up。
- SEC-02：不破坏Profile/Cookie、不自动输入/MFA绕过、不执行学术写操作；official origin与bounded auth-only probe边界持续生效。
- UAT-01：真实native Windows用户动作、paired-server-only two-source L events与completed SUMMARY三者可追溯；repo verifier只读且S/I/N/macOS/D不可代填。
- 无reauth planning receipt，固定脱敏反馈无秘密/路径/身份/课程/页面细节；macOS unchanged、Phase3 blocked。
</success_criteria>

<output>
只有hard checkpoint获得真实用户反馈且canonical verifier返回`WINDOWS_REAUTH_COMPLETE`后，才创建`.planning/phases/02-poc-live/02-33-SUMMARY.md`；failed/human_needed时SUMMARY记录阻塞且不得伪造completed/pass。不要创建任何02-33 planning receipt JSON，不要修改EvidenceLedger或其他计划文件。
</output>
