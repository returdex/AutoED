---
phase: 02-poc-live
plan: 31
type: execute
wave: 32
depends_on:
  - "02-30"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - UAT-01

must_haves:
  truths:
    - "02-31 只在用户已明确恢复原生Windows UAT、02-25 actual Windows update/current build及02-30 B3 strict pass全部成立后可执行；计划批准、macOS C、WSL/Linux、旧SUMMARY或自动设备检测都不是C授权。"
    - "用户本人按current signed beta发布步骤完整重启原生Windows 11；logout、sleep/hibernate、restart Worker/Codex/browser/service、WSL/Linux reboot、VM reset或脚本自报不能冒充Windows OS reboot。"
    - "重启前用户在paired protected UI明确开始C，paired server为Moodle/EdStem持久创建一套新的、durable、single-use `C_os_restart` Windows/L pending bundle；repo、chat、CLI、MCP、public API和用户均不能提供或伪造L authority。"
    - "重启后必须证明native Windows 11且非WSL、boot/session identity已变化、current signed build/runtime generation已恢复、旧generation/owner不能请求或提交；任何unknown、PID reuse或ownership mismatch fail closed且不kill/删锁/抢占。"
    - "Profile ownership通过后，Moodle与EdStem分别重新观察当前auth、Windows A2 confirmed identity/binding、approved scope、同一专属Profile和最小状态；需要登录/MFA/reauth、identity/scope/Profile漂移或任一来源失败立即停止。"
    - "C strict pass必须恰有两个current non-corrected `windows/L/C_os_restart` events，绑定同一新boot/current build/generation/config/scope/binding，并与本轮真实用户脱敏反馈一致；B3、macOS、S/I/N、单来源或overall状态不能代填。"
    - "C独立于A/B/D/reauth：不创建planning receipt、不改macOS cells、不读取课程内容、不自动进入D；pass只使02-32 eligible，Phase1/2 incomplete、shared Profile未最终采用、Phase3 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      provides: "用户未来explicit native Windows resume的上游授权；macOS-first/计划批准不能替代"
    - path: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      provides: "actual native Windows11/non-WSL current beta/build/services/paired UI machine gate"
      contains: "WINDOWS_UPDATE_COMPLETE"
    - path: ".planning/phases/02-poc-live/02-30-SUMMARY.md"
      provides: "Windows B3真实Codex完全退出/独立服务/reentry pass与C eligibility人工前置"
    - path: "packages/persistence/src/auth.ts"
      provides: "durable paired pending-action store与append-only EvidenceLedger；跨reboot保存C bundle并保存两个exact Windows/L cells"
      contains: "EvidenceLedger"
    - path: "packages/platform/src/profile-coordinator.ts"
      provides: "post-reboot Profile single-owner、OS actual process identity、generation/fence和only-owned恢复"
      contains: "ProfileOwnershipCoordinator"
    - path: "packages/platform/src/source-adapters.ts"
      provides: "persisted config/scope驱动的Moodle/EdStem fixed official-origin auth/minimal-status probes"
      contains: "SealedSourceAdapters"
    - path: "apps/status/src/main.ts"
      provides: "paired protected UI中的C start/pending/post-reboot核对、privacy projection与脱敏结果复制"
      contains: "CheckpointLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows C exact-cell/current-runtime read-only verifier及D anchor安全投影；无L writer或reboot/browser/source权限"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-31-SUMMARY.md"
      provides: "仅C两格+真实反馈+read-only gate strict pass后生成的D eligibility与安全时间交接"
  key_links:
    - from: ".planning/phases/02-poc-live/02-30-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "completed B3 SUMMARY与`--checkpoint B3 --platform windows` current exact-cell gate共同决定C可见性；SUMMARY不替代ledger"
      pattern: "WINDOWS_B3_CODEX_EXIT_COMPLETE|B3_codex_exit"
    - from: "apps/status/src/main.ts"
      to: "packages/persistence/src/auth.ts"
      via: "用户reboot前paired action创建durable source children，绑定pre-boot/current build/config/binding/generation且action ID不离开server"
      pattern: "C_os_restart|pending-action|human_action"
    - from: "post-reboot paired workflow"
      to: "packages/platform/src/profile-coordinator.ts"
      via: "native Windows new boot/current generation与exact owner proof先于任何source probe；unknown/in-use fail closed"
      pattern: "PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|generation|fence"
    - from: "post-reboot actual source results"
      to: "packages/persistence/src/auth.ts"
      via: "new boot/current paired user confirmation+server-only bundle同事务consume/append两个Windows/L/C_os_restart cells"
      pattern: "windows|C_os_restart|EvidenceLedger"
    - from: "Windows A1+A2 exact cells"
      to: "02-32 earliest gate"
      via: "六个current non-corrected Windows/L A1+A2 cells的server recordedAt取max为firstTrustworthySuccessAt，再+86,400,000ms；C不重置/提前anchor"
      pattern: "A1_login|A2_binding|A2_course_visibility|recordedAt"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-32 eligibility"
      via: "C read-only verifier+completed SUMMARY；两个exact cells、new boot/current build/ownership/binding/scope/minimal status/privacy全conjunction"
      pattern: "WINDOWS_C_OS_RESTART_COMPLETE|D_24h_recheck"
---

<objective>
执行未来原生Windows 11检查点C的不可自动跨越完整系统重启门：用户在重启前从paired UI启动durable C correlation，按current signed beta的发布步骤本人完整重启Windows，重启后复核Profile ownership、Moodle/EdStem认证、identity binding、approved scope、shared Profile与最小状态，并形成独立Windows/L C证据。

Purpose: 落实D-03、D-09、D-12、D-14、D-17–D-18与AUTH-02/AUTH-03/AUTH-04/UAT-01，证明同一个dedicated Profile及双来源身份在真实native Windows reboot、runtime generation与进程重建后仍安全恢复；拒绝WSL/Linux、B3、macOS或自动结果代填。

Output: 一个future-only blocking human-action checkpoint、Moodle/EdStem各一个`windows/L/C_os_restart` runtime event、canonical C read-only gate、D anchor安全交接与仅strict pass后创建的02-31 SUMMARY；无planning receipt、不执行D/reauth。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@.planning/phases/02-poc-live/02-27-SUMMARY.md
@.planning/phases/02-poc-live/02-28-SUMMARY.md
@.planning/phases/02-poc-live/02-29-SUMMARY.md
@.planning/phases/02-poc-live/02-30-SUMMARY.md
@release/phase2-beta-artifacts.json
@release/phase2-availability.json
@release/phase2-install-prompt.md
@packages/domain/src/model.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/browser-provider.ts
@packages/platform/src/source-adapters.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/verify-phase2-update-gate.mjs
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时直接使用published current Windows beta的final contracts；只允许机械import命名适配，不能复用macOS C correlation、创建第二ledger或改变canonical scenario/platform/evidence：

```typescript
type EvidenceCellKey = {
  platform: 'macos' | 'windows';
  source: 'moodle' | 'edstem';
  scenario:
    | 'A1_login' | 'A2_binding' | 'A2_course_visibility'
    | 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3'
    | 'B2_worker_restart' | 'B3_codex_exit'
    | 'C_os_restart' | 'D_24h_recheck' | 'reauth';
  evidence: 'S' | 'I' | 'N' | 'L';
};
```

Windows C required cells固定为：

```text
windows × moodle × C_os_restart × L
windows × edstem × C_os_restart × L
```

只有published paired server workflow可mint/consume C live authority。用户在reboot前的paired protected UI点击一次`开始 C：完整重启 Windows`后，server创建durable bundle及两个source children；内部字段名复用signed beta final pending-action contract，不在本计划另造export。语义必须绑定`platform=windows`、`checkpoint=C`、`scenario=C_os_restart`、`evidence=L`、source、current installation/version/build/artifact、approved config/scope/binding、Profile owner digest、pre-reboot runtime generation/fence、opaque native Windows boot/session identity、paired human provenance与expiry。bundle持久化于backend-owned state，不依赖Codex/UI/browser memory，只能在post-reboot证明不同native boot、current runtime generation与新paired session后单次consume。action ID不离开server；用户反馈、repo gate、CLI/MCP/public API无writer权。

Windows current update与B3 canonical preflight：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs \
  --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json \
  --artifacts release/phase2-beta-artifacts.json \
  --availability release/phase2-availability.json \
  --runtime current --platform windows --read-only

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint B3 --runtime current --platform windows --read-only
```

第二条strict pass code必须是`WINDOWS_B3_CODEX_EXIT_COMPLETE`，并与completed02-30 SUMMARY、Moodle/EdStem两个current non-corrected`windows/L/B3_codex_exit` cells、actual host exit/reentry、backend/Worker survived、actual client/build matched、current binding/scope/content boundary一致；它不能代替C。

Windows C canonical post-reboot gate：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint C --runtime current --platform windows --read-only
```

strict pass code固定为`WINDOWS_C_OS_RESTART_COMPLETE`。stdout只允许以下safe projection：

```typescript
interface Phase2WindowsCPassProjection {
  schema: 1;
  status: 'pass';
  checkpoint: 'C';
  platform: 'windows';
  scenario: 'C_os_restart';
  resultCode: 'WINDOWS_C_OS_RESTART_COMPLETE';
  version: string;
  buildId: string;
  nativePlatform: 'windows-11';
  platformKind: 'native';
  wsl: false;
  osRestart: 'confirmed';
  bootChanged: true;
  runtimeGeneration: 'current';
  oldGeneration: 'fenced';
  oldRequestsAfterBoundary: 0;
  oldCommitsAfterBoundary: 0;
  profileOwnership: 'proven';
  profile: 'same_dedicated';
  sources: ['moodle','edstem'];
  auth: 'authenticated';
  identityBinding: 'consistent';
  approvedScope: 'matched';
  minimalStatus: 'pass';
  contentBoundary: 'preserved';
  sensitiveOutput: 'none';
  dAnchorAt: string;
  dEarliestAt: string;
  dStatus: 'not_run/human_needed';
  macos: 'unchanged';
  phase3: 'blocked';
}
```

`dAnchorAt`只能由current immutable Windows build第一次同时拥有六个current/non-corrected Windows/L pass cells时计算：Moodle+EdStem`A1_login`、Moodle+EdStem`A2_binding`、Moodle+EdStem`A2_course_visibility`。取六个不可由payload覆写的server `recordedAt`之max，`dEarliestAt=dAnchorAt+86_400_000ms`。C/B lifecycle不重置、提前或替换anchor；任何anchor event被有效correction否定，或build/config/scope/binding漂移，anchor立即untrusted并使02-32 human_needed，必须在current build重做A。C gate可以安全投影这两个带timezone的审计时间，但不能宣称D ready/pass、创建D action或写D cell。

C gate只能经existing signed local-client discovery读取current redacted status与两个exact C cells；不得import persistence、直读DB/WAL/Profile、append/correct ledger、start/restart/spawn服务、打开browser/学校、接受URL/port/path/PID/Profile/boot ID/action ID/selector/browser handle/operation或时间override。raw boot/session/generation/fence/owner identity不输出。

paired UI只生成以下strict反馈；它是人工checkpoint事实，不是L authority：

```text
02-31_WINDOWS_C_RESULT
checkpoint=C
platform=windows
version=<current public version>
build_id=<current public build id>
result=pass|failed|human_needed
result_code=WINDOWS_C_OS_RESTART_COMPLETE|WINDOWS_OS_RESTART_NOT_CONFIRMED|WINDOWS_NATIVE_PLATFORM_NOT_CONFIRMED|WSL_NOT_ALLOWED|CURRENT_BUILD_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|IDENTITY_MISMATCH|APPROVED_SCOPE_MISMATCH|INTERACTION_REQUIRED|MINIMAL_STATUS_FAILED|CONTENT_BOUNDARY_VIOLATED|SENSITIVE_OUTPUT_DETECTED|RESULT_NOT_OBSERVED
os_restart=confirmed|not_observed
native_windows_11=confirmed|not_observed
wsl=excluded|detected|not_observed
actual_build=matched|mismatch|not_observed
profile_ownership=proven|in_use|unconfirmed|not_observed
profile=same_dedicated|mismatch|not_observed
moodle=pass|failed|human_needed
moodle_code=<published safe code>
edstem=pass|failed|human_needed
edstem_code=<published safe code>
identity_binding=consistent|mismatch|not_observed
approved_scope=matched|mismatch|not_observed
minimal_status=pass|failed|human_needed
content_boundary=preserved|violated|not_observed
sensitive_output=none|detected|not_observed
screenshot=not_used
observed_at=<ISO-8601 with timezone>
macos=unchanged
phase3=blocked
```

若current immutable beta缺durable cross-reboot C bundle、native boot proof、exact cells或上述read-only verifier，必须human_needed并回到修复→完整tests/scan→更高immutable beta→actual Windows update→重做依赖live gates；不得现场写PowerShell/path/service name、增加repo writer/public route或用SUMMARY/chat代写L。
</interfaces>

<hard_gates>
- future-only：02-24 explicit resume、02-25 current native update、02-26 A1、02-27 A2、02-28 B1、02-29 B2、02-30 B3任一未strict pass时，不呈现C、不mint bundle、不reboot、不probe、不写L。
- C必须是用户本人按current signed beta的exact published Windows reboot/recovery步骤完成的native Windows 11 full reboot；WSL/Linux/macOS/VM/CI、logout、sleep/hibernate、shutdown未重开、Worker/Codex/browser/service restart均不满足。
- C pending bundle必须在reboot前由current paired protected UI新建并durably committed；若bundle缺失、过期、重复或与pre-boot build/config/binding/generation不符，post-reboot不得补mint/修正，保持human_needed。
- post-reboot必须先证明different native boot/session、current signed build/generation与old fencing，再证明Profile ownership。running/unknown/PID reuse/executable/control mismatch只返回in_use/unconfirmed；不得kill/taskkill、按process name终止、删Chromium lock、抢占或启动第二Profile。
- ownership通过后才运行Moodle→EdStem fixed read-only auth/minimal-status probes；identity/binding/scope/Profile mismatch、登录/MFA/reauth/interaction或任一source non-pass立即停止，不在C内修复或登录。
- 不接受任意URL/JS/selector/browser handle/API fallback，不读取课程正文/帖子/文件/成绩/反馈，不download/POST/form/quiz/upload，不请求或返回截图/raw logs/network/DOM。
- 两个C cells只能由post-reboot actual results+pre-reboot bundle+本轮user confirmation形成payload外Windows/L authority；B3、macOS、WSL/Linux、S/I/N、另一scenario/source/build/overall/latest不可代填。failed/human_needed immutable且不能覆盖。
- 不创建C planning receipt、parallel ledger或public writer。repo gate只读；macOS event digests/writer count前后相等。
- C pass只允许02-32 D eligible；D anchor来自Windows A1+A2六格server time且C不重置。D/reauth仍not_run/human_needed，Phase1/2 incomplete、shared Profile未最终采用、Phase3 blocked，不自动执行02-32。
</hard_gates>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-31-1：用户完整重启native Windows 11并完成独立C双来源复查</name>
  <files>none（用户动作只影响仓库外current Windows beta/runtime、durable paired pending action、Profile observation与既有append-only EvidenceLedger；strict pass后仅创建脱敏02-31 SUMMARY，不创建planning receipt）</files>
  <read_first>AGENTS.md的Execution after plan approval、Product boundary、Credentials and login、Source and content policy、Data integrity and verification、Windows native evidence；02-CONTEXT D-03/D-09/D-12/D-14/D-17–D-18；02-24 explicit resume；02-25 update receipt/SUMMARY；02-30 SUMMARY与B3 canonical gate；current signed Windows beta install prompt/support matrix；02-03/05/07/12 SUMMARY的ownership/actual-exit/generation/fence/managed lifecycle；02-08/09 protected status/privacy；最终EvidenceLedger/pending-action/C verifier contracts。禁止读取legacy、Profile/Cookie/credential、raw DB/WAL、boot/PID/owner私有值、完整身份/course/origin或原始页面网络。</read_first>
  <what-built>未来用户已明确恢复native Windows UAT、实际更新current signed beta并完成Windows A/B1/B2/B3；C/D/reauth仍not_run/human_needed，published beta已具备durable cross-reboot C bundle、native boot proof与read-only gate。</what-built>
  <action>先验证future chain：02-24 SUMMARY记录explicit resume；运行02-25 current update gate证明nativeWindows11/non-WSL/current build/services/paired UI；读取02-30 completed SUMMARY并运行B3 verifier，要求`WINDOWS_B3_CODEX_EXIT_COMPLETE`及两个current non-corrected B3 Windows/L cells。重新核对current config/scope/binding/Profile/paired session与privacy，两个C cells、C pending bundle、D cells都必须为空/not_run；读取macOS cells/digests/writer baseline。任一缺失、旧build/generation、pre-filled/replayed C、B3 correction、macOS代填或敏感输出立即human_needed且不显示reboot action。

preflight通过后，只在current Windows paired protected UI让用户点击一次`开始 C：完整重启 Windows`。paired server在任何reboot前重新验证current B3/build/config/scope/binding/generation/Profile owner，并durably持久化一套新C bundle及两个source children；UI只有在transaction committed、无旧C correlation、expiry足以覆盖published normal reboot flow且exact signed reboot/recovery steps可用时显示`可以重启`。action ID/boot identity/generation/owner不出server。若bundle创建失败或beta无完整能力，停止；不得让用户先重启后补bundle。

向用户逐字显示signed manifest绑定的`release/phase2-install-prompt.md`/support matrix及02-30 SUMMARY指向的exact Windows full-reboot/recovery步骤、expected version/build和post-reboot UI状态；不得凭经验编造PowerShell、path、service name、Task Scheduler项、快捷键或命令。硬暂停，等待用户本人执行完整native Windows 11 restart。执行器/产品/script不得调用shutdown/reboot；WSL/Linux reboot、logout、sleep/hibernate、restart Codex/Worker/browser/service均不算。

用户按published步骤重新进入actual installed Windows Codex并恢复paired UI后，product先证明native Windows11/non-WSL、post boot/session identity与pre值不同、same installation/artifact/current version/build、new current runtime generation、old generation/fence在boundary后source request=0/commit=0。再执行Profile ownership检查：只有installation+nonce+PID OS-start identity+exact executable+authenticated control+current generation/fence全部成立，或OS已确认旧owner exited且本安装正常恢复same Profile，才可继续。unknown/running/PID reuse/mismatch/second instance返回PROFILE_IN_USE或PROFILE_OWNERSHIP_UNCONFIRMED，保持记录并停止，不kill/taskkill/删锁/抢占/触碰日常浏览器。

ownership通过后，用户只在paired protected UI本地核对Moodle/EdStem完整identity、organization/binding和approved scope；完整值不复制。product从persisted approved IDs内部构造fixed Moodle→EdStem auth/minimal-status probes，逐跳验证official origin，分别确认positive auth、Windows A2 binding/scope、same dedicated Profile和同一个approved course的最小visibility/status。需要login/MFA/reauth/interaction或identity/scope/Profile mismatch时本C返回failed/human_needed并停止，不顺带执行reauth。不得读course content或写/download。

只有new boot/current generation actual observations、用户本轮确认与pre-reboot C bundle的source/checkpoint/platform/build/config/scope/binding/Profile/boot/generation全匹配，paired server才一次性consume children并分别appendMoodle/EdStem immutable`windows/L/C_os_restart` results；failed/human_needed也保留，不能delete/overwrite/latest-hide。用户只复制`<resume_signal>`fixed block。反馈不是authority；收到后strict parse exact keys/enums/current version/build/ISO time，运行canonical C gate并重读macOS baseline。只有resultCode=`WINDOWS_C_OS_RESTART_COMPLETE`、两个cells current/non-corrected pass、native/boot/old fencing/ownership/Profile/auth/binding/scope/minimal status/content/privacy全conjunction、D anchor安全投影有效且macOS unchanged时完成；否则立即阻止02-32，不自动重试、登录或继续probe。</action>
  <how_to_verify>
    1. reboot前在Windows paired UI确认current beta/build、B3 pass、C等待本人动作、D/reauth not_run/human_needed、Phase3 blocked；不要复制identity/course或截图。
    2. 点击一次`开始 C：完整重启 Windows`并等待UI明确显示durable C checkpoint已准备；若失败/重复/过期或显示Profile/action/boot私有值，停止，不重启。
    3. 严格按UI引用的current signed beta步骤完整restart原生Windows 11；不要只logout、sleep/hibernate、restart Codex/Worker/browser/service，也不要重启WSL/Linux/VM来代替。
    4. 重启后按同一published步骤打开actual installed Windows Codex与paired local UI；若需pairing只按正式本地流程，不向聊天提供token/port/path/设备信息。
    5. 等待UI显示native Windows11/non-WSL、新boot confirmed、current build matched、old generation fenced、Profile ownership proven。in_use/unconfirmed/unknown/mismatch时如实human_needed并停止，不kill/taskkill、删锁或启动第二实例。
    6. 在protected UI本地逐项核对Moodle/EdStem authenticated、完整identity与Windows A2 binding一致、approved scope matched、same dedicated Profile observed、one-course minimal status pass；不要打开/读取课程正文、帖子、文件、grades/feedback。
    7. 检查C结果单只有public version/build、platform/scenario/source/time、safe codes、ownership/binding/scope/Profile/minimal status/content/privacy/gaps；若出现identity/course/origin/Profile/PID/boot/action ID/raw page/network/log/screenshot，标记sensitive_output=detected并停止，不粘贴敏感值。
    8. 只返回完整`02-31_WINDOWS_C_RESULT`。任一字段非confirmed/excluded/matched/proven/same/pass/consistent/preserved/none/not_used时如实failed/human_needed；即使pass也不要点击D或要求自动继续。
  </how_to_verify>
  <verify>
    <automated>checkpoint前运行`node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform windows --read-only`并要求B3 safe code=WINDOWS_B3_CODEX_EXIT_COMPLETE；用户真实reboot/feedback且paired server提交C results后运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint C --runtime current --platform windows --read-only`并要求WINDOWS_C_OS_RESTART_COMPLETE。全部命令只读、exit0；repo L-write/reboot/start/restart/browser/source调用为0。</automated>
  </verify>
  <acceptance_criteria>
    1. explicit Windows resume、02-25 current native update及02-30 completed SUMMARY+B3 gate均strict pass；C两个cells/bundle和D cells起始为空，macOS baseline已读取。
    2. pre-reboot paired server新建durable single-use C bundle，source children绑定Windows/C_os_restart/L/current installation/build/config/scope/binding/Profile/pre-boot generation/boot/paired provenance；repo/user无authority。
    3. 用户本人按signed steps执行native Windows11 full restart；post proof确认non-WSL、boot/session changed、same installation/current build、新generation current、old request/commit after boundary=0。任何替代reboot不通过。
    4. Profile ownership由完整OS+authenticated control proof确认；running/unknown/PID reuse/executable/control mismatch只返回in_use/unconfirmed/human_needed，kill/taskkill/delete-lock/second-instance=0。
    5. Moodle/EdStem分别执行fixed official-origin auth/minimal-status probes；identity binding/approved scope consistent、same Profile、one-course minimal status pass；course content/write/download/API fallback=0。
    6. EvidenceLedger恰有两个current non-corrected`windows/L/C_os_restart` pass cells，绑定同一new boot/current generation/build/config/binding；B3/macOS/WSL/S/I/N/另一source/scenario/overall不可代填。
    7. strict feedback与canonical gate一致且gate返回WINDOWS_C_OS_RESTART_COMPLETE；stdout证明native/boot/old fencing/ownership/Profile/auth/binding/scope/minimal/content/privacy，并安全投影dAnchorAt/dEarliestAt。
    8. dAnchorAt精确等于Windows A1+A2六个current non-corrected L cells的max(server recordedAt)，dEarliestAt精确+86,400,000ms；C不重置/提前anchor，D仍not_run/human_needed且未mint action/probe。
    9. feedback/gate/SUMMARY候选无credential/input/Cookie/Profile/PID/boot/generation/owner/full identity/course/origin/action ID/raw DOM/network/log/screenshot；macOS cell digests/writer count前后相等。
    10. 任一failed/human_needed/not_observed/privacy hit/verifier nonzero立即阻止02-32，不创建planning receipt或success SUMMARY、不覆盖失败、不执行D/reauth；pass也只使02-32 eligible且Phase3 blocked。
  </acceptance_criteria>
  <resume_signal>重启并回到actual installed Windows Codex后，只粘贴paired UI生成的完整`02-31_WINDOWS_C_RESULT`块，精确使用`<interfaces>`字段和值。不要发送PowerShell/Terminal命令或输出、设备/账户/身份/组织/course/origin/path、Profile/PID/boot/generation/Cookie/action ID、截图、日志或raw错误；`approved`/自由文本不会解除门禁。</resume_signal>
  <done>用户真实完成native Windows11 full reboot；new boot/current generation下Profile ownership、Moodle/EdStem auth、Windows binding/scope/same Profile/minimal status/privacy和两个exact Windows/L C cells全部strict pass，canonical gate返回WINDOWS_C_OS_RESTART_COMPLETE且macOS unchanged时才允许02-32 D；否则保持blocked。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| B3 gate/SUMMARY → C eligibility | 旧summary、部分cells或lower evidence可能冒充C前置 | completed02-30 SUMMARY+canonical current B3 verifier+02-25 native update；缺一不展示C |
| paired UI → pre-reboot authority | 未配对/重放/客户端自造action可能伪造durable C | current paired principal+same-origin CSRF；server mint source children；action ID server-only；transaction committed before reboot |
| pre-reboot → post-reboot runtime | boot/session/process/generation变化可能被缓存或WSL/服务重启冒充 | native Windows11/non-WSL attestation、opaque boot change、same installation/build、新generation、old request/commit=0 |
| OS process state → Profile ownership | PID reuse、unknown holder、lease expiry可能导致抢占/误杀/删锁 | installation+nonce+OS-start+exact executable+authenticated control+fence；actual exit；unknown fail closed |
| untrusted source pages → C probes | redirect/marker/页面指令可能扩大scope、读取内容或写来源 | persisted approved config/scope、sealed fixed actions、official-origin逐跳、bounded marker、无write/download/API fallback |
| protected local state → feedback/SUMMARY | identity/course/Profile/process/boot/raw network可能泄漏 | paired protected display、fixed redacted block、safe enums/times、no screenshot/log、401/403 purge |
| paired actual C results → EvidenceLedger | chat/repo/macOS/B3/S-I-N/单source可能伪造Windows C L | durable one-time server bundle+actual post-reboot result+human confirmation同事务append；exact coordinates；repo read-only |
| A1/A2 server times → D anchor | C时刻、user timestamp、单source或corrected/old-buildevent可能重置/提前24h | six-cell current Windows/L conjunction、max server recordedAt、+86400000ms；C只投影不修改anchor |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking verification |
|---|---|---:|---|---|---|---|
| T2-31-AUTHZ | Spoofing / Elevation of Privilege / Repudiation | Critical | resume/B3 → C | mitigate | V1架构、V4访问控制、V13流程 | explicit resume+current update+completed B3 SUMMARY+exact runtime gate；无default/timeout/auto路径。未授权C activity即Critical blocker。 |
| T2-31-REBOOT | Spoofing / Tampering / Repudiation | Critical | claimed reboot → native new boot | mitigate | V2平台身份、V4授权、V13状态、V16provenance | Windows11/native/WSL=false、opaque boot change、new generation/current build；logout/sleep/service/WSL/Linux/VM不能代替。错误platform/reboot pass即Critical blocker。 |
| T2-04 | Spoofing / Tampering / DoS / Elevation of Privilege | Critical | process state → Profile ownership | mitigate | V1架构、V2认证、V4授权、V14配置 | full owner/control/fence proof；lease/PID/process-name不等于exit；unknown/running/PID reuse不kill/delete-lock/reclaim。误杀/抢占即Critical blocker。 |
| T2-05 | Tampering / Repudiation | High | old runtime/owner → post-reboot request/commit | mitigate | V4访问控制、V13状态 | reboot boundary后before-request/before-commit current generation/fence检查；old counters=0；C cells绑定new generation。迟到访问/提交即High blocker。 |
| T2-03 | Spoofing / Tampering | Critical | post-reboot identities/binding/scope | mitigate | V2身份、V4对象授权、V11业务逻辑 | 两来源current positive auth、protected identity、Windows A2 binding/scope exact compare；mismatch先于继续访问硬停且不覆盖。串号即Critical blocker。 |
| T2-01 | Tampering / Elevation of Privilege | Critical | untrusted page → source adapter | mitigate | V4授权、V5输入、V12资源、V13API | persisted IDs、fixed sealed actions、official-origin逐跳、bounded marker；拒绝URL/JS/selector/handle/write/download/API fallback。越界请求即Critical blocker。 |
| T2-02 | Information Disclosure | Critical | Profile/source/runtime → output | mitigate | V7错误日志、V8数据保护、V14配置 | 无credential/input/Cookie/Profile/PID/boot/owner/full identity/course/raw DOM/network/action ID/screenshot/log；fixed safe output。任一泄漏即C failed。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | C cells/D gate/Phase3 | mitigate | V1证据分级、V4writer authority、V13exact state、V16provenance | two exact Windows/L/C cells、durable server-onlybundle、current non-corrected conjunction、repo read-only；macOS/B3/S-I-N不可代填；D/Phase3独立。跨格/auto pass即Critical blocker。 |
| T2-08-TIME | Tampering / Repudiation / Elevation of Privilege | High | Windows A cells → D earliest | mitigate | V4authority、V13state integrity、V16audit | six-cell server recordedAt max+86400000；no payload/user/C/B clock；correction/build drift invalidates anchor；C不能mint D。错误anchor/提前D即High blocker。 |

所有High/Critical项必须`mitigate`，不得accept/transfer、降级warning或把失败留给D。ASVS 5.0 L1仅表示适用控制意图，不声明完整合规。任何missing/failed/human_needed、平台/ownership/identity/privacy/anchor异常或verifier nonzero均保持C/D blocked；macOS证据不得写Windows，C不得解除Phase3。
</threat_model>

<verification>
1. future execution先验证02-24 explicit resume、02-25 current native update、02-30 completed SUMMARY与canonical B3 gate；任一缺失/旧build/non-pass时C activity=0。
2. C开始前两个Windows/L/C cells与pending bundle为空，macOS baseline已记录；paired user action在reboot前transactionally mint durable source children，repo/user无authority。
3. 用户真实按signed steps完整restart native Windows11；post proof确认non-WSL/new boot/current build+generation/old request+commit=0，替代reboot全部失败。
4. Profile coordinator对unknown/running/PID reuse/executable/control mismatch fail closed，无kill/taskkill/delete-lock/reclaim/second instance；完整proof后才probe。
5. Moodle/EdStem分别运行fixed auth/minimal-status probe并比较Windows A2 identity/binding/scope/same Profile；interaction/expiry/mismatch/non-pass不触发login/reauth或额外访问。
6. paired server以pre-bundle+actual post-reboot observations+user confirmation同事务append两个exact C events；failed/human_needed immutable，cross-cell/platform/evidence/build replay拒绝。
7. canonical C verifier严格要求WINDOWS_C_OS_RESTART_COMPLETE及native boot/old fencing/ownership/Profile/source/binding/scope/minimal/content/privacy conjunction；repo工具只读无副作用。
8. verifier从Windows A1+A2六cells输出正确dAnchorAt/dEarliestAt且C不修改；D仍not_run/human_needed、无action/probe/append。
9. fixed feedback/gate/SUMMARY敏感扫描零命中；macOS cells unchanged，pass仅使02-32 eligible且无auto-execute。
</verification>

<success_criteria>
- AUTH-02：用户完成一次独立native Windows11 full reboot，Moodle/EdStem各有current`windows/L/C_os_restart` strict pass；B3/macOS/WSL/S-I-N未被复用。
- AUTH-03：reboot后双来源auth、protected identity、confirmed binding、approved scope、same Profile与minimal status重新观察；漂移/interaction/unknown有明确non-pass且阻断D。
- AUTH-04：new boot下Profile single-owner与actual process/control identity成立，old generation/fence失效；unknown/PID reuse/lease expiry不导致抢占、误杀、删锁或第二实例。
- UAT-01：用户按signed Windows步骤本人重启并返回fixed redacted result；pre-reboot paired-server-only authority、exact cells与read-only gate不可由feedback/summary伪造。
- D handoff使用Windows A1+A2六格server-time anchor+24h；C/B不重置/提前时间，D/reauth独立且未运行。
- 无planning receipt/parallel ledger/public writer、任意browser/source参数、course content/write/download或敏感输出；macOS unchanged、Phase1/2 incomplete、sharedProfile unverified、Phase3 blocked。
</success_criteria>

<output>
只有真实用户返回strict`02-31_WINDOWS_C_RESULT`、Moodle/EdStem两个current non-corrected`windows/L/C_os_restart` cells均pass且canonical C gate返回`WINDOWS_C_OS_RESTART_COMPLETE`后，才创建`.planning/phases/02-poc-live/02-31-SUMMARY.md`。SUMMARY只记录public version/build、platform=windows、checkpoint=C、nativeWindows11/non-WSL/osRestart/current-generation/old-fenced结论、两source safe codes、Profile ownership/sameProfile/identityBinding/scope/minimalStatus/contentBoundary/privacy、observedAt、由runtime六个A1+A2 cells计算的dAnchorAt/dEarliestAt、macOS unchanged、02-32 eligible但未执行及D/reauth/Phase1/2/3 gaps；不创建C planning receipt，不记录设备/账户/身份/组织/course/origin/path、Profile/Cookie、PID/boot/generation/fence/owner/action ID、raw page/network/log/screenshot。failed/human_needed时保留runtime immutable result、不创建成功SUMMARY、不执行02-32。
</output>
