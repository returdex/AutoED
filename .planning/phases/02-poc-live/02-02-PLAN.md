---
phase: 02-poc-live
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "packages/persistence/src/database.ts"
  - "packages/persistence/src/auth.ts"
  - "tests/integration/auth-persistence.test.ts"
  - "tests/integration/job-storage.test.ts"
autonomous: true
requirements: ["AUTH-02", "AUTH-03", "AUTH-04", "SEC-02", "UAT-01"]
approval: pending_user_plan_approval
must_haves:
  truths:
    - "经批准的 Moodle/EdStem official origin 与限定 scope 以事务方式持久化；任意来源、任意 URL 或未批准 scope 不能进入数据库。"
    - "Moodle 与 EdStem 的当前 observation、最近成功 observation 和最近失败彼此独立；网络、parser、权限或认证失败不会清除最近成功状态，也不会把失败误记为登出或 confirmed empty。"
    - "账户绑定只持久化由安装密钥生成的不可逆 fingerprint、组织/租户 fingerprint、证据类别和 candidate/confirmed/mismatch 状态；姓名、完整学校邮箱及原始 subject/tenant 标识不进入 SQLite 或备份（D-04–D-07）。"
    - "Profile ownership 使用 installation identity、进程证明摘要、generation 与单调 fence；租约到期只使旧持有者失权，不能证明浏览器退出，也不能自动删除持有记录（D-03、D-09）。"
    - "UAT ledger 只追加、按 platform×source×scenario×evidence class 精确寻址；macOS/S/I/N 写入不能修改 Windows 或任何 L 单元，Windows 持续为 not_run/human_needed（D-14、D-17）。"
    - "Profile 路径/内容、Cookie/storage state、密码/MFA/输入值、原始 DOM/HTML、请求或响应资料、完整身份、课程名、截图/HAR/trace/video/console 均不进入数据库、WAL 或 SQLite 备份（T2-02）。"
    - "本计划只产生自动化 S/I 证据；不生成 live pass、不改变 Phase 1/2 完成状态，Phase 3 硬门禁保持关闭。"
  artifacts:
    - path: "packages/persistence/src/database.ts"
      provides: "从 Phase 1 schema 事务升级到 Phase 2 schema，并保留 WAL/FULL、maintenance generation 与失败回滚语义"
      contains: "PRAGMA user_version = 2"
    - path: "packages/persistence/src/auth.ts"
      provides: "approved source/scope、per-source observation/last-success、keyed binding、Profile ownership fence 与 append-only UAT ledger 的 SQLite stores"
    - path: "tests/integration/auth-persistence.test.ts"
      provides: "真实 SQLite 重开、事务回滚、隐私、last-success、ownership fence 与证据隔离反例"
    - path: "tests/integration/job-storage.test.ts"
      provides: "Phase 1 schema 到 Phase 2 schema 的兼容迁移及未知 schema fail-closed 回归"
  key_links:
    - from: "packages/persistence/src/database.ts"
      to: "packages/persistence/src/auth.ts"
      via: "单一有序 schema v1→v2 事务和外键/约束；store 不在运行时自行建表"
      pattern: "source_configs|source_observations|account_bindings|profile_ownership|uat_receipts"
    - from: "packages/persistence/src/auth.ts"
      to: "packages/application/src/ports.ts"
      via: "严格实现 02-01 定义的 persistence ports，不让 driver 发明旁路输入"
      pattern: "implements .*Store"
    - from: "packages/persistence/src/auth.ts"
      to: "packages/persistence/src/database.ts"
      via: "每次写入在 BEGIN IMMEDIATE 中校验 maintenance generation、写入 fence 与 recordWrite"
      pattern: "requireWrite|recordWrite|transaction"
    - from: "packages/persistence/src/auth.ts"
      to: "packages/contracts/src/index.ts"
      via: "写前解析 02-01 strict schemas；extra keys 与敏感字段 fail closed"
      pattern: "Schema\.parse"
---

<objective>
建立 Phase 2 认证 POC 的事务迁移与安全持久化边界：只保存经批准的来源/范围、分来源观察与最近成功、不可逆身份绑定、Profile 所有权 fence 和不可串格的追加式 UAT 结果单。

Purpose: 履行 AUTH-02、AUTH-03、AUTH-04、SEC-02、UAT-01 中的数据完整性和证据隔离要求；从数据层阻断 T2-02 凭据/私人资料泄漏、T2-06 状态混淆以及 T2-08 synthetic/native/live 或 macOS/Windows 串格。
Output: Phase 1 schema 的可回滚 v1→v2 迁移、严格 SQLite stores 与真实数据库集成反例。当前仅规划；执行只能发生在用户批准本 PLAN 后，且不得触发学校登录、live UAT、发布或 Phase 3。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-PLAN.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@packages/persistence/src/database.ts
@packages/persistence/src/runtime-status.ts
@packages/persistence/src/sessions.ts
@tests/integration/job-storage.test.ts
@tests/integration/job-recovery.test.ts

<interfaces>
02-01 是本计划的接口前置：执行时直接使用其 `SourceId`、`SourceAction`、`ApprovedSourceConfig`、`SourceObservation`、`AccountBinding`、`ProfileReservation`、`ProfileOwnerIdentity`、`ProfileOwnership`、`EvidenceClass`、`NativePlatform`、`UatScenario`、`EvidenceReceipt`、`EvidenceWriterAuthority`、`Phase2Gate` 及同名 `*Schema`，并分别实现 `SourceConfigStore`、`SourceObservationStore`、`AccountBindingStore`、`ProfileOwnershipStore`、`EvidenceLedger`。若 02-01 的最终导出名称不同，只调整 import，不复制或在 persistence driver 内重定义第二套契约。

现有写入门禁必须继续使用：

```typescript
export interface WriteContext {
  expectedGeneration: number;
  selfcheck?: { operationId: string; generation: number };
}

export function requireWrite(
  db: Database.Database,
  context: WriteContext,
  allowQuiescing?: boolean,
): MaintenanceGate;

export function recordWrite(db: Database.Database, context: WriteContext): void;
```

02-01 的 UAT 写入上下文必须独立于 receipt payload 携带可信 writer class 与实际平台；调用方不能仅靠把 payload 写成 `live/windows` 获得相应权限。02-02 的 store 在事务内同时校验上下文与 receipt 的 platform/evidence class。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-02-1: 以事务 migration v2 建立隐私最小化 schema</name>
  <read_first>AGENTS.md；.planning/phases/02-poc-live/02-CONTEXT.md 的 D-03–D-07、D-09、D-14、D-17；.planning/phases/02-poc-live/02-RESEARCH.md 的 Persistence、T2-02/T2-06/T2-08；02-01-PLAN.md 与 02-01-SUMMARY.md；packages/persistence/src/database.ts；packages/persistence/src/runtime-status.ts；tests/integration/job-storage.test.ts。不得读取 legacy 数据库、Profile 或私人样本作为迁移输入。</read_first>
  <files>packages/persistence/src/database.ts, tests/integration/job-storage.test.ts, tests/integration/auth-persistence.test.ts</files>
  <behavior>
    - RED：真实 schema v1 数据库升级到 v2 后，原 jobs/runtime/maintenance 数据逐值保留，`foreign_key_check` 与 `integrity_check` 均通过，关闭重开仍为 v2。
    - RED：在迁移中途注入 SQL 失败时，整个迁移回滚，`user_version`、旧表和旧数据保持 v1；不能留下部分 auth 表或伪造成功 migration row。
    - RED：fresh database 一次创建 v2；未知的未来 schema 版本及伪造/缺失 migration 记录均 `SCHEMA_INCOMPATIBLE`，不得静默降级或重建。
    - RED：schema 中不存在可保存 Profile path/content、Cookie/storage state、raw DOM/HTML/network、password/MFA/input、full name/email/course title、screenshot/HAR/trace/video/console 的列或通用 blob/json dump 列。
  </behavior>
  <action>先扩展 `tests/integration/auth-persistence.test.ts` 与既有 `job-storage.test.ts`，再改 `openDatabase()`。把 Phase 1 `user_version=1` 单向、原子迁移至 `user_version=2`；fresh database 也必须通过同一有序 migration 链抵达 v2，不维护两套易漂移的建库脚本。v2 新建 `source_configs`、`source_observations`、`account_bindings`、`profile_ownership`、`uat_receipts`，使用 source/platform/scenario/evidence/status 的 CHECK、外键、唯一键和长度边界；批准 origin 存规范化 origin（scheme+host+必要端口，不含 path/query/fragment），限定 scope 只存 02-01 定义的 opaque/keyed identifiers 与批准状态，绝不存显示名、完整邮箱或课程名。`source_observations` 分列/以严格、有限的 contract JSON 保存 current 与 last_success，不接受 arbitrary page/network payload；`account_bindings` 只允许 keyed fingerprint、组织/租户 fingerprint、证据类别与 candidate/confirmed/mismatch；`profile_ownership` 只允许 installation/build、PID、nonce/control-proof hash、OS start identity、受管 executable identity、started/lease/generation/fence/state，不存可重放的 raw nonce/control secret 或 Profile 绝对路径；`uat_receipts` 只允许脱敏机器字段。迁移在现有 WAL、FULL、busy timeout、foreign_keys 与单个 immediate transaction 内执行，成功后再写 `schema_migrations` v2 row 和 `PRAGMA user_version=2`。更新既有“未知 schema”测试使用大于 2 的版本，并增加 v1→v2/rollback 回归；禁止 drop/recreate Phase 1 表、直接复制活动 DB/WAL、自动读取或备份 Profile（D-03、D-05、D-09、D-14；T2-02）。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-persistence.test.ts tests/integration/job-storage.test.ts -t "schema v2|migration"</automated>
  </verify>
  <acceptance_criteria>
    1. v1 fixture 升级后 `user_version=2`，旧表逐值相等，五张 Phase 2 表存在，`foreign_key_check=[]` 且 `integrity_check=ok`；重开结果相同。
    2. 故障注入后 `user_version=1`、旧数据仍在且五张新表为 0 张；再次无故障打开才一次迁移成功。
    3. fresh database 只有一条有序 v2 migration 结果；`user_version=3` 或 migration 元数据不匹配明确抛 `SCHEMA_INCOMPATIBLE`。
    4. `pragma_table_info` 与 `sqlite_schema.sql` 断言没有 Profile path、cookie/storage、raw DOM/network、完整身份、课程名、截图或任意 capture/blob dump 字段。
    5. 自动结果只记 S/I；不创建 N/L receipt，不改变 Windows `not_run/human_needed` 或 Phase 3 blocked。
  </acceptance_criteria>
  <done>Phase 1 数据可在一个可回滚事务中迁移至隐私最小化 schema v2；失败没有半迁移，未知 schema fail closed，禁止资料没有持久化入口。</done>
</task>

<task type="auto" tdd="true">
  <name>02-02-2: 实现来源、观察、绑定与 Profile ownership fenced stores</name>
  <read_first>02-01-SUMMARY.md 中最终 domain/schema/port 导出；packages/application/src/ports.ts；packages/domain/src/model.ts；packages/contracts/src/index.ts；packages/persistence/src/database.ts；packages/persistence/src/runtime-status.ts；packages/persistence/src/sessions.ts；tests/integration/job-recovery.test.ts；02-CONTEXT.md D-03–D-07、D-09、D-17；02-RESEARCH.md T2-02/T2-06 及 PATTERNS 的 Transactions/last success/fences、Profile/process ownership。</read_first>
  <files>packages/persistence/src/auth.ts, tests/integration/auth-persistence.test.ts</files>
  <behavior>
    - RED：只有 `moodle`/`edstem`、strict normalized approved origin 与批准 scope 可写；extra URL/path/query/selector/JS/browser handle/download/write/raw payload 字段在事务前被 strict schema 拒绝。
    - RED：每来源 success 后的 NETWORK_UNAVAILABLE、PARSER_CHANGED、CAPABILITY_DENIED、AUTH_REQUIRED/REAUTH_REQUIRED 分别更新 current/error/checkedAt，但 last_success 和另一来源记录逐值不变；旧 checkedAt、未来时间、旧 generation 和错误 fence 均拒绝。
    - RED：同名/同邮箱但不同 stable subject fingerprint 不能变成 confirmed；mismatch 写入保留上次 confirmed binding 并把访问状态置为阻塞，不能自动覆盖或跨 source 合并。
    - RED：Profile 只有一个有效 owner；两连接竞争 acquire 仅一个成功，旧 fence 在 renew/release/clear 全部失败。lease expiry 本身不允许清除；只有与 owner identity+fence 匹配且由平台层确认 `exited` 的 transition 才能释放。
    - RED：关闭重开数据库后 source config、current/last-success、binding 与 ownership fence 一致；maintenance generation 改变后旧 writer 无法提交。
  </behavior>
  <action>新建 `packages/persistence/src/auth.ts`，严格实现 02-01 的 `SourceConfigStore`、`SourceObservationStore`、`AccountBindingStore`、`ProfileOwnershipStore`；不在 driver 内复制 domain 类型。所有 public writes 先以 02-01 strict schema parse，再进入 `BEGIN IMMEDIATE`，在同一事务调用 `requireWrite`、校验单调 checkedAt/generation/fence、执行条件 INSERT/UPDATE 并 `recordWrite`；SQLite busy/full/constraint 错误映射到现有脱敏 StorageError，不返回 SQL、值或路径。`SourceConfigStore.read(source)/confirm(config,context)` 只接受两个固定 source 的规范化已批准 origin 与 scope fingerprint/config version，更新 scope 必须生成新配置版本并保留来源隔离。`SourceObservationStore.read(source)/write(observation,context)` 保存 current 与 last_success：只有严格成功 observation 才替换 last_success；失败/未观察/partial 只更新 current/error/checkedAt，不清空 last_success，不把网络/parser/权限写成 unauthenticated，也不改另一来源（D-03、D-06；T2-06）。`AccountBindingStore.read()/write(binding,context)` 只接收由 application 使用安装 SecretStore 中 HMAC key 生成的固定长度 keyed fingerprint；SQLite 永不接触 raw subject、raw tenant、姓名、邮箱或 HMAC key。candidate→confirmed 需要 02-01 定义的严格证据或人工确认 authority；mismatch 为硬阻塞 observation，保留前一 confirmed 记录供本地核对，禁止按显示名/邮箱自动确认（D-04–D-07）。`SQLiteProfileOwnershipStore` 精确实现 `read/acquire(reservation,context)/renew(owner,leaseUntil,context)/markConfirmedExited(owner,context)/release(owner,context)`；接收 `ProfileOwnerIdentity` 作比较，但落库只记录 installationId/browserBuild/pid/nonceHash/controlProofFingerprint/osStartIdentity/managedExecutableIdentity/startedAt/generation/fence/leaseUntil/state，不保存或返回可重放的 raw nonce/control secret。acquire 原子递增 fence；其余方法要求完整 owner identity 的恒定时间摘要匹配、owner 自带当前 fence/generation，且另校验 `WriteContext`。租约仅 fencing，store 绝不根据时间、PID 缺失或锁文件推断退出；02-03 的 OS+authenticated-control proof 明确确认 exited 后才可调用 mark/release，且只能清理本安装记录（D-03、D-09）。不得保存 Profile 路径、锁文件内容、Cookie/storage state、捕获材料或完整身份。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-persistence.test.ts -t "source stores|last success|binding|profile ownership"</automated>
  </verify>
  <acceptance_criteria>
    1. 两个独立 SQLite 连接竞争同一 Profile owner 时恰有一个 acquire 成功；fence 递增，旧 owner 的 renew/release/observation commit 均以明确安全码失败。
    2. 将 leaseUntil 推到过去后记录仍存在且不能 acquire；只有匹配 installation/process identity、当前 fence/generation 和 `confirmed_exited` transition 才能释放，本 store 没有 kill/delete-lock 方法。
    3. Moodle success 后逐一写入 network/parser/capability/reauth 失败，查询仍返回原 last_success；EdStem 行未变化，confirmed empty/deleted 均未被推断。
    4. 相同显示文本但不同 keyed subject fingerprint 得到 `identity_mismatch`，课程访问 gate 为 blocked，原 confirmed binding 未被覆盖；raw subject/name/email sentinel 在 DB、WAL 和 SQLite backup 中均不存在。
    5. 任意 URL path/query、selector、JS、browser handle、Cookie、DOM/network capture、Profile path 或 screenshot extra field 均被 strict schema 拒绝且事务写入计数不变。
    6. 全部测试只产生 S/I 结果；Windows 与 L 单元仍为 `not_run/human_needed`，Phase 3 仍 blocked。
  </acceptance_criteria>
  <done>来源、状态、身份绑定和 Profile 持有权均以严格事务、last-success retention 与 generation/fence 持久化；租约不能冒充进程退出，禁止资料在主 DB/WAL/backup 中无入口且无残留。</done>
</task>

<task type="auto" tdd="true">
  <name>02-02-3: 实现 append-only UAT ledger 与不可串格证据门禁</name>
  <read_first>02-01-SUMMARY.md 的 UAT receipt/evidence writer contract；02-CONTEXT.md D-03、D-14、D-17；02-RESEARCH.md T2-08、Validation Architecture 和 Mandatory negative fixtures；02-VALIDATION.md Evidence ledger/Hard Gates；packages/persistence/src/auth.ts；packages/persistence/src/database.ts；tests/integration/auth-persistence.test.ts。不得把 fixture/native 运行描述成 live。</read_first>
  <files>packages/persistence/src/auth.ts, tests/integration/auth-persistence.test.ts</files>
  <behavior>
    - RED：receipt 由 platform×source×scenario/checkpoint×evidence class×artifact/build identity 精确寻址；A、B 三次重开、Worker restart、Codex exit、C、D、reauth 不能合并或互相满足。
    - RED：append 后没有 UPDATE/DELETE/replace 接口；相同幂等 receipt 重放返回原 event，冲突 payload 拒绝。纠错只能追加 linked correction，不改历史事件。
    - RED：synthetic/integration writer 只能写 S/I；macOS native writer 只能写 macOS N，不能写 Windows 或 L；macOS human-live writer 也不能写 Windows；payload 自称不同 authority 必须失败。
    - RED：任何 macOS pass 后，Windows 所有必需单元仍为 `not_run/human_needed`，总体 Phase 2 与 Phase 3 eligibility 仍 blocked；missing/failed/human_needed/not_run 不能通过 latest-row 或聚合漏洞消失。
    - RED：receipt/backup 只含版本、build/artifact identity、平台、来源、场景、时间、结果码、binding consistency、gap/next-action code；完整身份、课程名、origin path/query、Profile、Cookie、DOM/network 或截图字段拒绝且不落盘。
  </behavior>
  <action>在 `packages/persistence/src/auth.ts` 精确实现 02-01 的 `EvidenceLedger.append(receipt,authority,context)/list(key)`。采用只追加 INSERT 和稳定 event id/idempotency hash；数据库权限/实现不暴露通用 update/delete/upsert-replace。每个 event 强制保存 schema version、immutable prior/correction link、build/artifact identity、platform、source、单一 scenario/checkpoint、evidence class、result、safe code、binding consistency、observedAt/recordedAt 与缺口 code；不存自由文本私人资料。将可信 writer authority 放在 payload 外：automated authority 只能写 S/I/N（N 必须匹配其实际 native OS），human_action authority 只能写 L、必须带后续硬人工门禁生成的真实 action receipt id 且平台相同；本计划的 autonomous tests 无 human_action authority，因此不能生成 L。事务内交叉校验 authority/platform/evidence/source/scenario/build，拒绝 macOS→Windows、S/I/N→L、fixture→native/live 与一个 event 覆盖多个 checkpoint。读取时从 append-only events推导每个possible cell；没有合格event时保持`not_run/human_needed`，failed/human_needed不被较低证据或其他平台pass覆盖。Phase2Gate消费02-35在release前锁定的44个required L keys与named S/I/N obligations，绝不默认整个176-cell Cartesian product都required；即使所有macOS自动结果为pass，Windows required L与live缺口仍阻止整体通过（D-03、D-14、D-17；T2-08）。correction只能追加并引用现有同cell event，保留原记录和纠错理由code；禁止删除失败或回填live。测试用真实数据库重开、两个连接竞争重放/冲突、平台/证据污染矩阵、主DB/WAL/backup sentinel扫描证明。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-persistence.test.ts -t "append-only UAT ledger|evidence isolation|privacy backup"</automated>
  </verify>
  <acceptance_criteria>
    1. 数据库重开后所有 receipt 顺序、event id、correction link 和原始失败事件仍在；store API 与执行 SQL 均无历史 UPDATE/DELETE，冲突 idempotency payload 明确拒绝。
    2. 完整污染矩阵逐项断言：S→I/N/L、I→S/N/L、macOS N→Windows、macOS N→L、macOS L→Windows、fixture→N/L 全部失败且 ledger row count 不变。
    3. A、B1-1/B1-2/B1-3、B2、B3、C、D、reauth 任一缺失、failed、human_needed 或 not_run 时总体 gate 为 blocked；24h D 不能由 reauth 或较早 receipt 替代。
    4. 写入全部允许的 macOS S/I/N 测试事件后，所有 Windows 必需单元仍精确为 `not_run/human_needed`，L 仍空缺，Phase 1/2 不标 complete，Phase 3 eligibility=false。
    5. DB/WAL/SQLite backup 的 schema、行和字节 sentinel 扫描均找不到 full name/email/course name、Profile path/content、Cookie/storage state、password/MFA/input、raw DOM/HTML/request/response、screenshot/HAR/trace/video/console；receipt 查询也没有这些字段。
    6. `npm run typecheck` 与定向 integration 测试通过；自动结果记录为 S/I，绝不写 live pass。
  </acceptance_criteria>
  <done>UAT 结果单以不可改历史、可信写入 authority 和精确 cell key 持久化；macOS 与自动证据无法污染 Windows/L，所有硬门禁缺口继续可见并阻止 Phase 3。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input crossing it | Required control |
|---|---|---|
| 02-01 contracts/application → SQLite persistence | 来源配置、observation、binding、ownership 与 receipt payload | strict schema、长度/枚举/组合约束、事务内 generation/fence、无 generic JSON capture |
| Browser/Profile coordinator → ownership store | PID/lease/退出声明可能陈旧、重用或伪造 | installation+nonce+OS start identity+managed executable identity+control proof；lease 仅 fencing，confirmed exit 才清理 |
| S/I/N/L producer → UAT ledger | producer 可伪称 platform/evidence/result | payload 外可信 writer authority；platform/source/scenario/evidence/build 精确交叉校验；append-only |
| SQLite/WAL/backup → diagnostics/export/model | 本机数据可能被误当可公开证据 | 禁止敏感字段入库；脱敏 receipt schema；主 DB/WAL/backup sentinel tests；Profile 永远不在备份集合 |

## STRIDE Threat Register

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 control intent | Mitigation plan | Blocking test |
|---|---|---:|---|---|---|---|---|
| T2-02 | I | Critical | schema、stores、DB/WAL/backup | mitigate | V1 数据分类/最小化，V8 敏感数据保护，V16 安全日志 | 仅保存不可逆 keyed fingerprint 与脱敏枚举；无 Profile/Cookie/raw capture/full identity/screenshot 字段；strict parse + backup sentinel scan | `auth-persistence.test.ts` privacy schema/backup；任一残留阻止 02-03、02-08、02-13 与 live |
| T2-04 | S/E | Critical | Profile ownership store | mitigate | V1 架构边界，V4 访问控制，V13 受控接口 | 完整 owner identity、单调 fence、generation；lease 不等于退出；仅 confirmed-exited transition 可清理 | ownership race/expired lease/stale fence/PID reuse cases；失败阻止浏览器实现 |
| T2-06 | T/R | High | source observation 与 last_success | mitigate | V1 完整性设计，V4 授权状态，V13 API 数据完整性 | current/error/last_success 分开；单调 checkedAt；事务 rollback；跨来源隔离 | network/parser/permission/reauth retention matrix；任何成功丢失或状态折叠阻止后续 |
| T2-08 | T/R/E | High | UAT ledger 与 Phase 3 gate | mitigate | V1 信任级别分离，V4 authority，V13 schema/业务约束，V16 可审计事件 | payload 外 writer authority、精确 cell key、append-only correction、跨平台/证据污染拒绝 | evidence contamination matrix；任何 macOS→Windows 或 S/I/N→L 写入阻止 release/live/Phase 3 |

ASVS 5.0 L1 在此作为适用控制意图，不宣称完整合规。所有 Critical/High threat 的阻断测试、隐私扫描和迁移回滚必须通过；失败保持真实 failed/blocked，不可降级为警告或延期债务。
</threat_model>

<verification>
1. 每个 task 先写 RED 测试，再实现 GREEN，并在提交前运行其定向 integration 命令；无 watch mode、无真实学校网络、无 Profile/私人样本。
2. 计划完成后运行：`node scripts/dev/runtime.mjs npm run typecheck && node scripts/dev/runtime.mjs npm run test:unit -- --run && node scripts/dev/runtime.mjs npm run test:integration -- --run`。
3. 用真实临时 SQLite 文件、WAL、关闭重开、并发连接、迁移故障和 `db.backup()` 产物验证；fixture 仅是 S/I，不得标 N/L。
4. 断言 Phase 1 jobs/runtime/maintenance/session 行为回归不变；migration failure 不破坏旧 DB，generation/fence 失败没有部分写入。
5. 自动化只证明数据层合约。官方登录/MFA、native 生命周期、macOS A/B/C/D/reauth 和 Windows 全矩阵属于后续硬人工/原生计划；本计划不得执行或代填。
</verification>

<success_criteria>
- Phase 1 schema 可事务升级并回滚，所有既有数据和维护门禁保留；真实 DB 重开和 integrity/foreign-key checks 通过。
- approved source/scope、per-source current/last-success、keyed binding 与 Profile ownership fence 均通过 strict contract 和事务/generation/fence 写入。
- 失败不会清除最近成功，不同来源不会互相覆盖，identity mismatch 不会自动改绑或继续课程访问。
- append-only UAT ledger 在数据库层拒绝跨平台、跨 evidence class、跨 scenario 的替代；macOS 自动结果不改变 Windows/L，Phase 3 始终 blocked。
- DB、WAL 与 SQLite backup 不含 Profile path/content、Cookie、原始 DOM/网络资料、完整身份、课程名、截图或其他禁止 capture；Profile 从未进入备份集合。
- T2-02/T2-04/T2-06/T2-08 的 High/Critical 阻断反例全部通过；未通过即保持 blocked，不产生完成或 live 声明。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-02-SUMMARY.md`，只记录脱敏的 S/I 命令结果、迁移版本、测试数量和真实缺口；不得记录 Profile 路径、身份、课程、学校 origin path、数据库内容或 live pass。
</output>
