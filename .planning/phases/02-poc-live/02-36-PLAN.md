---
phase: 02-poc-live
plan: "36"
type: execute
wave: 8
depends_on: ["02-08", "02-09", "02-35"]
files_modified:
  - packages/application/src/live-checkpoints.ts
  - apps/api/src/auth.ts
  - apps/api/src/main.ts
  - apps/status/src/main.ts
  - tests/integration/live-checkpoint-workflows.test.ts
  - tests/ui/auth-live-actions.spec.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "The installed paired server can issue and consume durable action bundles for A1, A2, B1 rounds 1-3, B2, B3, C, D and reauth on macOS and Windows."
    - "Every bundle is created only after paired-session, same-origin, CSRF, current immutable build, platform, source, config/scope/binding, generation and predecessor checks."
    - "Actual bounded results mint payload-external human_action authority and transactionally append only the exact required L cell; a failed/human_needed result is retained and never becomes pass."
    - "B3, C, D and reauth bundles remain recoverable after Codex exit/API restart/OS reboot without reusing an ordinary restart-revoked login correlation."
    - "Public, CLI, MCP, repo scripts, feedback JSON and stale installed builds have no endpoint or port that can append/correct L."
  artifacts:
    - path: packages/application/src/live-checkpoints.ts
      provides: "Production workflow orchestrator for every live scenario"
      exports: [PairedLiveCheckpointService]
    - path: apps/api/src/auth.ts
      provides: "Fixed paired routes for live action issue/resume/result and read-only receipt access"
      contains: /api/auth/live-action
    - path: apps/status/src/main.ts
      provides: "Scenario-bound paired controls that never expose action secrets or mint evidence client-side"
    - path: tests/integration/live-checkpoint-workflows.test.ts
      provides: "Restart/reboot recovery, authority, atomic append and negative API coverage"
  key_links:
    - from: apps/api/src/auth.ts
      to: packages/application/src/live-checkpoints.ts
      via: "strict paired routes call only scenario-specific service methods"
      pattern: "PairedLiveCheckpointService"
    - from: packages/application/src/live-checkpoints.ts
      to: packages/persistence/src/auth.ts
      via: "durable issue/resume and transactional consumeAndAppend"
      pattern: "issue|resume|consumeAndAppend"
---

<objective>
Wire the durable live-action store into the paired production server and UI so the signed beta can produce every required L result itself.

Purpose: Make A/B/C/D/reauth reachable on both native platforms while preserving all hard human actions and D-02–D-18 privacy/evidence boundaries.
Output: Paired-server workflow, fixed routes/UI controls, and restart/reboot/negative tests. Automated tests use synthetic outcomes only and do not write live evidence.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-35-SUMMARY.md
@apps/api/src/auth.ts
@apps/status/src/main.ts
@packages/application/src/live-checkpoints.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement paired-server scenario workflows and L authority</name>
  <files>packages/application/src/live-checkpoints.ts, apps/api/src/auth.ts, apps/api/src/main.ts, tests/integration/live-checkpoint-workflows.test.ts</files>
  <behavior>
    - Scenario table covers both platforms and every A1/A2/B1-1/B1-2/B1-3/B2/B3/C/D/reauth source result with exact predecessor rules.
    - Issue/resume/result routes are fixed strict objects; caller authority, URL/selector/JS/credential/Profile fields and wildcard cells are rejected before service calls.
    - Restart/reopen recovers B3/C/D/reauth actions by opaque correlation and current server state, while expiry/replay/build or generation drift fails closed.
    - Only an actual sealed probe/lifecycle result and matching durable action cause one transactional L append; failure retains last success and failure audit.
  </behavior>
  <action>Extend the 02-35 service into a closed scenario table per D-12/D-16. Add explicit issue/resume/result methods for A1 login, A2 binding/course visibility, each B1 round, B2 Worker restart, B3 Codex exit, C OS restart, D 24-hour recheck, and reauth. Each method must validate the exact predecessor cells and current immutable build/generation/config/scope/binding/profile identity. Add fixed paired+same-origin+CSRF API routes; do not add dynamic operation or arbitrary cell routes. The server creates child actions per source when required and derives `EvidenceWriterAuthority {kind:'human_action'}` only after the actual result is committed. Consume action and append exact L result atomically through 02-35. Keep 02-08 `LoginActionRegistry` in-process and restart-revoked for ordinary window opening only; tests must prove its ID cannot be supplied where durable action authority is required.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/live-checkpoint-workflows.test.ts tests/integration/auth-api.test.ts</automated></verify>
  <done>The paired installed backend has a production, restart-safe and least-authority path to every live result required by later checkpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose scenario-bound paired controls and recovery states</name>
  <files>apps/status/src/main.ts, tests/ui/auth-live-actions.spec.ts</files>
  <behavior>
    - UI controls are derived from server next-action projections and submit only opaque action IDs plus exact allowed acknowledgement/result enums.
    - Action IDs never enter DOM attributes, storage, URL, clipboard, diagnostics or logs.
    - 401/403 clears actions; network failure retains a stale read-only view; restart/reboot recovery shows the durable pending scenario without creating a new action.
    - D has no skip and uses server time; reauth remains separate; Windows state never inherits macOS results.
  </behavior>
  <action>Per the approved UI-SPEC, wire the checkpoint ledger’s current CTA to the new fixed paired routes. Keep opaque correlations only in module memory during a live page session; after restart, request a server-projected resumable action by checkpoint/platform rather than accepting an ID from the user. Render failed/human_needed/not_run/pass exactly, preserve the single primary CTA and privacy purge rules, and prohibit client-side receipt/L construction. Include synthetic browser tests for every scenario, restart recovery, replay, 401/403, D time gate and cross-platform isolation.</action>
  <verify><automated>npm run test:ui -- --grep "durable live action|checkpoint recovery|payload external authority|platform isolation" &amp;&amp; npm run typecheck</automated></verify>
  <done>The paired UI can drive and resume every installed workflow without exposing correlations or gaining evidence-writer authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| paired UI → API | Session, CSRF, scenario and action references are untrusted. |
| API → workflow | Only fixed methods may reach durable actions or browser/lifecycle operations. |
| actual result → L authority | Authority is server-held and created only after exact result validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-36-01 | S/E | live-action routes | mitigate | Paired session, same-origin, CSRF, strict fixed route/body and no dynamic dispatch. |
| T2-36-02 | T/R | action recovery | mitigate | Immutable build/platform/source/scenario/generation/parent binding, expiry and one-time consumption. |
| T2-36-03 | E | L writer authority | mitigate | Server-only payload-external authority and atomic exact-cell append. |
| T2-36-04 | I | UI/action output | mitigate | Opaque in-memory IDs, protected projection, 401/403 purge and sentinel tests. |
</threat_model>

<verification>
Run targeted integration/UI suites, then full unit/integration/UI regression. Assert automated fixtures cannot create human_action authority and all current Windows/L cells remain unchanged.
</verification>

<success_criteria>
- Every later human checkpoint has a signed-production workflow capable of creating its exact evidence.
- B3/C/D/reauth survive the lifecycle boundary they are intended to test.
- All login/MFA/restart/24-hour/reauth actions remain hard human gates.
- No public or repo-side writer exists.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-36-SUMMARY.md` with fixed routes, scenario coverage and S/I test evidence only.
</output>
