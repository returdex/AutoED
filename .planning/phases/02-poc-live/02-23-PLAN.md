---
phase: 02-poc-live
plan: "23"
type: execute
wave: 23
depends_on:
  - "02-22"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "reauth 只有在 02-22 completed SUMMARY 与 current Moodle/EdStem `D_24h_recheck×macos×L` exact cells 都 strict pass 时才能开始；D 的 ≥24h 无干预 pass 不能被 reauth 合并或替代。"
    - "优先等待 Moodle 或 EdStem 任一来源自然过期；产品/agent 不改系统时钟、不删/改 Cookie、storageState、Profile/session、不伪造 401/过期、不调用学校登出或连续 probe 来制造 reauth。"
    - "项目没有已批准的‘合理观察期’时长，因此 agent/product 不选择天数。若尚未自然过期，用户必须给出 exact wait-until；期限前保持 waiting，期限到且仍未过期后，用户还必须明确授权一次 Moodle logout fallback。"
    - "自然过期只暂停实际失效来源并保留 last-success；另一来源若仍有效继续显示可用。最多三次有界只读恢复遵守既有 0/5s/30s 与总 deadline，任何 MFA/interaction 立即停止，不自动开窗或输入。"
    - "显式 Moodle 登出先记录 logout intent，再由用户本人在官方页面执行；产品尊重 intent，取消/拒绝该来源 background/manual recovery，不自动重开，不批量清 Cookie/session，不损坏专属 Profile 或 EdStem 状态。"
    - "只有用户点击实际失效来源的重新登录后才打开其官方窗口并本人输入密码/MFA；目标为 Moodle 时，Moodle positive auth/identity/binding 恢复后才创建一个新的 EdStem fixed auth probe，不能复制 Moodle pass或假设共享SSO；目标为EdStem时Moodle保持unaffected且不被重登。"
    - "Moodle reauth 与 EdStem follow-up 分别形成 source-specific `reauth×macos×L` append-only results；paired server 是唯一 human_action writer，repo gate只读，failed/human_needed不能被旧pass/S/I/N/另一source覆盖。"
    - "独立 reauth feedback 与 canonical verifier 都 strict pass 才允许 02-24；Windows仍全矩阵 not_run/human_needed，Phase1/2未整体完成，Phase3 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-22-SUMMARY.md"
      provides: "真实用户完成 D 至少24小时无干预复查的脱敏 prerequisite；不能替代 D/reauth ledger cells"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "D/reauth exact-cell read-only gate 与 Windows matrix read-only audit；不持有 L writer/DB/Profile/browser权限"
      contains: "verifyPhase2LiveCheckpoint"
    - path: "packages/application/src/auth-jobs.ts"
      provides: "显式 logout intent、source-isolated cancellation/recovery、user_login_completed 与 Moodle→EdStem follow-up 状态机"
      contains: "recordExplicitLogout"
    - path: "packages/persistence/src/auth.ts"
      provides: "logout intent/last-success/per-source observations 与 append-only reauth EvidenceLedger"
      contains: "EvidenceLedger"
    - path: "apps/status/src/main.ts"
      provides: "paired protected observation decision、one-time Moodle logout/re-login、EdStem follow-up 与 strict feedback UI"
      contains: "重新登录"
    - path: ".planning/phases/02-poc-live/02-23-SUMMARY.md"
      provides: "只在两source reauth exact L results和真实feedback strict pass后创建的02-24 prerequisite；不创建reauth planning receipt"
  key_links:
    - from: ".planning/phases/02-poc-live/02-22-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "completed SUMMARY + canonical D verifier 双前置；文本、时间戳或一个source不能代替D exact cells"
      pattern: "D_24h_recheck|--checkpoint D|pass"
    - from: "user observation decision"
      to: "paired protected reauth UI"
      via: "自然过期优先；无approved duration时用户给exact wait-until，期限到仍authenticated后另行一次明确Moodle logout授权"
      pattern: "wait_until|fallback_moodle_logout|authorized_once"
    - from: "POST /api/auth/logout-intent"
      to: "packages/application/src/auth-jobs.ts"
      via: "paired+same-origin+CSRF固定Moodle intent原子取消该source queued/retry/running并阻止自动恢复；不接受URL/Cookie/storage操作"
      pattern: "recordExplicitLogout|cancelSourceForLogout"
    - from: "Moodle user_login_completed"
      to: "EdStem follow-up auth probe"
      via: "Moodle current positive marker/identity/binding commit后才创建全新EdStem fixed auth probe；不复制session/pass"
      pattern: "user_login_completed|edstem.auth_probe"
    - from: "paired server reauth workflow"
      to: "packages/persistence/src/auth.ts"
      via: "source-specific one-time action correlation+actual result同事务append Moodle/EdStem reauth L events；public API/repo tool无writer"
      pattern: "human_action|reauth|actionReceiptId"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-24 eligibility"
      via: "reauth verifier证明two-source exact pass；Windows matrix audit同时证明macOS writes未触碰任一Windows cell且Phase3仍blocked"
      pattern: "--checkpoint reauth|--audit-matrix|windows"
---

<objective>
在 D 独立通过后执行 macOS reauth 硬人工门：优先等待 Moodle 自然过期；若用户自行确定的合理观察期限已到仍未过期，用户明确授权并亲自在官方页面退出 Moodle。产品尊重 logout intent、不破坏 Profile/Cookie/session、不自动重开；用户随后明确点击重新登录并亲自完成密码/MFA，Moodle恢复后才运行新的 EdStem 有界认证复查。

Purpose: 落实 D-08、D-10–D-12、D-14、D-16 与 AUTH-02/AUTH-03/SEC-02/UAT-01，分别证明自然/显式失效、source isolation、logout-intent persistence、user-controlled reauth 和 Moodle→EdStem follow-up，而不是通过破坏会话、自动弹窗、复制状态或合并 D 制造通过。
Output: 一个用户定义观察期限/一次性 fallback consent 决定门、一个真实 reauth human-action 门、Moodle/EdStem 各自的 `reauth×macos×L` results、reauth/Windows read-only gates及 strict pass SUMMARY；无 reauth planning receipt、不访问课程内容、不运行Windows。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-17-SUMMARY.md
@.planning/phases/02-poc-live/02-21-SUMMARY.md
@.planning/phases/02-poc-live/02-22-PLAN.md
@.planning/phases/02-poc-live/02-22-SUMMARY.md
@packages/domain/src/model.ts
@packages/application/src/auth.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs
@tests/integration/phase2-live-gate.test.ts

<interfaces>
执行时先读取 02-22 SUMMARY 与已发布 beta 的真实导出；只允许机械适配符号名，不得更改以下 scenario、authority、D prerequisite 或 reauth pass语义：

```typescript
export type UatScenario =
  | 'A1_login' | 'A2_binding' | 'A2_course_visibility'
  | 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3'
  | 'B2_worker_restart' | 'B3_codex_exit'
  | 'C_os_restart' | 'D_24h_recheck' | 'reauth';

export type EvidenceWriterAuthority =
  | {kind:'automated';evidence:'S'|'I'|'N';platform:'macos'|'windows';producerId:string}
  | {kind:'human_action';evidence:'L';platform:'macos'|'windows';actionReceiptId:string};

export interface AuthJobService {
  requestProbe(command: AuthProbeCommand, context: WriteContext): Promise<{jobId:string}>;
  recordExplicitLogout(source: SourceId, context: WriteContext): Promise<AuthFlowState>;
  cancel(jobId:string, source:SourceId, context:WriteContext): Promise<AuthJob>;
  query(jobId:string, source:SourceId): Promise<AuthJob|null>;
}
```

D 没有可替代的 planning receipt authority。reauth preflight exact command如下；必须再要求 `.planning/phases/02-poc-live/02-22-SUMMARY.md` completed/current，二者缺一即停：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint D --runtime current --platform macos --read-only
```

02-37 已在 identity selection/signing 前实现并穷尽测试同一 repo-side gate 的 reauth grammar；02-23 只消费且不得修改 gate/script tests。record仍只strict parse executor-owned 0700 mktemp/0600 feedback并经redacted API核对current build/artifact/runtime status与existing exact events；它不能append/read DB/Profile/browser/source：

```typescript
export interface ReauthObservationDecision {
  schema: 1;
  checkpoint: 'reauth-observation';
  naturalExpiry: 'moodle_observed' | 'edstem_observed' | 'not_observed';
  waitUntil: string | 'not_applicable';
  fallbackSource: 'moodle' | 'not_applicable';
  fallbackLogoutConsent: 'authorized_once' | 'not_authorized' | 'not_applicable';
  decidedAt: string;
}

export interface Phase2ReauthFeedback {
  schema: 1;
  checkpoint: 'reauth';
  platform: 'macos';
  path: 'natural_expiry' | 'explicit_moodle_logout';
  targetSource: 'moodle' | 'edstem';
  result: 'pass' | 'failed' | 'human_needed';
  resultCode: ReauthSafeCode;
  logoutIntent: 'not_applicable' | 'recorded' | 'missing';
  automaticReopen: 'none' | 'detected' | 'not_observed';
  moodle: 'reauthenticated' | 'unaffected_authenticated' | 'failed' | 'human_needed';
  moodleCode: SafeAuthErrorCode;
  edstem: 'reauthenticated' | 'followup_authenticated' | 'followup_recovered' | 'unaffected_authenticated' | 'failed' | 'human_needed';
  edstemCode: SafeAuthErrorCode;
  identityScope: 'consistent' | 'mismatch' | 'not_observed';
  sourceIsolation: 'preserved' | 'violated' | 'not_observed';
  lastSuccess: 'preserved' | 'lost' | 'not_observed';
  contentBoundary: 'preserved' | 'violated' | 'not_observed';
  screenshot: 'not_used';
  windows: 'not_run/human_needed';
  phase3: 'blocked';
  observedAt: string;
}

export type ReauthSafeCode =
  | 'REAUTH_COMPLETE'
  | 'D_PRECONDITION_FAILED'
  | 'NATURAL_EXPIRY_WAITING'
  | 'OBSERVATION_DECISION_REQUIRED'
  | 'LOGOUT_CONSENT_REQUIRED'
  | 'LOGOUT_INTENT_NOT_RECORDED'
  | 'AUTO_REOPEN_DETECTED'
  | 'MOODLE_REAUTH_FAILED'
  | 'EDSTEM_REAUTH_FAILED'
  | 'EDSTEM_FOLLOWUP_FAILED'
  | 'INTERACTION_REQUIRED'
  | 'IDENTITY_MISMATCH'
  | 'PROFILE_OWNERSHIP_UNCONFIRMED'
  | 'CONTENT_BOUNDARY_VIOLATION'
  | 'RESULT_NOT_OBSERVED';

export function parsePhase2ReauthObservationDecision(value:unknown): ReauthObservationDecision;
export function parsePhase2ReauthFeedback(value:unknown): Phase2ReauthFeedback;
```

Canonical reauth commands：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --record --checkpoint reauth --feedback <0700-mktemp>/reauth-result.json --runtime current --platform macos --read-only-inputs

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform macos --read-only
```

`REAUTH_COMPLETE` 唯一合法 conjunction：D current pass；two-source exact `reauth×macos×L` current/non-corrected pass；path 是任一来源真实自然过期，或用户deadline已到后的one-time explicit Moodle logout；若显式则targetSource=moodle且logoutIntent=recorded；automaticReopen=none。target=moodle时Moodle reauthenticated/positive marker/identity consistent且EdStem是followup_authenticated或followup_recovered；target=edstem只允许natural path，EdStem reauthenticated且Moodle=unaffected_authenticated、Moodle新login/probe=0。两分支都要求identityScope consistent、sourceIsolation/lastSuccess/contentBoundary preserved、screenshot not_used、Windows/Phase3固定。自然过期path不得有logout intent，显式path不得缺user decision/期限/consent。

reauth 是 02-37 closed grammar 中的 pass-only stdout planning handoff branch：它不接受 `--out`，不得写任何文件。failed/human_needed、missing/duplicate/corrected/conflicting event、decision/path/runtime disagreement或敏感输出均 nonzero，只保留 server runtime 的 append-only failure/human_needed event；repo filesystem/runtime ledger 必须保持不变。

paired server 是唯一L writer。它为Moodle/EdStem分别建立durable correlation并绑定 `checkpoint=reauth,source,scenario=reauth,platform=macos,current version/build/generation,approved config/scope,binding,observationDecisionDigest,path,logoutIntentId(if explicit),login action/actual results,paired session`；user/CLI/public API/repo gate不能传actionReceiptId。Moodle actual positive result与其one-time correlation同事务append；只有该commit后才mint新的EdStem follow-up correlation并运行fixed `edstem.auth_probe`。EdStem background job不能自升L，只能继承server-held本轮reauth workflow provenance；failed/human_needed也append immutable event。若beta缺此链，human_needed并回到修复→全测→新beta→更新→重做live gates，不用script/DB/public receipt route补写。

02-24 可只读消费的Windows matrix audit由本计划固定为：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only
```

strict pass output仅为 `{schema:1,status:'pass',audit:'platform_matrix',platform:'windows',requiredCellCount:number,notRunCellCount:number,humanNeededCellCount:number,passCellCount:0,macosWritesApplied:0,phase3:'blocked'}`，并要求required/notRun/humanNeeded计数相等。它只读required-cell projection，不创建/初始化/修正Windows row，不接受receipt payload/source/scenario override；任一Windows pass、macOS event影响Windows、缺cell或Phase3抬高均nonzero。
</interfaces>

<hard_gates>
- D与reauth独立；D verifier+02-22 SUMMARY缺一不可。D时间不能直接当自然过期观察期，reauth不能写回D。
- 没有已批准观察时长。用户未给exact wait-until时不能宣称“合理观察期已过”；wait-until未到必须暂停，不poll学校、不创建logout intent。
- fallback仅允许用户一次性明确授权Moodle logout；授权不等于已登出、不授权自动点击/清Cookie/销毁session/Profile。
- password/MFA仅用户在official page输入；产品/agent不请求、fill/type/press/submit/read input，不capture。
- failed/human_needed停止02-24；Windows保持not_run/human_needed，Phase3 blocked。
</hard_gates>

<tasks>

<task type="auto" tdd="true">
  <name>02-23-1：消费签名 reauth/Windows gates，并核验 D/current source baseline</name>
  <read_first>AGENTS.md 的 Credentials and login、Data integrity and verification；02-CONTEXT.md D-08、D-10–D-12、D-14、D-16；02-RESEARCH.md auth state/retry建议与T2-03/T2-06；02-VALIDATION.md natural expiry/explicit logout；02-22-PLAN.md与02-22-SUMMARY.md exact D contract；02-04/02-07 SUMMARY状态机；02-08/02-09 SUMMARY paired API/UI；scripts/release/phase2-live-gate.mjs；tests/integration/phase2-live-gate.test.ts。</read_first>
  <files>None (consume the signed installed gate read-only)</files>
  <behavior>
    - RED：reauth pass只接受current D + exact two-source reauth L events + valid natural/consented path + source isolation/identity/last-success/content完整conjunction。
    - RED：改时钟、删Cookie/storage/Profile、强制401、期限未到、无decision/consent、auto reopen、Moodle未commit先probe EdStem、跨source/scenario/platform/S/I/N代填全部fail closed。
    - RED：Windows matrix audit要求所有required cells not_run/human_needed、pass=0、macOSWritesApplied=0、Phase3 blocked；只读且不初始化row。
  </behavior>
  <action>消费02-37已完成、02-38已测试且02-39已签名的 reauth decision/record/verify与Windows audit branches，核对installed hash/capability manifest并原样运行existing regression/negative suite；本计划不编辑脚本或测试。record只核对feedback与server已有exact cells/status，不append；verify不依赖chat/SUMMARY；audit只读exact requiredness projection。若branch/hash/capability缺失，必须完整修复、全测、发布新immutable beta并实际更新，不能在live计划补分支。

运行D canonical verifier并要求02-22 SUMMARY current/completed；核对D two-source current-build exact pass、binding/scope/Profile observation一致、D与reauth cells分离。读取current redacted per-source auth/last-success/logout-intent/retry state，不发新probe：若Moodle或EdStem已自然`reauth_required`，只记录对应`moodle_observed|edstem_observed`候选，不自动开窗；若两者仍authenticated，仅显示`not_observed`，不能推断观察期结束。要求reauth cells/pending correlation/logout intent均空，另一来源当前状态独立，Windows audit exit0。任何D drift、预填reauth/logout、identity mismatch、last-success丢失、Windows污染或敏感输出阻止Task2。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts tests/integration/auth-worker.test.ts tests/integration/auth-security-matrix.test.ts &amp;&amp; test -s .planning/phases/02-poc-live/02-22-SUMMARY.md &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint D --runtime current --platform macos --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. reauth decision/parser/verifier与Windows audit positive/negative matrix通过，既有checkpoint regression不变；repo gate无append/DB/Profile/browser/source request权限。
    2. D verifier exit0且02-22 SUMMARY completed/current；两source `D_24h_recheck×macos×L` pass，reauth cells完全独立且空。
    3. baseline只读表达Moodle/EdStem哪一来源自然reauth_required或两者authenticated-not_observed，不触发probe/window/logout/expiry；last-success保留，另一来源独立。
    4. reauth pending correlation/logout intent/action id为空；无时钟/Cookie/storage/Profile/session修改或模拟学校错误。
    5. Windows matrix全部required cells not_run/human_needed、pass=0、macosWritesApplied=0、Phase3 blocked；audit不写row。
    6. stdout/stderr/test artifacts无identity/course/origin/path/Profile/Cookie/session/action/raw network/截图；任一non-pass阻止decision checkpoint。
  </acceptance_criteria>
  <done>D/current baseline与read-only gates strict pass，reauth尚未被执行且无副作用时才进入用户观察期/consent决定；否则保持blocked。</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>02-23-2：由用户确定自然过期观察期限与一次性 Moodle logout fallback consent</name>
  <read_first>Task1的脱敏current Moodle auth状态与observedAt；02-CONTEXT.md D-10/D-16；02-VALIDATION.md“优先自然过期、未过期才主动退出一个来源”；当前beta protected reauth observation panel。不得读取Cookie/session/Profile/full identity或学校页面。</read_first>
  <files>none（决定只作为本轮hard-gate input与server-side digest，不写源码/receipt/DB raw值）</files>
  <decision>项目没有批准固定观察时长。用户必须决定：当前已真实观察到Moodle或EdStem自然过期，或为继续等待自然过期给出一个exact wait-until并明确该时间到后两者仍未过期时是否一次性授权Moodle logout fallback。</decision>
  <context>agent/product不能把D的24小时自动当reauth观察期，也不能建议/选择天数。自然过期已经出现时绝不logout；仍authenticated时，deadline与fallback consent必须来自用户，期限前不允许推进。授权只允许后续UI呈现一次logout动作，用户仍需再次亲自点击；不授权自动清Cookie/session/Profile。</context>
  <options>
    <option id="natural-expiry-observed">
      <name>自然过期已观察</name>
      <pros>不制造失效，隔离实际过期来源并只由用户重登录该来源。</pros>
      <cons>仅在current protected status真实显示Moodle或EdStem之一reauth_required时可选；用户自称或旧状态不可用。</cons>
    </option>
    <option id="user-defined-deadline">
      <name>用户定义继续观察期限</name>
      <pros>保留自然过期优先，由用户给出确切时间；到期后仍未过期才可能使用一次Moodle logout fallback。</pros>
      <cons>wait-until前必须等待；fallback仍需明确authorized_once且之后由用户再次点击实际logout。</cons>
    </option>
  </options>
  <action>必须等待用户返回下方exact decision block，不得推荐/填入日期、用当前时间代替、从D推算或自动批准。strict parse后与current trusted time/status交叉核对：`natural_expiry=moodle_observed|edstem_observed`只在server current state对应来源为reauth_required且有自然expiry provenance时接受，wait/consent必须not_applicable；否则必须`natural_expiry=not_observed`、用户提供带timezone的future-or-current exact wait_until、fallback_source=moodle，并明确`fallback_logout_consent=authorized_once|not_authorized`。若wait_until未到，checkpoint保持waiting并要求用户届时回到同一paired UI；期间只读取本地current状态，不poll学校。到期时重新读取：若任一来源自然过期已发生，强制走该source natural path并忽略logout authorization；若两者仍authenticated，只有authorized_once才可呈现Task3 Moodle logout动作，not_authorized继续waiting/human_needed。decision仅保存规范化allowlisted digest/time/enums，不保存解释/free text。</action>
  <verify>
    <automated>使用`parsePhase2ReauthObservationDecision` strict parser验证用户block；对trusted current time执行`now &gt;= waitUntil`判定并只读核对Moodle current auth/expiry provenance。此自动校验不得调用学校、修改时钟/session或创建logout intent；期限未到必须返回waiting而非pass。</automated>
  </verify>
  <acceptance_criteria>
    1. 用户block是唯一duration/consent来源；agent/product未建议、默认或推算合理时长，D 24h未被复用。
    2. natural path仅在current server真实观察对应Moodle或EdStem reauth_required时成立；只隔离该来源且不创建logout intent。
    3. 非natural path含exact timezone wait-until；期限前保持waiting、学校请求=0、logout intent=0、reauth cell=0。
    4. deadline到时重新优先检查自然expiry；若发生即禁止logout；若未发生，只有authorized_once允许下一步，not_authorized保持blocked。
    5. consent只授权一次Moodle fallback UI动作，不授权agent点击、自动logout、Cookie/session/Profile操作或其他source。
    6. decision输出无identity/course/origin/Profile/Cookie/session/path/action ID；Windows/Phase3不变。
  </acceptance_criteria>
  <done>只有自然Moodle过期被真实观察，或用户deadline已到、仍未过期且一次性fallback consent明确时才完成；否则持续waiting/human_needed。</done>
  <resume-signal>只回复exact `02-23_REAUTH_OBSERVATION_DECISION` block：`schema=1`、`checkpoint=reauth-observation`、`natural_expiry=moodle_observed|edstem_observed|not_observed`、`wait_until=&lt;ISO-8601 with timezone&gt;|not_applicable`、`fallback_source=moodle|not_applicable`、`fallback_logout_consent=authorized_once|not_authorized|not_applicable`、`decided_at=&lt;ISO-8601 with timezone&gt;`。不要发送原因、身份、课程、学校页面、Cookie/Profile/session或截图。</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-23-3：用户控制 Moodle reauth，并在恢复后执行新 EdStem follow-up</name>
  <read_first>Task1 D/baseline strict pass；Task2 validated decision与current natural/deadline path；02-CONTEXT D-08/D-10–D-12/D-14/D-16；02-07 SUMMARY logout intent/job isolation；02-08/02-09 SUMMARY paired login/logout/probe与protected UI；当前beta exact official login步骤；本plan reauth result/verifier contract。</read_first>
  <files>none（真实动作只改变仓库外runtime/logout intent/source observations/Profile session与append-only EvidenceLedger；strict pass后创建SUMMARY）</files>
  <what-built>D已独立通过；Moodle或EdStem自然expiry已被观察，或用户定义期限已到且两者仍authenticated并明确授权一次Moodle fallback。reauth/Windows cells仍not_run。</what-built>
  <action>硬暂停并原样呈现`<how-to-verify>`/`<resume-signal>`。先再次运行D verifier、检查decision current且reauth cells空。natural path不得创建/调用logout；product只把实际过期来源标reauth_required、暂停该来源jobs、保留last-success，另一来源继续。explicit path必须由用户在paired UI点击一次`记录 Moodle 退出意图`，server调用`recordExplicitLogout('moodle')`原子取消/拒绝该source queued/retry/running recovery并持久logout intent；随后用户本人在Moodle official page使用正常logout。产品不得自动点击logout、调用任意logout URL、清Cookie/storage、删除/复制Profile或影响EdStem。登出后Moodle=unauthenticated且automatic reopen/probe=0。

两path都必须等用户在paired UI明确点击`重新登录 <实际目标来源>`后才签发该source一次性login action并打开approved official headed window；用户本人输入password/MFA并点击完成。sealed adapter只在origin chain/positive marker/bounded identity都current且binding/scope consistent后commit target reauthenticated；任何mismatch/interaction未完成/ownership不明立即停。target=moodle时，其commit前EdStem新probe=0；commit后server才mint本轮新的EdStem follow-up correlation并创建一个fixed background `edstem.auth_probe`，不能复制Moodle状态；若EdStem需要交互则停止等用户本人动作。target=edstem仅允许natural path：Moodle保持unaffected_authenticated且Moodle login/probe=0，用户本人完成EdStem re-login后只重验EdStem，不制造Moodle失效或额外follow-up。其他失败保持source isolated与last-success。

paired server在Moodle/EdStem actual results与本轮human workflow匹配时分别append`reauth×macos×L` immutable event；explicit path还必须绑定decision digest/logoutIntent，natural path必须证明无intent。failed/human_needed保留。用户返回strict feedback后executor写0700/0600 temp JSON，运行canonical reauth record（只读核对）并删除temp，再运行reauth verifier与Windows audit。repo gate不append。任何non-pass阻止02-24、不自动重试/覆盖失败/补写pass。</action>
  <how-to-verify>
    1. 在paired protected UI确认D仍pass、checkpoint=reauth、current path=`natural_expiry`或`explicit_moodle_logout`、Windows not_run/human_needed、Phase3 blocked；不复制身份/course或截图。
    2. natural path：确认页面明确标出实际过期来源=`reauth_required`、其last-success retained/jobs paused，另一来源保持真实current状态；不要点击任何logout。explicit path：只点击一次`记录 Moodle 退出意图`，再在Moodle官方页面本人使用其正常logout；不要让AutoED/agent代点或清理浏览器数据。
    3. explicit登出后确认Moodle=`unauthenticated`且页面没有自动重开/自动probe；EdStem未被logout/清session。若auto reopen、Cookie/Profile清理或另一source变化，立即failed。
    4. 点击一次`重新登录 <页面显示的实际目标来源>`；只在其approved official窗口本人输入password/MFA。不要把凭据、code、identity、页面、URL path或截图发给agent。完成后回paired UI点击对应`我已完成登录`。
    5. 本人核对目标来源positive auth与完整identity/binding/scope（完整值只在protected UI）。目标=Moodle时，Moodle pass前不得有EdStem新probe，pass后确认恰好一个新的fixed EdStem auth probe开始；目标=EdStem时确认Moodle未被重登/probe且保持unaffected。
    6. Moodle path的EdStem follow-up若直接authenticated/recovered，本人核对其protected identity/binding仍一致；若INTERACTION_REQUIRED，只按UI明确动作本人完成EdStem MFA。EdStem-natural path只核对EdStem reauthenticated和Moodle unaffected；产品不得连续弹窗或自动输入。
    7. 检查Moodle/EdStem reauth行分别为macos/L/scenario=reauth、本轮build/time/safe code；source isolation、last-success、content boundary preserved，body/posts/files/grades未读、screenshot not_used。
    8. 只返回下面exact block，不附解释、identity/course/origin/Profile/Cookie/session/path/action ID、页面、截图或日志：

```text
02-23_REAUTH_RESULT
schema=1
checkpoint=reauth
platform=macos
path=natural_expiry|explicit_moodle_logout
target_source=moodle|edstem
result=pass|failed|human_needed
result_code=REAUTH_COMPLETE|NATURAL_EXPIRY_WAITING|LOGOUT_CONSENT_REQUIRED|LOGOUT_INTENT_NOT_RECORDED|AUTO_REOPEN_DETECTED|MOODLE_REAUTH_FAILED|EDSTEM_REAUTH_FAILED|EDSTEM_FOLLOWUP_FAILED|INTERACTION_REQUIRED|IDENTITY_MISMATCH|PROFILE_OWNERSHIP_UNCONFIRMED|CONTENT_BOUNDARY_VIOLATION|RESULT_NOT_OBSERVED
logout_intent=not_applicable|recorded|missing
automatic_reopen=none|detected|not_observed
moodle=reauthenticated|unaffected_authenticated|failed|human_needed
moodle_code=<allowlisted SafeAuthErrorCode>
edstem=reauthenticated|followup_authenticated|followup_recovered|unaffected_authenticated|failed|human_needed
edstem_code=<allowlisted SafeAuthErrorCode>
identity_scope=consistent|mismatch|not_observed
source_isolation=preserved|violated|not_observed
last_success=preserved|lost|not_observed
content_boundary=preserved|violated|not_observed
screenshot=not_used
windows=not_run/human_needed
phase3=blocked
observed_at=<ISO-8601 with timezone>
```
  </how-to-verify>
  <verify>
    <automated>checkpoint前运行D verifier；真实feedback且server已有exact events后运行canonical reauth `--record`，再运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint reauth --runtime current --platform macos --read-only`与`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only`。只有reauth verifier `REAUTH_COMPLETE`且Windows audit strict pass才完成。</automated>
  </verify>
  <acceptance_criteria>
    1. path来自Task2真实decision/current state；无agent-invented wait、强制expiry、时钟/Cookie/storage/Profile/session破坏或模拟学校错误。
    2. natural path无logout intent；explicit path有user one-time consent+paired UI intent+本人official logout，Moodle自动reopen/recovery为0、EdStem未受损。
    3. 用户本人发起Moodle re-login并输入password/MFA；产品未request/read/fill/type/press/submit/capture凭据或input，official origin/positive marker/identity/binding全current。
    4. 失效期间只暂停Moodle，last-success保留；EdStem current状态独立。identity mismatch立即停止且不覆盖binding。
    5. target=Moodle时reauth commit严格先于新EdStem probe且恰好一个fixed follow-up；target=EdStem只允许natural path，Moodle unaffected且新login/probe=0。interaction需要新用户动作，自动popup/retry=0。
    6. Moodle/EdStem各有一个current-build `reauth×macos×L` immutable result，server-only authority绑定path/decision/logout/login/followup；action ID不外泄、跨格/重放失败。
    7. strict feedback与reauth verifier都满足唯一pass conjunction；failed/human_needed保留并阻止02-24，无planning receipt/自动补pass。
    8. Windows audit required=notRun=humanNeeded、pass=0、macosWritesApplied=0、Phase3 blocked；所有输出无private/raw/screenshot。
  </acceptance_criteria>
  <done>只有真实user-controlled Moodle reauth、post-Moodle EdStem follow-up、two-source exact L results、strict feedback与read-only gates全部pass才完成并允许02-24；其他结果保持blocked。</done>
  <resume-signal>只粘贴paired UI生成的完整`02-23_REAUTH_RESULT`脱敏block。不要回复approved/通过自由文本，不要发送wait原因、姓名/邮箱/组织/course/origin/path/Profile/Cookie/session/action ID、密码/MFA、页面、截图或日志。任何failed/human_needed会保留并停止02-24。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| D SUMMARY/ledger → reauth eligibility | D时间/文本/单source可能被误当reauth或current prerequisite |
| user observation decision → fallback eligibility | agent可能发明时长、提前deadline或把模糊同意当logout consent |
| source auth/session → expiry/logout state | product可能删Cookie/Profile、改时钟、强制401或误伤另一source制造失效 |
| paired UI logout/re-login → official source | 自动点击/logout URL/凭据读取/连续popup可能越过用户控制与source边界 |
| Moodle recovery → EdStem follow-up | EdStem可能提前probe、复制Moodle pass或后台job自升L |
| paired server → EvidenceLedger | action/correlation可能重放、跨source/scenario/platform/path使用或由repo tool伪造 |
| macOS reauth → Windows/Phase3 | macOS pass可能污染Windows cells或被夸大为完整Phase2/Phase3 eligibility |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Category | Severity | Component | Disposition | ASVS L1 intent | Mitigation plan / blocker |
|---|---|---:|---|---|---|---|
| T2-23-01 | Spoofing / Tampering | Critical | D→reauth prerequisite | mitigate | V2、V4、V13、V16 | completed02-22 SUMMARY+canonical D exact-cell verifier；D/reauth scenario分离。任一drift/missing阻断。 |
| T2-23-02 | Tampering / Repudiation | High | observation deadline/consent | mitigate | V4、V5 Validation、V13 | 无default duration；用户exact timezone wait-until；trusted clock不提前；one-time explicit Moodle fallback consent；strict digest。 |
| T2-23-03 | Tampering / Elevation / Disclosure | Critical | Cookie/Profile/session/expiry | mitigate | V4 least privilege、V8 protection、V13 | 禁改时钟/删改Cookie/storage/Profile/强制401；自然expiry或本人official logout only；source isolation+last-success retention。 |
| T2-23-04 | Spoofing / Elevation | Critical | logout intent / official re-login | mitigate | V2、V4、V13 | paired+same-origin+CSRF intent；本人official logout/login/MFA；无auto reopen/fill/read；fixed approved origin。 |
| T2-23-05 | Tampering / Repudiation | High | Moodle→EdStem sequencing | mitigate | V4持续授权、V13状态机、V16audit | Moodle current positive commit前Ed probe=0；之后exact one new fixed probe；不复制pass；interaction停等user。 |
| T2-23-06 | Tampering / Elevation | Critical | reauth L events | mitigate | V4 writer authority、V13 transaction、V16provenance | server-only path/decision/source correlations+actual result+one-time append；repo/public/feedback无writer；failed保留。 |
| T2-23-07 | Information Disclosure | High | protected UI/feedback/gate | mitigate | V7、V8、V14 | full identity只protected UI；strict output allowlist；无credential/MFA/Cookie/Profile/path/raw page/network/screenshot。 |
| T2-23-08 | Tampering / Repudiation | Critical | macOS→Windows/Phase3 | mitigate | V4、V13、V16 | exact platform keys+read-only Windows audit；全部Windows not_run/human_needed、pass0、macosWrites0、Phase3blocked。 |

ASVS 5.0 L1 仅作为适用控制意图，不声明完整合规。全部 High/Critical 威胁必须mitigate；任何deadline/consent不明确、自然expiry provenance缺失、session破坏、auto reopen、identity mismatch、EdStem提前/伪follow-up、writer不可信、输出泄漏或Windows污染都保持failed/human_needed，不得accept/transfer、自动批准或推进02-24。
</threat_model>

<verification>
1. reauth/Windows gate tests覆盖deadline、consent、natural/explicit路径、session破坏、source isolation、Moodle→EdStem顺序、writer/跨格与privacy负例；repo gate保持只读。
2. D canonical verifier+completed02-22 SUMMARY current；reauth起始cells/intent/correlation为空，Windows matrix strict not_run。
3. user decision是观察deadline与fallback consent唯一来源；deadline前零学校/logout动作，届时重新优先检查自然expiry。
4. natural path不logout；explicit pathuser intent+official logout且无auto reopen/session破坏；两path用户本人Moodle login/MFA。
5. Moodle positive commit后才new EdStem fixed probe；source isolation、last-success、identity/binding/scope、content boundary保持。
6. paired server写two-source reauth exact L events；strict feedback、reauth verifier与Windows audit全pass才SUMMARY/02-24 eligible。
7. private/raw/screenshot扫描零命中；failed/human_needed保留，Windows/Phase3不变。
</verification>

<success_criteria>
- AUTH-02/D-12/D-16：D后独立完成真实reauth；优先自然expiry，无批准时长时只用用户exact deadline与一次性Moodle fallback consent。
- AUTH-03/D-10–D-11：logout intent被尊重、不auto reopen；只暂停实际失效source、last-success保留；Moodle恢复后才EdStem新probe，EdStem自然过期时Moodle保持unaffected。
- SEC-02/D-08：用户本人official login/MFA；不破坏Cookie/Profile/session、不捕获凭据、不任意导航/写入/内容读取。
- UAT-01/D-14：Moodle/EdStem各有独立reauth macOS/L event与strict feedback；server-only authority，repo verifier只读，失败硬阻。
- Windows全矩阵仍not_run/human_needed且macOS writes=0；Phase1/2 incomplete、Phase3 blocked。
</success_criteria>

<output>
只有Task2真实decision与Task3真实feedback存在、Moodle/EdStem two-source `reauth×macos×L` exact events strict pass、canonical reauth verifier exit0且Windows matrix audit证明全部not_run/human_needed后，才创建`.planning/phases/02-poc-live/02-23-SUMMARY.md`。SUMMARY只记录checkpoint=reauth、public version/build、platform=macos、path=natural_expiry|explicit_moodle_logout、targetSource=moodle|edstem、observation decision digest（不抄用户解释）、logoutIntent enum、automaticReopen、Moodle/EdStem safe results、identityScope/sourceIsolation/lastSuccess/contentBoundary、observedAt、02-24 eligibility与Windows/Phase3 gaps；不记录wait原因、identity/course/origin/path/Profile/Cookie/session/action ID、password/MFA、raw page/network/log/screenshot。failed/human_needed不创建成功SUMMARY、不创建planning receipt、不推进02-24，保留append-only结果。
</output>
