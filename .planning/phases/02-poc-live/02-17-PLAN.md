---
phase: 02-poc-live
plan: "17"
type: execute
wave: 17
depends_on:
  - "02-16"
files_modified:
  - .planning/phases/02-poc-live/02-17-A2-RECEIPT.json
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "只有 02-16 A1 的真实 macOS Moodle 登录/MFA、EdStem 有界认证检查和双来源本地身份观察均为 strict pass，且当前 beta/build、paired session、approved config/scope 与 A1 完全一致时，用户才能进入 A2。"
    - "用户只在 paired protected local UI 中并列核对 Moodle 与 EdStem 的完整身份、组织/租户和已批准范围；完整姓名、邮箱、组织、课程名不进入聊天、CLI、MCP、日志、诊断、receipt、Git 或截图。"
    - "稳定 subject+organization/tenant+approved scope 只能产生 candidate，不能自动确认；首次 live 必须由用户明确确认一次。稳定证据不足时必须由用户在同一 protected UI 人工确认一次，不能按显示名或邮箱自动绑定。"
    - "任一身份、组织、范围或已确认绑定不一致都立即产生 `IDENTITY_MISMATCH` 或 `APPROVED_SCOPE_MISMATCH`，停止全部 course probe；不得自动切号、登出、覆盖绑定、继续读取或复制 Cookie/session。"
    - "D-13: 检查点 A 只确认 Moodle/EdStem 两个官方来源、仅在已配对本地页显示的完整账户身份，以及用户明确选择的一个课程对当前账户可见；不读取课程正文、帖子、文件、成绩或其他 Phase 3/4 内容。绑定确认后只对 02-15 已批准的同一个 `approvedScopeId`，分别调用 Moodle/EdStem 的固定 course-visibility action 至多一次；只能判断该课程对当前账户是否可见，不读取正文、帖子、文件、成绩、反馈或其他课程。"
    - "A2 的真实人类动作由 server 内部以一次性、精确绑定 macOS/build/generation/checkpoint/source/scenario 的 authority 写入 append-only L ledger；repo-side gate 只能只读核对现有 events 并生成脱敏 planning handoff，A2 binding/course、两个来源及后续 B1 轮次不能互填或重放。"
    - "用户只反馈 A2 checkpoint、pass/failed/human_needed 与封闭脱敏 code；失败和 human_needed 作为 append-only 结果保留并立即阻止 02-18，只有 strict pass 才生成 `02-17-A2-RECEIPT.json`。"
    - "macOS-first 只改变执行顺序；Windows 始终 `not_run/human_needed`，共享 Profile 仍是待验证假设，Phase 1/2 不整体完成，Phase 3 始终 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-16-A1-RECEIPT.json"
      provides: "02-16 strict pass 后唯一存在的 A1 machine gate，绑定实际 beta/build、macOS、双来源认证与 A-login L cells"
      contains: "A1_COMPLETE"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "02-16 建立且本计划扩展 A2 schema 的唯一 planning-handoff recorder/read-only verifier；只读核对 server 已写入的 exact L cells，不持有 live writer authority"
      contains: "verifyPhase2LiveCheckpoint"
    - path: "apps/status/src/main.ts"
      provides: "paired protected identity/binding/course visibility UI；完整身份与课程名唯一允许的呈现面"
      contains: "确认两个账户对应"
    - path: "packages/persistence/src/auth.ts"
      provides: "AccountBindingStore、per-source observations 与 append-only EvidenceLedger 的事务/fence 实现"
      contains: "EvidenceLedger"
    - path: ".planning/phases/02-poc-live/02-17-A2-RECEIPT.json"
      provides: "仅 strict human pass 后生成的脱敏、机器可读 macOS A2 gate；02-18 只读验证"
      contains: "A2_COMPLETE"
  key_links:
    - from: ".planning/phases/02-poc-live/02-16-A1-RECEIPT.json"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "A2 preflight 对 A1 receipt、当前 runtime、beta/build/config/scope、paired UI 和 exact A1_login L cells 做只读等值检查"
      pattern: "A1_AUTHENTICATED|A1_login|macos"
    - from: "apps/status/src/main.ts"
      to: "POST /api/auth/binding/confirm"
      via: "paired+same-origin+CSRF 用户点击只提交 current opaque candidateBindingId；服务端创建 source-specific one-time human-action correlations"
      pattern: "candidateBindingId|confirm_binding|binding/confirm"
    - from: "apps/status/src/main.ts"
      to: "POST /api/auth/probe"
      via: "绑定已确认后仅用 approvedConfigId/approvedScopeId 与固定 `*.course_visibility_probe`，不提交 URL/selector/课程名或读取动作"
      pattern: "course_visibility_probe|approvedScopeId"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "packages/persistence/src/auth.ts"
      via: "paired server workflow 在用户 action 与 actual result 同时成立时内部生成 human_action/L authority 并 append exact A2 cells；repo-side script 仅经 redacted API 读取 event digests"
      pattern: "human_action|actionReceiptId|A2_binding|A2_course_visibility"
    - from: ".planning/phases/02-poc-live/02-17-A2-RECEIPT.json"
      to: "02-18 eligibility"
      via: "只有 read-only verifier 证明 A1 hash + 四个 A2 L cells + binding/scope/course/content boundary 全部 strict pass 才可进入 B1"
      pattern: "A2_COMPLETE|not_read|phase3"
---

<objective>
完成 macOS 检查点 A 的第二个硬人工门禁：用户在 paired protected local UI 中核对 Moodle/EdStem 完整身份并首次确认账户绑定，然后只验证 02-15 选定的一个课程对当前账户可见；任何 mismatch、证据不足未确认、越界读取或不确定结果都立即停止。

Purpose: 落实 D-03、D-06–D-07、D-12–D-15，使“认证成功”“两来源属于同一目标账户”“一个批准课程可见”成为互相分离、可审计且不泄露身份/课程内容的真实 live 证据，并为 02-18 B1 提供不可由 synthetic/native/旧动作代填的 strict pass gate。
Output: A1 的只读 preflight、一次不可自动批准的 `checkpoint:human-action`、精确 A-binding/A-course-visibility L events，以及仅在 strict pass 后存在的 `.planning/phases/02-poc-live/02-17-A2-RECEIPT.json`。本计划不修改产品源码、不访问其他课程、不读取课程内容、不截图、不填写 Windows 单元。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-06-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-15-SUMMARY.md
@.planning/phases/02-poc-live/02-16-PLAN.md
@.planning/phases/02-poc-live/02-16-SUMMARY.md
@.planning/phases/02-poc-live/02-16-A1-RECEIPT.json
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/auth.ts
@packages/application/src/policy.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时先读取 02-16 SUMMARY 与真实导出。若 02-16 最终模块导出存在纯机械命名差异，只能统一 import/CLI 名称，不能另造 public route、平行 JSON 或削弱以下语义。02-01 的 `UatScenario` 必须稳定为下列 exact enum，后续 02-18–02-23 直接复用：

```typescript
export type UatScenario =
  | 'A1_login'
  | 'A2_binding'
  | 'A2_course_visibility'
  | 'B1_reopen_1'
  | 'B1_reopen_2'
  | 'B1_reopen_3'
  | 'B2_worker_restart'
  | 'B3_codex_exit'
  | 'C_os_restart'
  | 'D_24h_recheck'
  | 'reauth';

export type SourceId = 'moodle' | 'edstem';
export type SourceAction =
  | 'moodle.auth_probe'
  | 'edstem.auth_probe'
  | 'moodle.course_visibility_probe'
  | 'edstem.course_visibility_probe';

export interface EvidenceCellKey {
  platform: 'macos' | 'windows';
  source: SourceId;
  scenario: UatScenario;
  evidence: 'S' | 'I' | 'N' | 'L';
}

export type EvidenceWriterAuthority =
  | { kind: 'automated'; evidence: 'S' | 'I' | 'N'; platform: 'macos' | 'windows'; producerId: string }
  | { kind: 'human_action'; evidence: 'L'; platform: 'macos' | 'windows'; actionReceiptId: string };

export interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: EvidenceCellKey): Promise<EvidenceReceipt[]>;
}
```

02-16 必须提供唯一通用 live gate 入口，02-17/02-18 不得各自绕过 ledger：

```typescript
export interface RecordPhase2A1CheckpointInput {
  checkpoint: 'A1';
  feedbackPath: string;
  outPath: '.planning/phases/02-poc-live/02-16-A1-RECEIPT.json';
  runtime: 'current';
  readOnlyInputs: true;
}
export interface RecordPhase2A2CheckpointInput {
  checkpoint: 'A2';
  feedbackPath: string; // executor-owned 0700 mktemp/0600 file; finally removed
  outPath: '.planning/phases/02-poc-live/02-17-A2-RECEIPT.json';
  a1ReceiptPath: '.planning/phases/02-poc-live/02-16-A1-RECEIPT.json';
  runtime: 'current';
  readOnlyInputs: true;
}
export type RecordPhase2LiveCheckpointInput = RecordPhase2A1CheckpointInput | RecordPhase2A2CheckpointInput;
export type VerifyPhase2LiveCheckpointInput =
  | { receiptPath: '.planning/phases/02-poc-live/02-16-A1-RECEIPT.json'; runtime:'current'; readOnly:true }
  | { receiptPath: '.planning/phases/02-poc-live/02-17-A2-RECEIPT.json'; a1ReceiptPath:'.planning/phases/02-poc-live/02-16-A1-RECEIPT.json'; runtime:'current'; readOnly:true };
export function recordPhase2LiveCheckpoint(input: RecordPhase2LiveCheckpointInput): Promise<Phase2MacosA1GateReceipt | Phase2MacosA2GateReceipt>;
export function verifyPhase2LiveCheckpoint(input: VerifyPhase2LiveCheckpointInput): Promise<RedactedLiveCheckpointResult>;
export function parsePhase2A1Feedback(value: unknown): Phase2A1Feedback; // 02-16 existing
export function parsePhase2A2Feedback(value: unknown): Phase2A2Feedback;
export function validatePhase2MacosA1GateReceipt(value: unknown): Phase2MacosA1GateReceipt; // 02-16 existing
export function validatePhase2MacosA2GateReceipt(value: unknown): Phase2MacosA2GateReceipt;
```

Canonical CLI：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --receipt .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --record --checkpoint A2 --feedback <0700-mktemp>/A2-result.json --out .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --a1 .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only-inputs

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --receipt .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --a1 .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only
```

02-37 已在 identity selection/signing 前实现并穷尽测试这个 A2 grammar；02-17 只消费签名 parser/schema，不得扩展或修改 `phase2-live-gate.mjs`/`phase2-live-gate.test.ts`。`--record` 不是 live writer，只能 strict parse executor 放在 `mktemp -d`（目录 0700、文件 0600）的 allowlisted A2 feedback JSON，经既有 local client credential/discovery 读取 current redacted `GET /api/auth/status` 与四个 exact `GET /api/auth/receipts?platform=macos&source={source}&scenario={A2_binding|A2_course_visibility}&evidence=L`，核对 A1 hash/current version/build/artifact/runtime generation/status/event digests 后按 02-37 的 0600 same-directory temporary + fsync/re-parse + atomic no-replace contract写唯一 allowlisted planning handoff path；finally 删除临时 feedback。目标已存在、竞争写入、partial write 或任何 failed/human_needed 时不得覆盖/删除已有文件，也不得创建 pass receipt。它不得 import persistence、直接读 DB/WAL、调用 `EvidenceLedger.append`、接受/取得 actionReceiptId、访问 protected identity/config、打开 browser 或访问学校。

真正的 L append 只能发生在已发布 beta 的 paired server workflow 内：binding confirm 与 course-visibility user action 分别为四个 exact cells 建立 source-specific server correlation：`moodle×A2_binding`、`edstem×A2_binding`、`moodle×A2_course_visibility`、`edstem×A2_course_visibility`。每个 correlation 绑定 `checkpoint=A2, round=0, platform=macos, source, scenario, version, buildId, runtimeGeneration, pairedSessionDigest, approvedConfigId, approvedScopeId`，并在 actual result 与 `EvidenceLedger.append` 的同一事务中消费。唯一 `actionReceiptId`、checkpoint/round/source/scenario/build/generation/scope 全匹配与 idempotency 共同阻止重复、跨来源、跨 scenario、跨 checkpoint、旧 build、旧 generation 及 B1 round 代填；action ID 永不离开 server。paired click correlation 本身不是 pass，后台/fixture/executor/feedback/script 都不能自升 L。若当前 immutable beta 缺少此内部 provenance 链，立即 `human_needed`，回到修复→完整测试→新 immutable beta→实际更新→重做 A1；禁止在本计划用 script/DB/public route 补写。

严格 A2 gate schema：

```typescript
interface Phase2MacosA2GateReceipt {
  schema: 1;
  checkpoint: '02-17-A2-binding-course';
  platform: 'macos';
  evidence: 'L';
  version: `0.1.0-beta.${number}`;
  buildId: string;
  artifactSha256: string;
  a1ReceiptSha256: string;
  result: 'pass';
  resultCode: 'A2_COMPLETE';
  bindingConsistency: 'confirmed';
  bindingMode: 'stable_subject_org_confirmed' | 'manual_once_confirmed';
  identityReview: { moodle: 'confirmed'; edstem: 'confirmed' };
  approvedScope: 'matched';
  courseVisibility: {
    selectedScopeCount: 1;
    moodle: 'visible';
    edstem: 'visible';
  };
  contentBoundary: {
    body: 'not_read';
    posts: 'not_read';
    files: 'not_read';
    grades: 'not_read';
  };
  evidenceCells: readonly [
    { source: 'moodle'; scenario: 'A2_binding'; evidence: 'L'; result: 'pass'; resultCode: 'BINDING_CONFIRMED'; ledgerEventDigest: string },
    { source: 'edstem'; scenario: 'A2_binding'; evidence: 'L'; result: 'pass'; resultCode: 'BINDING_CONFIRMED'; ledgerEventDigest: string },
    { source: 'moodle'; scenario: 'A2_course_visibility'; evidence: 'L'; result: 'pass'; resultCode: 'COURSE_VISIBLE'; ledgerEventDigest: string },
    { source: 'edstem'; scenario: 'A2_course_visibility'; evidence: 'L'; result: 'pass'; resultCode: 'COURSE_VISIBLE'; ledgerEventDigest: string }
  ];
  screenshot: 'not_used';
  observedAt: string;
  feedbackDigest: string;
  windows: 'not_run/human_needed';
  phase3: 'blocked';
}
```

Pass gate file 绝不包含 full identity、email、organization、tenant、course name/id、origin/path、Profile、Cookie、actionReceiptId、paired session、CSRF、DOM/network 或 screenshot。允许的 A2 aggregate `resultCode` 仅为 `A2_COMPLETE`；非 pass ledger/SUMMARY safe code 仅允许：`A1_PRECONDITION_FAILED`、`PAIRING_REQUIRED`、`IDENTITY_MISMATCH`、`BINDING_CONFIRMATION_REQUIRED`、`BINDING_REJECTED`、`APPROVED_SCOPE_MISMATCH`、`COURSE_NOT_VISIBLE`、`COURSE_VISIBILITY_DENIED`、`COURSE_PROBE_FAILED`、`CONTENT_BOUNDARY_VIOLATION`、`RESULT_NOT_OBSERVED`。未知错误统一 `RESULT_NOT_OBSERVED` 并 hard stop，禁止输出 exception/message/stack/page/path/identity。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-17-1：消费签名 A2 handoff gate，并验证 A1/current runtime 零越界前置</name>
  <read_first>AGENTS.md 的 Credentials and login、Source and content policy、Data integrity and verification、Execution after plan approval；.planning/phases/02-poc-live/02-CONTEXT.md D-03、D-06–D-07、D-12–D-15；.planning/phases/02-poc-live/02-VALIDATION.md 的 live evidence/hard-gate 项；.planning/phases/02-poc-live/02-16-PLAN.md；.planning/phases/02-poc-live/02-16-SUMMARY.md；.planning/phases/02-poc-live/02-16-A1-RECEIPT.json；scripts/release/phase2-live-gate.mjs；packages/domain/src/model.ts；packages/persistence/src/auth.ts；apps/api/src/auth.ts；apps/status/src/main.ts。</read_first>
  <files>.planning/phases/02-poc-live/02-17-A2-RECEIPT.json</files>
  <behavior>
    - RED：A2 strict pass 只接受 exact feedback + current A1 hash/build/status + 四个 `A2_binding|A2_course_visibility×macos×L` non-corrected pass event digests，并生成 exact `Phase2MacosA2GateReceipt`。
    - RED：failed/human_needed、missing/duplicate/corrected/conflicting event、scenario/source/platform/evidence 串格、A1/B1 代填、旧 build/generation/scope、extra/sensitive feedback 均不写/不改 out。
    - RED：record/verify 的 persistence/DB/append/Profile/browser/login/probe/school request counters 均为 0；stdout/stderr 无 path/action id/private identity/course/raw response。
  </behavior>
  <action>消费02-37已完成、02-38已测试且02-39已签名的 A2 record/verify 分支，先核对installed hash/capability manifest，再原样运行其 strict A2 contract/negative tests；本计划不编辑脚本或测试。工具只读取 current redacted status、A1 receipt hash 与四个 exact A2 L cells，不能 import persistence、打开 DB/WAL、append ledger、取得 actionReceiptId、打开 browser 或访问学校。失败时 out 不存在；若已有 pass out，则 hash 不变。若分支/代码缺失或 installed beta 尚未通过 server workflow 写出 A2 L cells，返回 human_needed并走完整修复→新immutable beta→实际更新→从A1重做，不能在本计划补写。

随后运行 `<interfaces>` 的 A1 canonical read-only verifier；strict parse 要求 A1 receipt 的 version/build/artifact 与 02-14 actual-update gate 和当前 runtime 一致，platform=macos，Moodle/EdStem `A1_login×L` 均为本轮真实 pass，paired UI ready，02-15 两个 approved configs 与同一个 approvedScopeId 仍 current，双来源 observation 均有正面 auth marker 与 protected identity，且未出现 mismatch/logout intent/reauth_required/interaction_required/stale generation。只读取 protected status 的封闭状态和 ledger cell metadata；命令 stdout 仅允许 checkpoint/platform/version/build/result/safeCode/bindingConsistency/gaps/observedAt，不打印 identity、email、organization、course、origin/path、Profile、PID、Cookie/session、action ids、DOM/network 或 exception。

随后以 read-only runtime counters 与 exact-cell queries 证明 A2 尚未被自动执行：本轮 `A2_binding×L`、`A2_course_visibility×L` 均无 pass，course visibility request count=0，body/post/file/grade request/read count=0，pending A2 action correlation=0；Windows 所有 N/L cells 仍 `not_run/human_needed`、Phase3 blocked。不得为了 preflight 打开浏览器、重新 probe、查询任意课程、创建 candidate、确认 binding、mint human authority 或修补旧 receipt。若 A1 receipt/SUMMARY 不存在、非 pass、hash/build/config/scope/generation 不一致、paired session 失效、任一身份/范围 mismatch、旧 A2/B1 action 存在、content read 非零或输出泄漏，即返回 allowlisted `A1_PRECONDITION_FAILED|PAIRING_REQUIRED|IDENTITY_MISMATCH|APPROVED_SCOPE_MISMATCH|CONTENT_BOUNDARY_VIOLATION` 并停止；不得呈现 Task 2（D-03、D-06、D-12–D-15；T2-02/T2-03/T2-07/T2-08）。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --receipt .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. A1 verifier exit 0 且只输出 allowlisted redacted JSON；receipt↔actual beta↔current runtime 的 version/build/artifact/config/scope/generation exact match。
    2. Moodle 与 EdStem 的 `A1_login×macos×L` 是独立真实 pass cells；无 S/I/N 或另一 source/scenario/platform 代填。
    3. 当前 paired UI ready、双来源 auth positive、protected identities present、binding 尚待 A2 人工确认，且没有 identity/scope mismatch、logout intent、reauth/interaction/stale owner。
    4. A2 前 course probe、body/posts/files/grades read、A2 L pass 与 pending action bundle 均为 0；检查本身不访问学校。
    5. stdout/log/SUMMARY 无完整身份、邮箱、组织、课程、origin/path、Profile/PID、Cookie/session、action id、DOM/network、截图或原始异常。
    6. Windows 保持 `not_run/human_needed`，Phase 1/2 未整体完成，Phase3 `blocked`；任一非 strict pass 保持 02-17 blocking。
  </acceptance_criteria>
  <done>A2 handoff parser/verifier 只读权限与负例矩阵全部通过，且 A1 strict pass 与当前 runtime 完全一致、A2 尚未执行、内容读取为零时才可显示人工步骤；否则以封闭脱敏 code 停止，不改变任何 ledger/gate。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-17-2：用户核对双来源身份、确认一次绑定并验证一个课程可见</name>
  <read_first>Task 1 的 strict pass 输出；.planning/phases/02-poc-live/02-UI-SPEC.md 的 protected identity/privacy/checkpoint 文案；.planning/phases/02-poc-live/02-CONTEXT.md D-03、D-06–D-07、D-12–D-15；02-16-SUMMARY.md 中当前 paired local UI 的精确打开方式；apps/status/src/main.ts 的 identity/binding/course CTA；apps/api/src/auth.ts 的 binding/probe strict routes；scripts/release/phase2-live-gate.mjs 的 record/verify contract。</read_first>
  <files>.planning/phases/02-poc-live/02-17-A2-RECEIPT.json（仅 strict pass 后）</files>
  <action>此 task 必须暂停等待真实用户完成；executor/agent 不得代点、代看身份、代确认、猜测 pass、从 DOM/日志/DB 读取身份或自动恢复 checkpoint。向用户呈现以下精确步骤和隐私边界：

1. 使用 02-16 提供的当前 paired local management page；确认页面显示当前 public beta version/build、`macOS / A2`、Windows `not_run/human_needed` 和 Phase3 blocked。不要打开开发者工具、复制页面、上传截图或把任何身份/课程文字发到聊天。
2. 在 PrivacyNotice 后的 protected 区域并列核对 Moodle 与 EdStem 的完整账户身份、组织/租户和 02-15 已批准范围。用户必须亲自判断两者是否对应目标账户；agent 不得观察这些值。
3. 若页面显示 stable subject+organization/tenant+scope 的 strict candidate，理解它只是候选，仍点击一次 `确认两个账户对应`；若 strict evidence 不足，页面必须显示两来源身份供人工一次性确认，同样由用户点击确认。不得因相同显示名/邮箱跳过确认。
4. 只要任一身份、组织/租户、批准范围或 candidate 不一致，点击 `不一致并停止`，不要切号、自动登出、覆盖绑定或继续课程检查；结果必须是 `failed / IDENTITY_MISMATCH` 或 `APPROVED_SCOPE_MISMATCH`。
5. 只有 binding 状态变为 `confirmed` 后，核对 protected 页面显示的仍是 02-15 选定的唯一课程；点击一次 `验证所选课程可见性`。产品仅可对同一 approvedScopeId 分别发出固定 Moodle/EdStem course-visibility probe 至多一次，不得打开课程正文、帖子、文件、成绩、反馈、其他课程或任意链接。
6. 查看页面的 A2 脱敏结果单，确认两个来源的 binding consistency 与 course visibility；检查正文/帖子/文件/成绩均显示 `未读取`。任何 `not_visible/denied/not_observed`、弹窗/下载/写操作、越界页面、额外课程、内容读取或不确定状态都选择 failed/human_needed 并立即停止。
7. 截图不是条件且默认不要截图。若用户自愿保留本地辅助截图，必须先自行遮蔽身份/邮箱/组织/课程/login/MFA/Cookie/devtools，并且不得发给 agent、写入 Git/release/长期诊断；正式 evidence 仍只用 result receipt。
8. 返回聊天时只能逐行发送以下 `02-17_A2_RESULT`，不得加 actual identity/email/organization/course/origin/path、截图、日志或页面原文：

```text
02-17_A2_RESULT
checkpoint=A2
platform=macos
result=pass|failed|human_needed
result_code=A2_COMPLETE|IDENTITY_MISMATCH|BINDING_CONFIRMATION_REQUIRED|BINDING_REJECTED|APPROVED_SCOPE_MISMATCH|COURSE_NOT_VISIBLE|COURSE_VISIBILITY_DENIED|COURSE_PROBE_FAILED|CONTENT_BOUNDARY_VIOLATION|PAIRING_REQUIRED|RESULT_NOT_OBSERVED
binding=confirmed|mismatch|not_confirmed
binding_mode=stable_subject_org_confirmed|manual_once_confirmed|not_confirmed
moodle_identity=confirmed|mismatch|not_observed
edstem_identity=confirmed|mismatch|not_observed
approved_scope=matched|mismatch|not_observed
moodle_course=visible|not_visible|denied|not_observed
edstem_course=visible|not_visible|denied|not_observed
content_boundary=preserved|violated|not_observed
screenshot=not_used
windows=not_run/human_needed
phase3=blocked
observed_at=<ISO-8601>
```

收到真实回复后 strict parse 每一字段，拒绝 extra key/自由文本/未知 code。`pass` 的唯一合法 conjunction 是：`A2_COMPLETE`、binding confirmed、两种合法 confirmed binding mode 之一、两身份 confirmed、scope matched、两 source course visible、content boundary preserved、screenshot not_used、Windows/Phase3 固定；任何其他组合不能规范化为 pass。paired server workflow 必须已经在每次真实 binding/course action 与 actual result 同时成立时，以内部 `human_action/L/macos` authority 向四个 exact cells append immutable events；CLI/chat/用户不能提供 actionReceiptId，repo-side gate 也没有 append 权限。不得用旧 A1、S/I/N、paired click 本身、feedback payload 或 fixture 代替这些 server-generated events。

executor 仅把 strict allowlisted block解析为 JSON写到本轮 `mktemp -d` 的 0600 文件，运行 `<interfaces>` canonical `--record --checkpoint A2 ... --read-only-inputs`，并在 finally 删除临时文件；recorder 通过 redacted API 核对 current status、A1 hash 与四个 exact latest non-corrected L event digests，只在 strict pass 时原子生成 `Phase2MacosA2GateReceipt`，随后立即执行 canonical read-only verifier。failed/human_needed 的 server event 仍 append-only 保留并阻塞，但 gate tool 不生成 A2 pass file；若用户 feedback 与 ledger/status 不一致同样阻塞。任何 record/verify 失败、event missing/duplicate/corrected/conflicting、provenance/round/build/generation/scope mismatch、DB/fence 错误或敏感输出均保持 checkpoint blocking，禁止 script 直接写 ledger、重写/删除 event、禁止执行 02-18；重试必须由用户重新发起新 server action 并产生 correction-linked event（D-03、D-06–D-07、D-12–D-15；T2-01/T2-03/T2-06/T2-08）。</action>
  <how-to-verify>
    1. 在当前 paired protected UI 核对两来源完整身份、组织/租户和批准范围；不要把任何实际值发送给 agent。
    2. 无论 strict candidate 或 evidence-insufficient，都由本人明确确认一次；有任何 mismatch 立即选择停止，且不要运行 course probe。
    3. binding confirmed 后只点击一次所选课程可见性检查，确认 Moodle/EdStem 都为 visible，并确认 body/posts/files/grades 都显示未读取。
    4. 不使用截图作为证据；从 UI 复制严格脱敏 `02-17_A2_RESULT`，先自行确认其中没有姓名、邮箱、组织、课程、origin/path/Profile/Cookie/action id 或页面内容。
    5. 只按规定 block 恢复 checkpoint。pass 仍需 repo-side recorder 与 read-only verifier 对 A1/current status/four exact L event digests 全部复验；UI 文案或用户口头“通过”本身不解除 gate。
  </how-to-verify>
  <verify>
    <automated>用户真实反馈为 strict pass 并由 canonical `--record` 完成后，运行 `node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --receipt .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --a1 .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only`；在用户反馈前或非 pass 时，自动检查必须断言 A2 gate 文件不存在且 02-18 blocked。</automated>
  </verify>
  <acceptance_criteria>
    1. 用户本人在 paired protected UI 中看见并核对两来源完整身份/组织/范围；这些值未进入 chat/log/CLI/MCP/receipt/Git/截图，agent 未读取或判断它们。
    2. strict candidate 与 evidence-insufficient 两条分支都要求首次明确的一次用户确认；无按显示名/email 自动绑定。identity/scope mismatch 立即停止且 course request=0。
    3. 只检查 02-15 的一个 approvedScopeId；Moodle/EdStem 各至多一个固定 course-visibility probe，body/posts/files/grades/feedback/other-course read 均为 0。
    4. paired server workflow 才能消费四个精确 source×scenario action correlations 并 append L；重复、跨 source/scenario/checkpoint/round/build/generation/session、A1/B1 代填全部失败。repo-side recorder 的 append/DB/Profile/browser/source 权限与调用数均为 0。
    5. failed/human_needed 的 server-side allowlisted L result append-only 保留并阻止 02-18；不得删除/覆盖失败，gate tool 不得创建 `02-17-A2-RECEIPT.json` 或成功 SUMMARY。
    6. strict pass 时 gate receipt 精确符合 `Phase2MacosA2GateReceipt`，含 A1 hash、四个独立 macOS L pass cells、binding/scope/course/content boundary 与 feedback digest；无完整身份/课程/action id/路径/raw资料。
    7. A2 read-only verifier exit 0、stdout allowlisted，且 current runtime/build/config/scope/generation 与 receipt 一致；任一 mismatch/human_needed/unknown 继续阻塞。
    8. Windows 仍 `not_run/human_needed`，共享 Profile 仍待 B/C/D/reauth 验证，Phase 1/2 未整体完成，Phase3 `blocked`。
  </acceptance_criteria>
  <done>仅在用户本轮真实 strict pass、四个 A2 L cells 事务追加、A2 gate 文件生成且 read-only verifier 通过后完成；其他结果全部保持 hard human gate 未通过并停止 02-18。</done>
  <resume-signal>请只粘贴 paired UI 生成的完整 `02-17_A2_RESULT` 脱敏块；不要回复“approved/通过”自由文本，不要添加姓名、邮箱、组织、课程、origin/path、Profile/Cookie、截图、页面或日志。任何 failed/human_needed block 都保持本 checkpoint 未完成并停止 02-18。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 02-16 A1 receipt / current runtime → A2 preflight | 历史 gate、旧 build/config/scope/generation 或伪造 cell 可能冒充当前可继续状态 |
| school pages/adapters → application | 页面、redirect、DOM 与 source errors 均是不可信数据；只允许固定 auth/course-visibility observation |
| paired protected UI → binding/course mutations | 浏览器输入可能伪造 candidate、scope、URL/action 或绕过 CSRF；完整身份仅可在此边界内可见 |
| human checkpoint feedback → live recorder | 聊天回复可能缺项、重放、跨轮或含私人资料；执行器不能合成用户确认 |
| paired server workflow → EvidenceLedger | action correlation 可能被重放、跨 source/scenario/platform/checkpoint/round 代填或伪造为 L authority；repo-side recorder 必须保持只读 |
| macOS A2 evidence → Windows/Phase3 gate | macOS pass 可能被错误传播为 Windows/native/live 或整体 phase eligibility |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Category | Severity | Component | Disposition | ASVS L1 intent | Mitigation plan / blocker |
|---|---|---:|---|---|---|---|
| T2-17-01 | Spoofing / Tampering | Critical | A1→A2 preflight | mitigate | V2 Authentication、V4 Access Control、V13 API/Business Logic | A1 receipt hash、current signed build/runtime/config/scope/generation、exact `A1_login×L` cells 全等；任一 stale/missing/mismatch 阻止 checkpoint。不得 accept/transfer。 |
| T2-17-02 | Spoofing / Elevation | Critical | identity candidate/binding | mitigate | V2、V4、V13 | subject+org+scope 只建 candidate；首次用户显式确认；不足则人工一次确认；显示名/email 永不自动绑定；mismatch 硬停且 course request=0。任何自动确认或继续读取是 Critical blocker。 |
| T2-17-03 | Tampering / Elevation | Critical | course visibility actions | mitigate | V4、V5 Validation、V13 | 仅 approved IDs + 四个固定 actions，严格同源/CSRF/paired；每 source 至多一次 visibility probe；拒绝 URL/selector/body/write/download/other course。越界即 hard stop。 |
| T2-17-04 | Information Disclosure | High | protected identity/course/output | mitigate | V7 Error/Logging、V8 Data Protection、V14 Configuration | full identity/course 仅 protected visible UI；receipt/chat/log/CLI/MCP 递归 allowlist；无截图、DOM/network/Profile/Cookie/action id。任一泄漏停止并重发 beta。 |
| T2-17-05 | Repudiation / Tampering | Critical | human action → L ledger | mitigate | V4、V13、V16 Logging/Error Handling | server-internal source-specific correlations；绑定 checkpoint/round/platform/source/scenario/build/generation/session/scope；actual result+one-time consume+append 单事务；repo gate 只读，真实用户 feedback 只确认 handoff，失败 event 仍保留。重放/跨轮/fixture/脚本代填为 Critical blocker。 |
| T2-17-06 | Denial of Service / Tampering | High | binding mismatch/failure handling | mitigate | V7、V13 | 封闭 safe codes、last-success 与 current failure 分离、不覆盖绑定、不自动 logout/switch/retry；unknown=`RESULT_NOT_OBSERVED` 并停止，不泄漏 exception。 |
| T2-17-07 | Tampering / Repudiation | Critical | macOS→Windows/Phase3 projection | mitigate | V4、V13、V16 | exact platform cell keys；macOS authority 无法写 Windows；A2 pass 不证明 Profile lifecycle/B/C/D/reauth；Windows fixed not_run/human_needed、Phase3 blocked。任何传播为 eligible 即 Critical blocker。 |

ASVS 5.0 L1 仅表示本计划采用的适用控制意图，不声称完整合规。所有 High/Critical 威胁必须 `mitigate`；任一 mitigation 无法证明、输出泄漏、用户反馈缺失、身份/范围 mismatch、内容边界被突破或 evidence provenance 不可验证时，本计划保持 failed/human_needed，不得作为 debt 接受、自动批准或推进 02-18。
</threat_model>

<verification>
1. 运行 A1 canonical read-only verifier，证明当前 beta/runtime/config/scope/generation 与 02-16 A1 exact pass 一致，且 stdout 仅含脱敏 allowlisted 字段。
2. checkpoint 前查询 exact ledger/counters：A2 pass cells=0、course/body/posts/files/grades request/read=0、pending action bundle=0；Windows not_run/human_needed、Phase3 blocked。
3. 用户必须完成 protected identity comparison 与显式 binding confirmation；无用户真实恢复信号不得运行 recorder、创建 gate 或成功 SUMMARY。
4. mismatch/insufficient/not-visible/denied/not-observed/content-boundary-negative 表逐项检查：都产生封闭 code、append immutable L result、course 或后续计划停止；不得被改写为 pass。
5. strict pass 后验证 exact four A2 L cells、A1 receipt hash、current build/config/scope/generation、one-time action consumption 与 `Phase2MacosA2GateReceipt` strict schema；duplicate/cross-round replay 必须失败。
6. privacy scan 覆盖 gate receipt、ledger redacted projection、stdout/stderr、SUMMARY：无 full identity/email/org/course/origin/path/Profile/Cookie/actionReceiptId/DOM/network/screenshot/exception。
7. 断言本计划未写 S/I/N 或 Windows cell，未把共享 Profile 标为通过，未改变 Phase1/2 completion 或 Phase3 eligibility。
</verification>

<success_criteria>
- AUTH-01/D-12–D-13：A1 后仅执行双来源固定 binding/course visibility live 步骤，并只验证一个批准课程可见，不读取内容。
- AUTH-02/D-03：A2 是独立 macOS L gate；不能替代 B1/B2/B3/C/D/reauth、Windows 或最终共享 Profile 结论。
- AUTH-03/D-06–D-07：严格身份证据只产生 candidate且首次需人确认；证据不足人工一次确认；任何 mismatch 硬停并禁止课程访问/绑定覆盖。
- SEC-02/D-14–D-15：完整身份/组织/课程只在 protected UI；所有持久/外部输出仅脱敏字段，截图不要求、不记录，source/raw/private 数据不泄漏。
- UAT-01/T2-08：真实用户反馈 + server-minted one-time source-specific action receipts + append-only L cells 共同构成 evidence；失败保留，strict pass gate 才可解除 02-18 前置。
- macOS-first 例外不改变 Windows `not_run/human_needed`、Phase1/2 未整体完成与 Phase3 blocked。
</success_criteria>

<output>
仅在用户 strict pass、A2 gate receipt 写入且 read-only verifier 通过后创建 `.planning/phases/02-poc-live/02-17-SUMMARY.md`。SUMMARY 只记录 checkpoint=`A2`、public beta version/build、platform=`macos`、result=`pass`、safe code=`A2_COMPLETE`、binding consistency/mode、scope/course/content-boundary 的脱敏 enum、四个 exact cell keys、observedAt、Windows/Phase3 gaps；不得记录姓名、邮箱、组织、课程名/id、origin/path、Profile/PID、Cookie/session/CSRF/actionReceiptId、DOM/network、截图、日志或原始用户消息。failed/human_needed 时不创建成功 gate/SUMMARY，保留 append-only failed/human_needed L event并等待修复或真实新动作。
</output>
