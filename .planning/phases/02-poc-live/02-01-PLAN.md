---
phase: 02-poc-live
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/model.ts
  - packages/contracts/src/index.ts
  - packages/application/src/ports.ts
  - packages/test-support/src/auth-fixture.ts
  - tests/unit/auth-contracts.test.ts
  - tests/unit/import-boundaries.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "Moodle 与 EdStem 始终具有各自独立的 auth、capability、health、freshness、completeness 与 checkedAt；不得压成单一 logged-in/ready 布尔值。"
    - "运行时来源操作只能引用已批准配置与四个固定只读动作；任意 URL、JavaScript、selector、browser handle、请求体、下载、上传或源站业务写字段在 application/driver 之前即解析失败。"
    - "身份候选必须表达稳定主体、组织/租户和批准范围证据；仅同名或同邮箱不能产生 confirmed binding，identity_mismatch 必须是课程访问硬停止状态。"
    - "Profile 所有权契约不含 Profile 路径或内容，lease/fence 不等于进程退出；已有持有者运行或身份不明时只能返回 human_needed，只有 confirmed_exited 才允许后续清理陈旧记录。"
    - "每张结果单都由 evidence class、原生 OS、来源和场景组成不可互换的精确单元；fixture 只能生成 S，不能生成 L，macOS/S/I/N 不能填 Windows/L。"
    - "Windows 单元在真实 Windows 证据前保持 not_run / human_needed；Phase 1 部分通过与 macOS-first 例外不解除 Phase 3 硬门禁。"
  artifacts:
    - path: "packages/domain/src/model.ts"
      provides: "Phase 2 来源、观察、身份绑定、Profile ownership、证据结果单和总体门禁的纯领域类型"
      contains: "export type SourceId"
    - path: "packages/contracts/src/index.ts"
      provides: "固定来源动作、严格 ownership/receipt/gate 的 Zod 运行时契约及交叉字段约束"
      contains: "SourceProbeRequestSchema"
    - path: "packages/application/src/ports.ts"
      provides: "下游 persistence、Profile coordinator 与 sealed adapter 直接实现的接口"
      contains: "ProfileOwnershipCoordinator"
    - path: "packages/test-support/src/auth-fixture.ts"
      provides: "只能产生 synthetic S 证据的 Phase 2 fixture 工厂与恶意字段样本"
      contains: "makeSyntheticReceipt"
    - path: "tests/unit/auth-contracts.test.ts"
      provides: "契约拒绝、证据隔离、Windows/Phase 3 门禁与 ownership 状态的 Wave 0 单元证据"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/domain/src/model.ts"
      via: "每个 Phase 2 strict schema 以对应领域类型为 ZodType，避免静态类型与运行时解析漂移"
      pattern: "z\\.ZodType<.*(?:Source|Binding|Profile|Receipt|Gate)"
    - from: "packages/application/src/ports.ts"
      to: "packages/domain/src/model.ts"
      via: "ports 只消费固定动作、ownership 与 receipt 类型，不暴露通用浏览器操作"
      pattern: "SourceProbePort|ProfileOwnershipCoordinator|EvidenceLedger"
    - from: "packages/test-support/src/auth-fixture.ts"
      to: "packages/contracts/src/index.ts"
      via: "fixture 工厂通过 synthetic-only schema 产出 evidence=S 的结果单"
      pattern: "SyntheticEvidenceReceiptSchema"
    - from: "tests/unit/auth-contracts.test.ts"
      to: "packages/contracts/src/index.ts"
      via: "表驱动 unknown-key、cross-cell 与 gate negative tests"
      pattern: "SourceProbeRequestSchema|EvidenceReceiptSchema|Phase2GateSchema"
---

<objective>
建立 Phase 2 不可放宽的领域/运行时边界与 Wave 0 测试骨架，使后续 persistence、Profile、Playwright adapter、API、UI 和 live ledger 都只能依赖固定来源动作、独立状态维度、可证明所有权与不可串格证据。

Purpose: 落实 D-02–D-07、D-14、AUTH-01/AUTH-02/AUTH-03/SEC-02/UAT-01，并在任何学校页面、Profile 或人工登录被接触前，先封死 T2-01、T2-02、T2-08 的高危契约绕过路径。
Output: 纯领域类型、严格 Zod schemas、application ports、synthetic-only fixture 工厂及可执行单元/架构边界测试。此计划不安装、不发布、不启动浏览器、不访问学校来源、不登录、不写 live 证据。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md

<interfaces>
现有 `packages/domain/src/model.ts`：
```typescript
export interface Observation {
  auth: 'not_observed' | 'authenticated' | 'unauthenticated' | 'reauth_required';
  capability: 'unknown' | 'available' | 'unavailable' | 'denied';
  health: 'not_observed' | 'healthy' | 'degraded' | 'error';
  freshness: 'not_observed' | 'fresh' | 'stale';
  completeness: 'not_observed' | 'complete' | 'partial';
  outcome: 'partial' | 'empty' | 'error' | 'not_observed' | 'deleted' | 'present';
  checkedAt: string | null;
}
export interface ProcessIdentity {
  installationId: string;
  role: 'api' | 'worker';
  buildId: string;
  pid: number;
  nonce: string;
  osStartIdentity: string;
  executable?: string;
}
```

现有 `packages/contracts/src/index.ts`：
```typescript
export const ObservationSchema: z.ZodType<Observation> = z.strictObject({ /* orthogonal dimensions */ });
export const MaintenanceGateSchema: z.ZodType<MaintenanceGate> = z.strictObject({ /* ownership fields */ })
  .refine(/* consistent ownership */);
```

现有 `packages/application/src/ports.ts`：
```typescript
export interface OutputPolicy {
  authorize(scope: Scope, operation: OutputOperation, destination: OutputDestination): Promise<Authorization>;
}
export interface ProcessSupervisor {
  start(launch: ProcessLaunch): Promise<ProcessIdentity>;
  stop(identity: ProcessIdentity): Promise<void>;
  inspect(identity: ProcessIdentity): Promise<'running' | 'exited' | 'identity_mismatch' | 'unknown'>;
}
```

本计划必须创建并稳定导出，供 02-02–02-12 直接依赖：
```typescript
export type SourceId = 'moodle' | 'edstem';
export type SourceAction =
  | 'moodle.auth_probe'
  | 'edstem.auth_probe'
  | 'moodle.course_visibility_probe'
  | 'edstem.course_visibility_probe';
export interface SourceProbePort {
  probe(request: SourceProbeRequest, signal: AbortSignal): Promise<SourceProbeResult>;
}
export interface ProfileOwnershipCoordinator {
  reserve(input: { installationId: string; browserBuildId: string; generation: number; fence: number }): Promise<ProfileOwnership>;
  attach(reservation: ProfileReservation, process: { pid: number; osStartIdentity: string; executable: string; startedAt: string }): Promise<ProfileOwnership>;
  inspect(owner: ProfileOwnerIdentity): Promise<ProfileOwnership>;
  release(owner: ProfileOwnerIdentity): Promise<void>;
}
export interface ProfileOwnershipStore {
  read(): Promise<ProfileOwnership>;
  acquire(reservation: ProfileReservation, context: WriteContext): Promise<ProfileOwnership>;
  renew(owner: ProfileOwnerIdentity, leaseUntil: number, context: WriteContext): Promise<ProfileOwnership>;
  markConfirmedExited(owner: ProfileOwnerIdentity, context: WriteContext): Promise<ProfileOwnership>;
  release(owner: ProfileOwnerIdentity, context: WriteContext): Promise<void>;
}
export interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: EvidenceCellKey): Promise<EvidenceReceipt[]>;
}
export type EvidenceWriterAuthority =
  | { kind: 'automated'; evidence: 'S' | 'I' | 'N'; platform: NativePlatform; producerId: string }
  | { kind: 'human_action'; evidence: 'L'; platform: NativePlatform; actionReceiptId: string };
export interface SourceConfigStore {
  read(source: SourceId): Promise<ApprovedSourceConfig | null>;
  confirm(config: ApprovedSourceConfig, context: WriteContext): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-01-1：以失败测试锁定 Phase 2 领域与运行时契约</name>
  <read_first>AGENTS.md 的 Credentials and login、Source and content policy、Data integrity and verification；.planning/phases/02-poc-live/02-CONTEXT.md D-02–D-07、D-14、D-17–D-18；.planning/phases/02-poc-live/02-RESEARCH.md Recommended Architecture §§1–5、T2-01/T2-02/T2-08、Validation Architecture；.planning/phases/02-poc-live/02-VALIDATION.md Source contracts/Evidence ledger/Wave 0；packages/domain/src/model.ts；packages/contracts/src/index.ts；packages/test-support/src/status-fixture.ts；tests/unit/contracts.test.ts。</read_first>
  <files>packages/domain/src/model.ts, packages/contracts/src/index.ts, packages/test-support/src/auth-fixture.ts, tests/unit/auth-contracts.test.ts</files>
  <behavior>
    - RED：Moodle/EdStem 的 auth/capability/health/freshness/completeness 可独立组合；network/parser/capability/auth/identity_mismatch 不得互相改写或压成 ready。
    - RED：四个 `SourceAction` 必须与其 `SourceId` 配对，operation 只接受 `approvedConfigId`/`approvedScopeId` 等稳定引用；任何额外 `url`、`js`、`selector`、`browserHandle`、`requestBody`、`download`、`upload`、`write`、`post`、`reply`、`submit`、`quizStart` 字段均被 strict schema 拒绝。
    - RED：`ApprovedSourceConfig` 仅在专门的确认契约中接受规范化 HTTPS official origin，拒绝 credentials、path、query、fragment、IP/localhost 及来源不匹配；普通 probe 永不接收导航目标。
    - RED：仅显示名/邮箱相同不足以得到 confirmed binding；稳定主体+组织/租户+批准范围只产生 candidate，首次 live 人工确认后才能 confirmed；任一身份变化产生 identity_mismatch 并保持 courseAccess=`blocked`。
    - RED：Profile ownership 不含 profilePath/cookie/storageState；reserved/owned 可 proceed，已有运行持有者为 PROFILE_IN_USE/human_needed，未知或身份不匹配为 PROFILE_OWNERSHIP_UNCONFIRMED/human_needed，只有 confirmed_exited 才允许陈旧记录进入可清理状态。
    - RED：S/I/N/L×macos/windows×moodle/edstem×A/B 子项/C/D/reauth 是精确键；L 必须带 human_action provenance，自动 receipt 只能 S/I/N，`makeSyntheticReceipt` 永远返回 S；macOS receipt 无法更新 Windows cell，任何 Windows/L 缺口都令 `phase3Eligibility='blocked'`。
  </behavior>
  <action>先创建 `tests/unit/auth-contracts.test.ts` 的表驱动 RED 用例与 `packages/test-support/src/auth-fixture.ts` 的最窄 fixture API，然后实现领域类型和严格 schema 使其 GREEN。按 D-04/D-06 将既有 `Observation` 的 auth 扩展为可表达 `identity_mismatch`，但保留五个正交维度与 `lastSuccess`/当前结果分离的能力；定义 `SourceId`、精确四项 `SourceAction`、只持有批准配置引用的 `SourceProbeRequest`、有界结构化 `SourceProbeResult`、`ApprovedSourceConfig`、`ProtectedSourceIdentity`、`IdentityEvidence`、`AccountBinding`。完整姓名/邮箱仅允许存在于明确标为 protected-local 的内部类型，receipt/gate/fixture 不得含这些字段（D-04–D-07）。定义 `ProfileReservation`、`ProfileOwnerIdentity`、`ProfileOwnershipState` 与 `ProfileOwnership`：reservation 包含 installationId/browserBuildId/nonce/generation/fence/reservedAt，owner 另含 pid/osStartIdentity/executable/startedAt，绝不含 Profile 路径；以 schema refine 固定 state/disposition/resultCode/owner/reservation 的一致关系，lease/fence 不能单独导出 exited 结论。定义 `EvidenceClass='S'|'I'|'N'|'L'`、`NativePlatform='macos'|'windows'`、逐项 `UatScenario`（A login/binding/course visibility，B reopen 1/2/3、Worker restart、Codex exit，C OS restart，D 24h recheck，reauth）、`EvidenceReceipt`、`EvidenceCell` 与 `Phase2Gate`；用 discriminated union 将 automated provenance 限制为 S/I/N，将 L 限制为真实 user_action receipt。`makeSyntheticReceipt` 的参数类型不得含 evidence/provenance 覆盖口，内部通过 `SyntheticEvidenceReceiptSchema` 固定 evidence=S，fixtures 不能接触真实学校 host。Phase 2 gate 的默认 Windows 单元必须 `not_run/human_needed`，仅全矩阵真实必需单元 pass 才允许 eligible；当前 macOS-first 例外只能改变执行顺序，不能把 Phase 1/2 或 Phase 3 标为通过（D-03、D-14、D-17–D-18）。不得实现 browser、persistence、API/UI、发布或 live writer，也不得把课程正文/帖子/文件/成绩加入契约。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/auth-contracts.test.ts tests/unit/contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `npm run test:unit -- --run tests/unit/auth-contracts.test.ts` 逐项通过：所有 forbidden keys 都返回 schema failure；四个 fixed action/source 配对通过且交叉配对失败；同名/同邮箱不能 confirmed；identity_mismatch 阻断课程访问。
    2. `rg -n "export (type|interface) (SourceId|SourceAction|SourceObservation|AccountBinding|ProfileReservation|ProfileOwnerIdentity|ProfileOwnership|EvidenceReceipt|Phase2Gate)" packages/domain/src/model.ts` 可读到所有指定领域导出；`rg -n "export const (SourceProbeRequestSchema|SourceProbeResultSchema|ProfileOwnershipSchema|SyntheticEvidenceReceiptSchema|EvidenceReceiptSchema|Phase2GateSchema)" packages/contracts/src/index.ts` 可读到对应运行时导出。
    3. `rg -n "makeSyntheticReceipt|evidence: 'S'" packages/test-support/src/auth-fixture.ts` 显示 fixture evidence 固定为 S；测试证明传入 `evidence:'L'`、live provenance、Windows/L 代填均失败，而不是只靠注释声明。
    4. `rg -n "profilePath|cookie|storageState|password|mfa|selector|browserHandle" packages/domain/src/model.ts packages/test-support/src/auth-fixture.ts` 无生产字段命中；negative-test 字符串只允许出现在 `tests/unit/auth-contracts.test.ts`。
    5. `npm run typecheck` 与既有 `tests/unit/contracts.test.ts` 均通过，证明新增 contract 未破坏 Phase 1 严格 schema。自动/fixture 结果仅为 S，不构成 N 或 L；本任务不得生成 pass receipt、登录或学校访问记录。
  </acceptance_criteria>
  <done>领域与运行时契约可被下游直接导入；任意浏览器操作字段、身份误绑、ownership 误回收和证据串格在运行时解析或 gate refine 层 fail closed；Windows 保持 not_run/human_needed 且 Phase 3 blocked。</done>
</task>

<task type="auto" tdd="true">
  <name>02-01-2：发布 sealed ports 并加固跨层边界</name>
  <read_first>packages/application/src/ports.ts；packages/domain/src/model.ts 与 packages/contracts/src/index.ts 中 Task 1 新增导出；tests/unit/import-boundaries.test.ts；.planning/phases/02-poc-live/02-PATTERNS.md 的 application ports、layering、Profile/process ownership、Evidence and hard gates；.planning/phases/02-poc-live/02-RESEARCH.md Existing Architecture Fit 与 Recommended Architecture §§1–4。</read_first>
  <files>packages/application/src/ports.ts, tests/unit/import-boundaries.test.ts</files>
  <behavior>
    - RED：application 只看 domain/contracts 和窄 ports，不导入 Playwright、filesystem、SQLite、Fastify、platform driver 或 persistence driver。
    - RED：`SourceProbePort` 只暴露 `probe(SourceProbeRequest, AbortSignal)`；不存在 navigate/evaluate/selector/browser-handle/download/write 方法。
    - RED：`ProfileOwnershipCoordinator` 使用 reserve→attach→inspect→release；running existing holder 与 unknown/mismatch 均不能进入清理/继续路径。
    - RED：observation、binding、ownership 与 evidence ledger 是独立 stores；ledger append/list 必须以完整 OS/source/scenario/evidence key 工作，调用者不能用不完整 key 或宽泛 update-all。
  </behavior>
  <action>在 `packages/application/src/ports.ts` 仅导入 Task 1 的领域类型，新增 `SourceProbePort`、`SourceConfigStore`、`SourceObservationStore`、`AccountBindingStore`、`ProfileOwnershipStore`、`EvidenceLedger` 与 `ProfileOwnershipCoordinator`。`SourceProbePort.probe` 只接收 strict `SourceProbeRequest` 和 `AbortSignal`，没有 URL/JS/selector/handle/operation closure。`SourceConfigStore` 精确为 `read(source)` 与 `confirm(config,context)`，只有专门确认流程可写 `ApprovedSourceConfig`，普通 operation 只能引用 config id；`SourceObservationStore` 精确为 `read(source)` 与 `write(observation, context)`，每个 SourceId 独立；`AccountBindingStore` 精确为 `read()` 与 `write(binding, context)`。`ProfileOwnershipStore` 精确导出 `read()`、`acquire(reservation,context)`、`renew(owner,leaseUntil,context)`、`markConfirmedExited(owner,context)`、`release(owner,context)`；owner 自带当前 fence/generation，所有写入再受 `WriteContext.expectedGeneration` 约束，lease expiry 只能 fencing，绝不授权 markConfirmedExited/release。`EvidenceLedger` 精确导出 `append(receipt,authority,context)` 与 `list(key)`，其中 `EvidenceCellKey={platform,source,scenario,evidence}`；`EvidenceWriterAuthority` 是 payload 外可信 discriminated union：automated={kind,evidence:S/I/N,platform,producerId}，human_action={kind,evidence:L,platform,actionReceiptId}，receipt 自报 provenance 不得提升权限，append 必须核对 receipt platform/evidence 与 authority 完全一致。禁止跨平台 bulk update、自动推断 L，macOS authority 不得写 Windows cell。所有 store 写入保留 operationId/generation/fence 语义，由 02-02 实现事务。`ProfileOwnershipCoordinator` 精确导出：`reserve({installationId,browserBuildId,generation,fence})` 内部生成 nonce；`attach(reservation,{pid,osStartIdentity,executable,startedAt})`；`inspect(owner)`；`release(owner)`。接口文档必须规定：reserve 前检查现有 holder，running→PROFILE_IN_USE/human_needed，unknown/identity mismatch→PROFILE_OWNERSHIP_UNCONFIRMED/human_needed；只有同一 installation+nonce+OS start identity+exact executable 的 owned record 可 release，且陈旧记录只能在 inspect=confirmed_exited 后清理。不得让 UI/CLI/MCP 直接得到 browser/Profile/store 对象。扩展 `tests/unit/import-boundaries.test.ts` 的真实图与合成反例，证明 application 不能传递导入 browser/profile/platform/persistence，MCP 仍不得绕过 authenticated API/client boundary；保持 Phase 1 已批准的窄 client credential/discovery allowlist，不误删现有规则。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/auth-contracts.test.ts tests/unit/import-boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "export interface (SourceProbePort|SourceConfigStore|SourceObservationStore|AccountBindingStore|ProfileOwnershipStore|EvidenceLedger|ProfileOwnershipCoordinator)" packages/application/src/ports.ts` 七个接口全部存在；`rg -n "reserve\(|attach\(|inspect\(|release\(" packages/application/src/ports.ts` 显示两阶段 ownership 方法签名。
    2. `rg -n "navigate|evaluate|selector|browserHandle|download|upload|postBody|storageState|profilePath" packages/application/src/ports.ts` 零命中；接口只能接受 `SourceProbeRequest`、typed domain state、完整 `EvidenceCellKey` 和 `WriteContext`。
    3. `npm run test:unit -- --run tests/unit/import-boundaries.test.ts` 既通过真实仓库图，也能对 application→Playwright/platform/persistence/profile 及 MCP→Profile/browser/store 的直接与传递依赖反例产生非零错误；既有窄 client allowlist 仍通过。
    4. `npm run typecheck &amp;&amp; npm run test:unit -- --run` 全部通过，且本计划 files_modified 之外没有产品文件变化。测试记录只能标 S；不启动 Playwright、不访问 official origins、不写 runtime/Profile/database/receipt live 状态。
  </acceptance_criteria>
  <done>后续 plans 获得稳定且分层的 source/profile/store/ledger 接口；application、MCP、CLI 与 UI 无法借接口表达任意浏览器操作、直接 Profile 访问、跨平台 evidence 更新或 live fixture 伪造。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required behavior |
|---|---|---|
| 未可信调用方 → strict runtime contracts | API/job/fixture 输入在进入 application 或 driver 前解析 | unknown key、错配 source/action 与跨格 evidence 全部 fail closed |
| 不可信来源页面 → sealed SourceProbePort | 页面内容只能由后续固定 adapter 转换为有界结构化结果 | 不执行页面指令，不接受任意导航/JS/selector/handle，不开放写入或下载 |
| application → Profile/process driver | Profile 含敏感会话；lease/fence 只防失权，不证明进程退出 | 只传 reservation/owned identity；不传路径/Cookie；未知持有者必须 human_needed |
| automated/fixture/native producer → evidence ledger | S/I/N/L、OS、source、scenario 具有不同证明含义 | producer provenance 与 evidence class 强绑定，fixture 不可产生 L，macOS 不可更新 Windows |
| Phase 2 gate → Phase 3 eligibility | 当前仅允许 macOS-first 执行顺序，不允许改变双平台完成条件 | 任一 Windows/L 或必需场景非 pass 都保持 blocked |

## STRIDE Threat Register

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 applicability | Mitigation plan | Blocking verification |
|---|---|---:|---|---|---|---|---|
| T2-01 | S/T/E | Critical | SourceProbeRequest、SourceProbePort | mitigate | ASVS 5.0 L1：V1/V2 输入与架构边界、V4 API/访问控制、V8 授权 | discriminated fixed actions、approved config refs、strict objects、unknown-key rejection、port 无通用 browser primitive | `auth-contracts.test.ts` 必须拒绝任意 URL/JS/selector/handle/write/download 与错配 action；任一失败阻止合并及后续 driver plan |
| T2-02 | I/E | Critical | Protected identity、Profile ownership、fixture/output | mitigate | ASVS 5.0 L1：V6/V7 会话与认证数据、V13/V14 数据保护、V16 日志/错误 | Profile path/content/Cookie/storageState 不进入领域/port/fixture；完整身份与 receipt 类型分离；unknown owner=human_needed | grep gate 与 runtime negative tests 必须证明无敏感字段；发现路径/秘密字段或可继续 unknown owner 时立即阻止后续 Profile/live plan |
| T2-08 | S/T/R | High | EvidenceReceipt、EvidenceLedger、Phase2Gate | mitigate | ASVS 5.0 L1：V1/V2 完整性约束、V4 受控写入、V14/V16 证据与日志完整性 | discriminated provenance、完整 cell key、synthetic factory 固定 S、Windows 缺口默认 human_needed/not_run、gate refine | automated→L、macOS→Windows、S/I/N→L 任一污染用例必须失败；否则阻止 beta/live 与 Phase 3 |

以上为本计划适用的 ASVS L1 控制映射，不声称项目已取得完整 ASVS 合规。Critical/High 项没有可接受的降级：相关测试、grep gate 或 typecheck 失败即为 blocker，不得转为“待后补”后继续依赖 wave。
</threat_model>

<verification>
1. 每个任务先写 negative/behavior tests，再实现 GREEN；本计划只产生 S 证据，不执行浏览器、native、release 或 live。
2. 目标检查：`npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/auth-contracts.test.ts tests/unit/contracts.test.ts tests/unit/import-boundaries.test.ts`。
3. Wave 1 回归：`npm run typecheck &amp;&amp; npm run test:unit -- --run`；不得使用 watch、skip、todo 或只有 grep 的方式替代行为断言。
4. 安全静态检查：`rg -n "profilePath|storageState|browserHandle|postBody" packages/domain/src/model.ts packages/application/src/ports.ts packages/test-support/src/auth-fixture.ts` 必须零命中；测试文件中的恶意 key 只作为 rejection vectors。
5. 证据审阅：读 `auth-contracts.test.ts` 的 fixtures/provenance/cell/gate 断言，确认 Windows 初始为 `not_run/human_needed`、Phase 3 为 blocked，且没有测试代码写 `L/pass` 作为 fixture 产物。
</verification>

<success_criteria>
- D-02：Moodle/EdStem 只存在固定来源、固定只读动作和已批准 origin/config 引用；密码/MFA、任意导航、浏览器脚本、下载与业务写操作在契约上不可表达。
- D-03/D-04：共享 Profile 仍为待验证假设；双来源状态保持等权独立，Windows 与 live 缺口可见且不被 macOS 自动结果覆盖。
- D-05–D-07：protected identity、binding evidence、candidate/confirmed/mismatch 和 course-access block 有明确类型；receipt/fixture 不含姓名、完整邮箱、课程名或 Profile 数据。
- D-14：结果单严格绑定版本、platform、source、scenario、evidence、result code、binding consistency、gaps 与时间；fixture 永远不能生成 L。
- T2-01/T2-02/T2-08 的所有 Critical/High negative tests 通过；否则此计划不完成、后续 wave 不得依赖。
- macOS-first 仅是执行顺序例外；Phase 1/2 未因此完成，Windows 仍 `not_run / human_needed`，Phase 3 仍 blocked。计划完成不等于实现验证、beta 发布或 live 通过。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-01-SUMMARY.md`，逐项记录实际 S 测试结果、契约导出和遗留缺口；不得记录学校身份、official origin 实值、课程名、Profile 路径、凭据、Cookie 或任何伪造 N/L 结果。
</output>
