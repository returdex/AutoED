---
phase: 02-poc-live
plan: "16"
type: execute
wave: 16
depends_on:
  - "02-15"
files_modified:
  - .planning/phases/02-poc-live/02-16-A1-RECEIPT.json
autonomous: false
requirements:
  - AUTH-01
  - SEC-02
  - UAT-01

must_haves:
  truths:
    - "02-15 本轮真实 confirmed SUMMARY 与当前 runtime 中两份 persisted approved source config/scope 必须重新核对；缺失、旧 generation、部分确认或 mismatch 时不打开任何学校页面。"
    - "只有当前 paired local UI 中的用户明确点击 `打开 Moodle 官方登录窗口` 才能启动 headed managed Chromium；用户只在 Moodle 官方页本人输入密码/MFA，产品与执行器不请求、读取、填充、记录或回传任何 credential/input/keystroke。"
    - "用户在 UI 明确声明 Moodle 登录完成后，产品才验证 observed origin 全链、来源专属 positive marker 与 bounded 完整身份；URL 可达、HTTP 200、页面看似登录或用户口头说成功都不能单独成为 authenticated。"
    - "Moodle 正面结果提交成功后才创建一个新的 EdStem bounded read-only auth probe；它只使用预先批准 origin/config/scope 和固定 `edstem.auth_probe`，不浏览课程、不下载、不写来源。"
    - "EdStem 需要 interaction/MFA 时立即进入同一 blocking human-action checkpoint，不消耗自动恢复来输入或绕过交互；只有用户本人从 paired UI 打开 EdStem 官方窗口并完成后才可再次验证。"
    - "两来源各自 observed origin、positive marker 和完整身份均通过且无 identity_mismatch 后，才能形成 macOS A1 pass；完整姓名/邮箱只在 paired protected UI，聊天/SUMMARY/receipt 只有 safe code、绑定状态和不可逆 digest。"
    - "A1 不执行课程可见性、绑定确认、正文/帖子/文件/成绩读取，不填 Windows/L 的其他场景，也不解除 Phase 1/2 未完成或 Phase 3 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-15-SUMMARY.md"
      provides: "本轮用户已在 paired UI 确认双来源、账户/组织、单课程与目的地范围的脱敏前置；非 confirmed 时 02-16 不可开始"
      contains: "02-16"
    - path: "packages/persistence/src/auth.ts"
      provides: "重新读取已确认 Moodle/EdStem config/scope 与 append-only exact-cell EvidenceLedger"
      contains: "SourceConfigStore"
    - path: "apps/api/src/auth.ts"
      provides: "paired+same-origin+CSRF 的固定 login/open、login_completed probe 和只读 status/receipt routes"
      contains: "/api/auth/login/open"
    - path: "packages/platform/src/source-adapters.ts"
      provides: "observed origin、positive marker 与 bounded identity 的 sealed Moodle/EdStem read-only probe"
      contains: "SealedSourceAdapters"
    - path: ".planning/phases/02-poc-live/02-16-A1-RECEIPT.json"
      provides: "两来源 A1 exact L ledger events 已真实形成后的脱敏 machine handoff；failed/human_needed/partial 时不得存在"
      contains: "02-16-A1-login"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "只读消费 current runtime/redacted exact-cell ledger 与临时脱敏反馈、原子写 planning handoff并供下游复验的 live gate；不生成 L authority"
      exports:
        - "recordPhase2LiveCheckpoint"
        - "verifyPhase2LiveCheckpoint"
        - "parsePhase2A1Feedback"
  key_links:
    - from: ".planning/phases/02-poc-live/02-15-SUMMARY.md"
      to: "packages/persistence/src/auth.ts"
      via: "先验证本轮 scope checkpoint=confirmed，再由 server 在每次 login action 前重新读取两来源 persisted approved config/scope；SUMMARY 不携带或替代实际配置"
      pattern: "confirmed|SourceConfigStore"
    - from: "apps/status/src/main.ts"
      to: "POST /api/auth/login/open"
      via: "paired same-origin UI 的用户点击提交 source+approvedConfigId；server 签发一次性 AuthActionAccepted，UI 不自造或持久化 actionReceiptId"
      pattern: "打开 Moodle 官方登录窗口|login/open|AuthActionAccepted"
    - from: "POST /api/auth/probe"
      to: "packages/application/src/auth-jobs.ts"
      via: "用户点击登录完成后原子消费 session/source/config/generation 绑定的 actionReceiptId，以 user_login_completed 入队；Moodle pass 后才生成新 EdStem background probe"
      pattern: "user_login_completed|edstem.auth_probe"
    - from: "packages/application/src/auth-jobs.ts"
      to: "packages/platform/src/source-adapters.ts"
      via: "Worker 在 request 前与 commit 前复核 cancel/lease/generation/fence，adapter 只执行固定 auth action 并返回结构化 origin/marker/identity observation"
      pattern: "SourceProbePort|assertCurrent"
    - from: "packages/application/src/auth.ts"
      to: "packages/persistence/src/auth.ts"
      via: "由真实 paired human action correlation 建立 payload 外 macOS human_action authority，分别 append Moodle/EdStem scenario=A1_login、evidence=L events；后台 probe 自报 L 无效"
      pattern: "human_action|A1_login|EvidenceLedger"
    - from: "GET /api/auth/status"
      to: ".planning/phases/02-poc-live/02-16-A1-RECEIPT.json"
      via: "用户复制 UI 生成的 strict 脱敏 A1 result block；执行器只在两个 exact L events 与 current protected status 一致后构造 handoff，不复制 full identity/origin/course"
      pattern: "A1_AUTHENTICATED|candidate|courseVisibility"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "GET /api/auth/status + GET /api/auth/receipts"
      via: "复用 current installation 的只读 local client credential/discovery，读取 redacted status 和两个 exact A1/macOS/L cells；没有 DB、Profile、login 或 receipt-write 权限"
      pattern: "recordPhase2LiveCheckpoint|verifyPhase2LiveCheckpoint|read-only"
---

<objective>
执行 macOS 检查点 A1 的不可自动跨越登录门：用户从当前 paired local UI 打开 Moodle 官方窗口并本人完成密码/MFA；产品仅在用户声明完成后验证 Moodle 的 approved observed origin、positive auth marker 和本地完整身份，再对 EdStem 执行有界只读 auth probe；若 EdStem 需要交互/MFA，立即再次暂停等待用户本人处理。

Purpose: 落实 D-02、D-05、D-12–D-15 与 AUTH-01/SEC-02/UAT-01，用真实用户动作和真实 macOS 学校页面证明两个来源各自的认证事实，同时让 credential/input、完整身份、Profile、页面和网络资料始终留在受保护边界内。
Output: Moodle/EdStem 各自的 macOS A1 `EvidenceLedger` L event、仓库侧 strict read-only-input live gate、严格脱敏的 `02-16-A1-RECEIPT.json` 与 02-16 SUMMARY；不确认账户绑定、不运行课程可见性、不读取课程内容。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-06-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-15-SUMMARY.md
@.planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json
@release/phase2-beta-artifacts.json
@release/phase2-availability.json
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/ports.ts
@packages/application/src/auth.ts
@packages/application/src/auth-jobs.ts
@packages/persistence/src/auth.ts
@packages/platform/src/browser.ts
@packages/platform/src/source-adapters.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts

<interfaces>
执行时必须以 02-01–02-09 SUMMARY 与实际导出为准；允许机械适配名字，不得改变以下安全语义：

```typescript
export interface SourceConfigStore {
  read(source: 'moodle'|'edstem'): Promise<ApprovedSourceConfig|null>;
}

export interface AuthActionAccepted {
  accepted: true;
  actionReceiptId: string; // server-only one-time correlation; never shown/copy/log/persisted by UI
  resultCode: SafeAuthErrorCode;
}

export interface AuthProbeCommand {
  source: 'moodle'|'edstem';
  approvedConfigId: string;
  approvedScopeId: string;
  trigger: 'background'|'user_login_completed'|'manual_retry';
  idempotencyKey: string;
}

POST /api/auth/login/open
body: { source: 'moodle'|'edstem'; approvedConfigId: string }

POST /api/auth/probe
body: strict AuthProbeCommand & { actionReceiptId?: string }
```

`login/open` 和 `trigger='user_login_completed'` 都只允许 current paired local UI principal、exact same-origin、CSRF、current generation 与重新读取的 persisted approved config/scope；Moodle actionReceiptId 不能用于 EdStem，EdStem background probe 不得携带它。用户点击完成后 ID 被原子消费一次；reload、401/403、restart、source/config/generation mismatch 或重放均失败。ID 只证明 user action correlation，不因存在就等于 probe pass 或 L authority。

产品生成、用户只复制的 strict 脱敏响应契约如下。`resultCode` 必须来自已发布 `SafeAuthErrorCode` allowlist；本计划只接受成功 `AUTHENTICATED`，以及失败/阻断 `AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|INTERACTION_REQUIRED|ORIGIN_MISMATCH|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR`，不得接受自由字符串：

```text
02-16_A1_RESULT
checkpoint=02-16-A1-login
platform=macos
version=<current exact beta version>
build_id=<current exact build id>
moodle=pass|failed|human_needed
moodle_code=<SafeAuthErrorCode>
edstem=pass|failed|human_needed|not_run
edstem_code=<SafeAuthErrorCode|NOT_RUN>
binding=candidate|not_observed|mismatch
course_visibility=not_run
content_access=not_started
windows=not_run/human_needed
phase3=blocked
observed_at=<ISO-8601 with timezone>
```

当首次块为 `moodle=pass,moodle_code=AUTHENTICATED,edstem=human_needed,edstem_code=INTERACTION_REQUIRED` 时，它只是同一 blocking checkpoint 的中间状态：不创建 pass handoff、不推进 02-17；执行器必须显示 EdStem 人工步骤并再次暂停。最终 A1 pass 必须是 Moodle/EdStem 均 `pass/AUTHENTICATED`、binding=`candidate`、course/content 未开始与固定 gaps 全部同时成立。

strict pass 后才创建的唯一 planning handoff schema：

```typescript
export interface Phase2MacosA1GateReceipt {
  schema: 1;
  checkpoint: '02-16-A1-login';
  platform: 'macos';
  evidence: 'L';
  version: string;
  buildId: string;
  artifactSha256: string;
  result: 'pass';
  resultCode: 'A1_AUTHENTICATED';
  order: readonly ['moodle','edstem'];
  sources: readonly [
    { source:'moodle'; scenario:'A1_login'; result:'pass'; resultCode:'AUTHENTICATED'; observedOrigin:'matched_approved'; positiveMarker:'observed'; identity:'protected_local_observed'; interaction:'completed_by_user'; ledgerEventDigest:string },
    { source:'edstem'; scenario:'A1_login'; result:'pass'; resultCode:'AUTHENTICATED'; observedOrigin:'matched_approved'; positiveMarker:'observed'; identity:'protected_local_observed'; interaction:'not_required'|'completed_by_user'; ledgerEventDigest:string }
  ];
  bindingConsistency: 'candidate';
  courseVisibility: 'not_run';
  contentAccess: 'not_started';
  captures: { screenshot:false; rawNetwork:false; credentials:false; inputs:false };
  windows: 'not_run/human_needed';
  phase3: 'blocked';
  observedAt: string;
  feedbackDigest: string;
}
```

仓库侧 gate 的稳定接口与 CLI 已由 02-37 在 identity selection/signing 前实现并穷尽测试；本计划只消费，且不得修改 `phase2-live-gate.mjs` 或 `phase2-live-gate.test.ts`。签名 grammar 的 A1 分支必须精确为：

```typescript
export interface RecordPhase2LiveCheckpointInput {
  checkpoint: 'A1';
  feedbackPath: string; // executor-owned 0700 mktemp 下的 strict JSON；finally 删除
  outPath: '.planning/phases/02-poc-live/02-16-A1-RECEIPT.json';
  runtime: 'current';
  readOnlyInputs: true;
}

export function recordPhase2LiveCheckpoint(input: RecordPhase2LiveCheckpointInput): Promise<Phase2MacosA1GateReceipt>;
export function verifyPhase2LiveCheckpoint(input: {
  receiptPath: '.planning/phases/02-poc-live/02-16-A1-RECEIPT.json';
  runtime: 'current';
  readOnly: true;
}): Promise<{schema:1;status:'pass';checkpoint:'A1';version:string;buildId:string;binding:'candidate';courseVisibility:'not_run';windows:'not_run/human_needed';phase3:'blocked'}>;
```

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --record --checkpoint A1 --feedback <0700-mktemp>/A1-result.json \
  --out .planning/phases/02-poc-live/02-16-A1-RECEIPT.json \
  --runtime current --read-only-inputs

node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --receipt .planning/phases/02-poc-live/02-16-A1-RECEIPT.json \
  --runtime current --read-only
```

`--record` 不是 live receipt write authority：它只能通过既有 local client credential/discovery 对 current loopback API 执行 redacted `GET /api/auth/status` 与两个 exact `GET /api/auth/receipts?platform=macos&source={moodle|edstem}&scenario=A1_login&evidence=L`，strict parse user feedback JSON，核对 current version/build/artifact/runtime generation、两个 latest non-corrected pass event digest 与 status，然后只按 02-37 的 0600 same-directory temporary + fsync/re-parse + atomic no-replace contract写 exact `--out`。目标已存在、竞争写入、partial write 或任何 failed/human_needed 时不得覆盖/删除已有文件，也不得留下新 pass receipt。它不得 import persistence driver、直接打开 DB/WAL、调用 EvidenceLedger.append、接受 actionReceiptId、访问 protected identity/config、打开浏览器或发送学校请求。`--verify` 对 receipt/current redacted runtime/两 exact cells 重做相同只读核对且不写任何文件。两模式 argv 都 strict allowlist；stdout 仅为上述 allowlisted JSON，stderr 仅 stable safe code，不输出 feedback/input/output path、runtime endpoint/port、PID、Profile、login/private identity、origin/course、action ID、raw response。

`ledgerEventDigest` 与 `feedbackDigest` 均为规范化脱敏 event/block 的 SHA-256，不是 actionReceiptId、identity fingerprint 或 raw payload。Moodle/EdStem L append 的唯一 authority 是 server 在 current paired human action workflow 内生成的 payload 外 `{kind:'human_action',evidence:'L',platform:'macos',actionReceiptId}`：它必须同时绑定本轮 UI action correlation、actual probe result、source/config/generation/build 和 scenario=`A1_login`。后台 Worker、fixture、计划执行器、聊天 block 或 receipt payload 自报均不能生成此 authority；EdStem 无交互时只能继承 server 保存的本轮 A1 workflow correlation，不能凭 background job 自升 L。若已发布 beta 缺少这条内部 authority/provenance 链，立即 `human_needed` 并回到修复→完整测试→新 immutable beta→实际更新，不在本计划加 route/script/DB write 绕过。

02-17 先运行上述 `--verify`，再消费 current paired `ProtectedAuthStatusProjection`：两 source `auth='authenticated'`、current result=`AUTHENTICATED`、完整 `identity` 仅 protected local UI、binding `consistency='candidate'`，`nextAction={kind:'confirm_binding',candidateBindingId}`。candidate ID 只从 current protected projection 取得并仅由 UI 提交，不进入 A1 receipt/chat/SUMMARY。binding confirm 固定为 `POST /api/auth/binding/confirm {candidateBindingId,decision:'confirm'|'reject'}`；确认前 course visibility 必须 `not_run`。确认后 02-17 只能对两个来源分别提交 strict `SourceProbeRequest {source,action:'moodle.course_visibility_probe'|'edstem.course_visibility_probe',approvedConfigId,approvedScopeId}`，由 approved scope 内部解析一个课程并只返回 visible/not_visible safe result；请求/receipt/chat 不接受 course name/URL且不读正文/帖子/文件/成绩。
</interfaces>

<hard_gates>
- 必须先读取 02-15 SUMMARY 并确认 `result=confirmed`、`02-16 blocked=false`，再由 server 实时重新读取 Moodle/EdStem persisted config/scope；SUMMARY 不是配置来源。缺一来源、generation/config/scope drift、paired session 失效或 current build 不同都停止，不打开官方窗口。
- 用户的 password/MFA 只输入官方页面；执行器/产品不得请求用户在聊天或 AutoED UI 输入，不得调用 fill/type/press/submit 代输，不得读取 input value/keystroke/request body/header/Cookie/storageState，不得打开 DevTools 或 capture console。
- 无 screenshot、trace、video、HAR、raw HTML/DOM、request/response、login screenshot 或录屏；即使用户自愿截图也不是 pass 条件，本计划不请求、不保存、不放入 Git/SUMMARY/receipt。
- 只有 Moodle pass 后才可新建 EdStem auth probe；EdStem interaction/MFA 必须暂停等待新的真实 paired UI user action，不自动弹连续窗口、不重试输入、不假设共享 SSO。
- 任一 observed origin 越界、marker 缺失/歧义、identity_mismatch、Profile ownership unknown/in-use、write/download/popup、network/parser/capability error 都 fail closed；不继续课程、不覆盖旧 binding、不切号/登出。
- 02-16 只完成 A1 auth；binding confirmation、single-course visibility 和最终 A feedback 属于 02-17。Windows 仍 `not_run/human_needed`，共享 Profile 仍待 B/C/D/reauth 验证，Phase 3 blocked。
- checkpoint 必须等待本轮真实用户返回 exact 脱敏 block；generic auto-mode、`approved`、旧回复、人工猜 safe code、URL/200、发布/测试/native receipt 都不能自动批准。
- `phase2-live-gate.mjs` 只能读取 current redacted status/exact-cell receipts 和临时反馈并写 planning handoff；它没有 L writer/DB/Profile/browser/source 权限。若 beta 未由 server 内部形成合格 L event，工具必须阻断而非补写。
</hard_gates>
</context>

<tasks>

<task type="auto">
  <name>02-16-1：消费签名 live handoff gate，并复验 current beta、scope 与 A1 零副作用前置</name>
  <read_first>AGENTS.md 全文；02-CONTEXT.md D-02、D-05、D-12–D-15；02-RESEARCH.md Fixed source configuration/Auth and retry state machine/Probe contract/Validation Architecture；02-VALIDATION.md beta/live hard gates；02-PATTERNS.md browser/source adapter、API/UI、Evidence/UAT；02-15-SUMMARY.md 原文与 02-15 plan 的 exact response contract；02-14 update receipt、02-13 artifacts/availability；02-01/02-02/02-05/02-07/02-08/02-09 SUMMARY 的最终 stores、human-action correlation、Worker、routes、presenters 与 ledger contracts。不得读取 legacy、实际 Profile、Cookie、DB rows、配置值、身份或课程。</read_first>
  <files>.planning/phases/02-poc-live/02-16-A1-RECEIPT.json</files>
  <action>消费已在02-37实现、02-38完整测试并随02-39签名的 `<interfaces>` `phase2-live-gate.mjs`；先核对其当前安装hash/capability manifest与发布artifact完全一致，再原样运行签名的A1 negative/contract suite，不修改脚本或测试。gate只接受 exact final A1 feedback + current build/artifact + Moodle/EdStem 各一个 macOS/A1_login/L/pass/AUTHENTICATED event + binding candidate/course not_run，并原子写 strict handoff。missing/extra/unknown feedback key、非 allowlisted code、intermediate/failed/human_needed、时间或identity drift、missing/duplicate/conflicting/corrected event、source/scenario/platform/evidence串格、fixture/N/S/I冒充L或敏感输出均必须由既有签名实现fail closed；若能力缺失或hash不符，回到修复→02-38全测→新immutable beta→实际更新，绝不在本计划补分支。

随后要求 `.planning/phases/02-poc-live/02-15-SUMMARY.md` 存在且 strict 记录本轮 checkpoint=`02-15`、result=`confirmed`、allowlisted safe code、current version/build、`02-16 blocked=false`、Windows/Phase3 固定缺口；failed/human_needed、旧消息、缺 SUMMARY、build mismatch 或 SUMMARY 含 actual config/identity/course 时立即阻断。再运行 02-13 current runtime 只读 gate，要求实际 macOS beta version/build/artifact hash/signature、API/Worker/paired UI 与 02-14 receipt 相同；不得把 published/available 当 installed/current。

通过 application/status 的只读、脱敏 gate 验证 server 可对 `moodle` 与 `edstem` 各调用一次 `SourceConfigStore.read` 并 strict parse 当前 approved config/scope：两来源都 confirmed、config/scope IDs 存在、同 current generation/build、无 partial/mismatch；输出只允许 `{source,configured:true,generationCurrent:true}`，不得打印 origin/account/org/course/destination/ID/path。确认 ProtectedAuthStatusProjection current nextAction 精确为 Moodle `open_login` 且 full identity 尚未被非 paired output 读取；若已经存在 A1 pass、旧 pending actionReceiptId、EdStem job、school request 或 login window，必须显示 `human_needed`，不得重放/接管。

读取脱敏 counters 快照：officialLoginLaunch/sourceRequest/authProbeEnqueued/A1 L events 均为本轮基线；preflight 本身的增量必须全为 0。检查当前 Profile ownership 只允许 available 或本安装 current owned/可安全复用状态；`PROFILE_IN_USE`/`PROFILE_OWNERSHIP_UNCONFIRMED` 立即阻断，lease expiry 不清理、不 kill、不删锁。运行定向 integration/UI security tests，证明 login/open 在 paired+same-origin+CSRF+current config 之前 launch=0，且 route 不接受 URL/JS/selector/browser handle/credentials/input/screenshot/operation 字段。此 task 不访问学校、不创建 actionReceiptId、不写 L。</action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts tests/integration/auth-api.test.ts tests/integration/auth-security-matrix.test.ts tests/integration/auth-worker.test.ts &amp;&amp; test -s .planning/phases/02-poc-live/02-15-SUMMARY.md &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --read-only &amp;&amp; npm run test:ui -- --grep "Moodle login action|EdStem interaction required|protected identity|A1 checkpoint"</automated>
  </verify>
  <acceptance_criteria>
    1. 02-15 SUMMARY 为本轮 confirmed、current version/build、02-16 unblocked，且不含 actual origin/account/org/course/destination；任何 failed/human_needed/missing/stale/mismatch 停止。
    2. current runtime gate 退出 0并只输出脱敏 allowlisted build/readiness；current beta 与 02-13/02-14 receipt exact match，API/Worker/paired UI ready、Windows/Phase3 固定 blocked。
    3. server 在 action 前重新读取两来源 persisted config/scope，脱敏结果均 configured/current；缺任一来源、config/scope/generation drift 或 strict parse failure 时 login launcher/job/source call=0。
    4. preflight 前后 official login launch、source request、auth job 与 A1 L event 增量全为 0；没有 DNS/HTTP/Playwright official window/Profile content access。
    5. public/pending/CLI/MCP、跨源/缺错 CSRF、extra URL/JS/selector/handle/credential/input/screenshot/write/download 字段均在 registry/launcher/job/adapter 前拒绝；无 skip/todo。
    6. gate tests 证明 `--record` 仅从 strict feedback/current redacted status/两个 exact L cells构造 handoff，`--verify` 只复验；任一负例 out/ledger/runtime 不变，DB/persistence/append/login/probe/browser/school调用全为 0，输出无 path/PID/Profile/private scope。
    7. Profile ownership running-by-other/unknown/PID reuse 为 human_needed 且 kill/lock removal/release=0；本 task 不修改配置/ledger/runtime，不创建真实 A1 handoff。
  </acceptance_criteria>
  <done>当前安装、02-15 人工范围门、双来源 persisted config/scope、paired action admission 与 Profile ownership 均可进入 A1；尚未打开学校或生成任何 live 结果。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-16-2：用户本人完成 Moodle 登录，并在需要时本人完成 EdStem 交互</name>
  <read_first>Task 1 脱敏 pass；02-15-SUMMARY.md 的 current version/build 与 paired UI 打开方式；02-UI-SPEC.md LoginActionPanel/PrivacyNotice/CheckpointLedger/Copywriting Contract；02-05/02-06/02-07/02-08/02-09 SUMMARY 的 official-login、sealed probe、bounded Worker、one-time action、protected projection 与 receipt 语义；本 plan interfaces/hard_gates。不得读取 protected identity/config/DOM、Profile、Cookie、页面 URL path、截图、input 或 raw network。</read_first>
  <files>.planning/phases/02-poc-live/02-16-A1-RECEIPT.json（仅 strict final pass 后创建；intermediate/failed/human_needed 时不得存在）</files>
  <what-built>当前 immutable beta 已通过自动/原生/安装与范围门，paired local UI 可用一次性 human action 打开 Moodle 官方窗口；真实登录、MFA、两个来源的 live origin/marker/identity 仍未通过。</what-built>
  <action>完成 Task 1 后必须暂停并逐字展示 `<how-to-verify>` 与 `<resume-signal>`，不得代用户打开/点击 official login、输入凭据/MFA、观察输入、读取页面或合成结果。用户在 UI 点击 Moodle login/open 时，server 必须重新读取 current approved config/scope，签发一次性 session/source/config/generation 绑定 action receipt，并只让唯一 ProfileCoordinator 以受管 Chromium+专属 Profile headed 模式打开 approved Moodle authentication origin；不存在 arbitrary URL/selector/JS/handle，capture 全关。

用户点击 UI 的 `我已完成 Moodle 登录` 后，server 原子消费 Moodle action receipt，才入队 `user_login_completed` probe。Worker 在每次 request/commit 前检查 cancel/lease/generation/fence；sealed Moodle adapter 逐跳要求 observed origin=`matched_approved`、唯一 bounded positive marker 与 bounded identity evidence，完整姓名/学校邮箱只进入 paired protected status。成功 event 提交后才创建全新的 EdStem `background` auth probe；绝不复制 Moodle authenticated、复用 Moodle action ID或预先访问 EdStem。EdStem background probe 同样只允许 approved origin+GET/HEAD+fixed `edstem.auth_probe`，无课程/下载/写。

若 EdStem 直接得到 authenticated，UI 生成 final `02-16_A1_RESULT`。若返回 `INTERACTION_REQUIRED`（包括 MFA/popup 需要本人动作），立即停止 Worker 的自动恢复/新 popup，显示唯一 next action `打开 EdStem 官方登录窗口` 并先要求用户返回 intermediate exact block；执行器不得完成 task，而要再次暂停并向同一用户显示 EdStem 步骤。用户明确点击后 server 签发新的 EdStem-only action receipt，用户在 approved EdStem official window 本人完成交互/MFA并点击 `我已完成 EdStem 登录`；随后只重验 EdStem，Moodle last-success 保留且不重登。非 interaction 的 NETWORK/PARSER/CAPABILITY/ORIGIN/IDENTITY/ownership 错误按 02-07 规则停止/有界处理，不能借人工动作绕过。

每个实际 human login workflow 的 server-side correlation 与 exact probe result 才能构造 payload 外 macOS L authority；Moodle 与 EdStem 分别 append exact `{platform:'macos',source,scenario:'A1_login',evidence:'L'}` event。EdStem 无 interaction 时，由 server 内部 A1 workflow correlation 证明它是 Moodle human action 后的规定 follow-up；单独 background job/payload/chat 不能自升 L。两 source event 均 pass、current protected status 均 authenticated、binding=candidate、无 mismatch且用户返回 final strict block后，执行器把 strict allowlisted block解析为 JSON写入本次 `mktemp -d` 的 0600 文件（不保存原聊天/路径，finally删除），运行 canonical `phase2-live-gate.mjs --record --checkpoint A1 ... --read-only-inputs`。只能由该 gate 对照 current version/build/artifact、redacted status与两个 exact L event digests并原子创建 `02-16-A1-RECEIPT.json`；随后立即运行 canonical `--verify --runtime current --read-only`。文件不含 action ID/full identity/origin/course/Profile。任一非 final pass、record/verify失败不创建/保留 handoff，不执行 02-17。</action>
  <how-to-verify>
    1. 在 02-15 同一个 current beta paired local status page 查看 PrivacyNotice；不要在聊天粘贴地址、pairing token、identity、course 或截图。确认只有 Moodle CTA `打开 Moodle 官方登录窗口` 可用，EdStem/绑定/课程动作仍 disabled。
    2. 点击该 Moodle CTA 一次。只在新开的官方 Moodle 页面核对浏览器显示的 official host 与你在 02-15 确认的来源一致；若不一致、出现第二个意外窗口、download、课程页或非官方跳转，立即关闭本次窗口并返回 failed/human_needed safe code，不输入任何资料。
    3. 在 Moodle 官方页面本人输入密码并完成 MFA/学校安全挑战。不要把 password、MFA code、Cookie、页面内容、URL path、姓名/邮箱或屏幕截图发给 Codex/AutoED；本 agent 不会询问这些值。
    4. 页面明确完成登录后回到 paired local UI，点击 `我已完成 Moodle 登录` 一次；不要只在聊天说“完成”。等待页面依次显示 Moodle verification，再显示 EdStem bounded probe；Moodle 未显示 authenticated 前不得有 EdStem request。
    5. 在 protected Moodle card 本人查看 official origin、positive auth 状态与完整 identity；确认内容属于你预期账户即可，不把姓名/邮箱复制到聊天。若显示 mismatch、marker/origin/parser/network/ownership 错误，点击停止并复制 UI 的 strict 脱敏 A1 block。
    6. 若 EdStem 自动 authenticated，在 protected EdStem card 本人核对其 official origin、positive auth 状态与完整 identity；不确认 binding、不点击课程。使用 `复制脱敏结果单` 取得 final `02-16_A1_RESULT`，检查其中没有姓名/邮箱/origin/course/path/Profile，再按 resume signal 返回。
    7. 若 UI 显示 EdStem `INTERACTION_REQUIRED`，不要自行触发重复 probe；先复制并返回 intermediate `02-16_A1_RESULT`。执行器会保持同一 checkpoint blocking 并给出下一步，绝不会把它当 pass。
    8. 收到 EdStem 人工步骤后，只在 paired UI 点击 `打开 EdStem 官方登录窗口` 一次，在该官方页面本人完成必要交互/MFA，再回 UI 点击 `我已完成 EdStem 登录`；最后按第 6 步核对并返回新的 final block。不要主动登出/切号、复制 Cookie、迁移 Profile或打开课程内容。
  </how-to-verify>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --read-only &amp;&amp; if test -f .planning/phases/02-poc-live/02-16-A1-RECEIPT.json; then node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --receipt .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only; fi</automated>
  </verify>
  <acceptance_criteria>
    1. 用户本人在 paired UI 明确点击 Moodle open/completed；server action receipt 是一次性并绑定 current session/source/config/generation，UI/chat/storage/DOM/clipboard/log 不出现其值；重放与 EdStem reuse 均失败。
    2. Moodle 只在 approved observed origin 全链、唯一 positive marker 与 bounded identity 全部成立后为 pass；用户口头成功、URL/HTTP 200、login page 或旧 last-success 不能代替。完整 identity 只在 paired protected UI。
    3. EdStem request/job timestamp 严格晚于 Moodle positive result commit；只执行 fixed read-only auth probe。若 interaction/MFA，自动 retry/popup=0且 task 再次暂停；只有新的 EdStem user action 后才重验。
    4. 两 source current auth 均 authenticated、safe code 均 AUTHENTICATED、observed origin matched、positive marker observed、identity protected_local_observed；identity_mismatch/origin/parser/capability/network/ownership/write/download 任一结果不能创建 pass handoff或课程请求。
    5. Moodle 与 EdStem exact A1_login/macOS/L ledger events 各一项，authority 都来自 server-side current human workflow correlation；后台/fixture/executor/payload 自报 L、跨 source/platform/scenario、重复冲突 append 均失败。Windows L row 增量=0。
    6. 用户 final response 逐行匹配 exact `02-16_A1_RESULT`，两个 source 均 pass/AUTHENTICATED、binding=candidate、course_visibility=not_run、content_access=not_started、Windows/Phase3 固定；intermediate、自由文本 `approved`、旧 block、missing/extra/sensitive字段均不完成 task。
    7. `--record` 只在 strict final conjunction 后原子创建 `02-16-A1-RECEIPT.json`；version/build/artifact、source event digests、observedAt/feedbackDigest 与 current receipts 一致，无姓名/邮箱/origin/path/course/Profile/PID/Cookie/actionReceiptId/raw页面/网络/截图/input。临时 feedback JSON 已删除且不进入 Git。
    8. canonical `--verify --receipt ... --runtime current --read-only` 退出 0并仅输出 allowlisted status/checkpoint/version/build/binding/course/Windows/Phase3；receipt、current redacted status 或任一 exact L cell drift 时非零且文件/runtime/ledger不变。
    9. credential/input/capture audit 全为 false/zero；没有 password/MFA/readInput/fill/type/press/submit、screenshot/HAR/trace/video/console/request-body capture，也没有课程 visibility/content 请求。02-17 只因 A1 receipt strict pass 才可开始。
  </acceptance_criteria>
  <resume-signal>请只粘贴 paired UI 的完整 `02-16_A1_RESULT` 脱敏块，不要改字段、手工发明 safe code或附加说明。若块显示 EdStem `human_needed/INTERACTION_REQUIRED`，我会保持本 checkpoint 未完成并给出 EdStem 本人操作步骤；最终只接受 Moodle 与 EdStem 均 `pass/AUTHENTICATED` 的新块。不要发送密码、MFA、姓名、邮箱、origin、课程、Profile、Cookie、截图、页面或日志。</resume-signal>
  <done>只有本轮真实 Moodle 人工登录、随后 EdStem 自动或人工完成并经产品分别验证、两个 macOS A1 L events 与用户 final strict block全部一致时，本 task 才完成并创建 A1 handoff；其他状态全部保持 human_needed/failed 和 02-17 blocked。</done>
</task>

</tasks>

<threat_model>
本计划采用 OWASP ASVS 5.0 L1 的身份验证、会话、访问控制、输入/输出验证、敏感数据与日志安全意图；不声明完整合规。所有 Critical/High 项均 disposition=`mitigate`，任一控制未证明即阻断 A1/02-17。

## Trust Boundaries

| Boundary | 不可信输入/风险 | 强制控制 |
|---|---|---|
| 02-15 SUMMARY → persisted approved config | SUMMARY/旧回复被当配置，config drift 或部分确认后仍登录 | strict confirmed summary + current build；server 每次 action 重读两 source store；只使用 opaque config/scope IDs；缺失即 launch=0 |
| user/paired UI → login/open/completed | 未配对/CSRF/重放/自动点击或聊天声明伪造 human action | HttpOnly paired session、exact same-origin、CSRF、server one-time action registry、session/source/config/generation binding、blocking user response |
| official login page → credentials/MFA | 产品/agent 读取、输入、记录或把认证权限扩大为业务写 | 用户本人输入；无 fill/type/submit/readInput/request-body/console；interactive auth origin only；capture 全关；无 source business writes |
| source page → sealed adapter | URL/200/marker 欺骗、越界 redirect、恶意指令、download/write、过量 identity | 逐跳 approved origin、唯一 bounded positive marker、固定 locators/actions、GET/HEAD only、size/time limits、页面永不作为工具指令 |
| Moodle result → EdStem follow-up | 未证实 Moodle 就探测 Ed、复制 auth/identity、无限恢复或自动 MFA | Moodle committed positive result 先于新 Ed job；Ed 独立 probe/state；interaction 立即 human_needed；bounded retry/fence |
| protected identity → output/chat/receipt | 完整姓名/邮箱、origin/course/Profile/页面网络泄漏 | 完整 identity 只在 paired visible text；strict redacted block/receipt；no-store/401 purge；无截图/log/raw dump |
| human action + probe → EvidenceLedger | action ID/后台 job/fixture 自报 live，跨 source/OS/scenario 写入 | payload 外 human_action authority + actual result/source/config/build/A1 binding；append-only exact cell；macOS 不能写 Windows；chat block非authority |
| redacted runtime/feedback → planning handoff | repo tool 直读 DB、补写 L、接受篡改反馈或泄漏路径/identity | read-only local client + exact cells + strict feedback；record只写allowlisted out；verify零写；no persistence/browser/source imports |

## STRIDE / ASVS L1 威胁登记

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 控制意图 | 具体缓解与 blocker |
|---|---|---:|---|---|---|---|
| T2-01 | Spoofing/Tampering/Elevation | Critical | fixed login/probe route、adapter、origin/marker | mitigate | V1 boundary、V4 function authorization、V5 validation、V13 business logic | paired+CSRF、current approved IDs、逐跳 origin+positive marker、fixed action、无 arbitrary URL/JS/selector/handle/write/download。任一越界或 URL/200→auth 即 blocker。 |
| T2-02 | Information Disclosure | Critical | official page、credentials/input、Profile、output | mitigate | V6 auth secret handling、V8 data protection、V16 logging | user-only input；no input/body/header/Cookie read；no screenshot/HAR/trace/video/console/raw DOM/network；strict output allowlist。任一 capture/leak 即 blocker。 |
| T2-03 | Spoofing/Tampering | High | Moodle/EdStem identity 与 binding candidate | mitigate | V2 identity proof、V4 object authorization、V13 state transition | 每 source bounded stable evidence；完整 identity protected-only；mismatch hard stop；A1 只 candidate，02-17 人工确认。仅同名/邮箱或自动 confirm 即 blocker。 |
| T2-04/T2-05 | Spoofing/Tampering/DoS | Critical | Profile owner、Worker lease/generation/fence | mitigate | V1 lifecycle boundary、V4 ownership、V13 transaction integrity | installation+nonce+PID/start/executable/control proof；每 request/commit 双检查；lease expiry不清理；unknown/in-use human_needed，无 kill/删锁。 |
| T2-07 | Spoofing/Elevation/Disclosure | High | pairing/session/action registry/protected UI | mitigate | V2 session、V4 permission、V5 CSRF、V14 config | paired browser only、same-origin+CSRF、one-time action、401/403 purge、no-store；CLI/MCP/public不能打开窗口或看 identity。 |
| T2-08 | Tampering/Repudiation/Elevation | High | human checkpoint、L receipt、platform gate | mitigate | V1 evidence separation、V4 writer authority、V16 provenance | 本轮真实 user action + actual result 才有 macOS L authority；exact source/scenario cells；无 auto-approve/replay；Windows not_run、Phase3 blocked。任一伪/串格即 blocker。 |
| T2-16-GATE | Spoofing/Tampering/Information Disclosure | High | phase2-live-gate、feedback、planning receipt | mitigate | V4 least privilege、V5 strict validation、V8 data minimization、V16 safe logging | strict argv/schema、current runtime/build/two exact L digests、read-only endpoints、atomic allowlisted out、safe stdout/error；无 DB/append/Profile/browser/school权限。任一 drift/泄漏/写runtime即 blocker。 |
</threat_model>

<verification>
1. 在任何 human step 前执行 Task 1 current build verifier和定向 integration/UI suites；02-15 SUMMARY/current config/paired action/Profile ownership任一不符时学校 launch/request=0。
2. 记录脱敏 baseline counters；只有用户点击 Moodle open 后 officialLoginLaunch 增加，只有用户点击 completed 后 Moodle probe 增加，只有 Moodle committed AUTHENTICATED 后 EdStem probe 增加。
3. Moodle/EdStem 各验证 approved observed origin、唯一 positive marker、bounded protected identity；检查 public/CLI/MCP/log/diagnostic/receipt/chat 不含 full identity/config/page/Profile/credential。
4. EdStem INTERACTION_REQUIRED 必须返回 intermediate block并再次暂停；确认自动 retry/new popup=0，新的 EdStem action receipt 仅在用户点击后签发且只可消费一次。
5. final block strict parse 后只通过 `phase2-live-gate --record` 与两项 exact A1_login/macOS/L ledger events 对齐；failed/human_needed/mismatch/not_run/extra字段不创建 `02-16-A1-RECEIPT.json`。
6. 运行 canonical `phase2-live-gate --verify`；最终 handoff strict schema、version/build/artifact/event digests与 current redacted runtime一致，工具无 DB/ledger/browser/source写且输出脱敏。
7. course visibility/content request=0，Windows not_run/human_needed，Phase 3 blocked。
</verification>

<success_criteria>
- AUTH-01/D-02：用户本人只在官方页面完成 Moodle 密码/MFA；Moodle verified 后才有独立 EdStem bounded auth probe，Ed interaction/MFA 会再次停等本人。
- AUTH-01/D-05：两个来源都凭 approved observed origin、positive marker 与 bounded identity 成为 authenticated；完整身份只在 paired protected UI，非 protected输出只有 safe code/digest。
- SEC-02/D-13：只执行固定 auth probes，不读取 selected course visibility或正文/帖子/文件/成绩，不允许业务写/download或 arbitrary browser operation。
- UAT-01/D-12/D-14：两 source 的 macOS A1_login L cells 来自真实 user-action authority并独立追加；final handoff只在两个 pass 与真实用户脱敏反馈同时成立时存在。
- 下游门：repo-side gate 无 live writer 权限，只把 existing exact L cells+strict human feedback 绑定成 planning handoff；02-17/02-18 可用同一 `--verify --runtime current --read-only` 入口复验 A1，不能直接读 DB或复用 action ID。
- D-15：截图/录屏/页面/raw network/input 从未被请求或保存；截图不是证据或 pass 条件。
- A1 pass 只使 02-17 可以开始 identity/binding/course visibility；共享 Profile 仍待 B/C/D/reauth，Windows仍 not_run/human_needed，Phase 1/2未整体完成，Phase 3 blocked。
</success_criteria>

<output>
只有 strict final pass 后才创建 `.planning/phases/02-poc-live/02-16-SUMMARY.md`，记录 checkpoint=`02-16-A1-login`、current beta version/build、platform=macos、Moodle/EdStem safe result、interaction path=`automatic|user_completed`、binding=candidate、两个 ledger event digest、observedAt、course/content 未开始及 Windows/Phase3 gaps；不得记录实际 origin、完整身份/邮箱、organization/course、Profile/PID/path、Cookie/actionReceiptId、credentials/MFA/input、DOM/page/network、截图或日志。failed/human_needed/intermediate 时不创建 pass receipt/SUMMARY，不推进 02-17。
</output>
