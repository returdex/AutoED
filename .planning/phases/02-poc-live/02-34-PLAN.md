---
phase: 02-poc-live
plan: "34"
type: execute
wave: 35
depends_on: ["02-33"]
files_modified: []
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "Phase 2 completes only when Phase 1's still-applicable required native/human gates, every Phase 2 requirement and every current signed-build artifact/security/identity/rights/privacy condition strictly pass."
    - "The final gate distinguishes 176 possible cells from exactly 44 required L cells (22 per platform) and separately requires every named current-build S/I/N obligation."
    - "All 44 required L cells are current, non-corrected, payload-external human_action results for Moodle/EdStem × 11 scenarios × macOS/Windows; missing or non-pass required cells block, while possible nonrequired cells may remain not_run."
    - "No SUMMARY, chat response, publication, fixture, lower evidence class, other platform/source/scenario, WSL/static artifact or old build can satisfy an obligation."
    - "D-18: Completing macOS design, planning, implementation, automated tests, beta publication and macOS checkpoints cannot mark Phase 2 passed or enter Phase 3; Phase 3 becomes eligible only after the original dual-platform hard gates are complete, unless the user explicitly redefines platform scope and synchronizes requirements and roadmap, and even a strict final pass never starts Phase 3 automatically."
  artifacts:
    - path: scripts/release/phase2-live-gate.mjs
      provides: "Signed immutable final exact-requiredness verifier"
    - path: packages/domain/src/live-evidence.ts
      provides: "176 possible cells, 44 required L cells and named S/I/N obligations"
    - path: release/phase2-beta-artifacts.json
      provides: "Current immutable signed beta identity"
  key_links:
    - from: scripts/release/phase2-live-gate.mjs
      to: packages/domain/src/live-evidence.ts
      via: "closed requiredEvidenceKeys and PHASE2_BUILD_OBLIGATIONS; no CLI scope override"
      pattern: "requiredEvidenceKeys|PHASE2_BUILD_OBLIGATIONS"
    - from: scripts/release/phase2-live-gate.mjs
      to: apps/api/src/auth.ts
      via: "fixed local client reads only redacted current evidence/status projections"
      pattern: "runtime current|read-only|final"
    - from: release/phase2-beta-artifacts.json
      to: scripts/release/phase2-live-gate.mjs
      via: "exact manifest member hash and current version/build equality"
      pattern: "sha256|buildId|checkpointCapabilities"
---

<objective>
Perform the final zero-write, dual-platform Phase 2 reachability and completion audit.

Purpose: Declare completion only from the exact approved obligations, not from the entire possible-cell vocabulary or planning artifacts.
Output: Deterministic safe final projection and, only on strict pass, a redacted Phase 2 SUMMARY. No Phase 3 execution occurs.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-35-SUMMARY.md
@.planning/phases/02-poc-live/02-39-SUMMARY.md
@.planning/phases/02-poc-live/02-40-SUMMARY.md
@.planning/phases/02-poc-live/02-33-SUMMARY.md
@scripts/release/phase2-live-gate.mjs
@release/phase2-beta-artifacts.json

<interfaces>
Canonical command:
`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint final --runtime current --platform all --read-only`

Required pass projection:
`{schema:1,status:'pass',resultCode:'PHASE2_DUAL_PLATFORM_COMPLETE',possibleCells:176,requiredLiveCells:44,requiredLivePass:44,macosRequiredLive:22,windowsRequiredLive:22,buildObligations:{S:'pass',I:'pass',N:{macos:'pass',windows:'pass'}},requirements:{AUTH_01:'pass',AUTH_02:'pass',AUTH_03:'pass',AUTH_04:'pass',SEC_02:'pass',UAT_01:'pass'},phase1:'complete',phase2:'complete',phase3:'eligible',sharedProfile:'adopted'}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the immutable final evidence closure read-only</name>
  <files>None (read-only audit)</files>
  <action>Require completed current-build outputs for Plans 02-01–02-33 and 02-35–02-40 as applicable, including every hard human update/login/lifecycle/reauth gate; missing, blocked or stale artifacts become sorted safe gaps and are never fabricated. Validate the Phase 1 fixed required-gate registry independently: current partial/no-Node/OS-denial/Windows gaps remain blockers and Phase 2 evidence cannot promote Phase 1. Verify the selected version/commit/tree/build/artifact/signature/capability hashes match the actually installed macOS and Windows runtimes and anonymous publication receipts. Consume the fixed registries only from signed production code: `possibleEvidenceKeys()` must be 176, `requiredEvidenceKeys()` exactly 44 L cells (22 per platform), and named S/I/N obligations must have complete duplicate-free requirement mappings. Read every required L key through the redacted API and require current, non-corrected, exact platform/source/scenario/build human_action authority. Separately verify all fixed T2-01–T2-08 S/I reports and actual native macOS/Windows N receipts, with no skip/todo/only/fixture promotion. Check binding, rights, read-only operation, protected identity, last-success, Profile ownership/continuity and no sensitive capture. Do not read DB/WAL/Profile/private payloads or run school/browser operations.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform macos --runtime current --read-only &amp;&amp; node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --audit-matrix --platform windows --runtime current --read-only &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts tests/integration/evidence-isolation-matrix.test.ts -t "final|requiredness|one-cell|cross-platform|old build"</automated></verify>
  <done>The closure has 176 possible/44 required-live semantics, all named S/I/N obligations and all cross-cutting prerequisites, with deterministic gaps and zero side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the signed final conjunction and preserve blocked truth</name>
  <files>None (read-only final projection)</files>
  <action>Run the signed final branch from the current installed artifact. It may return `PHASE2_DUAL_PLATFORM_COMPLETE` only when Phase 1 is complete, all 44 required L cells pass, every named S/I/N obligation passes on its required current build/platform, all six requirements pass, artifact/security/binding/rights/privacy/Profile conditions pass, and the macOS/Windows API/UI projections agree. Run negative fixtures deleting one required L cell, one named S/I/N obligation and one Phase 1 gate, plus wrong build, forged SUMMARY, fixture-L and macOS→Windows substitutions; each must return `PHASE2_GATE_BLOCKED` and preserve input hashes/counters. Nonrequired possible cells left not_run must not appear as required gaps. On blocked result, do not create a success SUMMARY or modify STATE/ROADMAP/Phase 3. On strict pass, create only the redacted SUMMARY and report Phase 3 eligible-but-not-executed.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint final --runtime current --platform all --read-only &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-live-gate.test.ts -t "final gate|required live 44|named build obligations|Phase 1 prerequisite"</automated></verify>
  <done>Completion is deterministic from exact required evidence; otherwise the phase remains truthfully blocked without mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| possible vocabulary → requiredness | Nonrequired possible cells must not become blanket completion gates or default pass. |
| reports/ledger → final gate | Stale, forged, cross-class or cross-platform evidence must not satisfy current obligations. |
| Phase 1/2 → Phase 3 | Completion projection must not execute downstream work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-34-01 | T/R | exact requiredness | mitigate | Fixed signed 176 possible/44 required-live counts plus closed named S/I/N registry and count tests. |
| T2-34-02 | S/T/E | evidence provenance | mitigate | Payload-external authority, current build/platform/source/scenario keys and correction-chain validation. |
| T2-34-03 | T/E | Phase 1/3 reachability | mitigate | Independent Phase 1 registry and eligible-only Phase 3 output; no downstream invocation. |
| T2-34-04 | I | final output | mitigate | Stable safe gaps and allowlisted projection; no private payload, path, account, Profile or action ID. |
</threat_model>

<verification>
Verify both platform audits, the exact final command and one-obligation deletion/substitution negatives. All commands must be read-only and bound to the signed current build.
</verification>

<success_criteria>
- 176 possible cells remain auditable; exactly 44 L cells are required and pass.
- Every named S/I/N obligation and all six requirements pass on the current signed build.
- Phase 1 is independently complete and Phase 3 is only marked eligible.
- Any genuine gap returns a stable blocker without fabricating or deleting evidence.
</success_criteria>

<output>
Only after strict pass, create `.planning/phases/02-poc-live/02-34-SUMMARY.md` with public version/build/artifact hashes, possible=176, requiredLive=44 (22/22), named S/I/N pass, six requirement results, Phase1/2 complete, sharedProfile adopted and Phase3 eligible-but-not-executed. Never include device, path, PID, Profile, account, course, origin, action ID, raw error, log, network or screenshot data.
</output>
