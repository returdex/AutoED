---
phase: 02-poc-live
plan: "18"
type: execute
wave: 14
depends_on:
  - "02-17"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-04
  - UAT-01

must_haves:
  truths:
    - "用户在真实 macOS 上逐轮正常关闭并重开 AutoED 专属 Profile 三次；每轮必须完成后才显示下一轮，执行器不得自动关闭/重开、合并三轮或代填用户结果。"
    - "每轮重开都分别复核 Moodle、EdStem、同一专属 Profile 观察、已确认身份/授权范围及恰好一个批准课程的最小可见性读取；不读取课程正文、帖子、文件、成绩或其他 Phase 3/4 内容。"
    - "每轮只有在 OS 确认本安装拥有的 browser 已实际退出后才可重开；running/unknown/PID reuse/identity mismatch 返回 profile_in_use 或 human_needed，不杀日常/未知浏览器、不删仍在使用的锁。"
    - "B1-1、B1-2、B1-3 分别形成独立 macOS/L/scenario/source 脱敏 receipt；一轮 pass、另一场景 receipt 或任何 S/I/N 结果都不能满足缺失的轮次。"
    - "任一轮 Moodle 或 EdStem failed/human_needed、identity/scope 不一致、共享 Profile 未观察、最小读取未确认、敏感输出或 receipt 缺失都会立即停止，不呈现下一轮并阻断 02-19 B2。"
    - "三轮全 pass 仍只说明 B1；共享 Profile 继续是待验证假设，Windows 保持 not_run/human_needed，Phase 3 继续 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-17-A2-RECEIPT.json"
      provides: "A2 strict pass machine gate：绑定 A1 hash、当前 beta/build、confirmed binding、matched scope、单课程双来源可见及四个 A2 L cells"
      contains: "A2_COMPLETE"
    - path: ".planning/phases/02-poc-live/02-17-SUMMARY.md"
      provides: "A2 身份、绑定、单课程可见性及当前 beta/build 的严格人工成功前置；非 pass 时 B1 不可开始"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger，按 macOS×source×B1 round×L 精确保存三轮真实人工结果并拒绝跨格替代"
      contains: "EvidenceLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "B1 唯一 repo-side read-only verifier；只读取 paired runtime 的 exact cells 并派生脱敏编排输出，绝不 mint/consume action、append/correct ledger 或充当 L writer"
      contains: "verifyPhase2LiveCheckpoint"
    - path: "packages/platform/src/profile-coordinator.ts"
      provides: "专属 Profile single-owner、OS actual-exit confirmation、generation/fence 与 only-owned recovery"
      contains: "ProfileOwnershipCoordinator"
    - path: "packages/platform/src/browser-provider.ts"
      provides: "受管 Chromium persistent Profile 的正常 close/reopen 与每请求 ownership guard"
      contains: "LocalPlaywrightBrowserProvider"
    - path: "apps/status/src/main.ts"
      provides: "paired protected UI 的 B1-1/B1-2/B1-3 独立行、当前唯一动作、完整本地身份/范围核对与脱敏结果单复制"
      contains: "CheckpointLedger"
    - path: ".planning/phases/02-poc-live/02-18-SUMMARY.md"
      provides: "只含三轮脱敏结果、build/platform/time/codes/gaps 与 B2 eligibility 的硬门记录"
  key_links:
    - from: "apps/status/src/main.ts"
      to: "packages/platform/src/profile-coordinator.ts"
      via: "每轮用户明确触发正常 close/reopen；UI 等待 actual-exit/ownership projection，不能以 lease expiry 或按钮点击冒充完成"
      pattern: "PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|PROFILE_EXITED"
    - from: "packages/platform/src/browser-provider.ts"
      to: "packages/application/src/auth-jobs.ts"
      via: "重开后仅以当前 generation/fence/owner 运行 Moodle→EdStem fixed auth/course-visibility probes；旧 owner/Worker 不得请求或提交"
      pattern: "BrowserRequestGuard|assertCurrent"
    - from: "packages/application/src/auth-jobs.ts"
      to: "packages/persistence/src/auth.ts"
      via: "每轮只有 published paired server workflow 以新 mint 的 pending-action bundle、真实 user action 与 actual observation 同事务 append exact B1 round/source/L receipts"
      pattern: "EvidenceWriterAuthority|EvidenceLedger"
    - from: ".planning/phases/02-poc-live/02-17-A2-RECEIPT.json"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "B1 每轮前以 canonical A2 verifier 重验 current build/runtime、A1 hash、四个 A2 L cells 与内容未读边界"
      pattern: "A2_COMPLETE|A2_binding|A2_course_visibility"
    - from: "B1-1/B1-2/B1-3 exact L cells"
      to: "02-19 eligibility"
      via: "三轮×双来源 required cells 全部 pass 的 conjunction；missing/failed/human_needed/S/I/N/Windows/另一 scenario 均不可替代"
      pattern: "reopen|macos|moodle|edstem|evidence"
---

<objective>
执行 macOS live 检查点 B1：由用户将同一个 AutoED 专属 Profile 正常关闭并重开三次，每轮独立验证双来源会话、共享 Profile 观察、身份/授权范围与一个批准课程的最小可见性读取，并生成不可互相替代的脱敏 live receipts。

Purpose: 落实 D-03、D-12、D-14 与 AUTH-02/AUTH-04/UAT-01，以真实用户/真实 Profile 生命周期证据验证“共享 Profile 能连续恢复”而不是凭一次成功或 S/I/N 推断；任一失败即时阻断 B2。
Output: 三个严格顺序的 blocking human-action checkpoints，以及 B1-1/B1-2/B1-3 各自的 macOS L receipts/脱敏反馈；不修改产品源码，不执行 Worker/Codex/OS restart、24h 或 reauth。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-12-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-15-SUMMARY.md
@.planning/phases/02-poc-live/02-16-SUMMARY.md
@.planning/phases/02-poc-live/02-17-SUMMARY.md
@.planning/phases/02-poc-live/02-17-A2-RECEIPT.json
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/browser-provider.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时先读取 02-17 SUMMARY 与它交付的 exact A2 gate/只读验证命令；不得以 A1、配置确认、synthetic UI、原生 ownership 测试或自由文本“登录过了”代替 A2。直接使用最终 02-01/02-02/02-03/02-05/02-07/02-08 contract，不重定义第二套 receipt、Profile 或 browser 接口：

```typescript
export type UatScenario =
  | 'A1_login' | 'A2_binding' | 'A2_course_visibility'
  | 'B1_reopen_1' | 'B1_reopen_2' | 'B1_reopen_3'
  | 'B2_worker_restart' | 'B3_codex_exit'
  | 'C_os_restart' | 'D_24h_recheck' | 'reauth';

export interface EvidenceCellKey {
  platform: 'macos' | 'windows';
  source: 'moodle' | 'edstem';
  scenario: UatScenario; // 使用最终导出的 B1 reopen round 1/2/3 三个 exact enum
  evidence: 'S' | 'I' | 'N' | 'L';
}
export type EvidenceWriterAuthority =
  | { kind: 'automated'; evidence: 'S' | 'I' | 'N'; platform: NativePlatform; producerId: string }
  | { kind: 'human_action'; evidence: 'L'; platform: NativePlatform; actionReceiptId: string };
export interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: EvidenceCellKey): Promise<EvidenceReceipt[]>;
}

export function verifyPhase2LiveCheckpoint(input: Phase2LiveCheckpointVerification): Promise<RedactedLiveCheckpointResult>;
export function validatePhase2MacosA2GateReceipt(value: unknown): Phase2MacosA2GateReceipt;

export interface ProfileOwnershipCoordinator {
  reserve(input: { installationId: string; browserBuildId: string; generation: number; fence: number }): Promise<ProfileOwnership>;
  attach(reservation: ProfileReservation, process: { pid: number; osStartIdentity: string; executable: string; startedAt: string }): Promise<ProfileOwnership>;
  inspect(owner: ProfileOwnerIdentity): Promise<ProfileOwnership>;
  release(owner: ProfileOwnerIdentity): Promise<void>;
}

export interface BrowserProbeSession {
  navigate(target: URL, guard: BrowserRequestGuard): Promise<NavigationObservation>;
  waitFor(locator: BrowserLocatorSpec, guard: BrowserRequestGuard): Promise<void>;
  readVisible(locator: BrowserLocatorSpec, guard: BrowserRequestGuard): Promise<string>;
  readAttribute(locator: BrowserLocatorSpec, name: string, guard: BrowserRequestGuard): Promise<string | null>;
  close(guard: BrowserRequestGuard): Promise<void>;
}
```

每轮只允许四个 sealed actions：`moodle.auth_probe`、`edstem.auth_probe`、`moodle.course_visibility_probe`、`edstem.course_visibility_probe`，且 operation 只引用 02-15 已确认的 `approvedConfigId/approvedScopeId`。所谓“最小读取”就是正面认证标志、稳定身份/组织证据与一个批准课程的 visible marker；不读取/输出课程正文、帖子、文件、成绩、DOM、URL path 或原始网络资料。

02-17 A2 前置只读命令精确为：

```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --receipt .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --a1 .planning/phases/02-poc-live/02-16-A1-RECEIPT.json --runtime current --read-only
```

三轮必须分别使用 exact `B1_reopen_1`、`B1_reopen_2`、`B1_reopen_3`，并为 Moodle/EdStem 各形成 exact macOS/L cell。每一轮都必须由 published beta 的 paired protected UI 创建新的 server-minted pending-action bundle；客户端、执行器、repo script 和用户不能传 `actionReceiptId`。每个 source child 绑定 checkpoint B1、round、platform=macos、source、exact scenario、version/build、runtime generation、paired session、approved config/scope 与 expiry；只有 paired server workflow 能在真实 user action + actual result 同时成立时，一次性 consume 与 append 同事务。failed/human_needed 也作为 immutable L result 保留并停止；重试必须由新的真实用户动作生成新 bundle/correction link，不能覆盖，一个 round/source 不能重放到另一格。

`phase2-live-gate.mjs` 对 B1 只允许执行下列 read-only verification；它可以派生不含隐私的编排输出，但绝不能取得 pending-action、mint/consume authority、append/correct ledger、直读 DB 或把 chat feedback 写回 runtime。用户返回的 `02-18_B1_<n>_RESULT` 只供 executor strict parse 与 checkpoint 编排，不是 L authority。不得新增 route、自由写 receipt API、任何 repo-side 写模式、第二个 ledger、B1 planning receipt JSON，或把 SUMMARY 当 source of truth：

```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round <1|2|3> --a2 .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --runtime current --platform macos --read-only
```

若已发布 beta 不支持 exact B1 discriminator、`--round`、每轮新的 server pending-action bundle、paired server-only L append 或 exact-cell read-only verification，必须返回 `human_needed` 并回到修复→完整测试→新 immutable beta→实际更新；不得在 live 时加临时 DB/CLI writer、改脚本绕过或把用户反馈/SUMMARY当 authority。
</interfaces>

<hard_gates>
- 02-17 A2 必须是当前 exact beta/build 上的真实 macOS pass：双来源身份/组织/批准范围一致、binding confirmed、同一指定课程 visibility confirmed、无敏感输出；非 pass/缺 receipt/旧 build/identity mismatch 均不得开始 B1-1。
- 三轮严格顺序：B1-1 pass 才呈现 B1-2，B1-1+B1-2 pass 才呈现 B1-3；任一轮 failed/human_needed/not_run 立即停止并阻断 02-19，不能要求用户继续“收集更多信息”。
- 用户只能正常关闭产品创建的专属 Profile/browser；不得关闭/杀日常 Chrome、手工删锁、按 PID/进程名终止、修改 lease/Cookie/storage 或复制 Profile。running/unknown/ownership mismatch 立即 human_needed。
- 每轮重开只由 paired local UI 的当前 B1 动作触发；不得用 shell、任意 URL、浏览器 handle、Playwright closure、fixture、fresh Profile 或另一安装绕过。
- 每轮 receipt 必须是 macOS+L+exact round+exact source+current build 的真实 human-action 结果；S/I/N、另一 round/source/platform、旧 build 或一个合并 pass 均不能满足 required cells。
- feedback/SUMMARY/log 只含 checkpoint/round、version/build、platform、source、time、result/safe code、binding consistency/shared-profile observation 与 gap；无完整身份、课程名、origin/path、Profile/PID/Cookie、actionReceiptId、截图或 raw page/network。
- B1 不执行 B2 Worker restart、B3 Codex exit、C/D/reauth；Windows 仍 not_run/human_needed，Phase 3 blocked，共享 Profile 不标最终采用。
</hard_gates>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-18-1：用户完成专属 Profile 第一次正常关闭重开并核对 B1-1</name>
  <read_first>AGENTS.md 的 Credentials and login、Data integrity and verification；02-CONTEXT.md D-03、D-09、D-12–D-15、D-17–D-18；02-17-SUMMARY.md 的 exact A2 pass gate、build、binding/scope/course状态、paired server-only L writer与repo read-only verifier；02-UI-SPEC CheckpointLedger/Privacy；02-03/02-05/02-07 SUMMARY 的 actual-exit、close/reopen、guard/fence；最终 UatScenario/EvidenceLedger/RedactedEvidenceReceipt contract。</read_first>
  <files>none（用户动作只影响仓库外专属 Profile runtime 与既有 append-only UAT ledger；不修改源码、Profile 内容或学校资料）</files>
  <what-built>当前 beta 已通过 A：用户完成 Moodle 登录/MFA、EdStem 有界认证、双来源身份/绑定与一个批准课程可见性核对；B1-1 行仍为 not_run/human_needed，B2/B3/C/D/reauth 和 Windows 均未开始。</what-built>
  <action>执行器先运行 `<interfaces>` 的 exact A2 read-only command，并核对当前 installed version/build、paired session、两个来源 authenticated、binding/approved scope/current single-course visibility 与 no-sensitive-output 状态仍匹配；同时用 B1 read-only verifier 查询 B1-1 Moodle/EdStem macOS L cells 必须为空/not_run，B1-2/B1-3 也未运行。任何 mismatch/旧 build/预填 receipt 即停止。全部通过后硬暂停，原样呈现 `<how-to-verify>` 与 `<resume-signal>`；不得代替用户关闭/重开、点击 UI 或自动批准。

    用户只能从 paired protected UI 启动 B1-1；published paired server 必须为本轮 mint 新 pending-action bundle，并在真实 user action、close→actual-exit→reopen 与 actual product observation 全部成立时，一次性 consume/append Moodle 与 EdStem 的 `B1_reopen_1` macOS/L results。执行器、repo gate 与聊天 feedback 都不得取得 authority 或写 ledger。恢复时只 strict parse 本轮固定脱敏反馈；即使 product observation 为 pass，未收到真实用户本轮反馈也保持 checkpoint 未完成。failed/human_needed 必须保留 runtime immutable 结果/缺口并立即结束本计划，不呈现 Task 2；反馈后只运行 exact B1 `--verify --round 1`，证明两 source pass、B1-2/B1-3仍空，才允许继续。</action>
  <how-to-verify>
    1. 在 paired local UI 核对当前 checkpoint 是 `B1-1 第一次关闭重开`、version/build 与 A2 相同，Moodle/EdStem、binding 与批准范围未出现 mismatch。不要把页面中的姓名、邮箱、课程或 origin 发到聊天。
    2. 只通过产品提供的正常关闭动作关闭 AutoED 专属 Profile/browser；不要强退、kill PID、删锁、退出日常 Chrome、修改 Profile/Cookie 或重启 Worker/Codex/macOS。
    3. 等待 UI 明确报告 browser actual exit/ownership released。若显示 `PROFILE_IN_USE`、`PROFILE_OWNERSHIP_UNCONFIRMED`、仍在关闭或未知，停止并返回 human_needed；不要重复启动。
    4. 在 paired UI 触发 `B1-1` 的重开/再次检查一次。产品应以同一专属 Profile 在后台依次检查 Moodle、EdStem；有效时不弹窗。出现登录/MFA/额外交互、第二实例、越界页面或下载时立即停止。
    5. 在 protected UI 本地核对两来源 authenticated、shared Profile=`已观察复用`、完整身份与 A2 相同、binding/组织/批准范围一致、同一指定课程 visibility=`已确认可见`；不要打开课程正文、帖子、文件或成绩。
    6. 从 B1-1 ledger 行读取/复制脱敏结果：两来源各自显示 platform=macos、evidence=L、scenario=B1-1、current build/time、result/safe code、binding consistency/shared-profile/gap；内容不得含私人字段、action ID、Profile/PID、截图或 raw network/page。
    7. 只按 `<resume-signal>` 返回本轮脱敏块。若任一来源或共同项非 pass，返回真实 failed/human_needed 并停止，不执行第二轮。
  </how-to-verify>
  <verify>
    <automated>checkpoint 前运行 exact A2 verifier；用户反馈且 paired server workflow 已形成本轮真实结果后，只运行 `node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 1 --a2 .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --runtime current --platform macos --read-only`。repo tool 不得 append/correct L；任何 non-pass 只读取 immutable failure 并保持阻断。</automated>
  </verify>
  <acceptance_criteria>
    1. 02-17 exact A2 gate 与 current build/status 全 pass；`B1_reopen_1|B1_reopen_2|B1_reopen_3` 在动作前无 macOS L pass，Windows/L 的其他单元未被预填。
    2. 用户本人正常关闭且 OS actual-exit confirmed 后才重开；无 kill/delete-lock/lease-as-exit/daily-browser影响，旧 owner/guard/Worker 请求与提交为 0。
    3. 重开只执行四个 fixed actions；Moodle/EdStem 均核对 auth、shared Profile、同一身份/binding/approved scope 与一个课程 visibility，没有正文/下载/写操作。
    4. scenario=`B1_reopen_1` 的 Moodle/EdStem exact macOS/L receipts 各自存在且 current build/time/result 一致；`B1_reopen_2|B1_reopen_3` cells 仍 not_run，S/I/N 或另一 source不能满足。
    5. user feedback/receipt/SUMMARY候选无 identity/course/origin/Profile/PID/Cookie/actionReceiptId/screenshot/raw page/network；Windows/Phase3不变。
    6. 任一 non-pass 立即阻止 Task 2 与02-19；只有两 source `B1_reopen_1` pass conjunction 和真实用户脱敏反馈才能完成本 task。
  </acceptance_criteria>
  <resume-signal>只回复完整 `02-18_B1_1_RESULT` 脱敏块：`result=pass|failed|human_needed`、`result_code=&lt;allowlisted code&gt;`、`moodle=pass|failed|human_needed`、`moodle_code=&lt;code&gt;`、`edstem=pass|failed|human_needed`、`edstem_code=&lt;code&gt;`、`shared_profile=observed|not_observed`、`identity_scope=consistent|mismatch|not_observed`、`minimal_read=pass|failed|human_needed`、`content_boundary=preserved|violated|not_observed`、`screenshot=not_used`、`observed_at=&lt;ISO-8601 with timezone&gt;`。不要发送姓名、邮箱、课程、origin、Profile/PID/Cookie、action ID、截图或日志。</resume-signal>
  <done>真实用户在当前 macOS beta 上完成 B1-1 正常关闭/actual-exit/重开；双来源 `B1_reopen_1` exact L cells 与脱敏反馈均 pass 后才可进入 B1-2，否则 B1/02-19 保持阻断。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-18-2：用户完成专属 Profile 第二次正常关闭重开并核对 B1-2</name>
  <read_first>Task 1 的 strict pass 与 B1-1 双来源 exact macOS/L receipts；02-17 A2 gate/current build；02-03/02-05/02-07 lifecycle/fence contracts；02-UI-SPEC B1-2 独立 ledger row；最终 `B1_reopen_2` UatScenario、paired server-only L writer 与 repo read-only verifier。</read_first>
  <files>none（只更新仓库外 runtime/Profile observation 与既有 append-only B1-2 ledger cells）</files>
  <what-built>B1-1 已有当前 build、双来源、macOS/L 的独立真实 pass；B1-2/B1-3 与 B2 仍为 not_run/human_needed，第一次结果不能复用。</what-built>
  <action>先运行 exact A2 verifier 与 B1 `--verify --round 1`，重新证明 A2 与 B1-1 两来源 pass、B1-2/B1-3 cells 空、current build/session/config/binding未变；任何历史 correction、failed/human_needed、build/identity/scope变化均停止。然后硬暂停并要求用户从 paired protected UI 启动 B1-2，完整重复 `<how-to-verify>` 的正常 close→actual exit→paired UI reopen→双来源核对流程，不能把第一次已开过算第二次。published paired server 必须 mint 与 `B1_reopen_2`/current session/build/generation 绑定的新 pending-action bundle，拒绝 B1-1 receipt/action重放，并只在真实 human action + actual observation 同事务 consume/append。恢复只接受 `02-18_B1_2_RESULT`；repo 侧只 strict parse feedback 并运行 B1 `--verify --round 2`，不得调用任何写接口、append DB 或把 feedback 变成 authority。任一 non-pass 立即停止，不呈现 Task 3。</action>
  <how-to-verify>
    1. 确认 UI 当前行明确是 `B1-2 第二次关闭重开`，B1-1仍独立 pass，B1-2未运行；version/build、双来源身份/binding/scope与上一轮一致。
    2. 再次只用产品正常关闭动作关闭专属 Profile，等 OS actual-exit/ownership release；running/unknown/profile_in_use即停止，不kill/删锁/重复启动。
    3. 只在 paired UI 触发 B1-2 重开一次，等待 Moodle→EdStem fixed background probes；不自动输入凭据、不处理意外popup/download/write，不访问课程正文。
    4. protected UI 本地核对两来源 authenticated、shared Profile observed、identity/binding/approved scope一致、同一单课程 visibility confirmed；任何变化返回 failed/human_needed。
    5. 核对 B1-2 新脱敏结果属于 current build+macos+L+B1-2，并同时保持 B1-1原 receipt不变、B1-3空；只返回本轮固定脱敏块。
  </how-to-verify>
  <verify>
    <automated>checkpoint 前运行 exact A2 verifier 与 B1 round 1 verifier；用户反馈且 paired server 已形成本轮真实结果后，只运行 `node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B1 --round 2 --a2 .planning/phases/02-poc-live/02-17-A2-RECEIPT.json --runtime current --platform macos --read-only`；repo tool 写操作必须为零。</automated>
  </verify>
  <acceptance_criteria>
    1. `B1_reopen_1` 两 source exact L pass 仍不可变，`B1_reopen_2` 动作前为空；第一轮 receipt/action 不能重放到第二轮。
    2. 第二次 normal close与actual exit是新的真实生命周期事件，新的 owner generation/fence有效，旧owner/guard不能请求或提交。
    3. Moodle/EdStem/shared Profile/identity/binding/approved scope/single-course minimal visibility全部独立复核，无正文/下载/写/敏感输出。
    4. scenario=`B1_reopen_2` 的两 source current-build macOS/L receipts 均 pass，`B1_reopen_1` 保持原值、`B1_reopen_3` 仍 not_run；没有 S/I/N/Windows/另一 scenario 代填。
    5. fixed feedback严格脱敏；任一failed/human_needed/缺项/隐私命中立即阻止Task 3和02-19。
  </acceptance_criteria>
  <resume-signal>只回复完整 `02-18_B1_2_RESULT` 块，字段与 B1-1 模板完全相同；所有值必须来自 B1-2 当前脱敏 UI结果。不要发送任何实际身份、课程、origin、Profile/PID/Cookie、action ID、截图或日志。</resume-signal>
  <done>第二次真实 close/reopen 的双来源 `B1_reopen_2` macOS/L cells 独立 pass 且未改写第一次后才可进入 B1-3；其他结果保持 B1/02-19 blocked。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-18-3：用户完成专属 Profile 第三次正常关闭重开并封闭 B1 gate</name>
  <read_first>Task 1/2 的 strict pass 与 B1-1/B1-2 exact receipts；02-17 A2/current build gate；最终 `B1_reopen_3` UatScenario、paired server-only L writer、EvidenceLedger/repo read-only verifier；02-CONTEXT D-03/D-12/D-14/D-17–D-18。</read_first>
  <files>none（只更新仓库外 runtime observation 与 append-only B1-3 cells；完成后由 executor 写脱敏02-18 SUMMARY）</files>
  <what-built>B1-1、B1-2 已分别由真实用户动作和双来源 macOS/L receipts通过；B1-3仍未运行，02-19 B2、后续macOS和全部Windows单元继续阻塞。</what-built>
  <action>先运行 exact A2、B1 round 1 与 round 2 read-only verifiers，重验 exact cells/current build 与隐私约束并确认 B1-3 空；不能因前两轮 pass 预先判 B1 成功。然后硬暂停，要求用户从 paired protected UI 启动 B1-3 并第三次完整执行 normal close→OS actual exit→paired reopen→Moodle/EdStem/shared Profile/identity-scope/minimal visibility核对。published paired server 必须 mint 绑定 `B1_reopen_3`/current session/build/generation 的新 pending-action bundle，只在本轮真实 human action + actual observation 同事务 consume/append，拒绝前两轮重放。恢复仅 strict parse `02-18_B1_3_RESULT`；repo 侧不记录/写 L，只分别运行 round 1/2/3 read-only verifier做完整 conjunction。只有 3 rounds×2 sources 全部 current-build macOS/L pass、无 correction 使 pass 失效、所有 user feedback 存在时才允许 02-19；任一 non-pass 保持阻断。不得把 B1 完成写成共享 Profile 最终采用、Phase 2 complete 或 Windows/Phase 3 通过。</action>
  <how-to-verify>
    1. 确认UI当前唯一checkpoint为`B1-3 第三次关闭重开`，前两轮各自pass且第三轮not_run；build/identity/binding/scope未变。
    2. 第三次只正常关闭AutoED专属Profile并等待actual exit；任何所有权未知/仍运行/第二实例立即human_needed，不采取kill/delete-lock。
    3. 只在paired UI触发B1-3重开，等待Moodle→EdStem固定只读检查；不执行Worker/Codex/OS restart、24h、logout/reauth或额外浏览。
    4. 本地核对两来源、shared Profile observed、identity/binding/approved scope及同一课程最小visibility；核对B1-3脱敏receipt，敏感字段不得离开protected UI。
    5. 只返回B1-3固定脱敏块。若任一项非pass，停止；不要尝试第四轮覆盖失败，也不要要求执行B2。
  </how-to-verify>
  <verify>
    <automated>checkpoint 前运行 exact A2 与 B1 round 1/2 verifiers；用户反馈且 paired server 已形成本轮真实结果后，对 `--round 1`、`--round 2`、`--round 3` 分别运行同一 B1 read-only verifier，三次均 exit 0 才允许完成；repo-side ledger write 调用必须为零。</automated>
  </verify>
  <acceptance_criteria>
    1. 第三次生命周期事件独立且actual-exit先于reopen；旧owner/fence/guard/Worker无后续请求或提交，不触碰未知/日常浏览器。
    2. scenario=`B1_reopen_3` 的 Moodle/EdStem/shared Profile/identity/binding/scope/minimal visibility 全部 pass，无课程正文/写/download/敏感输出。
    3. ledger 最终恰有 `B1_reopen_1|B1_reopen_2|B1_reopen_3` × Moodle/EdStem 六个 required current-build macOS/L pass cells；三 round 事件/时间/authority 独立，无一个 receipt 覆盖多格。
    4. 删除/失败任一cell、替换为S/I/N/另一round/source/platform/旧build后B1 gate必须blocked；不能通过追加第四轮或latest-row聚合隐藏失败。
    5. 三轮反馈与SUMMARY仅含脱敏allowlist；Windows仍not_run/human_needed，B2/B3/C/D/reauth未运行，Phase3 blocked，共享Profile仍待后续验证。
    6. 只有完整六cell conjunction和第三轮真实user feedback允许02-19；任何non-pass停止且不创建成功SUMMARY/推进B2。
  </acceptance_criteria>
  <resume-signal>只回复完整 `02-18_B1_3_RESULT` 块，字段与前两轮模板完全相同且仅来自B1-3脱敏UI结果。不要发送私人字段、Profile/PID/Cookie、action ID、截图或日志；执行器必须等待真实回复，绝不能代填。</resume-signal>
  <done>三次真实 macOS close/reopen 均有双来源 `B1_reopen_1|B1_reopen_2|B1_reopen_3` 独立 L pass，且隐私/ownership/gate复验通过时，B1才完成并允许02-19 B2；否则保持阻断。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | 不可信输入/风险 | 强制控制 |
|---|---|---|
| 02-17 A2 state → B1 eligibility | A1、旧build、部分身份/课程结果或自由文本可能冒充A2 pass | exact A2 read-only gate；current build/config/binding/two-source/course conjunction；non-pass阻断 |
| paired user close → ProfileCoordinator/OS process | 点击关闭、lease expiry或PID缺失可能被误当actual exit，或误杀日常浏览器 | normal close；installation+nonce+PID+OS start+executable+control+fence证明；OS confirmed absent才release；unknown不动 |
| reopen → browser/Worker/source | 第二实例、旧generation/owner继续请求、任意导航或课程内容读取 | single-owner reserve/attach；BrowserRequestGuard+job request/commit fence；四个fixed actions+approved IDs；无任意URL/selector |
| source observation → protected identity/scope | 页面漂移、账户变化或一个来源失效被误写为共享恢复 | positive markers+official origin；双来源独立状态；binding/scope一致性；mismatch/interaction立即human_needed |
| paired checkpoint action → L ledger | 自动/S/I/N、旧round action或用户自报pass伪造live receipt | server-side一次性human authority绑定session/build/generation/round/source；append-only exact cells；用户反馈只是必要附加条件 |
| protected UI/receipt → chat/SUMMARY | 身份、课程、origin、Profile/PID/Cookie、action ID、截图或raw资料泄漏 | protected visible-only；RedactedEvidenceReceipt allowlist；固定feedback模板；敏感命中阻断/不复制 |
| three receipts → B2/phase gate | 一次pass、latest row、另一round/source/platform或较低证据填满三轮 | 3 rounds×2 sources exact macOS/L conjunction；missing/failed/human_needed不可被替代；Windows/Phase3固定blocked |

## STRIDE / ASVS L1 威胁登记

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1控制意图 | 具体缓解与硬阻断 |
|---|---|---:|---|---|---|---|
| T2-02 | Information Disclosure | Critical | Profile、protected UI、receipts/chat/log | mitigate | V8敏感数据保护、V13响应allowlist、V16安全日志 | 不读/导出Cookie/storage/raw page；identity/course仅protected；脱敏receipt/反馈；无截图。任一敏感值离开protected UI即Critical blocker。 |
| T2-04 | Spoofing / Tampering / Denial of Service / Elevation of Privilege | Critical | close/actual-exit/reopen/ownership | mitigate | V1架构、V2进程身份、V4访问控制、V14安全生命周期 | full owner proof、lease≠exit、actual absence才release、single instance、only-owned；unknown不kill/删锁。误回收或未退出重开即Critical blocker。 |
| T2-03 | Spoofing / Tampering | High | two-source identity/binding/scope | mitigate | V2身份、V4对象/范围授权、V5语义验证 | 每轮双来源稳定身份/组织/binding/approved scope/单课程marker复核；mismatch硬停。任一串号/范围漂移即High blocker。 |
| T2-05 | Tampering / Repudiation / Elevation of Privilege | High | old Worker/browser guard after reopen | mitigate | V4持续授权、V13竞态/事务、V16事件完整性 | 每request/commit generation+fence+owner校验；旧owner/Worker请求提交为0；迟到结果丢弃。任一stale访问/写回即High blocker。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | High | three L receipts/B2 gate | mitigate | V1证据等级、V4writer authority、V13状态机、V16provenance | per-round/source current-build macOS/L exact cells、one-time human authority、append-only、三轮conjunction；S/I/N/Windows/一轮pass不可替代。污染/自动批准即High blocker。 |

ASVS 5.0 L1仅表示适用控制意图，不声明完整合规。所有Critical/High项均为02-19前硬阻断；不得accept/transfer、降级warning、用第四轮覆盖失败、或因前一轮/自动测试通过继续。
</threat_model>

<verification>
1. 每个task在显示用户步骤前，重新运行02-17 exact gate与current-build/exact-cell检查；上一轮non-pass或本轮预填都立即停止。
2. 三个checkpoint必须依次分别暂停并等待真实用户反馈；不得一次展示三轮后自动继续、合成回复、复用旧消息或generic auto-approve。
3. 每轮normal close必须有OS actual-exit/ownership release；Profile running/unknown/mismatch时保持human_needed，不kill/删锁/启动第二实例。
4. 每轮重开仅四个sealed actions和一个approved course visibility marker；双来源、shared Profile、identity/binding/scope分别核对，不读课程正文或产生业务写/download。
5. EvidenceLedger按最终exact scenario枚举读取六个macOS/L cells；逐项证明S/I/N、另一round/source/platform/build与latest row不能替代，failed/human_needed不会消失。
6. feedback/RedactedEvidenceReceipt/SUMMARY敏感扫描无name/email/course/origin/path/Profile/PID/Cookie/action ID/DOM/network/screenshot；发现即停止并要求重新脱敏，不把敏感内容复制到文件。
7. B1 complete只在3 rounds×2 sources全pass且三次真实反馈存在时成立；否则02-19 blocked。Windows、B2/B3/C/D/reauth、Phase3保持原缺口。
</verification>

<success_criteria>
- AUTH-02/D-12：真实macOS同一专属Profile完成三次独立正常关闭/actual-exit/重开，Moodle/EdStem每轮都通过auth、shared-profile observation、identity/scope和单课程最小读取。
- AUTH-04/D-03：每轮只有single owner；lease不冒充exit，旧owner/Worker不继续请求/提交，不终止日常/未知浏览器；共享Profile仍待B2/B3/C/D/reauth完成后决定。
- UAT-01/D-14：B1-1/B1-2/B1-3各有独立脱敏live结果，source/round/platform/evidence/build不可串格；用户逐轮反馈真实pass/fail/human_needed与safe codes。
- 任一轮non-pass立即停止，不运行后续轮或02-19；一次pass、S/I/N/native、另一source/round/platform或第四轮不能代填/覆盖。
- 仅核验一个approved course visible marker，不读取正文/帖子/文件/成绩，不请求截图或输出身份/course/Profile/network资料。
- 三轮pass不等于Phase2完成或共享Profile最终采用；Windows not_run/human_needed、Phase3 blocked及后续macOS gates保持不变。
</success_criteria>

<output>
只有Task 1、2、3各自收到真实strict feedback，最终六个B1 macOS/L exact cells均pass且完整gate复验通过后，才创建`.planning/phases/02-poc-live/02-18-SUMMARY.md`。SUMMARY只记录公开version/build、platform=macos、B1-1/B1-2/B1-3、每来源result/safe code、binding consistency/shared-profile/gap、三轮observed time、B2 eligibility与Windows/Phase3缺口；不记录姓名/邮箱/course/origin/Profile/PID/Cookie/action ID/raw page/network/screenshot。任一轮failed/human_needed时保持当前task未完成并阻断02-19，不创建成功SUMMARY、不追加补偿pass、不执行第四轮覆盖。
</output>
