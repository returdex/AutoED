---
phase: 02-poc-live
plan: 32
type: execute
wave: 28
depends_on: [02-31]
files_modified: []
autonomous: false
requirements: [AUTH-02, AUTH-03, UAT-01]
must_haves:
  truths:
    - "Windows D 的anchor只来自current immutable Windows build上Moodle/EdStem的A1认证、A2 binding与A2 course visibility六个current non-corrected Windows/L pass cells第一次共同可信时的server recordedAt最大值。"
    - "paired protected UI以anchor+精确86,400,000ms显示带时区earliest；真实时钟未到期时始终human_needed，禁止skip、force、time override、自动probe、pending action、L append或成功SUMMARY。"
    - "production gate只使用native Windows backend不可注入的真实wall clock并审计rollback/非单调/未来anchor；agent不得sleep/poll到期、改系统时钟、使用fixture Clock、macOS时间或B/C时间代替。"
    - "从anchor到D必须无login/logout/reauth/MFA、切号、binding/config/scope、Cookie/Profile干预；Windows B/C规定生命周期不重置anchor，审计unknown或检测到干预都不可通过。"
    - "到期只把唯一paired D action变成eligible，不自动运行。用户点击后产品分别执行Moodle→EdStem fixed read-only auth/minimal-status check，不打开登录、不读课程正文且不在D中reauth。"
    - "Moodle/EdStem分别append exact `windows/D_24h_recheck/L` immutable event；只有paired server one-time human correlation+actual source result能写L，macOS/S/I/N/C/reauth不能代填。"
    - "D不创建planning receipt；只有strict用户反馈、两个exact cells和canonical verifier的`WINDOWS_D_24H_RECHECK_COMPLETE`共同通过才创建SUMMARY并释放独立reauth gate。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-31-SUMMARY.md"
      provides: "Windows C真实用户反馈与D eligibility；不替代C verifier、A anchor或D runtime cells"
      contains: "D"
    - path: "packages/persistence/src/auth.ts"
      provides: "Windows A/D exact EvidenceLedger cells、server recordedAt与append-only human provenance"
      contains: "EvidenceLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows D anchor/真实clock三态与two-source exact-cell只读verification；无writer或planning receipt"
      contains: "verifyPhase2LiveCheckpoint"
    - path: "apps/status/src/main.ts"
      provides: "paired protected UI的anchor/earliest/remaining、无绕过等待态和到期后唯一D action"
      contains: "D_24h_recheck"
    - path: ".planning/phases/02-poc-live/02-32-SUMMARY.md"
      provides: "仅strict D pass后的reauth人工前置；runtime ledger/verifier仍是machine authority"
  key_links:
    - from: "Windows A1/A2 six exact L cells"
      to: "D earliest recheck"
      via: "current/non-corrected conjunction的server recordedAt max + 86,400,000ms"
      pattern: "A1_login|A2_binding|A2_course_visibility|earliest"
    - from: ".planning/phases/02-poc-live/02-31-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "先fresh复验Windows C machine gate；SUMMARY只证明真实feedback"
      pattern: "--checkpoint C|--platform windows"
    - from: "backend actual wall clock"
      to: "apps/status/src/main.ts"
      via: "not due只显示human_needed/remaining；ready仍需用户点击，不接受时间输入"
      pattern: "WINDOWS_D_NOT_DUE|WINDOWS_D_READY_NOT_RUN"
    - from: "paired Windows D action"
      to: "packages/persistence/src/auth.ts"
      via: "server-only source children绑定anchor/build/config/Profile/session与actual read-only result，分别appendWindows/L"
      pattern: "human_action|windows|D_24h_recheck|L"
    - from: "Windows D exact cells"
      to: "02-33 reauth eligibility"
      via: "canonical read-only D verifier + completed 02-32 SUMMARY；D与reauth各自新human gate"
      pattern: "WINDOWS_D_24H_RECHECK_COMPLETE|reauth"
---

<objective>
为未来原生Windows 11执行不可加速的D跨日硬人工门：从current Windows build首次可信A1+A2六格联合成功的server时间计算真实earliest，至少经过24小时且期间无认证干预后，才允许用户本人发起Moodle/EdStem逐来源只读复查，并形成独立Windows/L D证据。

Purpose: 用真实elapsed time、无干预审计与独立source observations验证AUTH-02/AUTH-03/UAT-01；防止macOS间隔、B/C重启、fixture/test Clock、用户自报时间或reauth结果冒充Windows D。

Output: paired UI真实等待/ready门、Moodle/EdStem两个`windows/D_24h_recheck/L` runtime events、canonical read-only verifier与仅strict pass后的02-32 SUMMARY。无planning receipt，不混入reauth，macOS evidence unchanged，Phase3 blocked。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-22-PLAN.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@.planning/phases/02-poc-live/02-27-SUMMARY.md
@.planning/phases/02-poc-live/02-28-SUMMARY.md
@.planning/phases/02-poc-live/02-29-SUMMARY.md
@.planning/phases/02-poc-live/02-30-SUMMARY.md
@.planning/phases/02-poc-live/02-31-PLAN.md
@.planning/phases/02-poc-live/02-31-SUMMARY.md

<interfaces>
<!-- 执行时只能机械适配已发布export名；不得改变anchor/clock/scenario/writer语义或现场patch current beta。 -->

Windows C prerequisite machine gate：
```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint C --runtime current --platform windows --read-only
```
它必须fresh exit0并返回`WINDOWS_C_OS_RESTART_COMPLETE`，证明actual native Windows11/non-WSL full OS restart、new boot identity、same installation/current build/generation/managed entrypoints、old generation fenced且request/commit=0、API/Worker recovered、Profile ownership proven+same_dedicated、Moodle/EdStem authenticated、identityBinding consistent、scope matched、minimalStatus pass、content preserved、sensitiveOutput none、macOS unchanged、D pending、Phase3 blocked。还必须有completed `.planning/phases/02-poc-live/02-31-SUMMARY.md`证明本轮真实用户feedback；SUMMARY不是clock/ledger authority。

Windows D anchor固定来自以下六个runtime EvidenceLedger exact cells：
```typescript
type WindowsDAnchorCell = {
  platform:'windows'; evidence:'L'; source:'moodle'|'edstem';
  scenario:'A1_login'|'A2_binding'|'A2_course_visibility';
  version:string; buildId:string; result:'pass'; corrected:false; recordedAt:string;
};
```
`dAnchorAt/firstTrustworthySuccessAt = max(recordedAt)`，只在同current immutable build第一次同时拥有Moodle+EdStem `A1_login`、Moodle+EdStem `A2_binding`、Moodle+EdStem `A2_course_visibility`六个current/non-corrected pass时成立；`dEarliestAt = dAnchorAt + 86_400_000ms`。下文safe projection中的`anchorAt/earliestAt`分别是这两个canonical字段的presenter名。单来源、SUMMARY/chat/file mtime、macOS A时间、B/C observedAt、当前打开计划的时间都无效且不能重置/提前anchor。build/config/scope/binding drift、任一有效correction、future/non-ISO/non-monotonic timestamp使anchor invalid，必须重做current build的A链，不能偷偷从新时间继续。C gate/SUMMARY可安全交付这两个audit timestamps，但绝不使D action/probe/append/pass发生。

Production clock必须是native Windows backend不可注入的actual system wall clock。production D API/CLI/UI不得接受`now|elapsed|clock|advance|due|timezone`参数、环境变量或query/payload override。每次`checkedAt`不早于latest trusted server event与前一次持久time-check，并与runtime monotonic observation相容；rollback/forward anomaly/future anchor/clock source unknown返回`WINDOWS_D_CLOCK_UNTRUSTED`。agent不得sleep/poll/automation等待、改系统时间或用fixture/test Clock、远程Date header、macOS时间解除门。

Canonical Windows D verifier：
```bash
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs \
  --verify --checkpoint D --runtime current --platform windows --read-only
```
strict三态：
```typescript
type WindowsDGateProjection =
  | {schema:1;status:'human_needed';checkpoint:'D';platform:'windows';resultCode:'WINDOWS_D_NOT_DUE';anchorAt:string;earliestAt:string;checkedAt:string;remainingSeconds:number;actionEligible:false;authIntervention:'none';reauth:'not_run';macosEvidence:'unchanged';phase3:'blocked'}
  | {schema:1;status:'human_needed';checkpoint:'D';platform:'windows';resultCode:'WINDOWS_D_READY_NOT_RUN';anchorAt:string;earliestAt:string;checkedAt:string;remainingSeconds:0;actionEligible:true;authIntervention:'none';reauth:'not_run';macosEvidence:'unchanged';phase3:'blocked'}
  | {schema:1;status:'pass';checkpoint:'D';platform:'windows';resultCode:'WINDOWS_D_24H_RECHECK_COMPLETE';anchorAt:string;earliestAt:string;checkedAt:string;elapsedSeconds:number;actionEligible:false;sources:['moodle','edstem'];binding:'consistent';sharedProfile:'observed';minimalStatus:'pass';authIntervention:'none';contentBoundary:'preserved';reauth:'not_run';macosEvidence:'unchanged';phase3:'blocked'};
```
前两态均固定nonzero human-needed exit；到期不自动pass。verifier只经signed local-client discovery读取redacted clock/status/exact cells，不直接读DB/WAL、不append/correct ledger、不启动probe/browser/process、不创建planning receipt。anchor/earliest/checkedAt是允许输出的非私人审计时间；不得输出clock internals、boot identity、PID、Profile path或source identity。

等待期`authIntervention='none'`只允许已经完成的B1/B2/B3/C规定process/Profile lifecycle与无凭据health observation；不允许explicit logout、login/reauth/MFA、切号、binding/config/scope改变、Cookie/storage/Profile复制/拆分/清理、人工打开source页面或额外course/content访问。B/C不重置anchor。任何介入或审计unknown返回`WINDOWS_D_AUTH_INTERVENTION_DETECTED|RESULT_NOT_OBSERVED`并永久使本次D non-pass，不能从干预后新计24小时覆盖要求。natural expiry不是reauth：D若观察`AUTH_REQUIRED|REAUTH_REQUIRED`只写D failed/human_needed，02-33仍是独立gate。

每source Windows D event strict包含`platform=windows,evidence=L,scenario=D_24h_recheck,source,current version/build,anchorAt,earliestAt,checkedAt,elapsedSeconds,authIntervention,source result/safe code,binding/Profile/minimalStatus/contentBoundary,observedAt`，不含full identity/course/origin/Profile/PID/Cookie/actionID/raw page/network。到期后用户点击唯一D action时，paired server才mint source-specific one-time children，绑定anchor/earliest/checkedAt/current build/config/scope/binding/Profile owner/paired session；actual probe+human action correlation共同生成payload外Windows/L authority。repo gate/feedback/CLI无writer权，failed/human_needed也immutable。

02-33 reauth handoff：machine prerequisite是上述D verifier fresh exit0=`WINDOWS_D_24H_RECHECK_COMPLETE`，另需completed `02-32-SUMMARY.md`证明真实feedback；无D planning receipt。D pass只把reauth标记`pending_human_decision`，不得mint reauth correlation、logout或login。自然过期优先；若D时未过期，当前批准资料没有“合理观察期”的exact时长/起点，02-32不得用24小时D interval充当它。02-33必须先获得新的明确用户决定：要么提供exact `wait_until`继续等自然过期，要么明确同意单一来源（固定Moodle）logout fallback；没有选择则human_needed。reauth使用新`scenario='reauth'`、新paired-server Windows/L correlations，绝不复用D action/cells。
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 锁定 Windows A联合anchor并等待真实earliest时刻</name>
  <read_first>AGENTS.md的hard live gates/native Windows/证据隔离；02-CONTEXT D-03、D-12、D-14、D-16–D-18；02-RESEARCH/VALIDATION/PATTERNS的24h vs reauth与clock gate；02-31 PLAN/SUMMARY和canonical C contract；02-26/02-27 Windows A exact cells；current production clock/EvidenceLedger/live-gate exports。不得读raw DB/Profile/full identity/course或使用test Clock。</read_first>
  <files>none（waiting/eligibility只读；不得修改runtime/ledger/config/Profile/system clock，创建receipt/SUMMARY，访问学校或调度automation）</files>
  <what-built>current native Windows beta已经完成A/B/C；D与reauth仍独立not_run。产品从Windows六个A exact cells计算并显示earliest，等待本身不是pass。</what-built>
  <action>先strict要求completed02-31 SUMMARY并运行canonical Windows C verifier，得到`WINDOWS_C_OS_RESTART_COMPLETE`；再由current backend/EvidenceLedger按`<interfaces>`读取六个Windows A exact cells，计算`anchorAt=max(server recordedAt)`和`earliestAt=anchor+86,400,000ms`。执行器不得直读DB后手算覆盖产品值。任一cell缺失/corrected、build/config/scope/binding drift、时间future/non-monotonic、macOS代入或clock unknown，显示allowlisted blocker并停止。

运行canonical Windows D verifier。若`WINDOWS_D_NOT_DUE`，确认nonzero human_needed、remaining>0、actionEligible=false、D exact cells/pending correlation/source request/L append/SUMMARY增量为0；paired UI显示带timezone的anchor/earliest/checkedAt/remaining与“等待跨日复查”，没有skip/continue/force/reauth/logout/login或D action。随后硬暂停；agent不得sleep/poll到期、创建automation/cron、改clock、让用户加速或把对话经过时间当证据。用户只能等待真实时间并保持无auth/session/Profile干预。

若首次已为`WINDOWS_D_READY_NOT_RUN`也必须暂停，不能自动进入Task2。用户未来返回后fresh重跑C与D verifier；只有checkedAt>=earliest、remaining=0、actionEligible=true、clock trusted、authIntervention none、D cells/correlation仍空且真实用户确认UI ready，Task1才完成。用户自报时间只证明看见gate，不是due authority。NOT_DUE继续同checkpoint等待；clock/anchor/intervention/unknown立即failed/human_needed且不重置anchor。</action>
  <how-to-verify>
    1. 在原生Windows 11 current beta paired UI确认C pass，D行显示anchor、earliest、current checked time与timezone；不要发送identity/course/origin/Profile等内容。
    2. 若显示`WINDOWS_D_NOT_DUE`，确认无skip/force/probe/login/logout/reauth按钮。记住UI显示的earliest即可离开；不要改系统时钟、运行脚本加速、清Cookie/Profile、切号或重新登录。
    3. 等到真实earliest之后再回到同一native Windows current beta。正常锁屏、退出Codex不算auth intervention；不要额外打开学校页面或修改config/binding/session。
    4. 若期间自然expiry、意外login prompt、账号变化、Profile/config/binding操作或clock warning，不要修复/reauth；返回对应safe blocker，D与reauth保持分离。
    5. 到期后确认UI显示`WINDOWS_D_READY_NOT_RUN`、remaining=0、action eligible但尚未probe/pass；只返回下列waiting signal，Task2复验前不要点击D action。
  </how-to-verify>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint C --runtime current --platform windows --read-only &amp;&amp; sh -c 'd_status=0; d_output=$(node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint D --runtime current --platform windows --read-only) || d_status=$?; test "$d_status" -eq 2 &amp;&amp; printf "%s\n" "$d_output" | rg -q WINDOWS_D_READY_NOT_RUN'</automated>
  </verify>
  <acceptance_criteria>
    1. completed02-31 SUMMARY+C verifier=`WINDOWS_C_OS_RESTART_COMPLETE`均current pass；D anchor仅为Windows A1/A2六cells current/non-corrected conjunction的server recordedAt max。
    2. earliest精确为anchor+86,400,000ms；macOS interval、B/C time、chat/user timestamp/file mtime/单来源/旧build均未代入或重置。
    3. NOT_DUE时verifier nonzero human_needed、remaining>0/actionEligible=false；UI无bypass/action，source request/pending/D L/SUMMARY增量=0。
    4. production gate拒绝now/elapsed/clock/TZ/env/query/payload override和test Clock；rollback/future/non-monotonic为safe blocker且不写pass。
    5. agent未sleep/poll/automation/改clock/伪造反馈；从anchor以来logout/login/reauth/MFA/account/config/binding/Profile/Cookie intervention为0且audit可信。
    6. Task1只在READY_NOT_RUN、checkedAt>=earliest、remaining=0/actionEligible=true、D cells/correlation空与真实用户ready反馈共同成立时完成；到期仍未probe/append/pass。
  </acceptance_criteria>
  <resume-signal>到期前只回复`02-32 WINDOWS_D_WAITING WINDOWS_D_NOT_DUE &lt;UI earliest_at&gt;`；到期后只回复`02-32 WINDOWS_D_READY WINDOWS_D_READY_NOT_RUN &lt;UI earliest_at&gt; &lt;checked_at&gt; auth_intervention=none`。若clock/anchor/intervention不可信，只回复`02-32 WINDOWS_D_WAIT_FAILED &lt;allowlisted safe code&gt;`。不要发送身份、课程、origin、Profile、Cookie、设备/PID、截图或日志；任何回复都不能替代server clock gate。</resume-signal>
  <done>只有真实Windows clock达到server计算的earliest、无bypass/认证干预、D仍未运行且用户本轮确认ready时才进入D probe；否则保持waiting/human_needed。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 用户发起 Windows D逐来源无干预复查并形成独立证据</name>
  <read_first>Task1的WINDOWS_D_READY_NOT_RUN与current anchor/earliest/checkedAt；02-31 C pass；02-UI-SPEC D row/Privacy/Failure；source isolation/expiry、paired action/protected identity、EvidenceLedger/phase2-live-gate D exact contracts。不得读取full identity/config/DB/Profile、主动登录/reauth或访问课程内容。</read_first>
  <files>none（用户动作只使paired server向仓库外runtime append-only ledger写两个D events；pass后execute-plan创建02-32-SUMMARY.md；不创建planning receipt、不修改源码/Profile/学校资料）</files>
  <what-built>真实24小时clock gate已到期且no-intervention audit可信；Windows D仍not_run，用户必须亲自发起，reauth仍blocked且macOS不参与。</what-built>
  <action>fresh运行canonical C+D verifiers并要求D仍`WINDOWS_D_READY_NOT_RUN`；任何时间/anchor/audit/build变化回Task1或阻断。然后硬暂停；只有用户在current paired protected UI点击一次`开始Windows D：无干预复查`，paired server才为Moodle/EdStem mint source-specific one-time children，绑定anchor/earliest/current checkedAt、checkpoint D/scenario D_24h_recheck、Windows/L、current build/config/scope/binding/Profile owner/paired session。actionID不出server，repo gate/反馈不能append。

产品只用approved IDs按Moodle→EdStem运行fixed background auth/minimal-status probes，分别验证positive auth、confirmed binding/scope与same dedicated Profile；每步检查ownership/generation/cancellation/origin。不打开headed login、不自动输入/MFA、不logout、不恢复/重试interaction、不读course正文/帖子/文件/成绩、不write/download。若任一source自然过期或需interaction，D结果为failed/human_needed（如`WINDOWS_D_SOURCE_SESSION_EXPIRED`），保留另一source current state/last-success并停止；不得当场进入reauth或把后续reauth写回D。

actual no-intervention observation+current user action correlation共同mint payload外human_action/Windows/L authority，分别appendMoodle/EdStem exact D events；failed/human_needed也immutable。用户只返回paired UI生成的strict block：
```text
02-32_WINDOWS_D_RESULT
checkpoint=D
platform=windows
version=<public version>
build_id=<public build id>
result=pass|failed|human_needed
result_code=WINDOWS_D_24H_RECHECK_COMPLETE|WINDOWS_D_ANCHOR_UNTRUSTED|WINDOWS_D_CLOCK_UNTRUSTED|WINDOWS_D_AUTH_INTERVENTION_DETECTED|WINDOWS_D_SOURCE_SESSION_EXPIRED|WINDOWS_D_SOURCE_RECHECK_FAILED|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|CONTENT_BOUNDARY_VIOLATION|RESULT_NOT_OBSERVED
anchor_at=<server ISO-8601 timezone>
earliest_at=<server ISO-8601 timezone>
checked_at=<server ISO-8601 timezone>
elapsed=at_least_24h|under_24h|not_observed
auth_intervention=none|detected|not_observed
moodle=pass|failed|human_needed
moodle_code=AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR
edstem=pass|failed|human_needed|not_run
edstem_code=AUTHENTICATED|AUTH_REQUIRED|REAUTH_REQUIRED|NETWORK_UNAVAILABLE|PARSER_CHANGED|CAPABILITY_DENIED|IDENTITY_MISMATCH|PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|BROWSER_ABORTED|BROWSER_FENCED|UNKNOWN_SOURCE_ERROR|INTERNAL_ERROR|NOT_RUN
binding=consistent|mismatch|not_observed
shared_profile=observed|mismatch|not_observed
minimal_status=pass|failed|not_observed
content_boundary=preserved|violated|not_observed
reauth=not_run
screenshot=not_used
macos_evidence=unchanged
phase3=blocked
observed_at=<ISO-8601 with timezone>
```
executor strict拒绝extra/free text/sensitive值并运行canonical D verifier。唯一pass conjunction是verifier exit0/`WINDOWS_D_24H_RECHECK_COMPLETE`、两exact cells current/non-corrected、elapsed>=86400、authIntervention none、两source/binding/Profile/minimal/content pass、reauth not_run、macOS unchanged与真实feedback全部成立。任何non-pass立即阻断02-33，不重复D覆盖、不创建planning receipt。</action>
  <how-to-verify>
    1. 确认UI仍显示WINDOWS_D_READY_NOT_RUN、checkedAt>=earliest、auth intervention none、C pass；D与reauth为独立rows，macOS evidence unchanged。
    2. 只点击一次`开始Windows D：无干预复查`。不要点击login/logout/reauth、打开source窗口、修改Profile/Cookie/config或重复触发。
    3. 观察Moodle后EdStem分别完成fixed auth/minimal-status check；核对binding/scope与same dedicated Profile状态。完整身份只在protected UI，不复制。
    4. 确认未读取body/posts/files/grades、未write/download、未截图或输出raw页面/网络；没有headed window/MFA。
    5. 任一source expired/interaction/mismatch/network/parser/ownership不明时保留真实failed/human_needed并停止；不要登录、logout或开始reauth，另一source不能代填。
    6. 检查D ledger显示Moodle/EdStem各一个Windows/L/D_24h_recheck结果及anchor/elapsed/no-intervention safe状态；S/I/N/C/reauth/macOS未代填。
    7. 只复制完整`02-32_WINDOWS_D_RESULT`脱敏块；不得附身份/course/origin/Profile/PID/Cookie/actionID、截图、页面、网络或日志。
  </how-to-verify>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint D --runtime current --platform windows --read-only</automated>
  </verify>
  <acceptance_criteria>
    1. action前D gate仍READY_NOT_RUN、clock/anchor/no-intervention可信且D cells空；用户本人只点击paired fixed D action一次。
    2. server one-time children绑定Windows/D_24h_recheck/current build/anchor/config/scope/binding/Profile/session且actionID不外泄；repo/chat/fixture无L writer权。
    3. 两source仅运行fixed background auth/minimal-status probes；无headed login/MFA/logout/reauth、arbitrary URL/selector/JS/write/download/course content。
    4. runtime ledger有两个current/non-corrected`windows/D_24h_recheck/L`events，elapsed>=86400、authIntervention none；S/I/N/C/reauth/macOS/oldbuild不能替代。
    5. strict feedback与canonical verifier共同得到`WINDOWS_D_24H_RECHECK_COMPLETE`；source/binding/Profile/minimal/content全部pass，否则02-33 blocked且不重复覆盖失败。
    6. feedback/gate/SUMMARY候选无full identity/course/origin/Profile/PID/Cookie/actionID/raw page/network/screenshot/log；敏感命中即block。
    7. pass只完成Windows D；reauth仍`pending_human_decision`且无correlation，macOS unchanged、Phase3 blocked。
  </acceptance_criteria>
  <resume-signal>只回复paired UI生成的完整`02-32_WINDOWS_D_RESULT`脱敏块。不要发送身份、course、origin、Profile/PID/Cookie/actionID、截图或日志；`approved`/自由文本不解除门禁。若任一source expiry或interaction，原样返回failed/human_needed并停止，绝不在本checkpoint登录或reauth。</resume-signal>
  <done>只有真实Windows clock已满24小时、无认证干预、用户本人发起、两个source-specific Windows/L D events与strict feedback/verifier全部pass时D才完成；其他结果保持blocked且reauth未执行。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 与阻断政策

采用OWASP ASVS L1的V1架构边界、V2会话身份、V4持续授权、V5输入验证、V8数据保护、V13业务状态与V16审计意图，不声称完整合规。所有High/Critical项必须`mitigate`；任何时间不可证、绕过、writer provenance缺失、source non-pass、敏感泄漏或真实feedback缺失都是02-32/02-33 blocker，不得accept/transfer/自动批准。

## Trust Boundaries

| Boundary | 风险 |
|---|---|
| Windows A ledger → D anchor | 单source、macOS、SUMMARY/user time、old/corrected event可能伪造首次联合成功 |
| backend clock → D eligibility | test Clock、override、系统回拨/快进、agent等待脚本可能伪造24小时 |
| waiting UI → D action/source | 未到期skip/force/自动probe或到期自动pass可能越过human gate |
| waiting interval → auth state | login/logout/reauth/MFA/config/Profile干预后仍声称no-intervention |
| paired D action → EvidenceLedger L | replay/background/repo/feedback可能伪造human authority或跨平台/scenario写入 |
| school source → D result | 200/stale last-success/单来源或expiry可能被误称current cross-day pass |
| protected state → feedback/SUMMARY | identity/course/Profile/clock internals/raw页面网络可能泄漏 |
| D completion → reauth | D interval或D action可能被复用为reauth期限/authority并自动logout/login |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | ASVS | Mitigation Plan |
|---|---|---:|---|---|---|---|
| T2-32-ANCHOR | Spoofing/Tampering | Critical | A cells→anchor | mitigate | V1,V4,V13,V16 | six exact current/non-corrected Windows/L cells；server recordedAt max；build/config/binding binding；macOS/B/C/chat/time拒绝，correction使anchor invalid。 |
| T2-32-CLOCK | Tampering/Repudiation/Elevation | Critical | production clock/eligibility | mitigate | V4,V5,V13,V16 | no-input actual wall clock、persistent monotonic checks、future/rollback detection、exact86400000ms；禁止override/testClock/sleep/automation。 |
| T2-32-BYPASS | Elevation/Repudiation | Critical | waiting UI/checkpoint | mitigate | V1,V4,V13 | NOT_DUE nonzero human_needed且零action/request/L；READY仍需fresh verifier+user click；无skip/default/timeout/auto approval。 |
| T2-32-INTERVENE | Tampering | High | 24h auth state | mitigate | V4,V13,V16 | append-only no-intervention audit；logout/login/reauth/MFA/account/config/binding/Profile/Cookie change或unknown阻断，不重置anchor。 |
| T2-32-L | Spoofing/Repudiation/Elevation | Critical | paired action→EvidenceLedger | mitigate | V4,V5,V13,V16 | paired same-origin/CSRF one-time source children绑定anchor/build/session；actual result+human action；exact Windows/D/L cells；failed immutable。 |
| T2-32-SOURCE | Spoofing/Tampering | High | fixed probes | mitigate | V4,V13 | approved IDs/origin/positive marker/current result；Moodle→EdStem；expiry/interaction non-pass；无login/reauth/content/write/download。 |
| T2-32-LEAK | Information Disclosure | Critical | UI/gate/feedback/SUMMARY | mitigate | V8,V16 | only audit timestamps/safe enums；禁止identity/course/origin/Profile/PID/Cookie/actionID/raw page/network/screenshot/log；401/403清除。 |
| T2-32-MERGE | Tampering/Elevation | Critical | D→reauth/macOS/Phase3 | mitigate | V1,V4,V13 | exact scenario/platform/evidence cells；macOS unchanged；D不定义reauth observation period、不mint reauth action；无planning receipt，Phase3 blocked。 |
</threat_model>

<verification>
1. fresh运行Windows C verifier并strict读取completed02-31 SUMMARY；任一non-pass/build drift阻断D。
2. 只读验证Windows A1/A2六exact cells、server recordedAt max anchor与earliest+86400000；macOS/B/C/summary/user time/correction不得代入。
3. 未到期canonical D gate只返回WINDOWS_D_NOT_DUE/human_needed，UI无action/bypass且pending/source/L/SUMMARY增量0；agent不sleep/automation/改clock。
4. 到期仍先取得真实用户WINDOWS_D_READY_NOT_RUN反馈并fresh复验；到期本身不probe/append/pass。
5. 审计anchor以来authIntervention none；logout/login/reauth/MFA/config/binding/Profile/Cookie操作或unknown阻断，B/C lifecycle不重置anchor。
6. 用户点击后两source分别运行fixed D checks并写exactWindows/L cells；expiry/interaction/mismatch/单sourcepass不能触发或代替reauth。
7. strict final block+canonical D verifier同时pass才创建SUMMARY；敏感扫描零命中，reauth pending decision、macOS unchanged、Phase3 blocked。
</verification>

<success_criteria>
- AUTH-02：Windows D有Moodle/EdStem两个source-specific `D_24h_recheck/L` immutable events与真实user provenance；S/I/N/C/reauth/macOS不能代填。
- AUTH-03：两来源current auth、confirmed binding/approved scope、same Profile与minimal status独立复核；expiry/mismatch/unknown为visible non-pass且不自动切号/覆盖/reauth。
- UAT-01：real-clock gate无skip/time override；到期前human_needed/零副作用，到期后仍需paired user action、strict feedback与read-only verifier。
- 无planning receipt/parallel ledger/public writer；runtime EvidenceLedger为machine source，SUMMARY仅证明真实反馈。
- D与reauth完全分离；D不login/MFA/logout，不用24小时作为未批准的reauth观察期，02-33必须取得独立用户decision/action。
- macOS evidence unchanged，Windows reauth仍pending，Phase3 blocked。
</success_criteria>

<output>
仅在真实用户final strict feedback、Moodle/EdStem两个current/non-corrected`windows/D_24h_recheck/L`cells与canonical verifier的`WINDOWS_D_24H_RECHECK_COMPLETE`全部pass后创建`.planning/phases/02-poc-live/02-32-SUMMARY.md`。SUMMARY只记录checkpoint=D、public version/build、platform=windows、anchorAt/earliestAt/checkedAt/elapsedSeconds、authIntervention=none、两source safe result、binding/sharedProfile/minimalStatus/contentBoundary、reauth=pending_human_decision、observedAt、macOS unchanged、02-33 eligibility与Phase3 blocked；不得记录full identity/course/origin/Profile/PID/Cookie/actionID/raw page/network/screenshot/log。NOT_DUE/READY不创建success SUMMARY；failed/human_needed保留runtime immutable result、阻断02-33且不重复D覆盖。不创建planning receipt。
</output>
