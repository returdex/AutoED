---
phase: 02-poc-live
plan: "10"
type: execute
wave: 7
depends_on:
  - "02-02"
  - "02-04"
  - "02-06"
  - "02-07"
  - "02-08"
files_modified:
  - packages/test-support/src/security-matrix.ts
  - tests/unit/auth-security-matrix.test.ts
  - tests/integration/auth-security-matrix.test.ts
  - tests/integration/evidence-isolation-matrix.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SEC-02
  - UAT-01

must_haves:
  truths:
    - "未登录、自然过期、权限拒绝、网络超时和 parser drift 保持不同的来源状态、结果码、重试决定与下一步；任何失败均不清除最后一次成功，也不污染另一个来源。"
    - "相同显示名但 stable subject 不同不能自动绑定；identity_mismatch 必须硬停所有课程可见性探测，且不能自动切号、登出、覆盖既有绑定或继续来源访问。"
    - "越界 origin、任意 URL/JS/selector/browser handle/operation 参数、业务写与下载在 application/adapter/driver 前 fail closed，测试可证明没有请求、文件或来源写副作用。"
    - "Profile 仍被持有、PID reuse 或所有权证明不完整时只能 profile_in_use/ownership_unconfirmed + human_needed；lease 过期不授权 kill、删锁、release 或接管。"
    - "旧 Worker 在请求前或提交前失去 cancel/lease/generation/fence 权威后，来源调用与 observation/receipt/follow-up 写入分别保持为零。"
    - "结果单按 platform×source×scenario×evidence class 精确隔离；fixture 与自动 producer 只能形成 S/I，不能写 L、不能写另一平台、不能以较低证据或另一场景满足 live/Windows cell。"
    - "T2-01–T2-08 每一项至少有一个命名 negative case，且任何 High/Critical case 失败都会使矩阵测试非零退出并继续阻断 beta/live 与 Phase 3。"
  artifacts:
    - path: "packages/test-support/src/security-matrix.ts"
      provides: "不含可执行闭包或敏感资料的 T2-01–T2-08 canonical case manifest、固定 sentinels 与 S/I-only fixture helpers"
      exports:
        - "SECURITY_MATRIX_CASES"
        - "REQUIRED_SECURITY_CASE_IDS"
        - "assertSecurityMatrixCoverage"
    - path: "tests/unit/auth-security-matrix.test.ts"
      provides: "contracts、pure auth/binding reducer、policy/presenter 的表驱动 fail-closed 单元矩阵"
      contains: "describe('Phase 2 auth security matrix: unit'"
    - path: "tests/integration/auth-security-matrix.test.ts"
      provides: "sealed adapter、Profile ownership、durable Worker、SQLite 与 paired API 的跨层故障注入矩阵"
      contains: "describe('Phase 2 auth security matrix: integration'"
    - path: "tests/integration/evidence-isolation-matrix.test.ts"
      provides: "S/I/L、macOS/Windows、source/scenario/cell 污染与 Phase 3 gate 的真实 SQLite negative matrix"
      contains: "describe('Phase 2 evidence isolation matrix'"
  key_links:
    - from: "packages/test-support/src/security-matrix.ts"
      to: "tests/unit/auth-security-matrix.test.ts"
      via: "unit suite 必须消费 canonical case IDs，并由 coverage assertion 拒绝漏掉任一 T2 或必测情形"
      pattern: "SECURITY_MATRIX_CASES|assertSecurityMatrixCoverage"
    - from: "tests/integration/auth-security-matrix.test.ts"
      to: "packages/platform/src/source-adapters.ts"
      via: "本地恶意页面 fixture 只经 SealedSourceAdapters/SourceProbePort，记录固定请求和禁止副作用计数"
      pattern: "SealedSourceAdapters|SourceProbePort"
    - from: "tests/integration/auth-security-matrix.test.ts"
      to: "packages/application/src/auth-jobs.ts"
      via: "AuthJobRunner 的 before-request/before-commit fault hooks 验证失权后零访问与零提交"
      pattern: "AuthJobRunner|assertCurrent"
    - from: "tests/integration/auth-security-matrix.test.ts"
      to: "apps/api/src/auth.ts"
      via: "actual loopback HTTP 证明未配对/过期/权限不足与 arbitrary operation 在 application/driver 前被拒绝"
      pattern: "registerAuthRoutes|/api/auth/"
    - from: "tests/integration/evidence-isolation-matrix.test.ts"
      to: "packages/persistence/src/auth.ts"
      via: "EvidenceLedger 使用 payload 外 authority 做真实 SQLite append/list，并验证受拒写入对全部 cell 零影响"
      pattern: "EvidenceLedger|append|list"
---

<objective>
建立 Phase 2 跨层 S/I 安全与证据隔离矩阵，以同一组 canonical case IDs 贯穿 strict contracts、认证/绑定状态机、sealed adapters、Profile ownership、durable Worker、paired API、SQLite observation/ledger 和 Phase 3 gate，证明 T2-01–T2-08 对指定负例全部 fail closed。

Purpose: 落实 D-03、D-05–D-07、D-09–D-11、D-14、D-17，并在 beta 组装或任何真实学校访问前，为 AUTH-01–AUTH-04、SEC-02、UAT-01 建立不可被 fixture、跨平台 receipt 或状态折叠伪造的自动安全证据。
Output: canonical security matrix test-support、unit 矩阵、跨层 integration 矩阵、evidence pollution 矩阵；只产生 S/I 测试结果，不产生 N/L 或 live 结论。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-06-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@packages/test-support/src/auth-fixture.ts
@packages/contracts/src/index.ts
@packages/application/src/auth.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/policy.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/browser-provider.ts
@packages/platform/src/source-adapters.ts
@apps/api/src/auth.ts

<interfaces>
执行时先读取依赖计划 SUMMARY 与真实导出；允许机械适配依赖计划最终文件名，不得改变以下安全语义或增加通用操作入口。

```typescript
export type SourceId = 'moodle' | 'edstem';
export type SourceAction =
  | 'moodle.auth_probe' | 'moodle.course_visibility_probe'
  | 'edstem.auth_probe' | 'edstem.course_visibility_probe';

export type EvidenceClass = 'S' | 'I' | 'N' | 'L';
export interface EvidenceCellKey {
  platform: 'macos' | 'windows';
  source: SourceId;
  scenario: UatScenario;
  evidence: EvidenceClass;
}
export type EvidenceWriterAuthority =
  | { kind: 'automated'; evidence: 'S' | 'I' | 'N'; platform: NativePlatform; producerId: string }
  | { kind: 'human_action'; evidence: 'L'; platform: NativePlatform; actionReceiptId: string };

export interface SourceProbePort {
  probe(request: SourceProbeRequest, signal: AbortSignal): Promise<SourceProbeResult>;
}
export interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: EvidenceCellKey): Promise<EvidenceReceipt[]>;
}
```

02-04 的封闭结果分支必须保持：`AUTH_REQUIRED`、`REAUTH_REQUIRED`、`NETWORK_UNAVAILABLE`、`PARSER_CHANGED`、`CAPABILITY_DENIED`、`INTERACTION_REQUIRED`、`ORIGIN_MISMATCH`、`IDENTITY_MISMATCH`。02-07 的 `AuthJobRunner` 必须在 probe 前、commit 前调用 `AuthJobStore.assertCurrent`，最终 `commitTransition` 仍使用相同 owner/fence/generation/lease/cancel 条件。02-08 API 只允许七类固定 route，所有 mutation 需要 paired browser principal + exact same-origin + CSRF，receipt route 只读且输出 redacted projection。

本计划新增且只供测试使用的 manifest 必须为纯数据，不得包含执行闭包、URL、selector、browser handle、凭据或 Profile path：

```typescript
export type ThreatId =
  | 'T2-01' | 'T2-02' | 'T2-03' | 'T2-04'
  | 'T2-05' | 'T2-06' | 'T2-07' | 'T2-08';

export type SecurityCaseId =
  | 'AUTH_UNAUTHENTICATED'
  | 'AUTH_EXPIRED'
  | 'AUTH_PERMISSION_DENIED'
  | 'AUTH_NETWORK_TIMEOUT'
  | 'AUTH_PARSER_DRIFT'
  | 'BINDING_SAME_NAME_DIFFERENT_SUBJECT'
  | 'BINDING_IDENTITY_MISMATCH'
  | 'ORIGIN_OUT_OF_ORIGIN'
  | 'INPUT_ARBITRARY_OPERATION'
  | 'EFFECT_BUSINESS_WRITE'
  | 'EFFECT_DOWNLOAD'
  | 'OUTPUT_SENSITIVE_SENTINEL'
  | 'PROFILE_HELD'
  | 'PROFILE_PID_REUSE'
  | 'WORKER_STALE_BEFORE_REQUEST'
  | 'WORKER_STALE_BEFORE_COMMIT'
  | 'RETENTION_LAST_SUCCESS'
  | 'UI_UNPAIRED_PROTECTED_READ'
  | 'EVIDENCE_OS_POLLUTION'
  | 'EVIDENCE_CLASS_POLLUTION'
  | 'EVIDENCE_SCENARIO_POLLUTION'
  | 'EVIDENCE_FIXTURE_L';

export interface SecurityMatrixCase {
  id: SecurityCaseId;
  threats: readonly ThreatId[];
  layers: readonly ('contract'|'state'|'adapter'|'profile'|'worker'|'persistence'|'api'|'evidence')[];
  expectedCode: string;
  expectedDisposition: 'reject' | 'human_needed' | 'preserve_and_pause';
  mustNotCall: readonly ('source_request'|'source_write'|'download'|'profile_reclaim'|'job_commit'|'ledger_append_l')[];
}
```
</interfaces>

<hard_gates>
- 本计划只能执行 synthetic/contract 与 integration fixtures；不得打开官方学校页面、使用真实 Profile、请求/读取密码或 MFA、读取课程内容、写来源、下载、发布 beta 或执行 live/native UAT。
- fixture 和 autonomous test authority 只能产生 S/I；不得创建 `human_action` authority、伪造 actionReceiptId、写 L/pass，或把本地恶意页面结果描述为 native/live。
- macOS-first 只允许后续人工执行顺序先行；Windows 原生与 live 单元继续是 `not_run / human_needed`，macOS/S/I/N 不得更新 Windows/L。
- 任一 T2-01–T2-08 High/Critical negative case 失败即测试失败并阻断 02-13 beta assembly、所有 live plans 与 Phase 3；不得 skip、todo、降级为 warning 或修改 expected result 来绕过。
- Phase 1 保持部分通过，Phase 2 不因本计划 green 整体完成；共享 Profile 仍是待 live 验证假设。
</hard_gates>
</context>

<tasks>

<task type="auto">
  <name>02-10-1：建立 canonical T2 case manifest 与 contracts/state/policy 单元矩阵</name>
  <read_first>AGENTS.md 的 Credentials and login、Source and content policy、Data integrity and verification；02-CONTEXT.md D-03、D-05–D-07、D-10–D-11、D-14、D-17；02-RESEARCH.md T2-01–T2-08、Common Pitfalls；02-VALIDATION.md Source contracts、Probe/binding、Retry/retention、Local UI privacy、Evidence ledger；02-01/02-04/02-08 SUMMARY；packages/test-support/src/auth-fixture.ts、packages/contracts/src/index.ts、packages/application/src/auth.ts、packages/application/src/policy.ts、packages/contracts/src/presentation.ts、tests/unit/auth-contracts.test.ts、tests/unit/auth-state.test.ts、tests/unit/output-policy.test.ts。</read_first>
  <files>packages/test-support/src/security-matrix.ts, tests/unit/auth-security-matrix.test.ts</files>
  <action>
创建 `packages/test-support/src/security-matrix.ts`，精确导出 `<interfaces>` 中的 `ThreatId`、`SecurityCaseId`、`SecurityMatrixCase`、包含全部 22 个 ID 的 `REQUIRED_SECURITY_CASE_IDS`、只读 `SECURITY_MATRIX_CASES` 和 `assertSecurityMatrixCoverage(cases)`。manifest 只能保存枚举、预期安全码/处置和禁止调用类别，不得保存 URL、selector、browser handle、operation closure、源站内容、完整身份、Profile path、Cookie/凭据或任意可执行函数。coverage assertion 必须拒绝重复/缺失 case ID、未知 threat/layer、缺少 T2-01–T2-08 任一 threat、空 mustNotCall，以及没有覆盖 contract/state/adapter/profile/worker/persistence/api/evidence 任一层。复用 `makeSyntheticReceipt`，不创建可覆盖 evidence/platform 的第二套 receipt factory；固定敏感 sentinel 仅为明显虚构的测试字符串，永不来自真实资料。

创建 `tests/unit/auth-security-matrix.test.ts`，按 D-05–D-07、D-10–D-11 对 manifest 的 contract/state/policy rows 做 `it.each`：未登录→`AUTH_REQUIRED/unauthenticated`，自然过期→`REAUTH_REQUIRED`，权限拒绝→`CAPABILITY_DENIED`，网络超时→`NETWORK_UNAVAILABLE`，parser drift→`PARSER_CHANGED`；逐项断言 auth/capability/health/freshness/completeness、retry/human-needed、lastSuccess 和另一来源保持其各自语义，不允许把五类压成 logout/error 布尔值。相同 display name/email 但 stable subject 或 organization 不同不得 candidate/confirmed；`IDENTITY_MISMATCH` 令 courseAccess blocked、保持旧 binding 可审计且不发 course probe/自动 logout/overwrite。

对 `SourceProbeRequestSchema`、02-07 `AuthProbeCommand` strict schema 和 02-08 每类 request schema 做同一 forbidden-key 表：`url`、`javascript`、`selector`、`browserHandle`、`operation`、`method`、`requestBody`、`download`、`upload`、`submit`、`reply`、`quizStart`、`cookie`、`storageState`、`profilePath` 与 extra nested key；每项必须 parse 失败且 port/launcher/job/store spy 为 0。对 presenter/error mapping 注入完整姓名/邮箱/course、Profile/Cookie、page/path/query、DOM/HTML、request/response、message/cause/stack sentinel；只有显式 paired protected projection 可包含测试姓名/邮箱，redacted/API error/receipt/log shape 零 sentinel。`makeSyntheticReceipt` 结果必须固定 evidence=S；将 payload 改为 L 或 automated provenance+L 时 schema 拒绝。所有 assertions 是真正行为检查，不得只 grep 类型或复制上游测试名。
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/auth-security-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `rg -n "AUTH_UNAUTHENTICATED|AUTH_EXPIRED|AUTH_PERMISSION_DENIED|AUTH_NETWORK_TIMEOUT|AUTH_PARSER_DRIFT|BINDING_SAME_NAME_DIFFERENT_SUBJECT|BINDING_IDENTITY_MISMATCH|ORIGIN_OUT_OF_ORIGIN|INPUT_ARBITRARY_OPERATION|EFFECT_BUSINESS_WRITE|EFFECT_DOWNLOAD|OUTPUT_SENSITIVE_SENTINEL|PROFILE_HELD|PROFILE_PID_REUSE|WORKER_STALE_BEFORE_REQUEST|WORKER_STALE_BEFORE_COMMIT|RETENTION_LAST_SUCCESS|UI_UNPAIRED_PROTECTED_READ|EVIDENCE_OS_POLLUTION|EVIDENCE_CLASS_POLLUTION|EVIDENCE_SCENARIO_POLLUTION|EVIDENCE_FIXTURE_L" packages/test-support/src/security-matrix.ts` 找到全部 22 个 canonical ID。
    2. `npm run test:unit -- --run tests/unit/auth-security-matrix.test.ts -t "manifest coverage"` 证明 T2-01–T2-08 与八个 layer 均非空；删除任一 case/threat/layer 的合成反例会让 `assertSecurityMatrixCoverage` 抛错。
    3. 单元矩阵逐项断言五类 auth failure 的 exact code、五维状态、retry/disposition、lastSuccess 与另一 source；same-name/different-subject 和 identity_mismatch 均不能产生 confirmed binding/course probe。
    4. forbidden-key 表的每一个键对 SourceProbeRequest、AuthProbeCommand 和对应 API request 都在任何 spy 前失败；测试断言 source_request/source_write/download/profile_reclaim/job_commit/ledger_append_l 计数为 0。
    5. `if rg -n "https?://|selector:|browserHandle:|profilePath:|password:|cookie:" packages/test-support/src/security-matrix.ts; then exit 1; fi` 成功，证明 canonical manifest 不携带可执行来源操作或敏感 fixture 字段。
    6. 自动化命令退出 0，无 `.skip`、`.todo`、真实学校 host、N/L producer 或 live/pass receipt。
  </acceptance_criteria>
  <done>T2-01–T2-08 的必测 ID、层级、预期安全码和禁止副作用有单一 canonical manifest；contracts/state/policy 的关键负例通过表驱动行为断言且 fixture 只能产生 S。</done>
</task>

<task type="auto">
  <name>02-10-2：贯通 adapter/Profile/Worker/API/SQLite 的故障与竞态 integration 矩阵</name>
  <read_first>Task 1 完成后的 security-matrix.ts 与 unit suite；AGENTS.md 的 Profile 独占、lease expiry、来源只读和敏感输出规则；02-CONTEXT.md D-03、D-06–D-07、D-09–D-11、D-17；02-RESEARCH.md T2-01–T2-07；02-VALIDATION.md Profile ownership、Probe/binding、Retry/retention、Local UI privacy；02-02/02-03/02-05/02-06/02-07/02-08 SUMMARY；packages/platform/src/source-adapters.ts、browser-provider.ts、profile-coordinator.ts，packages/application/src/auth-jobs.ts、packages/persistence/src/auth.ts、apps/api/src/auth.ts，tests/integration/source-adapters.test.ts、profile-ownership.test.ts、auth-worker.test.ts、auth-api.test.ts。</read_first>
  <files>tests/integration/auth-security-matrix.test.ts</files>
  <action>
创建 `tests/integration/auth-security-matrix.test.ts`，使用真实 SQLite 临时数据库、02-06 本地恶意页面 fixture、真实 sealed adapter/application/store/runner/loopback route 和可控 clock/process inspector/fault hooks；禁止真实学校网络、真实 Profile、真实 browser user data、OS kill 或凭据。测试启动时从 `SECURITY_MATRIX_CASES` 筛选 integration rows，结束时断言每个筛选 row 被执行恰一次，任何未执行/重复均失败。

按 T2-01/T2-06 注入：越界 redirect、跨 origin popup、POST/form/quiz/reply/upload、download event、oversize/缺失/歧义 positive marker、network timeout、permission denied、parser drift。分别断言 `ORIGIN_MISMATCH`/`PARSER_CHANGED`/`NETWORK_UNAVAILABLE`/`CAPABILITY_DENIED` 等 safe code，adapter abort/close，只有批准 fixed read action 可见；source write count=0、download accepted/file count=0、未批准 request count=0、fallback API count=0。所有失败保留 SQLite `last_success` 与另一个 source observation，不把 current error 当 deletion/confirmed empty/unauthenticated。

按 T2-03 注入两个完整 synthetic protected identities：相同 display name/email、不同 stable subject；相同 subject、不同 organization/scope；confirmed 后任一 stable identity drift。三者均不得自动 confirm/overwrite，mismatch 后 adapter/course visibility/job enqueue/store write 计数为 0，paired protected status 可并列测试身份而 redacted/API error/receipt/captured stdout/stderr 零敏感 sentinel。

按 D-09 与 T2-04 使用 fake process inspector 注入现有 holder running、unknown、相同 PID 但 osStartIdentity 不同、executable 不同、nonce/control proof 不匹配、lease 已过期但 process 仍运行。结果分别只能 `PROFILE_IN_USE` 或 `PROFILE_OWNERSHIP_UNCONFIRMED` + `human_needed`；`kill`、lock delete、release、reclaim、browser launch、generation adoption 计数全部为 0。只有测试中同一 installation+nonce+OS start identity+exact executable 且 `confirmed_exited` 的 owned record 可清理；不能据此生成 N/L。

按 T2-05 对 `AuthJobRunner` 注入 claim 后请求前 cancel/lease expiry/generation bump/fence change，adapter 调用必须为 0；probe 返回后提交前重复四类失权，observation、lastSuccess、EdStem follow-up、receipt 写入必须为 0；第二次 assert 后的 SQL race 令条件更新影响 0 行并回滚整笔事务。进行中 heartbeat 失权必须触发 AbortSignal，不能以新 lease 在同一 runner 内重试。

按 T2-07 经真实 loopback HTTP 覆盖 public/pending/CLI/MCP bearer、缺/错/过期/撤销/restart session、缺/错 CSRF、cross-origin/host/fetch-site 和 paired current session。未配对/失效者不能读取 protected identity；所有非 paired/非 same-origin mutation 均 401/403，launcher/adapter/job/store/ledger write=0。再向固定 routes 逐项提交 Task 1 forbidden keys、额外 verb/path 和 dynamic operation；必须在 driver 前 400/404/405。注入含敏感 sentinel 的 adapter/store/unknown error 后 HTTP、headers、captured stdout/stderr 只出现 safe allowlist，不出现原始 exception、URL/path/query、身份、Profile/Cookie/DOM/network 字段。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-security-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. integration suite 输出/断言覆盖 `ORIGIN_OUT_OF_ORIGIN`、`EFFECT_BUSINESS_WRITE`、`EFFECT_DOWNLOAD`、`OUTPUT_SENSITIVE_SENTINEL`、`PROFILE_HELD`、`PROFILE_PID_REUSE`、`WORKER_STALE_BEFORE_REQUEST`、`WORKER_STALE_BEFORE_COMMIT`、`RETENTION_LAST_SUCCESS`、`UI_UNPAIRED_PROTECTED_READ`，每项执行恰一次。
    2. malicious adapter cases 的 write/download/unapproved request/fallback API/file counters 全部等于 0；每个失败返回独立 safe code，SQLite last_success 与另一 source 在 DB reopen 后字节语义相同。
    3. same-name/different-subject、organization/scope mismatch 与 confirmed drift 均使 binding 非 confirmed/进入 mismatch，后续 course visibility probe、auto logout、binding overwrite 计数为 0；redacted/error/stdout/stderr 零敏感 sentinel。
    4. running/unknown/PID reuse/identity mismatch/expired lease Profile cases 的 kill/delete/release/reclaim/launch/adopt counters 全部为 0，结果精确为 profile_in_use 或 ownership_unconfirmed + human_needed。
    5. stale Worker before-request cases 的 adapter calls=0；before-commit/SQL-race cases 的 observation/lastSuccess/follow-up/receipt 增量=0；in-flight loss 的 signal.aborted=true。
    6. actual HTTP unauthorized/cross-origin/CSRF/strict-input cases 返回精确 400/401/403/404/405，所有下游 spy=0；paired protected read 是唯一可见完整 synthetic identity 的响应。
    7. `if rg -n "\.skip|\.todo|test\.only|describe\.only" tests/integration/auth-security-matrix.test.ts; then exit 1; fi` 成功；自动化命令退出 0，且 harness 未启动真实学校网络、真实 Profile、OS process kill 或 live writer。
  </acceptance_criteria>
  <done>adapter、Profile、Worker、persistence 与 paired API 的关键故障/竞态均由同一矩阵 ID 驱动；每个 fail-closed 行为同时证明安全码、持久状态与零副作用。</done>
</task>

<task type="auto">
  <name>02-10-3：证明 receipt 跨平台/证据类/场景污染不可写且无法推进门禁</name>
  <read_first>Task 1 的 canonical evidence case IDs；AGENTS.md 的证据分类、Windows 原生门禁和 Phase 3 阻断；02-CONTEXT.md D-03、D-14、D-17–D-18；02-RESEARCH.md T2-08；02-VALIDATION.md Evidence ledger、Manual-Only Verifications、Hard Gates；02-01/02-02/02-08 SUMMARY；packages/domain/src/model.ts、packages/contracts/src/index.ts、packages/application/src/ports.ts、packages/test-support/src/auth-fixture.ts、packages/persistence/src/auth.ts、packages/contracts/src/presentation.ts、tests/integration/auth-persistence.test.ts、tests/integration/auth-api.test.ts。</read_first>
  <files>tests/integration/evidence-isolation-matrix.test.ts</files>
  <action>
创建 `tests/integration/evidence-isolation-matrix.test.ts`，对真实 SQLite `EvidenceLedger` 使用完整 required-cell 集合和 payload 外可信 `EvidenceWriterAuthority`。先记录所有 macOS/windows×moodle/edstem×A/B1-1/B1-2/B1-3/B2/B3/C/D/reauth×S/I/N/L cell 的 baseline hash/count/status，再逐项运行 canonical pollution cases；每次拒绝后重读全部 cells，baseline 必须完全不变而不是只检查目标 cell。

`EVIDENCE_OS_POLLUTION` 覆盖 macOS authority+Windows receipt、Windows authority+macOS receipt、macOS event 被 query/gate 当 Windows、同 event 声称多 platform；全部拒绝。`EVIDENCE_CLASS_POLLUTION` 覆盖 S authority 写 I/N/L、I authority 写 S/N/L、fixture 自报 N/L、automated provenance 携带 human action 字段、伪造/空 actionReceiptId、paired API action correlation 被当 EvidenceWriterAuthority；全部拒绝，fixture 实际成功 append 只能 evidence=S。`EVIDENCE_SCENARIO_POLLUTION` 覆盖 A 替 B/C/D/reauth、B 三次互填、B2/B3 互填、D 与 reauth 合并、Moodle 填 EdStem、一个 receipt 覆盖多 scenario/source；全部拒绝或只影响其精确 cell，不能满足另一 cell。

仅以 synthetic S 与 integration I authority 在各自 macOS 精确 cell 追加 pass，随后读取 Phase2Gate/receipt projection：Windows 所有 N/L 和 live required cells仍为 `not_run / human_needed`，macOS L 也仍缺失，Phase 1/2 completion=false、phase3Eligibility=blocked、sharedProfile 不得变为最终 adopted。追加 failed/human_needed 后，较低 evidence、另一 platform/source/scenario 的后续 pass 不得覆盖失败；correction 只能 append 到同一精确 cell 并保留 prior link。验证 API/presenter 只返回 `RedactedEvidenceReceipt` allowlist，完整 identity/course/Profile/Cookie/screenshot/DOM/network sentinel 不进入 DB、WAL、backup、JSON、stdout/stderr。

测试结束调用 `assertSecurityMatrixCoverage` 并核对 T2-01–T2-08 在 unit+integration+evidence suite 的静态 registry 中均有至少一个实际 test binding；任何缺失、skip/todo、仅 grep 无行为断言都失败。不得构造 human_action authority 来测试成功 L 写入；L 的正向写入保留给后续真实人工 checkpoint，本计划只证明自动/fixture 无法取得该权限。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/evidence-isolation-matrix.test.ts &amp;&amp; npm run test:unit -- --run tests/unit/auth-security-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. evidence suite 明确执行 `EVIDENCE_OS_POLLUTION`、`EVIDENCE_CLASS_POLLUTION`、`EVIDENCE_SCENARIO_POLLUTION`、`EVIDENCE_FIXTURE_L`，每次受拒写入后全矩阵 hash/count/status 与 baseline 完全一致。
    2. authority×payload 表覆盖 macOS↔Windows、S↔I/N/L、fixture→N/L、automated→human_action、source/scenario 多值/错配；所有非法 append 返回受控错误且没有 DB/WAL/backup 污染。
    3. 仅写入所有 macOS S/I pass 后，查询仍显示每个 Windows N/L/live cell 为 `not_run / human_needed`、macOS L 缺失、Phase 1/2 incomplete、Phase 3 blocked、shared Profile unverified。
    4. A、B1-1/B1-2/B1-3、B2、B3、C、D、reauth 和 Moodle/EdStem 每格独立；测试通过删除/替换单一 receipt 证明只有该 cell 的投影变化。
    5. failed/human_needed event 不能被较低 evidence 或另一 platform/source/scenario pass 覆盖；同 cell correction 保留原 event、prior link、safe reason code，禁止 update/delete/replace。
    6. `rg -n "T2-01|T2-02|T2-03|T2-04|T2-05|T2-06|T2-07|T2-08" packages/test-support/src/security-matrix.ts tests/unit/auth-security-matrix.test.ts tests/integration/auth-security-matrix.test.ts tests/integration/evidence-isolation-matrix.test.ts` 找到全部 threat 与实际 test binding；任一缺失使 coverage test 失败。
    7. 定向命令退出 0；测试文件无 `.skip/.todo/.only`、human_action success authority、真实身份/学校 host/Profile、N/L pass 或 Phase 3 eligible fixture。
  </acceptance_criteria>
  <done>S/I 自动 evidence 只能进入其精确 platform/source/scenario/class cell；所有污染尝试对全 ledger 零影响，Windows/L/live 缺口和 Phase 3 硬阻断不能被自动化结果改变。</done>
</task>

</tasks>

<threat_model>
本计划以 OWASP ASVS 5.0 L1 的输入验证、访问控制、会话、数据保护、失败安全和可审计证据原则组织负向矩阵；这不是完整合规声明。T2-01–T2-08 全部为 Critical/High，统一 disposition=`mitigate`；任一命名 case 不通过即为 blocking failure，不得 accept、transfer 或延期到 live 后。

## Trust Boundaries

| Boundary | 不可信输入/竞态 | 强制控制与矩阵证据 |
|---|---|---|
| fixture/caller → strict contracts/API | extra key、任意 operation、伪 principal/CSRF/authority | strict parse 与 paired+same-origin authorization 先于任何 port；forbidden-key/permission 表断言下游调用为 0 |
| 未可信页面 → sealed adapter | redirect/popup、脚本指令、form/POST、download、marker/identity 欺骗 | 固定 origin/action、只读路由、有界 marker/identity、download=false；malicious fixture 断言零写/零文件 |
| Profile/OS process → coordinator/browser | stale PID、PID reuse、过期 lease、错误 executable/nonce/control proof | 完整 owner identity 与 confirmed exit；running/unknown 只 human_needed；零 kill/delete/reclaim |
| durable DB/lease → Worker/source/commit | cancel、lease expiry、generation/fence 变化、TOCTOU | request 前与 commit 前 assert，heartbeat abort，最终 SQL fence；零 stale request/commit |
| internal identity/error → API/presenter/log | 同名欺骗、完整身份、Profile/Cookie、raw exception | stable subject+organization+scope；protected/redacted 分离；safe allowlist 与 sentinel scan |
| automated producer → EvidenceLedger/gate | payload 伪造 platform/evidence/scenario/result | payload 外 authority、精确 cell、append-only；全矩阵 baseline 验证零污染，fixture 无 L authority |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Severity | STRIDE | Component | ASVS L1 intent | Mitigation / blocking assertion |
|---|---:|---|---|---|---|
| T2-01 | Critical | T/E | contracts、API、sealed adapters | V4/V5/V13 fixed authorization and input validation | 任意 URL/JS/selector/handle/operation、越界 origin、业务写/download 在 driver 前拒绝；任一 source write/file/unapproved request 非零即 blocker。 |
| T2-02 | Critical | I | Profile、DB、presenter、logs/test output | V8 data protection、V14 secure config、V16 safe error/logging | Profile/Cookie/credential/raw DOM/network/identity sentinel 不得进入 DB/WAL/backup/redacted/error/stdout；任一泄漏即 blocker。 |
| T2-03 | High | S/T/E | identity evidence、binding、course gate | V2 identity proof、V4 authorization、V13 business rules | same display name/email 不绑定；stable subject/organization/scope mismatch 与 drift 硬停 course access；任一 auto-confirm/overwrite/continued probe 即 blocker。 |
| T2-04 | Critical | S/T/D/E | Profile ownership/process proof | V1 trust boundary、V4 privileged operation、V14 process configuration | running/unknown/PID reuse/identity mismatch 只 human_needed；lease expiry 不 kill/delete/reclaim；任一非 owned process action 即 blocker。 |
| T2-05 | High | S/T/E | durable Worker、lease/fence、commit | V4 authorization re-check、V13 transaction integrity | request/commit 前 cancel+lease+generation+fence，最终 SQL fence；任一 stale request/observation/follow-up/receipt 写入即 blocker。 |
| T2-06 | High | T/R | reducer、observation persistence | V13 state integrity、V16 auditable error classification | auth/expiry/permission/network/parser 分离并保留 last-success/other source；任一折叠、清除或跨源污染即 blocker。 |
| T2-07 | High | S/I/E | paired API、protected presenter | V2/V3 session、V4 CSRF/access control、V8 privacy | public/pending/expired/CLI/MCP/cross-origin 不能读 protected 或 mutation；401/403 下游为 0；任一越权/完整身份泄漏即 blocker。 |
| T2-08 | High | T/R/E | EvidenceLedger、receipt projection、Phase2Gate | V4 writer authority、V13 business rules、V16 provenance | platform/source/scenario/evidence 精确 cell，fixture/automated 无 L；macOS S/I 不填 Windows/L/Phase3；任一污染或伪进度即 blocker。 |
</threat_model>

<verification>
1. Unit matrix：`npm run typecheck && npm run test:unit -- --run tests/unit/auth-security-matrix.test.ts`。
2. Cross-layer integration：`node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-security-matrix.test.ts`。
3. Evidence isolation：`node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/evidence-isolation-matrix.test.ts`。
4. Upstream focused regression：`npm run test:unit -- --run tests/unit/auth-contracts.test.ts tests/unit/auth-state.test.ts tests/unit/output-policy.test.ts && node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-persistence.test.ts tests/integration/source-adapters.test.ts tests/integration/auth-worker.test.ts tests/integration/auth-api.test.ts`。
5. Coverage/static safety：确认 22 个 required case、T2-01–T2-08 与八层全部绑定到实际 test；扫描四个新文件无 skip/todo/only、真实学校 host、Profile/Cookie/credential fixture、human_action success authority 或 L/pass 生成。
6. 失败注入的受控输出只含 synthetic IDs、安全码、计数与矩阵 case ID；不得保存 screenshot、trace、HAR、video、raw HTML/DOM/network 或完整身份。
7. 所有测试 green 仅表示 S/I automated matrix 通过；Windows 仍 `not_run / human_needed`，Phase 1/2 未整体完成，Phase 3 仍 blocked。
</verification>

<success_criteria>
- 22 个 canonical cases 全部执行，T2-01–T2-08 各至少一个行为 negative test 且八个层级无缺口。
- 未登录/过期/权限/网络/parser 状态严格区分，last-success 与另一来源始终保留。
- 同名不同主体不绑定，identity_mismatch 后所有课程访问与自动修复副作用为零。
- 越界 origin、任意 operation、业务写/download 在 driver 前拒绝；没有来源写、下载文件或 fallback API。
- Profile held/PID reuse/unknown owner 不 kill/delete/reclaim，旧 Worker 在请求/提交两个窗口都无法产生副作用。
- S/I receipts 只能写精确 cell；fixture/automated 不能写 L，macOS 不能更新 Windows，任一污染尝试对全 ledger 零影响。
- protected/redacted/error/log/DB 边界无敏感泄漏；所有 High/Critical case 失败均阻断后续 beta/live/Phase 3。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-10-SUMMARY.md`，仅记录 22 个 case ID、T2 coverage、脱敏 safe code/副作用计数、S/I 测试命令与仍未运行的 N/L/Windows/live gaps；不得包含完整身份、学校 origin/path、Profile/PID、Cookie/credential、DOM/network、截图或 live pass。
</output>
