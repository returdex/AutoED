---
phase: 02-poc-live
plan: "27"
type: execute
wave: 23
depends_on:
  - "02-26"
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "02-27 是未来门禁：当前 02-24 Windows deferred checkpoint 未被用户显式恢复时不得执行、不得提示 Windows A2、不得打开学校来源；计划文件存在或 macOS 已通过均不是 resume。"
    - "只有 02-24 completed SUMMARY 证明用户主动恢复原生 Windows 11、02-25 实际 Windows beta/update strict pass、02-26 Windows A1 双来源登录 strict pass，且 current installed build/session/config/scope 未漂移，才能进入 Windows A2。"
    - "用户必须在 Windows paired protected UI 中独立核对 Moodle 与 EdStem 的完整真实账户、组织/租户和 approved scope，并在 Windows 本轮明确确认一次；macOS A2 binding、相同姓名/邮箱、旧 confirmed binding 或 shared Profile 观察都不能代替该动作。"
    - "稳定 subject+organization/tenant+approved scope 只能产生本轮 Windows review candidate；证据不足仍需用户在同一 protected UI 人工确认，任一 identity/organization/tenant/scope mismatch 立即硬停且 course probe=0。"
    - "binding 通过后只对 02-15 持久确认且 02-26 current 的同一个 approvedScopeId/单一明确课程，分别执行 Moodle/EdStem 固定 course-visibility probe；不接受任意 URL/selector/JS/API fallback，不读取正文、帖子、文件、成绩或反馈。"
    - "Windows A2 必须形成四个独立 current non-corrected Windows/L exact cells：Moodle/EdStem × `A2_binding|A2_course_visibility`；macOS cells、A1 receipt、S/I/N、另一来源/场景或一个合并 pass 都不能满足。"
    - "L authority 只由 published paired server workflow 为本轮 Windows A2 mint/consume；repo `phase2-live-gate.mjs`、public API、CLI、MCP、聊天反馈与用户自造 ID均只读或无权，不能 append/correct L。"
    - "任何 mismatch、unauthenticated/interaction required、scope/course不确定、越界读取、敏感输出、缺 cell、旧 build/generation、非原生 Windows或用户 non-pass 都保持 failed/human_needed 并阻止 02-28；A2 pass仍不完成Windows链、Phase 1/2或Phase3。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      provides: "只有用户未来明确恢复原生 Windows UAT 后才存在的停止解除交接；仍要求所有 Windows cells 起始为not_run/human_needed"
    - path: ".planning/phases/02-poc-live/02-25-SUMMARY.md"
      provides: "当前 Windows 11 已实际获取/更新到不可覆盖签名 beta，build/runtime/paired UI ready 的独立人工前置"
    - path: ".planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json"
      provides: "02-25 strict pass-only native Windows update machine gate；证明非WSL Windows 11、exact build/cleanup/services/paired UI且学校尚未访问"
      contains: "windows"
    - path: ".planning/phases/02-poc-live/02-26-SUMMARY.md"
      provides: "当前 Windows build 上 Moodle 登录/MFA、EdStem有界认证与双来源本地身份观察的strict A1 handoff；非pass时A2不可开始"
    - path: "apps/status/src/main.ts"
      provides: "Windows paired protected UI 的双来源完整身份/组织/范围核对、平台独立一次确认、course visibility与脱敏结果单"
      contains: "CheckpointLedger"
    - path: "packages/platform/src/source-adapters.ts"
      provides: "从confirmed config/scope内部构造的Moodle/EdStem固定identity/course-visibility probes及official-origin/positive-marker边界"
      contains: "SealedSourceAdapters"
    - path: "packages/persistence/src/auth.ts"
      provides: "AccountBinding/current observations与append-only EvidenceLedger；exact Windows/source/scenario/L cells及server-only writer authority"
      contains: "EvidenceLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "Windows A1/A2 current exact cells的canonical read-only verifier与脱敏编排输出；无L writer或binding confirmation权限"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-27-SUMMARY.md"
      provides: "仅真实Windows A2 strict pass后生成的脱敏交接；runtime EvidenceLedger仍是source of truth且无planning receipt JSON"
  key_links:
    - from: ".planning/phases/02-poc-live/02-24-SUMMARY.md"
      to: ".planning/phases/02-poc-live/02-26-SUMMARY.md"
      via: "Windows resumed→actual beta update→native A1三重前置；任一缺失都不呈现A2动作"
      pattern: "windows|resumed|A1"
    - from: "apps/status/src/main.ts"
      to: "packages/persistence/src/auth.ts"
      via: "Windows protected UI基于本轮current Windows observations生成独立review candidate；用户明确确认后paired server mint/consume A2_binding source children，不复用macOS authority"
      pattern: "A2_binding|confirm_binding|human_action"
    - from: "apps/status/src/main.ts"
      to: "packages/platform/src/source-adapters.ts"
      via: "只有Windows binding复核pass后，提交固定approvedConfigId/approvedScopeId的course_visibility_probe；不提交URL/selector/course name"
      pattern: "course_visibility_probe|approvedScopeId"
    - from: "packages/platform/src/source-adapters.ts"
      to: "packages/persistence/src/auth.ts"
      via: "Windows actual visible result、paired human correlation与current build/generation/config/scope/binding同事务产生四个exact Windows/L cells"
      pattern: "A2_binding|A2_course_visibility|windows"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-28 eligibility"
      via: "只读conjunction验证四个Windows/L current non-corrected cells、用户真实feedback、identity/scope/content/privacy边界；macOS/S/I/N不可代填"
      pattern: "A2|windows|read-only"
---

<objective>
在用户未来明确恢复原生 Windows 11 UAT 且 Windows A1 已真实通过后，执行 Windows A2 硬人工门：用户在 paired protected UI 独立核对 Moodle/EdStem 的完整账户、组织/租户和授权范围，明确确认一次 Windows binding，然后只验证同一个已批准课程的双来源可见性。

Purpose: 落实 D-03、D-06–D-07、D-12–D-15、D-17–D-18 与 AUTH-01/AUTH-02/AUTH-03/SEC-02/UAT-01，防止把 macOS binding、同名账户或旧全局状态冒充 Windows 真实账户绑定；把身份/范围 mismatch 与课程访问严格隔离。
Output: 一个未来才可执行的 blocking `checkpoint:human-action`、Moodle/EdStem × `A2_binding|A2_course_visibility` 四个独立 Windows/L events、canonical read-only A2 gate与strict pass后的02-27 SUMMARY。当前不执行、不访问Windows/学校、不创建planning receipt JSON、不释放02-28。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-06-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-10-SUMMARY.md
@.planning/phases/02-poc-live/02-11-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-15-SUMMARY.md
@.planning/phases/02-poc-live/02-17-SUMMARY.md
@.planning/phases/02-poc-live/02-24-SUMMARY.md
@.planning/phases/02-poc-live/02-25-SUMMARY.md
@.planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json
@.planning/phases/02-poc-live/02-26-PLAN.md
@.planning/phases/02-poc-live/02-26-SUMMARY.md
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/auth.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/source-adapters.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时必须使用下列final Windows update/A1/A2 gates与published beta的Windows A2 paired workflow；若命令/字段未进入signed beta，必须human_needed→修复/全测/新beta/实际更新，不能临时复制macOS receipt命令、直读DB或另造writer。稳定contract为：

```typescript
type SourceId = 'moodle' | 'edstem';
type NativePlatform = 'macos' | 'windows';
type EvidenceClass = 'S' | 'I' | 'N' | 'L';
type UatScenario =
  | 'A1_login'
  | 'A2_binding'
  | 'A2_course_visibility'
  | 'B1_reopen_1'
  | 'B1_reopen_2'
  | 'B1_reopen_3'
  | 'B2_worker_restart'
  | 'B3_codex_exit'
  | 'C_os_restart'
  | 'D_24h_recheck'
  | 'reauth';
```

Windows A2 required cells固定为：

```text
windows × moodle × A2_binding × L
windows × edstem × A2_binding × L
windows × moodle × A2_course_visibility × L
windows × edstem × A2_course_visibility × L
```

每个cell的L authority只能由published paired server内部生成：新bundle绑定platform=windows、checkpoint=A2、source、exact scenario、current installation/version/build、runtime generation、paired session、approvedConfigId、approvedScopeId、Windows本轮identity/binding candidate、expiry；action ID不出server。repo gate只读redacted status/exact cells，chat feedback只是checkpoint必要条件，不是authority。

02-25 native Windows update gate：

```text
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only
```

02-26 Windows A1 gate：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A1 --runtime current --platform windows --read-only
```

strict pass code固定为`WINDOWS_A1_COMPLETE`，必须有Moodle/EdStem两个current non-corrected `windows/L/A1_login` cells、completed 02-26 SUMMARY、02-25 native update/resume proof，且macOS cells完全不变。

02-27 Windows A2 post-action gate：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A2 --runtime current --platform windows --read-only
```

strict pass code固定为`WINDOWS_A2_COMPLETE`，必须有四个current non-corrected Windows/L cells（`A2_binding|A2_course_visibility`×Moodle/EdStem）、completed 02-27 SUMMARY所需的本轮真实feedback、02-25 update/resume与A1 proof；macOS cells必须保持不变。不得自行添加/删除flags或以macOS `02-17-A2-RECEIPT.json`/A2 verifier代替。两个live gate都只读，不创建A1/A2 planning receipt。
</interfaces>

<hard_gates>
- 这是future-only计划。02-24 SUMMARY不存在、没有用户explicit Windows resume、02-25/02-26任一未strict pass时，不运行preflight以外动作、不提示登录/绑定、不访问学校、不mint authority。
- Windows A2从四个not_run/human_needed cells开始；macOS A2 pass/receipt/binding、全局confirmed状态、相同display name/email、S/I/N或A1不能预填/跳过本轮确认。
- 完整姓名、邮箱、组织/租户、课程名只在current Windows paired protected UI显示；chat/SUMMARY/gate/stdout/log/diagnostic无这些值，也无Profile/PID/Cookie/DOM/selector/request/action ID/screenshot。
- mismatch在任何course probe前硬停：不自动切号、logout、覆盖binding、复制Cookie/session、尝试另一个课程或复用macOS identity。
- A2只做identity/binding与一个approved course visibility；不读正文/帖子/文件/grades/feedback，不download/write/POST/form/quiz/upload，不猜API。
- Windows A2 pass只允许02-28 B1 eligible；Windows B/C/D/reauth仍not_run/human_needed，Phase1/2 incomplete、shared Profile未最终采用、Phase3 blocked。
</hard_gates>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-27-1：用户在原生Windows独立确认双来源binding并核对单课程可见性</name>
  <files>none（用户动作只影响仓库外current Windows beta/runtime、paired protected UI、server pending-action与既有append-only EvidenceLedger；不修改源码、学校资料或planning receipt JSON）</files>
  <read_first>AGENTS.md的Credentials and login、Source and content policy、Data integrity and verification；02-CONTEXT D-03/D-06–D-07/D-12–D-15/D-17–D-18；02-24-SUMMARY的explicit Windows resume；02-25-SUMMARY的actual Windows update/build；02-26-SUMMARY的Windows A1 exact gate/current observations/canonical verifier；02-15-SUMMARY与runtime persisted approved config/scope；02-04/02-06/02-08/02-09 SUMMARY的binding state、sealed probes、paired API/protected UI；最终EvidenceLedger/pending-action/read-only gate contract。禁止读取legacy Profile、真实Cookie/凭据、raw DB/WAL、私人页面/网络资料或课程内容。</read_first>
  <what-built>用户未来已明确恢复原生Windows UAT并在Windows 11更新current signed beta；Windows A1的Moodle登录/MFA、EdStem有界认证及两来源本地身份观察已strict pass，但Windows A2四格仍not_run/human_needed，macOS binding不能代填。</what-built>
  <action>首先严格验证future state：02-24 completed SUMMARY含用户explicit native Windows resume且仅释放02-25；运行exact 02-25 update gate并要求completed SUMMARY+pass-only receipt共同证明native non-WSL Windows 11、actual beta/update/current build/cleanup/services/paired UI；运行exact A1 gate并要求`WINDOWS_A1_COMPLETE`、completed 02-26 SUMMARY及Moodle/EdStem两个current non-corrected `A1_login` Windows/L cells。重新读取runtime confirmed Moodle/EdStem config/scope及02-15同一个approvedScopeId；四个Windows A2 cells必须仍为空/not_run且macOS cells前后不变。任何缺失、macOS代填、旧build/generation、partial/failed/corrected A1、非原生Windows或scope drift立即human_needed，不呈现A2 UI。

preflight通过后，用户只在current Windows paired protected UI并列核对Moodle/EdStem完整姓名、学校邮箱、organization/tenant、稳定身份类别和approved scope，并分别对照02-15目标账户/组织；这些值绝不复制到chat。product必须从本轮Windows actual observations创建新的platform-specific review candidate，即使global/macOS binding已confirmed也显示Windows A2 pending，不能自动pass。稳定subject+organization/tenant+scope可支持candidate但仍需用户点击一次`确认Windows中的两个账户对应`；证据不足时也只能由用户在protected UI明确确认。相同姓名/邮箱不足以绑定。

任一source身份、组织/tenant、approved scope、current A1或既有target不一致，立即产生safe `IDENTITY_MISMATCH`/`APPROVED_SCOPE_MISMATCH`（以published enum为准），courseAccess=blocked、course probe/request=0、binding overwrite/logout/cookie copy=0，并返回failed/human_needed阻止02-28。不得让用户为了“试试看”继续课程步骤。

用户确认Windows binding时，published paired server为Moodle/EdStem分别mint新的Windows `A2_binding` pending-action children，绑定current build/generation/session/config/scope/candidate并一次性consume/append；macOS action/binding evidence、A1 action或client自造ID不可重放。binding两格strict pass后，server才允许对02-15同一approvedScopeId分别创建新的`A2_course_visibility` children并调用Moodle/EdStem固定course-visibility probe各一次；adapter内部构造目标并逐跳校验official origin，只确认visible positive marker/current identity/scope，不读取course body/posts/files/grades/feedback。任何popup/interaction/download/non-GET/write/parser drift/API fallback/越界redirect均fail closed。

四个actual results由paired server与human correlations同事务append到exact Windows/L cells；failed/human_needed也immutable保留并阻断，不能由repo script、chat feedback或later latest-pass覆盖。用户在Windows protected UI检查脱敏A2结果单并返回`<resume-signal>`；未收到真实反馈即使runtime observations pass也保持checkpoint未完成。反馈后repo侧只运行exact Windows A2 read-only verifier；只有resultCode=`WINDOWS_A2_COMPLETE`、四格current non-corrected pass、用户确认、identity/scope/course/content/privacy全conjunction且macOS cells不变才完成。不得创建A2 planning receipt JSON或把SUMMARY当ledger。
  </action>
  <how_to_verify>
    1. 确认当前设备是02-24后明确恢复的原生Windows 11，paired UI显示current version/build与02-25/02-26相同、Windows A1 pass、Windows A2 pending；不要用WSL、远程macOS或fixture。
    2. 在protected UI本地逐项核对Moodle与EdStem完整账户、组织/tenant、approved scope都对应02-15目标；不要把姓名、邮箱、组织、课程或origin粘贴到聊天。
    3. 即使页面显示macOS/global binding已confirmed，也必须看到Windows本轮candidate并本人点击一次`确认Windows中的两个账户对应`；若任何值不一致，选择failed/human_needed并立即停止。
    4. binding pass后，只触发同一个approved course的固定visibility检查一次；确认两source显示visible/current identity/scope matched，不打开正文、帖子、文件、成绩、反馈或其他课程。
    5. 核对脱敏result只含version/build、platform=windows、source、scenario、time、safe code、binding/scope/course visibility/content boundary/gaps；无identity/course/origin/Profile/PID/Cookie/action ID/raw page/network/screenshot。
    6. 只复制`<resume-signal>`的fixed redacted block。任一项非pass/confirmed/matched/preserved/not_used时如实返回failed/human_needed，不尝试第二个binding/course或继续02-28。
  </how_to_verify>
  <verify>
    <automated>checkpoint前运行`node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-25-WINDOWS-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --platform windows --read-only`与`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A1 --runtime current --platform windows --read-only`，要求后者`WINDOWS_A1_COMPLETE`并确认四个Windows A2 cells均not_run；用户反馈且paired server已形成actual results后，运行`node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint A2 --runtime current --platform windows --read-only`并要求`WINDOWS_A2_COMPLETE`。三命令均exit 0且只读。</automated>
  </verify>
  <acceptance_criteria>
    1. 02-24 explicit resume、02-25 update receipt+SUMMARY+canonical gate、02-26 SUMMARY+A1 gate=`WINDOWS_A1_COMPLETE`全部current strict pass；非原生/未resume/macOS证据不能进入A2。
    2. Windows A2动作前四个`windows×{moodle,edstem}×{A2_binding,A2_course_visibility}×L` cells均not_run/human_needed；macOS A2/全局binding/A1/S/I/N没有预填。
    3. 用户在Windows protected UI独立核对两source完整identity、organization/tenant与approved scope并明确确认一次；完整私人值仅本地visible text，不进入反馈/receipt/SUMMARY/日志。
    4. 相同name/email不自动绑定；任一identity/org/scope mismatch使course probe=0、binding overwrite/logout/cookie copy=0并立即failed/human_needed阻止02-28。
    5. 每轮Windows A2使用server-minted source/scenario/current-build/generation/session/config/scope/candidate绑定的新one-time bundle；macOS/A1/另一source/scenario/action replay全部拒绝，repo/public/chat writer调用为0。
    6. binding strict pass后只执行Moodle/EdStem各一次同一approved course visibility；正文/posts/files/grades/feedback/另一course/download/write/popup/API fallback为0。
    7. EvidenceLedger最终恰有四个current non-corrected Windows/L pass cells，runtime actual results与用户fixed feedback一致；任一missing/failed/human_needed/另一platform/evidence不能代填。
    8. canonical Windows A2 verifier exit 0且resultCode=`WINDOWS_A2_COMPLETE`，输出无identity/course/origin/Profile/PID/Cookie/action ID/path/DOM/network/screenshot；没有A2 planning receipt JSON，macOS cells保持不变。
    9. pass只允许02-28 B1 eligible；Windows B/C/D/reauth仍not_run/human_needed，Phase1/2 incomplete、sharedProfile unverified、Phase3 blocked，02-28不自动执行。
  </acceptance_criteria>
  <resume-signal>只粘贴Windows protected UI生成的完整`02-27_WINDOWS_A2_RESULT`脱敏块：`result=pass|failed|human_needed`、`result_code=&lt;published allowlisted code&gt;`、`platform=windows`、`moodle_identity_review=confirmed|mismatch|not_observed`、`edstem_identity_review=confirmed|mismatch|not_observed`、`binding=confirmed|mismatch|not_observed`、`approved_scope=matched|mismatch|not_observed`、`moodle_course_visibility=pass|failed|human_needed`、`moodle_code=&lt;safe code&gt;`、`edstem_course_visibility=pass|failed|human_needed`、`edstem_code=&lt;safe code&gt;`、`content_boundary=preserved|violated|not_observed`、`screenshot=not_used`、`observed_at=&lt;ISO-8601 with timezone&gt;`、`phase3=blocked`。不要发送姓名、邮箱、组织、课程、origin、Profile/PID/Cookie、action ID、截图、日志或原始错误。</resume-signal>
  <done>未来用户在明确恢复的原生Windows/current beta上独立确认两来源identity/binding/scope，并仅验证同一个approved course visibility；四个Windows/L exact cells和真实脱敏feedback strict pass后才允许02-28，否则保持blocked。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| deferred macOS-first state → Windows A2 | 计划存在/macOS pass可能被误当Windows已恢复 | 02-24 explicit resume SUMMARY + 02-25 update + 02-26 A1三重strict preflight；缺一不执行 |
| macOS/global binding → Windows review | 旧binding或相同显示身份可能跳过Windows独立人工确认 | current Windows observations生成新platform review candidate；用户本轮确认；macOS cells/action无authority |
| protected identity → user/chat | 完整姓名、邮箱、org/course只可在paired Windows UI | paired principal+same-origin CSRF；visible-local-only；fixed redacted feedback；401/403 purge |
| untrusted source pages → course probe | 页面/redirect/marker可能扩张范围或诱导读取 | persisted config/scope、sealed fixed action、逐跳official origin、bounded visible marker、无body/write/download/API fallback |
| paired human action → EvidenceLedger | client/chat/repo或旧platform action可能伪造Windows L | server-minted one-time platform/source/scenario-bound bundle；actual result同事务consume/append；repo gate read-only |
| four A2 cells → B1 eligibility | macOS/A1/S/I/N/一个source或latest row可能代填 | exact Windows/L four-cell current non-corrected conjunction +真实feedback；missing/mismatch立即blocked |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking test |
|---|---|---:|---|---|---|---|
| T2-03 | Spoofing / Tampering / Elevation of Privilege | Critical | Windows identities/binding/scope | mitigate | V2身份、V4对象授权、V11业务逻辑、V13状态 | current Windows stable identity+org/tenant+scope、平台独立user confirmation；same-name/email不可绑定；mismatch先于course probe硬停。任何自动复用macOS/global binding或mismatch后访问即Critical blocker。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | platform/evidence/scenario cells与B1 gate | mitigate | V1信任分层、V4writer authority、V13状态机、V16provenance | 四个exact Windows/L cells、new server bundle、current build/generation、repo read-only；macOS/A1/S/I/N不可代填。跨格或自动批准即Critical blocker。 |
| T2-01 | Tampering / Elevation of Privilege | Critical | UI command → sealed course probe | mitigate | V4访问控制、V5输入验证、V12资源、V13API | operation只引用approved config/scope与fixed action；无URL/JS/selector/browser handle；official-origin逐跳；无write/download/API fallback。任一越界调用即Critical blocker。 |
| T2-02 | Information Disclosure | Critical | protected UI/source → feedback/receipt/SUMMARY | mitigate | V7错误/日志、V8数据保护、V14配置 | full identity/org/course仅protected visible text；固定redaction；无Profile/Cookie/DOM/network/screenshot/action ID。任一敏感输出即failed并阻止02-28。 |
| T2-07 | Spoofing / Information Disclosure / Elevation of Privilege | High | Windows browser → paired mutations/protected identity | mitigate | V2认证、V3session、V4CSRF/授权、V13API | current paired principal+same-origin CSRF、server candidate/action；public/CLI/MCP无完整identity或writer。401/403 purge；绕过pairing即High blocker。 |
| T2-06 | Tampering / Repudiation | High | stale macOS/Windows observation → A2 pass | mitigate | V11业务状态、V13事务、V16审计 | current build/generation/source observations与last-success分离；A1/A2 exact current cells；stale/macOS状态不可复用。旧状态通过即High blocker。 |

所有High/Critical项必须`mitigate`，不得accept、transfer、降级warning或以future计划补救。本表仅表达ASVS 5.0 L1适用控制意图，不声明完整合规。任一未resume、missing verifier、mismatch、越界读取、隐私泄漏、非current cell或writer污染都使02-27保持failed/human_needed并阻止02-28/Phase3。
</threat_model>

<verification>
1. future execution首先证明02-24 explicit native Windows resume、02-25 actual update/current build与02-26 A1 strict pass；当前未resume状态绝不运行。
2. A2前四个Windows/L exact cells为空/not_run；macOS A2/global binding/A1/S/I/N不能预填或省略Windows用户确认。
3. Windows protected UI本地核对两source完整identity/org/tenant/scope并由用户明确确认；same-name/email与stable evidence都不能自动pass。
4. mismatch分支在任何course request前停止且不改binding/logout/session；pass分支只运行同一个approved course的两次fixed visibility probes，无content/write/download/API fallback。
5. paired server每source/scenario mint新Windows-bound one-time authority并以actual result同事务append；repo/public/chat/action replay写入为0。
6. read-only verifier严格要求四个current non-corrected Windows/L cells+真实feedback+identity/scope/course/content/privacy conjunction；negative substitutions全部nonzero。
7. receipt/feedback/verifier/SUMMARY敏感扫描无identity/org/course/origin/Profile/PID/Cookie/action ID/path/DOM/network/screenshot。
8. pass后仅02-28 eligible；Windows B/C/D/reauth、Phase1/2、sharedProfile、Phase3仍保持缺口且无auto-advance。
</verification>

<success_criteria>
- AUTH-01/AUTH-03：用户在原生Windows独立核对Moodle/EdStem真实identity/org/tenant/scope并确认binding；mismatch有safe code且course access硬停。
- AUTH-02/UAT-01：Windows A2形成独立于macOS/A1的四个真实L cells与用户fixed feedback；未resume/未更新/A1失败/用户non-pass不推进。
- SEC-02：只探测同一个approved course visibility；无任意operation、课程正文、下载、业务写、未授权API或敏感输出。
- T2-03/T2-08：macOS binding/receipt、相同姓名邮箱、S/I/N、另一cell或旧action均无法成为Windows A2 pass；paired server-only writer与repo read-only gate成立。
- Windows A2 pass只释放02-28 B1，自身不执行02-28、不完成Windows全链/Phase1/2、不最终采用sharedProfile、不解除Phase3。
</success_criteria>

<output>
只有future explicit Windows resume、02-25/02-26 strict pass、真实Windows用户fixed feedback和canonical A2 read-only verifier全部通过后，才创建`.planning/phases/02-poc-live/02-27-SUMMARY.md`。SUMMARY只记录公开version/build、platform=windows、checkpoint=A2、四个source/scenario safe result codes、binding/scope/course/content/privacy结论、observedAt、02-28 eligibility与剩余Windows/Phase3 gaps；不创建A2 planning receipt JSON，不记录姓名/邮箱/org/course/origin/Profile/PID/Cookie/action ID/raw page/network/log/screenshot。当前未resume或任一failed/human_needed时不执行、不创建成功SUMMARY、不释放02-28。
</output>
