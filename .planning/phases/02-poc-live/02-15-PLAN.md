---
phase: 02-poc-live
plan: "15"
type: execute
wave: 15
depends_on:
  - "02-14"
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-03
  - SEC-02
  - UAT-01

must_haves:
  truths:
    - "只有已完成 02-14 实际 macOS 更新、当前 paired local UI 会话中的用户本人，才能确认 Moodle/EdStem official origins、目标账户/组织、恰好一个指定课程及允许目的地；聊天、CLI、MCP、日志和自动化不能代替确认。"
    - "02-14的current signed-build macOS named S/I/N receipt必须strict pass；它不替代后续22个required macOS L cells。"
    - "范围未确认、只确认一部分、校验失败或 paired session 失效时，产品不打开学校页面、不启动 Playwright 官方窗口、不入队 auth/course probe，也不发送任何学校请求。"
    - "配置确认只接受规范化 HTTPS official origin 与封闭的账户/组织/单课程/目的地范围；保存后所有 operation 只能携带 approvedConfigId/approvedScopeId 和固定动作，不能逐操作传入 URL、JS、selector 或浏览器句柄。"
    - "确认页面在提交前清楚展示两个 official origins、目标账户/组织、一个课程、允许目的地与隐私提示；用户不需要把任何实际值、姓名、邮箱或课程名发到聊天。"
    - "聊天、SUMMARY、日志与诊断只记录 checkpoint 编号、构建身份、平台、时间、脱敏结果码和 gap；不记录 origin、账户、组织、课程、Profile、Cookie、截图或原始页面/网络资料。"
    - "真实用户未给出明确脱敏恢复信号时本计划保持 human_needed，02-16 不得开始；失败、缺项或隐私疑虑同样阻断后续 live。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json"
      provides: "02-14 strict pass 时唯一存在的 machine gate；证明实际更新完成、paired UI ready、source config 均未确认且 school access 未开始"
      contains: "02-14-macos-update"
    - path: ".planning/phases/02-poc-live/02-14-MACOS-NATIVE-RECEIPT.json"
      provides: "当前实际安装build的macOS named S/I/N obligations；无L authority"
    - path: ".planning/phases/02-poc-live/02-14-SUMMARY.md"
      provides: "用户已实际更新到 02-13 不可覆盖 beta 的脱敏 hard-gate 前置证据；失败或 cleanup_pending 不可进入范围确认"
    - path: "apps/status/src/main.ts"
      provides: "仅 paired protected view 可达的来源与范围确认 UI，以及未确认时唯一的确认 CTA"
      contains: "确认来源与范围"
    - path: "apps/api/src/auth.ts"
      provides: "paired+same-origin+CSRF 的固定 configuration confirm route，以及后续仅引用批准配置/范围的控制面"
      contains: "/api/auth/configuration/confirm"
    - path: "packages/persistence/src/auth.ts"
      provides: "SourceConfigStore 对已确认来源/范围的持久化与 generation/fence 保护"
      contains: "SourceConfigStore"
    - path: ".planning/phases/02-poc-live/02-15-SUMMARY.md"
      provides: "仅含脱敏 checkpoint 结果与 02-16 是否仍被阻断的人工门禁记录"
  key_links:
    - from: "apps/status/src/main.ts"
      to: "POST /api/auth/configuration/confirm"
      via: "paired same-origin UI 逐字段构造 strict ApprovedSourceConfig；不从 URL/query/storage/聊天导入配置"
      pattern: "configuration/confirm|ApprovedSourceConfig"
    - from: "apps/api/src/auth.ts"
      to: "packages/application/src/policy.ts"
      via: "paired principal、same-origin、CSRF、permission 与 strict schema 先于任何 store/launcher/job 调用"
      pattern: "auth:configuration:write|confirmConfiguration"
    - from: "packages/application/src/policy.ts"
      to: "packages/persistence/src/auth.ts"
      via: "只有完整双来源+单课程+目的地确认后写入 approved config/scope；后续动作只使用 opaque IDs"
      pattern: "approvedConfigId|approvedScopeId|SourceConfigStore"
    - from: "paired local UI confirmation"
      to: ".planning/phases/02-poc-live/02-15-SUMMARY.md"
      via: "用户只返回 checkpoint 编号与脱敏 safe code；执行器不得抄录 protected 字段或合成确认"
      pattern: "02-15|confirmed|failed|human_needed"
---

<objective>
执行 Phase 2 首个 live 前的硬人工范围确认：在 02-14 已完成实际 macOS beta 更新后，由用户只在已配对本地管理页核对并确认两个官方来源、目标账户/组织、一个指定课程与允许目的地；确认前后均不自动访问学校，直到后续 02-16 的独立登录动作。

Purpose: 落实 D-02、D-05、D-07、D-13–D-15，在任何真实学校页面或 probe 可达前建立可审计且不泄露私人范围的用户授权边界，并阻断 T2-01 任意导航/操作扩大、T2-02 配置泄漏、T2-03 错误账户/范围绑定、T2-07 未配对控制与 T2-08 伪造人工证据。
Output: 一次真实用户 paired-UI scope confirmation 或明确的 failed/human_needed 阻断结果，以及只含脱敏信息的 02-15 SUMMARY；不修改产品源码、不登录、不运行 probe、不写 A/B/C/D/reauth L pass。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-10-SUMMARY.md
@.planning/phases/02-poc-live/02-11-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-14-MACOS-NATIVE-RECEIPT.json
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/ports.ts
@packages/application/src/policy.ts
@packages/persistence/src/auth.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts

<interfaces>
执行时以 02-14 strict pass 后才存在的 `.planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json` 为唯一机器门，并运行其只读 verifier；不得把 02-13 的发布成功或自动 availability receipt 当成用户已更新。receipt exact schema 如下；任何缺键、extra key、非 exact enum、hash/build mismatch 或非 pass 都阻断：

```typescript
interface Phase2UpdateGateReceipt {
  schema: 1;
  checkpoint: '02-14-macos-update';
  platform: 'macos';
  version: string; tag: string;
  artifactSha256: string; manifestSha256: string; buildId: string;
  result: 'pass'; resultCode: 'UPDATE_COMPLETE'; cleanup: 'complete';
  actualBuild: 'matched'; entrypoints: 'matched'; api: 'healthy'; worker: 'healthy';
  pairedUi: 'ready';
  sourceConfiguration: { moodle: 'not_confirmed'; edstem: 'not_confirmed' };
  schoolAccess: 'not_started';
  windows: 'not_run/human_needed'; phase3: 'blocked';
  observedAt: string; feedbackDigest: string;
}
```

只读 gate 命令固定为：

```bash
node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --read-only
```

随后直接消费 02-01/02-08 的最终导出，不重定义配置、范围、目的地或 receipt：

```typescript
export interface SourceConfigStore {
  read(source: SourceId): Promise<ApprovedSourceConfig | null>;
  confirm(config: ApprovedSourceConfig, context: WriteContext): Promise<void>;
}

// 唯一配置 mutation；必须是 paired browser principal + exact same-origin + CSRF。
POST /api/auth/configuration/confirm
body: { config: ApprovedSourceConfig }

// 配置确认之后的所有 source 操作只引用批准记录，不再接受导航目标。
export interface SourceProbeRequest {
  source: SourceId;
  action: SourceAction;
  approvedConfigId: string;
  approvedScopeId: string;
  // exact upstream fields only; no url/js/selector/browser handle/operation closure
}
```

`ApprovedSourceConfigSchema` 是唯一写入 schema：official origin 必须是规范化 HTTPS origin，拒绝 credentials/path/query/fragment、IP/localhost、source mismatch 和 extra keys。目标账户/组织、恰好一个课程与允许目的地必须由最终 upstream scope/destination 类型表达；若当前 beta 的 protected UI 或 strict schema 缺少任一项，本 checkpoint 必须 failed/human_needed 并回到修复→新不可覆盖 beta→重新更新，禁止在本计划新增自由字符串、在聊天收集值或用备注字段补洞。

02-08 固定 routes 不变：status/receipts 为只读，configuration/login/probe/logout/binding 是 paired mutations；没有任意 operation、URL 或 receipt-write route。配置确认不得调用 `OfficialLoginLauncher.open`、`AuthJobService.requestProbe`、`SourceProbePort.probe` 或 BrowserProvider；成功后的唯一下一步只是允许用户在 02-16 再明确点击 `打开 Moodle 官方登录窗口`。
</interfaces>

<hard_gates>
- 02-14 必须是真实用户更新成功，且当前安装 build/version/hash 与 02-13 发布记录一致、无 `cleanup_pending`；任何缺失、失败、旧 build 或仅发布未更新都保持 human_needed，不进入 Task 2。
- 用户只能在 paired local UI 查看/输入/确认实际 Moodle/EdStem origin、账户、组织、课程与目的地；不得在聊天、CLI、MCP、日志、截图、计划/SUMMARY 或诊断中请求、回显或保存这些实际值。
- 配置确认前以及确认 mutation 本身，school window open、login/probe job enqueue、BrowserProvider launch、source adapter call 和 school network request 必须全部为零。02-16 是独立人工登录门，不可被本计划合并或自动触发。
- 只有两个来源、同一目标账户/组织语境、恰好一个命名课程与允许目的地全部通过 strict validation 后才算 scope confirmed；部分成功必须回滚/保持不可用，不得让其中一个来源先访问学校。
- 用户不提供真实脱敏恢复信号时，执行器不得猜测、复用旧消息、根据 UI/API 状态自动代签、调用 generic auto-approve 或生成 pass。失败/取消/401/403/网络错误/隐私疑虑都保持 failed/human_needed 并阻断 02-16。
- 截图不是条件，不请求、不上传、不保存；Windows 仍 `not_run / human_needed`，本计划不写 A/B/C/D/reauth L cell，Phase 1/2 不整体完成且 Phase 3 持续 blocked。
</hard_gates>
</context>

<tasks>

<task type="auto">
  <name>02-15-1：只读核验更新前置、paired 配置门与零学校副作用</name>
  <read_first>AGENTS.md 全文，尤其 Execution after plan approval、Credentials and login、Source and content policy、Data integrity and verification；02-CONTEXT.md D-02、D-05、D-07、D-13–D-15、D-17–D-18；02-RESEARCH.md Fixed source configuration、API and UI、T2-01/T2-02/T2-03/T2-07/T2-08；02-UI-SPEC.md 未配置来源 state、PrivacyNotice、LoginActionPanel、Copywriting Contract；02-13-SUMMARY.md 与 02-14-SUMMARY.md 的真实 artifact/build/update receipt 和精确校验命令；02-08/02-09/02-11 SUMMARY；最终 ApprovedSourceConfigSchema、AuthControlApplication、registerAuthRoutes、SourceConfigStore 与 status UI。</read_first>
  <files>none（只读 preflight；不得修改源码、配置、runtime DB、Profile 或学校状态）</files>
  <action>先要求 `.planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json` 存在并通过 `<interfaces>` 的 exact schema，再执行给出的 `verify-phase2-update-gate.mjs ... --runtime current --read-only` 命令，校验当前 macOS 安装的 version/build/hash/signature/trust root 与 02-13 不可覆盖 beta 完全一致；同时强制 result/resultCode/cleanup/actualBuild/entrypoints/api/worker/pairedUi 为 pass contract、两个 `sourceConfiguration` 均为 `not_confirmed`、`schoolAccess='not_started'`、Windows 与 Phase 3 仍 blocked。命令输出只保留 build/safe result，不打印安装路径、Profile、PID、密钥或用户资料。receipt 不存在（包括 02-14 非 pass 的规定状态）、任一 mismatch、旧版本、不可验证签名、cleanup_pending、仅有 availability receipt、来源已确认、学校访问已开始或 02-14 未得到用户反馈时立即返回 `human_needed`，不得打开状态页之外的浏览器、不得继续 Task 2。

    对最终 production composition 做只读安全 preflight：运行既有 auth API/UI/integration 定向测试，确认 unconfigured state 的 protected UI 只显示 `尚未配置学校来源`、固定说明 `先确认 Moodle、EdStem 的官方地址、目标账户和一个指定课程；确认前不会打开学校页面。` 与唯一 primary CTA `确认来源与范围`；login/probe/binding/course actions 不存在或 disabled。配置 route 的 strict negative matrix 必须证明 public/pending/CLI/MCP/cross-origin/缺 CSRF/extra key/URL/path/query/多课程/未知目的地在 store/launcher/job/adapter 前拒绝。

    读取脱敏 runtime status/counters（不得 dump config/DB/DOM/network）：`school_page_open=0`、`official_login_launch=0`、`auth_probe_enqueued=0`、`course_probe_enqueued=0`、`source_request=0`。若现有安装已经有来源配置，必须由 paired UI 明确显示其状态并要求用户核对；不得从 DB/日志抄出值，也不得把历史配置视为本次确认。任何 preflight 发现学校请求、自动 probe、已有 live receipt、受保护字段出现在非 UI 输出或缺少四类确认字段，均为 High/Critical blocker，停止并要求修复/重发 beta，而不是临时放宽门禁。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --read-only &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-api.test.ts tests/integration/auth-security-matrix.test.ts &amp;&amp; npm run test:ui -- --grep "paired auth layout|auth actions|auth E2E local containment"</automated>
  </verify>
  <acceptance_criteria>
    1. `02-14-UPDATE-RECEIPT.json` 存在、通过 exact schema且 verifier 退出 0；version/build/hash/signature 与 02-13 manifest 一致，resultCode=`UPDATE_COMPLETE`、cleanup=`complete`、pairedUi=`ready`、两个 source config=`not_confirmed`、schoolAccess=`not_started`；输出无路径/PID/Profile/secret/private scope。
    2. 未配置/待重新确认页面只有 `确认来源与范围` 这一 primary CTA；不存在/不可激活登录、probe、绑定或课程动作，且 PrivacyNotice 在任何 protected scope 字段上方。
    3. public/pending/CLI/MCP/cross-origin/缺错 CSRF、credentials/path/query/fragment/IP/localhost/source mismatch、extra key、零/多课程、未知/多目的地逐项在 store/launcher/job/adapter 前失败。
    4. preflight 前后 `school_page_open`、`official_login_launch`、`auth_probe_enqueued`、`course_probe_enqueued`、`source_request` 均为 0；没有真实学校 DNS/HTTP、Playwright official window 或 Profile access。
    5. 若已有 config，preflight 不把它当本次用户确认、不打印实际字段；Task 2 仍要求 paired protected UI 核对全部五组信息。若 UI/schema 缺字段则明确 failed/human_needed，不增加自由格式 fallback。
    6. 自动化命令全部退出 0、无 skip/todo；不写 L/N/Windows receipt，不改变 runtime config，02-16 与 Phase 3 仍 blocked。
  </acceptance_criteria>
  <done>当前安装确为用户已更新的 02-13 beta，未确认范围门在真实控制面保持关闭且零学校副作用；只有真实 paired UI 人工确认仍待执行。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-15-2：用户只在 paired local UI 确认 live 来源与范围</name>
  <read_first>Task 1 的脱敏 preflight 结果；02-14-SUMMARY.md 的当前安装版本/打开 paired local UI 的精确步骤；02-UI-SPEC.md 的未配置来源、PrivacyNotice、LoginActionPanel 与 Copywriting Contract；D-02、D-05、D-07、D-13–D-15。不要读取或要求用户在聊天提供实际 origin、账户、组织、课程或目的地。</read_first>
  <files>none（人工动作只更新产品仓库外 runtime DB 中既有 02-08 scope/config 状态；不修改源码、PLAN、证据 ledger 或学校状态）</files>
  <what-built>已更新的 macOS 认证 POC beta 现处于“范围未确认、不会打开学校页面”的阻断态；paired local UI 提供唯一的范围确认动作，并把实际私人字段留在本机 protected view。</what-built>
  <action>执行器完成 Task 1 后必须暂停，原样向用户呈现 `<how-to-verify>` 与 `<resume-signal>`，不得代替用户打开/操作 protected configuration UI、从 DB/DOM/日志读取实际配置、把旧配置或 02-14 receipt 当作本次批准，也不得使用 generic auto-approve。用户回复后仅解析三种固定前缀 `02-15 confirmed|failed|human_needed` 和一个来自 02-08 最终 allowlist 的页面 safe code；消息包含 actual origin/account/organization/course/destination、姓名/邮箱、截图或日志时不得把这些内容写入 SUMMARY，应要求用户重新给纯脱敏信号。confirmed 只表示既有 02-08 `SourceConfigStore` 中 paired-UI scope/config 状态已由本轮用户确认；不新建独立机器 JSON、不写 L evidence cell、不打开学校页面，也不写 A/B/C/D/reauth pass。failed/human_needed 或无真实回复时保持阻断。</action>
  <how-to-verify>
    1. 按 02-14 已验证的本机步骤打开当前 beta 的 local status page；若出现配对提示，只在该本机页面完成配对。不要在聊天粘贴地址、配对 token、姓名、邮箱或截图。
    2. 确认页面显示 `尚未配置学校来源`（已有旧配置时显示“需要重新核对”）和固定说明 `先确认 Moodle、EdStem 的官方地址、目标账户和一个指定课程；确认前不会打开学校页面。`。此时不得出现学校窗口、自动 probe 进度或课程内容。
    3. 只在 PrivacyNotice 下方的 protected 配置区核对：Moodle official origin、EdStem official origin、目标账户与组织/租户、恰好一个明确命名的课程、一个允许目的地。确认 origin 只含 `https://host[:port]`，没有 credentials/path/query/fragment；课程是本次可见性 POC 的单一课程，不是全校/全学期；目的地符合你允许的本地/模型披露边界。
    4. 若任一值不确定、页面缺字段、出现多个课程/目的地、页面要求粘贴密码/MFA/Cookie、或你不愿授权，点击取消/停止并按失败恢复信号返回。不要猜测，也不要把值发给 Codex。
    5. 全部正确时点击唯一 primary CTA `确认来源与范围` 一次。确认动作应只保存 approved config/scope：不得打开 Moodle/EdStem、不得弹出登录窗口、不得开始 auth/course probe。页面应显示“配置已确认”及 02-08 最终 allowlist 中实际返回的脱敏 exact safe code，并把下一步切换为 `打开 Moodle 官方登录窗口`；不要在本计划点击下一步，也不要由本计划发明新的结果码。
    6. 检查页面确认结果只显示 allowlisted safe code 与不含私人值的下一步；不要复制 protected 字段。若任何结果、日志/诊断或复制控件会带出 origins、姓名/邮箱、账户/组织、课程名、目的地详情、Profile/PID/Cookie、URL path、截图或 DOM/network 资料，停止并按失败恢复信号报告 `PRIVACY_OUTPUT_BLOCKED`。
    7. 返回聊天时只发送下面规定的脱敏 checkpoint 响应。不要附解释性私人资料、实际配置、截图、页面文本 dump 或日志；本步骤不会创建新的 JSON receipt 或 L evidence cell。
  </how-to-verify>
  <acceptance_criteria>
    1. 用户本人在当前已配对 local UI 中看到并核对两个 official origins、目标账户/组织、恰好一个课程及允许目的地；没有通过聊天/CLI/MCP/日志提供这些值。
    2. 用户明确点击 `确认来源与范围`；执行器没有自动点击、复用旧批准、根据已有 DB 推断或合成确认。
    3. 确认前及 confirmation request 本身没有学校窗口、Playwright launch、auth/course job、adapter call 或学校网络；确认后的 next action 仍等待 02-16 独立点击。
    4. 页面只接受 canonical official origins 和封闭 scope/destination；随后的 operation 只显示/使用 approvedConfigId/approvedScopeId 语义，没有逐操作 URL/JS/selector/browser handle 输入。
    5. 用户返回的消息仅为规定格式的 checkpoint+safe code；SUMMARY/chat/log 不出现 actual origin/account/organization/course/destination 或任何截图/凭据/Profile/页面网络资料。
    6. 任一失败、取消、缺字段、401/403、网络错误或隐私问题保持 `failed/human_needed` 并阻断 02-16；只有明确 confirmed 恢复信号可继续。
  </acceptance_criteria>
  <verify>
    <automated>node scripts/dev/runtime.mjs node scripts/release/verify-phase2-update-gate.mjs --receipt .planning/phases/02-poc-live/02-14-UPDATE-RECEIPT.json --artifacts release/phase2-beta-artifacts.json --availability release/phase2-availability.json --runtime current --read-only</automated>
  </verify>
  <resume-signal>成功时仅回复 `02-15 confirmed &lt;页面显示的 allowlisted exact safe code&gt;`；失败/取消时仅回复 `02-15 failed &lt;页面脱敏错误码&gt;` 或 `02-15 human_needed &lt;脱敏缺口码&gt;`。不要发送 origin、账户、组织、课程、目的地详情、姓名、邮箱、截图或日志。执行器必须等待用户真实回复，绝不能代填或发明 safe code。</resume-signal>
  <done>只有用户本轮真实脱敏 confirmed 回复且 02-08 persisted scope/config 状态可供 02-16 重新读取时，此 checkpoint 才完成；不创建新 scope receipt/L cell、不访问学校。其他结果均保持 02-16 blocked。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | 不可信输入/风险 | 强制控制 |
|---|---|---|
| 02-14 update receipt → 02-15 live gate | 发布成功、旧 build 或自动 availability 被冒充为用户已更新 | 只接受 02-14 真实 user-action receipt；重验 installed version/build/hash/signature/cleanup；失败保持 human_needed |
| browser → paired local UI/API | 未配对、跨源、CSRF、旧/撤销 session 伪造配置确认 | HttpOnly paired session、exact same-origin/Host/fetch metadata、CSRF、细粒度 `auth:configuration:write`、401/403 purge |
| protected form → ApprovedSourceConfig/scope policy | 任意 URL、路径、多个课程、未知目的地或自由字段扩大权限 | strict canonical origin schema；封闭 source/account/org/single-course/destination；extra keys fail；普通 operation 仅 opaque IDs |
| config confirmation → BrowserProvider/job/source network | 确认动作被错误接成自动登录或 probe，造成未授权学校访问 | configuration route 只能写 config/scope；launcher/job/adapter/network 调用计数为零；02-16 独立 human gate |
| protected config/UI → chat/log/SUMMARY | official origins、完整身份、组织、课程、目的地或页面资料泄漏 | protected visible text only；redacted allowlist receipt；不截图/不复制 protected values；user resume signal 固定 safe code |
| user checkpoint → executor/evidence | 自动化或执行器伪造确认、旧回复重放、confirmation 被误写成 A/live pass | blocking human-action；必须等本轮真实回复；scope gate 与 A/B/C/D/reauth ledger 分离；无自动 L append |

## STRIDE / ASVS L1 威胁登记

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 控制意图 | 具体缓解与硬阻断 |
|---|---|---:|---|---|---|---|
| T2-01 | Spoofing / Tampering / Elevation of Privilege | Critical | config form/schema、fixed route、source admission | mitigate | V1 架构边界、V4 功能授权、V5 输入验证、V13 API/业务逻辑 | canonical HTTPS origins、单课程/目的地封闭范围、paired-only confirm、operation 只用 approved IDs、确认零 source calls。任意 URL/JS/selector/handle/多课程/未知目的地可到 driver，或确认触发学校请求，即 Critical blocker。 |
| T2-02 | Information Disclosure | Critical | protected UI、receipt、chat/log/SUMMARY | mitigate | V8 敏感数据保护、V13 响应 allowlist、V16 安全日志 | 实际配置只在 paired protected view；结果单只含 build/platform/time/safe code/gap；无截图、DOM/network dump。任一非 protected 输出出现 origin/identity/org/course/destination/Profile/Cookie 即 Critical blocker。 |
| T2-03 | Spoofing / Tampering | High | target account/org/course scope | mitigate | V2 身份语境、V4 对象级授权、V5 语义验证 | 用户本人核对 account+organization+one course；配置确认不等于 binding confirm；02-17 仍单独核对真实双来源身份。缺失/冲突/多课程均 human_needed；不得仅凭显示名/邮箱继续。 |
| T2-07 | Spoofing / Elevation of Privilege / Information Disclosure | High | pairing/session/CSRF、configuration mutation | mitigate | V2 会话、V4 权限、V5 CSRF、V14 安全配置 | current paired cookie session+exact same-origin+CSRF；CLI/MCP/public/pending deny；401/403 清 protected state。非 paired 可读/写 protected config 即 High blocker。 |
| T2-08 | Repudiation / Tampering | High | human checkpoint、SUMMARY、后续 live gate | mitigate | V1 证据等级分离、V4 writer authority、V16 审计 provenance | blocking human-action、固定脱敏恢复信号、不得 auto-approve/复用旧消息；scope confirmation 不写 A/B/C/D/reauth pass，Windows/Phase3 不变。任一合成确认或跨格证据即 High blocker。 |

ASVS 5.0 L1 仅表示本计划适用的控制意图，不声明完整合规。所有 High/Critical 条目都是 02-16 前硬阻断：不得降级为 warning、通过隐藏 UI 代替 server authorization、用发布/测试成功代替用户确认，或为方便 live 临时增加任意 URL/自由范围入口。
</threat_model>

<verification>
1. 运行 Task 1 的 current-install verifier 与定向 integration/UI suites；若 02-14 receipt、installed build/hash/signature、cleanup 或任一自动 negative case失败，停止在 Task 1，不呈现 Task 2。
2. checkpoint 前确认 runtime counters 对 school window/login launch/auth job/course job/source request 全为 0；检查过程不得打印实际配置或访问学校。
3. Task 2 必须由用户在 paired local UI 亲自完成；执行器不读取 DOM/DB 提取实际值、不远程点击、不接受聊天中的实际范围、不综合旧批准。
4. 用户确认后仅接受固定格式的脱敏恢复信号；对消息做敏感字段检查，若包含实际 origin/账户/组织/课程/目的地/截图/日志则不复制进 SUMMARY，并要求用户撤回/重新给脱敏 code。
5. 成功只解除 02-16 的“范围未确认”前置，不打开登录、不写 A/B/C/D/reauth pass；失败/human_needed 保持 02-16 blocked。
6. Windows 全部 `not_run / human_needed`，Phase 1/2 未整体完成，Phase 3 `blocked`；macOS scope confirmation 不代替 Windows 或任何 live auth结果。
</verification>

<success_criteria>
- AUTH-01/D-02：用户只在 paired local UI 预先确认双来源与范围；确认之前及确认动作本身没有学校页面、凭据/MFA请求或 auth probe，真实 Moodle 登录仍等待 02-16。
- AUTH-03/D-07：目标账户/组织与一个课程得到明确人工范围确认，但不把范围确认冒充双来源 identity binding；实际身份/候选绑定仍由 02-17 单独核对。
- SEC-02/D-13：仅两个 canonical official origins、固定四项 adapter action、一个批准课程可见性范围与一个允许目的地可达；普通 operation 无任意 URL/JS/selector/handle，课程正文/帖子/文件/成绩不可达。
- D-05/D-14/D-15：actual origins、账户、组织、课程和目的地只在 protected local UI；chat/log/SUMMARY 只有 checkpoint/build/platform/time/safe code/gap，无截图或敏感/raw资料。
- UAT-01/T2-08：只有真实用户的本轮 blocking checkpoint 回复可解除 02-16 前置；没有自动确认、synthetic/native代填、旧 receipt 重放或 A/B/C/D/reauth pass。
- 任一 ASVS L1 High/Critical blocker、缺字段、partial confirm、401/403、隐私输出或用户失败均保持 failed/human_needed；Windows 和 Phase 3 gate 完全不变。
</success_criteria>

<output>
用户真实回复后才创建 `.planning/phases/02-poc-live/02-15-SUMMARY.md`。SUMMARY 只记录：checkpoint=`02-15`、当前 beta version/build、platform=`macos`、确认时间、用户返回的 allowlisted safe code、结果 `confirmed|failed|human_needed`、02-16 是否仍 blocked，以及 Windows/Phase3 缺口。不得记录 actual Moodle/EdStem origin、账户、组织、姓名、邮箱、课程、目的地详情、Profile/PID/Cookie/session/CSRF、页面/网络、截图或日志；不得写 A/B/C/D/reauth L pass。未收到用户真实恢复信号时不得创建成功 SUMMARY 或推进依赖。
</output>
