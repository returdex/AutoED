---
phase: 02-poc-live
plan: "21"
type: execute
wave: 21
depends_on:
  - "02-20"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "只有 02-20 B3 的 Moodle/EdStem `B3_codex_exit` macOS/L exact cells 与完成 SUMMARY 都 strict pass，且 current runtime/build/config/binding 未漂移，才向用户呈现 C；B3 receipt、B1/B2 结果或 S/I/N 证据不能代替 C。"
    - "用户本人按当前已签名 beta 的安装提示/support matrix 与 02-20 SUMMARY 中的已发布步骤完整重启 macOS；退出 Codex、重启 Worker、注销、睡眠、重启浏览器或自动脚本不能冒充 OS reboot。"
    - "C 开始前由 paired server 创建新的、一次性 `C_os_restart` pending-action bundle；它绑定 pre-reboot current build/config/binding/runtime generation，并且只能在实际 boot/session identity 已变化后由 post-reboot paired workflow 消费，repo gate、聊天、CLI flags 或用户反馈均不能生成 L authority。"
    - "重启后先证明受管 Chromium/Profile 的单一所有权与本安装关系；运行中、未知、PID reuse、exact executable/control proof 不明或陈旧 holder 均为 `PROFILE_IN_USE`/`PROFILE_OWNERSHIP_UNCONFIRMED`、`human_needed`，不得删锁、抢占、强杀或触碰日常/无归属浏览器。"
    - "重启后分别以当前 approved config/scope 和固定 probe 复核 Moodle、EdStem 的 auth、已确认账户绑定、授权范围、共享 Profile 观察与最小状态；不读取课程正文、帖子、文件、成绩，不接受任意 URL/selector/JS/API fallback，也不在 C 内执行 reauth。"
    - "C strict pass 必须恰有 Moodle 与 EdStem 两个 `C_os_restart`、macOS、L、current、未被 correction 否定的 pass cells；二者绑定同一个新 boot identity/current generation/build/config/binding，并且 ownership proven、scope matched、minimal status pass、`sensitive_output=none`、resultCode=`C_OS_RESTART_COMPLETE`。"
    - "任何 ownership unknown、身份/组织/binding/scope 漂移、来源 failed/human_needed、需要 MFA/reauth、最小状态不确定、敏感输出、缺 cell 或 receipt 污染都立即阻止 02-22 D；不得继续探测、自动批准或用 B/S/I/N/另一平台结果补格。"
    - "macOS C pass 仍不完成共享 Profile 假设或 Phase 2；Windows 保持 `not_run/human_needed`，Phase 3 保持 blocked。"
  artifacts:
    - path: ".planning/phases/02-poc-live/02-20-SUMMARY.md"
      provides: "B3 完成、当前已发布 beta/build、真实 Codex 完全退出/重新进入步骤与 C 使用的已发布 reboot/recovery 步骤来源；非 strict pass 时 C 不可开始"
    - path: "packages/persistence/src/auth.ts"
      provides: "append-only EvidenceLedger 与 durable paired pending-action store；保存精确 macOS/source/scenario/L 结果并阻止跨格替代"
      contains: "EvidenceLedger"
    - path: "packages/platform/src/profile-coordinator.ts"
      provides: "installation/nonce/PID/OS start identity/exact executable/authenticated control/generation/fence 的 single-owner 与 actual-exit 证明"
      contains: "ProfileOwnershipCoordinator"
    - path: "apps/status/src/main.ts"
      provides: "paired protected UI 中唯一的 C human action、双来源本地完整身份/范围核对、所有权状态和脱敏结果单"
      contains: "CheckpointLedger"
    - path: "scripts/release/phase2-live-gate.mjs"
      provides: "只读验证 B3/C exact runtime ledger cells 与 current runtime 的 canonical gate；不 append L、不持有 pending-action authority"
      contains: "verifyPhase2LiveCheckpoint"
    - path: ".planning/phases/02-poc-live/02-21-SUMMARY.md"
      provides: "仅 strict pass 后生成的 C 脱敏编排门，记录版本/build、macOS、双来源 safe codes、时间、binding/scope/ownership/gap 与 D eligibility"
  key_links:
    - from: ".planning/phases/02-poc-live/02-20-SUMMARY.md"
      to: "scripts/release/phase2-live-gate.mjs"
      via: "C preflight 同时要求完成 SUMMARY 与 `--verify --checkpoint B3 --runtime current --platform macos --read-only` 成功，防止只凭文本或旧 B result 进入 C"
      pattern: "B3_codex_exit|B3|read-only"
    - from: "apps/status/src/main.ts"
      to: "packages/persistence/src/auth.ts"
      via: "用户在 reboot 前从 paired UI 明确开始 C，server mint durable、single-use、build/config/binding/generation 绑定的 `C_os_restart` bundle；public API/repo tool 不接收自造 authority"
      pattern: "C_os_restart|pending-action|human_action"
    - from: "post-reboot paired workflow"
      to: "packages/platform/src/profile-coordinator.ts"
      via: "先证明新 boot/session identity、current runtime generation 与 exact Profile owner，再允许任何双来源 probe；未知/运行中 fail closed"
      pattern: "PROFILE_IN_USE|PROFILE_OWNERSHIP_UNCONFIRMED|generation|fence"
    - from: "post-reboot paired workflow"
      to: "packages/persistence/src/auth.ts"
      via: "只有新的 boot identity、当前 paired session 与真实 fixed-probe result 同时成立时，原子 consume C bundle 并 append Moodle/EdStem exact macOS/L cells"
      pattern: "C_os_restart|EvidenceLedger|actionReceiptId"
    - from: "scripts/release/phase2-live-gate.mjs"
      to: "02-22 eligibility"
      via: "`--verify --checkpoint C --runtime current --platform macos --read-only` 对两个 current/non-corrected pass cells、同 boot/generation/build/config/binding、ownership/scope/minimal status/privacy 做 conjunction"
      pattern: "C_OS_RESTART_COMPLETE|C_os_restart|phase3"
---

<objective>
执行 macOS 检查点 C 的不可自动跨越完整系统重启门：用户本人按当前已签名 beta 的已发布步骤完整重启 macOS，随后在 paired protected UI 复核专属 Profile 所有权、Moodle/EdStem 认证、账户绑定、授权范围、共享 Profile 与最小状态，并生成独立于 B 的两个来源 C live cells。

Purpose: 落实 D-03、D-09、D-12、D-14 与 AUTH-02/AUTH-03/AUTH-04/SEC-02/UAT-01，证明同一专属 Profile 与双来源身份在真实 OS reboot、runtime generation 变化及进程重建后仍安全恢复；任何未知所有权、身份漂移或隐私失败立即阻断 D。
Output: 一个 blocking `checkpoint:human-action`、Moodle/EdStem 各自的 `C_os_restart` macOS/L exact result、strict read-only C gate 与仅在通过后创建的 02-21 SUMMARY；不新增 planning receipt JSON、不修改源码、不执行 D/reauth/Windows。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@.planning/phases/02-poc-live/02-08-SUMMARY.md
@.planning/phases/02-poc-live/02-09-SUMMARY.md
@.planning/phases/02-poc-live/02-12-SUMMARY.md
@.planning/phases/02-poc-live/02-13-SUMMARY.md
@.planning/phases/02-poc-live/02-14-SUMMARY.md
@.planning/phases/02-poc-live/02-15-SUMMARY.md
@.planning/phases/02-poc-live/02-16-SUMMARY.md
@.planning/phases/02-poc-live/02-17-SUMMARY.md
@.planning/phases/02-poc-live/02-18-SUMMARY.md
@.planning/phases/02-poc-live/02-19-SUMMARY.md
@.planning/phases/02-poc-live/02-20-PLAN.md
@.planning/phases/02-poc-live/02-20-SUMMARY.md
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/contracts/src/presentation.ts
@packages/application/src/auth.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts
@packages/platform/src/profile-coordinator.ts
@packages/platform/src/browser-provider.ts
@apps/api/src/auth.ts
@apps/status/src/main.ts
@scripts/release/phase2-live-gate.mjs

<interfaces>
执行时以 02-20 SUMMARY 和已发布 beta 的真实导出为准；只能接受纯机械 import 名称差异，不能更改下列 canonical scenario、writer authority、read-only 命令或 pass conjunction：

```typescript
export type UatScenario =
  | 'A1_login'
  | 'A2_binding'
  | 'A2_course_visibility'
  | 'B1_reopen_1'
  | 'B1_reopen_2'
  | 'B1_reopen_3'
  | 'B2_worker_restart'
  | 'B3_codex_exit'
  | 'C_os_restart'
  | 'D_24h_recheck'
  | 'reauth';
```

只有 paired server workflow 能 mint/consume C live authority；repo tool、客户端和用户均不能传入或伪造。bundle 的内部字段名必须直接复用 02-20 SUMMARY/已发布 beta 的既有 pending-action contract，不在本计划另造 type；语义上必须绑定 source、`C_os_restart`、checkpoint C、macOS/L、current build/config/scope/binding、pre-reboot runtime generation 与 opaque boot identity、paired human workflow provenance 和 expiry，并在 post-reboot 新 boot/current generation 下单次消费。

Canonical preflight：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform macos --read-only
```

Canonical post-reboot strict gate：

```text
node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint C --runtime current --platform macos --read-only
```

post-reboot verifier 只读 current redacted status 与 exact ledger cells；strict pass 固定为 `C_OS_RESTART_COMPLETE`，要求 Moodle/EdStem 两个 `C_os_restart` macOS/L current non-corrected pass cells，且二者绑定同一新 boot identity/current runtime generation/build/config/binding，并证明 Profile ownership、scope matched、minimal status pass、`sensitive_output=none`、Windows `not_run/human_needed`、Phase 3 blocked。它不得 import persistence、直读 DB、append/correct receipt、启动/关闭浏览器、重启服务或读取 Profile。
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>02-21-1：用户完整重启 macOS 并完成独立 C 双来源复查</name>
  <files>none（用户动作只影响仓库外当前 beta/runtime、专属 Profile observation、paired pending-action 与既有 append-only EvidenceLedger；不修改源码、学校资料或 planning receipt JSON）</files>
  <read_first>AGENTS.md 的 Execution after plan approval、Credentials and login、Source and content policy、Data integrity and verification；02-CONTEXT.md D-03、D-09、D-12、D-14、D-17–D-18；02-20-SUMMARY.md 的 strict B3 pass、current beta/build、实际 Codex 生命周期结果与已发布 reboot/recovery 步骤来源；当前 signed beta install prompt/support matrix；02-03/02-05/02-07/02-12 SUMMARY 的 owner/actual-exit/generation/fence/managed lifecycle；02-08/02-09 SUMMARY 的 protected status与隐私投影；最终 `UatScenario`、EvidenceLedger、pending-action 与 `phase2-live-gate` contract。禁止读取 legacy Profile、真实 Cookie、凭据、raw DB/WAL、私人页面、浏览器输入、原始网络资料或学校课程内容。</read_first>
  <what-built>当前已发布 beta 的 macOS A、B1、B2、B3 已各自 strict pass；C、D、reauth 与全部 Windows 单元仍为 `not_run/human_needed`，共享 Profile 与 Phase 2/3 尚未通过。</what-built>
  <action>
先确认 `.planning/phases/02-poc-live/02-20-SUMMARY.md` 是本次 current build 的完成 SUMMARY，并运行 canonical B3 preflight；缺 SUMMARY、verifier nonzero、B3 cell 非 current/non-corrected pass、build/config/binding/generation 漂移、C 已有 pass 或任何隐私命中都返回 `human_needed`，不呈现 reboot 指令。禁止用旧 B3 handoff、自检 spawn、聊天反馈或 S/I/N 结果绕过。

preflight 通过后，只在 paired protected local UI 让用户明确选择“开始 C：完整重启 macOS”。由 published beta 的 paired server 为 Moodle/EdStem mint 一套新的 durable one-time C pending-action bundle，精确绑定 `checkpoint=C`、各 source、`scenario=C_os_restart`、macOS/L、current build/config/scope/binding、pre-reboot runtime generation/boot identity、paired session provenance 与 expiry；不得让用户/CLI/API 输入 actionReceiptId，不得由 repo gate append L。若 beta 无该能力、bundle 不能安全跨 reboot、C cells 非空或 pre-reboot ownership/current status 不确定，停止为 `human_needed`，回到修复→完整测试→不可覆盖新 beta→实际更新，不能现场补 writer 或放宽 gate。

随后向用户逐字呈现当前 signed beta install prompt/support matrix 与 02-20 SUMMARY 指向的完整 macOS reboot/recovery 步骤、expected version/build 和预期 protected UI 状态；不得凭经验编造路径、launchctl label、端口或命令。硬暂停，等待用户本人执行完整 macOS restart。执行器、产品、脚本不得代为调用 reboot/shutdown；退出 Codex、重启 Worker、注销、睡眠、重启 Finder/WindowServer/浏览器均不算 C。

用户重启并按已发布步骤重新进入后，先由产品证明 boot/session identity 与 pre-reboot 不同、current runtime generation 已更新、旧 generation/owner/fence 无请求或提交能力、实际 client/beta build 匹配。再复核 Profile ownership：只有完整 installation+nonce+PID+OS start identity+exact executable+authenticated control/fence 证明成立的 current owner，或 OS 已确认退出后由本安装正常恢复，才可继续；running/unknown/PID reuse/mismatch 均显示 `PROFILE_IN_USE`/`PROFILE_OWNERSHIP_UNCONFIRMED` 与 `human_needed`，保留记录，不 kill、pkill、killall、删除 Chromium lock、启动第二实例或触碰日常浏览器。

所有权通过后，用户仅在 paired protected UI 本地核对 Moodle/EdStem 的当前完整身份、组织/绑定和已批准范围；完整值不得复制到聊天。产品只从 persisted confirmed config/scope 内部构造固定 background auth/minimal-status probes，逐跳限制 official origins，并确认两来源共享 Profile observation。只判断 auth、binding consistency、approved scope 与一个已批准课程的最小可见性/状态；不读正文、帖子、附件、文件、成绩/反馈，不 download/POST/form/quiz/upload，不接受任意 URL/selector/JS/browser handle 或未授权 API fallback。需要登录/MFA/额外交互时本 C 返回 `human_needed`，不得在 C 中顺带完成 reauth。

只有 post-reboot actual observation、用户本轮明确确认与新 C bundle 的 source/checkpoint/build/config/scope/binding/boot/generation 全部匹配时，paired server 才能原子消费 bundle child 并 append Moodle/EdStem 各一个 immutable `C_os_restart` macOS/L result；failed/human_needed 也必须如实保留，不能覆盖或用 correction 伪造成首次 pass。用户反馈不是 writer authority，repo gate保持只读。随后运行 canonical C verifier；只有 exit 0 且 resultCode=`C_OS_RESTART_COMPLETE`、两个 exact cells current/non-corrected pass、ownership proven、binding/scope matched、minimal status pass、`sensitive_output=none` 才完成本计划。任何其他结果立即阻断 02-22 D，不继续探测或自动重试。
  </action>
  <how_to_verify>
    1. 在 reboot 前确认 protected UI 总体门禁只显示 A/B pass、C 等待本人动作、D/reauth 未开始；Windows 为 `not_run/human_needed`、Phase 3 blocked。
    2. 在 paired UI 点击唯一的“开始 C：完整重启 macOS”，确认 UI 显示 pending 且没有展示 Profile path、PID、boot identity、action ID、Cookie、完整身份、课程名、request path 或 selector。
    3. 严格按 UI 引用的当前 signed beta 已发布步骤完整重启 macOS；不要只退出 Codex、注销、睡眠或只重启 Worker/browser。
    4. 重启后按同一发布步骤恢复/打开 current paired local UI；若需要重新 pairing，只在本地按正式流程完成，不向聊天提供 token、端口、路径或身份。
    5. 等待 UI 报告新 OS boot 已确认、current beta/build matched、Profile ownership proven。若为 in_use/unconfirmed、旧 runtime、cleanup_pending 或 unknown，选择真实 failed/human_needed 并停止，不删除锁或终止进程。
    6. 在 protected UI 本地逐项核对 Moodle 与 EdStem 都 authenticated、完整身份/组织仍与 A2 confirmed binding 一致、approved scope matched、shared Profile observed、批准课程的最小 visibility/status pass；不打开或阅读课程内容。
    7. 检查 C 脱敏结果单只包含 version/build、platform=macos、source、scenario、time、safe result code、binding/scope/ownership一致性和 gap；若出现完整姓名/邮箱/课程、origin path、Profile/PID/Cookie、DOM/selector/request、action ID、日志、截图或原始错误，标记 `sensitive_output=detected` 并停止，不把敏感值粘贴回来。
    8. 只复制 `<resume-signal>` 规定的 fixed redacted block。任一项非 pass/confirmed/matched/none 时如实返回 failed/human_needed；不得写“approved”或根据 UI 外观推断 pass。
  </how_to_verify>
  <verify>
    <automated>checkpoint 前运行 `node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint B3 --runtime current --platform macos --read-only`；用户返回 fixed redacted feedback 且 paired server 已提交本轮 actual result 后，运行 `node scripts/dev/runtime.mjs node scripts/release/phase2-live-gate.mjs --verify --checkpoint C --runtime current --platform macos --read-only`。只有后者 exit 0 且输出 strict safe code `C_OS_RESTART_COMPLETE` 才通过；工具必须保持 read-only、无学校访问/浏览器启动/ledger append。</automated>
  </verify>
  <acceptance_criteria>
    1. B3 canonical verifier 与 02-20 completed SUMMARY 同时匹配 current build/runtime；C 开始前 exact C cells 无 pass，B3/B1/B2/S/I/N 不可替代 C。
    2. pre-reboot paired server 创建的是新的、durable、一次性 C bundle；两个 source child 均绑定 `C_os_restart`、macOS/L、current build/config/scope/binding 与 pre-reboot boot/generation，repo/用户无 writer authority。
    3. 用户明确反馈本人执行的是完整 macOS restart；post-reboot proof 确认 boot/session identity 已变化、current runtime generation/build匹配、旧 Worker/owner 无请求或提交能力。
    4. Profile ownership 由完整 OS+authenticated control proof 确认；running/unknown/PID reuse/mismatch路径只返回 profile_in_use/ownership_unconfirmed/human_needed，kill/删锁/第二实例调用为零。
    5. Moodle 与 EdStem 分别完成 fixed official-origin auth/minimal-status复核；identity/binding/scope一致、shared Profile observed、仅一个 approved course 的最小 visibility/status pass，course body/posts/files/grades/download/write/API fallback为零。
    6. EvidenceLedger 恰有两个 current non-corrected `C_os_restart` macOS/L pass cells，绑定同一新 boot identity/current generation/build/config/binding；missing/failed/human_needed/另一 scenario/source/platform/evidence不可代填。
    7. canonical C verifier exit 0、resultCode=`C_OS_RESTART_COMPLETE`、ownership proven、scope matched、minimal status pass、`sensitive_output=none`；用户 fixed feedback 与 runtime receipt一致。
    8. 未请求/捕获/保存 screenshot、credential/input、Cookie、Profile path、PID/boot identity、完整身份/课程、DOM/selector/request path/raw network/log；任何泄漏为 blocker而非可忽略告警。
    9. 02-22 只在上述全部 strict pass 后 eligible；D/reauth仍未执行，Windows仍 `not_run/human_needed`，共享 Profile未最终采用，Phase 1/2未整体完成、Phase 3 blocked。
  </acceptance_criteria>
  <done>真实用户按已发布步骤完整重启 macOS；新的 C authority 在新 boot/current generation 下产生双来源独立 `C_os_restart` macOS/L strict pass，ownership、identity/binding/scope、minimal status与隐私全部通过后，才允许 02-22 D；否则 C/D 保持 blocked。</done>
  <resume-signal>只粘贴 protected UI 生成的完整 `02-21_C_RESULT` 脱敏块：`result=pass|failed|human_needed`、`result_code=C_OS_RESTART_COMPLETE|&lt;allowlisted safe code&gt;`、`os_restart=confirmed|not_observed`、`actual_build=matched|mismatch|not_observed`、`profile_ownership=proven|in_use|unconfirmed|not_observed`、`moodle=pass|failed|human_needed`、`moodle_code=&lt;allowlisted safe code&gt;`、`edstem=pass|failed|human_needed`、`edstem_code=&lt;allowlisted safe code&gt;`、`binding_scope=consistent|mismatch|not_observed`、`shared_profile=observed|not_observed`、`minimal_status=pass|failed|human_needed`、`sensitive_output=none|detected|not_observed`、`observed_at=&lt;ISO-8601 with timezone&gt;`、`windows=not_run/human_needed`、`phase3=blocked`。不要发送姓名、邮箱、组织、课程、origin、Profile/PID/boot identity/Cookie、action ID、截图、日志或原始错误。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required control |
|---|---|---|
| B3 gate/SUMMARY → C eligibility | 仓库侧文字与 runtime exact cells 共同决定是否可进入 reboot | completed SUMMARY + canonical B3 read-only verifier；缺一即停，B/S/I/N 不代填 C |
| paired UI → pre-reboot server authority | 用户动作请求 durable C correlation | paired principal + same-origin CSRF；server mint one-time source children；客户端不提交 authority |
| pre-reboot runtime → post-reboot runtime | boot/session、进程与 generation 全部改变 | 证明真实 boot identity change、current build/generation；旧 owner/fence/request/commit失效 |
| OS process state → Profile coordinator | PID、OS start、executable/control proof 决定能否恢复专属 Profile | 完整 exact proof；lease只 fencing；running/unknown/PID reuse fail closed且绝不 broad kill/删锁 |
| untrusted school pages → sealed probes | Moodle/EdStem 页面、redirect 与 marker 都是不可信数据 | persisted approved origin/config/scope、逐跳 origin guard、bounded visible marker、fixed read-only action |
| protected local UI → user/chat | 完整身份/课程只可本地显示，反馈跨越到聊天/规划资料 | fixed redacted block；sensitive-output scan；401/403 purge；不截图/不复制隐私 |
| runtime EvidenceLedger → repo verifier/D gate | C cells 决定 D eligibility | server-only append、append-only exact coordinates、current/non-corrected conjunction；repo verifier read-only |

## ASVS L1 Security Intent and STRIDE Register

| Threat ID | STRIDE | Severity | Component / boundary | Disposition | ASVS L1 intent | Mitigation and blocking test |
|---|---|---:|---|---|---|---|
| T2-01 | Tampering / Elevation of Privilege | Critical | school page → source adapter | mitigate | V5 input validation、V12 file/resource、V13 API | 只用 persisted confirmed config/scope 与 sealed action；逐跳 official origin；任意 URL/JS/selector、popup/download、POST/form/quiz/upload、未授权 API fallback 必须 fail closed。任何越界请求为 Critical blocker。 |
| T2-02 | Information Disclosure | Critical | Profile/browser/protected UI → feedback/gate | mitigate | V7 error/logging、V8 data protection、V14 configuration | 不读取/输出 Cookie、input、Profile、DOM/network、完整身份/课程；fixed redaction + sensitive scan；检测到任何敏感输出即 C failed 并阻断 D，不得接受“已遮蔽大部分”。 |
| T2-03 | Spoofing / Tampering | High | post-reboot source identity → confirmed binding/scope | mitigate | V2 authentication、V4 access control、V11 business logic | 双来源 positive marker、current protected identity、本地人工核对、binding/scope exact match；identity/organization/scope drift 立即停止全部 probe并返回 failed/human_needed。 |
| T2-04 | Spoofing / Tampering / DoS / Elevation of Privilege | Critical | OS process state → Profile ownership | mitigate | V1 architecture、V2 authentication、V4 access control、V6 cryptographic verification、V14 configuration | installation+nonce+PID+OS start+exact executable+authenticated control+generation/fence 全证明；lease不等于exit；unknown/running/PID reuse不得回收、kill或删锁。误抢占/误杀是 Critical blocker。 |
| T2-05 | Tampering / Repudiation | High | pre-reboot Worker/owner → post-reboot requests/commits | mitigate | V4 access control、V13 state management | boot/generation/fence切换后旧 Worker/owner前后置 guard 均失败；post-reboot cells必须绑定current generation；任何迟到请求/提交为 High blocker。 |
| T2-06 | Tampering / Repudiation | High | stale status → C result | mitigate | V9 communications、V11 business logic、V13 state management | auth/capability/health/freshness/completeness分离；failed probe不删除last-success，但 C 只接受本轮 current actual result，stale snapshot/HTTP 200/URL不可作 pass。 |
| T2-07 | Spoofing / Elevation of Privilege | High | reboot后 browser/public client → paired mutation/protected identity | mitigate | V2 authentication、V3 session、V4 access control、V13 API | reboot后重新证明 paired local session与same-origin CSRF；401/403立即清 protected DOM；public/CLI/MCP无 C mutation、完整身份或 writer authority。绕过 pairing 为 High blocker。 |
| T2-08 | Tampering / Repudiation / Elevation of Privilege | Critical | B/C/OS/evidence coordinates → D gate | mitigate | V1 architecture、V4 access control、V13 state management、V16 provenance | 新 C bundle + 两个 exact `C_os_restart` macOS/L current non-corrected cells +同boot/generation/build/config/binding conjunction；B、S/I/N、Windows或单来源不可替代。repo gate read-only。跨格/自动批准为 Critical blocker。 |

所有 High/Critical 项都必须 `mitigate`，不得 accept、transfer、降级为后续债务或以用户口头描述覆盖。表中 ASVS 5.0 L1 仅表示本计划适用控制意图，不声明完整合规。任一 blocker、missing/failed/human_needed、敏感输出或 verifier nonzero 均保持 C 未通过、02-22 D blocked；macOS 结果不得写 Windows，C 不解除 Phase 3 双平台硬门。
</threat_model>

<verification>
1. 开始前只读核验 02-20 completed SUMMARY 与 canonical B3 verifier；两者绑定 current build/runtime 且 exact `B3_codex_exit` macOS/L cells有效。
2. 确认 C action 只能由 paired UI 用户点击触发，server 在 reboot 前 mint durable one-time bundle；repo script/public API/CLI/chat不能生成、传递或补写 L authority。
3. 用户真实完整重启 macOS；post-reboot observation证明 boot/session identity变化、runtime generation/build current、旧 Worker/owner/fence被拒绝。
4. Profile coordinator 对 running/unknown/PID reuse/executable/control mismatch 均 fail closed，无 kill/delete-lock/第二实例；只有完整 owner proof允许 probe。
5. Moodle/EdStem各运行固定 official-origin auth/minimal-status probe，binding/scope/shared Profile一致；课程正文、帖子、文件、成绩、下载、写操作和未授权 endpoint访问均为零。
6. EvidenceLedger 只由 paired server actual workflow append两个 exact `C_os_restart` macOS/L results；B/S/I/N/Windows/另一source/scenario/build/generation不能替代，failed/human_needed不能被 latest-pass 隐藏。
7. canonical C verifier严格要求 `C_OS_RESTART_COMPLETE`、两个 current/non-corrected pass cells、same new boot/current generation/build/config/binding、ownership/scope/minimal status/privacy全部成立。
8. fixed user feedback、verifier stdout与最终 SUMMARY不含姓名/邮箱/组织/course/origin/Profile/PID/boot identity/Cookie/DOM/selector/request/action ID/raw error/log/screenshot。
9. 成功后仅 C/macOS/L 变为 pass；D/reauth仍未运行，Windows保持`not_run/human_needed`，共享 Profile仍待后续 gates，Phase 3 blocked。
</verification>

<success_criteria>
- AUTH-02/D-12：真实用户完成一次独立 macOS full reboot，Moodle/EdStem各有 current `C_os_restart` macOS/L strict pass；B结果未被复用。
- AUTH-03：重启后双来源 auth、完整本地身份、confirmed binding、approved scope与最小状态重新观察；漂移/不确定性有明确 failed/human_needed且阻断D。
- AUTH-04/D-09：新 boot 后 Profile single-owner与actual process identity被证明，旧 generation失效；未知/运行/PID reuse不导致抢锁、删锁、误杀或第二实例。
- SEC-02：只运行 sealed official-origin read-only probes；无任意操作参数、业务写、下载、课程正文读取、Cookie桥接、未授权API fallback或敏感输出。
- UAT-01/D-14：用户按已发布步骤本人重启并反馈fixed redacted C result；repo verifier只读，server-only L authority与exact cells不可伪造，失败不会推进。
- Windows保持`not_run/human_needed`，D/reauth未执行，共享Profile未最终采用，Phase 1/2未整体完成，Phase 3继续blocked。
</success_criteria>

<output>
只有收到真实用户 fixed redacted feedback，且 canonical C verifier以 `C_OS_RESTART_COMPLETE` strict pass 后，才创建 `.planning/phases/02-poc-live/02-21-SUMMARY.md`。SUMMARY 仅记录公开 version/build、platform=macos、checkpoint=C、Moodle/EdStem safe result codes、ownership/binding/scope/shared-profile/minimal-status结论、observed time、D eligibility、Windows/Phase3 gaps；不得记录姓名/邮箱/组织/course/origin/Profile/PID/boot identity/Cookie/action ID/raw page/network/log/screenshot。failed/human_needed时保留runtime immutable结果并保持本计划未完成，不创建成功SUMMARY、不执行02-22、不补写planning receipt JSON。
</output>
