---
phase: 02-poc-live
plan: "35"
type: execute
wave: 6
depends_on: ["02-02", "02-07"]
files_modified:
  - packages/domain/src/live-evidence.ts
  - packages/contracts/src/live-evidence.ts
  - packages/application/src/live-checkpoints.ts
  - packages/persistence/src/database.ts
  - packages/persistence/src/auth.ts
  - tests/unit/live-evidence.test.ts
  - tests/integration/live-checkpoint-store.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "The evidence registry distinguishes 176 possible cells from exactly 44 required live cells (2 platforms × 2 sources × 11 scenarios × L); final completion separately evaluates named build-bound S/I/N obligations."
    - "A durable pending live action survives API/Worker restart, Codex exit and OS reboot while remaining bound to immutable beta build, installation, platform, source, scenario, config, scope, binding, generation and parent checkpoint."
    - "Pending actions expire and are consumed once; replay, wrong build/generation/source/scenario/platform, corrected predecessor, or missing parent evidence fails without changing any evidence cell."
    - "Human-action L authority is minted outside the request payload only by the paired-server workflow after the actual bounded result; callers, feedback files, repo scripts, CLI/MCP and ordinary in-process login correlations cannot self-promote to L."
    - "Exact-cell evidence append and pending-action consumption occur in one SQLite IMMEDIATE transaction; failure retains the pending/failure audit and all last-success evidence without partial pass writes."
  artifacts:
    - path: packages/domain/src/live-evidence.ts
      provides: "Exact evidence-requiredness registry and durable live-action domain contracts"
      exports: [PHASE2_EVIDENCE_REQUIREMENTS, requiredEvidenceKeys, PendingLiveAction]
    - path: packages/contracts/src/live-evidence.ts
      provides: "Strict schemas for requiredness, pending actions, paired results and payload-external authority"
      exports: [PendingLiveActionSchema, PairedLiveResultSchema, EvidenceRequirementSchema]
    - path: packages/application/src/live-checkpoints.ts
      provides: "Narrow store/workflow ports distinct from restart-revoked ordinary login actions"
      exports: [LiveCheckpointStore, LiveCheckpointWorkflow]
    - path: packages/persistence/src/auth.ts
      provides: "Transactional durable pending-action store and exact-cell append/failure retention"
      contains: pending_live_actions
  key_links:
    - from: packages/domain/src/live-evidence.ts
      to: packages/persistence/src/auth.ts
      via: "store accepts only registry-backed exact keys"
      pattern: "requiredEvidenceKeys|EvidenceRequirement"
    - from: packages/application/src/live-checkpoints.ts
      to: packages/persistence/src/auth.ts
      via: "consume-and-append transaction with payload-external authority"
      pattern: "consumeAndAppend|recordFailure"
---

<objective>
Define and persist the production contracts that make every Phase 2 live checkpoint reachable after process and OS restarts.

Purpose: Close the durable-correlation and evidence-requiredness gaps while preserving D-03, D-12–D-18 and the strict S/I/N/L separation.
Output: Exact requiredness registry, durable pending-action workflow contracts, SQLite migration/store, and negative tests. No school access, publication, update, or live evidence occurs here.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@packages/domain/src/model.ts
@packages/contracts/src/index.ts
@packages/application/src/ports.ts
@packages/persistence/src/auth.ts

<interfaces>
The registry must encode obligations, not a blanket Cartesian completion rule:
```typescript
export interface EvidenceRequirement {
  requirement: 'AUTH-01'|'AUTH-02'|'AUTH-03'|'AUTH-04'|'SEC-02'|'UAT-01';
  platform: 'macos'|'windows'|'cross-platform';
  source: 'moodle'|'edstem'|'both'|'none';
  scenario: UatScenario|'security_matrix'|'native_preflight'|'distribution';
  evidence: EvidenceClass;
  producer: 'signed_automated'|'paired_human_action';
}
```
All 176 legacy cells remain possible/readable. The exact cell-required set is 44: the two-source L cells for A1/A2 binding/A2 course visibility/B1-1/B1-2/B1-3/B2/B3/C/D/reauth on both platforms. S, I and N remain distinct named build-bound obligations outside that source×scenario live-cell count: S/I use the fixed security/integration case registry and N uses current native macOS/Windows runtime obligations explicitly mapped to AUTH-02/AUTH-04/UAT-01. A possible-but-nonrequired cell may remain not_run and cannot satisfy or block a different obligation. No omitted item defaults to required or pass.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the evidence-requiredness and durable action contracts</name>
  <files>packages/domain/src/live-evidence.ts, packages/contracts/src/live-evidence.ts, packages/application/src/live-checkpoints.ts, tests/unit/live-evidence.test.ts</files>
  <behavior>
    - The registry maps every Phase 2 requirement to explicit platform/source/scenario/evidence obligations and rejects duplicate or contradictory keys.
    - `possibleEvidenceKeys()` returns 176 legacy cells and `requiredEvidenceKeys()` returns exactly 44 L cells (22 per platform); adding pass to a possible-but-not-required cell cannot change final eligibility.
    - Pending action schemas contain no URL, selector, JavaScript, browser handle, credential, Profile path, Cookie, raw page/network data or caller-supplied authority.
    - Action identity is bound to build/artifact, installation, platform, source, scenario, config/scope/binding, generation, parent and expiry.
  </behavior>
  <action>Follow `02-PATTERNS.md` “Plans 35–41 Revision Surface”: `live-evidence.ts`, its strict contracts and `live-checkpoints.ts` are research-contract-first with only the cited model/strict-schema/ports/job-runner shapes as analogs. Per D-12–D-18, create the exact requirement registry and strict durable action/result types. Keep the existing 176-cell vocabulary for display/audit compatibility, but make `requiredEvidenceKeys()` return exactly the 44 L cells above and make final eligibility also consume the closed named `PHASE2_BUILD_OBLIGATIONS` registry for S/I/N. Encode AUTH-01 S plus A1 L, AUTH-02 native obligations plus all live lifecycle L scenarios, AUTH-03 security/integration obligations plus identity/binding/D/reauth L, AUTH-04 ownership/fencing S/I/N obligations, SEC-02 fixed-operation S/I plus applicable live confirmation, and UAT-01 distribution/native-update/live obligations. Add exact duplicate/count tests for 176 possible, 44 required L, 22 per platform, and each named S/I/N obligation-to-requirement mapping. Create narrow `LiveCheckpointStore`/`LiveCheckpointWorkflow` ports for issue, read, fail, and transactional consume-and-append. Keep the ordinary 02-08 in-process `LoginActionRegistry` explicitly separate and restart-revoked; it may authorize opening a login window but never implement `EvidenceWriterAuthority`.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/live-evidence.test.ts tests/unit/auth-contracts.test.ts</automated></verify>
  <done>Requiredness is exact and machine-testable, and downstream production code has a durable narrow contract that no request payload can turn into L authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist one-time live actions and transactional exact-cell outcomes</name>
  <files>packages/persistence/src/database.ts, packages/persistence/src/auth.ts, tests/integration/live-checkpoint-store.test.ts</files>
  <behavior>
    - Pending actions survive close/reopen and process restart with the same immutable binding and expiry.
    - Valid consumption appends exactly the registry-authorized cell and marks the action consumed in one transaction.
    - Failure, crash, busy error, replay or binding mismatch appends no pass; failure audit and prior evidence remain available.
    - Corrections are append-only, same-cell, prior-linked and cannot erase a failure or substitute another class/platform/scenario.
  </behavior>
  <action>Follow the `02-PATTERNS.md` persistence mapping: ordered migration uses `database.ts`; IMMEDIATE consume/append and reopen/crash/fence tests use the cited sessions/job-recovery analogs, while live authority remains research-contract-first. Add an ordered transactional migration for `pending_live_actions` and its append-only outcome/audit linkage without rebuilding existing auth/evidence tables. Store only hashes/opaque IDs and safe enums. Implement issuance with server-generated nonce and monotonic expiry; reload/recovery must retain actions needed for B3/C/D/reauth. Implement `consumeAndAppend` as one IMMEDIATE transaction that rechecks unconsumed/unexpired state, current build/artifact/installation/platform/source/scenario/config/scope/binding/generation/parent, server-held authority, and actual result before appending one exact event and consuming the action. A failed transaction leaves the prior ledger untouched and records only a safe failure via a separate append-only audit path. Never infer L from UI feedback or turn lease expiry into browser exit proof.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/live-checkpoint-store.test.ts tests/integration/auth-persistence.test.ts</automated></verify>
  <done>Durable action bundles and exact-cell evidence remain correct through restart/reboot boundaries, replay and transaction failure, with no partial or inferred pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller → pending action | Request data is untrusted and cannot carry writer authority. |
| persisted action → current runtime | Restarted state must still match current build, generation, source, scenario and parent. |
| actual result → ledger | Consumption and exact append must be atomic and append-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-35-01 | S/E | live action authority | mitigate | Server-generated opaque actions and payload-external authority; wrong/replayed/expired binding tests. |
| T2-35-02 | T/R | requiredness registry | mitigate | Static duplicate-free registry, explicit requirement mapping, no Cartesian default-pass semantics. |
| T2-35-03 | T/D | SQLite consume/append | mitigate | IMMEDIATE transaction, exact predicates, failure retention and crash/reopen tests. |
| T2-35-04 | I | action/evidence persistence | mitigate | Opaque hashes and safe enums only; forbidden-field and DB/WAL sentinel scans. |
</threat_model>

<verification>
Run both targeted suites, then `npm run typecheck && npm run test:unit -- --run && node scripts/dev/runtime.mjs npm run test:integration -- --run`. Confirm no L success is created by tests and Windows/live remains not_run/human_needed.
</verification>

<success_criteria>
- Exact requiredness replaces the impossible blanket 176-pass rule without weakening any requirement.
- Durable actions survive restart/reboot and remain one-time, expiring and fully bound.
- Exact evidence append is atomic, payload-external and failure-retaining.
- No user, source, Profile, credential, publication or live action is touched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-35-SUMMARY.md` with only schemas, counts, safe codes and S/I test results.
</output>
