---
phase: 02-poc-live
plan: "07"
type: execute
wave: 5
depends_on:
  - "02-02"
  - "02-03"
  - "02-04"
  - "02-05"
  - "02-06"
files_modified:
  - packages/application/src/auth-jobs.ts
  - packages/persistence/src/database.ts
  - packages/persistence/src/auth.ts
  - apps/worker/src/main.ts
  - tests/integration/auth-worker.test.ts
  - tests/integration/job-storage.test.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - SEC-02

must_haves:
  truths:
    - "D-11: 共享 Profile 中单一来源失效只暂停该来源，另一来源若仍有效则保持可用；自然过期或暂时性失败最多三次有界只读恢复探针，不输入凭据、不绕过 MFA、不连续弹窗，三次失败后停止并标记 reauth_required；只用现有提示渠道，不新增通知系统，但失败持续显示于管理页和状态接口，Moodle reauth 成功后再自动复查 EdStem。Moodle 与 EdStem 各自拥有可恢复的 durable auth job；一个来源失败、等待重试或要求人工操作时，不会改变另一个来源的已知成功状态。"
    - "自然过期或临时网络失败只会在恢复窗口起点、+5 秒、+30 秒执行至多三次只读探测；需要交互或 MFA 时立即进入 human_needed，不排队自动重试。"
    - "显式登出会取消该来源的排队/等待任务并要求运行中任务停止，且在用户明确完成新登录前不会自动重开；Moodle 重新认证成功只会新建一次 EdStem 探测，不复制认证状态。"
    - "Worker 在发出每次来源请求前与提交每次结果前都验证 cancel、lease、generation、fence；旧 Worker 在失去任一权威后既不能继续访问来源，也不能提交状态。"
    - "网络、解析、权限、认证和人工交互状态保持可区分，失败不会清除该来源最后一次成功记录。"
    - "自动化与 fixture 结果只形成 S/I 证据；Windows 原生/live 单元保持 not_run / human_needed，任何 Phase 3 依赖仍被真实 live gate 阻断。"
  artifacts:
    - path: "packages/application/src/auth-jobs.ts"
      provides: "严格 AuthJob 命令、存储端口、服务 facade 与带双重权威检查的 AuthJobRunner"
      exports:
        - "AuthProbeCommand"
        - "AuthJob"
        - "AuthJobStore"
        - "AuthJobService"
        - "AuthJobRunner"
    - path: "packages/persistence/src/database.ts"
      provides: "从 schema v2 到 v3 的可恢复 source_auth_jobs 迁移"
      contains: "source_auth_jobs"
    - path: "packages/persistence/src/auth.ts"
      provides: "SQLiteAuthJobStore 的事务性 claim、fencing、状态提交、登出取消与 EdStem follow-up"
      exports:
        - "SQLiteAuthJobStore"
    - path: "apps/worker/src/main.ts"
      provides: "现有 synthetic jobs 与 per-source auth jobs 的 Worker 组合及受控轮询"
    - path: "tests/integration/auth-worker.test.ts"
      provides: "重试时序、来源隔离、登出、MFA、follow-up 与旧 Worker 竞态测试"
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "packages/application/src/auth-jobs.ts"
      via: "Worker 每轮调用 AuthJobRunner.runOnce，生产依赖由现有组合根注入"
      pattern: "AuthJobRunner|runOnce"
    - from: "packages/application/src/auth-jobs.ts"
      to: "packages/application/src/ports.ts"
      via: "AuthJobRunner 只通过 SourceProbePort 发出固定 SourceProbeRequest"
      pattern: "SourceProbePort|SourceProbeRequest"
    - from: "packages/application/src/auth-jobs.ts"
      to: "packages/application/src/auth.ts"
      via: "探测结果交给 reduceAuthFlow，并执行其显式 effect"
      pattern: "reduceAuthFlow|AuthEffect"
    - from: "packages/application/src/auth-jobs.ts"
      to: "packages/persistence/src/auth.ts"
      via: "assertCurrent 在请求前与提交前验证，commitTransition 再以同一 lease/fence/generation 条件提交"
      pattern: "assertCurrent|commitTransition"
    - from: "packages/persistence/src/auth.ts"
      to: "source_observations"
      via: "auth job 状态转换与 current/last-success observation 在同一 SQLite 事务提交"
      pattern: "source_observations|last_success"
---

<objective>
建立可持久恢复、按来源隔离的认证探测任务与 Worker：自然过期/网络中断遵循 immediate/5s/30s 的有界只读恢复，需要交互/MFA 和显式登出遵守硬人工边界，并以请求前、提交前的 cancel/lease/generation/fence 检查消除旧 Worker 访问或写入的窗口。

Purpose: 落实 D-08、D-10、D-11 与 T2-05/T2-06，使后端在重启或 Codex 退出后仍可安全管理 Moodle/EdStem 认证健康，同时不把恢复机制扩大为凭据自动化、任意浏览器操作或跨来源认证推断。
Output: 严格 auth-job application facade、SQLite v3 durable queue、事务性 fenced store、Worker 组合和竞态/恢复集成测试。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-03-SUMMARY.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-05-SUMMARY.md
@.planning/phases/02-poc-live/02-06-SUMMARY.md
@packages/application/src/job-runner.ts
@packages/persistence/src/jobs.ts
@packages/persistence/src/claims.ts
@apps/worker/src/main.ts

<interfaces>
以下接口是本计划依赖的既定边界；执行时以依赖计划 SUMMARY 中的真实导出为准，只允许名称对齐，不得放宽语义。

```typescript
// 02-01 domain contracts
export type SourceId = 'moodle' | 'edstem';
export interface SourceProbePort {
  probe(request: SourceProbeRequest, signal: AbortSignal): Promise<SourceProbeResult>;
}
export interface WriteContext {
  authority: WriterAuthority;
  platform: NativePlatform;
  evidenceClass: EvidenceClass;
}

// 02-04 pure state machine
export const AUTH_RECOVERY_DELAYS_MS = [0, 5_000, 30_000] as const;
export function reduceAuthFlow(state: AuthFlowState, event: AuthEvent): AuthTransition;
// effects are closed: probe | schedule_recovery_probe | pause_source | require_user_action

// 02-05/02-06 guarded browser and fixed adapters
export class SealedSourceAdapters implements SourceProbePort {
  probe(request: SourceProbeRequest, signal: AbortSignal): Promise<SourceProbeResult>;
}
```

本计划新增的跨层契约必须精确为：

```typescript
export type PublicAuthProbeTrigger =
  | 'background'
  | 'user_login_completed'
  | 'manual_retry';

export interface AuthProbeCommand {
  source: SourceId;
  approvedConfigId: string;
  approvedScopeId: string;
  trigger: PublicAuthProbeTrigger;
  idempotencyKey: string;
}

export type AuthJobState =
  | 'queued' | 'running' | 'retry_wait'
  | 'succeeded' | 'failed' | 'cancelled' | 'human_needed';

export interface AuthJobLease {
  owner: string;
  fence: number;
  generation: number;
  leaseUntil: string;
}

export interface AuthJobStore {
  enqueue(command: AuthProbeCommand, context: WriteContext): Promise<AuthJob>;
  claim(input: { owner: string; now: string; leaseMs: number }, context: WriteContext): Promise<AuthJob | null>;
  assertCurrent(jobId: string, lease: AuthJobLease, now: string, context: WriteContext): Promise<AuthJob>;
  heartbeat(jobId: string, lease: AuthJobLease, now: string, leaseMs: number, context: WriteContext): Promise<AuthJobLease>;
  commitTransition(jobId: string, lease: AuthJobLease, transition: AuthTransition, now: string, context: WriteContext): Promise<AuthJob>;
  requestCancel(jobId: string, source: SourceId, context: WriteContext): Promise<AuthJob>;
  cancelSourceForLogout(source: SourceId, context: WriteContext): Promise<void>;
  acknowledgeCancel(jobId: string, lease: AuthJobLease, context: WriteContext): Promise<AuthJob>;
  recoverExpired(now: string, context: WriteContext): Promise<number>;
  get(jobId: string, source: SourceId): Promise<AuthJob | null>;
}

export class AuthJobService {
  requestProbe(command: AuthProbeCommand, context: WriteContext): Promise<{ jobId: string }>;
  recordExplicitLogout(source: SourceId, context: WriteContext): Promise<AuthFlowState>;
  cancel(jobId: string, source: SourceId, context: WriteContext): Promise<AuthJob>;
  query(jobId: string, source: SourceId): Promise<AuthJob | null>;
}

export class AuthJobRunner {
  runOnce(owner: string, context: WriteContext): Promise<AuthJob | null>;
}
```

`AuthJob` 只包含固定来源/动作、批准配置与 scope 引用、内部 trigger、attempt/recoveryStartedAt/nextRunAt、cancelRequested、lease/generation/fence、结构化安全结果和 `lastSuccess` 引用；不得包含 URL、JS、selector、browser handle、operation closure、下载/写操作、DOM/HTML、Profile 路径、Cookie/storageState、请求头/体、凭据或登录输入。
</interfaces>

<hard_gates>
- 本计划只实现代码与 S/I 自动化测试；不得打开官方登录页、请求用户密码/MFA、访问学校真实数据、执行 live UAT、发布或把 fixture 结果记录成 L。
- macOS-first 只改变人工执行顺序，不降低跨平台门槛；Windows 原生/live 单元必须保持 `not_run / human_needed`，Linux/WSL 不替代 Windows。
- Phase 1 与 Phase 2 均不得因本计划自动化通过而宣告完成；Phase 3 仍由真实 manual login、Profile reopen/restart/Codex exit/cross-day/reauth/account-binding live gate 阻断。
- 专用 Profile 仍是敏感凭据载体并保持独占；lease 超时不证明浏览器退出，不得 kill、接管或回收不属于当前 install/job 的浏览器进程。
- 所有探测只能经 02-06 的固定只读 adapter；不得新增任意 URL/JS/selector/browser handle、学术来源写入、下载、凭据自动填写、MFA 自动处理或绕过限制的入口。
</hard_gates>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立严格 durable auth-job facade、SQLite v3 queue 与事务状态转换</name>
  <files>packages/application/src/auth-jobs.ts, packages/persistence/src/database.ts, packages/persistence/src/auth.ts, tests/integration/auth-worker.test.ts, tests/integration/job-storage.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/02-poc-live/02-CONTEXT.md, .planning/phases/02-poc-live/02-RESEARCH.md, .planning/phases/02-poc-live/02-VALIDATION.md, .planning/phases/02-poc-live/02-PATTERNS.md, .planning/phases/02-poc-live/02-02-SUMMARY.md, .planning/phases/02-poc-live/02-03-SUMMARY.md, .planning/phases/02-poc-live/02-04-SUMMARY.md, packages/domain/src/model.ts, packages/contracts/src/index.ts, packages/application/src/ports.ts, packages/application/src/auth.ts, packages/application/src/job-runner.ts, packages/persistence/src/database.ts, packages/persistence/src/auth.ts, packages/persistence/src/claims.ts, tests/integration/job-storage.test.ts</read_first>
  <behavior>
    - Case 1（D-08/D-11）：Moodle 与 EdStem 可分别 enqueue/claim/transition；同一来源+idempotencyKey 重放返回同一任务，不同来源互不覆盖。
    - Case 2（D-11/T2-06）：自然过期或暂时网络失败从同一 `recoveryStartedAt` 严格排为第 1 次立即、第 2 次 +5,000ms、第 3 次 +30,000ms；第三次仍失败进入 `human_needed/reauth_required`，不存在第四次探测。
    - Case 3（D-10/D-11）：`interaction_required` 或 `mfa_required` 第一次即进入 human_needed 且 `nextRunAt=null`；显式登出取消 queued/retry_wait、标记 running cancel_requested，并拒绝 background/manual_retry，只有 `user_login_completed` 可清除 logout intent 并新建任务。
    - Case 4（D-11）：Moodle 的 `user_login_completed` 任务认证成功时，同一事务仅插入一个 EdStem follow-up probe；它有独立 approved EdStem config/scope、独立 job/fence，且不复制 Moodle session/auth/identity。
    - Case 5（T2-05）：错误 fence、错误 generation、过期 lease、cancel_requested 任一条件使 `assertCurrent` 与 `commitTransition` 拒绝；lease expiry 只恢复 durable job，不证明或回收 Profile/browser。
    - Case 6（T2-06）：网络、parser、permission、authentication、interaction/MFA 产生不同结构化状态；任一失败都保留此前 `lastSuccess`，confirmed empty 也不等于 error/deletion。
    - Case 7：schema version 2 数据库迁移到 version 3 后原 jobs、observations、binding、Profile ownership 与 evidence 数据保持不变；失败迁移回滚，`user_version` 只在完整成功后为 3。
  </behavior>
  <action>
先写上述失败测试，再实现。按照 D-08、D-10、D-11 在 `packages/application/src/auth-jobs.ts` 建立 `<interfaces>` 中的闭合类型、`AuthJobStore` 端口、`AuthJobService` 和 `AuthJobRunner`。公开 `AuthProbeCommand` 只接受 source、批准 config/scope 引用、三个公开 trigger 与有界 idempotencyKey；用现有 zod/strict-object 模式拒绝额外字段，尤其 URL、JS、selector、browser handle、operation、write、download 与任何 Profile/凭据字段。内部 follow-up/recovery trigger 不得暴露给 API 调用方。`recordExplicitLogout` 必须先通过 reducer 得出显式登出状态，再事务取消该来源排队/等待任务并设置运行中 cancel；它不能打开窗口或安排恢复。

在 `packages/persistence/src/database.ts` 新增原子 schema version 2→3 迁移，创建规范化 `source_auth_jobs`（固定 source/action/trigger/state、config/scope 外键或受验证引用、attempt、recovery_started_at、next_run_at、cancel_requested、lease owner/fence/until、generation、safe result code、last-success 引用、parent job 与唯一 idempotency 约束），不得使用可携带任意浏览器请求的开放 payload。更新 `tests/integration/job-storage.test.ts` 的 schema 版本断言和 version 2→3 保留/失败回滚覆盖。

在 `packages/persistence/src/auth.ts` 实现 `SQLiteAuthJobStore`：claim、heartbeat、recoverExpired 与现有 durable job 语义一致；`assertCurrent` 必须在单次一致性读取中同时检查 state=running、cancel=false、lease 未过期、owner/fence/generation 完全匹配。`commitTransition` 再以同一谓词执行条件更新，并在一个事务中更新 auth job 与该来源 observation/current/last-success；失败只更新 current error/health，不清空 last-success。恢复调度只能消费 reducer 明示的可恢复 effect，严格使用 `[0, 5_000, 30_000]` 三个绝对偏移，不能因进程重启重置次数。Moodle reauth 成功时，在相同事务基于已批准 EdStem config/scope 创建唯一 follow-up（以 parent job + effect kind 防重复），绝不复制 Moodle 状态。interaction/MFA、parser、permission、identity/origin mismatch 均不自动重试。所有持久化写入必须接受 payload 外的 `WriteContext` 并沿用 02-02 的 writer authority/evidence 约束；测试不得生成 L receipt。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-worker.test.ts tests/integration/job-storage.test.ts --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/application/src/auth-jobs.ts && rg -n "export (interface|type|class) (AuthProbeCommand|AuthJob|AuthJobStore|AuthJobService|AuthJobRunner)" packages/application/src/auth-jobs.ts` 能找到五个公开契约。
    - `rg -n "0.*,.*5_?000.*,.*30_?000|AUTH_RECOVERY_DELAYS_MS" packages/application/src/auth-jobs.ts tests/integration/auth-worker.test.ts` 能证明调度复用固定三次窗口；测试包含“无第四次”断言。
    - `rg -n "assertCurrent|cancelSourceForLogout|commitTransition|recoverExpired" packages/application/src/auth-jobs.ts packages/persistence/src/auth.ts` 能找到权威检查、登出取消、事务提交和过期恢复实现。
    - `rg -n "source_auth_jobs|user_version.*3|schemaVersion.*3" packages/persistence/src/database.ts tests/integration/job-storage.test.ts` 能找到 v3 迁移与测试。
    - `if rg -n "(storageState|cookie|password|mfaCode|rawHtml|requestHeaders|requestBody|browserHandle|selector|javascript|downloadUrl)" packages/application/src/auth-jobs.ts packages/persistence/src/auth.ts; then exit 1; fi` 成功，证明 auth job 生产契约未引入禁止字段。
    - 自动化命令退出 0，并覆盖双来源隔离、三次绝对时序、interaction/MFA 立即 human_needed、显式登出零自动恢复、Moodle→EdStem 唯一 fresh probe、last-success 保留和 v3 回滚。
  </acceptance_criteria>
  <done>durable auth job 的公开命令保持闭合，SQLite v3 可恢复且不丢 v2 数据；来源隔离、精确三次恢复、显式登出和 Moodle→EdStem fresh probe 均由事务与测试锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 auth runner 接入 Worker，并证明请求前/提交前双重 fencing</name>
  <files>packages/application/src/auth-jobs.ts, apps/worker/src/main.ts, tests/integration/auth-worker.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/02-poc-live/02-CONTEXT.md, .planning/phases/02-poc-live/02-VALIDATION.md, .planning/phases/02-poc-live/02-04-SUMMARY.md, .planning/phases/02-poc-live/02-05-SUMMARY.md, .planning/phases/02-poc-live/02-06-SUMMARY.md, packages/application/src/auth-jobs.ts, packages/application/src/job-runner.ts, packages/persistence/src/auth.ts, apps/worker/src/main.ts, tests/integration/worker.test.ts, tests/integration/auth-worker.test.ts</read_first>
  <behavior>
    - Case 1（T2-05/request gate）：claim 后、调用 `SourceProbePort.probe` 前发生 cancel、lease 过期、generation 增加或 fence 改变时，fake adapter 的调用次数保持 0，任务不提交成功。
    - Case 2（T2-05/in-flight gate）：probe 阻塞期间 heartbeat 发现失权/取消时 AbortSignal 被触发；02-05 guarded session 的同样检查仍是第二道来源访问防线。
    - Case 3（T2-05/commit gate）：probe 返回后、提交前注入 cancel/失权时第二次 `assertCurrent` 失败，`commitTransition` 未执行，observation/last-success/follow-up 均无新写入。
    - Case 4：通过第二次 `assertCurrent` 后又发生竞争时，`commitTransition` 自身的条件更新影响 0 行并回滚所有 observation/follow-up 写入。
    - Case 5（D-11）：Moodle retry_wait 不阻止 EdStem runnable job，反之亦然；Worker 每轮在现有 synthetic queue 和 auth queue 各尝试一个工作单元，不发生永久饥饿。
    - Case 6：Worker 重启调用 recoverExpired 可恢复 auth job 的持久 attempt/nextRunAt，但不会重置恢复窗口、重启浏览器、reclaim Profile 或把旧 generation 领回。
  </behavior>
  <action>
先扩展竞态测试，再实现 Worker 连接。`AuthJobRunner.runOnce` 必须遵循不可调换的顺序：recover/claim → 启动可停止 heartbeat 与 AbortController → `store.assertCurrent(jobId, capturedLease, now, context)` → 构造固定 `SourceProbeRequest` → `SourceProbePort.probe(request, signal)` → `reduceAuthFlow` → 再次 `store.assertCurrent(...)` → `store.commitTransition(...)`。任何检查失败都 abort；若已请求则等待 adapter 收敛，不得以旧 lease 重试提交。`commitTransition` 必须保留第三道 SQL 条件谓词，消除第二次检查后的 TOCTOU。不得以 lease expiry 推断 Profile/browser 已退出；Profile generation/fence 必须来自 02-03 coordinator 的当前证明，runner 不自行制造、猜测或续用旧 owner identity。

在 `apps/worker/src/main.ts` 保留 Phase 1 synthetic `JobRunner`，并通过可注入组合根构造 `AuthJobRunner`、02-02 store、02-03 Profile ownership coordinator、02-05 guarded local provider 与 02-06 sealed adapters。轮询每轮最多分别尝试一个 synthetic job 与一个到期 auth job；没有 auth work 时不得打开浏览器。生产组合只允许固定 adapter，测试用 fake `SourceProbePort` 注入结果且明确为 S/I。Worker 接收到停机信号时先停止新 claim、abort 进行中的 probe、停止 heartbeat，再释放当前安装实际持有的 lease；不得 kill unrelated browser。显式登出/cancel、interaction/MFA、三次恢复耗尽后均保持 durable terminal/human-needed 状态，禁止 Worker 自行打开登录窗口或自动恢复。确保日志仅包含 job id、source、安全状态码与 fence/generation 元数据，不记录 URL、DOM、Profile 路径、Cookie、请求内容、身份详情或登录输入。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-worker.test.ts tests/integration/worker.test.ts tests/integration/job-recovery.test.ts --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "assertCurrent" packages/application/src/auth-jobs.ts` 至少报告两个 runner 调用点，且测试分别命名/覆盖 before-request 与 before-commit 失权场景。
    - `rg -n "AbortController|\.abort\(|signal" packages/application/src/auth-jobs.ts apps/worker/src/main.ts tests/integration/auth-worker.test.ts` 能找到 heartbeat/cancel 到 adapter AbortSignal 的连线。
    - `rg -n "AuthJobRunner|SealedSourceAdapters|LocalPlaywrightBrowserProvider|ProfileOwnership" apps/worker/src/main.ts` 能找到生产组合边界；不存在直接 page/browser 任意操作调用。
    - `if rg -n "(page\.goto|page\.evaluate|locator\(|\$\$?\(|storageState|context\.cookies|killall|pkill)" apps/worker/src/main.ts packages/application/src/auth-jobs.ts; then exit 1; fi` 成功，证明 Worker 未绕过固定 adapter、读取凭据或粗暴清理浏览器。
    - 自动化命令退出 0；lost lease/cancel/stale generation/stale fence 在请求前令 adapter 调用为 0，在提交前令 observation 与 follow-up 写入为 0，SQL 竞争令整个提交回滚。
    - `rg -n "not_run|human_needed|Windows|Phase 3|S/I" tests/integration/auth-worker.test.ts .planning/phases/02-poc-live/02-07-PLAN.md` 能找到证据分离和跨平台/Phase 3 阻断断言或计划硬门说明，且测试无 L receipt。
  </acceptance_criteria>
  <done>Worker 可在进程重启后继续处理严格 per-source auth jobs；每次来源访问和每次状态提交均受 cancel/lease/generation/fence 保护，旧 Worker 的请求与写入竞态被自动化测试否定。</done>
</task>

</tasks>

<threat_model>
本计划执行 OWASP ASVS L1 所要求的输入验证、会话/授权边界、失败安全与敏感数据保护原则；不宣称未经 live UAT 或独立审计的合规认证。下列 High/Critical 项任一未通过自动化验证即为本计划 blocker，也继续阻断 Phase 3。

## Trust Boundaries

| Boundary | 不可信/可变化输入 | 强制控制 |
|---|---|---|
| API/application caller → AuthJobService | source、trigger、config/scope 引用、幂等键与额外字段 | strict schema、固定枚举、payload 外 WriteContext、无任意浏览器字段 |
| Durable DB → Worker claim | 旧任务、取消标志、过期 lease、旧 generation/fence | 原子 claim；请求前 assertCurrent；heartbeat 失权 abort |
| Worker → fixed SourceProbePort/Profile | 网络响应、未可信页面、交互/MFA、所有权变化 | sealed adapter、固定只读动作、02-03/02-05 owner+fence+generation 检查、AbortSignal |
| Probe result → durable state/effects | 迟到结果、错误分类、来源混淆、重放 follow-up | 提交前 assertCurrent；事务性条件更新；source-scoped state；唯一 parent effect |
| 自动化证据 → live gate | fixture/synthetic 成功 | 只写 S/I；L 必须由用户在声明的 native platform 完成，未运行保持 not_run/human_needed |

## STRIDE / ASVS L1 Threat Register

| Threat ID | Severity | STRIDE | Component | Disposition | Mitigation / blocker rule |
|---|---|---|---|---|---|
| T2-05 | High | S/T/E | `AuthJobRunner`、`SQLiteAuthJobStore`、Profile lease | mitigate | 请求前与提交前校验 cancel/lease/owner/fence/generation，heartbeat 失权触发 AbortSignal，最终 commit 使用同一条件谓词；任何 stale Worker 可访问来源或影响行数即 blocker。 |
| T2-06 | High | T/R | reducer→durable observation | mitigate | 网络/parser/permission/auth/interaction 分离；失败只更新 current 状态并保留 last-success；类别合并、旧数据被抹除或跨来源覆盖即 blocker。 |
| T2-01 | Critical | T/E | `AuthProbeCommand`→adapter | mitigate | strict closed command 与固定 SourceProbePort；拒绝 URL/JS/selector/browser handle/write/download/额外字段；存在任意浏览器操作入口即 blocker。 |
| T2-04 | Critical | S/E | Worker→专用 Profile | mitigate | 仅接受 02-03 当前 installation/job owner identity + fence + generation；lease expiry 不授权回收或 kill；缺少完整证明、暴露 Profile 路径或影响非 owned browser 即 blocker。 |
| T2-07 | High | I | logs/job payload/test evidence | mitigate | 只持久化/记录安全枚举和引用；禁止 Cookie、storageState、DOM/HTML、请求头/体、凭据、Profile 路径；fixture 不得生成 L receipt，发现敏感字段或假 live claim 即 blocker。 |
| T2-08 | High | D/E | recovery scheduler/logout boundary | mitigate | 仅自然过期/临时网络使用 `[0,5000,30000]`；MFA/interaction 立即 human_needed；显式登出零自动恢复；出现第四次 probe 或自动打开登录窗口即 blocker。 |

## Security verification gates

- ASVS V2/V3：没有凭据采集、session 复制或跨来源认证推断；Moodle 成功只触发独立 EdStem probe。
- ASVS V4：WriteContext、source scope、owner/fence/generation 均在可信 payload 外传入并于提交时重新验证。
- ASVS V5：所有外部命令严格校验，来源内容只作为数据，不可能成为 Worker 指令。
- ASVS V7/V8：日志、数据库与测试 failure output 不含认证材料、Profile 路径、原始页面或网络捕获。
- 任何 High/Critical threat 未由命名测试证明缓解时，不得把计划标为完成，也不得继续真实学校访问或 Phase 3。
</threat_model>

<verification>
1. `node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-worker.test.ts tests/integration/job-storage.test.ts tests/integration/worker.test.ts tests/integration/job-recovery.test.ts --maxWorkers=1`
2. `npm run typecheck`
3. `rg -n "assertCurrent" packages/application/src/auth-jobs.ts` 显示来源请求前与提交前两个独立检查点；`commitTransition` 的实现还包含 owner/fence/generation/lease/cancel 条件更新。
4. `if rg -n "(storageState|cookie|password|rawHtml|requestHeaders|requestBody|browserHandle|page\.goto|page\.evaluate|locator\(|downloadUrl)" packages/application/src/auth-jobs.ts packages/persistence/src/auth.ts apps/worker/src/main.ts; then exit 1; fi`
5. 阅读 `tests/integration/auth-worker.test.ts` 的测试名确认覆盖 D-08、D-10、D-11、T2-05、T2-06，且 live/Windows 未运行项没有被 fixture 填充。
</verification>

<success_criteria>
- Moodle/EdStem auth jobs 可在 SQLite 中独立持久化、claim、恢复、取消与查询，进程重启不重置 attempt 或恢复起点。
- 自然过期/临时网络严格至多三次 `[0,5s,30s]` 只读探测；interaction/MFA 立即 human_needed；显式登出在新的人类登录完成前零自动重试。
- Moodle reauth 成功只事务创建一次新的 EdStem probe，二者 session/auth/identity 永不复制或合并。
- cancel、lease、generation、fence 在请求前与提交前均检查，最终 SQL 条件提交关闭 TOCTOU；旧 Worker 无法访问来源或写 observation/follow-up。
- 各失败类别保持独立，last-success 不因失败或重启丢失，一个来源的失败不污染另一个来源。
- 所有测试只产生 S/I 证据；macOS live gate、Windows native/live gate 与 Phase 3 阻断状态保持不变。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-07-SUMMARY.md`，明确记录 auth-job facade、迁移版本、双重 fencing 测试结果与未执行的 live/Windows hard gates；不得把自动化结果写成真实登录证据。
</output>
