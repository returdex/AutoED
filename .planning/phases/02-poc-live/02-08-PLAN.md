---
phase: 02-poc-live
plan: "08"
type: execute
wave: 6
depends_on:
  - "02-02"
  - "02-04"
  - "02-06"
  - "02-07"
files_modified:
  - packages/application/src/policy.ts
  - packages/contracts/src/presentation.ts
  - apps/api/src/auth.ts
  - apps/api/src/security.ts
  - apps/api/src/main.ts
  - tests/unit/output-policy.test.ts
  - tests/integration/auth-api.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - SEC-02
  - UAT-01
must_haves:
  truths:
    - "未配对、待配对或已失效的浏览器会话看不到任何来源 origin、完整姓名、学校邮箱、绑定候选或历史结果；只有当前已配对且 same-origin 的本地 UI principal 能读取 protected projection。"
    - "CLI 与 MCP 只能读取脱敏状态/结果单：固定安全码、五个独立状态维度、binding consistency、12 字符不可逆展示指纹、时间与缺口；完整姓名、完整邮箱、课程名、页面/path/query、Profile/Cookie 或原始异常永不出现。"
    - "控制面只注册 status、configuration confirm、login open、probe、logout intent、binding confirm、receipt read 七类固定 auth route；请求无法表达任意 URL、JS、selector、browser handle、HTTP body、下载或源站业务写。"
    - "每个 auth mutation 同时要求当前 paired browser principal、精确 same-origin 与有效 CSRF；CLI/MCP bearer、无 CSRF、跨源、过期/重启/撤销 session 即使知道请求格式也不能变更配置、打开登录窗口、入队 probe、确认绑定或记录登出意图。"
    - "完整身份只存在于 `ProtectedAuthStatusProjection`；所有其他 presenter 均从字段 allowlist 重建 DTO，未知 source/adapter/application error 统一为安全错误，绝不序列化 message、cause、stack、页面、路径、身份或 source payload。"
    - "登录动作关联 ID 由服务端在 paired+CSRF 校验后签发并绑定 session/source/config/generation，只能消费一次；UI 不能自造 human/live authority，自动测试也不能据此写 L/pass receipt。"
    - "本计划仅形成 S/I API 与输出策略证据，不访问学校、不启动真实登录、不写 N/L；Windows 全部保持 not_run/human_needed，Phase 1/2 不整体宣称通过且 Phase 3 仍 blocked。"
  artifacts:
    - path: "packages/application/src/policy.ts"
      provides: "AuthControlApplication、细粒度 auth permissions、fixed-operation admission 与 fail-closed error mapping"
      exports:
        - AuthControlApplication
        - OfficialLoginLauncher
        - SafeAuthErrorCode
    - path: "packages/contracts/src/presentation.ts"
      provides: "protected/redacted auth projections、receipt projection 与安全错误 schema/presenters"
      exports:
        - ProtectedAuthStatusProjection
        - RedactedAuthStatusProjection
        - RedactedEvidenceReceipt
        - presentProtectedAuthStatus
        - presentRedactedAuthStatus
        - presentEvidenceReceipts
        - SafeAuthApiErrorSchema
    - path: "apps/api/src/auth.ts"
      provides: "七类固定 paired loopback auth routes 与服务端一次性 login action registry"
      contains: "registerAuthRoutes"
    - path: "tests/integration/auth-api.test.ts"
      provides: "实际 loopback HTTP 上的 pairing/CSRF/permission/fixed-schema/privacy/unknown-error 集成证据"
      contains: "describe('paired fixed auth api'"
  key_links:
    - from: "apps/api/src/auth.ts"
      to: "packages/application/src/policy.ts"
      via: "每条 route 只调用 AuthControlApplication 的对应固定方法；route 不直接访问 Playwright、SQLite auth stores 或 EvidenceLedger"
      pattern: "AuthControlApplication|registerAuthRoutes"
    - from: "packages/application/src/policy.ts"
      to: "packages/application/src/auth-jobs.ts"
      via: "probe/logout 只调用 02-07 AuthJobService.requestProbe/recordExplicitLogout，API 不传 retry/follow-up/browser operation"
      pattern: "AuthJobService|requestProbe|recordExplicitLogout"
    - from: "apps/api/src/auth.ts"
      to: "apps/api/src/pairing.ts"
      via: "所有 mutation 复用 browserPrincipal→sameOrigin→SQLiteSessions.authenticate(cookie, csrf)，并用返回 session id 绑定一次性动作"
      pattern: "browserPrincipal|x-autoed-csrf|autoed_session"
    - from: "apps/api/src/auth.ts"
      to: "packages/contracts/src/presentation.ts"
      via: "GET status 按 principal 选择 protected 或 redacted presenter，receipt 永远经过 redacted presenter"
      pattern: "presentProtectedAuthStatus|presentRedactedAuthStatus|presentEvidenceReceipts"
---

<objective>
在现有 application authorization、paired loopback session 与 02-07 durable auth jobs 外建立唯一安全控制面：只暴露固定配置/登录/probe/登出意图/绑定/结果单动作，并按 destination 严格分离完整本地身份与所有其他脱敏输出。

Purpose: 落实 D-04–D-07、D-14–D-15 和 AUTH-01/AUTH-03/SEC-02/UAT-01，在 UI 接入及任何 live 登录前阻断 T2-01 任意操作注入、T2-02 敏感资料外泄与 T2-07 未配对/非本地完整身份泄漏。
Output: application auth policy/facade、protected/redacted presenters、固定 paired loopback auth routes，以及覆盖 actual HTTP pairing/CSRF/权限/隐私/错误边界的 S/I 测试。本计划不打开真实学校页面、不登录、不发布、不执行 live UAT。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@.planning/phases/02-poc-live/02-02-SUMMARY.md
@.planning/phases/02-poc-live/02-04-SUMMARY.md
@.planning/phases/02-poc-live/02-06-SUMMARY.md
@.planning/phases/02-poc-live/02-07-SUMMARY.md
@packages/application/src/policy.ts
@packages/application/src/auth.ts
@packages/application/src/auth-jobs.ts
@packages/application/src/ports.ts
@packages/contracts/src/presentation.ts
@apps/api/src/main.ts
@apps/api/src/security.ts
@apps/api/src/pairing.ts
@packages/persistence/src/sessions.ts
@tests/unit/output-policy.test.ts
@tests/integration/pairing.test.ts

<interfaces>
执行时以 02-01/02-02/02-04/02-07 SUMMARY 和实际导出为准；仅允许机械适配名称，不得放宽下列语义：

```typescript
export interface AuthJobService {
  requestProbe(input: AuthProbeCommand, context: WriteContext): Promise<{ jobId: string }>;
  recordExplicitLogout(source: SourceId, context: WriteContext): Promise<AuthFlowState>;
  query(jobId: string, source: SourceId): Promise<AuthJob | null>;
  cancel(jobId: string, source: SourceId, context: WriteContext): Promise<AuthJob>;
}

export interface AuthProbeCommand {
  source: SourceId;
  approvedConfigId: string;
  approvedScopeId: string;
  trigger: 'background' | 'user_login_completed' | 'manual_retry';
  idempotencyKey: string;
}

export interface SourceConfigStore {
  read(source: SourceId): Promise<ApprovedSourceConfig | null>;
  confirm(config: ApprovedSourceConfig, context: WriteContext): Promise<void>;
}
export interface SourceObservationStore {
  read(source: SourceId): Promise<SourceObservation | null>;
  write(observation: SourceObservation, context: WriteContext): Promise<void>;
}
export interface AccountBindingStore {
  read(): Promise<AccountBinding | null>;
  write(binding: AccountBinding, context: WriteContext): Promise<void>;
}
export interface EvidenceLedger {
  append(receipt: EvidenceReceipt, authority: EvidenceWriterAuthority, context: WriteContext): Promise<void>;
  list(key: EvidenceCellKey): Promise<EvidenceReceipt[]>;
}
```

现有 pairing/session 安全边界必须原样复用：`browserPrincipal()` 先做 `sameOrigin()`，再以 HttpOnly `autoed_session` cookie 和 mutation 的 `x-autoed-csrf` 调用 `SQLiteSessions.authenticate()`；session 在 8 小时、显式撤销及 API restart 后失效。CLI/MCP 使用 Bearer principal，不能冒充 `local_ui`。

本计划稳定导出供 02-09 直接消费，名称与 wire shape 不得另建别名：

```typescript
export interface ProtectedAuthStatusProjection {
  overall: { code: SafeAuthErrorCode; phase3Eligibility: 'blocked'; gaps: readonly string[] };
  sources: readonly {
    source: SourceId;
    officialOrigin: string; // canonical origin only; no path/query/fragment
    identity: { displayName: string; schoolEmail: string } | null;
    auth: SourceObservation['auth']; capability: SourceObservation['capability'];
    health: SourceObservation['health']; freshness: SourceObservation['freshness'];
    completeness: SourceObservation['completeness']; checkedAt: string | null;
    resultCode: SafeAuthErrorCode; sharedProfile: 'candidate' | 'observed' | 'unverified';
  }[];
  binding: { consistency: 'not_observed' | 'candidate' | 'confirmed' | 'mismatch'; identityFingerprint: string | null };
  nextAction: { kind: 'open_login'; source: SourceId; approvedConfigId: string }
    | { kind: 'confirm_binding'; candidateBindingId: string }
    | { kind: 'wait' | 'none' };
}

export interface RedactedAuthStatusProjection {
  overall: { code: SafeAuthErrorCode; phase3Eligibility: 'blocked'; gaps: readonly string[] };
  sources: readonly { source: SourceId; auth: string; capability: string; health: string; freshness: string;
    completeness: string; checkedAt: string | null; resultCode: SafeAuthErrorCode; identityFingerprint: string | null }[];
  bindingConsistency: 'not_observed' | 'candidate' | 'confirmed' | 'mismatch';
}

export interface AuthActionAccepted { accepted: true; actionReceiptId: string; resultCode: SafeAuthErrorCode }
export interface SafeAuthApiError { code: SafeAuthErrorCode; stage: 'auth_api'; nextAction: string }
```

`identityFingerprint` 是从 persistence 已保存的完整 keyed HMAC fingerprint 派生的 12 字符大写 base32 展示码（`^[A-Z2-7]{12}$`）；只用于本人核对/支持沟通，绝不参与授权、绑定比较或唯一性判断。presenter 不接收 raw subject/tenant，也不自行对姓名/邮箱做无 key hash。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-08-1：以 allowlist presenter 和应用 admission 固定身份/错误输出边界</name>
  <read_first>AGENTS.md 的 Credentials and login、Source and content policy、Data integrity and verification；02-CONTEXT.md D-04–D-07、D-14–D-15、D-17–D-18；02-RESEARCH.md API and UI、T2-01/T2-02/T2-07/T2-08 与 Validation Architecture；02-VALIDATION.md Source contracts、Local UI privacy、Evidence ledger；02-PATTERNS.md application policy/presenter/strict DTO 约定；02-UI-SPEC.md 的 protected identity、OverallGate/SourceCard/BindingPanel 数据需求；02-01/02-02/02-04/02-07 SUMMARY 及实际 model/schema/store/AuthJobService 导出；packages/application/src/policy.ts、packages/contracts/src/presentation.ts、tests/unit/output-policy.test.ts。</read_first>
  <files>packages/application/src/policy.ts, packages/contracts/src/presentation.ts, tests/unit/output-policy.test.ts</files>
  <behavior>
    - RED：同一内部状态传给 paired local UI 得到完整 canonical origin/displayName/schoolEmail；传给 local_cli/model/log/diagnostic/receipt presenter 时这些字段、课程名和 protected identity 对象均不存在，而不是值被替换成可逆占位。
    - RED：redacted source 仅有 source、五维状态、checkedAt、安全 resultCode、binding consistency、12 字符 base32 fingerprint 与 gap；递归 extra keys、raw subject/tenant、Profile path、Cookie、URL path/query、DOM/HTML、request/response、stack/cause/message、截图字段全部被 schema 拒绝或 allowlist 丢弃。
    - RED：display fingerprint 只能从已验证的完整 keyed fingerprint 派生，长度/字母表固定，稳定但不用于 binding equality；raw identity、姓名或邮箱作为输入时 fail closed。
    - RED：identity_mismatch 保持两个 protected identity 供 paired UI 并列核对，同时所有 course access/mutation eligibility 为 blocked；redacted 输出只有 mismatch、短 fingerprint 与 safe code。
    - RED：unknown adapter/source/storage/error（含带敏感 sentinel 的 Error.message/cause/stack）统一为 `UNKNOWN_SOURCE_ERROR` 或 `INTERNAL_ERROR`，输出严格只有 `{code,stage:'auth_api',nextAction}`。
    - RED：AuthControlApplication 对 configuration/login/probe/logout/binding/receipt/read 分别检查细粒度 permission、installation/source/config/scope/current binding；CLI/MCP 只有 auth read/receipt read，不能调用任何 mutation 或让 GET 间接打开浏览器/入队课程访问。
  </behavior>
  <action>先扩展 `tests/unit/output-policy.test.ts` 写失败测试，再实现。`packages/contracts/src/presentation.ts` 新增并稳定导出 `ProtectedAuthStatusProjection`、`RedactedAuthStatusProjection`、`RedactedEvidenceReceipt`、`AuthActionAccepted`、`SafeAuthApiErrorSchema` 及 `presentProtectedAuthStatus`、`presentRedactedAuthStatus`、`presentEvidenceReceipts`。每个 presenter 必须从明确字段 allowlist 新建对象，不能 `spread` 内部 observation/identity/error/receipt，也不能把通用 `redactOutput()` 当字段级授权。protected presenter 仅接受 application 已证明的 `{destination:'local_ui', paired:true}` context；canonical origin 只允许 origin，无 path/query/fragment；完整姓名/邮箱只在 `identity` 子对象，空值保持 null/not_observed，不猜测。redacted presenter 对 CLI/MCP/log/diagnostic/receipt 使用同一 schema：保留五维状态、checkedAt、safe result code、binding consistency、12 字符展示 fingerprint 和 gap code，绝不输出 official origin、displayName、schoolEmail、course title/name、protected identity、Profile/browser/process、原始错误或自由文本私人资料。`RedactedEvidenceReceipt` 精确保留 D-14 的 version/build、native platform、source、scenario、evidence、time、result/safe code、binding consistency、display fingerprint 与 gap/next-action code；无截图或任意附件字段（D-14–D-15）。

在 `packages/application/src/policy.ts` 扩展 `Permission` 为 `auth:read`、`auth:receipts:read`、`auth:configuration:write`、`auth:login:write`、`auth:probe:write`、`auth:logout:write`、`auth:binding:write`，并新增 `AuthControlApplication`。它只依赖 02-02 stores/EvidenceLedger、02-07 `AuthJobService` 及一个窄 `OfficialLoginLauncher.open({source,approvedConfigId,actionReceiptId},signal)` port；不得 import platform、Playwright、Fastify 或 persistence driver。每个 public method 先 strict parse，再检查 principal destination+permission、installation、固定 SourceId/SourceAction、approvedConfigId/approvedScopeId 与当前 binding/courseAccess。`confirmConfiguration` 只允许 paired UI 将完整 `ApprovedSourceConfigSchema` 写入；`openLogin` 只接受 source+config 引用并调用窄 launcher；`requestProbe` 机械映射到 AuthJobService 的三种 trigger，不能接收 retry delay/follow-up/URL/selector；`recordLogoutIntent` 调用 `recordExplicitLogout`；`confirmBinding` 只按 server-side `candidateBindingId` 查当前 candidate 后写 confirm/reject，禁止请求体提交姓名、邮箱、fingerprint 或新身份；`readStatus/readReceipts` 分别调用 protected/redacted presenter。identity mismatch 时所有 probe course action 和 binding overwrite fail closed。将 Error/Zod/driver 异常映射到封闭 `SafeAuthErrorCode`；未知异常永不读取/序列化 message/cause/stack。短指纹只截取/编码完整 keyed fingerprint 的展示摘要，比较仍使用完整 keyed value（D-05–D-07）。不得新增通用 auth operation router、自由字符串 action 或 receipt append mutation；本计划不把 paired click 自动写成 L/pass。
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/output-policy.test.ts tests/unit/auth-contracts.test.ts tests/unit/auth-state.test.ts tests/unit/import-boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `tests/unit/output-policy.test.ts` 对同一双来源内部对象逐 destination 比较：仅 paired local UI projection 含 canonical `officialOrigin`、完整 `displayName`、`schoolEmail`；local_cli/model/log/diagnostic/receipt 序列化结果均零命中敏感 sentinel。
    2. 测试证明 redacted status/receipt 的 key 集合精确等于 schema allowlist；identity fingerprint 每次匹配 `^[A-Z2-7]{12}$`、同一完整 keyed fingerprint 稳定、不同 keyed fingerprint 不被 presenter 用作授权；raw subject/name/email 输入被拒绝。
    3. 带 Profile path、Cookie/token、完整 URL path/query、DOM、request body、course title、screenshot、Error.message/cause/stack sentinel 的 unknown error 只得到 `UNKNOWN_SOURCE_ERROR`/`INTERNAL_ERROR` 与 `stage/nextAction`，JSON 中无 sentinel、message、cause、stack 或 extra key。
    4. `rg -n "export (interface|function|const) (ProtectedAuthStatusProjection|RedactedAuthStatusProjection|RedactedEvidenceReceipt|presentProtectedAuthStatus|presentRedactedAuthStatus|presentEvidenceReceipts|SafeAuthApiErrorSchema)" packages/contracts/src/presentation.ts` 找到全部稳定导出；protected 与 redacted 类型没有 `any`、index signature 或交叉 destination union。
    5. `rg -n "playwright|fastify|better-sqlite3|packages/platform|packages/persistence|Page|BrowserContext|profilePath|storageState|cookies\\(" packages/application/src/policy.ts` 对本任务新增 auth 区块零命中；import-boundary 回归继续通过。
    6. permission 矩阵逐项证明 paired local UI 才可执行五种 mutation；CLI/MCP 只可 readStatus/readReceipts；所有 GET/read 调用后 login-launch/probe enqueue/course-access/store-write 计数均为 0。
    7. 全部测试只使用 synthetic identity/safe code，既不写 EvidenceLedger L、不启动浏览器/学校网络，也不改变 Windows/Phase 3 gate。
  </acceptance_criteria>
  <done>应用层具有单一、可测试的 auth admission 与投影边界：完整身份仅 protected local UI 可达，其他 destination 只有严格脱敏 DTO；未知错误和任意附加字段无法穿透。</done>
</task>

<task type="auto" tdd="true">
  <name>02-08-2：注册七类固定 paired loopback routes 并封死 CSRF/权限/动作伪造</name>
  <read_first>Task 1 完成后的 policy.ts/presentation.ts/output-policy.test.ts；02-CONTEXT.md D-04–D-07、D-14–D-15；02-RESEARCH.md API and UI 与 T2-01/T2-02/T2-07；02-UI-SPEC.md paired/protected/401/403 contract；02-07-SUMMARY.md 与 auth-jobs.ts 的最终 facade；apps/api/src/main.ts、security.ts、pairing.ts；packages/persistence/src/sessions.ts；tests/integration/pairing.test.ts；tests/integration/auth-api.test.ts（若 Wave 0 skeleton 已存在）。</read_first>
  <files>apps/api/src/auth.ts, apps/api/src/security.ts, apps/api/src/main.ts, tests/integration/auth-api.test.ts</files>
  <behavior>
    - RED：只注册 `GET /api/auth/status`、`POST /api/auth/configuration/confirm`、`POST /api/auth/login/open`、`POST /api/auth/probe`、`POST /api/auth/logout-intent`、`POST /api/auth/binding/confirm`、`GET /api/auth/receipts`；未知 method/path、动态 source path、通用 operation route 全部 404/405 且 driver 计数为 0。
    - RED：五个 POST 对 public/pending/CLI/MCP bearer、缺 session、过期/撤销/restart session、missing/wrong CSRF、cross-origin/host/fetch-site 全部 401/403；只有 current paired same-origin session+CSRF 可到 application，且响应 no-store。
    - RED：configuration body 严格 `{config: ApprovedSourceConfig}`；login body 严格 `{source,approvedConfigId}`；probe body 严格 `{source,approvedConfigId,approvedScopeId,trigger,idempotencyKey,actionReceiptId?}`；logout body严格 `{source,acknowledged:true}`；binding body严格 `{candidateBindingId,decision:'confirm'|'reject'}`。任意 url/js/selector/handle/method/body/download/upload/submit/reply/quiz/Cookie/Profile/identity/extra key 在 driver 前 400。
    - RED：login/open 的 actionReceiptId 由 server 生成并绑定 current session id+source+config+generation+kind；只有 login_completed probe 可携带，必须完全匹配并原子消费一次。浏览器自造、另一 session/source/config/generation、重放或 restart 后 ID 全部拒绝且 requestProbe=0；该 ID 不自动生成 L/pass receipt。
    - RED：GET status 对 paired UI 返回 `ProtectedAuthStatusProjection`，对 CLI/MCP 返回 `RedactedAuthStatusProjection`，public/pending 为 401；GET receipts 对所有已授权 destination 只返回 `RedactedEvidenceReceipt[]`，永不返回 protected identity。
    - RED：unknown source/adapter/job/store 异常带页面/path/query/full identity/Profile/Cookie sentinel 时 HTTP 只返回 `SafeAuthApiError`；Fastify logger/console 被禁用，响应无 message/cause/stack，404 也不回显请求 URL。
  </behavior>
  <action>先创建 `tests/integration/auth-api.test.ts`，复用 `pairing.test.ts` 的 actual loopback HTTP harness 与 SQLiteSessions，注入 synthetic stores、AuthJobService、OfficialLoginLauncher spy 和敏感 sentinel；确认 route/permission/privacy 测试 RED。新建 `apps/api/src/auth.ts` 并导出 `registerAuthRoutes(app,deps)`，只显式注册上述七类 route，不使用 `/:source/:action`、catch-all dispatch 或通用 command body。为每个 body/query 定义 `z.strictObject`/02-01 schema，parse 必须先于任何 application/launcher/job/store 调用。`GET /api/auth/receipts` query 必须是完整 `EvidenceCellKey`，没有 wildcard/all-platform/bulk key；它只 list+redact，不提供 receipt append/update/delete route。

每个 POST 先走现有 `assertTransport` hook，再强制请求为 cookie-based `browserPrincipal`；即使有效 CLI/MCP Bearer 拥有 read 权限也返回 403。复用 `sameOrigin()` 和 `SQLiteSessions.authenticate(autoed_session,x-autoed-csrf)`，不得自制第二种 cookie/token/CSRF；在 `apps/api/src/security.ts` 的 destination permission map 中只给 local_cli/model `auth:read`/`auth:receipts:read`，浏览器权限由 `browserPrincipal` 的 paired principal 明确授予五种 auth mutation。所有 auth responses 保持现有 no-store/CSP/nosniff；protected status 只可由 local_ui 分支选择，不能按请求 query/header 强制。

在 auth route closure 中实现有界、进程内、restart-revoked 的 one-time `LoginActionRegistry`：server 使用 `randomUUID()` 签发 ID，仅存 session id、source、approvedConfigId、runtime generation、kind、expiresAt/consumedAt，不存 identity/Cookie/CSRF/Profile/页面资料；login/open 成功后返回 `AuthActionAccepted`。`trigger:'user_login_completed'` 要求 body 的 actionReceiptId 与 registry 完全匹配并在调用 AuthJobService 前原子 consume；background/manual_retry 禁止携带 ID，显式登出/CSRF failure/restart 清除或使其不可用。ID 只是 authenticated human-action correlation，不是用户反馈、live pass 或 `EvidenceWriterAuthority`；本计划不得据此 append L。binding confirm 不接受 actionReceiptId 或 identity payload：只消费 protected status 下发的 opaque current `candidateBindingId`，由 paired mutation本身生成脱敏 action correlation 供后续硬人工计划使用，但不得自动写 pass。

修改 `apps/api/src/main.ts` 将 02-02 stores、02-07 AuthJobService、02-05/02-06 背后的窄 login launcher 作为 `ApiOptions.auth` 的明确依赖传给 `AuthControlApplication/registerAuthRoutes`；production standalone composition 必须使用真实受管实现，测试可注入 synthetic port，缺依赖时 auth routes fail closed/503 而不是使用 dummy pass。保持 MCP/CLI 通过同一 loopback API，绝不直连 DB/Profile/browser。扩展 `safeCodes` 与 error handler，仅 allowlist SafeAuthErrorCode；Zod=INVALID_REQUEST，未知异常=INTERNAL_ERROR，未知来源内部错误=UNKNOWN_SOURCE_ERROR，均返回固定 `stage:'auth_api'`/nextAction，不回显 error、request URL、page、path/query、identity。不得增加 screenshot/upload/diagnostic capture route，也不得让 status/receipt GET 调用 launcher或入队 probe。
  </action>
  <verify>
    <automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-api.test.ts tests/integration/pairing.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/output-policy.test.ts tests/unit/import-boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. actual HTTP 测试逐一得到七类固定 route 的预期响应；`/api/auth/:source/:action`、额外 path、PUT/PATCH/DELETE、通用 operation/receipt write 均 404/405，且 launcher/job/store/ledger write 计数全部为 0。
    2. 五个 POST 的 principal×Origin×CSRF 矩阵证明：仅 paired current session+exact same-origin+correct CSRF 可调用；public、pending、CLI、MCP、cross-origin、missing/wrong CSRF、expired/revoked/restarted session 全部被拒且无副作用。
    3. 每个 strict body 的 forbidden-key 表（url/js/selector/browserHandle/method/requestBody/download/upload/submit/reply/quizStart/cookie/storageState/profilePath/displayName/schoolEmail/courseName/screenshot）逐项返回 400，且 application/launcher/job/store 调用数为 0。
    4. login action 测试证明 ID 仅 server response 产生；正确 session/source/config/generation 可消费恰一次，伪造、错 session/source/config/generation、replay、过期/restart 均拒绝；所有分支 EvidenceLedger.append(L) 计数为 0。
    5. paired GET status 含两个 protected identities；同状态经 CLI/MCP GET 的 JSON 零命中完整姓名/邮箱/origin/course/Profile sentinel，只含 redacted allowlist。GET receipts 对 paired/CLI/MCP 都只满足 `RedactedEvidenceReceipt` schema。
    6. 注入含 sentinel 的 unknown source、adapter throw、job/store error 与未知 URL 后，响应 JSON 仅有 `code/stage/nextAction`，response/header/captured stdout/stderr 中无 page/path/query/identity/Profile/Cookie/message/cause/stack；所有 auth response `cache-control: no-store`。
    7. `rg -n "api/auth/(status|configuration/confirm|login/open|probe|logout-intent|binding/confirm|receipts)" apps/api/src/auth.ts` 只显示固定注册；`rg -n "evaluate|selector|browserHandle|requestBody|storageState|cookies\\(|screenshot|trace|har|video" apps/api/src/auth.ts` 零命中生产能力。
    8. 定向 integration、pairing 回归、unit、typecheck 全部通过且无 skip/todo；无真实学校网络/Playwright/login，未写 N/L/pass，Windows 仍 not_run/human_needed，Phase 3 仍 blocked。
  </acceptance_criteria>
  <done>paired loopback API 具有唯一固定动作面：所有 mutation 受 session+same-origin+CSRF 与细粒度 permission 保护，登录关联不可伪造/重放，protected/redacted 响应按 principal 不可混淆，未知错误不泄密。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | 不可信输入跨越 | 强制控制 |
|---|---|---|
| browser/CLI/MCP → loopback Fastify | Host/Origin/fetch metadata、cookie/Bearer、CSRF、route/body/query 可能伪造或重放 | 127.0.0.1 exact Host、same-origin、paired session/credential、细粒度 permission、strict route/body/query、rate/session expiry |
| fixed route → AuthControlApplication | source/config/scope/action/candidate 引用可能错配，GET 可能被利用触发副作用 | 七类显式 route、无动态 dispatch；application 再授权/核对；read path 调用计数为零副作用 |
| internal auth state → presenter/destination | 完整身份、origin、course、Profile 或错误对象可能被 spread 到 CLI/MCP/receipt | protected/redacted 不相交 schema；allowlist reconstruction；destination 由 authenticated principal 决定，不接受 caller override |
| paired click → login/binding human action | 浏览器可能伪造/replay action ID 或把点击冒充 live pass | server-issued one-time ID 绑定 session/source/config/generation；原子 consume；click 不自动 append L/pass |
| adapter/job/store error → HTTP/log/diagnostic | 异常 message/cause/stack 可能含 page/path/query/identity/Cookie/Profile | safe-code allowlist、固定 SafeAuthApiError、Fastify logger off、sentinel response/stdout tests |
| EvidenceLedger → receipt/API/model | receipt 可能携带完整身份、截图或跨 cell 资料 | 完整 EvidenceCellKey read、RedactedEvidenceReceipt schema、无 auth receipt write route、02-02 append-only authority 继续生效 |

## STRIDE / ASVS L1 威胁登记

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 控制意图 | 具体缓解与硬阻断 |
|---|---|---:|---|---|---|---|
| T2-01 | Spoofing / Tampering / Elevation of Privilege | Critical | auth routes、strict body、AuthControlApplication | mitigate | V1 架构边界、V4 访问控制、V5 输入验证、V13 API/业务逻辑 | 仅七类 fixed route；无 URL/JS/selector/handle/write；配置/来源/范围二次核对；GET 零副作用。任意 forbidden field 可达 launcher/driver、动态 operation route 可调用或源站业务写可表达即为 Critical blocker，阻止 02-09/02-13/live。 |
| T2-02 | Information Disclosure | Critical | presenter、HTTP error、receipt、logs | mitigate | V8 数据保护、V13 受控响应、V16 安全日志/错误 | 完整身份只在 protected DTO；redacted allowlist；无 Profile/Cookie/raw capture；unknown error 固定 schema；敏感 sentinel 扫描。任一 CLI/MCP/receipt/error/log 出现完整身份、page/path/query、Profile/Cookie 或 raw error 即为 Critical blocker。 |
| T2-07 | Spoofing / Information Disclosure / Elevation of Privilege | High | pairing/session/CSRF、protected status、mutation authorization | mitigate | V2 会话、V4 功能级访问控制、V5 CSRF/输入、V14 配置、V16 错误 | paired HttpOnly SameSite session、exact same-origin、mutation CSRF、restart/revoke/expiry、principal-selected presenter；CLI/MCP mutation deny。未配对/过期/跨源能读 protected identity 或执行 mutation 即为 High blocker。 |
| T2-08 | Repudiation / Tampering | High | one-time human action、receipt read | mitigate | V4 authority separation、V13 transaction semantics、V16 audit provenance | server-issued/one-time/session+generation bound action；browser 不提交 authority；本计划无 L append；receipt 精确 cell read。任意客户端自造 actionReceiptId 可写 L/pass、fixture 可填 live/Windows 或 macOS 自动解除 Phase 3 即为 High blocker。 |

ASVS 5.0 L1 仅作为本计划适用控制意图，不声称完整合规。所有 High/Critical 条目都必须由上述自动 negative tests 证明并作为依赖执行硬阻断；不得降为 warning、接受风险、以 UI 隐藏替代 server authorization，或以 synthetic pass 替代 live/native 证据。
</threat_model>

<verification>
1. Task 1、Task 2 均先运行新增 RED 测试并确认因目标行为缺失而失败，再实现 GREEN；不得先加宽泛 route/DTO 后补过滤。
2. unit/contract：`npm run typecheck && npm run test:unit -- --run tests/unit/output-policy.test.ts tests/unit/auth-contracts.test.ts tests/unit/auth-state.test.ts tests/unit/import-boundaries.test.ts`。
3. actual loopback integration：`node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/auth-api.test.ts tests/integration/pairing.test.ts`。
4. Wave 回归：`npm run test:unit -- --run && node scripts/dev/runtime.mjs npm run test:integration -- --run`；无 watch、skip/todo、外部网络、真实 Profile/身份或浏览器。
5. route/副作用审计：七类 route allowlist、所有 forbidden-key/verb/path 测试，以及每个 unauthorized/read case 的 launcher/job/store/ledger-write 调用计数均须为 0。
6. 敏感输出审计：对 protected/redacted/status/receipt/safe-error、captured stdout/stderr 和 HTTP headers/body 递归扫描 full name/email/course/page/path/query/Profile/Cookie/DOM/network/screenshot/stack sentinel；只有 paired protected projection 可出现专门的姓名/邮箱 sentinel。
7. 证据边界：所有测试仅记 S/I，不启动真实登录、不写 N/L/pass；macOS-first 只改变后续执行顺序，Windows 持续 `not_run / human_needed`，Phase 1/2 不整体完成，Phase 3 持续 blocked。
</verification>

<success_criteria>
- AUTH-01：用户未来只能经 paired same-origin local UI 的明确 login route 打开官方登录动作；CLI/MCP/status GET 不能打开窗口或触发探针，请求不能携带凭据/输入/任意浏览器操作。
- AUTH-03：paired UI 可读取两个来源的完整身份与五维独立状态；CLI/MCP/receipt 仅得到严格脱敏投影，identity mismatch 仍硬停课程访问且不覆盖既有绑定。
- SEC-02：API 只表达固定 source/config/login/probe/logout/binding/receipt 行为；URL/JS/selector/handle/download/upload/submit/quiz 等字段、跨源/无 CSRF/非 paired mutations 在 application/driver 前 fail closed。
- UAT-01：结果单 read 保持 platform/source/scenario/evidence 精确键与脱敏字段；server-issued human action correlation 不等于 live pass，本计划不创建 L/Windows/native 证据或推进 hard gate。
- D-04–D-07、D-14–D-15：完整身份只存在于 protected local UI projection；所有其他 destination 只输出 safe code、binding consistency、12 字符不可逆展示 fingerprint、状态/时间/缺口，不默认请求或保存截图。
- T2-01/T2-02/T2-07/T2-08 与 ASVS L1 High/Critical negative tests 全部通过；任一失败阻止 02-09、beta assembly 和 live。
- hard gates 不变：官方登录/MFA、live 账户核对、更新、重启、跨日、reauth 都必须等待后续真实用户动作；Windows 仍 not_run/human_needed，Phase 3 仍 blocked。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-08-SUMMARY.md`，只记录脱敏 S/I 测试命令、固定 route/type 名、安全码与未完成的 beta/live/Windows 缺口；不得记录完整身份、学校/course path、Profile/Cookie、CSRF/session/action token、原始错误、截图或 live pass。
</output>
