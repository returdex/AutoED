---
phase: 02-poc-live
plan: "39"
type: execute
wave: 12
depends_on: ["02-38"]
files_modified:
  - release/phase2-beta-artifacts.json
  - release/phase2-install-prompt.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "Both target archives are assembled and Ed25519-signed from exactly the 02-38 version/commit/tree/build and test-report hash."
    - "The archives include production durable live workflows, complete live-gate and native-evidence commands with recorded hashes; no checkpoint behavior is added after signing."
    - "Manifest, archive closure, license, support matrix and install prompt are exact and sensitive scans remain zero."
  artifacts:
    - path: release/phase2-beta-artifacts.json
      provides: "Locally built, signed and verified artifact receipt"
    - path: release/phase2-install-prompt.md
      provides: "Exact update instructions bound to selected immutable identity"
  key_links:
    - from: release/phase2-test-report.json
      to: release/phase2-beta-artifacts.json
      via: "test report hash and build identity"
      pattern: "testReportSha256|buildId"
    - from: scripts/release/phase2-live-gate.mjs
      to: release/phase2-beta-artifacts.json
      via: "signed member hash and declared checkpoint capability set"
      pattern: "phase2-live-gate|checkpointCapabilities"
    - from: scripts/release/verify-phase2-update-gate.mjs
      to: release/phase2-beta-artifacts.json
      via: "signed member hash and macOS/Windows pre/post capability set"
      pattern: "verify-phase2-update-gate|updateCapabilities"
---

<objective>
Assemble, sign and locally verify the selected unified Moodle+EdStem beta without publishing it.

Purpose: Keep signed assembly separate from quality selection and network publication while guaranteeing the production evidence path and complete gate are in the artifact.
Output: Signed dual-target artifacts, exact prompt and local verification receipt.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-38-SUMMARY.md
@release/phase2-build-selection.json
@release/phase2-test-report.json
@scripts/build/assemble.mjs
@scripts/release/preflight.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble and sign exact dual-target archives</name>
  <files>release/phase2-beta-artifacts.json</files>
  <behavior>
    - darwin-arm64 and win32-x64 manifests bind the same version/commit/tree/build/test report and complete capability set.
    - Gate/native command hashes and all durable workflow production files are members of both archives.
    - Wrong/missing member, signature, trust root, dependency closure, license or support state blocks local receipt creation.
  </behavior>
  <action>Consume the source-complete assembly tooling from02-41 without editing it. Package the complete production workflows from02-35/36, full live/update gates and native-evidence command. Record their hashes and all A1/A2/B1/B2/B3/C/D/reauth/audit/final capabilities in each manifest. Use only the approved Keychain Ed25519 key and fingerprint; do not expose private material or accept alternate-key/skip-sign flags. Verify both archives’ exact members, hashes, signatures, dependency closure, target identity, unchanged license and support gaps before writing the artifact receipt.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "signed assembly|checkpoint capability closure|artifact scan"</automated></verify>
  <done>Both immutable target archives contain the same fully tested production evidence and gate implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate exact install prompt and final local preflight</name>
  <files>release/phase2-install-prompt.md, release/phase2-beta-artifacts.json</files>
  <action>Generate the prompt only from strict 02-38/39 receipts. Include exact version/tag/build/fingerprint, target URLs/names/hashes, update/reload/readiness expectations, checkpoint capabilities, Windows not_run/human_needed and publication-not-live wording. Run artifact/archive/prompt sensitive scans and immutable repository/version preflight again. Any tree/version/identity drift invalidates all outputs and returns to 02-38.</action>
  <verify><automated>node scripts/dev/runtime.mjs node scripts/release/preflight.mjs --phase2 release/phase2-beta-artifacts.json --prompt release/phase2-install-prompt.md --read-only</automated></verify>
  <done>Signed local artifacts and exact prompt are ready for immutable publication, with no live or update claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test report → artifacts | Build must not drift after quality binding. |
| signing key → manifests | Only approved protected key may authorize release bytes. |
| archive/prompt → user | Missing or sensitive members must fail before publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-39-01 | T/R | assembly identity | mitigate | Exact selection/report hashes and archive closure checks. |
| T2-39-02 | S/I | release signing | mitigate | Fixed Keychain Ed25519 identity and no export/override. |
| T2-39-03 | I | archives/prompt | mitigate | Member allowlist and sensitive scans before receipt. |
</threat_model>

<verification>
Run targeted assembly tests and preflight. Confirm no network publication, installed update, browser/source access or EvidenceLedger L write occurred.
</verification>

<success_criteria>
- Each signed target contains complete prerelease production behavior.
- Artifact identity is exactly the one selected and tested in 02-38.
- No release plan touches more than ten files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-39-SUMMARY.md` with artifact hashes/counts and remaining native/live gaps.
</output>
