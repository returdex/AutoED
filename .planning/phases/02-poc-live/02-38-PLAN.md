---
phase: 02-poc-live
plan: "38"
type: execute
wave: 11
depends_on: ["02-10", "02-11", "02-12", "02-36", "02-37", "02-41"]
files_modified:
  - packages/platform/src/installation.ts
  - packages/platform/src/client-host.ts
  - packages/installer/src/launchers.ts
  - packages/installer/src/ownership-recovery.ts
  - packages/installer/src/install.ts
  - tests/integration/installation-ownership-recovery.test.ts
  - scripts/release/phase2-rehearsal.mjs
  - scripts/release/phase2-gate.mjs
  - scripts/release/phase2-rehearsal-reporter.mjs
  - scripts/release/sensitive-scan.mjs
  - scripts/release/preflight.mjs
  - scripts/build/assemble.mjs
  - packages/installer/src/archive-core.ts
  - tests/integration/bootstrap.test.ts
  - tests/integration/artifact-assembly.test.ts
  - tests/integration/release-gates.test.ts
  - tests/integration/phase2-release-gates.test.ts
  - .planning/release-rehearsals/{source-commit}-{build-id}.json
  - release/phase2-test-report.json
  - release/phase2-build-selection.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "For every corrective candidate after beta.31, an unnumbered internal rehearsal passes under `.planning/RELEASE-STABILIZATION.md` before a beta.N is observed or selected; beta.31 is the sole grandfathered historical execution."
    - "One unoccupied immutable beta version and one commit/tree/build identity are selected before quality execution and remain fixed through assembly/publication."
    - "Typecheck, full unit/integration/UI/macOS-native suites, complete live-gate branch tests, native-evidence tests and sensitive scans all pass on that identity."
    - "Any version race, source/tree drift, failed/partial test, skip/todo, sensitive match or missing gate branch invalidates the selection and requires a new version and full rerun."
    - "A published candidate whose real updater exposes a reproducible installation-identity defect is immutable HUMAN_PRODUCT history; recovery is implemented only in a later signed updater after R0/R1, never by rewriting the installed receipt or retrying the failed candidate."
  artifacts:
    - path: release/phase2-build-selection.json
      provides: "Single immutable version/commit/tree/build selection"
    - path: release/phase2-test-report.json
      provides: "Build-bound complete automated evidence report"
    - path: scripts/release/phase2-gate.mjs
      provides: "Quality/build binding gate only"
  key_links:
    - from: .planning/release-rehearsals/{source-commit}.json
      to: release/phase2-build-selection.json
      via: "post-beta.31 exact-source rehearsal digest; beta.31 is grandfathered"
      pattern: "sourceCommit|sourceTree|status|digest"
    - from: release/phase2-build-selection.json
      to: release/phase2-test-report.json
      via: "exact version/commit/tree/build equality"
      pattern: "version|commit|tree|buildId"
---

<objective>
For a post-beta.31 corrective run, stabilize the complete release path under an unnumbered rehearsal first; then select the one immutable Phase 2 beta identity and bind the complete automated quality/security evidence to it. The completed beta.31 historical run is unchanged.

Purpose: Split quality/build binding from assembly and publication while ensuring the newly complete production live workflow and gate are tested before signing.
Output: Build selection, quality gate and test report. No signing, publishing, update or live work.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-10-SUMMARY.md
@.planning/phases/02-poc-live/02-11-SUMMARY.md
@.planning/phases/02-poc-live/02-12-SUMMARY.md
@.planning/phases/02-poc-live/02-36-SUMMARY.md
@.planning/phases/02-poc-live/02-37-SUMMARY.md
@.planning/phases/02-poc-live/02-41-SUMMARY.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/RELEASE-STABILIZATION.md
@scripts/release/phase2-gate.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 0: Post-beta.31 unnumbered release rehearsal and failure classification</name>
  <files>packages/platform/src/installation.ts, packages/platform/src/client-host.ts, packages/installer/src/launchers.ts, packages/installer/src/ownership-recovery.ts, packages/installer/src/install.ts, tests/integration/installation-ownership-recovery.test.ts, scripts/release/phase2-rehearsal.mjs, scripts/release/phase2-rehearsal-reporter.mjs, scripts/release/sensitive-scan.mjs, scripts/release/preflight.mjs, scripts/release/phase2-gate.mjs, scripts/build/assemble.mjs, packages/installer/src/archive-core.ts, tests/integration/bootstrap.test.ts, tests/integration/artifact-assembly.test.ts, tests/integration/release-gates.test.ts, tests/integration/phase2-release-gates.test.ts, .planning/release-rehearsals/{source-commit}-{build-id}.json (sanitized planning evidence only; required only for corrective candidates after beta.31)</files>
  <action>Do not reserve, observe as active, tag or write a new `beta.N` selection yet. For the beta.44 HUMAN_PRODUCT recovery approved on 2026-09-11, first replace the volatile schema-1 macOS device equality with a schema-2 durable local-root identity. The later signed installer may offer a one-time migration only when device is the sole legacy mismatch and the canonical protected local root, inode/UID, installation/active/launcher identities, old release signature, complete program/Node/browser closures, exact exited process/client inventories, absence of pending update/launch locks and installer credential digest all pass. It must present a bounded recovery preview and receive an exact confirmation before an atomic schema migration; any inode, UID, path, volume, permission, installation ID, manifest, signature, closure, launcher, credential, process, client-host or interruption mismatch remains fail-closed. The current controller must not inspect credentials, rewrite the real receipt, update beta.19, or retry beta.44. New launchers validate schema 2 while schema 1 remains readable only when its original complete ownership tuple still matches or through this one migration path. Add focused positive/negative/interruption tests and preserve data/Profile canaries without reading their contents in production recovery. This is the only source/test/tool editing stage in the corrective chain, and any edit requires restarting R1 from its exact resulting source. Preserve the existing machine-enforced rehearsal and selection contracts: every version greater than beta.31 still requires exact rehearsal digest/source/tree/build equality, managed runtime, fixed-port/process/upgrade/cleanup determinism, complete five-suite/zero-disabled/sensitive gate, real dual-target dependency/member closure rehearsal, prompt self-reference/schema checks, GitHub absent-tag/public-versus-consumed fixtures, volatile receipt semantics and bounded CDN-readiness/one-verifier tests. Rehearsal uses no public release coordinates, release-key signing, remote mutation, canonical release receipt, update, source access or live evidence. Classify every failure using the policy and write only a strict sanitized rehearsal attestation after the complete R1 conjunction passes.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run typecheck &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/bootstrap.test.ts tests/integration/artifact-assembly.test.ts tests/integration/release-gates.test.ts tests/integration/phase2-release-gates.test.ts &amp;&amp; node scripts/dev/runtime.mjs npm run test:unit -- --run &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run &amp;&amp; node scripts/dev/runtime.mjs npm run test:ui &amp;&amp; node scripts/dev/runtime.mjs npm run test:native -- --run</automated></verify>
  <done>The exact proposed source and release lifecycle are stable before any later beta number is selected; later selection is machine-blocked without matching rehearsal evidence, while historical beta.31 remains readable; the attestation contains only bounded hashes/counts and no private/runtime/live data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Select and lock one immutable beta identity</name>
  <files>release/phase2-build-selection.json</files>
  <behavior>
    - Local tags, remote tags, releases, assets and prior receipts agree on max beta N; select only N+1.
    - Repository ID, returdex identity, remote, trust root, license, commit and clean tree are exact.
    - Existing/conflicting version or later drift invalidates the selection; no overwrite/reuse/force path exists.
  </behavior>
  <action>For beta.31 historical execution, preserve the existing immutable receipts. For every later corrective run, first require Task 0's exact-source rehearsal attestation and bind its digest into the selection; missing, failed, differently sourced or stale rehearsal evidence blocks selection. Then use the `02-PATTERNS.md` mappings for `phase2-gate.mjs` and the generated build-selection artifact: consume the source-complete gate from02-41 without editing it, and use only its strict public identity schema. Select the unused beta and write a strict selection receipt containing repository/version/commit/tree/build, rehearsal digest and observed version-set digests. Preserve Phase1 trust/license/identity rules. Source/test/tool/artifact drift after this point invalidates the receipt. A proven pre-publication runner/transient failure follows `RELEASE-STABILIZATION.md` rather than automatically consuming the version; any ambiguity, recurrence, artifact drift or public object still invalidates it. Never mutate an existing remote object.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "quality gate binds exact selection|immutable publication rejects version races|update handoff is read-only"</automated></verify>
  <done>Exactly one unused beta identity is locked for all downstream receipts.</done>
</task>

<task type="auto">
  <name>Task 2: Run and bind the complete quality/security gate</name>
  <files>release/phase2-test-report.json</files>
  <action>Use the generated test-report contract mapping in `02-PATTERNS.md`: store command/source hashes, bounded counts and gaps only, never captured raw output or live evidence. Run `npm run typecheck`, full unit, integration, UI and native scripts, including `phase2-live-gate.test.ts`, durable live workflow tests and native-evidence negatives. Run tracked/history, working-tree and captured-output sensitive scans. Record command hashes/results and exact source/build selection only after every command succeeds and no skip/todo/only or forbidden output exists. Record Windows native/live as not_run; macOS automated/native pass does not create L or Phase 3 eligibility.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run test:unit -- --run &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run &amp;&amp; npm run test:ui &amp;&amp; node scripts/dev/runtime.mjs npm run test:native -- --run</automated></verify>
  <done>The complete prerelease suite and scans are bound to the selected identity with no live claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository/version observations → selection | Remote/local state may race or conflict. |
| workspace/tests → report | Partial or stale results must not be bound to a release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-38-01 | S/T | release identity | mitigate | Exact repo/identity/version-set checks and immutable N+1 selection. |
| T2-38-02 | T/R | quality report | mitigate | Exact command/source hashes, full suites, no skip/todo and drift invalidation. |
| T2-38-03 | I | scans/reports | mitigate | Safe summaries only and four-surface sensitive scanning. |
</threat_model>

<verification>
Validate the strict selection/report schemas and rerun the complete automated command. No signing or publishing is permitted in this plan.
</verification>

<success_criteria>
- One version/build identity is selected and remains immutable.
- All production workflow and verifier code is present and green before signing.
- Windows/live gaps remain explicit and blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-38-SUMMARY.md` with only public identity, command hashes/counts and gaps.
</output>
