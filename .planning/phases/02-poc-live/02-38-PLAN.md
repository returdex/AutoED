---
phase: 02-poc-live
plan: "38"
type: execute
wave: 11
depends_on: ["02-10", "02-11", "02-12", "02-36", "02-37", "02-41"]
files_modified:
  - release/phase2-test-report.json
  - release/phase2-build-selection.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-02, UAT-01]
must_haves:
  truths:
    - "One unoccupied immutable beta version and one commit/tree/build identity are selected before quality execution and remain fixed through assembly/publication."
    - "Typecheck, full unit/integration/UI/macOS-native suites, complete live-gate branch tests, native-evidence tests and sensitive scans all pass on that identity."
    - "Any version race, source/tree drift, failed/partial test, skip/todo, sensitive match or missing gate branch invalidates the selection and requires a new version and full rerun."
  artifacts:
    - path: release/phase2-build-selection.json
      provides: "Single immutable version/commit/tree/build selection"
    - path: release/phase2-test-report.json
      provides: "Build-bound complete automated evidence report"
    - path: scripts/release/phase2-gate.mjs
      provides: "Quality/build binding gate only"
  key_links:
    - from: release/phase2-build-selection.json
      to: release/phase2-test-report.json
      via: "exact version/commit/tree/build equality"
      pattern: "version|commit|tree|buildId"
---

<objective>
Select the one immutable Phase 2 beta identity and bind the complete automated quality/security evidence to it.

Purpose: Split quality/build binding from assembly and publication while ensuring the newly complete production live workflow and gate are tested before signing.
Output: Build selection, quality gate and test report. No signing, publishing, update or live work.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-poc-live/02-10-SUMMARY.md
@.planning/phases/02-poc-live/02-11-SUMMARY.md
@.planning/phases/02-poc-live/02-12-SUMMARY.md
@.planning/phases/02-poc-live/02-36-SUMMARY.md
@.planning/phases/02-poc-live/02-37-SUMMARY.md
@.planning/phases/02-poc-live/02-41-SUMMARY.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@scripts/release/phase2-gate.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select and lock one immutable beta identity</name>
  <files>release/phase2-build-selection.json</files>
  <behavior>
    - Local tags, remote tags, releases, assets and prior receipts agree on max beta N; select only N+1.
    - Repository ID, returdex identity, remote, trust root, license, commit and clean tree are exact.
    - Existing/conflicting version or later drift invalidates the selection; no overwrite/reuse/force path exists.
  </behavior>
  <action>Use the `02-PATTERNS.md` mappings for `phase2-gate.mjs` and the generated build-selection artifact: consume the source-complete gate from02-41 without editing it, and use only its strict public identity schema. Select the unused beta and write a strict selection receipt containing repository/version/commit/tree/build and observed version-set digests. Preserve Phase1 trust/license/identity rules. A race or any source drift invalidates the receipt and requires a newly selected version followed by this entire plan; never mutate an existing remote object.</action>
  <verify><automated>node scripts/dev/runtime.mjs npm run test:integration -- --run tests/integration/phase2-release-gates.test.ts -t "build selection|version conflict|identity binding"</automated></verify>
  <done>Exactly one unused beta identity is locked for all downstream receipts.</done>
</task>

<task type="auto">
  <name>Task 2: Run and bind the complete quality/security gate</name>
  <files>release/phase2-test-report.json</files>
  <action>Use the generated test-report contract mapping in `02-PATTERNS.md`: store command/source hashes, bounded counts and gaps only, never captured raw output or live evidence. Run `npm run typecheck`, full unit, integration, UI and native scripts, including `phase2-live-gate.test.ts`, durable live workflow tests and native-evidence negatives. Run tracked/history, working-tree and captured-output sensitive scans. Record command hashes/results and exact source/build selection only after every command succeeds and no skip/todo/only or forbidden output exists. Record Windows native/live as not_run; macOS automated/native pass does not create L or Phase 3 eligibility.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run test:unit -- --run &amp;&amp; node scripts/dev/runtime.mjs npm run test:integration -- --run &amp;&amp; npm run test:ui &amp;&amp; node scripts/dev/runtime.mjs npm run test:native -- --run</automated></verify>
  <done>The complete prerelease suite and scans are bound to the selected identity with no live claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository/version observations → selection | Remote/local state may race or conflict. |
| workspace/tests → report | Partial or stale results must not be bound to a release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-38-01 | S/T | release identity | mitigate | Exact repo/identity/version-set checks and immutable N+1 selection. |
| T2-38-02 | T/R | quality report | mitigate | Exact command/source hashes, full suites, no skip/todo and drift invalidation. |
| T2-38-03 | I | scans/reports | mitigate | Safe summaries only and four-surface sensitive scanning. |
</threat_model>

<verification>
Validate the strict selection/report schemas and rerun the complete automated command. No signing or publishing is permitted in this plan.
</verification>

<success_criteria>
- One version/build identity is selected and remains immutable.
- All production workflow and verifier code is present and green before signing.
- Windows/live gaps remain explicit and blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/02-poc-live/02-38-SUMMARY.md` with only public identity, command hashes/counts and gaps.
</output>
