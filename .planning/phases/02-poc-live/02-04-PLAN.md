---
phase: 02-poc-live
plan: "04"
type: tdd
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/application/src/auth.ts
  - tests/unit/auth-state.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - SEC-02
must_haves:
  truths:
    - "一次双来源认证循环总是先核验 Moodle；只有 Moodle 具有 approved-origin match、明确正面认证标志与可绑定身份结果后，状态机才产生 EdStem 只读 auth probe effect。"
    - "Moodle 与 EdStem 各自保留 auth、capability、health、freshness、completeness、当前结果与 last-success；一个来源失败不得抹除或降级另一个仍有效来源。"
    - "相同显示名或学校邮箱不能自动绑定；严格候选必须同时具有可核对的稳定/统一主体、组织或租户与批准范围证据，且首次 live 仍需用户确认一次。"
    - "D-06: 两来源身份不一致立即进入 identity_mismatch，阻止课程访问与继续采集，不得自动切号、登出或覆盖旧绑定；已配对本地管理页可并列完整身份供用户核对。严格证据不足进入一次人工确认待办；已确认身份发生变化或两来源证据冲突立即进入 identity_mismatch，阻止所有课程访问且不得自动切号、登出或覆盖旧绑定。"
    - "显式登出只将该来源置为 unauthenticated 并要求用户重新发起登录，绝不自动恢复；自然过期、网络失败、parser 变化和权限拒绝具有不同 code、重试资格与状态影响。"
    - "自然过期/暂时网络失败最多产生立即、5 秒、30 秒三次有界只读恢复决策；parser、权限、身份不一致或需要 MFA/交互立即停止自动恢复，任何失败都保留 last-success。"
    - "本计划只产生 S 单元证据，不写 live receipt、不改变 Windows not_run/human_needed，也不解除 Phase 3 双平台硬门禁。"
  artifacts:
    - path: "packages/application/src/auth.ts"
      provides: "双来源认证、绑定、失败隔离与有界恢复的纯 application reducer"
      exports:
        - createAuthFlowState
        - reduceAuthFlow
        - decideAccountBinding
        - AUTH_RECOVERY_DELAYS_MS
    - path: "tests/unit/auth-state.test.ts"
      provides: "状态转移、绑定、顺序、失败分类、last-success 与禁止 effects 的表驱动 S 证据"
      contains: "describe('dual-source auth state machine'"
  key_links:
    - from: "packages/application/src/auth.ts"
      to: "packages/domain/src/model.ts"
      via: "只消费 02-01 的 SourceObservation、SourceProbeResult、IdentityEvidence、AccountBinding 与 SourceId"
      pattern: "import type .*SourceObservation|SourceProbeResult|AccountBinding"
    - from: "packages/application/src/auth.ts"
      to: "packages/application/src/ports.ts"
      via: "effects 仅引用 SourceProbePort 可表达的固定 SourceAction；reducer 本身不执行 IO"
      pattern: "moodle\\.auth_probe|edstem\\.auth_probe"
    - from: "tests/unit/auth-state.test.ts"
      to: "packages/application/src/auth.ts"
      via: "RED→GREEN 表驱动事件序列验证每次转移及 effect 顺序"
      pattern: "reduceAuthFlow|decideAccountBinding"
---

<objective>
以测试先行交付 Moodle→EdStem 双来源认证与账户绑定的纯 application 状态机，准确区分认证、能力、健康、时效、完整性、身份和恢复状态，并在任何身份冲突或权限边界不明时 fail closed。

Purpose: 落实 D-02、D-06–D-08、D-10–D-11 与 AUTH-01/AUTH-03/SEC-02，在 BrowserProvider、sealed adapter、Worker 和 UI 接入前固定 T2-03 身份误绑定及 T2-06 状态混淆的不可回退行为。
Output: `packages/application/src/auth.ts` 的纯 reducer/绑定决策与 `tests/unit/auth-state.test.ts` 的完整 RED→GREEN→REFACTOR 证据。此计划不打开浏览器、不访问学校、不持久化、不发布、不登录、不写 N/L 结果。
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/VALIDATION-STRATEGY.md
@.planning/phases/02-poc-live/02-CONTEXT.md
@.planning/phases/02-poc-live/02-RESEARCH.md
@.planning/phases/02-poc-live/02-VALIDATION.md
@.planning/phases/02-poc-live/02-PATTERNS.md
@.planning/phases/02-poc-live/02-UI-SPEC.md
@.planning/phases/02-poc-live/02-PLAN-OUTLINE.md
@.planning/phases/02-poc-live/02-01-PLAN.md
@.planning/phases/02-poc-live/02-01-SUMMARY.md
@packages/application/src/policy.ts
@packages/application/src/job-runner.ts
@tests/unit/job-state.test.ts
@tests/unit/output-policy.test.ts

<interfaces>
02-01 执行后必须以实际导出为准；若 SUMMARY 记录的名字与下列契约不同，只做机械适配，不得放宽语义：

```typescript
export type SourceId = 'moodle' | 'edstem';
export type SourceAction =
  | 'moodle.auth_probe'
  | 'edstem.auth_probe'
  | 'moodle.course_visibility_probe'
  | 'edstem.course_visibility_probe';

export interface SourceProbePort {
  probe(request: SourceProbeRequest, signal: AbortSignal): Promise<SourceProbeResult>;
}

export interface SourceObservation {
  source: SourceId;
  auth: 'not_observed' | 'authenticated' | 'unauthenticated' | 'reauth_required' | 'identity_mismatch';
  capability: 'unknown' | 'available' | 'unavailable' | 'denied';
  health: 'not_observed' | 'healthy' | 'degraded' | 'error';
  freshness: 'not_observed' | 'fresh' | 'stale';
  completeness: 'not_observed' | 'complete' | 'partial';
  checkedAt: string | null;
  currentResultCode: string | null;
  lastSuccess: SourceProbeResult | null;
}
```

本计划在 `packages/application/src/auth.ts` 中稳定导出以下纯接口；后续 02-07/02-08 直接调用，不得改成 driver callback 或 transport DTO：

```typescript
export const AUTH_RECOVERY_DELAYS_MS = [0, 5_000, 30_000] as const;

export type AuthEffect =
  | { kind: 'probe'; source: 'moodle' | 'edstem'; action: 'moodle.auth_probe' | 'edstem.auth_probe' }
  | { kind: 'schedule_recovery_probe'; source: SourceId; attempt: 1 | 2 | 3; delayMs: 0 | 5000 | 30000 }
  | { kind: 'pause_source'; source: SourceId; reason: AuthResultCode }
  | { kind: 'require_user_action'; source: SourceId; reason: 'login' | 'reauth' | 'interaction' | 'binding_confirmation' };

export interface AuthTransition {
  state: AuthFlowState;
  effects: readonly AuthEffect[];
}

export function createAuthFlowState(input?: Partial<AuthFlowState>): AuthFlowState;
export function reduceAuthFlow(state: AuthFlowState, event: AuthEvent): AuthTransition;
export function decideAccountBinding(input: BindingDecisionInput): BindingDecision;
```

`AuthEffect` 绝不包含 URL、JS、selector、browser handle、下载、业务写、凭据输入、Cookie/Profile 操作、evidence receipt、平台完成或 Phase 3 eligibility；IO、持久化和计时分别由后续 sealed adapter/Worker/store 实现。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>02-04-1：用 RED→GREEN→REFACTOR 固定双来源认证与绑定状态机</name>
  <read_first>AGENTS.md 的 Credentials and login、Source and content policy、Data integrity and verification；.planning/phases/02-poc-live/02-CONTEXT.md D-02、D-06–D-08、D-10–D-11、D-17–D-18；.planning/phases/02-poc-live/02-RESEARCH.md Recommended Architecture §§3–4、T2-03/T2-06、Common Pitfalls 与 Validation Architecture；.planning/phases/02-poc-live/02-VALIDATION.md Probe/binding、Retry/retention、Hard Gates；.planning/phases/02-poc-live/02-PATTERNS.md 的 `packages/application/src/auth.ts`、`tests/unit/auth-state.test.ts`、Transactions/last-success/fences、Evidence/hard gates；.planning/phases/02-poc-live/02-UI-SPEC.md Interaction State Machine 与错误文案语义；.planning/phases/02-poc-live/02-01-SUMMARY.md 及其实际新增的 packages/domain/src/model.ts、packages/contracts/src/index.ts、packages/application/src/ports.ts；现有 packages/application/src/policy.ts、packages/application/src/job-runner.ts、tests/unit/job-state.test.ts。</read_first>
  <files>packages/application/src/auth.ts, tests/unit/auth-state.test.ts</files>
  <behavior>
    - RED 1—顺序与正面证据：`start_probe_cycle` 只产生 `moodle.auth_probe`；仅 approved origin match + explicit positive marker + authenticated structured result 可继续 `edstem.auth_probe`。URL 可达或 HTTP 200 不是事件/成功证据；额外交互/MFA 产生 human action 并停止自动流程。
    - RED 2—严格绑定：两来源稳定统一主体、组织/租户和批准 scope 全部一致时仅产生 `candidate`，仍需首次用户确认；缺少跨来源稳定证据时产生 `manual_confirmation_required`；同名/同邮箱绝不构成候选；任一稳定主体、组织/租户、批准 scope 或既有 confirmed fingerprint 变化产生 `identity_mismatch`。
    - RED 3—全局硬停止：`identity_mismatch` 将全局 `courseAccess` 置为 `blocked`，两来源都不得产生 course visibility probe；不自动切号、登出、覆盖 confirmed binding 或清除两边身份，只有重新观察并再次人工确认的后续事件才可能解除。
    - RED 4—来源隔离：Moodle/EdStem 任一侧 unauthenticated、expired、network、parser 或 capability denied 时只更新该 source；另一侧 observation、lastSuccess、auth 与 capability 深度不变。Moodle reauth 成功后产生一个新的 EdStem auth probe，而不是复制会话或把 EdStem 直接改 authenticated。
    - RED 5—失败分类与保留：explicit logout→unauthenticated + manual login、零自动恢复；natural expiry→reauth_required + 最多三次只读恢复资格；network→NETWORK_UNAVAILABLE、health error/freshness stale、auth 不被改写；parser→PARSER_CHANGED、不能解释为登出且立即暂停；permission→CAPABILITY_DENIED、auth 可保持 authenticated 但 capability denied；所有失败保留上一 successful probe/identity/checkedAt。
    - RED 6—有界恢复：仅 natural expiry 与明确 temporary network failure按 `[0,5000,30000]` 产生 attempt 1/2/3；第三次仍失败后只产生 pause/require reauth，不再 schedule。parser、permission、identity_mismatch、unknown origin、interaction/MFA 和 explicit logout 从不进入自动恢复。
    - RED 7—硬门禁与证据：reducer effects 中不存在 live/native receipt、Windows cell、平台 pass、Phase completion 或 Phase 3 eligibility；全部测试仅为 S，不可将 synthetic 状态当真实登录/绑定。
  </behavior>
  <action>RED：先创建 `tests/unit/auth-state.test.ts`，以 immutable table-driven event sequences 覆盖上述七组行为；每个失败类型同时断言当前 source 的五维状态、result code、retry decision、lastSuccess 与另一 source 完全不变。对 binding 使用无敏感值的短 fingerprint fixtures，分别覆盖严格 candidate、证据不足人工确认、仅同名/邮箱、组织不一致、scope 不一致、confirmed identity drift 和 mismatch 后重复事件；确认测试因缺少实现而失败后原子提交 `test(02-04): add failing dual-source auth state tests`。GREEN：创建纯 `packages/application/src/auth.ts`，只 import domain/contracts/application ports 的 types；实现 `createAuthFlowState`、`reduceAuthFlow`、`decideAccountBinding` 与常量 `[0,5000,30000]`。状态必须保存 Moodle/EdStem 两个独立 source slots、binding、全局 courseAccess、当前 sequence step 和 per-source recovery attempt；所有转移返回新对象，不原地修改输入。将探针结果分成正面成功、AUTH_REQUIRED、REAUTH_REQUIRED、NETWORK_UNAVAILABLE、PARSER_CHANGED、CAPABILITY_DENIED、INTERACTION_REQUIRED、ORIGIN_MISMATCH、IDENTITY_MISMATCH 的封闭分支；未知 code fail closed 为 source paused/unknown，不把异常字符串或页面内容透传。只有完整正面证据能推进 Moodle→EdStem；仅 confirmed binding 且两边当前 authenticated 时才可把 auth-only course visibility 置为 eligible，但本计划不得实际读取课程或生成 course probe driver call。显式登出设置持久的 logoutIntent，只有明确 `user_requested_login` 事件可清除；普通启动/retry 不能清除。自然过期/网络恢复成功更新 current+lastSuccess 并清 retry；失败永不清空 lastSuccess。Moodle reauth 的成功 effect 必须是新的 `edstem.auth_probe`，不能拷贝 Moodle 认证结论；全部目标测试通过后原子提交 `feat(02-04): implement dual-source auth state machine`。REFACTOR：提取总函数式 helper 和 exhaustive `never` 检查，保持 `auth.ts` 无 Playwright、Fastify、filesystem、SQLite、timer、process、Profile、Cookie、DOM 或 transport imports；不修改 02-01 契约文件；若产生实质重构，重复全套测试后独立提交 `refactor(02-04): clarify auth transition invariants`。不得实现实际等待、Job、store write、页面 marker selector、UI 文案、release/live 或人工确认模拟。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/auth-state.test.ts tests/unit/auth-contracts.test.ts tests/unit/import-boundaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `npm run test:unit -- --run tests/unit/auth-state.test.ts` 通过至少以下表驱动断言：Moodle→EdStem 严格顺序；origin/positive-marker 缺一不推进；MFA/interaction 立即停止；strict candidate、manual confirmation、same-name/email rejection、identity drift mismatch；mismatch 后所有 course access blocked。
    2. 同一测试命令证明 explicit logout 的 schedule 数为 0；natural expiry/network 的 delay 严格为 `0, 5000, 30000` 且不出现第 4 次；parser/permission/mismatch/origin/interaction 的 schedule 数为 0；Moodle reauth 成功只产生新的 EdStem probe，不直接更改 EdStem auth。
    3. 对每个 AUTH_REQUIRED、REAUTH_REQUIRED、NETWORK_UNAVAILABLE、PARSER_CHANGED、CAPABILITY_DENIED、INTERACTION_REQUIRED、ORIGIN_MISMATCH、IDENTITY_MISMATCH 用例，测试读取失败前后的对象并证明 `lastSuccess` 未丢失；单来源失败时另一来源对象 `toEqual` 原值。
    4. `rg -n "export (const AUTH_RECOVERY_DELAYS_MS|function createAuthFlowState|function reduceAuthFlow|function decideAccountBinding|type AuthEffect|interface AuthTransition)" packages/application/src/auth.ts` 找到全部公开契约；`rg -n "moodle\\.auth_probe|edstem\\.auth_probe" packages/application/src/auth.ts` 只显示固定只读 auth effects。
    5. `rg -n "playwright|better-sqlite3|fastify|node:fs|node:child_process|setTimeout|setInterval|profilePath|cookie|storageState|selector|browserHandle|download|upload|submit|quiz|evidenceReceipt|phase3Eligibility" packages/application/src/auth.ts` 零命中；`npm run test:unit -- --run tests/unit/import-boundaries.test.ts` 继续通过。
    6. `npm run typecheck &amp;&amp; npm run test:unit -- --run` 全部通过且无 skip/todo；测试没有连接外部网络、启动 Playwright、读写 SQLite/Profile/runtime、创建 receipt 或宣称 N/L/pass。Windows 仍 `not_run / human_needed`，Phase 3 仍 blocked。
  </acceptance_criteria>
  <done>纯状态机可确定性地驱动后续 Worker/API：仅正面证据推进 Moodle→EdStem；绑定不充分须一次人工确认，冲突硬阻所有课程访问；各类失败和重试语义不混淆、last-success 不丢失、另一来源不受污染。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Required behavior |
|---|---|---|
| sealed adapter result → pure auth reducer | 来源页面是不可信数据；02-01/02-06 将其收敛为结构化结果，本 reducer 仍不得把 reachability/200 当登录 | 只有 approved-origin match + positive marker + typed identity 才可 authenticated/推进 |
| Moodle identity ↔ EdStem identity | 两来源可共享 SSO/Profile，但不等于同一账户、组织或授权 scope | 严格证据只建 candidate；首次人工确认；冲突/变化 hard stop |
| current failure → retained success | 网络/parser/权限/过期是不同事实，不能伪造登出、清空旧成功或改变另一来源 | typed transition、per-source isolation、lastSuccess immutable retention |
| auth reducer effects → Worker/adapter/API | effect 是后续 IO 的授权上限 | 只允许固定 auth probe、bounded schedule、pause 与 human action；无浏览器通用操作、源站写入或 gate/evidence 写入 |

## STRIDE Threat Register

| Threat ID | STRIDE | Severity | Component | Disposition | ASVS L1 applicability | Mitigation plan | Blocking verification |
|---|---|---:|---|---|---|---|---|
| T2-01（继承） | T/E | Critical | `AuthEffect` 与未知 probe-result 分支 | mitigate | ASVS 5.0 L1：V1 架构边界、V2 输入验证、V4 API/访问控制、V8 授权 | 仅接受 02-01 strict typed result；effect union 只含固定 auth probe/schedule/pause/human action，未知结果 fail closed，不新增 URL/JS/selector/write/download 能力 | forbidden-effect grep、unknown-result test 与 import-boundary 任一失败即 blocker；此计划不替代 02-01/02-06 对 T2-01 的主要验证 |
| T2-03 | S/T/E | High | `decideAccountBinding`、identity drift transition | mitigate | ASVS 5.0 L1：V1 架构边界、V2 输入验证、V6/V7 身份认证、V8 授权 | 稳定统一主体+组织/租户+批准 scope；同名/邮箱无效；candidate 仍需人工确认；mismatch 全局 course block且不覆盖旧 binding | strict/manual/same-name/mismatch/drift 表驱动 tests 任一失败即 blocker，不得接入 Worker/API/live |
| T2-06 | S/T/R/I | High | per-source observation、retry/retention reducer | mitigate | ASVS 5.0 L1：V1/V2 完整性与状态验证、V8 授权状态、V14 数据保护、V16 错误/日志语义 | auth/capability/health/freshness/completeness 独立；typed codes；single-source isolation；lastSuccess 保留；未知结果 fail closed | 八类 failure、另一来源不变、lastSuccess、三次上限及禁止第4次 tests 任一失败即 blocker |

本计划不声称完整 ASVS 合规。继承的 Critical T2-01 与 T2-03/T2-06 High 均没有“暂时放行”处置：任何相关测试、typecheck 或 import-boundary 失败都阻止本计划完成及所有依赖计划。若后续 adapter 暴露不满足正面标志/稳定身份的结果，状态机必须保持 blocked/human-needed，不允许调用方绕过。
</threat_model>

<verification>
1. RED：提交前先运行 `npm run test:unit -- --run tests/unit/auth-state.test.ts` 并确认因缺少状态机行为而失败；不得以 skip/todo 作为 RED。
2. GREEN：实现后运行 `npm run typecheck &amp;&amp; npm run test:unit -- --run tests/unit/auth-state.test.ts tests/unit/auth-contracts.test.ts tests/unit/import-boundaries.test.ts`。
3. REFACTOR：整理纯函数/exhaustive branches 后重复 GREEN 命令，再运行 `npm run test:unit -- --run` 做 Wave 2 单元回归。
4. 静态边界：读取 `packages/application/src/auth.ts` imports/effects，确认只有 domain/contracts/application type imports与四类安全 effects；验收 grep 必须零命中 driver/transport/secret/evidence/gate 词。
5. 所有输出属于 S；不运行 integration/native/UI/live，不登录、不打开 official origin、不创建 Profile、不写数据库或 receipt。自动测试成功不改变 Windows、Phase 1/2 或 Phase 3 状态。
</verification>

<success_criteria>
- D-02/D-08：正常流程先 Moodle 后 EdStem；两边均有效时纯状态可保持后台无窗口，任何交互/MFA 只产生 human-action requirement，不自动输入或绕过。
- D-06/D-07：严格证据只生成待核对 candidate，证据不足只允许一次人工确认；同名/邮箱不绑定，identity_mismatch 全局阻断课程访问且不自动切号/登出/覆盖。
- D-10：显式登出保持 unauthenticated/logoutIntent，直到用户明确请求登录；自然过期是独立 reauth_required，并保留上次成功资料。
- D-11：一个来源失败不污染另一个来源；仅 natural expiry/temporary network 具有三次有界恢复，Moodle reauth 后重新 probe EdStem而不复制认证状态。
- AUTH-01/AUTH-03/SEC-02 与 T2-03/T2-06 的表驱动 negative tests、typecheck、全 unit 回归全部通过；任何 High finding 仍存在则计划未完成。
- macOS-first 只改变后续执行顺序；此 S 计划不填 N/L、不改变 Windows `not_run / human_needed`、不完成 Phase 1/2、不解除 Phase 3 硬门禁。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-poc-live/02-04-SUMMARY.md`，记录 RED/GREEN/REFACTOR 的真实 S 测试、公开状态机接口和未执行的 integration/native/live 缺口；不得记录学校身份、official origin 实值、课程名、Profile/凭据/Cookie 或伪造 N/L 结果。
</output>
